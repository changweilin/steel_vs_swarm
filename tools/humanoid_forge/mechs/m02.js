// ============ m02 逐機零件檔(dev-only;獸型雙足 biped + tinyArms + 重尾)============
// ── m02「壓艙石」重型突擊機甲(trex 暴龍):水平體軸、巨顎藏無後座砲、重尾配重、退化短前臂 ──
// 2D 定案圖:public/assets/cyberpunk_art/mechs/m02_static.jpg(★ = m02_static)
// 幾何語彙一律取自 ../geo.js;MUST NOT 在本檔自建 BufferGeometry。
// 2026-08-12 照 ★ 定案圖逐部位重建:
//   頭 = prismF 側剖顱殼(稜面楔形)+ 逐顆齒列 + 鉤形前獠牙;顎鉸是真樞軸(mount 的
//   heavyPivot 驅動「張口 = 射擊姿勢」);口腔藏四管旋管砲束(輕/重同膛:lMuz 管束小環、
//   hMuz 充能大環)。頸 = 芯柱 + latheF 收分環 + 喉管纜束。
//   軀幹 = 斜置深胸艙 + 外凸肋甲環;背甲大蓋板覆在骨盆與尾根之上(★ 圖語彙)。
//   尾 = chainF 收分節鏈 + 背脊板。圖騰紅黃帶紋 / 鏽蝕補丁 = paint 層。
// 2026-08-12 v3 依暴龍正模骨架照調姿(★ 仍管機械外觀,骨架照管姿態比例):脊線壓平、頭前伸、
//   尾加長加粗、腿改趾行深屈(legBase −0.28 股前傾 + 膝 0.62 深屈 + 踝 −0.32 補償)。
// 2026-08-13 v4 使用者:「暴龍:身體再向前延伸長一點,補上兩隻前爪」——
//   軀幹沿體軸串成兩段(前端 z 1.15 → 1.75)、頭只前移 +0.15、補兩指前爪。
//
// ── 2026-08-13 v5 使用者手繪稿(側視):「暴龍頭應該要從身體前方延伸,脊椎是橫向的,
//    前爪在更前面的位置,脖子和尾巴都加長一點」──
// 手繪稿讀數:頭 → 頸 → 軀幹 → 尾 幾乎連成一條水平線,頭與尾尖落在同一個高度帶,骨盆是支點。
// 本輪的四條改動與**它們各自的自由度在哪裡**(這一段是本檔最容易被日後改壞的地方):
//
//  ⓪ **rig 的兩條硬約束**(逐項在 forge.js / locomotion.js 原文核對過,MUST NOT 憑記憶重建):
//    ㋐ `locomotion.js:482` 每幀寫 `rig.head.position.y = rig.headY0 + …` ——
//       **只寫 y**。⇒ 頭**節點**的執行期胸腔高度恆 = `neckAt[1] + headYl` = `neckAt[1] + 0.90`,
//       forge 為 neckAt 做的 −neckAt[1] 補償整個被蓋掉。反過來說 **`neckAt[1]` 就是頭高的旋鈕**
//       (降 0.48 → 頭節點降 0.48),而它**不動 z**:forge 給的 `head.position.z = 0.04 − neckAt[2]`
//       加上頸樞軸自己的 neckAt[2] ⇒ 頭節點胸腔 z **恆為 0.04**,與 neckAt 無關。
//    ㋑ 頭幾何因此只能靠靜態中介 Group `hh` 往前送,而 `hh.z` **就是凝視/roar 的槓桿臂**
//       (頭節點在 z 0.04)。v4 檔頭那條「槓桿臂 MUST NOT 等量長大」仍然成立:本輪 hh.z
//       1.50 → 2.20(+47%),而 roar 的 −0.90 rad(head −0.55 + neck −0.35)會把顱殼抬起
//       ≈1.5(v4 是 ≈1.00)⇒ **上頸套筒改掛在 hh 上**(隨頭走,不隨頸走),張口仰頭時
//       頸柱與顱殼之間不再整段裂開。再往前送 MUST 先看 cast 那一張。
//
//  ① **脊椎橫向化 = 把「頸根 → 骨盆」這條線壓平**,而它 MUST 靠幾何做(prop/pose/gait 是
//     對 locomotion 校準的,一格不准動)。舊制 collar(頸根)在胸腔 (0, 0.95, 0.95)、尾根在
//     (0, 0.42, −1.00) ⇒ 這條線 **−15°**;新制頸根 (0, 0.60, 1.02)、尾根 (0, 0.55, −1.00)
//     ⇒ **−1.4°**,近水平。做法是三件事一起:㋐頭下修 0.60(neckAt[1] 0.86 → 0.38 給 0.48、
//     hh.y −0.30 → −0.52 給 0.22)㋑尾根抬 0.42 → 0.55 且尾幾乎不再下垂 ㋒喉艙前端面下修
//     (h 0.95 → 0.70)讓頸自**軀幹前緣**長出來而不是自頂面直上(手繪稿 ㋑)。
//  ② **頸加長**:可用的自由度只有「軀幹前緣往回收」+「頭往前送」兩邊夾出來的那一段 ——
//     軀幹前端 z 1.75 → 1.49(前端面上緣 1.44)、顱殼後緣 z 1.28 → 1.98 ⇒ 露出來的頸
//     0(貼著)→ **0.54 淨空 + 0.98 長的芯柱四環**,側視讀得出是一段有長度的頸。
//     軀幹全長只從 2.13 收到 1.88(−12%):v4 的「身體向前延伸」用**整體剪影**保住
//     (吻端 z 3.04 → 3.74),而不是靠喉艙那一截。
//  ③ **前爪再往前**:肩/肘/腕三個樞軸仍是 forge 凍結的 (±0.6, 0.42, 0) / (±0.6, −0.18, 0) /
//     (±0.6, −0.64, 0.282),前置**一律逐點反算**(v4 就是拿錯樞軸讓爪縮回前臂裡):
//     視覺肘 z 0.62 → 0.76(上臂 sz −0.38 → −0.48)、視覺腕 (0,−0.55,1.00) → (0,−0.58,1.15)
//     (前臂在肘局部框重解 h 0.545 / sz −0.123)、手掌基準 B = R(+0.70)(視覺腕 − 腕樞軸)
//     = (0, −0.513, 0.703)(v4 是 (0,−0.394,0.607))⇒ **爪尖胸腔 z 1.42 → 1.57**,
//     落到軀幹前端面(z 1.44~1.54)**之下**,與新頸根同一個 z 帶(手繪稿 ㋓)。
//  ④ **尾加長**:節數契約 `n = 11`(`rig.tailSegs` 的長度)一格不准動 ⇒ 只加節長帶
//     (len0 0.66 → 0.70、len1 0.26 → 0.30 ⇒ 全長 5.06 → 5.50 ≈ 軀幹+頸+頭 4.74)。
//     **下垂整組壓平**:tailRoot.rotation.x −0.12 → −0.02、rot0 −0.03 → −0.012、
//     rotD 0.006 → 0.0024(累積角 Σ 仍收斂回 0)⇒ 尾尖胸腔 y −0.51 → **+0.29**,
//     與顱殼(y 0.58~1.15)落在同一個高度帶。**MUST 記得 whipTail 逐幀覆寫節樞軸**,
//     基礎姿勢只能住 tailRoot 這個中介 Group 與 chainF 的 rot0/rotD。
//  ⑤ 側視實測跨距(Box3 於執行期量,見回傳):吻端 z 3.77 → 尾錐 z −6.86 = **水平 10.63**;
//     最高點(背甲頂蓋 world 4.50)→ 最低點(足爪 world −0.21)= **垂直 4.71**
//     ⇒ 橫向 : 縱向 = **2.26**(手繪稿要的「橫向遠大於縱向」;v4 是 9.4 : 4.7 = 2.0,
//     而那 9.4 有一大截是「頭高高在上」撐出來的**垂直**距離,不是橫向)。
//
// ── 2026-08-14 v6 使用者:「現在前腳和後腳都黏在一起,應該要分別在身體的前端和後端,
//    後腳加裝蹠骨」──
// 病灶(b_m02_side45 實測):肩樞軸與髖樞軸**都在胸腔 z = 0**(forge 凍結),而 v5 的前爪只
// 推到 z 1.57、股骨幾何又以 z +0.08 為中心往前傾 ⇒ 兩叢量體在 z 0.3~1.0 / y 2.2~3.1 完全重疊,
// 側視讀成「四肢黏在一起」。樞軸不能動 ⇒ **分離只能由幾何的反向位移堆出來**,三個槓桿一起用:
//  ㋐ **軀幹整段前移 +0.30**(肋腔/喉艙/肋環/胸甲/龍骨/領環/背甲前緣 + 頸 + 頭 + 顎 + 砲束同量,
//     頸的那一份沿頸軸走 (0, +0.06, +0.30) 而不是純 z ——純 z 位移會讓四個頸環偏離芯柱軸
//     0.061 = 環整圈掉到芯柱下緣)⇒ 髖樞軸 z 0 自此落在**軀幹後段**,而不是正中央。
//  ㋑ **整條前肢連肩球一起前送 +0.71**(逐點反算,方法同 ③;第一輪只推肘/腕 +0.24,真側視
//     實測仍讀成「臂自肚子中段長出來」—— 肩球還留在樞軸 z 0.26):肩球 0.26 → 0.60、
//     視覺肘 0.76 → 1.34、視覺腕 1.15 → 1.86、手掌基準 B = R(+0.70)(視覺腕 − 腕樞軸)
//     = (0, −0.985, 1.233)、**爪尖 z 1.57 → 2.28**(喉艙前端面 1.79~1.85 之外、頜下)。
//     肩球的**上界**是肋腔桶本身(world z −0.12~1.06、半寬 0.58 ≈ 肩 x 0.6):再往前就離開
//     肋腔、變成浮在喉艙旁邊的一顆球。
//  ㋒ **股骨視覺根部後退 0.45**(頂/髖端 z −0.39、底/膝端 z +0.06 ⇒ sz −0.45)+ 新增**臀肌罩**
//     (prismF 側剖卵形,★ 圖髖後那塊大圓弧量體)⇒ 後腿量體整組退到骨盆之下。
//     順手修掉一個舊 bug:tboxF 的 **w0/d0 是底面(= 肢體遠端)**、w1/d1 是頂面(近端),
//     v3~v5 的股/脛/上臂/前臂四處都用反了(股 0.62→0.36 讀成「髖細膝粗」,正面基準圖看得到
//     腿是往下張開的)。四處一律改成近端粗、遠端細。
//  ㋓ **蹠骨**:biped 鷹架的腿鏈**恰三節**(股/脛/足)寫死在 forge.mkLeg,**不准加第四個樞軸**
//     ⇒ 蹠骨住 shin() 的幾何:脛骨只畫到段長的一部分(v6 是 61.3%,v7 依骨架照改成 65.5%)、其下一個真的跗關節
//     (latheF 盤 + 橫軸),再接一根明顯更細的蹠骨幹到踝樞軸(y −1.50);兩段剪切**方向相反**
//     (脛末往後 z −0.20、蹠末回到 z +0.02)⇒ 側視是「脛後傾 → 跗突出在後 → 蹠前傾落到趾根」
//     的鳥腳折線。foot() 那塊 0.55×0.54 的長足底板是**蹠行**的讀法 ⇒ 收成只蓋趾根的蹠端墊,
//     **趾節與爪的 y/z 一格未動**(趾尖高度 = 接地面,改了就會陷地或浮空)。
//  ㋔ **驗收實測**(執行期 Box3,逐肢鏈量;45° 斜視會把兩叢投影在一起 ⇒ 這一項 MUST 拿
//     **真側視**或這組數字判,不能只看 side45):
//       前肢 world z **0.38 ~ 2.28**(y 2.09~3.64) / 後腿 world z **−0.85 ~ 0.94**(y −0.20~3.28)。
//     後腿只有在**膝以下**(y ≤ 1.6)才碰得到 z 0.9,而前肢最低只到 y 2.09 ⇒ 兩叢在任何
//     一條等高線上都不重疊;前肢在 y 2.5 的 z 帶是 1.7 上下、後腿是 −0.42~0.33 ⇒ **空隙 1.3**。
//     足爪最低 y −0.19/−0.21 = 與 v5 逐位元同高(驗收 iii:趾尖仍踩在地面)。
//     全身跨距 吻端 z 4.07 → 尾錐 −6.86 = 水平 10.93、垂直 4.70 ⇒ 橫向:縱向 = 2.33。
//
// ── 2026-08-14 v7 使用者定案:「**體型骨架參考搜索照片,原始生成圖提供特徵為主**」──
// 一句話:**骨頭照照片,皮照原圖**。骨架權威 = 鄭州自然博物館 T. rex 裝架側視全身照;
// ★ 圖仍管裝甲分片 / 發光 / 武器造型 / 塗裝分色,那些一格未動。
//
// ⓐ **量法與骨架照讀數**(2026-08-14 v8 **整組重量過**;v7 那一組前肢座標整體左移 50~65px、
//    記的「爪尖 (283,738)」落在背景的橘色牆上 —— 舊值已刪除,不與新值並列)。
//    作法:原圖 1280×1920 疊 100px 網格(scratchpad/grid.mjs)+ 逐部位放大裁切(crop.mjs,
//    參數 x0 y0 x1 y1 scale name),逐骨端讀像素。**本輪實際重讀的裁切**:
//      前肢 crop 250 580 620 900 ×4 與 330 700 500 810 ×9、390 690 530 810 ×10
//      後肢 crop 560 1030 820 1780 ×2、600 1040 800 1360 ×6、590 1520 780 1760 ×6
//      頭頸 crop 120 160 720 660 ×2
//    讀數(px,±值 = 我自己重讀時的擺動):
//      吻端(前上頜)(187, 198)±6   下頜前端 (157, 310)±6   枕髁/顱後 (500, 385)±8
//      頸根(第一背椎)(600, 590)±15  肩臼/肱骨頭 (447, 760)±6  肘 (426, 780)±6
//      腕 (402, 750)±6              爪尖 (352, 757)±4
//      髖臼 (700, 1140)±25          膝 (682, 1317)±8          踝 (657, 1573)±6
//      蹠骨遠端 (655, ≥1675) —— **末端被玻璃欄杆擋住,只量得到下界**
//    ⇒ 顱 365 / 頸弦 228(沿弧 ≈235)/ 肱 29 / 橈 39 / 掌+爪 50 / 股 178 / 脛 243 / 蹠 ≥102。
//
//    ⚠ **這張照片能不能拿來定比值,逐項不同 —— 這一段就是不要再被騙的地方**:
//      ㋐ **前肢的骨長不可用**:肱骨在這個機位幾乎正對鏡頭(量到 29px < 橈骨 39px),而暴龍的
//         肱骨本來就長於橈骨 ⇒ 它被壓縮掉了。照片只證明「整條前肢很小」。
//      ㋑ **股骨不可用**:量到 股:脛 = 1 : 1.37,而暴龍成體是 股 > 脛(股 128cm / 脛 114cm)
//         ⇒ 股骨同樣朝鏡頭傾。相機在展台下方仰拍,越高的骨頭壓縮越多。
//      ㋒ **可用的是 脛 與 脛:蹠 的下界**(蹠/脛 ≥ 0.42)、**顱:頸:軀幹**(同一景深帶)、
//         以及「髖是體軸最高的支點 / 體軸近水平」這兩條姿態事實。
//
// ⓑ **比值表(以股骨 = 1)** ← 驗收標的。可用的取照片,不可用的取骨學值並註明:
//      股 : 脛 : 蹠      = 1 : 0.95 : 0.50    骨學值(照片只給 蹠/脛 ≥ 0.42,與之相容)
//      (肱 + 橈) ÷ 股    = 0.43               骨學值(肱 ≈ 0.29 股、橈 ≈ 0.15 股);照片見 ⓐ㋐
//      肩臼 → 髖臼 ÷ 股  = 1.60               照片 (447,760)→(700,1140) = 458px ÷ 股(骨學修正
//                                             後的 ≈286px)= 1.60
//      顱 : 頸 : 軀幹    = 1 : 0.65 : 1.55    照片 365 : 235 : 559 = 1 : 0.64 : 1.53(軀幹沿視線
//                                             遠離 ⇒ 真值只會更大,取 1.55)
//      髖臼是全身最高的支點;體軸(枕髁→髖臼)近水平 —— v5 做到,v7/v8 未動。
//      尾:**照片裡看不到**(裝架尾部在畫面外)⇒ 不動尾(n = 11 亦是 rig 契約)。
//
// ⓒ **rig 凍結的邊界(v8 修正 v7 的誤判)**:
//    ㋐ ⚠ **v7 寫的「三段的絕對比值註定對不上」是錯的,已刪除**。forge.js 凍結的是**樞軸**
//       (股段 1.56 / 脛段 1.50),而 **draw 回呼可以畫比段長短的骨頭** —— 本檔對肱骨/橈骨
//       早就用了這一招。v8 把同一招套到股骨:**股殼只畫 0.98**(膝端不動、頂端往下收 0.58),
//       ⇒ 股 : 脛 : 蹠 = 0.98 : 0.983 : 0.517 = **1 : 1.00 : 0.53**,落在骨架照帶內。
//       代價是股骨頂與髖樞軸之間空出 0.58,由**臀肌罩**蓋住(它本來就長在那裡,v8 再加大一圈)。
//       連帶:**比值的分母換了** ⇒ 前肢兩節同量收(肱 0.40 → 0.25、橈 0.27 → 0.17),
//       否則 (肱+橈)/股 會從 0.43 跳到 0.68。**本檔一切比值一律以「畫出來的骨殼」為準**,
//       不以樞軸段長為準 —— 兩把尺混用正是 v7 那張表自相矛盾的原因。
//    ㋑ **肩樞軸與髖樞軸都在胸腔 z = 0**(forge.mkArm / mkLeg)⇒ 肩→髖只能靠幾何反向位移堆,
//       兩端各有硬上界:肩球 MUST 半埋在喉艙裡(再往前就是浮在體側的一顆球)、股骨頂 MUST
//       收在骨盆塊之下。v8 實得 **1.53 × 股**(v7 是 1.22)—— 分母變小賺到的,幾何本身
//       只再前推了 0(肩球仍 z 1.26)。
//    ㋒ **roar 的裂口是幾何無法「對齊」只能「橋接」的**:頭節點在胸腔 (0, 1.272, 0.04),
//       而頸與軀幹的交界在 z ≈ 1.5 ⇒ 交界離頭樞軸 1.77。roar 讓 head.rotation.x 走到 −0.56,
//       任何掛在頭上的零件在該處都會相對頸滑動 2 × 1.77 × sin(0.28) ≈ **0.98**。
//       ⇒ 「把套筒加長」「把頭往回收」都補不完(v8 實測 hh.z 收 0.50 只換到 8%),
//       唯一有效的是 **v8 ⓐ 的喉套:一片掛在頭上、靜姿時藏在芯柱與腹龍骨裡的腹側頸甲**。
//
// ⓓ **「前後肢分不分得開」MUST 用頂點級逐高度帶量,MUST NOT 用 AABB**:斜向股骨的單一網格
//    AABB 會把整根骨頭的 z 跨距灌進它經過的**每一條**高度帶,於是「臂的 z 帶」與「腿的 z 帶」
//    永遠相交,結論與眼睛看到的相反。正確作法 = 逐頂點轉世界座標、以 0.25 為寬分 y 帶、
//    逐帶取 z 的 min/max 再相減(腳本 scratchpad/fx_probe.mjs)。
//
// ── 2026-08-14 v8:修第二輪稽核指名的四個缺陷(不再創作,已通過的六項逐項回歸)──
//  ⓐ **D1 roar 顱殼與頸柱分離**(最嚴重)。病因見 ⓒ㋒。修法三件一起:
//     ①新增**喉套**(head 的 hh 上一片 prismF 腹側頸甲,自顎鉸後方往後下伸到 hh (−1.16, −0.80));
//     ②上頸套筒半徑 0.27/0.31 → **0.30/0.375**(舊值比該處芯柱的 0.32 還細 = 一根插銷);
//     ③喉管纜束自 neck() 搬到 hh(留在頸側的話它自己會在 roar 時裂開)。
//     hh.z **維持 2.50**(收它只換到 8%,而 D1 同時要求「頭位不得明顯後退」)。
//     驗收 = scratchpad/fx_cast.mjs 逐幀量「頭側幾何到芯柱表面的最短距離」:
//     roar 全程(head.rot.x 0 → −0.562 → −0.50)**恆 ≤ −0.20**(負 = 重疊 = 無裂口),
//     全程最大值 −0.201。⚠ 這是**全動畫**的保證,不只是 shot 那一幀(step 18)。
//  ⓑ **D2 後肢三段比例**:見 ⓒ㋐。股殼畫 0.98(sz −0.52)。
//  ⓒ **D3 頸太短**:外露頸只能由「軀幹前緣往回收」買 —— 收分喉艙 h 0.70 → **0.52**、
//     肋腔桶 h 1.48 → **1.66**(軀幹總長 2.18 一格不動)⇒ 前端面 z 1.789 → **1.587**,
//     外露頸 0.56 → **0.75**(顱:頸 由 1:0.32 → **1:0.43**;骨架照 1:0.65 ⇒ **仍未達標**)。
//     ⚠ **卡住的是肩球**:再收 0.15 喉艙,肩球(z 1.26、r 0.20)就會凸出前端面 = ⓒ㋑ 的那顆
//     浮球。要再長只剩「把肩樞軸往後挪」或「縮小顱殼」,兩者都要使用者裁決(前者動 rig 觀感、
//     後者削 ★ 圖招牌),**MUST NOT 自行決定**。
//  ⓓ **D4 檔頭錯座標**:ⓐ 整組重量,舊座標已刪除。
//  ⓔ **D5 驗收表漏頸**:ⓕ 補上頸那一列。
//
// ⓕ **v8 實得比值**(執行期實測 scratchpad/fx_ratio.mjs + fx_probe.mjs;骨架照值在括號):
//      股 : 脛 : 蹠(骨殼軸長)= **1 : 1.003 : 0.528**  (1 : 0.95 : 0.50)—— **命中**
//        同一組含剪切的真長 = 1 : 0.912 : 0.514(脛低 4%,在照片本身的誤差內)
//      (肱 + 橈) ÷ 股        = **0.428**            (0.43)—— 命中(肱 0.249 / 橈 0.170)
//      肩球 → 股骨頂 ÷ 股    = **1.53**             (1.60)—— v7 是 1.22
//      顱 : 頸 : 軀幹        = **1 : 0.43 : 1.24**  (1 : 0.65 : 1.55)—— 頸見 v8 ⓒ、軀幹見 ⓓ
//        (顱 1.762 / 頸 0.754 / 軀幹 2.187 世界單位)
//      髖是體軸最高的支點 / 體軸近水平            —— v5 做到,v8 未動
//      前肢 world z 1.04~1.95(y 3.15~3.64)、後腿 world z −0.92~0.91(y 0.00~3.13)
//        ⇒ 兩叢**只在 y 3.00 那一帶共存**,該帶間隙 **1.35**;足爪最低 y +0.001 / −0.033
//      網格 **226**(≤250)
import * as THREE from 'three';
import {
  matF, dimF, bxF, cylF, sphF, coneF, torusF, tboxF, prismF, latheF, finF, fanF, chainF, cablesF,
  hydCyl, sinew, seg2, IRON, GUNMETAL, COAL, INK, BONE, BRASS,
} from '../geo.js';

// 本機專用色:齒列亮灰 / 口腔暗紅 / 爪鋼 / 活塞亮桿
const TEETH = 0xcfd4d9, MAWRED = 0x6e1a1a, CLAWSTEEL = 0xb9c0c8, PISTON = 0xd8dde2;
const EYERED = 0xff2a18;      // 眼的紅(**不吃 accent**:那是塗裝色相,換色時眼睛不該跟著變)

// 頸軸(胸腔框):自 (0, 0.60, 1.02) 至 (0, 0.80, 1.98);長 0.98、傾角 1.3654 rad。
// 頸樞軸 neckAt = (0, 0.38, 0.90) ⇒ 頸幾何(neckG)與上頸套筒(掛 hh)共用這一個角度,
// 少了共用就是「頸柱與套筒各指一個方向」,而靜態三視圖看起來只是「接縫怪怪的」。
const NECK_ROT = 1.3654;

// ── 2026-08-14 使用者:「後腳的骨盆關節調整到再往身體兩側、跟前腳肩關節同高」+
//    追加定案:「**只動 thigh() 裡的髖盤落點,軀幹一格不動**」──────────────────────
// forge 的樞軸(髖 hips×H = 3.0 / 肩 shoulderY×H = 3.42)一格未動 ⇒ prop / pose / gait /
// 骨盆 / 軀幹 / 腿長 / 接地全部逐位元同舊制。改的只有**看得見的那個髖關節**:
// ★ 圖裡「髖」讀的是臀肌罩外側那一枚大圓盤,不是 forge 的樞軸點。
//   ① 盤心抬到**肩樞軸的世界高度**(差值 DY 由 PROP 推導,MUST NOT 手寫 0.42);
//   ② 盤心外撇到臀肌罩的新外緣;
//   ③ 臀肌罩(髖的殼)同步往上、往外各長一圈把盤接住 —— 只搬盤不長罩 = 一枚浮在腰側的環。
// ⚠ 盤畫在**已經前傾 legBase 的股節框**裡 ⇒ 目標是世界偏移,MUST 先轉回股節框
//   (下方 Rx(−legBase) 的反解);直接把世界值寫進 local 會讓盤沿體軸滑掉 0.15。
const PROP = { hips: 0.5, legSplay: 0.1, thigh: 0.52, shin: 0.5, shoulderY: 0.57, shoulderX: 0.1, upperArm: 0.1, foreArm: 0.09, head: 0.65, girth: 1.1 };
const LEG_BASE = -0.34;       // = gait.legBase(股前傾;髖盤的落點在這個旋轉之後量)

export default {
  label: '壓艙石(m02 機甲・暴龍)', hue: 0x9aa3ad, height: 6.0,
  prop: PROP,
  gait: { strideF: 1.25, bob: 0.12, sway: 0.08, top: 7, legBase: LEG_BASE, armBase: 0 },   // legBase 股前傾(趾行)
  pose: {
    knee: { base: 0.72, k: 0.62, d: 0.15 }, ankle: { base: -0.36, k: -0.34, d: 0.55 },  // 膝深屈+踝補償(足底放平)
    elbow: { base: -0.55, k: -0.3, d: 0.3 }, wrist: { base: -0.15, k: 0.12, d: 0.5 },   // 短前臂深屈在胸前(暴龍招牌)
  },
  neckAt: [0, 0.38, 0.90],                                      // 頸樞軸;[1] = 頭高旋鈕(檔頭 ⓪㋐),[2] 不影響頭節點 z
  moveSig: { poise: 0.70, idleF: 0.48, idleA: 1.62, launch: 0.14, spool: 0.85, brake: 0.20, settle: 1.90 },
  castSig: { omni: 'roar', dir: 'jab' },
  doc: [
    ['頭(巨顎藏無後座砲)', 'prismF 側剖稜面顱殼+頰甲 ×2+怒眉稜+逐顆齒列(上 10 下 8)+鉤形前獠牙 ×2+latheF 文氏尾噴(壓在顱頂後段的頂邊上)'],
    ['顎鉸+口腔砲', '下顎 prismF 側剖+軸桿+鉸盤 ×2;四管旋管砲束(cylF ×4+芯)+lMuz 管束環+hMuz 充能環;heavyPivot 充能張顎 = 射擊姿勢'],
    ['頸(水平長頸・兩段)', 'cylF 芯柱 0.98(近水平)+latheF 收分環 ×4 沿頸軸(半徑吃芯柱在該處的收分值)+頸背鱗板 ×2(貼芯柱表面);外露頸 **0.75**(v8 由軀幹前緣回收買來,顱:頸 = 1:0.43,骨架照 1:0.65)'],
    ['喉套+上頸套筒(roar 不裂開)', '**兩件都掛在頭的靜態 Group** ⇒ 隨顱殼走:prismF 腹側喉套(靜姿藏在芯柱與腹龍骨內,roar 抬頭時掃下來把頭頸之間填滿)+cylF 套筒(r 0.30/0.375,真的套在芯柱外)+cablesF 喉管 ×3(壓在喉套腹緣下 0.10 ⇒ 仍看得見)。逐幀實測:roar 全程頭側幾何恆與芯柱重疊 ≥0.20'],
    ['軀幹(橫向脊線・後端加長)', 'tboxF 肋腔桶(v8 h 1.66)+tboxF 收分喉艙(v8 h 0.52,sz 0.18 前端面下推 ⇒ 頸自前緣長出)沿同一體軸串成 **2.18** 長的水平艙體(z −0.60~1.59)⇒ 顱:軀幹 1:1.24(骨架照 1:1.55)、髖樞軸落在艙體後段+latheF 外凸肋甲環 ×4(逐段中心線)+prismF 胸甲 ×2+頸根領環+腹龍骨+cablesF 腹管+側百葉'],
    ['背甲(水平脊背蓋板)', 'prismF 側剖蓋板放平(肩後覆至尾根 = 脊背線,髖最高向肩微下斜)+頂板+INK 百葉 ×4+側鉸座 ×2+後支柱+後排氣 ×2'],
    ['退化短前臂 ×2(骨架照比例)', '球肩埋在喉艙最前端 z 1.26(骨架照:肩臼在肋廓前端、頸根下)+肩甲+繞 x 轉 −1.38 的短肱骨殼(v8 只 **0.25**)+短橈骨(**0.17**)⇒ (肱+橈)÷股 = **0.428**(骨架照 0.43)+hydCyl 活塞 ×2+latheF 肘盤/腕帽+**兩指**鉤爪(指節 ×2+finF 弧爪)—— v8 只收兩節骨殼,爪/活塞/盤件一件未縮'],
    ['強健後腿 ×2(身體後端・趾行)', '巨股楔殼**只畫 0.98 而非 1.56 的樞軸段長**(髖粗膝細+沿體軸後剪 0.52)⇒ 股:脛:蹠 = **1 : 1.00 : 0.53**(骨架照 1 : 0.95 : 0.50)、肩→髖 = 1.53 × 股+prismF 臀肌罩(v8 加大一圈蓋住股骨頂與髖樞軸之間空出的 0.58;2026-08-14 再往上 0.36、往外 0.24 把抬到肩高的髖盤接住)+prismF 膝前甲板+latheF **髖盤(盤心 = 肩樞軸的世界高、貼罩子新外緣;兩軸皆推導)**/膝盤+hydCyl 腱活塞 ×2'],
    ['小腿 = 脛骨 + 跗關節 + 蹠骨', '脛骨楔殼(膝粗跗細,**佔段長 65.5% = 骨架照的 脛:蹠 = 0.95:0.50**,末端後剪 0.24)+latheF 跗關節盤(deep)+橫軸+**明顯較細的蹠骨幹**(斷面約脛骨膝端一半、色階 dark 與跗盤分開一階,前剪 0.24 回到踝樞軸)+脛前甲+腱活塞+踝環 —— 三節腿鏈不加樞軸,折線由剪切表達'],
    ['足 ×2(只剩趾)', '蹠端墊(收成只蓋趾根)+踝前斜甲+三趾(tboxF 指節+finF 弧爪)+後距爪 —— 爪一趾一件;v7 全組抬 0.15 讓趾尖落在 y ≈ 0'],
    ['重尾(chainF 十一節配重)', '十一節收分節鏈(全長 5.50 ≈ 頭頸軀幹合計;尾根抬到脊線高 y 0.55、下垂壓到 −1.1° ⇒ 尾尖與顱殼同高度帶)+節環+背脊板 ×7(索引遞減)+coneF 尾梢錐'],
  ],
  head(c, h) {
    const { PAL, accent } = c;
    // ── 2026-08-14 使用者:「暴龍**頭部放大 50%**」──────────────────────────────────
    // 樞軸鏈是 forge 凍結的 ⇒ 只能放大**幾何**。而放大 MUST **繞著接頸處**做:
    //   直接對頭節點 setScalar(1.5) 是繞節點原點縮放,而顱殼中心距節點 2.5 ⇒ 整顆頭往前
    //   飛 1.25、往下 0.26,頸當場裂開一個看得見的洞(而每一條既有斷言都正常)。
    // ⇒ 用「錨點 sc + 反位移 g」兩層:子件座標 p 的像 = ANCH + F·(p − ANCH) ⇒ 錨點恰為不動點。
    //   ANCH 取上頸套筒的位置(= 顱殼後緣與頸芯柱交會處)。
    // 顎與砲束在 mount() 才建 ⇒ 它們 MUST 掛同一個 g(c.headG),否則頭放大了而顎沒有。
    const HEAD_F = 1.5, ANCH = [0, -0.49, 2.10];
    const sc = new THREE.Group();
    sc.position.set(...ANCH);
    sc.scale.setScalar(HEAD_F);
    h.add(sc);
    const g = new THREE.Group();
    g.position.set(-ANCH[0], -ANCH[1], -ANCH[2]);
    sc.add(g);
    c.headG = g;                       // mount() 的顎/砲同框(檔頭:兩處 F.head.add 改吃它)
    h = g;                             // 以下逐行沿用舊座標 —— 放大是外面那一層的事
    // 靜態中介 Group:頭幾何整組前移下修(檔頭 ⓪㋑ —— 頭**節點**的執行期胸腔位置恆為
    // (0, neckAt[1] + 0.90, 0.04) = (0, 1.28, 0.04),樞軸不動,只推幾何)。
    // v5:z 1.50 → 2.20(顱殼 z 1.98~3.74,自軀幹前緣 1.44 前伸出 0.54 的淨空 = 頸的位置;
    //     d1 那一輪只推到 2.05 ⇒ 淨空 0.29,側視特寫實測「顱殼後緣直接壓在肋環上」)、
    //     y −0.30 → −0.52(顱殼 y 0.58~1.15,落在尾尖 0.29 與背甲線 1.38 之間 = 手繪稿的水平線)。
    // v6:z 2.20 → **2.50** —— 軀幹整段前移 +0.30(檔頭 v6 ㋐)之後,頸的 0.54 淨空由頭同量
    //     跟進來保住;不跟的話喉艙前端面 1.79 直接頂到顱後 1.98,頸只剩 0.19 = 又變回沒有頸。
    // jawG / tg 吃同一組偏移(−0.70 / −0.86 系,見 mount)。
    const hh = new THREE.Group();
    hh.position.set(0, -0.52, 2.50);
    h.add(hh);
    // 上頸套筒(**掛 hh = 隨頭走**)。v8 半徑放大到 0.30/0.375 ⇒ 真的「套」在芯柱外面而不是
    // 塞進去(舊值 0.27/0.31 比該處芯柱的 0.32 還細 = 一根插銷,側視讀不出兩截的先後)。
    const sleeve = cylF(hh, 0.30, 0.375, 0.36, 10, 0, 0.03, -0.40, PAL.dark, { metalness: 0.65 });
    sleeve.rotation.x = NECK_ROT;
    // ── 喉套(v8 修 D1;檔頭 v8 ⓐ)── 掛 hh = 隨頭走的**腹側頸甲**,自顎鉸後方往後下方伸,
    // 靜姿時整段藏在芯柱與腹龍骨之內(量測見檔頭),roar 抬頭時隨顱殼掃下來把裂口填滿。
    // 剖面 (x = hh z, y = hh y) 逆時針:後下 → 前下 → 前上 → 後上;rotation.y = −π/2 讓 x → +z。
    // ⚠ 後下角 (−1.16, −0.80) 是這一族唯一的旋鈕:太短 → roar 仍見裂口、太長 → 靜姿捅出腹部。
    const gular = prismF(hh, [
      [-1.16, -0.80], [-0.60, -0.74], [-0.10, -0.50], [-0.10, 0.06], [-0.74, -0.14], [-1.30, -0.40],
    ], 0.46, 0, 0, 0, PAL.dark, { metalness: 0.6 });
    gular.rotation.y = -Math.PI / 2;
    // 喉管纜束 ×3(v8 自 neck() 搬到 hh):喉套是頭側件,纜束留在頸側就會在 roar 時自己裂開。
    // 落點壓在喉套腹緣**之下** 0.10 ⇒ 仍看得見(它是這一段的最底層)。
    cablesF(hh, { p0: [0, -0.90, -1.05], p1: [0, -0.60, -0.14], k: 3, r: 0.035, sag: 0.03, spread: 0.10 }, GUNMETAL, { metalness: 0.75 });
    // 側剖稜面顱殼(prismF 剖面 = (前,上) 平面,rotation.y = −π/2 讓剖面 x → +z 前伸)——
    // 水平體軸機種:頭幾何自樞軸往前懸伸(★ 圖:楔形吻部、眉峰隆起、後顱收圓)
    const skull = prismF(hh, [
      [-0.22, -0.02], [0.33, -0.15], [1.19, -0.18], [1.47, -0.11], [1.50, 0.04],
      [1.21, 0.18], [0.57, 0.33], [0.26, 0.39], [-0.11, 0.33], [-0.26, 0.13],
    ], 0.76, 0, 0, 0, PAL.main, { metalness: 0.55 });
    skull.rotation.y = -Math.PI / 2;
    // 頰甲板 ×2(prismF 六角側貼;★ 圖眼下層疊頰甲)
    for (const sx of [-1, 1]) {
      const cheek = prismF(hh, [
        [0.06, -0.13], [0.44, -0.18], [0.68, -0.02], [0.61, 0.15], [0.22, 0.22], [0.00, 0.09],
      ], 0.10, sx * 0.435, 0, 0, PAL.mid, { metalness: 0.6 });
      cheek.rotation.y = -Math.PI / 2;
      // 怒眉稜(內端下壓的斜楔 = ★ 圖的兇相)
      const brow = tboxF(hh, { w0: 0.22, d0: 0.22, w1: 0.16, d1: 0.14, h: 0.10, sz: 0.02 }, sx * 0.28, 0.35, 0.46, PAL.deep, { metalness: 0.6 });
      brow.rotation.z = -sx * 0.28;
      brow.rotation.x = -0.08;
      // ── 眼(2026-08-14 使用者:「**眼睛畫出來,紅光閃爍**」)──
      // 舊制只有一片 0.055 厚的 accent 薄板嵌在頰甲上 —— 那是條**縫**不是眼:accent 是機體
      // 色相(這台是灰藍),而且沒有窩、沒有球、沒有瞳,側視特寫只看得到一道亮邊。
      // 三件一組:暗眼窩(旋成環,盤面法線朝側)+ 紅光眼球 + 亮瞳;紅色 MUST 是**自己的常數**,
      // MUST NOT 沿用 accent(換塗裝色的時候眼睛會跟著變成藍的)。
      const ex = sx * 0.40, ey = 0.16, ez = 0.45;
      latheF(hh, [[0.03, -0.05], [0.13, -0.04], [0.16, 0.015], [0.10, 0.055]], 10, ex, ey, ez, PAL.deep, { metalness: 0.8 })
        .rotation.z = -sx * Math.PI / 2;                                   // 眼窩:開口朝體側
      const eye = sphF(hh, 0.105, ex + sx * 0.035, ey, ez, EYERED, { emissive: EYERED, emissiveIntensity: 1.8 });
      eye.scale.set(0.62, 1.0, 1.15);                                      // 壓成側向的橢球 = 貼在頰面上
      const pup = sphF(hh, 0.048, ex + sx * 0.075, ey, ez + 0.015, 0xffe2d2, { emissive: EYERED, emissiveIntensity: 2.6 });
      // 閃爍名冊(locomotion `rig.blink`;左右**反相**⇒ 不是兩顆一起呼吸)
      (c.eyes || (c.eyes = [])).push({ mesh: eye, f: 3.6, ph: sx > 0 ? 0 : Math.PI },
        { mesh: pup, f: 3.6, ph: sx > 0 ? 0 : Math.PI, lo: 0.3 });
      // 鼻孔排氣(吻頂小孔 = 引擎排氣的生物機械雙關)
      cylF(hh, 0.03, 0.03, 0.05, 6, sx * 0.12, 0.24, 1.34, INK, { metalness: 0.7 }).rotation.x = 0.4;
      // 鉤形前獠牙(sz 剪切:根前傾、尖端下勾 —— ★ 圖吻端鉤喙)
      tboxF(hh, { w0: 0.035, d0: 0.042, w1: 0.10, d1: 0.11, h: 0.29, sz: 0.065 }, sx * 0.175, -0.29, 1.36, TEETH, { metalness: 0.35 });
    }
    // 上齒列(一齒一件:根寬在上、尖端在下,前大後小索引遞變)
    for (let i = 0; i < 5; i++) {
      const th = 0.22 - i * 0.022;
      for (const sx of [-1, 1])
        tboxF(hh, { w0: 0.028, d0: 0.033, w1: 0.095, d1: 0.11, h: th }, sx * 0.315, -0.165 - th / 2, 1.15 - i * 0.165, TEETH, { metalness: 0.35 });
    }
    // 口腔頂板(暗紅;張口時從顎縫看得到)
    bxF(hh, 0.50, 0.05, 0.80, 0, -0.19, 0.80, MAWRED, { metalness: 0.2 });
    // 文氏尾噴(無後座砲識別:後噴抵銷後座)—— 落點 MUST 貼**顱殼那一段的頂邊**:
    // 上頸套筒佔住了顱後下角,尾噴不能留在原位(會整顆埋進套筒);但推到顱後上緣 (0.36, −0.24)
    // 又太後面 —— 顱殼頂邊在 x −0.24 只有 y 0.157,尾噴整顆浮在殼外 0.20,45° 特寫讀成
    // 一塊獨立的深色方塊(d1b 實測)。改落在 x 0.02(頂邊 y 0.351)、基部壓進去 0.07。
    const vent = latheF(hh, [[0.06, 0], [0.09, 0.10], [0.125, 0.21]], 10, 0, 0.28, 0.02, GUNMETAL, { metalness: 0.8 });
    vent.rotation.x = -Math.PI / 2;
  },
  neck(c, nk) {
    const { PAL } = c;
    // ── 水平長頸(v5)。樞軸 neckAt = 胸腔 (0, 0.38, 0.90);頭節點恆在胸腔 (0, 1.28, 0.04)。
    // 芯柱兩端(胸腔框)= (0, 0.60, 1.02) → (0, 0.80, 1.98):長 0.98、rot.x = 1.3654(近水平前伸)。
    //   起點壓在軀幹前端面(上緣 z 1.44)**之內** ⇒ 頸是自軀幹前緣長出來的(手繪稿 ㋑),
    //   終點插進顱殼後緣(z 1.98)⇒ 與掛在 hh 的上頸套筒重疊 0.30,兩截各自黏在自己那一端。
    // 頸局部 = 胸腔 − (0, 0.38, 0.90) ⇒ 芯中心 (0, 0.32, 0.60)。
    // v6:軀幹與頭各前移 +0.30(檔頭 v6 ㋐)⇒ 頸整組跟著走,但 MUST 沿**頸軸**走
    //   (0, +0.06, +0.30)而不是純 +z:純 z 的 0.30 有 0.061 落在垂直頸軸的方向上,
    //   四個頸甲環會整圈偏到芯柱下緣(芯半徑才 0.30~0.42)。
    //   ⇒ 新芯兩端(胸腔框)= (0, 0.66, 1.32) → (0, 0.86, 2.28),仍是 0.98 長、近水平。
    const core = cylF(nk, 0.30, 0.42, 0.98, 10, 0, 0.38, 0.90, PAL.dark, { metalness: 0.65 });
    core.rotation.x = NECK_ROT;                                  // rt(前/頭端)0.30 < rb(後/體端)0.42:往頭收分
    // 分節頸甲環 ×4(latheF 沿頸軸 t = 0.16/0.38/0.60/0.82 收分;半徑吃芯柱在該處的收分值
    // (0.42 → 0.30 線性)—— 手寫一組固定半徑就會在頭端整圈飄在芯柱外)
    // v8:軀幹前端面自 z 1.79 退到 1.59(D3 加長外露頸)⇒ 四環沿芯柱重新分布到 t = 0.26/0.43/
    //   0.60/0.77(舊制 0.16/0.38/0.60/0.82,最後那一環會被放大後的套筒吃掉)。
    //   半徑仍逐環吃芯柱在該處的收分值 r(t) = 0.42 − 0.12t。
    const rings = [[0.3322, 0.6697, 0.3888, PAL.main], [0.3661, 0.8328, 0.3684, PAL.mid],
      [0.4000, 0.9960, 0.3480, PAL.main], [0.4339, 1.1591, 0.3276, PAL.mid]];
    for (const [ry, rz, rr, col] of rings) {
      const ring = latheF(nk, [[rr * 0.86, -0.075], [rr, -0.035], [rr, 0.035], [rr * 0.86, 0.075]], 10, 0, ry, rz, col, { metalness: 0.55 });
      ring.rotation.x = NECK_ROT;
      ring.scale.z = 1.05;
    }
    // 頸背鱗板 ×2(喉管纜束 v8 搬到 head 的 hh —— 見那裡的理由)。
    // 鱗板落點 MUST 貼**芯柱在該處的表面**(t 0.37 / 0.74 各解一次),而且要壓進去一點:
    // d1 那一輪拿中心線 + 固定高度擺,兩片整個飄在頸背上方 = 側視特寫讀成兩塊碎片。
    for (const [py, pz] of [[0.730, 0.780], [0.750, 1.050]]) {
      const sc = tboxF(nk, { w0: 0.26, d0: 0.22, w1: 0.16, d1: 0.12, h: 0.14, sz: -0.03 }, 0, py, pz, PAL.deep, { metalness: 0.6 });
      sc.rotation.x = 0.90;
    }
  },
  chest(c, ch, d) {
    const { PAL, accent } = c;
    // ── 橫向長軀幹(v5:手繪稿「脊椎是橫向的」)──
    // 體軸 u = rot.x 1.42 的 +y 像 = (0, 0.1502, 0.9887),自骨盆前緣往前伸;兩段串接:
    //   ①肋腔桶 h 1.18(不動;sz 0.10 ⇒ 深胸落肩後)前端面中心 = (0, 0.528, 0.761)
    //   ②收分喉艙 h 0.70(v4 是 0.95),底面接在 ①的前端面上 ⇒ 置點 = (0, 0.528, 0.761) + 0.35u
    //     = (0, 0.580, 1.107);斷面 1.20×1.24 收到 **0.68×0.72**(v4 是 0.86×0.92);
    //     sz 0.24 ⇒ 前端面中心 (0, 0.396, 1.489)、上緣 (0.751, 1.435)、下緣 (0.040, 1.543)。
    // 兩段共 1.88(v4 是 2.13,−12%)—— 收掉的那 0.25 **不是把身體變短**,是讓出頸的位置:
    // 顱殼後緣同時自 z 1.28 推到 1.98 ⇒ 軀幹前緣與顱殼之間開出 **0.54** 的淨空(v4 是貼著的,
    // 側視完全看不出有頸),而整體剪影反而更長(吻端 3.04 → 3.74)。
    // v6:兩段一起 **+0.30 z**(檔頭 v6 ㋐)—— 髖樞軸釘死在 z 0,要讓「後腳在身體後端」
    //   成立就只能把身體往前搬:艙體自 z −0.42~1.49 移到 **−0.12~1.79**,於是 z 0 的髖落在
    //   艙體最後段(骨盆與背甲更在其後),而 z 0 的肩則落在艙體中段、由 ㋑ 的爪再往前送。
    //   肋環/胸甲/側百葉/領環/龍骨/腹管/背甲前緣**全部同量**,漏掉任何一件就是零件飄在艙外。
    // v7:骨架照 顱 : 軀幹 = 1 : 1.45(檔頭 ⓑ),而 v6 是 1.76 : 1.88 = 1 : 1.07 —— 顱殼是
    //   ★ 圖的招牌(巨顎藏砲)不動,只能**把軀幹加長**。加在**後端**:前端面一動,頸與頭
    //   整組要跟著走(檔頭 v6 ㋐),而 roar 的槓桿臂 hh.z 已經 2.50。
    //   ⇒ 肋腔桶 h 1.18 → 1.48,前端面中心 (0, 0.627, 1.046) **逐位元不動**,置點反算 =
    //     前端面 − u × h/2,u = (0, cos1.42, sin1.42) = (0, 0.1494, 0.9888)
    //     ⇒ (0, 0.516, 0.314);後端面自 z −0.12 退到 **−0.42**(壓在骨盆塊上,同骨架照
    //     「肋廓後端罩住髖臼」)。軀幹 1.88 → 2.18 ⇒ 顱 : 軀幹 = 1 : 1.24。
    // v8(D3:頸太短)——**收分喉艙往回收 0.18、肋腔桶往後補 0.18**,軀幹總長 2.18 一格不動:
    //   喉艙 h 0.70 → 0.52(剪切 sz 依比例 0.24 → 0.18,剪切**角**不變)⇒ 前端面中心
    //   (0, 0.395, 1.789) → **(0, 0.527, 1.587)**(退 0.20、抬 0.13);
    //   肋腔桶 h 1.48 → 1.66,前端面 (0, 0.627, 1.046) 逐位元不動 ⇒ 置點 = 前端面 − u·h/2,
    //   u = (0, cos1.42, sin1.42) = (0, 0.1494, 0.9888) ⇒ (0, 0.503, 0.225);後端面 z −0.42 → −0.60。
    //   ⇒ **外露頸 0.49 → 0.69**(自前端面 z 1.59 到顱殼後緣 2.28)。
    //   ⚠ 上界是**肩球**:肩球在 z 1.26、半徑 0.20,喉艙在該處半寬 0.50 ⇒ 仍半埋成肩窩;
    //     喉艙再收就變成浮在體側的一顆球(v7 檔頭 ⓒ㋑ 的同一條界線)。
    const body = tboxF(ch, { w0: 1.00, d0: 1.02, w1: 1.20, d1: 1.24, h: 1.66, sz: 0.10 }, 0, 0.503, 0.225, PAL.main, { metalness: 0.55 });
    body.rotation.x = 1.42;
    const fore = tboxF(ch, { w0: 1.20, d0: 1.24, w1: 0.68, d1: 0.72, h: 0.52, sz: 0.18 }, 0, 0.666, 1.303, PAL.main, { metalness: 0.55 });
    fore.rotation.x = 1.42;
    // 外凸肋甲環 ×4(latheF 鼓環貼各段中心線;環心壓在中心線下緣、凸在腹側 = ★ 圖的一圈圈裝甲帶)
    //   ①段中心線緩升、②段因剪切往前下走 ⇒ 前兩環 rot.x 1.42、後兩環跟著轉平/轉負;
    //   第 4 環半徑隨新的收分喉艙一起收(0.60 → 0.54),否則整圈飄在艙壁外面。
    // v8:肋腔桶再往後長 0.18、喉艙收 0.18 ⇒ 四環沿新的中心線重排,半徑逐環吃該處的半深:
    //   肋腔 half-d(z) = (1.02 + (z+0.595)/1.641 × 0.22)/2、喉艙 half-d(z) = (1.24 − (z−1.046)/0.541 × 0.52)/2。
    //   環心仍壓在中心線下緣 0.19~0.26(與 v7 逐環同一組偏移量)。
    const hoops = [[0.23, -0.30, 0.60, 1.42, PAL.mid], [0.32, 0.25, 0.65, 1.42, PAL.main],
      [0.33, 0.80, 0.68, 1.46, PAL.mid], [0.33, 1.30, 0.54, 1.58, PAL.main]];
    for (const [hy, hz, hr, hrot, col] of hoops) {
      const hp = latheF(ch, [[hr * 0.92, -0.08], [hr, -0.035], [hr, 0.035], [hr * 0.92, 0.08]], 12, 0, hy, hz, col, { metalness: 0.55 });
      hp.rotation.x = hrot;
      hp.scale.z = 0.92;
    }
    // 左右胸甲板(prismF 六角,貼收分喉艙的前上側)+ 側百葉(散熱開口)
    for (const sx of [-1, 1]) {
      const pec = prismF(ch, [[-0.17, -0.24], [0.17, -0.24], [0.28, 0], [0.17, 0.22], [-0.17, 0.22], [-0.28, 0]]
        .map(([x, y]) => [sx * x, y]), 0.10, sx * 0.24, 0.50, 1.40, PAL.lite, { metalness: 0.55 });
      pec.rotation.x = -0.15;
      bxF(ch, 0.06, 0.26, 0.44, sx * 0.58, 0.42, 0.82, INK, { metalness: 0.7 }).rotation.y = sx * 0.1;
    }
    // 頸根領環(套在頸芯柱的體端,壓在軀幹前端面內側 —— 與頸軸同一個角度,見 NECK_ROT)
    // v8:領環跟著新的前端面往回退 —— 落點吃芯柱在該處的收分值(z 1.50 ⇒ r 0.399)。
    const collar = latheF(ch, [[0.40, -0.06], [0.45, -0.02], [0.45, 0.02], [0.40, 0.06]], 12, 0, 0.715, 1.50, PAL.mid, { metalness: 0.6 });
    collar.rotation.x = NECK_ROT;
    collar.scale.z = 1.1;
    // 腹龍骨(深胸下緣長楔,沿腹線 z 0.51 → 1.61)+ 腹側管束
    // v8:喉艙前端收 0.20 ⇒ 龍骨同量收短(h 1.30 → 1.10),否則它會單獨戳在喉艙之外。
    const keel = tboxF(ch, { w0: 0.62, d0: 0.46, w1: 0.40, d1: 0.30, h: 1.10 }, 0, -0.11, 1.06, PAL.dark, { metalness: 0.65 });
    keel.rotation.x = 1.55;
    cablesF(ch, { p0: [0, -0.26, 1.55], p1: [0, -0.12, 0.60], k: 3, r: 0.03, sag: 0.03, spread: 0.09 }, IRON, { metalness: 0.8 });
    // ── 背甲大蓋板(放平 = 脊背線:髖最高、向肩微下斜,肩後覆至尾根)──
    // 尾根抬到 y 0.55 之後,尾首節(r 0.54)整段仍收在蓋板的寬度(±0.725)與高度之內 ⇒ 不露餡;
    // 蓋板後緣 (−1.92, 1.06) 恰好在尾管上緣(y 1.02)之上 —— **改尾根高或 r0 MUST 回頭核這一條**。
    // v6:**前緣**跟著艙體 +0.30(1.25/0.82/0.65),後緣三點一格不動 —— 背甲因此變長而不是
    //   整片前移:後緣 (−1.92, 1.06) 是壓在尾首節(r 0.54、根高 0.55 ⇒ 頂 1.09)上的那一條線。
    const slab = prismF(ch, [
      [1.25, 0.84], [0.82, 1.20], [-0.40, 1.38], [-1.30, 1.34], [-1.92, 1.06], [-1.72, 0.84], [-0.70, 0.78], [0.65, 0.70],
    ], 1.45, 0, 0, 0, PAL.mid, { metalness: 0.6 });
    slab.rotation.y = -Math.PI / 2;
    const cap = tboxF(ch, { w0: 1.00, d0: 0.88, w1: 0.86, d1: 0.72, h: 0.08 }, 0, 1.42, -0.72, PAL.main, { metalness: 0.55 });
    cap.rotation.x = 0.02;
    // 前斜面散熱百葉 ×4(INK 橫柵,索引沿前斜面 (0.95,0.84)→(0.52,1.20) 爬升;斜面向後升 ⇒ 負仰角)
    for (let i = 0; i < 4; i++)
      bxF(ch, 0.78, 0.04, 0.15, 0, 0.90 + i * 0.075, 1.16 - i * 0.09, INK, { metalness: 0.7 }).rotation.x = -0.70;
    // 側鉸座 ×2 + 後支柱(蓋板落在骨盆上的結構件)+ 後排氣 ×2
    for (const sx of [-1, 1]) {
      tboxF(ch, { w0: 0.09, d0: 0.58, w1: 0.075, d1: 0.44, h: 0.32 }, sx * 0.70, 0.90, -0.90, PAL.deep, { metalness: 0.6 });
      cylF(ch, 0.08, 0.08, 0.18, 8, sx * 0.30, 1.00, -1.88, INK, { metalness: 0.75 }).rotation.x = Math.PI / 2 + 0.3;
    }
    const strut = tboxF(ch, { w0: 0.50, d0: 0.70, w1: 0.44, d1: 0.75, h: 0.80, sz: -0.10 }, 0, 0.45, -0.85, PAL.deep, { metalness: 0.6 });
    strut.rotation.x = -0.12;
    // 脊背識別燈
    bxF(ch, 0.42, 0.05, 0.26, 0, 1.41, -0.45, accent, { emissive: accent, emissiveIntensity: 0.8 });
  },
  pelvis(c, hips) {
    const { PAL } = c;
    // 骨盆塊(拉高接脊背線,後傾收分接尾根 —— 骨架照:髖是全身最高的支點)+ 下腹護板 + 尾根座環
    // v6:艙體前移 +0.30 之後骨盆改**往後長**(中心 −0.20 → −0.38、深 1.15 → 1.30)⇒ 髖樞軸
    //   (z 0)落在骨盆塊的**前半**,而臀肌罩(thigh)的後緣正好收在這一塊之下。
    // v7:股殼後剪加深到 0.74(股骨頂 world z −0.68)⇒ 骨盆再往後長 0.06 並加深 0.06,
    //   蓋住新的股骨頭(z 帶 −1.18~0.22)。骨盆不跟著長,髖後就是一個看得見的洞。
    const block = tboxF(hips, { w0: 1.05, d0: 1.36, w1: 0.92, d1: 1.10, h: 0.85, sz: -0.08 }, 0, 0.18, -0.44, PAL.mid, { metalness: 0.6 });
    block.rotation.x = -0.10;
    tboxF(hips, { w0: 0.75, d0: 0.80, w1: 0.62, d1: 0.62, h: 0.32 }, 0, -0.32, -0.30, GUNMETAL, { metalness: 0.7 });
    const tr = latheF(hips, [[0.36, -0.06], [0.42, -0.02], [0.42, 0.02], [0.36, 0.06]], 10, 0, 0.55, -0.92, IRON, { metalness: 0.8 });
    tr.rotation.x = Math.PI / 2;                                 // 尾根座環:隨尾根抬到脊線高(y 0.42 → 0.55)
  },
  thigh(c, l, d) {
    const { PAL } = c;
    // ── 巨股(v6:「後腳應該在身體的後端」)── 兩件事一起改,見檔頭 v6 ㋒:
    //   ㋐ 收分改成**髖粗膝細**:tboxF 的 w0/d0 是**底面 = 肢體遠端(膝)**、w1/d1 是頂面(髖),
    //      v3~v5 寫成 0.62→0.36 = 髖細膝粗(基準正面圖看得到兩條腿往下張開)。
    //   ㋑ 沿體軸**後剪**(底/膝端 z +0.06 不動;頂/髖端 v6 是 −0.39、**v7 推到 −0.68** ⇒ sz −0.74):
    //      髖樞軸在 z 0 是 forge 凍結的,股骨的**視覺**根部只能靠剪切退進骨盆之下,
    //      「肩→髖 = 1.6 × 股」(檔頭 ⓑ)的後半段就是靠這 0.74 買來的。
    //      **上界是骨盆塊**:頂面(d1 0.86)後緣轉到世界 z −1.07、y 2.62,恰在骨盆塊
    //      (world z −1.18~0.22、底 y ≈ 2.75)的後下角外一點點,由臀肌罩接住;再退就是
    //      「股骨頭掛在骨盆後面的空氣裡」。改 d1 或 sz MUST 回頭核這一條。
    // ── v8:**股殼只畫 0.98,不畫滿 1.56 的樞軸段長**(檔頭 v8 ⓑ)──
    // 樞軸鏈(股 1.56 / 脛 1.50)是 forge 凍結的,但 draw 回呼**可以畫比段長短的骨頭** ——
    // 本檔對肱骨/橈骨早就用了這一招。股殼改畫 FEM = 0.98(膝端不動、頂端往下收 0.58)⇒
    // 股 : 脛 : 蹠 = 0.98 : 0.983 : 0.517 = **1 : 1.00 : 0.53**(骨架照 1 : 0.95~1.07 : 0.50)。
    // 代價 = 股骨頂與髖樞軸之間空出 0.58,而**臀肌罩本來就蓋在那一段上**(下方 v8 再加大一圈)。
    const FEM = 0.98;
    tboxF(l, { w0: 0.42, d0: 0.56, w1: 0.68, d1: 0.86, h: FEM, sz: -0.52 }, 0, -d.len + FEM * 0.5, 0.06, PAL.main, { metalness: 0.55 });
    // 臀肌罩(prismF 側剖卵形;rotation.y = −π/2 ⇒ 剖面 x → 世界 z)= ★ 圖髖後那一大塊圓弧量體
    // (★ 圖的「大腿」本來就幾乎全是這一塊,股殼只露膝上那一截 ⇒ 短股殼反而更貼原圖)。
    // 兩條落點邊界(都要在 legBase −0.34 的股前傾**之後**量):頂緣 MUST 進到骨盆塊的 y 帶裡,
    // 不然髖後開一道縫;後緣 MUST 收在骨盆塊的 z 帶內。
    // v8:股殼頂端下收 0.58 之後,罩子 MUST **同時往前、往上、往下各長一圈**把新空出的
    //   y[−0.58, 0] × z[−0.89, −0.03] 整塊蓋住 —— 沒長的話髖前會開一個看得見的缺口。
    // 2026-08-14:罩子**往上長 0.36、往外長 0.24**(檔頭 ③)—— 髖盤搬到肩高之後,不長的話
    //   那枚盤浮在罩子上方的空氣裡。上緣四點各抬(頂點抬到局部 y 0.66 = 世界 y 3.42,恰是
    //   肩樞軸那條線),深度 0.70 → 0.94 但**整組只往外推**(位置 x = sx·0.12 ⇒ 內緣 −0.35
    //   逐位元不動、外緣 0.35 → 0.59),內側仍埋在骨盆塊之下。
    const haunch = prismF(l, [
      [-0.55, -0.14], [-0.42, -0.46], [-0.05, -0.60], [0.40, -0.44],
      [0.54, 0.18], [0.36, 0.74], [-0.15, 0.86], [-0.48, 0.66],
    ], 0.94, c.sx * 0.12, -0.20, -0.46, PAL.mid, { metalness: 0.6 });
    haunch.rotation.y = -Math.PI / 2;
    // 膝前甲板(prismF 圓角六角;落點吃股殼在該高度的**剪切後**前表面)
    // v8:股殼縮短 ⇒ 甲板改錨在**膝端**(y −1.02;殼中心線 z −0.23、半深 0.37 ⇒ 前表面 0.14)。
    const plate = prismF(l, [[-0.23, -0.63], [0.23, -0.63], [0.34, -0.11], [0.21, 0.32], [-0.21, 0.32], [-0.34, -0.11]],
      0.12, 0, -1.02, 0.16, PAL.lite, { metalness: 0.55 });
    plate.rotation.x = 0.06;
    // 髖側大圓盤關節(latheF;★ 圖髖部醒目圓盤,盤面圖騰歸 paint)+ 軸帽 —— 貼在臀肌罩的外側面。
    // ── 落點兩軸都是**推導**的(檔頭 ①②)──
    //   高度:目標 = 肩樞軸的世界高 ⇒ 世界偏移 DY = (shoulderY − hips) × H;
    //   而這裡是**已前傾 legBase 的股節框** ⇒ world = Rx(legBase)·local,反解 local = Rx(−legBase)·world。
    //   z 沿用舊盤的世界 z(HDZ)⇒ 盤只是沿體軸抬上去,不順手前後滑掉。
    const DY = (PROP.shoulderY - PROP.hips) * c.H, HDZ = -0.368;
    const ca = Math.cos(LEG_BASE), sa = Math.sin(LEG_BASE);
    const hdY = DY * ca + HDZ * sa, hdZ = -DY * sa + HDZ * ca;      // ⇒ (0.519, −0.207)
    const hd = latheF(l, [[0.06, -0.06], [0.29, -0.05], [0.34, 0], [0.29, 0.05], [0.06, 0.06]], 12, c.sx * 0.62, hdY, hdZ, PAL.mid, { metalness: 0.7 });
    hd.rotation.z = Math.PI / 2;
    const hub = cylF(l, 0.09, 0.09, 0.06, 10, c.sx * 0.67, hdY, hdZ, PAL.deep, { metalness: 0.8 });
    hub.rotation.z = Math.PI / 2;
    // 後側腱活塞(hydCyl 亮桿芯;落點吃殼在 y −0.92 的**背**表面 −0.66)+ 膝前護楔
    // v8:長度收到 0.68,兩端才都收在縮短後的股殼(y −1.56 ~ −0.58)之內。
    hydCyl(l, 0.05, 0.68, -c.sx * 0.16, -0.92, -0.60, 0.12, PISTON);
    const kg = tboxF(l, { w0: 0.32, d0: 0.13, w1: 0.25, d1: 0.10, h: 0.28, sz: 0.04 }, 0, -d.len * 0.94, 0.29, PAL.deep, { metalness: 0.6 });
    kg.rotation.x = -0.15;
  },
  shin(c, l, d) {
    const { PAL } = c;
    // ── 脛骨 + 跗關節 + 蹠骨(v6 使用者:「後腳加裝蹠骨」;檔頭 v6 ㋓)──
    // biped 鷹架的腿鏈**恰三節**(股/脛/足)寫死在 forge.mkLeg ⇒ **不准加第四個樞軸**,
    // 蹠骨一律由這一段的幾何表達。踝樞軸在段末 y = −d.len(1.50),一格不能動。
    // v7 依骨架照重分配(檔頭 ⓑ・ⓒ㋐):骨架照 脛 : 蹠 = 0.95 : 0.50,正規化 = **65.5% : 34.5%**;
    // 脛段 d.len = 1.50 ⇒ TIB = 1.50 × 0.655 = 0.983、蹠 = 0.517(v6 是 0.92 / 0.56 = 61.3% : 37.3%)。
    // ⚠ 這個 65.5% 是**三處唯一的值**(檔頭 ㋓ / doc 列 / 此處);v6 曾三處各寫一個數字。
    const TIB = 0.983, TARY = -0.983;                            // 脛骨長 / 跗關節高(段長的 65.5%)
    // ①脛骨(w0/d0 = 底面 = 跗端、w1/d1 = 頂面 = 膝端 ⇒ 膝粗跗細;末端後剪 0.24)
    tboxF(l, { w0: 0.24, d0: 0.30, w1: 0.34, d1: 0.48, h: TIB, sz: 0.24 }, 0, -TIB * 0.5, -0.22, PAL.mid, { metalness: 0.6 });
    // 膝側圓盤(latheF)+ 軸帽
    const kd = latheF(l, [[0.04, -0.05], [0.19, -0.038], [0.23, 0], [0.19, 0.038], [0.04, 0.05]], 10, c.sx * 0.21, -0.05, 0.02, PAL.mid, { metalness: 0.7 });
    kd.rotation.z = Math.PI / 2;
    const hub = cylF(l, 0.065, 0.065, 0.05, 8, c.sx * 0.25, -0.05, 0.02, PAL.deep, { metalness: 0.8 });
    hub.rotation.z = Math.PI / 2;
    // ②跗關節(鳥腳的「反折膝」):latheF 盤 + 貫穿橫軸 —— 軸向 MUST 是 x(鉸鏈),
    //    畫成環(軸向沿肢體)就只是一圈裝飾,讀不出「這裡會折」。
    const td = latheF(l, [[0.05, -0.055], [0.16, -0.042], [0.195, 0], [0.16, 0.042], [0.05, 0.055]], 10, c.sx * 0.15, TARY, -0.21, PAL.deep, { metalness: 0.75 });
    td.rotation.z = Math.PI / 2;
    const tax = cylF(l, 0.055, 0.055, 0.34, 8, 0, TARY, -0.21, IRON, { metalness: 0.85 });
    tax.rotation.z = Math.PI / 2;
    // ③蹠骨幹(明顯更細:斷面只有脛骨膝端的一半;剪切方向與脛骨**相反** ——
    //   跗端 z −0.22 前傾回到踝樞軸 z +0.02)
    // 色階 MUST 與跗盤(deep)分開一階:同色的話那一段在賽璐璐階梯上與關節盤糊成一團暗塊,
    // 特寫下讀成「小腿末端的陰影」而不是一根骨頭。
    tboxF(l, { w0: 0.17, d0: 0.21, w1: 0.21, d1: 0.26, h: d.len + TARY, sz: -0.24 }, 0, (TARY - d.len) * 0.5, 0.02, PAL.dark, { metalness: 0.65 });
    // 脛前甲(細長楔,貼脛骨前表面)+ 後側腱活塞(退到後剪之後的脛骨背面)+ 踝座環(套在蹠骨上)
    const greave = tboxF(l, { w0: 0.18, d0: 0.08, w1: 0.25, d1: 0.11, h: 0.52 }, 0, -0.55, 0.06, PAL.lite, { metalness: 0.55 });
    greave.rotation.x = -0.06;
    hydCyl(l, 0.038, 0.62, -c.sx * 0.08, -0.55, -0.36, 0.1, PISTON);
    // 踝座環落點吃蹠骨幹在該高度的中心線:蹠骨自 (y −0.983, z −0.22) 走到 (y −1.50, z +0.02)
    // ⇒ y −1.38 處 z = −0.22 + (0.397/0.517) × 0.24 = −0.036。
    latheF(l, [[0.115, -0.04], [0.135, -0.016], [0.135, 0.016], [0.115, 0.04]], 10, 0, -1.38, -0.036, IRON, { metalness: 0.8 });
  },
  foot(c, l, d) {
    const { PAL } = c;
    // v6:蹠骨移進 shin() 之後,足**只剩趾**(趾行)。舊的 0.55 × 0.54 長足底板是**蹠行**的
    // 讀法(整隻腳掌貼地),收成一塊只蓋住趾根的蹠端墊。
    // v7(結掉 v6 未結案 ③):足爪最低點實測 world y **−0.151**(略陷地,其餘四足機體都在 0 附近)
    //   ⇒ 全組抬 FY = 0.15。踝樞軸位置由凍結的 prop/pose 決定、動不了,能動的只有「趾掛在
    //   踝下多深」。⚠ 這一組的 y 是**接地面**:改完 MUST 用 run 那張與探針的最低 y 一起核。
    const FY = 0.15;
    tboxF(l, { w0: 0.30, d0: 0.30, w1: 0.36, d1: 0.34, h: 0.20, sz: 0.02 }, 0, FY - d.clear * 0.45, d.footL * 0.19, PAL.deep, { metalness: 0.55 });
    const guard = tboxF(l, { w0: 0.32, d0: 0.09, w1: 0.25, d1: 0.07, h: 0.30 }, 0, FY + 0.05, 0.16, PAL.mid, { metalness: 0.6 });
    guard.rotation.x = 0.5;
    // 三趾:tboxF 指節 + finF 弧爪(一趾一件;中趾索引放大)
    for (let i = -1; i <= 1; i++) {
      const k = 1 - Math.abs(i) * 0.16;
      const kn = tboxF(l, { w0: 0.19, d0: 0.16, w1: 0.13 * k, d1: 0.12, h: 0.34 * k }, i * 0.22, FY - d.clear * 0.45, d.footL * 0.5, PAL.mid, { metalness: 0.6 });
      kn.rotation.x = Math.PI / 2;
      const claw = finF(l, { len: 0.38 * k, w0: 0.13, w1: 0.026, t: 0.10, sweep: 0.05, camber: 0.045 },
        i * 0.22, FY - d.clear * 0.5, d.footL * 0.72, CLAWSTEEL, { metalness: 0.7 });
      claw.rotation.x = Math.PI / 2 + 0.5;
    }
    // 後距爪(finF 朝後下)
    const spur = finF(l, { len: 0.22, w0: 0.09, w1: 0.022, t: 0.075, sweep: 0.04, camber: 0.035 }, 0, FY - d.clear * 0.3, -d.footL * 0.17, CLAWSTEEL, { metalness: 0.7 });
    spur.rotation.x = -(Math.PI / 2 + 0.4);
  },
  armUp(c, a, d) {
    const { PAL } = c;
    // ── 退化短前臂 ①上臂(v6:使用者「前腳應該在身體的前端」)──
    // 肩樞軸是 forge 凍結的胸腔局部 (±0.6, 0.42, 0);前置只能靠幾何,樞軸鏈一格都不知道。
    // **連肩球一起往前搬**(v6-2,真側視實測):只推肘/腕的話,肩球仍留在樞軸 z 0.26 = 艙體
    //   正中央,整條臂讀成「從肚子中段長出來」;肩球推到 z 0.60 之後它落在肋腔桶側面
    //   (肋腔世界 z −0.12~1.06,半寬 0.58 ≈ 肩 x 0.6)⇒ 讀成「從胸腔前段的側面長出來」,
    //   而它仍**貼在殼上**(再往前就會離開肋腔、變成浮在喉艙旁邊的一顆球)。
    // ── v7 依骨架照重解(檔頭 ⓑ・ⓒ㋒)。兩件事一起改:
    //  ㋐ **肩球再往前**:v6 的 z 0.60 落在肋腔正中央,肩→髖只有 0.99 = 0.63 × 股。
    //     骨架照的肩臼在**肋廓最前端、頸根下方**(讀數 (405,745),而髖臼在 (672,1105))⇒
    //     肩球推到 z **1.18**:肋腔前端面在 z 1.05(半寬 0.60)、其後是收分喉艙,z 1.18 處
    //     半寬 0.55 ⇒ 半徑 0.20 的球半埋進殼裡 = 讀成肩窩而不是浮球。**這就是上界**。
    //  ㋑ **整條前肢畫短到骨架照的 0.43 × 股**:樞軸鏈(0.60 + 0.54)是 0.73 × 股,比暴龍
    //     前肢長 70% ⇒ 肱骨只畫 **0.40**、橈骨只畫 0.27(合 0.67 = 0.43 × 1.56 ✓)。
    //     **這一段不再用剪切**:骨架照的肱骨自肩臼往**前下** 24°(讀數 (405,745)→(345,772)),
    //     用 tboxF 的 h + sz 去湊那個角度會把斷面壓成薄片(h 只剩 0.16)⇒ 改成 h = 全長、
    //     整塊繞 x 轉 **φ = −1.38 rad**(局部 −y ↦ (0, −cosφ, −sinφ) = (0, −0.190, +0.982))。
    //     ⚠ 第一輪取 φ = −1.15(骨架照的 24° 俯角)⇒ 截圖上整條臂讀成「胸側的一個疙瘩」:
    //     機甲的胸比暴龍的肋廓深得多,0.67 長的臂在 24° 俯角下**打不出喉艙前端面**(z 1.79)、
    //     整組貼在殼上。骨架照的俯角是**在牠自己那個窄肋廓上**量的 ⇒ 這裡改成幾乎純前伸,
    //     長度比值(這一輪真正的驗收標的)一格未動。
    //     肩球 S = (0, 0.03, **1.26**)(v7-2 再前推 0.08,喉艙在該處半寬 0.50 ⇒ 球仍半埋成肩窩)。
    // ── v8:**股殼改畫 0.98(v8 ⓑ)⇒ 比值的分母換了**,前肢兩節必須同量收才守得住
    //     「(肱+橈) ÷ 股 = 0.43」這條驗收(不收的話 0.67/0.98 = 0.68 = 直接紅字)。
    //     肱骨 0.40 → **0.25**、橈骨 0.27 → **0.17** ⇒ (0.25+0.17)/0.98 = **0.4286**。
    //     ⚠ 收的只有**兩節骨殼**;肩球/肩甲/活塞/肘盤/腕帽/兩指鉤爪一件不動、一格不縮
    //     (★ 圖的招牌在爪與活塞,不在骨殼長度)。
    //     dir = R_x(−1.38)·(0,−1,0) = (0, −0.18937, +0.98191);
    //     肱骨中心 = S + 0.125·dir = (0, 0.006, 1.383);**視覺肘 E = S + 0.25·dir = (0, −0.017, 1.505)**。
    sphF(a, 0.20, 0, 0.03, 1.26, PAL.mid, { metalness: 0.65 });
    const pad = tboxF(a, { w0: 0.24, d0: 0.28, w1: 0.34, d1: 0.38, h: 0.20, sz: -0.05 }, c.sx * 0.04, 0.02, 1.28, PAL.mid, { metalness: 0.6 });
    pad.rotation.z = -c.sx * 0.18;
    // 殼色 lite → mid、前臂再降一階(見 armFore):亮色小臂疊在亮色胸甲前面,45° 特寫下
    // 讀不出那是一條臂;★ 圖的前肢本來就比胸甲暗。
    const hum = tboxF(a, { w0: 0.17, d0: 0.19, w1: 0.26, d1: 0.29, h: 0.25 }, 0, 0.006, 1.383, PAL.mid, { metalness: 0.55 });
    hum.rotation.x = -1.38;                                      // w0/d0 = 底面 = 肘端 ⇒ 肩粗肘細
    // 屈肌活塞:與肱骨同軸同心(tilt 就是 rotation.x)
    hydCyl(a, 0.028, 0.18, c.sx * 0.14, 0.006, 1.383, -1.38, PISTON);
  },
  armFore(c, a, d) {
    const { PAL } = c;
    // ── ②前臂:肘樞軸 (±0.6, −0.18, 0) 且肘屈 pose.elbow.base = −0.55 ⇒ 這個框已整組前傾 31.5°。
    // v8:armUp 定的**視覺肘 E = 臂根框 (0, −0.017, 1.505)**;換回肘局部 = 先減樞軸 (0,−0.60,0)、
    //   再繞 x 轉 +0.55(cos 0.85252 / sin 0.52269):
    //     E − (0,−0.60,0) = (0, 0.583, 1.505) → **E_fore = (0, −0.290, 1.588)**
    // 橈骨長 **0.17**(與肱骨 0.25 合計 0.42 = 0.43 × 新的股殼 0.98 ✓;見 armUp 的 v8 註)。
    // **朝向同肱骨那一條理由**(v7-3):沿肘局部 −y 直走 = 臂根框的 (0, −0.853, +0.523)「往前下
    //   31.5°」,爪因此落到 y 2.94、z 2.07 —— 那正好是**腹龍骨**的高度帶(world y 2.66~3.12)
    //   與前緣(z 1.81),45° 特寫下整隻手與龍骨前端糊在一起(z2_arm 實測)。骨架照的前肢是
    //   **與肩臼幾乎同高地往前伸**(讀數 肩 (405,745) → 腕 (305,758),落差只有 13px)⇒ 這裡改成
    //   繞 x 轉 ψ = −0.666(局部 −y ↦ (0, −0.787, +0.617),回臂根框 = 前伸略下 20°),
    //   ψ = −0.666 ⇒ 局部 −y ↦ (0, −0.78578, +0.61825)(回臂根框 = 前伸略下 20°)。
    //   ⇒ 中心 = E_fore + 0.085·dir = (0, −0.357, 1.641)、**W_fore = (0, −0.424, 1.693)**。
    const ed = latheF(a, [[0.045, -0.05], [0.125, -0.04], [0.145, 0], [0.125, 0.04], [0.045, 0.05]], 10, c.sx * 0.11, -0.290, 1.588, PAL.deep, { metalness: 0.75 });
    ed.rotation.z = Math.PI / 2;
    const rad = tboxF(a, { w0: 0.15, d0: 0.17, w1: 0.20, d1: 0.22, h: 0.17 }, 0, -0.357, 1.641, PAL.dark, { metalness: 0.6 });
    rad.rotation.x = -0.666;
    hydCyl(a, 0.022, 0.12, -c.sx * 0.10, -0.357, 1.641, -0.666, PISTON);
  },
  mount(c, F) {
    const { PAL, accent } = c;
    // ── 顎鉸 + 下顎(真樞軸:heavyPivot 充能張顎 = 射擊姿勢;rest 常開 0.36 露砲)──
    const jawG = new THREE.Group();
    jawG.position.set(0, -0.70, 2.63);                          // 吃 head hh 偏移(−0.52/+2.50)+ (−0.18/+0.13)
    jawG.rotation.x = 0.36;
    (c.headG || F.head).add(jawG);       // 頭放大 50% 的那一層(head() 建的 c.headG)
    const axle = cylF(jawG, 0.06, 0.06, 0.64, 8, 0, 0.02, 0, IRON, { metalness: 0.85 });
    axle.rotation.z = Math.PI / 2;
    for (const sx of [-1, 1]) {
      const hd = latheF(jawG, [[0.045, -0.038], [0.11, -0.03], [0.125, 0], [0.11, 0.03], [0.045, 0.038]], 10, sx * 0.34, 0.02, 0, PAL.deep, { metalness: 0.8 });
      hd.rotation.z = Math.PI / 2;
    }
    const jaw = prismF(jawG, [
      [-0.07, -0.18], [0.61, -0.24], [1.05, -0.18], [1.19, -0.02], [1.12, 0.07], [0.88, 0.02], [0.17, 0.02], [-0.11, -0.02],
    ], 0.52, 0, 0, 0, PAL.mid, { metalness: 0.6 });
    jaw.rotation.y = -Math.PI / 2;
    // 下齒列(一齒一件,尖端朝上、前大後小)+ 口腔底板(暗紅)
    for (let i = 0; i < 4; i++) {
      const th = 0.14 - i * 0.016;
      for (const sx of [-1, 1])
        tboxF(jawG, { w0: 0.065, d0: 0.08, w1: 0.022, d1: 0.028, h: th }, sx * 0.21, 0.02 + th / 2, 1.0 - i * 0.19, TEETH, { metalness: 0.35 });
    }
    bxF(jawG, 0.38, 0.05, 0.80, 0, 0.02, 0.55, MAWRED, { metalness: 0.2 });
    // ── 口腔四管旋管砲束(輕/重同膛:lMuz 管束小環、hMuz 充能大環;藏在顎裡,張口才見)──
    const tg = new THREE.Group();
    tg.position.set(0, -0.86, 2.83);                            // 吃 head hh 偏移(−0.52/+2.50)+ (−0.34/+0.33)
    (c.headG || F.head).add(tg);         // 同上:顎/砲束 MUST 與顱殼同框
    const breech = tboxF(tg, { w0: 0.28, d0: 0.26, w1: 0.24, d1: 0.22, h: 0.32 }, 0, 0, 0.02, GUNMETAL, { metalness: 0.8 });
    breech.rotation.x = Math.PI / 2;
    cylF(tg, 0.08, 0.08, 0.80, 8, 0, 0, 0.46, COAL, { metalness: 0.85 }).rotation.x = Math.PI / 2;
    for (let i = 0; i < 4; i++) {
      const a2 = i * Math.PI / 2 + Math.PI / 4;
      cylF(tg, 0.045, 0.045, 0.86, 8, Math.cos(a2) * 0.09, Math.sin(a2) * 0.09, 0.52, INK, { metalness: 0.85 }).rotation.x = Math.PI / 2;
    }
    const hMuz = latheF(tg, [[0.13, -0.038], [0.17, -0.014], [0.17, 0.014], [0.13, 0.038]], 10, 0, 0, 0.84, accent, { emissive: accent, emissiveIntensity: 1.5 });
    hMuz.rotation.x = Math.PI / 2;
    const lMuz = latheF(tg, [[0.065, -0.026], [0.095, -0.011], [0.095, 0.011], [0.065, 0.026]], 10, 0, 0, 1.02, accent, { emissive: accent, emissiveIntensity: 1.3 });
    lMuz.rotation.x = Math.PI / 2;
    cylF(tg, 0.058, 0.058, 0.05, 8, 0, 0, 1.0, INK, { metalness: 0.3 }).rotation.x = Math.PI / 2;
    // ── ③兩指鉤爪 ×2(暴龍前肢就是**兩指**;F.hand:腕帽 + 每指兩節指骨 + finF 鉤爪,一件一件)──
    // 腕樞軸(forge)在胸腔 (±0.6, −0.640, 0.282) —— **它在 z 0.282 而不是視覺腕的 z 1.79**:
    // 前置量是幾何加上去的,樞軸鏈一格都不知道。v8 的 armFore 定的視覺腕(肘局部)
    //   W_fore = (0, −0.424, 1.693) ⇒ 先減前臂樞軸 (0,−0.54,0)、再繞 x 轉 +0.15(腕屈 −0.15)
    //   ⇒ 手局部基準 **B = R(+0.15)(0, 0.116, 1.693) = (0, −0.138, 1.691)**
    //   (v7 是 (0,−0.342,1.835))。手掌零件一律自 B 起算,**逐件尺寸一格未縮**(v8 只收骨殼)。
    // **逐件尺寸與偏移一律 ×0.7**(檔頭 ⓒ㋒):骨架照的掌部(腕 (305,758) → 爪尖 (283,738))
    //   只有 32px = 0.13 × 股 ⇒ 世界 0.20;v6 那隻手自 B 起算長 0.45,是暴龍手的兩倍多。
    // 指節朝向:手局部 rot.x = π/2 + ψ 的長軸 = (0, −sinψ, cosψ),再繞 x 轉 −0.70 回胸腔框:
    //   ψ 0.75 水平前伸 / ψ 1.00 中節下傾 / 爪 ψ 1.45 下鉤。逐節首尾相接(節長 0.10 / 0.08 / 爪 0.16)。
    for (const [g, sx] of [[F.handL, -1], [F.handR, 1]]) {
      const cap = latheF(g, [[0.055, -0.04], [0.09, -0.02], [0.09, 0.02], [0.055, 0.04]], 10, 0, -0.138, 1.691, PAL.deep, { metalness: 0.7 });
      cap.rotation.x = Math.PI / 2 + 0.75;
      for (const i of [-1, 1]) {
        const fx = i * 0.07 - sx * 0.015;                       // 兩指並列,整組略偏體側內緣
        // 指節取 PAL.deep(暗)、爪刃取 CLAWSTEEL(亮):正面視角前爪疊在**亮色巨股**前面,
        // 同色系會整組讀不出來(m02_c3_front 實測);★ 圖的爪本來也是暗色金屬。
        const k1 = tboxF(g, { w0: 0.078, d0: 0.082, w1: 0.064, d1: 0.068, h: 0.10 }, fx, -0.183, 1.739, PAL.deep, { metalness: 0.7 });
        k1.rotation.x = Math.PI / 2 + 0.75;
        k1.rotation.z = -i * 0.10;                              // 張指(Rz 先作用 ⇒ x 分量保留,不與 Rx 互轉)
        const k2 = tboxF(g, { w0: 0.064, d0: 0.068, w1: 0.050, d1: 0.054, h: 0.08 }, fx, -0.244, 1.790, PAL.deep, { metalness: 0.7 });
        k2.rotation.x = Math.PI / 2 + 1.00;
        k2.rotation.z = -i * 0.10;
        const claw = finF(g, { len: 0.16, w0: 0.056, w1: 0.012, t: 0.050, sweep: 0.025, camber: 0.025 }, fx, -0.273, 1.809, CLAWSTEEL, { metalness: 0.7 });
        claw.rotation.x = Math.PI / 2 + 1.45;
        claw.rotation.z = -i * 0.10;
      }
    }
    return {
      gunR: null, gunL: null,
      muzzles: { light: { n: lMuz, r: 0.095 }, heavy: { n: hMuz, r: 0.17 } },
      lightGlowM: [lMuz], heavyGlowM: [hMuz],
      heavyPivot: [{ obj: jawG, rest: { x: 0.36, y: 0, z: 0 }, deploy: { x: 0.68, y: 0, z: 0 } }],   // 充能張顎
      weap: { light: 'N', heavy: 'N' },
      hvy: { chest: 0.06 },
      aimPose: null,
      wpn: { light: { nodes: [tg], ref: tg, muz: lMuz, fwd: 'z' }, heavy: { nodes: [tg, jawG], ref: tg, muz: hMuz, fwd: 'z' } },
    };
  },
  extra(c, F, rig) {
    const { PAL } = c;
    // 重尾(chainF 十一節收分)—— whipTail 逐幀「覆寫」節樞軸旋轉 ⇒ 基礎姿勢 MUST 住幾何:
    // 掛在近水平的尾根 Group 上(whipTail 摸不到它)。v5(手繪稿「脊椎是橫向的 / 尾巴加長」):
    //   ㋐尾根抬到與頸根同高(y 0.42 → 0.55 ⇒ 「頸根 → 骨盆 → 尾根」這條線 −1.4°,近水平);
    //   ㋑下垂整組壓平(tailRoot −0.12 → −0.02、rot0 −0.03 → −0.012、rotD 0.006 → 0.0024;
    //      累積角 Σ_{i<11}(rot0 + rotD·i) 仍恰好收斂回 0 ⇒ 尾梢回正、不翹不垂)
    //      ⇒ 尾尖胸腔 y −0.51 → **+0.29**,與顱殼(0.58~1.15)落在同一個高度帶;
    //   ㋒加長只准動節長帶(**n = 11 是 rig.tailSegs 的契約,一格不准動**):
    //      len0 0.66 → 0.70、len1 0.26 → 0.30 ⇒ 全長 5.06 → 5.50,背脊板 6 → 7 片。
    // 節間分隔 = 深色窄環(INK,v2 實測定案)。
    const tailRoot = new THREE.Group();
    tailRoot.position.set(0, 0.55, -1.00);
    tailRoot.rotation.x = -0.02;
    F.hips.add(tailRoot);
    const { segs, tip } = chainF(tailRoot, {
      n: 11, y: 0, z: 0,
      len0: 0.70, len1: 0.30, r0: 0.54, r1: 0.11,
      rot0: -0.012, rotD: 0.0024, ring: false, seg: 10,
      drawSeg: (t, i, { r, len }) => {
        const sep = cylF(t, r * 1.02, r * 1.02, r * 0.18, 10, 0, 0, -0.015, INK, { metalness: 0.7 });
        sep.rotation.x = Math.PI / 2;
        if (i < 7) tboxF(t, { w0: r * 0.55, d0: len * 0.5, w1: r * 0.3, d1: len * 0.32, h: 0.15 - i * 0.017 },
          0, r * 0.9, -len * 0.5, PAL.deep, { metalness: 0.6 });
      },
    }, PAL.main, { metalness: 0.55 });
    // 尾梢錐(coneF 朝後;★ 圖尾端收尖微翹)
    const tipCone = coneF(tip, 0.09, 0.42, 6, 0, 0, -0.46, PAL.deep, { metalness: 0.6 });
    tipCone.rotation.x = -Math.PI / 2;
    rig.blink = c.eyes || [];   // 紅眼閃爍(locomotion 的 rig.blink 通道;base 由它第一幀就地取)
    rig.tailSegs = segs;
    rig.tailUp = 0.3;
    rig.tinyArms = 1;      // 退化短前臂:不套一般雙足擺臂公式(locomotion)
    rig.leanF = 0.3;       // 水平體軸:加速不再壓頭,改抬尾配平
  },
};
