# tools/ — Offline Tools (module pointer)

> Tier-1 SSOT is root `AGENTS.md`. Tool boundaries live in `tools/AGENTS.md`.
> Change-to-guard mapping lives in `.claude/rules/verification.md`; per-assertion
> formulas live in each `tools/audit_*.mjs` header. Commands live in `package.json`.
> Audit-writing discipline stays in the audit headers themselves.
