# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 6(2026-08-02):只掃四張新/改場地。
#   run 5 開了全場地掃描,實測跑超過一小時仍未完 —— `--max-min` 只在**場地之間**檢查,
#   單一場地卡在 Overpass 逾時(45s × 3 鏡像 × 2 輪)時整輪都停不下來。四張新場地的
#   scen(③ 陸上高架橋 / ⑨ 水上高架橋 / ② 地下道)才是這次要複驗的東西,先拿到它們。
#   代價:`--only` 會跳過 scen/relief 標記複驗區段(那段刻意只在全場地掃描時比對,
#   否則沒掃到的場地會被誤判成漏標)⇒ 標記照抄逐場地行的實測值,全量複驗留待下一輪。
SCAN_ONLY=london,berlin,madrid,chicago
SCAN_BAKE=
SCAN_PROBE=
SCAN_PROBE_R=
