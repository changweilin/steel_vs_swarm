# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 4(2026-08-02):使用者要「地下道 / 陸上高架橋 / 水上高架橋各有預設地圖」。
#   **探測先行** —— 探測不需要 baked 兵線,一輪就能同時回報三種結構的中點座標
#   (2026-08-02 起 probe 也報橋,陸/水分流走 makeSpansWater 同一縫);
#   選好錨點再烤兵線,免得「烤完才發現那條路上沒有要的結構」白跑一輪 Overpass。
#   SCAN_ONLY 只留 jinlong:掃描步驟排在探測前面且吃 14 分鐘預算,
#   全場地掃會把時間用光、探測拿不到 runner。
SCAN_ONLY=jinlong
SCAN_BAKE=
SCAN_PROBE=52.50500,13.44890,柏林-華沙大街跨東站調車場;41.88880,-87.62450,芝加哥-密西根大道跨河;51.50090,-0.12460,倫敦-西敏橋;40.44560,-3.69120,馬德里-卡斯提亞大道北;40.43350,-3.68960,馬德里-哥倫布廣場;37.53450,126.97300,首爾-三角地;35.68120,139.76600,東京-八重洲;52.52000,13.37600,柏林-布蘭登堡門
SCAN_PROBE_R=2
