# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 10(2026-08-02):替 ② 地下道找第二張圖(馬德里兩輪失敗後重新選點)。
#   選點改用**結構性條件**而非「地下道密度」—— docs/lane_scenarios.md「② 為什麼市區
#   地下道咬不住」的結論:市區地下道與地面街道是等長的兩條 way,最短路徑沒有理由鑽下去;
#   `PREFER_TUNNEL` 只在「地下道本身就是唯一通路」時才咬得住(taroko 的峽谷公路)。
#   ⇒ 這批候選一律是「**鐵路/滑行道把平坦市鎮切成兩半、道路下穿是唯一跨越點**」的地方:
#      軌道兩側數百公尺內沒有第二個交會 ⇒ 兩堡分踞軌道兩側時,兵線非鑽下去不可。
#   讀數注意:probe 的「覆蓋 Nm」是拿**合成東西向假兵線**的高度場算的,與正式掃描
#   (吃烘焙兵線的高度場)不會逐位元相同 —— 是量級參考不是保證值,但仍遠優於圖資 way 全長
#   (馬德里 Joaquín Costa:圖資 165m、probe 165m、正式掃描只挖得出 29m)。
SCAN_ONLY=jinlong
SCAN_BAKE=
SCAN_PROBE=24.06200,120.53900,彰化-台鐵縱貫線下穿;23.96000,120.57300,員林-台鐵下穿;22.67200,120.48800,屏東-台鐵下穿;52.62500,10.08100,德國Celle-鐵道下穿;52.57300,-0.24700,英國Peterborough-鐵道下穿;35.80600,139.56500,日本新座-武藏野線下穿;41.65600,-0.87900,西班牙Zaragoza-鐵道下穿;25.07600,121.22800,桃園機場-滑行道下穿
SCAN_PROBE_R=2
