# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 5(2026-08-02):四張新/改場地的兵線已烘焙入庫(Actions run 30736945377)——
#   ①複驗 berlin/chicago/london/madrid 的 scen 實測值(③ 陸上高架橋 / ⑨ 水上高架橋 / ② 地下道),
#   ②順便補齊**全部**場地的 relief(側翼峰值):新增的起伏分級標記與 scen 同一條規則,
#     MUST 由實測產生 ⇒ 需要一次全場地掃描把值抓回來(每個場地的行尾都印「側向峰值 +Nm」)。
#   全場地掃描吃滿 14 分鐘預算是常態,掃不完的下一輪接著跑(.scen_cache 跨 run 保留)。
SCAN_ONLY=
SCAN_BAKE=
SCAN_PROBE=
SCAN_PROBE_R=
