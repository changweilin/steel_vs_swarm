# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 8(2026-08-02):四張新/改場地的 scen / relief 標記已依實測填入 venues.js。
#   下一輪該做的是**全場地掃描**,把其餘 21 個場地的 relief 補齊 + 跑標記全量複驗
#   (那段刻意只在全場地掃描時比對)。留空 SCAN_ONLY 即可,但 Overpass 慢的時候
#   一輪掃不完是常態 —— `.scen_cache` 跨 run 保留,再推一次接著跑。
SCAN_ONLY=
SCAN_BAKE=
SCAN_PROBE=
SCAN_PROBE_R=
