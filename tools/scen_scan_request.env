# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 10(2026-08-04)第一階段:回答使用者三問的**實測取數**,不動任何標記。
#   ①「市民大道沒有高架橋」:civicblvd 的 L1 兵線 bases 實測 = 25.0495~25.0526,整條落在
#      錨點(25.0470)以北 280~620m —— 兵線根本不在市民大道上。要取的數是:錨點一帶到底
#      有沒有車行高架橋、在哪(probe 回報陸橋中點座標,正好拿來當新錨點)。
#   ②「澀谷的地下道不見了」:2026-07-29 `strucTunnel` 資格閘上線後,澀谷站那條 indoor
#      footway tunnel 不再成洞。要取的數是:澀谷一帶有沒有**車行**地下道可以接手。
#   ③「找兵線有地下道的地圖」:2026-07-28 那張「八個候選點全部 0 條成洞」的表是
#      **underpassPlan 上線前**量的(舊制平地路面永遠沉不下去)⇒ 同一批候選點用現行引擎
#      重探一次,成本遠低於另外找新場地。madrid 一帶探到過 17 條車行地下道。
# 探測半徑取 3(L1 bbox 半徑僅 ~266 真實公尺,找錨點要看得更寬)。
SCAN_ONLY=civicblvd,shibuya,madrid,taroko,jinlong
SCAN_BAKE=
SCAN_PROBE=25.0470,121.5180,台北市民大道;35.6595,139.7004,東京澀谷;40.4378,-3.6892,馬德里卡斯提亞;40.7820,-73.9700,紐約中央公園79街;35.6628,139.7315,東京六本木;35.0100,135.7850,京都蹴上;48.8600,2.3300,巴黎河岸;37.4979,127.0276,首爾江南
SCAN_PROBE_R=3
