# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 11(2026-08-04)第二階段:探測模式修好之後重取錨點座標。
# 第一階段(run 30883603424)已經取到的數,直接寫在這裡當這一輪的前提:
#   civicblvd  場景 **0 種**;③候選陸橋 新生高架道路 **5184m,離兵線 226m**;
#              落空地下道 service 60m(underpassPlan 規劃放棄)⇒ 這張圖有高架橋,
#              但兵線被 PREFER_TUNNEL 拉去追一條挖不出來的 60m service 隧道。
#   shibuya    只中 ⑥;**②候選地下道 間坂 覆蓋 128m/沉 10m,離兵線 310m** ⇒ 澀谷有一條
#              引擎真的挖得出來的車行地下道,只是兵線沒走到。
#   madrid     中 ⑦;**②候選地下道 Calle de López de Hoyos 覆蓋 175m,離兵線 0m**
#              ⇒ 兵線就貼著它(從洞頂跨過去),換錨點就有機會走進去。
#              舊的兩個錨點(Joaquín Costa / María de Molina)都不是這一條。
# 這一輪只要三個點的**座標**:probe 會回報每條地下道/陸橋的覆蓋段中點經緯度,
# 那正是 ANCHORS 要填的值(L1 bbox 半徑僅 ~266 真實公尺,憑地名下錨必偏)。
# 探測模式在 2026-08-03 抽出 venue_field.mjs 時被刪掉 MAPGEO import 整支炸掉,本輪一併修復。
SCAN_ONLY=
SCAN_BAKE=
SCAN_PROBE=25.0470,121.5180,台北市民大道;25.0492,121.5232,市民大道東段;35.6595,139.7004,東京澀谷間坂;40.4378,-3.6892,馬德里卡斯提亞;40.4450,-3.6780,馬德里LopezDeHoyos;37.4979,127.0276,首爾江南;35.6628,139.7315,東京六本木;48.8600,2.3300,巴黎河岸
SCAN_PROBE_R=3
