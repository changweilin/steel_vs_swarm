# ============ 兵線場景掃描請求檔 ============
# 沙箱的整合權杖沒有 `actions: write`(dispatch API 403)時的遠端執行入口:
# 改這個檔推上分支 → `.github/workflows/lane-scenarios.yml` 的 push+paths 觸發器
# 以下列 SCAN_* 參數在有外網的 runner 上跑掃描/烘焙/探測,結果看 job log 與 artifact。
# 參數語意與 workflow_dispatch inputs 一一對應;不需要的參數留空即可。
#
# run 13(2026-08-04)第三階段**重跑**:參數與 run 12 相同,重跑的理由是 workflow 改了 ——
# 重烤結果改成由 runner 自己 commit 推回分支(舊制只把 diff 印進 log,而 venueLanes.js
# 一條兵線就是一行幾百個座標,從 log 抄回來比重跑一次還危險),另補一個「重點摘要」步驟
# 把決策要用的欄位印在**最後**(沙箱只讀得到 log 尾端)。
# run 12(30886341670)已完成、資料進了快取 ⇒ 這一輪的 Overpass 幾乎不用重抓。
#
# ---- run 12 的說明(參數不變,留著)----
#   civicblvd  ③ 陸上高架橋:錨點移到「市民大道高架道路」覆蓋段中點(1526m @25.04974,121.51228)、
#              偏好由 PREFER_TUNNEL 改成 PREFER_BRIDGE、方位角夾東西向(高架軸)。
#   roppongi   ② 地下道(**新場地**):探測到七條引擎真的挖得出來的車行地下道,
#              錨點取其中三段的覆蓋中點(265m / 107m / 547m),不夾方位角。
#              547m 那條比整條 L1 兵線(481m)還長 ⇒ 選它會把兩座主堡一起塞進洞裡,
#              排序會自己避開(tunLen 同分才比 tunBad)。
#   madrid     ② 地下道:錨點改成夾住 María de Molina 277m 覆蓋段的兩端(舊錨只涵蓋一半,
#              兵線因此從洞頂跨過去 = 中 ⑦ 不是 ②),另補 Joaquín Costa 212m 當第三錨。
# 全量掃描(SCAN_ONLY 留空)才會跑標記複驗:civicblvd/roppongi 的 scen/relief 目前留白,
# 掃描結果就是要拿來填的值。
#
# ---- run 11(已完成,run 30884234796)取到的座標,留檔 ----
# 台北市民大道 r3:地下道 3 條(忠孝西路地下道 128m @25.04576,121.51921 等)、
#                 陸橋 5 座(市民大道高架道路 1526m @25.04974,121.51228)
# 東京澀谷     r3:地下道 4 條(玉川通り 596m @35.65398,139.69311、首都高3号 136m/147m)
# 東京六本木   r3:地下道 7 條(乃木坂トンネル 547m @35.66675,139.72644、
#                 環状三号線 265m @35.66163,139.72851 / 107m @35.66387,139.72581)、陸橋 22 座
# 馬德里       r3:地下道 6 條(María de Molina 277m @40.43785,-3.68759、
#                 Joaquín Costa 212m @40.44491,-3.68517)
# 巴黎河岸     r3:地下道 3 條(Av. du Général Lemonnier 237m @48.86177,2.33065)
# 首爾江南     r3:地下道 0 條
# ⇒ 2026-07-28 那張「八個候選點全部 0 條成洞」的表是 underpassPlan **上線前**量的,已失效。
#
# ---- run 10 的參數(留檔)----
# 第一階段(run 30883603424)已經取到的數,直接寫在這裡當這一輪的前提:
#   civicblvd  場景 **0 種**;③候選陸橋 新生高架道路 **5184m,離兵線 226m**;
#              落空地下道 service 60m(underpassPlan 規劃放棄)⇒ 這張圖有高架橋,
#              但兵線被 PREFER_TUNNEL 拉去追一條挖不出來的 60m service 隧道。
#   shibuya    只中 ⑥;**②候選地下道 間坂 覆蓋 128m/沉 10m,離兵線 310m** ⇒ 澀谷有一條
#              引擎真的挖得出來的車行地下道,只是兵線沒走到。
#   madrid     中 ⑦;**②候選地下道 Calle de López de Hoyos 覆蓋 175m,離兵線 0m**
#              ⇒ 兵線就貼著它(從洞頂跨過去),換錨點就有機會走進去。
#              舊的兩個錨點(Joaquín Costa / María de Molina)都不是這一條。
# 這一輪只要三個點的**座標**:probe 會回報每條地下道/陸橋的覆蓋段中點經緯度,
# 那正是 ANCHORS 要填的值(L1 bbox 半徑僅 ~266 真實公尺,憑地名下錨必偏)。
# 探測模式在 2026-08-03 抽出 venue_field.mjs 時被刪掉 MAPGEO import 整支炸掉,本輪一併修復。
SCAN_ONLY=
SCAN_BAKE=civicblvd,roppongi,madrid
SCAN_PROBE=
SCAN_PROBE_R=
