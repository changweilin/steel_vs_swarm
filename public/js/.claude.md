# public/js — 活躍維護指南(觀測窗:2026-07-04 → 2026-07-24)

> **本檔定位**:雙層情境系統的**模組層**(短期高保真衝刺指南),搭配根目錄 [`/.claude.md`](../../.claude.md) 的全域準則使用。本檔內容有時效性,系統改版後 **MUST** 更新對應段落與日期戳;逐日完整紀錄見根目錄 `CLAUDE.md`。

---

## 1. 活躍維護脈絡

本目錄 = **客戶端全部**(渲染/FPV/物理/HUD/展示台)**加上全遊戲平衡數值**(`data.js`,伺服器直接 import)。近 14 天 commit 集中度:`data.js` 45 次、`game.js` 43、`models.js` 39、`main.js` 35、`locomotion.js` 22 — 全 repo 前五名都在本目錄。四條改版主線:

1. **第三人稱戰鬥可讀性**(07-13 ~ 07-17):32 角色全機種武裝建模(同型雙模/異型雙槍規則)、手持武器開火/蓄力動畫、槍口錨 + 機載後座、NPC 開火視覺全面化(shot 事件全兵種、交戰朝向、榴彈拋物線)。
2. **招式與動作演出**(07-15 ~ 07-16):`castfx.js` 14 演出原型 × 19 程序圖騰、施法/跳躍動作 post-pass、招式追加效果(控場/走位)動畫。
3. **FPV 座艙與 HUD 改版**(07-16 ~ 07-17):視野開闊帶(上帶/側帶禁大面積件)、小地圖圖資烤製 + 戰爭迷霧、雙展示台 + NPC 圖鑑 + 放大 modal。
4. **經濟與戰場改制**(07-17 ~ 07-18):八軌升級、賞金/助攻、空投物資、第三方軍隊(GUER/MILI)、高度制空、波次加坦克、地圖佈營調參 — **數值全住本目錄 `data.js`,結算全住 `server/sim.js`**。

---

## 2. 檔案職責地圖(Source of Truth)

| 檔案 | 職責 | 邊界與關係 |
|---|---|---|
| `data.js` | 平衡數值唯一真相:UNITS/WEAPONS/ECON(八軌 UPGRADES + 階梯單價 UPG_BASE/UPG_INC)/GAME/CHARACTERS/HEROIC/SQUAD/THIRD/AIRDROP/ALTITUDE/RECOIL/BALLISTIC + 衰減公式 + **機體高度表**(`SOLDIER_H`/`HERO_SIZE`/`heroTargetH()`/`TARGET_H`/命中量體 `hitH()`,07-23 自 models.js 移入)+ `tierVal`(含 Lv4 外推)+ `solveTowerSites()` + 砲塔規則稽核 `towerLayoutAudit()`(#4)/`towerTunnelAudit()`(#5) | 伺服器 import;敘事文字 MUST NOT 住這(去 `lore.js`);推導值(賞金/drone hp/bunker hp/SQUAD.DMG/Lv4 階)MUST NOT 手寫 |
| `lore.js` | 角色敘事(生平/台詞/`proto`/`art`) | **客戶端專用,伺服器 MUST NOT import** |
| `models.js` | `makeUnit()`:機體建模、分節骨架(segLimb/chain)、武裝掛點、rig 欄位**生產者**(`weap/hvy/gunR/gunL/muzzles/castSig/gait/...`)、`MORPH_HUMANOID`/`CAST_SIG`/`GUN_MOUNT` 表 | rig 欄位只准在這登記;新增地面體態 MUST 同步進 `MORPH_HUMANOID` |
| `locomotion.js` | rig **消費者**:`stepLocomotion`(步態/頭部穩定/尾配重)+ `stepCombatFx`(開火/蓄力,MUST 在 stepLocomotion **之前**呼叫)+ `stepCastPose`/`stepJumpPose`(post-pass,尾端疊加) | 戰場與展示台共用同一套;MUST NOT 另寫預覽專用動畫 |
| `game.js` | `BattleClient`:FPV/物理/插值/彈道/準星/`_entMuzzle`/`_npcMuzzle`/小地圖/RECOIL 手感 + **FPV 座艙取景**(`COCKPIT` 常數 + `_buildCockpit` 系列) | 彈道學在此(伺服器只驗證);RECOIL 純客戶端 MUST NOT 搬 sim;座艙取景四規則唯一縫 = `COCKPIT`(稽核 `tools/audit_cockpit.mjs`) |
| `charPreview.js` | 展示台:與戰場共用 `makeUnit` + `stepLocomotion`;跑步三態/跳躍/招式演出 | 事件源(`castFx/fireFx/heavyFx`)與戰場同一套欄位 |
| `castfx.js` | 招式 3D 演出,唯一入口 `spawnCastFx()` | 戰場(cast 事件)與展示台共用;純視覺不涉 sim |
| `cutin.js` | 招式立繪演出,純 DOM overlay | MUST NOT 拉進 3D 場景 |
| `audio.js` | 戰場音效唯一縫 `GameAudio`(雙層:Web Audio 程序合成 fallback + `public/audio/` CC0 樣本 manifest)。開火音由 `trajClass` 推導 | app 層(main.js)持有、跨戰局存活;hooks 僅 3 處(`game.js _onEvent`/`_tryFire`、main.js UI 委派);純表現層不涉 sim,bal/e2e 天然無關 |
| `paint.js` | `heroPalette()` 色版 + `paintUnit()` 性格花紋(徽記/國旗/覆蓋三類);花紋數值唯一真相(國旗色 = data.js `visual.flag`) | 在 fitToHeight/outlinify **之前**呼叫;烤死靜止姿勢局部矩陣;`map:'single'` 走 ClampToEdge + 朝向閘(見 §3 07-24 塗裝改制) |
| `main.js` | 房間/選角/雙展示台管理(`app.stages.char`/`unit`)/HUD DOM/NPC 圖鑑/地圖預建(`startPrebuild`) | shell 按鈕 class + 閉包定位,MUST NOT 共用 id |
| `toon.js` | 賽璐璐渲染核心(`mat()`/`outlinify`/`CEL_PAINT`) | 唯一材質包裝縫 |
| `biomes.js` / `ground.js` / `terrain.js` | 地景/道路/橋隧/水域/地被拼圖 | 站立表面唯一縫 = `terrain.surfaceAt(x,z,curY)`;MUST NOT 各自呼叫 `heightAt` 當地面高度 |
| `hazards.js` / `vfx.js` | 戰場物件(含 `buildAirdrop`)/特效 | — |
| `venueLanes.js` | 離線烘烤的真實道路兵線(63 組) | 只准由 `tools/bake_venue_lanes.mjs` 重新生成 |
| `net.js` / `venues.js` / `mapSelect.js` / `environment.js` | WS 通訊/場地表/選址 UI/環境 | — |

---

## 3. 近期技術債與地雷(新 → 舊;動到相鄰程式前 MUST 讀完)

**07-24 機體塗裝改制:徽記化 + 國旗逐件實色 + 櫻吹雪**(`paint.js` PATTERNS/`paintUnit` 重寫 + 新 `paintNationalLivery`/`paintEmblemPlate`/`paintMat`、`toon.js` CEL_PAINT 加朝向閘、`data.js` 11 角 `visual.paint` 改派 + 5 角新增 `visual.flag`;**純客戶端視覺,sim 不讀 `visual` ⇒ bal/e2e 天然不受影響**):使用者定案「除迷彩外全部換掉」,兩輪細修後三取向唯一縫 = `vis.paint`:
①**徽記**(`map:'single'`;totem/tattoo/flag)—— 透明底畫**一枚**對比徽章(`paper↔ink` 明暗對 + `hot` 色心,單色機身也顯眼)。**只貼在「完整平面、無配件插入」的一片外緣板**(`paintEmblemPlate`:於顯眼面挑「面積大 × 薄 × 靠外」的最高分 mesh,`score = 面積/(1+2.4·厚) × (1+0.6·外緣)`,**只對那一片 mesh 上漆**,其餘完全純色)—— 徽記因此不被相鄰配件戳破/分割。顯眼面依長寬高比:直立機甲(高為最大軸)取 **+Z 胸板**、橫置飛行器/獸型取 **+Y 頂板/背板/機翼**。ClampToEdge + 平移烤進 `uPaintM`(`vPaintP=(rig座標−板心)/(0.82·板短邊)+0.5`)。
②**國旗塗裝**(natflag,**不進 PATTERNS**,走 `paintNationalLivery`)—— **逐件實色重染**(非貼圖):可上漆件依 root 空間包圍盒**體積由大到小排序,累積達總量 80% 的大件全染 `flag[0]` 主色、其餘小件輪替 `flag[1..]` 配色,每件單一顏色**(使用者定案 80/20)。`flag[0]` MUST 取最貼近角色體色者(保留識別);色值只住 `data.js visual.flag`(**MUST NOT 在 paint.js 硬編國旗色**)。**現行僅 3 角**(s01 烏金/t01 俄白/m01 克紅);波蘭 s08、巴西 m06 經使用者定案**不套國旗色**,依原始 `flag`/`graffiti` 衍伸回徽記(s08→`flag` 十字盾徽、m06→`totem` 品牌戳記)—— 要增減國旗角改該角 `visual.paint`/`visual.flag` 即可。
③**覆蓋型**(`map:'tile'`)—— 迷彩(camo,唯一維持整機三平面平鋪)+ 淡櫻吹雪(sakura,淡色近裝甲低對比、給唯一粉色女角 t08)+ 雙色(minimal)。**純單色(solid)不進 PATTERNS**:`paintUnit` early-return 不掛貼花(t05)。
**朝向閘唯一縫 = `toon.js` uniform `uPaintFace` + `CEL_PAINT_GATE`**(`pa *= smoothstep(0.02,0.4, dot(法線,uPaintFace))`,徽記背面淨空)—— rig +Z = 機體正面(建模朝向,已驗);**MUST NOT** 改回純 `vPaintN.z`(橫置機種 +Z 只是機鼻,徽記會消失)。可上漆件過濾集中在 `paintMat()`(徽記/國旗/覆蓋三路共用同一支;跳描邊殼/透明件/發光識別條/noPaint 徽章)。**驗證**:`shot_units.mjs` 全 32 角三視角建模 0 例外 + 逐型別目視(natflag 大件主色小件配色且**逐件單色**、徽記落單一平板 s12 機翼圓徽/t12 胸徽最佳、sakura 淡 petal、solid 純色、camo 未變、minimal 雙色)+ `npm run bal` 四不變式全綠。**已知殘留**:①持槍待機姿勢會遮住部分胸徽(t10;動態姿勢無此問題);②綠系機身的綠盾徽對比天生偏低(單色系推導,已用 paper↔ink 拉到極限);③natflag 覆蓋全機故識別改依 emissive 識別條(paint 跳過發光件,已保留)。

**07-24 道路拼貼層次 + 車道分級 + 路口填面/寬度縮減**(`biomes.js buildRoads` + `buildBiomes` 新 `roadClearAt`、`ground.js buildGroundCover`/`tryPatch`;**data.js/sim.js/server 未動** ⇒ bal/e2e 天然不受影響):使用者五條需求「道路拼貼圖層必在其他地貌之上 / 依寬度分單車道・雙線道・四線道 / 路口接合完成度 / 不同寬度對接要縮減 / 拼接不要有缺口」。全部**純視覺**(不進 raycast、不動 `decks/cols/gradeCorridors/塔墩/splitWaterPieces`)。①**層次**:路面/標線材質加 `polygonOffset`(路面 −2 / 標線 −3,`envMat` 的 `...rest` 直通建構子,無快取污染)⇒ 橫坡路塹段(路面夾到 `hMax−CLAMP`)與地被同高不 z-fight,恆蓋過拼貼。②**走廊遮罩**(根治 3D 件戳穿):`buildBiomes` 新獨立線段索引 `rmGrid` + `roadClearAt(x,z)`(逐段**點到線段距離** + 半寬 margin;**MUST NOT 復用 rdGrid** —— 其 12m 稀疏取樣會讓窄路遮罩呈串珠縫,且動它會改 `roadDirAt`),傳入 `buildGroundCover`;`ground.js tryPatch` 在 `isBlocked` 旁、**首個 `rnd()` 之前**加 `roadClear` 拒絕 ⇒ 特徵拼圖不落路面、確定性序列不變(§2.3)。③**車道分級**:標線區由既有寬度**推導** `lanes = round(hw×2/3.2)`(單一縫,不硬編);單車道非主幹本就無中線、雙線道現行雙黃/單白虛、`lanes≥4` 加同向車道分隔白虛線(抽出 `dashLine(off)`,`off=0` 逐位元同舊中線)。④**路口填面 + ⑤縮減**:路面 Mesh 建立前掃 `nodeArms`(擴充記 `armHw[]`/`main`),`arms≥3` 補**扇形圓面**(R=節點最大臂半寬,補內角露地)、`arms==2` 近反向且半寬差 >0.6 補**梯形**(hw0↔hw1 收窄)—— 進路面桶(`bucketOf`,同色同材質故緞帶內部重疊不可見)、`classify` 傳 `mix=null`(同斑馬線迴圈)**零共享 rnd**;繞行朝 +Y 依緞帶截面推導(扇形 `(心,緣k+1,緣k)`、梯形反向)。橋/隧節點本就不入 `nodeArms`。**驗證**:`node --check` 雙檔通過 + 西門町 5v5 真開房(`osmRoads:true`、`roads:2119`、`decks:10333`/`towerPads:10`/`portals:0` 結構完好、pageerror 0)+ 六向空拍(路口填滿無角落露地、道路恆蓋地被、細節不戳路、主幹白虛線+路緣線、`yellowVerts:18156` 幹道雙黃、9 道路桶全帶 polygonOffset)+ `npm test` + `npm run bal` 全綠。**已知殘留**:幹道多為高架橋(`brg` 跳標線)故 4 線分隔線在此圖少見;主+次路口填面取主幹色,與次要臂 ribbon 有輕微色縫(路口統一鋪面觀感,可接受)。

**07-24 沼澤水平面 + 暗紫帷幕 + 兵線跨沼造橋**(`biomes.js` 新 `buildSwampSurface()` + `splitWaterPieces`/`buildRoads`/`markGradeCorridors` 加 `inclSwamp`、`game.js _updateWaterVeil`;**data.js/sim.js 未動**):使用者三條需求「沼澤跟水域一樣有水平線 / 視線低於水平線呈現暗紫色 / 兵線通過沼澤時也要造橋」。**唯一縫 = `swampY = waterY + WATER.SWAMP_BAND`**(= `terrainEnvCode` 沼澤分類界、`envTrigger` 沼面,三處共用同一數字,**MUST NOT 另寫**)。①**水平面**:`buildSwampSurface(group, terrain)` 在沼澤格(`terrainEnvCode===2`)上方 `swampY` 鋪暗紫濁沼半透明盤(`MeshToonMaterial` 0x4a3358/0.72/DoubleSide,~10m 格取樣、只出沼格不覆蓋水域/乾地 —— 水盤在更低的 `waterY`,紫盤蓋上去會把水變紫)。純視覺、不進 raycast(game.js 只打 `terrain.mesh`)、透明材質 outlinify 自動跳過、不耗共享 rnd。②**帷幕**:`_updateWaterVeil` 把「視線沉入」的判定線由固定 `waterY` 改成**依格分類取線**(水域 `waterY` / 沼澤 `swampY`)—— 眼位(最終 `camera.y`)低於該線即上色,水域藍(既有)、沼澤混濁紫→紫黑(`[96,66,128]→[22,10,34]`,越深越濃)。③**兵線跨沼造橋**:`splitWaterPieces(run, terrain, inclSwamp)` 的泡水判定加 `inclSwamp` 分支(`terrainEnvCode!==0`,水+沼;預設 `false` = 舊 `isWaterPt` 水域限定 ⇒ **所有既有呼叫端零影響**)。**只兵線路徑吃 `inclSwamp=true`**:`laneWetWays` 偵測、兵線補橋 `buildRoads` pass、`markGradeCorridors(laneWetWays,…,true)` + 離線 `roadInput=兵線` 的主 `buildRoads`/`markGradeCorridors`(`!osmRoads?.length`);真 OSM 道路維持水域限定(免濕地圖每條小徑長出短橋)。`markGradeCorridors` 由一趟拆兩趟(真道路 vs 兵線補橋各自 `inclSwamp`,`blocked` 兩趟累加同一 Set)。橋面 `deckAt` 抬升 `BRIDGE_RISE` 恆高過沼面 ⇒ 橋上機體不吃暗紫濁沼(電子失效/滯留同水域)。**沼面狀態(freeze/扣血)仍走 `envTrigger` 的 `SWAMP_EYE_F(1/2)`**,MUST NOT 為對齊帷幕改成 1(`eyeH≈SWAMP_BAND` ⇒ 改 1 幾乎全沼不觸發,破壞沼澤 gameplay 與「沼澤帶上緣 code=0」測試);帷幕是純表現層,吃實際 `camera.y < swampY`。**驗證**:淡水真開房 —— 沼面 mesh 2300 頂點 @ swampY 2.5、空拍見紫盤只出河岸沼帶不覆蓋藍水;`_updateWaterVeil` 5 例(沼眼下=紫 0.59 / 沼眼上=無 / 沼深沉=紫黑 0.77 / 水眼下=藍 0.47 / 水眼上=無);直呼 `buildBiomes` 塞一條穿沼兵線(乾→沼 36m→水)→ 沼段 x∈[134,172] 全被 deck 覆蓋(橋面 9.4~10.4 ≫ 沼面 2.5、沼地 1.42);倫敦 5v5 12 座墩座全對稱(泰晤士河岸沼帶兵線也升橋)、真水橋不受影響、pageerror 0;`npm test` + `npm run bal` 全綠。**已知殘留**:沼面 ~10m 方格取樣 ⇒ 沼/乾邊界略呈鋸齒(濁沼半透明,不明顯);沼面狀態門檻(EYE_F 1/2)與帷幕門檻(camera.y)不完全同進同出(中低位沼有「凍結但未變紫」窄帶,取視覺 WYSIWYG 優先)。

**07-24 一條路線上太靠近的兩座橋直接連成一座**(`biomes.js` 新 `chainWays()`(自 `mergeGradeChains` 抽出的共用走訪)+ 新 `joinWaterRouteWays()` + `splitWaterPieces` 合併門檻;**data.js/sim.js 未動**):使用者需求「兩座橋太靠近導致下橋又突然上橋 ⇒ 直接連在一起」。**兩層根因,各修一刀**:①**同一 run 內**兩段泡水:`splitWaterPieces` 舊制只在引道區間**重疊**時併(= 水段間距 ≤ 2×RAMP_M),間距 48~96m 時留下一段短乾路 → V 形谷。改判「兩跨之間的乾地塞不下一趟下坡 + 上坡」(`JOIN_M = WATER.RAMP_M × 2`,**推導值 MUST NOT 手寫**)即併;但**中間乾地高過 `BRIDGE_RISE` 一半就不併**(那是真陸地/丘陵,併了橋面會爬上山脊)。②**跨 way**(威尼斯實測的真兇):OSM 把一條連續街道切成多條 way,而 `splitWaterPieces` 是**逐 way** 判泡水 —— 夾在兩段泡水 way 之間的短 way(實測 17m / 23m / 34m)自成乾段,兩側各建一座橋。新 `joinWaterRouteWays()` 在 **`roadInput` 定案之前**(`markGradeCorridors` 與 `buildRoads` MUST 吃同一份)把「共用端點、順向(dot ≥ 0.5)、同 `highway` 分級」的 way 串成一條,泡水段落進同一個 run 後由 ① 一併吃掉,`deckAt` 剖面也只在真正的兩端起降。**只在接點兩側至少一條沾水時才串,且一條都沒串到就原樣回傳** ⇒ 無水地圖的 way 陣列逐項不變(rnd 序列 / 建物植被佈局零影響)。橋/隧道 way **不在候選內**(已由 `mergeGradeChains` 併鏈,且 `way._tun` 的逐 run 索引不可打亂)。`chainWays(ways, canJoin, branchDot)` 新增 `branchDot`:橋隧鏈維持「節點恰 2 條候選才續行」(平行雙孔共用洞口節點保守不併),跨水路線則允許在分岔節點取 dot ≥ 0.85 的**直行續段**(街道 T/十字路口);degree 2 時兩者逐項同舊版。**驗證**:威尼斯 5v5 真開房 —— 同路線「頭尾 <90m 且同向」的橋對 **5 → 0**、橋數 45 → 34、decks 1058 → 1104(舊乾段改為橋面),舊接縫沿線橋面剖面實測**恆 8.28m 平直**(舊制為降到地表 −0.3 再爬升);倫敦 5v5 墩座/塔位/decks 不受影響、剩 1 對間距 87.8m(真有一段路,非 V 谷);**金龍隧道 portals 8 + 天花段 180 與改制前基準逐項相同**(= `mergeGradeChains` 抽出 `chainWays` 無回歸);`npm test` + `npm run bal` 全綠。**已知殘留**:分岔節點若無 dot ≥ 0.85 的直行續段(真正的 Y 形岔路)仍維持兩座橋。

**07-24 兵線砲塔跨橋墩座(與橋重疊的砲塔改蓋在橋面上)**(`biomes.js` 新 `buildTowerBridgePads()` + `main.js terrain.towerPadY` + `game.js _addEnt`/靜態實體每幀落位;**data.js/sim.js 未動** ⇒ bal/e2e 不受影響):使用者需求「兵線上的砲塔如果與橋重疊,則直接蓋在橋面上,底部是可支撐砲塔的橋墩,有凸出橋的範圍」。舊制砲塔一律 `heightAt` 貼地 ⇒ 兵線跨水段的塔沉在橋面之下(河床 / 07-22 乾地帶抬出的土墩),與走在橋上的兵線分家。**唯一縫 = `biomes.buildTowerBridgePads()`**:塔位走 `solveTowerSites`(與 `sim._spawnStructures` 同一支 ⇒ 客戶端墩座與伺服器塔位逐點對得上)、橋面走 `roadRes.decks`(真橋 + 兵線補橋皆已併入,故 **MUST 排在兩次 `buildRoads` 之後、`blockers.push(cols)` 之前**),輸出 `userData.towerPads` → `terrain.towerPadY` → `game.js` 落位(`ent.padY`,`_addEnt` 與靜態實體每幀重擺**共用同一份**);**MUST NOT** 在 game.js 另寫一套「塔在不在橋上」的判定。**判定錨在兵線中心點,不是逐塔找最近橋**(倫敦實測:逐塔搜尋讓同塔位左右塔各自吸附到相鄰的平行橋,橋面差 5.6m 一高一低)—— 中心點須真的落在橋面內(`|lat| ≤ hw`)+ 橋段方向與兵線切線 `|cos| ≥ 0.6`(濾掉剛好平行經過的別條橋)+ 縱向容差 1m(同 `makeDeckIndex` LONG_TOL,橋頭之外不算重疊)⇒ 兩座塔共用同一段橋 = 對稱同高。幾何:台面(`TOWER_PAD_R` 10.5 > 塔基外接 ~8;自橋緣 `hw−0.6` 伸出到 `lat+R`)+ 擴張墩帽(上寬下窄錐)+ 墩身(上窄下寬錐)落地埋 0.6。**台面 MUST NOT 往橋心多伸**(舊版取 `lat−R`):橋面路面 quad 就畫在 `deckAt`,同高疊上去 = 整條車道 z-fighting;繪製另沉 `TOWER_PAD_SINK`(2cm,非共面),**站立面仍記 `dy` = 橋面高 ⇒ 上橋→上台面無台階**。台面進 `decks`(線段沿橋法線、`hw = R` ⇒ 站立矩形 2R × 伸出長)、墩身進 `cols`(**柱頂封在台面底緣**,同橋墩紀律 —— 封到頂面的話站台上的機體 `myBot == 柱頂`,`_collide` 垂直閘不跳過 → 被隱形柱側推下橋)。純幾何不消耗共享 rnd ⇒ 建物/植被佈局序列不變。**驗證**(倫敦 5v5 真開房):8 座墩座、24 座塔中 8 座 `padY` 命中且 `mesh.y == padY == deckY == surfaceAt`、同塔位左右塔同高(3.82/3.82、9.91/9.91、3.77/3.77、3.62/3.62);自橋心沿法線行走取樣 0→26m 恆 9.91、27m 才落地(= 台面外緣)⇒ 上橋走到塔基連續;pageerror 0;`npm test` + `npm run bal` 全綠。**已知殘留(評估後接受)**:①07-22「兵線砲塔外接寬度強制乾地」仍會在河中抬出土墩(terrain 早於 biomes 建,拿不到橋資料);②陣營光環 `r ≈ 14.3` > 台面 10.5,邊緣約 4m 懸空(貼地決標,與斜坡地形既有行為一致);③兩橋側向重疊處 `deckY` 取最高面 ⇒ 台面的站立高度會被上層橋蓋過(倫敦實測 1 例,落差 0.77m)。

**07-24 FPV 座艙取景規則(視野不擋視線 / 各件壓在 HUD→準星 2/3 線下 / 裝置小於武器 / 透視消失點在準星)**(`game.js` 新 `COCKPIT` 常數 + `ndcH()` + `_mountCockpitWeapon` framed 二分解 + **`_frameCockpitStruct` 結構後處理**、`_cockCanopy`/`_cockFixed`/`_cockRotor`/`_cockNeck`/`_cockAvian` 取景改制、新工具 `tools/audit_cockpit.mjs`;**data.js/sim.js/models.js 未動** ⇒ bal/e2e/audit_muzzle 全不受影響):使用者兩輪 FPV 需求。**唯一真相 = `game.js COCKPIT`**(`SIGHT_DEG 11`/`AREA_MAX .21`/`WPN_AREA_MAX .12`/`TOP_NDC = (−1+2·HUD_BOTTOM_F)/3 ≈ −0.187`/`WPN_BOX_MAX .042`/`DEV_AREA_MAX .035`/`VP_Z 25`/`VP_TOL_DEG 12`);取景換算唯一縫 = `ndcH(z)`(深度 |z| 處、NDC 半徑 1 對應世界半高 = |z|·tan34)。

**規則②頂緣天花板(第二輪核心)**:每件頂緣 MUST ≤ `TOP_NDC` = HUD 下帶上緣(−0.56)→ 準星(0)的 **2/3 處** ⇒ 準星上方 1/3 恆淨空、座艙壓在畫面下段。武器與結構**共用同一條線,MUST NOT 分家**。**頂緣量測 MUST 逐頂點投影,不是 AABB** —— VP 旋轉後的長槍/傾斜肩莢實渲染頂緣比軸對齊盒高 ~0.05,AABB 會漏(+0.016 = 描邊殼著色器外推,不進幾何)。武器:framed 二分解內 `wrapTopNdc()` 逐頂點;結構:`_frameCockpitStruct` 逐頂點下移。**規則③裝置面積**:與武器/招式無關的**裝置**每件 ≤ `DEV_AREA_MAX .035`(**< WPN_BOX_MAX**,裝置恆比武器小)—— `_frameCockpitStruct` 對非武器頂層件等比縮小;武器持槍機構(`cockStruct` 標記)豁免面積(它是武器的一部分),只吃頂緣/準星錐。**規則①視野**:準星錐 `SIGHT_DEG` 半角零遮擋 —— 武器 framed 二分解、結構/機構 `_frameCockpitStruct` 沿離軸方向推出(置中件直接下沉)。**規則④消失點**:武裝轉朝視軸 `VP_Z` 公尺處(螢幕 = 準星),軸線 = 量體中心 → 最遠槍口,阻尼迭代 `slerp DAMP=0.6`(細長 L 形量體直接套完整修正會震盪);**`_frameCockpitStruct` MUST NOT 平移武器 wrap** —— 平移會改變砲管軸線與消失點夾角、破壞 VP(t12 實測歪到 89°),頂緣全交給 framed 二分解在**縮小**時一併壓下(縮小保持 VP 方向)。

**`_frameCockpitStruct(g)` 是結構取景唯一縫**(MUST 在 `outlinify` **之後**跑:描邊殼掛各 mesh 底下、外擴頂緣 ≈0.012m,包圍盒要含殼才量得準);件彼此獨立掛容器(非骨架鏈)故逐件平移/縮放不拆關節。**共用儀表台原本單件吃 26.4% 畫面**(舊 1.7×0.28 @ −0.85 頂緣 −0.40)→ 收窄下沉;`_cockFixed` 重複的第二片儀表台已刪;手持機構只留「前臂+手」(雙持兩整臂 = 使用者「手部超出太多」);hand 錨 `s` 1.3~1.4→1.1~1.12。**FPV 武器轉朝 VP 只改視覺剪影**:彈道仍自 `_muzzle` 沿 `_resolveAim`(準星)射出,與 mesh 朝向解耦 ⇒ 所見即所射。**驗證**:`node tools/audit_cockpit.mjs`(真 WebGLRenderTarget 讀回 alpha 量遮擋 + 逐頂點頂緣 + bbox 裝置面積;40 例六規則全綠,`--parts` 逐件揪元凶)+ 12 例截圖疊準星錐目視 + `npm run bal`/`npm test`/`audit_muzzle` 全綠(客戶端 FPV 專屬,權威層未動)。

**07-23 跳躍脫離地形效果 + 地形異常狀態改「視線高度制」**(`data.js` 新 `AIR`/`jumpApex`/`airUnitY`/`offGround`/`airUnit`/`envTrigger` + `TERRAIN_FX.*_EYE_F`、`game.js` 新 `_eyeH()`/`_envAt` 改寫、`sim.js` `_tickHazards`/`_tickMines`):使用者五條需求。**兩條高度線語意不同,MUST NOT 混用**:①**`offGround(kind,y)` 離地**(ε=`AIR.OFF_GROUND` 0.05)→ 跳躍/蓄力跳躍騰空期間**不吃地面傷害**(火場走伺服器 `_tickHazards` 豁免;水域/沼澤走客戶端 `_envAt` 回報 `wet=0` ⇒ 伺服器凍結/扣血自動停,不必加封包欄位);②**`airUnit(kind,y)` 空中狀態** = `y > airUnitY(kind) = jumpApex(UNITS[kind].jump)`(robot 實測 0.4219m)→ 視同空中單位**不踩地雷**。**普通小跳頂點(實測 0.38~0.40m)刻意低於此線 ⇒ 小跳跳不過雷區,只有蓄力跳過得去**;門檻 **MUST NOT 手寫**(改 `UNITS[].jump`/`COMBAT_SCALE`/`AIR.GRAV` 即同步),`AIR.GRAV` 同時是 `game.js` 跳躍重力的唯一來源(舊版硬寫 24)。適用機種限 `AIR.KINDS`(robot/morph);**飛行機種照舊吃 `HAZARDS.fire.maxY` / `GAME.AA_MIN_ALT`,MUST NOT 併進來**(無人機 y=10 仍該被火場灼傷 = 平衡不動),morph 踩雷仍走 `MORPH.GROUND_Y`(飛行型可低空懸停,套 0.42m 會讓懸停免雷)。**地形異常狀態改視線高度制**(`data.envTrigger`,唯一縫):舊制「踩到水/沼格子就觸發」讓 0.27m 淺灘也凍結電子系統,與「眼位沒入水面才變色」的水下帷幕對不上;新制水域要 `眼位 < waterY`、沼澤要 `眼位×SWAMP_EYE_F(1/2) < waterY + WATER.SWAMP_BAND`(沼澤面 = `terrainEnvCode` 的分類界,**MUST NOT 另寫數字**)⇒ 跳躍把眼位抬離水平面的那段時間狀態自然解除,不必另設計時器。`_envAt` 回傳擴為 `{code,depth,ground,air}`:**`ground`(腳下地表)驅動涉水/陷沼減速、`code`(異常狀態)驅動 wet 回報/帷幕/沼澤滯留** —— 兩者 **MUST NOT 互換**(淺灘照樣涉水變慢,只是不再電子失效)。`_eyeH()` 是眼位唯一縫(相機 eye 與狀態判定共用,含蓄力下蹲 `CROUCH_M` ⇒ 蓄力時眼位真的會沉進水面)。**實測(倫敦泰晤士真開房,同步泵幀迴圈)**:深水腳下 −4.70/眼 −1.83 → code 1;淺灘腳下 0.03/眼 2.90 → code 0(舊制會凍結);沼澤 h=0.98 → code 2、h=2.29(沼澤帶上緣)→ code 0;四種地形 tap/charge 跳躍期間 air 幀 code 全 0。**另證:水域/沼澤的跳躍/蓄力跳躍原本就沒有被擋**(同一組迴圈量到 tap 0.38m / charge 26.4m,與乾地逐項相同)—— 使用者回報的「無法跳躍」是**跳了也脫離不了狀態**(深水沒頂 5m,小跳 0.38m 抬不出水面),修的是狀態面不是物理面;**MUST NOT** 為此去動 `onGround`/`gy` 的水中有效地板(那條是 07-19 可涉水改制的地基)。`npm test` + `npm run bal` 四不變式 + headless 13 項閘門斷言全綠。

**07-23 榴彈火控解 + 命中量體(拋物線改真彈道學、光暈只在對準時亮、打哪個部位就在那裡結算)**(`data.js` `BALLISTIC.LOB_*`/新 `hitH` + 高度表移入、`game.js` 新 `_lobAim`/`_arcTrace`/`_lobSolve`、`sim.js` 新 `_bodySpan`/`_bodyDy`、`server.js` tracer 加 `mv`):使用者四條需求。

① **拋物線改火控解(唯一縫 = `game.js _lobAim()`,MUST 排在 `_tickWeapons` 之前)**:`trajClass(def)==='lob'` 的重武器不再「沿準星直射 + 玩家自己抬仰角」——每幀取準星射線交點當瞄準點,解出**命中該點的拋射角**(`_lobSolve` 兩根解,取低伸解),`_tryFire` 的出膛向量、瞄準虛線 `_updateArcGuide`、鎖定光暈 `_tickLock`、FPV 砲管仰角(`gunGroup.rotation.x += fc.sup`)**全部消費同一份 `this._lobFc`**;**MUST NOT** 在任何一端另解一次(兩份會分家)。⇒ 弧高/出膛角/飛行時間隨距離自然改變(實測 109m 弧高 1.5m、172m 3.7m、40m 0.2m),不再是一條與準星綁死的固定曲線。**越過遮蔽物走「降裝藥」不走「高角度解」**:低伸解被地形/障礙截斷(`_arcTrace` 回傳 `cut==='block'`)就依 `BALLISTIC.LOB_CHARGE [1, .78, .6, .46]` 逐級降初速(命中同一點所需仰角自動抬高、弧線變高 = 真實榴彈砲選裝藥號數);**MUST NOT** 改用同初速的另一個根 —— 現尺度下那是 85°、飛行 20 秒的迫砲彈。

② **光暈只在拋物線對準時才亮**:`_arcTrace` 回傳 `minD`(彈道與瞄準點的最近距離);`minD > BALLISTIC.LOB_TOL(3.5m)` = 未對準 ⇒ `_tickLock` 對 lob 武器**不送 lock**、虛線轉警示紅。使用者原始症狀「射程光暈亮著卻沒命中」= 舊制拿**準星直射線**判鎖定,而彈道是拋物線(170m 落差 14m)。實測:合成 22m 稜線擋在中途 → 裝藥降到 46 仍 `ok:false`、`lock` 送出 0 次;移除稜線立刻回 `ok:true` 並送鎖定(無殘留狀態)。

③ **對空彈射「更快更直」**:`AA_MV` 340 → 720,實際初速取 `min(武器真實 mv, AA_MV)` = **全裝藥直射**(152mm 650 / 無後座砲 435 / 集束彈 400;溫壓火箭真實初速本就只有 120)。瞄準點改取飛行目標的**機體幾何中心**(沿用 `_updateAaMode` 掃到的 `_aaEnt`,不另掃)。實測 105m 弦長弧垂 **0.03m**(吊射同距為 3.97m)。

④ **命中量體:打到哪個部位就在那裡結算**(`sim._bodySpan/_bodyDy` + `data.hitH`):舊制 `_blast` 一律量到單位**底部**(使用者說的「最底部光圈」)—— 26m 的塔被打中塔身中段只吃 22% 爆風、10m 機甲被爆頭同樣被當成十幾公尺外。改成量到**機體垂直帶 [腳底, 腳底+hitH]** 的最近距離(帶內 = 0 = 全額);`_lanceHits` 的垂直帶同步從單點 `_tgtY` 改成整條帶。為此把 `SOLDIER_H`/`HERO_SIZE`/`heroTargetH`/`TARGET_H` **從 `models.js` 移進 `data.js`**(models.js 只 re-export,呼叫端不動):渲染縮放與命中量體 **MUST 是同一把尺**,分家就是「看得到打不到」。`npm run bal` 不受影響(balance.mjs 只用 `dmgFalloff`,爆風本就以直擊全額建模)。

⑤ 連帶:他人畫面的彈體改吃 tracer 新欄位 `mv`(射手實際初速)+ 瞄準點當 `to`,兩端弧線一致(舊制送「槍口前 60m」示意點,對方會吊到射手沒瞄的地方);NPC 榴彈兵/坦克的 `_arcTracer` 也從 `h = 射距×0.22` 的裝飾曲線改成同一支 `_lobVel` 真解(取打得到的最低裝藥)。**效能**:`_lobAim` 每幀要一次 `_resolveAim`(地形 7.3 萬面 + 全場機體),故加「鏡頭/槍口幾乎沒動就沿用上一幀瞄準點」快取(60ms / 0.999998 dot / 0.15m)—— 靜止架砲 3.7ms → 1.0ms;掃視時的成本 100% 來自 `_resolveAim`(彈道積分只佔 0.33ms,且實測平均只跑 1 次 trace,降裝藥幾乎不觸發)。**驗證**:headless 直測命中量體 6 項(腳/胸/頭同額、1.9r 外歸零、塔頂 = 塔底、貫穿垂直帶)+ 瀏覽器真開房冒煙(真 `_tryFire`:瞄塔身 3.5/12/20.8m 三處,爆點各自落在 3.7/11.6/19.9m、彈體出膛向量與 `_lobFc.vel` 位元相同、出膛角 4.6°/9.3°/13.5°;pageerror 0)+ `npm test` + `npm run bal` 全綠。

**07-23 重武器範圍攻擊三分類 + 彈道五分類(直線貫穿上線)**(`data.js` 新 `aoeClass`/`trajClass`/`LANCE`/`ARMING`、`sim.js` 新 `heroLance`/`_lanceHits`、`server.js` 新 `lance` 路由、`game.js`/`vfx.js`/`main.js`):使用者定案「**重武器必屬於其中一種範圍攻擊**」——沒有單體直擊的重武器。**唯一分類縫 = `data.js aoeClass(def)` / `trajClass(def)`**(推導自 `def.type`/`fan`/`guide`,**MUST NOT 手寫逐武器表**,也 MUST NOT 在 sim/game/HUD 各自比對 type)。分類結果(32 角)= 爆炸 13(launcher 7 + missile 6)/ 直線貫穿 14(rail 5 + beam 4 + gun 5)/ 扇形 5(plasma);彈道 = 低初速拋物線 4 / 高初速近似直線 10 / 完全直線 9 / 雷射導引 3 / 射後不理 6。`aoeClass` **只約束重武器**(`def.id==='heavy'`),非扇形輕武器 29 支維持單體 `heroHit` 不變。

三分類中**只有「直線貫穿」是新結算**(爆炸走既有 `_blast`、扇形走既有 `heroPlasma`):新增 `sim.heroLance(pid, o, d, len)` —— 客戶端回報射線(`o=[x,z,y]` 槍口 sim 座標、`d=[dx,dz,dy]` 3D 單位向量、`len` 射線長,已被本端地形/障礙截斷),伺服器沿 `lanceR(def)` 圓柱(beam 3.6 / rail 2.8 / gun 2.2 m)結算**全部**目標,由近至遠套 `LANCE.DECAY(0.75)^i`、上限 `LANCE.MAX(6)`。**首個目標恆為全額** —— 單體 DPS 與舊 `heroHit` 位元相同 ⇒ `npm run bal` 四不變式(只模型化 1v1)不受影響,**MUST NOT** 改成首發也衰減。迷霧 `_visibleTo` + `_losBlocked` 逐目標把關(同 `heroPlasma`);槍口離本體 >20m 靜默丟棄(防作弊)。**幾何近似(刻意)**:圓柱判定取**水平垂距 + 垂直帶 `R×VBAND_F(2.2)`**,不是純 3D 垂距 —— 伺服器無地形高程、y 語意是「離站立表面高」,射線絕對高度算不出來(射手站山頂打山下,3D 垂距會整條落空);近乎垂直的射線(`hd<0.15`,仰角 >81°)水平投影退化,`_lanceHits` 另走「高度差當軸向」分支。bot 走 `botFire` 的 line 分支共用 `_lanceHits`(主目標已全額結算故跳過,貫穿序不變)。**來襲防空飛彈的擊落併進 `heroLance` 的圓柱掃描** —— line 類一發只過一次 `_gateFire`,客戶端 **MUST NOT** 對 line 武器另送 `hitMissile`(會重複扣彈藥/電力)。

客戶端:重武器 beam 走 `_resolveAim` 後直接送 `lance`;rail/gun 走**貫穿彈體**(`b.pierce`:`_updateBullets` 讓單位/飛彈不終止彈道,只有地形/障礙/射程終點才終止),**落點定案才送 lance**(以「槍口→終點」直線圓柱近似;flat 彈道 mv 900~2500、射程 ≤400m ⇒ 下墜 <0.4m)。`b.oy` **MUST** 在擊發當下取樣(動能彈飛 0.1~0.4s,拿落點那幀的 `_altAG` 會漂)。演出唯一入口 `_lanceVisual(from,to,def,side,barrage)` —— 自機 / 他人 `onTracer` / bot `shot` 三處共用,**圓柱粗細 = `lanceR` ⇒ 看到多粗就是打到多粗**,不是裝飾性放大。**軌跡修正期(最短距離)**:`ARMING`(guide 45m/0.055rad、fnf 60m/0.075rad)—— 離架時一次性 `_armSpread` 圓錐偏角,`b.dist < b.arm` 前導引/追蹤**不接手** ⇒ 貼臉開導引彈會偏(使用者規則「最短距離內命中率較低」);純客戶端(伺服器不模擬彈道)。`guide` 類另有 FPV **導引雷射** `_updateGuideLaser`(單一持久 Mesh 以 position/scale/quaternion 驅動,**MUST NOT 每幀重建幾何** —— 同 `_arcGuide` 紀律;環在最短距離內轉黃並脈動示警),且與拋物線虛線 `_updateArcGuide` **互斥**(騎波後不吃重力,再畫拋物線是錯的指示)。

演出(使用者指定參考):`vfx.gundamBeam()` 鋼彈式 mega beam(熾白內芯 + 飽和外暈 + 槍口衝擊環 + **沿軸行進的能量環**)、`vfx.ionBreath()` 哥吉拉式離子吐息(**槍口最粗、末端收束**的錐狀噴喉 = 扇形「越近越強」的可視化 + 半徑大於噴喉的螺旋纏繞能量帶 + 放電弧 + 雙端綻放)。離子吐息掛在 `_fanBlast`(自機)與 `plasma` 事件(他人)**同一支**,**MUST NOT** 各寫一套。**驗證**:headless 直測 16 項(貫穿衰減 0.750/0.562 逐項對上公式、離軸/背後/垂直帶/垂直分支/型別閘門/防作弊/一發一扣彈)、瀏覽器冒煙 7 型別(真 `_tryFire` → 訊息形狀 + 特效件數 + 特效跑完零 NaN 零殘留 + 導引雷射顯示與 `trajClass` 一致,pageerror 零)、`npm test` + `npm run bal` 全綠。

**07-23 射擊感震動 + 震波範圍 + 榴彈對空彈射模式**(`data.js` 新 `SHAKE`/`BALLISTIC` 擴充、`game.js`):使用者兩條需求。①**發射震動**:重武器擊發的 trauma 由 `0.03×kick`(0.07~0.14,肉眼幾乎無感)改吃 `SHAKE.FIRE×kick×HEAVY_F(3.0)` = 0.22~0.41,傾洩窗內每發再 `×BARRAGE_F(1.3)` 疊成巨炮轟鳴;三種狙擊長按機種招統一走 `SHAKE.KAMI/DECOY/BARRAGE`(**餌機原本完全沒有震動** —— 補在 `decoy` 事件而非 `_launchDecoy`,伺服器確認分離才震,CD 被拒不誤震;kami 由 `trauma=0.6` 賦值改累加,不再洗掉既有震動)。輕武器倍率 1.0 手感不動。②**震波作用範圍 = 該武器攻擊半徑**:`_applyBlast` 的 `R = max(20, r×3)` 改 `r × SHAKE.BLAST_F(1.0)`,`r` 即伺服器 `boom` 事件帶的 `wp.def.r`/`A.r`/`DECOY.R`/`M.R`。**MUST NOT** 加回固定下限或倍數放大(使用者指示震波不可無限遠傳遞);爆點核心強度公式未動 ⇒ 近炸手感不變,只是邊界從 3r 收到 r。**已知落差**(評估後接受):sim 的 `blastFalloff` 傷害延伸到 1.8r,故 1.0~1.8r 有「吃得到傷害但無震感」帶 —— 要對齊改 `BLAST_F` 一個常數即可。③**榴彈對空彈射模式**:launcher 準星錐(`BALLISTIC.AA_CONE` 0.14rad)內掃到飛行類目標(`TARGET_CLASS 'air'`:drone/heli/decoy)→ 初速由 `LAUNCH_MV`(100)換 `AA_MV`(340),彈道拉成高速近直線(拋物線吊射打不到會動的飛行單位)。**唯一判定縫 = `_updateAaMode()`,MUST 排在 `_loop` 的 `_tickWeapons`(擊發)之前**,擊發 `_tryFire`、瞄準虛線 `_updateArcGuide` 消費同一份 `this._aaAim` ⇒ 所見即所射;**MUST NOT** 在擊發處另做一次掃描(兩份會分家)。`_aaTarget` 逐幀掃全場故先過 `TARGET_CLASS` 再算角度、純量運算不配置向量,並複用 `_obstHitT` 遮擋判定。他人視覺彈體(`_spawnVisShell`)拿不到對方準星,改以「落點離地 > `AA_ALT`(10m)」推定對空(`_lobVel` 初速換高即自然拉平)。虛線改冷色 `0x9adfff` + 落點環畫在**空中攔截點**(對空時投影到地面會落在遠方山腳,讀不出交會位置)。射程/傷害/彈藥/伺服器驗證全不變(`heroBurst` 只驗 2D 射程 ×1.15 與 y 夾制)。**實測**(t01 152mm 榴彈砲真開房):吊射 61 點 chord 175m **弧高 3.86m** vs 彈射 18 點 chord 173m **弧高 0.30m**(13× 平);擊發彈體 mv 100 → 340;目標離錐/出射程/陣亡即自動退回吊射;`_loop` 泵幀零例外。`npm test` + `npm run bal` 全綠(伺服器 `sim.js` 未動)。

**07-23 砲塔洞口射程規則 #5(隧道內的塔至少 20% 射程涵蓋洞口外)**(`data.js` `GAME.TOWER_TUNNEL_OUT_F` + 新 `towerTunnelAudit()`/`tools/bake_venue_lanes.mjs`/`tools/audit_lane_grade_sep.mjs`/重烤 `venueLanes.js`):使用者新增兵線規則。動機 = 隧道側牆對 LOS **全擋**(`sim._slabBlocked` ①,只有沿軸出洞口放行),埋在山體深處的塔只能沿 9m 洞內走廊對射;現尺度 `tower.range` 155 < 深塔距洞口 161 ⇒ **連洞口都打不到**,而敵方同一座隧道的淺塔(距洞口 37m)照常壓制 = 單方面失守。判定:塔沿兵線到最近洞口的弧長 `d ≤ range × (1 − 0.2)` = 124 遊戲公尺。**唯一結算縫 = `data.js towerTunnelAudit(lanes, tunSpans)`**(內部照跑 `solveTowerSites`,與 #4 同框;`tunSpans[li]=[[s0,s1]…]` 為該線隧道弧長區間,遊戲公尺)。**MUST NOT 改成執行期挪塔**:覆蓋區間 = 圖資 tunnel way × 地形高度(`biomes.tunnelCoverIntervals`),開局時 `solveTowerSites` 只有兵線、伺服器更沒有地形 ⇒ 兩端拿不到同一份洞口資料,塔位會分家(規則因此定位為**選線期**判定,與 #4 的烘焙偏好同一層)。烘焙 `tryBearing` 詞典序改 **`tunBad` → `resid` → `score`**(洞內塔違規比重疊殘餘嚴重;無隧道場地 `tunBad` 恆 0 ⇒ 排序退化為舊版,選線結果不動)。稽核併入 `audit_lane_grade_sep.mjs`(已有完整節點路徑重建 = 現成的隧道邊來源,不另立工具);隧道段取**圖資 way 全長 = 保守上界**(執行期只有地形蓋得住的段落成洞,平坦市區 tunnel way 根本不立結構 —— 見 07-16 備忘)。**實測(改前)3 處違規**:jinlong L1 前塔(SWARM)159m、tamsui L1/L2 後塔(SWARM)135/134m;重烤後**同錨點換方位角**即全合規(jinlong 70°→65°、tamsui L1 85°→335°/L2 60°→65°),22 場地只有這兩個 venue 的兵線變動。**執行期冒煙(jinlong L1 真開房)**:`userData.portals` 8 座 + 天花段 180(真山體隧道成立),四座前塔距最近洞口 **31.5~40.1m**(門檻 124m)、後塔在洞外 229~259m;另實測**覆蓋區間遠短於圖資 way**(SRTM 平滑後山體只蓋得住兩段 ~50m,中間是敞開路塹)⇒ 離線稽核的上界比實況嚴格,合規判定安全(門洞取自 OSM 抓取成功那次;塔位跨三次重開逐點相同,Overpass 連跑會限流見 07-16 備忘)。`audit_map_rules`(#4 殘餘 0/疊塔 0)、`audit_lane_sep`、`npm run bal`(不變式 ② 186~218m/0 對射)、e2e 全綠。**已知缺口**:自訂地圖(mapSelect)套不上此規則 —— OSRM 兵線無 tunnel tag,選址期沒有隧道資料(要補得在候選掃描多打一次 Overpass,違反 §2.4 的外部服務紀律)。

**07-23 隧道洞口透明化(洞外直接看見真實內部,黑板暗面退役)**(`terrain.js punchPortalHoles`/`biomes.js buildRoads` collar/`ground.js` 無改動):洞口望進去原本是**一面土牆** —— 高度場先天無法上下交疊,隧道採「真・下沉」⇒ 覆蓋轉換處的地表必須從山高降到路面高,那道**一格寬(~8.3m)的拉伸陡面**橫在天花以下、兩側牆之間,側牆天花都遮不到,舊版只能靠一片只朝外的黑色暗面正面擋住(= 使用者看到的「洞口一片黑」)。三刀修法:①**`terrain.punchPortalHoles(bores, covers)`**:把戳進隧道斷面的三角形從**繪製 index** 移除(就地壓實 + `setDrawRange`),`heights[]`/`position` **完全不動** —— heightAt/碰撞/LOS/bakeWetGrid 全讀陣列不讀三角形 ⇒ 純視覺開洞,單位照樣走不過山、砲火照樣打不穿(實測 heightAt 逐點與改前位元相同)。刪除條件**三合一,缺一不可**:任一頂點在走廊內(|側offset| ≤ hw、進洞 0.5~depth)∧ 有部分高過路面(全在路面下 = 被路面緞帶蓋著,留著)∧ 有部分低於天花(**全在天花之上 = 山體本體,MUST 留著** —— 拿掉這條就是高空俯瞰破頂)。②**collar 漏斗裙**(biomes,門洞同材質 `envMat(0x9a958c)`):外環 = 洞緣頂點**本身**(與地形共用座標 ⇒ 逐點水密),內環 = 把洞緣點夾制到斷面矩形(±(hw−INSET)、路面~天花−INSET;縱向也夾在 −depth~−0.2,投到管外就是貼到不存在的面)⇒ 視線不是打到地形、就是打到 collar、再不然就進管內看見隧道,沒有第四種可能。**DoubleSide 是刻意的**(與暗面那條 MUST NOT 不衝突):collar 恆在管身**之外**、幾何上不可能橫跨斷面 ⇒ 不會變成出洞黑牆;單面若有一片繞行判錯就是一個看穿的洞,取水密不取單面。③**暗面退居備援**:只有 `touched[pi]` 為 false(打洞落空)才掛回那片黑板。**三個踩過的坑**:(a)**地被是獨立圖層** —— 只挖地形的話洞口仍是一坡貼在崖面上的草皮拼圖,故 `buildGroundCover` 新增的 children 切片(`gcStart`)MUST 一併送進 punch 吃**同一把尺**(Mesh 打洞 / InstancedMesh 縮零;實例幾何共用,**MUST NOT** 動 InstancedMesh 的 index);(b)**平行雙孔隧道**兩座門洞僅相距 3~6m、bore 大幅重疊,重疊區三角形只會被第一座認領 ⇒ 判「打洞成功」MUST 用 `touched`(**含被鄰座認領的**)而非 `rims`,拿 rims 判會讓第二座掛回黑板、那片黑板正好擋在第一座洞口後面(實測 jinlong 8 座有 2 座中招);(c)洞緣算完 MUST 在 index 壓實**之前**取(邊只被一個被刪三角形用到 = 洞緣)。**驗證**(jinlong L1,8 座門洞):洞外見內部/洞內往外見天空與市景/高空俯瞰只露天花板頂面;terrCut 46 + coverTri 232 **重開兩次逐格一致**(純幾何無 rnd);`userData.portals` 恆 8 座、位置與改前相同;e2e + `npm run bal` 全綠。**同批後續(洞內看得見了才發現的兩個老問題)**:①**結構隧道恆柏油** —— 路面 biome 取「中點衛星像素」分類,隧道中點取樣落在**覆蓋段上方的山體**(森林/裸岩)⇒ 判成綠地/裸露地、鋪成泥土路(金龍隧道實測);隧道是工程結構物,`if (strc) biome='urban'`(與既有「橋段取到水色 → urban」同一道理)。②**隧道畫標線** —— 舊版 `!strc` 整段跳過標線,因為 `putMark` 貼地取樣會把標線畫到覆蓋段上方山頂;修法是給 `putMark`/`emitLine` 開 `yB`/`yBAt` 基準高參數(結構隧道傳 `tFloorAt(cum[i])`,與路面緞帶同源;`lift2` 0.56~0.58 疊上去 = 高於路面 0.11~0.13,與地面道路一致),不是放寬 guard。**路燈仍 MUST `!strc`**(燈桿會戳穿天花板與山體,洞內照明走天花燈)。

**07-23 大型障礙物掃掠防穿透(建物/神木/巨岩不可任向破圖)**(`game.js _collide`/新 `_sweepBlockers`):使用者需求「建築/神木/巨石等大型障礙,玩家不可由各種方向破圖穿透,頂部同橋梁可站立」。稽核既有系統:三型障礙的**水平碰撞**(建物有向盒 Minkowski / 神木·巨岩·橋墩圓柱 push-out)與**頂面站立**(`std`/`bld` 旗標 → `makeBlockerTopIndex` → `surfaceAt` mount 台階,2026-07-22 已上線)皆完備 —— 缺口是**離散 push-out 的單幀穿隧**:高速擊退(爆炸 `power` 可達 55)/ 飛行機 / 掉幀大 dt 讓「位移終點」落在障礙**另一側**,終點無重疊 → push-out 偵測不到 → 破圖。修法兩刀:①**掃掠防穿透 `_collide(px0,pz0)` → `_sweepBlockers`**:沿本幀位移 P0(px0,pz0)→P1(pos)對每障礙做「線段 × 圓柱 / 線段 × 有向盒(local slab)」,求**首個真正被橫越**障礙的進入參數,夾 pos 於前緣(留 SKIN 0.3)+ 吃掉沿位移方向速度。**只補「終點無重疊」的洞**:終點落在障礙內的正常慢速貼牆一律**交給 push-out 沿牆滑**(手感不變)—— 唯一例外 = 終點在內但落在障礙「遠半」(`fwd=(P1−中心)·位移 > 0`),此時 push-out 會把機體推出**另一側**(半穿透),故也夾在進入面。②**push-out 多趟收斂**(至多 3,穩定即止):密集街廓單趟可能把機體從 A 推進 B(殘留重疊)。兩者**共用 push-out 同一垂直閘**(`myBot >= b.y+b.h−0.1 || myTop < b.y`)**與 onDeck 豁免**(橋上不被橋下街廓側推,#INC 高架橋掉橋);站頂面時 `myBot≥top` → 掃掠與 push-out 皆 skip = 屋頂/岩頂/樹頂正常走。**MUST NOT** 讓掃掠在「終點近半」時夾住(會變成撞牆黏死、失去平滑沿牆滑);**MUST NOT** 移除 onDeck 豁免或垂直閘(掃掠與 push-out 必須同式,否則橋面/頂面行為分歧)。純客戶端(伺服器 sim/data.js 未動)⇒ bal/e2e 不受影響;驗證 = LIVE `BattleClient.prototype._collide` 對 24 方向 × 6 速度(至 200m/幀)× 圓柱+有向盒 共 288 例終點恆在障礙外全綠(瀏覽器內對真原型跑,零漂移),頁面 console 零錯誤。

**07-23 場地圖資快取(首次完整成功即定案)**(新檔 `geocache.js`/`terrain.js`/`biomes.js`):使用者實測倫敦兵線過河橋數仍逐局 1~3 浮動(07-22 改制驗的是「同一份圖資重建兩輪」位元級一致,沒驗「圖資本身逐局不同」)。實測定因:全資料下倫敦 5v5 恆 6 座兵線補橋(3 線 × 泰晤士河 + 公園湖各 1 span),Overpass 成敗兩路徑橋數一致、50% 陣營對調(bases 換 + lanes 反轉)span 位元級不變 —— 殘存變異源是**建圖輸入**:①高程 terrarium 單磚失敗 = 全組換 open-meteo 33×33 粗網格(高度場整個不同,且 open-meteo 分鐘級 429);②影像缺磚留底色(藍色水判定帶洞);③Overpass 鏡像輪替/限流(真橋/隧道 way 集合忽有忽無)。修法 = **IndexedDB 圖資快取 `geocache.js`**(`geoKey(kind,v,bbox,extra)`/`geoGet`/`geoPut`):高程 raw N×N 網格(平滑/AMP 前;**僅 terrarium 成功入庫**,備援粗網格 MUST NOT 定案)、影像原始像素(stylize 前;**零缺磚才入庫**)、Overpass 兩查詢(**`data.remark`(截斷/逾時)不入庫**;入庫走 `structuredClone` —— IDB 寫入非同步,下游 `way._tun`/mergeGradeChains/buildRails 會就地變異物件)。同 bbox 第二場起全走快取 → 建圖輸入位元級一致,整張圖(含兵線橋/隧道)恆定;快取層任何失敗(隱私模式/配額)靜默照走網路,§2.4 降級鏈不變。LRU 每類保留筆數住 `KEEP`(影像 ~16MB/筆故收 4);**改資料格式/查詢形狀 bump `geoKey` 的 v 或 extra 即自然失效,MUST NOT 手寫遷移**。另:terrarium/Esri 單磚各加一次重試;`fetchImagery` 回傳新增 `complete` 旗標(零缺磚)。**已知取捨**:跨客戶端各自定錨(A 先玩定案舊 OSM、B 後玩定案新 OSM = 兩人真橋/建物可能長期不同)—— 權威層(occ/wet/corridors 房主上傳 + sim)不受影響,兵線補橋由高程驅動(terrarium 靜態資料)故跨客戶端一致;要強制刷新圖資 = DevTools 清 IndexedDB `svs_geo` 或 bump v。**MUST NOT** 把「殘缺結果」入庫條件放寬(缺磚/remark/備援一旦定案 = 永久帶洞的地圖)。**同日追加(OSM 房間補抓)**:開房當下 Overpass 被限流 → `main.js scheduleOsmRetry`(單航班,間隔 90s、至多 2 次)在房間等待期間背景重試 `biomes.warmOsm(bbox)`(只發查詢不建幾何,fetcher 自身入庫);成功且仍在 room 階段同 key → 清 `app.pre` 重建預建(全走快取秒級)= 第一場即拿到完整圖資,已開戰則下一場命中。依據 = `stats.osm`/`stats.osmRoads`(新旗標:路網查詢成敗)。**MUST NOT** 改成「選場地點擊就發 Overpass」—— 瀏覽場地連點會把分鐘級限流額度燒光,反而害正式開房抓不到(warmModels 是靜態 CDN 才適合選定即暖)。

**07-22 四項武器/機體演出改制(榴彈虛線/巨炮氣旋/自殺機半爆/餌機補投)**(`data.js`/`server/sim.js`/`game.js`/`vfx.js`):使用者四條需求一次落地。①**榴彈拋物線瞄準虛線**:launcher 型(榴彈/火箭,`def.type==='launcher'`)瞄準時畫預測拋物線 `LineDashedMaterial` 虛線 + 落點環(`game._updateArcGuide`/`_ensureArcGuide`,每幀進 `_loop`)。與 `_updateBullets` 同一組 mv/`BALLISTIC.G` 積分,終點吃地形/`_obstHitT`/射程(`def.range × _altRangeMul × (barrage?RANGE_F:1)`)。**地雷**:虛線 geometry MUST 預配置固定 `Float32Array`(MAXP=264)+ `DynamicDrawUsage` + `setDrawRange(0,n)`,**MUST NOT** 每幀 `setFromPoints`(重建 attribute 洩漏 GPU buffer);lineDistance 自算進預配置緩衝(**MUST NOT** 呼 `line.computeLineDistances()` —— 它每幀重建 attribute)。②**巨炮(重砲/barrage)氣旋噴射 + 傷害 ×2**:`BARRAGE.DMG_F` 1.33→**2.0**(=+100% 強化);傾洩窗內的重武器砲彈掛 `cycloneJet()`(vfx.js:3 後掠渦輪葉 + 熾亮尾焰)+ `_spinCyclone`(自旋 + 沿航向撒外旋螺旋煙圈)。自己那份在 `_tryFire`(`barraging` 判),他人靠 `barrage` 事件記 `_barragePids`(pid→until)→ `_spawnVisShell(..., _isBarraging(pid))`。bal 不含 barrage(burst 非持續 DPS)⇒ 四不變式不受影響;e2e 讀 `BARRAGE.DMG_F` 常數故不破。③**自殺攻擊機(kami)被擊毀原地半爆**:`_kill` 的 kami 分支改叫 `_kamiDeathBoom`(舊版只消失、不引爆)—— 傷害 `×KAMI.DMG_F×KAMI.DEATH_F(0.5)`、半徑 `×DEATH_F`,爆點仍算主機頭上(吃火力升級/擊殺記主機);boom 事件帶 `kami:1` → 客戶端熾橙火球 + `_emberBurst`。④**餌機(decoy)被擊毀補投彈**:`_kill` 的 decoy 分支在 `bombsLeft>0` 時補 `_decoyBomb`(復用單一投彈縫);客戶端 `decoyBomb` 事件改播**拋擲動畫**(`_spawnDecoyBomb`/`_updateDecoyBombs`:自餌機高度翻滾墜落 + 類型拖尾 → 落地才引爆)——`decoyBombMesh(type)`(vfx.js)四型專屬造型(fire 火苗/freeze 冰晶/poison 毒囊/thunder 電極)+ `_decoyBombLandFx` 四型地面演出。**驗證**:`npm run bal` 四不變式全綠、e2e 60+ 斷言全綠、headless 直測(kami 半爆 r/dmg、decoy 補投/無彈不投)、瀏覽器實跑(t01 進戰鬥注入四型投彈 + kami boom + 89 點拋物線虛線 + 氣旋彈體全生命週期,`__SMOKE_ERRS` 零例外、rAF 迴圈存活)。伺服器只動 `_kill` 兩分支 + 新增 `_kamiDeathBoom`;純視覺全住 game.js/vfx.js。**MUST NOT** 把 barrage DMG_F 搬進 bal 模型(burst 非持續),也 **MUST NOT** 讓拋物線虛線每幀重建 line/lineDistance attribute。

**07-22 32 機體視覺細節升級(依 lore proto 原型重構外觀)**(`models.js` 全 7 家族 builder、新工具 `tools/shot_units.mjs`):依各角色 `lore.js proto`(現實軍武 × 仿生原型)為 32 台機體加建模細節(液壓缸/裝甲分版/散熱結構/供彈鏈/航行燈/原型招牌特徵),每台新增不透明 mesh ≤25、全部收在既有包圍盒內(bbox 漂移 ≤±5%;僅 t10 寬 +5.2% = VLS 側掛彈筒外凸、s12 寬 −6.5% = 修正既有浮空端板)。**rig 契約零接觸**(weap/muzzles/wpn/gunR/gunL/pose/aimM/castSig 欄位與槍口節點未動)⇒ audit_muzzle 32/32+22/22、audit_cast_jump 32/32、data.js 未動 ⇒ bal/e2e 不受影響。**新工具 `node tools/shot_units.mjs [--ids s01,… |all] [--out DIR]`**(伺服器 8620 需在跑):對每台機體出 front/side/back(morph 加 fly)截圖 + 建模例外/mesh 數/bbox —— 改 models.js 幾何一律用它跑「生成→渲染→審視→修正」閉環;fly 圖走 `ent.heroY=60` + 300 幀收斂(與 audit_muzzle ③ 同法,直接 `pose(1)` 會被 stepLocomotion 拉回地面姿)。**既知殘留(皆評估後接受)**:t04 散熱百葉與彈藥箱 ~0.05 幾何相交(cel 下不可見);t12 髖軸環錨 hips、gait.bob 有 ≤0.09×a 相對位移(半埋設計吸收,MUST NOT 為此改 stepper);s08 鞭天線穿槳掃掠面為既有;m05 minY=0.7 為既有基準;t10 盾面觀察窗與縱肋小幅嵌入(非共面、無 z-fighting)。新增件多為靜態 emissive 識別燈,**未進 glows/lightGlow**(戰鬥脈動件才進);液壓缸一律單端錨+斜置近似(MUST NOT 雙端錨跨關節 —— 會被步態拉伸)。

**07-22 槍口朝向/戰鬥姿勢/後座全面整備(全機體含 NPC)**(`models.js`/`locomotion.js`/`game.js`/`sim.js`/`charPreview.js`/`tools/audit_muzzle.mjs`):使用者七條規則(槍口朝移動/攻擊方向、拋物線槍管 = 射擊角、手持據槍不垂手、發射點與火光在槍口、後座對應、拆無用槍管、全機體適用)+ 四項微調一次落地。**微調四項**:①gorilla 輕重拆左右肩雙座(右肩三管霰彈 sgR / 左肩電漿 sgL,各自 gunPitch;FPV `backPair` 雙肩 back 對 = 右輕左重鏡射分掛,**電漿口噴規則對 backPair 破例**,game.js `_mountCockpitWeapon`);②raptor 右手輕狙 / 左手重狙分持(`weap {light:'R',heavy:'L'}`、aimM 雙臂對稱、雙 gp 皆 air=π);③cthulhu 四持武觸手改矢狀面前捲(wArm 收 lift/curl 參數),**末節 tipP 以 build-time 世界對齊反解(`node.getWorldQuaternion().invert()`)= 槍口恆朝前的唯一保證縫,MUST NOT 改回手調角度**;④monkey 尾砲:`rig.tailPose`(尾鏈子集 poser)+ `rig._aimH`(stepCombatFx 新增的重武器射姿通道)→ 重武器交戰尾砲蠍式前捲 + `tailAimComp` 補地面殘餘仰角;elephant 象鼻 `rig.trunkAim` 交戰抬鼻(靜止/開火保持才抬,行軍垂放)。**規則整備**:bee 螫針火箭巢移駐腹下前向莢(nest 單根,fxOnly 已除役;尾端螫針純剪影);morph 飛行機身後座解鎖(stepMorph cbt 的 kick 項不再 ×(1−m));四車輛(apc/swarmApc/tank/rebelTank/swarmTank)砲塔加 `turret.userData.pitch` 俯仰節點 + `_aimVehicleTurret` 俯仰追瞄與 `_turKick` 砲管後座;tank(wid 'siege')曳光改 `_arcTracer` 拋物線 + `ent._arcPitch` 回寫砲管仰角;三直升機 `userData.gunTilt` 共軛俯仰(`_aimGunTilt` 對地壓槍)+ `turretMuzzles` 雙槍口輪替(`_npcMuzzle` 已通用化,不再限 tower;**makeUnit MUST 把 turretMuzzles/gunTilt 提上外層**);三個肩射火箭筒兵補 aimPose 扶筒;NPC 車輛 rig 直給 `kickAmp`(無 recoilTier);英雄 launcher 砲管仰角回寫 `_aimHeavyBarrel`(**gp.aim0 = 首寫快取的水平據槍角,MUST NOT 直接改 aim 不存 aim0**;NPC howitzer 沿用 `_arcTracer` comp 路徑);bot 重武器 heroBurst 補發 shot(`isBotId` gate,真人仍走 tracer)→ 客戶端 pid 分支 launcher/missile 畫視覺彈體;shot 事件加 heli `oy` / base 本體砲 `gi` 輪替;僚機/bot 靜止開火轉向攻擊目標(`_markFire` 記 `_aimAt`);碉堡駐軍曳光自面向目標的射孔射出;SAM 發射點加火光;拆裝飾槍管(hound 肩莢/zero 翼砲/ostrich 膝莢/swarmHeli 翼莢)。**驗證**:`tools/audit_muzzle.mjs` 重建(Playwright 範式:32 英雄地面+飛行 × 朝向/槍口前端/焰球/後座/據槍收斂 + NPC 四陣營 22 項;roo 拳砲走「蓄力前伸→擊發」全程峰值、悟空飛行懸停直立要**開火中**量測 —— 這兩個不是 bug 是設計)全綠;audit_cast_jump 32/32;e2e 全綠(首跑單項 = 既知時序 flaky)。data.js 未動 ⇒ bal 不受影響。**追修(使用者截圖回饋)**:①bee 火箭巢 y −0.46bs 會**半埋進蜂腰環/錐腹曲面**(軸向正確、稽核 dot 抓不到,但管口貼弧面看起來歪)→ 移 (−0.62bs, −0.5bs) 吊掛腹線之外 + 短柱生根蜂腰 —— 腹下掛件 MUST 檢查不與 abd 橢球/waist 環相交;②raptor 前肢縮短(QUAD[7]/[8] 0.5/0.55 → 0.36/0.4,馳龍臂 ≈ 腿半長)+ aimM 改收臂式(肩 −0.55/肘 −0.85/腕 0.25;**鏈角總和維持 0.1 ⇒ gp.aim 1.47 不用動** —— 調 aimM 一律保持總和不變,否則要連動重算 aim)。

**07-22 FPV 武裝/彈藥同源改制(輕重武器外觀 = 第三人稱)**(`models.js`/`game.js`/`vfx.js`/`charPreview.js`):使用者回報「FPV 輕武器全是機槍、重武器全是球,與展示台完全不同」。根因 = `_buildCockpitGun` 自畫通用幾何且**只建輕武器**(重武器唯一視覺 = flash 蓄力球),彈體又是 `_bombMesh` 灰球/純色方塊。三刀修法:①**`rig.wpn` 登記(models.js 唯一生產者,契約註解在 podWeapon 上方)**:每英雄 builder 登記 `{light|heavy:{nodes,ref,muz,fwd}}` — 單根武器 nodes=[root]、ref=root 自身、fwd 為武器局部軸;散件武器(嘴砲/背鰭/翼掛)ref=共同軀幹節點,消費端以 `ref.matrixWorld⁻¹×node.matrixWorld` 烘相對排列。fwd 以「muzzle 相對 ref 的最大分量軸」判定,**MUST NOT 用猜的**(bastion/seraph/morph 手持 = 'y',bee 尾巢 = '-z')。②**FPV 座艙複製第三人稱武裝**(`game._mountCockpitWeapon`/`_cloneWpnSet`/`_cockMountStruct` 取代 `_buildCockpitGun`):建拋棄式 `makeUnit(...).group`(**注意 makeUnit 回傳 `{group,mixer}` 不是 Group**;從未 render 無 GPU 資源,複本共享其幾何/材質故 MUST NOT dispose)→ 複製武裝子樹(剔除 `userData.isOutline` 描邊殼與隱形焰球、標 `noPaint` 防二次塗裝)→ 依 `WPN_FWD_ROT` 轉正 → 包圍盒定尺(`COCK_WLEN`×錨點 s)+ 近場保護(後端 ≤ −0.55)。**輕重兩把同時可見**(與第三人稱一致),`HEAVY_MOUNT` 表管地面重武器掛點(rig.weap 'L'/'R'/'B' 手持邊優先);同型雙模(`wpn.light.nodes[0]===wpn.heavy.nodes[0]`)只建一次、雙槍口取自同複本;異型雙掛左輕右重(buildDrone 同約定);瞄準只切 `_muzzle`/flash(`_syncCockpitWeapon`,取代 `_syncMorphCockpit`,morph 型態切換一併管)。缺登記(GLB 覆蓋)退回 export 的 `podWeapon`。**地雷**:back 掛點散件對(雙肩 VLS/四背鰭)X 預算收緊 0.55×s + 底緣坐上砲座頂,否則浮空/漂到畫面中央;cthulhu 三觸手武器 **MUST NOT** 整組登記(聯集=整機寬,FPV 縮成一排小點;已改輕=右槍莢/重=orb 單根)。③**彈體單一縫 `vfx.projectileMesh(def,{col,hue,heavy})`**(幾何 +z 朝前):FPV 自機彈道(`_tryFire`,彈體恆對準航向)、他人重武器 launcher/missile 視覺彈道(`onTracer`→`_spawnVisShell`/`_updateVisShells`,pid→ent.ch→`_heroDefOf` 快取解析 def;重力直線近似、地形/`_obstHitT` 截斷,純表現不進 A6 raycast 清單)、展示台 `_stepHeavy`、塔射 SAM(`_syncMissiles`,scale 1.55;**朝向已從 +y 改 +z**)全部同源;`_bombMesh` 已刪。FPV flash 改 additive 暖白(= attachMuzzleFlames 焰球語彙)。驗證:32 角色假實例 `_buildCockpit` 零例外 + 槍口齊全/左右分掛正確;真戰鬥(t01)實測槍口切換/火箭彈體(Group)/曳光/console 零錯誤。伺服器/data.js 未動 ⇒ bal/e2e 不受影響。**MUST NOT** 回退通用機槍幾何,也 **MUST NOT** 在 rig.wpn 之外另闢第二條 FPV 武器外觀來源。

**07-22 地圖預建(固定項目提前到房間階段)**(`main.js`/`index.html`):使用者需求「遊戲啟動時的固定項目移到預設地圖/建立地圖房間就先處理,遊戲準備只留玩家選擇/隨機項目」。原 `enterLoading` 的固定工程(preloadModels → buildTerrain → buildBiomes → deck/tunnel/blockerTop/surfaceAt/ceilingAt 索引掛載)整段搬進 **`startPrebuild(cfg)`**,由 `onSync` phase `'room'` 以 `lobby.battleConfig` 觸發(開房/入房/再戰回房;battleConfig 開房時已由伺服器定案:resolveEnv + 50% 主堡對調 + teamSize,故房間階段即最終設定)。`enterLoading` 只剩:等 `pre.promise` + 隨機背景圖 + 房主 world 上傳(occ/cor/wet/slabs,讀 `pre.ud` = biomes.userData)+ `loaded` 握手。**地雷紀律**:①預建結果住 **`app.pre`**,**MUST NOT** 直接寫 `app.terrain` —— onSync room 分支每次回房都 `app.terrain=null`(1913),直接寫會被抹掉;`enterLoading` 消費後 `app.pre=null`。②**單次使用**:進過戰鬥的地形被 `clearAround` 不可逆變異(建物縮零 + splice blockers)—— **MUST NOT** 跨場重用,再戰回房 sync 自動重新預建(3 秒級,磚片走 HTTP 快取)。③`startPrebuild` 以 `prebuildKey`(center/sizeM/teamSize/env/bases/lanes 的 JSON)冪等,sync 重播多次不重建。④`preloadModels` 本身無重複守衛(重呼叫整批重抓 GLB)→ 一律經 **`warmModels()` 單航班**;選定預設場地/最愛(`selectVenue`/`selectVenueOpen`/fav click)即暖模型(與 cfg 無關的固定項目最早觸發點)。⑤預建失敗記 `pre.error` 不外拋(房間 UI 不受影響),`enterLoading` 消費時重建一次、再失敗才顯示錯誤 —— §2.4 fallback 紀律不變。⑥被換掉/失敗的預建 **MUST NOT** 加 dispose:從未 render 的 group 無 GPU 資源(上傳發生在首次 render)交給 GC;且 biomes 的 `_facadeCache`/`_roadTexCache` 是模組級共享貼圖,誤 dispose 會弄壞下一場。⑦`#roomPreload`(index.html)獨立於 `roomMapInfo`,renderRoom 的 sync 重繪不覆寫進度。已驗(瀏覽器冒煙):房間預建完成後開戰 = loading 沿用同一份 terrain(identity 相等、零重建);再戰回房自動重新預建(fresh 物件);在途消費(createRoom 後立即 startBattle = 劇情戰役時序)loading 條無縫接手進度;console 零錯誤。伺服器/data.js 未動 ⇒ bal/e2e 不受影響。

**07-22 兵線橋確定性改制 + 單層原則 + NPC 水上站橋(倫敦三問題)**(`biomes.js`/`data.js`/`main.js`/`game.js`):使用者實測倫敦 ①NPC 走橋下水面 ②橋上下兩層 ③兵線過河橋數 1~4 逐局浮動。根因 = 兵線橋依賴 Overpass 逐局回傳(`DECK_COVER` 覆蓋率去重)+ 外部 fetch 成敗連鎖(影像 gate 3458 舊版直接跳過 OSM)。五刀修法:①**兵線泡水段一律自建全跨補橋**(`laneWetWays` 提前於 roadInput 定案處計算,不查覆蓋率;`WATER.DECK_COVER` 已刪)——兵線橋數 = 兵線跨水 span 數,恆定;osmRoads 失敗路徑(兵線當道路)自建橋,兩路徑橋數一致。②**單層原則**:`dropLaneBridges`(兵線走廊內側向重疊 ≥35% 的真橋剔除;X 形斜交保留)+ `dedupeParallelBridges`(平行雙幅 ≥60% 重疊只留長鏈)。③**沉錨橋碎片不建**(buildRoads + markGradeCorridors 同步):bridge way 任一錨、wet 自動橋雙錨高程 < `WATER.LEVEL − 1.0` 即整段跳過。**閾值紀律(實測踩過三個坑)**:岸壁/碼頭高程可低到 ~0.1 ⇒ 用 `isWaterPt` 或 `WATER.LEVEL` 當閾值會把真橋岸錨整批誤殺(decks 1034→308);影像藍色也不可靠(斷點常在他橋正下方 = 橋面灰,或缺磚底色);「沒入水下整整 1m」才是河床專屬特徵(倫敦實測斷點錨 h=−2.48 vs 岸 0.1~0.25)。**MUST NOT** 回退這三種判定。④**OSM 抓取去影像 gate + Overpass 鏡像輪替**(`OVERPASS_URLS` 三站,!ok 換站、abort 放棄)+ **mergeGradeChains 鏈排前**(maxRuns 截斷不再優先犧牲橋/隧道)。⑤**NPC 水上站橋**(`game._updateEnts` waterY 夾制處):地面 NPC `ny < waterY` 時先查 `terrain.deckY(nx,nz,terrain.deckMargin)`,有橋直接貼橋 —— mount 台階是單向棘輪(生成抖動/塔推擠/迷霧刪重建把 curY 播種到水面後永遠爬不回 7.5m 橋面),乾地高架下不受影響(ny ≥ waterY 不進分支)。已驗:倫敦 L3 兩輪重建位元級一致(decks 988、三線各 1 span、行進模擬 swim=0、兵線疊層 0、maxStep ≤0.87)、威尼斯無回歸、e2e 全綠。殘留:地圖角落 (−620,561) 外圍水域路口兩座自動橋相位差 7 個疊層中點(遠離兵線,既有問題)。冒煙法:頁面內對 cfg.lanes densify 1m + 複製 isWaterPt 判 wet span,行進式 `surfaceAt(x,z,prevY)` 模擬 NPC 貼地,數 swim/maxStep/疊層 —— 1m 取樣才等價遊戲逐幀粒度,6m 取樣爬不上 24m 引道(smoothstep 最大坡 0.47,6m 步進 2.8m > DECK_STEP 2.2)會誤報 mount 失敗。

**07-21 無人機完美迴避 + 三機動 CD 顯示 + 狙擊正圓 + 陣營位置隨機(五項使用者需求)**(`data.js`/`sim.js`/`game.js`/`main.js`/`index.html`/`css/style.css`/`server.js`/`e2e.mjs`):①**無人機完美迴避**——`IFRAME` 加 `DRONE_CD:30`/`COMBAT_S:5`。伺服器 `heroIframe` **移除 `h.kind==='drone'` 早退**,CD 依機種(`drone`=`DRONE_CD` 30s、其餘=`CD` 15s)—— 免傷/CD 仍伺服器權威(`_damage` 的 `invUntil` 早退不變)。客戶端 `_updatePlayer` 飛行分支:`isDrone` 於**空白鍵上升邊**(`keys.Space && !_spaceWas`)+ **戰鬥狀態**(近 `COMBAT_S` 秒內 `lastFireAt` 攻擊 **或** `_lastHurtAt` 被擊,後者在 `_applySnap` vital 下降處記時戳)+ **本地 `_dodgeCd` 就緒** → `_perfectDodge(u,now)`(垂直衝刺 + `_reqIframe` + 樂觀本地 30s CD)。**MUST NOT** 改每幀觸發(會連發)或把 combat gate 搬伺服器(免傷只利己、無作弊誘因,沿用「客戶端決定何時用」)。**注意**:既有的**招式 `dodge` fx**(直射武器必閃,`data.js CHARACTERS` skill/ult)是**另一套機制**,勿與此無敵幀混淆。②**升空變形無敵=離地後 1s**——現況 `_morphLaunch` 已於離地當下 `_reqIframe`(1s),無功能變更,僅統一註解。③**三機動能力 CD 顯示**——`_weaponHud` 加 `mobil` 欄(`isDrone`→完美迴避/`_dodgeCd`、`isMorph`→升空變形/`_morphCd`、else robot→蓄力跳躍/`_cjumpCd`,皆客戶端時戳);`index.html` ab-row 加 `#abMobil`(␣ 鍵盒,共用 `.ab-box`),`main.js self()` 渲染(就緒亮 `ready`)。④**狙擊模式正圓可視**——`index.html` 加 `#scopeVig`,`css` `.scope-vig` 用 `radial-gradient(circle …vmin)`(`circle` 關鍵字保證正圓不隨長寬比變橢圓)中央透明 + 外圈 `#000`;`main.js self()` 依 `w.aiming` 切 `body.aiming`(陣亡 `aiming` 已歸零 → 自動收起)。z=7:壓在 HUD(10)/hurt·env(8)之下,只黑周邊 3D。⑤**地圖雙邊位置陣營隨機**——`server.js createRoom` `resolveEnv` 後 **50% 對調 `cfg.bases.SWARM/STEEL` + 反轉每條 `cfg.lanes` 點序**(維持 sim 約定「`lane[0]`≈`bases.SWARM` 端」,見 `_spawnPoint` / `sim.js:981`)。反轉+換標**只變點序不動幾何** → `validateBattleConfig`/兵線分離/塔位稽核不受影響;伺服器定案一次、隨 battleConfig 廣播全房 → 地形/出生/小地圖全一致。**MUST NOT** 只換 bases 不反轉 lanes(SWARM 會生到敵堡);同房再戰保留同一組(config 屬房間)。已驗:e2e(含改寫的「無人機完美迴避 30s CD」段)+ `npm run bal` 四不變式 + headless 對調驗證(對調後 towers/camps 不變、雙方仍生在自家堡)全綠。

**07-20 機甲操作修正(兩批使用者需求,最終態)**(`data.js`/`game.js`/`sim.js`/`e2e.mjs`):①**重砲(巨炮)傾洩自動化**——`game._tryFire` 開頭原以 `!this.firing` 早退,使「狙擊模式長按右鍵」觸發重砲後彈夾**根本不傾洩**(玩家觀感 = 無傷害/無射程加成)。修法:開頭先解析 `barraging = id==='heavy' && _barrageUntil>now`,`if(!this.firing && !barraging)return`——窗內**免持左鍵逐幀自動擊發**清空彈夾;伺服器 `_gateFire`/`heroBurst`/`_heroDmg`(`_barraging` 解閘 + ×DMG_F/×RANGE_F)早已就緒,缺的只是客戶端自動開火。**MUST NOT** 退回「須持左鍵才傾洩」。**30s CD**:伺服器 `heroBarrage` 權威把關(已驗:+5s 再觸發被拒、+31s 接受);客戶端 `_launchBarrage` 加**本地 CD 時戳 `_barrageCdUntil`**(閘門取 `max(bcd, 本地剩餘)`,防在途舊快照的 `bcd=0` 把樂觀 `barrageCd` 洗回 0 → 30s 內誤判就緒)+ **彈夾檢查**(裝填中/空夾不啟動,與伺服器同條件,免本地時戳誤鎖 30s)。**注意**:重砲窗僅 0.5s 傾洩,窗後的「持左鍵連射重武器」是**正常開火(吃 reload CD 6~15s,非 30s)**——別把它誤判為重砲無 CD。②**無敵幀=起跳離地 1s;蓄力跳與變形起飛「能力本身」各 15s CD**——無敵幀請求**移到起跳離地當下**(`_chargeJump` / `_morphLaunch` 內 `_reqIframe`;舊「中段 `vy≤_cjV·0.5` 才請求」整塊移除,`_cjV`/`_ifSent` 已刪)。能力 CD 走**客戶端閘門**(位置本就客戶端權威):`CJUMP.CD`/`MORPH.CD`(皆 15,對齊 `IFRAME.CD`)—— `_updatePlayer` 放開時 `now < _cjumpCd/_morphCd` → 只普通小跳並提示冷卻。伺服器 `heroIframe` 的 `IFRAME.CD` 15s 仍是免傷的權威後盾(防作弊繞客戶端 CD)。③**蓄力跳前向距離 ∝ 機體速度**——`CJUMP.FWD`(定值)改 `CJUMP.FWD_F`(比例 1.0);前向初速 = `u.speed × _modF('speed') × FWD_F × 蓄力`(`_chargeJump(u)` 收參),低重力滑行距離 ∝ 初速 ⇒ 最大距離 ∝ 機體速度。注意 `u.speed` 已被 COMBAT_SCALE ×0.5(robot 21→10.5),FWD_F=1.0 剛好維持舊 10.5 手感且隨升速/角色 mod 放大。純客戶端,bal/e2e 不受影響。④**鎖定光暈:錐形瞄準輔助**(`game._tickLock`+`_coneAcquire`)——敵機甲頭上鎖定光暈有時不出現 / 忽明忽滅 = 單一中心射線常穿過 humanoid 四肢縫隙,或**狙擊模式移動靠近時準星微偏** → 射線瞬間掃空,`_lockId` 從未建立 ⇒ 純黏著(retention-only)救不了。改**錐形取得(acquisition)**:精確射線命中射程內敵方**優先**(尊重精準瞄準、可切換目標);否則 `_coneAcquire` 取「準星 ~8°錐內 + 射程內 + `_obstHitT` 無遮擋」的存活**敵方英雄**,遲滯優先既有 `_lockId`(防兩敵接近時抖動)。只鎖英雄(NPC/塔體型大,精確射線本就打得中)。**MUST NOT** 讓錐形輔助繞過遮擋檢查(否則隔牆亮光暈),也 **MUST NOT** 退回 retention-only(接近中的目標永遠鎖不上)。⑤**碉堡小地圖標記 Z 翻軸修正**——`_seenBunkers` 舊版存 sim 的 `e.z`(北正)未翻軸,但 `_world2mm` 與其餘標記(`ent.mesh.position`/`this.pos`)全吃 three 世界座標(z=−e.z)⇒ 碉堡標記畫在 **Z 鏡像位置**(玩家在真實位置看不到 → 誤判「離開後就消失」)。修法:存 `z:-e.z`(鍵一併用翻軸值)。持久層本就繪於迷霧之上、摧毀待重建保留(碉堡靜態 ⇒ 見到位置即重生位置)。**MUST NOT** 在任一 `_world2mm` 呼叫端混用 sim 座標。⑥**第三方 NPC 記仇追擊**(`sim._damage`/`_tpAggroTarget`/`_tpMove`+`THIRD.AGGRO_TTL` 8s)——出堡機動的第三方單位**被攻擊 → 記 `t.aggro=攻擊者`**(`_damage` 內,`t.gar` 免傷早退故必為機動單位;object 參照,`_serializeEnt` 白名單不外洩)。`_tpMove` 追擊改「`_tpAggroTarget`(脫離視野仍追,過期/陣亡/匿蹤/同陣營清除)> 一般視野追擊」;越界撤回仍由 `_tpBehave` 的 `TETHER_M`(90m)繫繩把關(追過頭即 `ret` 撤回)⇒「追到離開碉堡範圍」。

**07-20 八軌面向改制 + 空投地形避讓**(`data.js`/`sim.js`/`game.js`/`main.js`/`bots.js`/`e2e.mjs`/`balance.mjs`):四項使用者需求。①**開場所有武器招式皆 Lv1 可用**:`abil{light,heavy,skill,ult}` 全初始化 **1**(招式不再吃擊殺數解鎖);`heroCast`/`_castAbility` 移除「未解鎖」分支。②**品質+精通 4 軌 → 4 戰鬥面向**:舊 `wq/aq`(連動品質)+ `wm/am`(精通)共四軌,重整為 `lw/hw/sk/ult` 四個**獨立**面向 —— 每軌直接推進該武器/招式 `abil` 階(開場 Lv1 → 升 **3 次到 Lv4**;`abil = 1 + upg`)。**精通已整條刪除(masteryF 不存在)**:填彈/招式冷卻/電力折減全併入「階級成長」(3 階數組本身含 rate↑/reload↓/cd↓),`_reloadT`/`heroCast` 去 masteryF、`heavyMpCost(def)` 去 wm 參數。**MUST NOT** 再引入任何精通式全域折減旋鈕(使用者定奪「砍精通」)。③**Lv4 靠 `tierVal` 外推,不手寫**:`tierVal(v,lvl)` 超出資料階(Lv4+)沿「末段增量」線性外推(遞增續增、遞減續減、夾 ≥0)—— **單一推導縫**,免手寫 32 角色第 4 階(§2.1)。**MUST NOT** 各處硬寫第 4 階數值,也 **MUST NOT** 把 range 丟進 tierVal(heroWeapon.range 本就不過 t(),Lv4 不動射程 → 重武器 > 塔 310 不等式不破)。④**八軌階梯單價**:`ECON.UPGRADES` 統一為 8 軌(4 面向帶 `abil` 欄 + 4 防禦 hp/ar/sp/ch),全 `max:3`;`upgradePrice(u,lvl)=UPG_BASE(75)+UPG_INC(50)×lvl` **隨等級遞增**(Lv0→1=75、1→2=125、2→3=175;八軌全滿 24×75+24×50 = $3000,同固定單價時總價)—— 使用者 2026-07-20 由「固定單價」改「隨等級遞增」;防禦 step 改 hp/sp **0.27**、ar **10**(3 級達舊 5 級端點 ×1.8/+30,`ch` 沿 chargeF 公式吃 `ch.max=3` 自動 =1.0)。`buy()` 收斂成單一 ECON.UPGRADES 分支(帶 `abil` 者設階並清該槽彈夾)。⑤**空投避地形**:`sim._airdropPoint` 加 `_terrainBlocked(x,z)`(火場/水域/沼澤/淹水)—— 與野營/平民佈點同一縫;既有 `hazBlockers` 檢查續擋障礙物。**balance 校準(改任一數值 MUST 重跑)**:③ 八軌總價 = 8×3×125 = **$3000** ≈ 10 分鐘收入 $2954(101.6%,無擊殺門檻);④ 取消精通(reload ×0.6)後純量填彈重武器在滿級(Lv4 品質但無填彈折減)持續 DPS ↓~15% → 無人機站外預算 **180→200s**(平均 181s;s07/s03/s06/s10 等 vs.building≤0.5 的防空/反甲機種本就龜拆,check 吃平均、設計容許)。機甲/變形 maxPush 改吃 Lv4 + 無 wmF(剩 3.9~7.8%,仍在 0~20%)。e2e「八軌養成」段全面改寫(移除擊殺門檻/masteryF、加「輕/重/小招獨立升階」與「Lv4 外推 > Lv3」驗證);`npm run bal` 四不變式 + e2e(240 斷言)全綠。**MUST NOT** 退回連動 wq/aq 或雙軌品質+精通(面向已拆四)。

**07-20 兵線互不接觸/交叉(新規則,全禁)**(`data.js`/`bake_venue_lanes.mjs`/`mapSelect.js`/`server.js`/`venues.js`/`tools/audit_lane_sep.mjs`):同一 L 內任兩條兵線,排除兩座主堡的共享扇出段(沿 A→B 主軸進度 ∈[SKIP,1−SKIP]=`LANE_SEP_SKIP_FRAC` 0.15 之外豁免)後,中段最近距離 MUST ≥ `LANE_MIN_SEP_M`(**40 遊戲公尺 = 20m 真實**,REAL_SCALE 0.5)且 2D 不得相交——**含橋/隧立體交叉亦禁**(伺服器/烘焙無高程,一律保守把「2D 相交」視為接觸,寧多禁)。**單一結算縫** = `data.js laneSeparationAudit(lanesGameXZ)→{ok,minGap,crosses}`,四處共用:bake `tryBearing` 硬門檻(`fail:'touch'`)、`mapSelect laneRuleOK`、`server validateBattleConfig`(權威把關,防作弊)、`venues.js venueConfig` runtime guard(非合規 baked→synth)。座標一律**先轉遊戲公尺**(與 `towerLayoutAudit` 同框)再判——四個消費端各自 ll→game 轉換後傳入。折線最近距用「雙向 vertex→segment」最小值(不相交時精確)。**關鍵切分(勿混用)**:`LANE_SEP_SKIP_FRAC` 扇出豁免帶**只作用於「最近距離/接觸」檢查**(三線收斂到共享主堡端點不可避免地貼近);**交叉檢查全線不套豁免**(端點接觸由 segX 端點守衛排除,但兩線「換邊」= 交叉,近堡處亦禁)。**前科(2026-07-20 使用者實測倫敦 5 人 L3)**:初版把交叉也套豁免帶,近堡(進度 0.076 / 0.911)換邊的真交叉被藏起來 → 玩家看到 X;修法 = 交叉計數移除 `mid(prog(x))` 閘。校準:`skipFrac=0.15` 使 synthLane 中段最近 ~114 遊戲公尺、且 synthLane 對稱弧**永不交叉** ⇒ 降級一定合規。已離線重烤 `venueLanes.js`(用 `.osm_cache`,無網路):禁近堡交叉後多數高階 L 湊不出不交叉的真實道路解 → 降級 synthLane;**真實道路 entries 57→50**(9 場地全 3 線真實:shibuya/manhattan/paris/hehuanshan/venice/iguazu/rio/barcelona/kyoto;其餘高階 L 走 synth,如 london L3)。稽核 `node tools/audit_lane_sep.mjs`(exit 1,無需 osm cache);改 `LANE_MIN_SEP_M`/`LANE_SEP_SKIP_FRAC`/`REAL_SCALE` MUST 重烤 + 重跑此稽核。**MUST NOT** 把交叉檢查也套回豁免帶(近堡交叉會漏);**MUST NOT** 放寬成「只禁平面交叉」或給立體交叉開後門(2026-07-20 定奪全禁);**MUST NOT** 把門檻改判在真實公尺(縫吃遊戲公尺)。

**07-20 小地圖迷霧吃 LOS 陰影**(`game.js _drawMinimap`/`_mmShadows`/`_initMinimap`):使用者需求「躲在建物/神木/巨岩後的單位完全不可檢測」的**最後一塊**。單位面(看不見/打不到/無 HP 條)早由**伺服器** LOS 完成(`sim.snapshotFor`→`_visibleTo`→`_losBlocked` 過濾單位、`heroHit`/`heroLock` 同規則;離開快照 → `_removeEnt` 連 mesh 子節點血條/spotted marker/鎖定光暈一起消)。缺的是**小地圖迷霧**——舊 `_mmVision` 每源畫**完整視野圓**不管遮蔽。新增 `_mmShadows(vx,vz,r)`:對 `terrain.blockers` 每根圓柱投切線本影多邊形(半徑 `min(60,b.r)` 與 `main.js` 上傳 `occ` 同上限 ⇒ **與伺服器 `_losBlocked` 同一份圓柱幾何**);`_drawMinimap` 改用**每源暫存罩 `_mmScr`**(先畫視野圓、`destination-out` 挖陰影,隔離後才合成 seen/fog)——**MUST NOT** 直接在共用 seen/fog 上 `destination-out` 挖陰影(會誤刪他源已照亮區)。**設計取捨**:2D 不入帳障礙高度、一律當不透明圓(對齊伺服器「地面觀察者→地面目標」對任何有碰撞柱皆擋的語意,寧多霧不漏);純**迷霧視覺層**不動單位標記(單位仍畫在迷霧之上、源自伺服器過濾後快照)⇒ **A10 相容**。無 blockers 的地圖 `shadows` 為 null → 行為同舊版。純客戶端,e2e/bal 不受影響。

**07-19 水域/沼澤/火場地形危險系統**(`data.js`/`biomes.js`/`sim.js`/`server.js`/`game.js`/`main.js`/`index.html`/`style.css`):九項使用者需求一次落地。**雙通道權威切分(MUST 內化,勿混用)**:①**領機狀態效果走客戶端回報 env**——`game._envAt` 每幀算 `{code:0乾/1水/2沼, depth}` 存 `this._env`,隨 `pos` 訊息帶 `wet` byte 上傳;`sim.heroPos(...,wet)` 記 `h.wet/h.wetT`;tick 依此結算 沼澤(停護盾/裝甲恢復 + 滯留 `SWAMP_DRAIN_S` 後 `_damage` 扣血)、水域(停電力回充 + 滯留 `WATER_FREEZE_S` 後凍結換彈/招式 CD)。**env 是「輸入」非「狀態改寫」**(位置本就客戶端權威),故 **A1 相容,MUST NOT** 誤判為違規搬回伺服器重算。②**AI 迴避走主機上傳粗網格**——`main.bakeWetGrid` 逐格 `terrainEnvCode` 烘烤(sim 座標),隨 `world` 訊息帶 `wet`;`server.js` 存 `room.world.wet`;`new BattleSim(config, world)` **構造時**吃進(`_ingestWorldWet`→`_wetGrid`,Uint8Array)→ 初次佈點就避;`setWorld` 冪等補收(房主晚傳)。`_wetAt/_terrainBlocked`(水沼網格 + 火/淹水 ent 圈)接進 `_civPoint/_civWander/_moveCiv/_seedCamps/_tpMove` 五處(平民**移動**也擋,`_moveCiv` 與 `_tpMove` 同一邊緣守衛)。**此網格 MUST NOT 驅動任何權威傷害**(僅佈點/移動迴避),否則破壞 world 上傳「只能減攻」信任契約。**地雷**:(a)`terrainEnvCode`(`biomes.js`)是水/沼分類**唯一縫**——客戶端偵測與主機烘烤共用 = WYSIWYG,MUST NOT 各寫一套;(b)`game._terrainSlowF` 水域 **MUST 夾 `Math.min(WATER.SLOW, 依深度插值)`**——影像水色/內陸水(`waterY==null` 或水面在海平面盤之上)depth=0,不夾就回 1.0 = 客戶端不減速但伺服器已凍結的不一致(已修);(c)**深水不再是牆**——`game.js passable()` 移除深水擋牆、地板改 `waterY − FULL_D`(可涉水橫渡、全滅頂半浮 `FULL_D` 深),pos 回報 `sEff` 亦改 FULL_D(回報 y≈0 = 地面單位不誤判空中);(d)reload/CD 凍結 = **每凍結 tick 把 `reloadUntil`/`acd` deadline `+= dt`**(絕對 `this.t` deadline,跳過工作不會自停);(e)`_hazOk` 改**半徑感知** `_distToLanes − def.r < HAZ_LANE_MIN`(火/淹水邊緣不侵兵線走廊);(f)火場視野霧化純客戶端(`this.fires` + 滯留累計 → `#envFog` opacity,傷害仍伺服器 `_tickHazards`);(g)ESC 陣亡倒數叫離開選單——死亡時指標已解鎖 `_onPlc` 不觸發,故直接綁 `_onKey` keydown,`_setPaused(false)` 死亡中不重鎖指標,倒數頁 `this.paused` 時壓住。**確定性**:e2e/headless 不上傳 → `_wetGrid` 空、`h.wet` 0 → 全數新路徑 no-op(smoke 20 斷言 + bal 四不變式 + camps 2/4/6 + e2e 全綠)。

**07-18 武器/自殺機/餌機大改制**(`data.js`/`sim.js`/`game.js`/`models.js`/`server.js`):①**重武器全面取消蓄力(charge),改「彈夾 mag 2~5 + 裝填 reload 6~15s」**(依型別:launcher 3/12・missile 4/11・rail 2/8・gun 3/9・beam 5/8・plasma 3/7)。單發傷害由 `tools/../heavy_recompute` 思路**反推以保持持續 DPS**(bal 公式 `mag/rate+reload` 自動沿用,四不變式不動)——**MUST NOT** 把舊單發傷害搬回(會爆 DPS)。5 把 rail 的 `charge` 欄已刪;`RECOIL.heavy.high.steady` 改 0(彈夾要連發,不能每發停穩)。**地雷:`heavyMpCost` 已改「彈夾週期(mag/rate+reload)均攤到每發」** —— 若退回「每發 = reload×PER_CD」會讓 mag>1 的耗電率 ×mag 倍(Lv1 秒枯電、拆堡龜速,e2e 勝負逾時就是這個),`HEAVY_MP_PER_CD` 是**每秒**率不是每發。②**無人機護衛自殺機**:`SQUAD.KAMI` 兩架改 `SIZE_F=1/2`、`DMG_F=0.5`(半傷,`_kamiBoom` 套用);**觸發前是純客戶端貼身外觀(`game._buildDroneEscorts/_updateEscorts`,不進 sim ⇒ 不可鎖定/受傷)**,狙擊長按左鍵觸發才走既有 kami 實體衝出,`kamiCd` 歸零才重現。③**非變形機甲(robot)移除餌機**(`models.js:6539` 掛點限 `hero:morph`)、改**重砲模式**(`sim.heroBarrage` + `h.barrageUntil` 窗:`_gateFire` 解射速閘+免電力、`_heroDmg` ×`BARRAGE.DMG_F`、heroHit/Burst/Plasma 射程 ×`RANGE_F`;獨立 30s CD)。④**變形機甲餌機沿途投彈**(`_tickDecoys`→`_decoyBomb`,依 `MORPH_BOMB` 機體類型 fire/freeze/poison/thunder,復用 `bleed/slow/EMP/stun`,純狀態也補 `asst` 戳記)。⑤**輸入統一**:三機種能力全走 `game._tickSnipeAbility`(`aiming && firing` 持續 ≥ `GAME.SNIPE_HOLD_S`),**F 鍵綁定已移除**;net 訊息 `kami`/`decoy`/`barrage` 三條路由(server.js)對應 `heroKamikaze`/`heroDecoy`(限 morph)/`heroBarrage`(限 robot)。快照新增 `bcd`(重砲 CD)、`decoy dcd` 改 morph-only。**e2e + `npm run bal` 皆全綠**。

**07-18 高架橋連續斜坡**(`biomes.js deckAt`):端點緩坡由線性 `Math.max(0, Math.min(1,s/24,(total-s)/24))` 改 **smoothstep** `t*t*(3-2*t)` —— 舊線性版在 s=24 有 0.3125 的斜率硬折角(= BRIDGE_RISE/24)= 「階梯感」;smoothstep 兩端切線歸零 → 與地面路/水平橋面 **C1 連續**。`t=0` 與 `total≥48` 跨中(t=1)逐位元同舊版,**BRIDGE_RISE 峰值不變** → 不觸發淨空重驗、不 re-bake、不跑 bal/e2e;`total<48` 短橋跨中微幅上抬(淨空不減,安全)。`deckAt` 是餵橋面/decks 碰撞/欄杆/橋墩/路燈的單一縫,改它全部連動。**鐵軌 `liftAt`(buildRails)是獨立閉包、坡度緩(55m/8m)無回報缺陷,刻意未動**。

**07-18 高架橋出入口破圖 + 上不去**(實測渡月橋 OSM 橋:1042 端點中 133 個底緣沉入地表 0.65m):①**破圖**——邊梁/底板(soffit)底緣 `dy−1.1` 在低淨空引道段(deck≈terrain+0.45)鑽出地面;修法 `Math.max(dy−1.1, terrain.heightAt(緣座標)−0.2)` 逐緣夾在地表上(留 0.2m 微埋)→ 箱梁在橋頭平順沒入地面,133→0 clip。②**上不去**——橋面是窄帶、真實橋道會彎,直線走一段就掉出 `hw` 落回地表鑽到橋下;修法給 `makeDeckIndex` 查詢加 **margin 參數**,`surfaceAt` 站立查詢帶 `DECK_MARGIN=1.2`(貼緣不掉),`ceilingAt` 仍用 0(免橋緣外憑空撞頭)。實測沿橋面轉向前進 420/420 幀貼橋、爬到 10.78m 橋頂。上橋 mount 本身(`curY ≥ deck−DECK_STEP(2.2)`)在引道口原本就成立;真實兵線沿 OSM 路走 = 沿橋道 → 自然上橋。③**卡住(東京首都高密集高架)**——引道段 deck−ground 落在「> DECK_STEP(2.2,上不了橋)且 < 機高+餘裕(底緣鑽不過)」的**死區**:機體既 mount 不上、又被 `passable()` 的「夾縫 < 機高」擋下 = 卡死(合成單元測 4.5m 機體 x=8~16.5m 全卡)。修法 `main.js` 加 `MAX_MECH_H=4.8`/`DECK_UNDER=1.2`:`surfaceAt` 增「橋底緣淨空 `(d−DECK_UNDER)−h < MAX_MECH_H` → 一律上橋」(鑽不過就上去),`ceilingAt` 只在「底緣淨空 ≥ MAX_MECH_H(真高架能鑽)」才擋頭 → 引道段不擋、交給 surfaceAt 上橋。**MUST NOT** 讓兩門檻之間留死區(用底緣淨空 vs 機高判斷「上橋 or 鑽過」,非用 deck 頂 vs DECK_STEP 單一門檻)。合成單元測:死區 18 格 → 0 格。④**上得去但掉下橋**(倫敦案,任何橋):實測「站上橋面直線走中線」Y 追蹤完美,掉橋是**側向漂移出橋緣** —— (a) `game.js _collide`:橋下街廓的高樓碰撞柱**垂直涵蓋橋面高度**,走在高架橋上會被橋下建物側推撞下橋 → 修法「站在高架橋面(`_surf > 地表+1`)時,基座低於腳下 3m 的建物不推擠」(高架路飛越街廓);(b) `DECK_MARGIN` 1.2→**3.0**:走位/轉向/後座/兵線平滑漂移貼近橋緣仍站得住(NPC 也吃此 margin,因 ent 貼地走 `_surf`),代價=可站到可見橋緣外 3m,取「不掉橋」優先。**注意**:OSM 節流時無法載入真實倫敦橋幾何驗證,以上為合成場 + 機制推導;真掉橋若仍在,下一步查 makeDeckIndex 彎道外角覆蓋缺口 / 兵線是否真沿橋道。⑤**上不去 真因(已實測定位)**:④ 加的 `DECK_MARGIN=3.0` 是 `makeDeckIndex` 查詢用 `hypot(到夾制端點) > hw+margin` 判定,margin 同時**縱向溢出**——斜引道上一個查詢點會夾到「相鄰較高橋段」的端點(實測倫敦錨點:z=−626 真橋面 4.53m,margin=3 回報 7.06m,高估近一整段),導致上橋台階 `curY≥deck−DECK_STEP(2.2)` 超標 → 爬到一半掉回地面卡在橋下(=「破圖穿越 / 無法走上去」)。修法(`biomes.js makeDeckIndex`):把 margin 拆成**側向垂距**(叉積/段長,放寬)vs **縱向溢出**(`LONG_TOL=1.0`,遠低於 ROAD_SEG 6m,不夠到相鄰段端點)兩維——側向仍吃 margin(貼緣不掉),縱向不再洩漏。實測(t01 bastion,同步 `_updatePlayer` 迴圈)修前爬到 z=−626 掉橋→卡水下;修後 z=−640 地面一路上引道爬到 8.6m 橋面走完全程 `_onDeck` 恆真。**MUST NOT** 把 margin 放回「合併縱橫距」判定。

**07-18 障礙物防穿模(建物盒碰撞 + 鏡頭 de-clip)**(`game.js _collide`/`_cameraDeClip` + `biomes.js` 建物 blocker):①建物 blocker 碰撞柱 `r = 0.8×盒角` **內切**於方盒 → 斜向進入鑽進盒角(body 中心可到盒內)= 撞建築破圖。建物 blocker 增 `hw2/hd2/ry`(真實半寬/半深/朝向),`_collide` 對 `b.hw2!=null` 走**有向盒推擠**(world→local 繞 −ry、最小穿透軸推出、local→world 繞 +ry);`r` 保留給投影彈道 `_blockerHitT` 圓柱(A6 不動)。神木/巨岩/橋墩仍走圓柱(本就近似)。②第一人稱鏡頭掛頭艙 = pos 前方 headF,硬碰撞只擋 pos → 鏡頭仍戳進障礙看穿牆面。新增 `_cameraDeClip()`(掛 `_updatePlayer` 相機定位後,純視覺不動 pos/vel):沿「pos→鏡頭」水平軸把鏡頭拉回障礙外緣(圓柱=射線圓交點、建物=射線 slab-box 交點,留 `SKIN=0.9`,絕不拉到 pos 後方)。站橋面 `_onDeck` 時跳過橋下街廓(共用 `_collide` 快取的 `_surfHere/_onDeck`)。實測(倫敦錨點):16 向撞建物 `worstCamInsideBox=0`、撞神木(r3.8)/巨岩(r38.8)`worstCamPenetration=0`。③**視覺件超出碰撞盒的兩個缺口**(第二輪修,否則仍看穿):(a)**臨街裙樓**(commercial 40%,視覺 `1.4w×1.28d` 齊眼高但只有主體 `w×d` 有碰撞)→ 玩家鑽進裙樓看穿;修法裙樓另登記自己的盒 blocker(`hw2=0.7w/hd2=0.64d`,基座段 `h=ph+1`)。(b)**地標建物**(`bld:1` 無 hw2,多箱體自訂幾何 + 隨機朝向,`col.r` 常內切於側翼)→ 用 `Box3.setFromObject(g)` 外接半徑當碰撞 r(`max(col.r×sc, footR)`,圓柱對旋轉不變寧大不小)。伺服器不涉入(blockers 是客戶端 biome 幾何)。**MUST NOT** 只給主體盒就當「建物防穿模完成」——凡視覺件超出碰撞盒(裙樓/側翼/未來新增外挑件)都要各自補碰撞,否則鏡頭鑽進去看穿。④**牆角漏(第三輪修,實測「撞牆壁不破圖但撞牆角仍破」)**:`_cameraDeClip` 的 broad-phase 半徑對建物盒誤用 `max(hw2,hd2)`(內切)——貼牆角時 pos 落在盒**對角**外緣(距心 `hypot(hw2,hd2)` > max),被誤判「太遠」跳過整棟 → 牆面近盒面故過、牆角漏。修法 broad-phase 半徑改**外接** `Math.hypot(hw2,hd2)`。**MUST NOT** 回退成 max。實測:強制鏡頭戳進 8 棟最大建物(hw2≤16)4 角落各 1.5m 後 `_cameraDeClip` 全推出(cornerInsideDepth=0);真實 16 向(含角)走撞 `worstInside=0`。注意 round-1 的 bastion 測試「角落=0」是假通過——bastion `headF(0.4)<myR(1.26)` 鏡頭本就在盒外,沒真的觸發角落 de-clip;要測 de-clip 角落 MUST 直接把鏡頭塞進盒角再呼叫,不能只靠角色自然淨空。

**07-18 陣亡第一人稱殉爆過場**(`game.js`,純表現層):`_deathSeq` 單一 nullable 欄位狀態機(null=未播/物件=播放中,所有 gate 皆 truthiness、teardown 皆 `= null`)。`_onSelfDeath` 於死亡快照捕捉**當幀最後活體姿態**(camera/pos/vel/yaw/pitch/roll —— `_updatePlayer` 對 dead 早退未覆寫);`_updateDeathSeq` 每幀**獨佔 `this.camera`**(掛在 `_loop` 的 `_updatePlayer` 之後,以 `_deathSeq && !_gameOver` 為閘)——**MUST NOT 移除 `_updatePlayer:3688` 的 `if(this.dead)return`**(否則死亡單位恢復物理/開火/pos 回報)。地面=劇震+smoothstep 側翻下陷+火煙柱(`_deathPlume`)+尾段殉爆(爆+地環+碎片+字卡+白閃);飛行=**自適應重力**(依墜高夾 30~95)拋物墜落+per-axis 翻滾+拖尾煙(`_crashSmoke`),觸地爆;**高空逾時(墜不到地)就地空中爆,MUST NOT 瞬移鏡頭到地面**(前科:固定 G 半空硬切)。過場期間 `hud.dead`(1912)/`_renderDeathCam`(4546)讀 `_deathSeq` **延後倒數頁與砲塔 PiP**、`cockpit.visible`(4463)**保留座艙隨鏡頭傾倒**(第一人稱)。teardown 三處:`_onSelfRespawn`(重生硬切)、game-over 塊。DOM=`#deathFlash`(白閃 z29)/`#deathAlert`(紅框 z9)+ `hud.deathCine(on,flash)`。火煙改用**柔邊 sprite billboard**(恆面向相機、無 facet → 第一人稱近距離高解析度;火 additive、煙 normal、`depthWrite:false`、`userData.noOutline`);貼圖用**程序生成 512px 細節貼圖**:`_fireTex`(白熱核心 + 中層暖輝 + 130 條細火舌 + 熱斑點)、`_smokeTex`(60 亮斑 + 34 暗紋挖空的 fBm 翻騰感 + 圓遮罩),取代舊純漸層 blob(太糊);`_explosion` 的 Points 也套 `_fireTex`。**第一人稱吞沒感**:`_engulfFPV(cam)` 每幀在鏡頭前方(fwd/right 由 `cam.quaternion` 求)撒**火 + 煙**(前兩顆 additive 火、後兩顆 normal 煙)+ `_emberBurst` 火星;殉爆 climax 追加 `_deathPlume` + 大批 ember。**火煙集中左右兩側、中央 1/3 保持視野(可讀性優先,2026-07-18)**:交替左右均衡撒點,側向外推量與焰球尺寸掛鉤(`lat = ±(2.4|1.9 + base×0.35 + rand)`)→ 大焰球推更遠,內緣不越過中央 1/3(FOV 68° → ±11°);火星也只沿 `right` 軸兩側迸射(中央不撒)。實測 10 次 engulf 40 顆大 sprite:左右各 20、中央越界 0、最小內緣 23°。**MUST NOT** 退回「鏡頭正前方置中撒火填滿 FPV」(擋死視野)。過場峰值 ~95 個 transient sprite(貼圖共用快取、一次性 cinematic,可接受)。effect 帶 `dispose` 釋放材質(貼圖共用快取不 dispose)。伺服器完全不涉入(die 事件的爆點在死點下方,飛行另補一記空中火花)。**MUST NOT** 退回低多邊形 MeshBasicMaterial 球(6×5 facet 在第一人稱看起來粗糙)。

**07-18 四項戰場機制**:
- **自殺攻擊傷害隨武器等級**:`bomb.dmg` 改**三階陣列** `[240,300,360]`,只准經 `sim._bombDef(owner)`(以 `tierVal` 吃 `owner.abil.light` = 1+wq)解析成純量再進 `_blast`。`_kamiBoom`/`_boom` 共用此縫;**MUST NOT** 把 `WEAPONS.bomb.dmg` 當純量直接乘(NaN)。Lv1 = 240 不變(e2e/bal 基準),升階才增威。
- **重生點防撞兵線 NPC**:`GAME.HERO_SPAWN_SIDE` 8→18 且兼作橫向防撞;**伺服器 `sim._spawnPoint` 與客戶端 `game._spawnAt` 共用此偏移**——客戶端原本漏套(落在兵線中央被波次撞開),已補 `(dz,−dx)` 單位垂直 ×SIDE。值須 > 波次抖動 ±7 + 最大機體半徑 + 自機半徑,且 < `LANE_SAFE_M` 45。
- **碉堡永久標示(client)**:碉堡走迷霧(非恆可見),`game._applySnap` 見到即記 `_seenBunkers`(量化座標去重),`_drawMinimap` 另開持久層畫出(離開視野/摧毀待建也保留);ent 迴圈的 bunker 分支已改 `continue`(單一繪製來源)。
- **清空第三方營地釋出平民**:`sim._onThirdDeath(t, by)`(新增 `by`)在 `_campCleared(ci)` 成立且 `!camp.freed` 時 `_freeCampCivilians`——釋出 `THIRD.CLEAR_CIVS`(4)名隨機陣營、`follow=清營者`、`noRespawn`(`_kill` civ 路徑略過 `civRespawns`)平民。`camp.freed` 一次性(營地照常重生,獎勵只給首次)。

**07-18 鐵軌高度一致性 + 平交道**(`biomes.js buildRails`):OSM 把連續鐵軌切成「地面 way + `bridge` way」時,舊版對整條 way 套固定 lift → 接點瞬跳 8m(看起來斷掉)。改**逐頂點 lift**:高架段只在「未接續另一段高架」的端點沿 `RAIL_RAMP`(55m)爬升緩坡回地面,接續高架處(共用端點 `ekey` 量化計數 ≥2)保持全高。端點連續性靠**世界座標 0.5m 量化 key**(共用 OSM 節點 → 同座標 → 同 key),**MUST NOT** 改回整條固定 lift。橋墩改吃 `pierPts`(過矮 <2.5m 略過,免爬坡段殘樁)。平交道 = OSM `railway=level_crossing` 節點(fetch 已加 `out 40`),`buildLevelCrossings` 取最近**地面**軌切線定方位(高架/隧道段附近 >30m 略過),遮斷器**抬起**狀態(不擋兵線、無碰撞)。列車 `trainDriver` 仍用 `Math.random` 起始位置(既有、純表現,不動)。

**07-18 地圖佈營調參**:`MAPGEO.MAP_EXPAND`(1.33,只放大 battleBBox 不動兵線)+ `THIRD.CLEAR_F`(1.5→1.25)合修野營生成率,現值 L1 2/2、L2 4/4、L3 6/6。`CLEAR_F` 唯一底線 = **> 1.0**(淨空 388 > 塔射程 310 才不交火)。動任一 → headless 冒煙數 `sim.camps.length`;**不需** +GEO_SCALE_VER、不需重烤 venueLanes。

**07-17 高度制空**:客戶端射程增益住 `game.js _altRangeMul()`(套在開火閘門),**MUST NOT 改動 `heroWeapon().range`** — e2e #INC-104 的射程斷言會動。傷害純伺服器結算。

**07-17 助攻戳記**:任何新增的負面狀態路徑(傷害/CC/EMP/bleed)**MUST** 記 `t.asst` 貢獻戳記 — 漏一路 = 該類輔助角色零收入。戳記靠 tick 內在場刷新,`_kill` 結算只驗 TTL,MUST NOT 再看擊殺當下距離。

**07-17 battery overcharge**:電力可超過 maxMp 的唯一支撐縫 = tick 回充迴圈的 `if (h.mp < h.maxMp)` 守衛。這不是 bug,**MUST NOT**「修正」;overcharge 只靠消耗降回、重生清除。

**07-17 SQUAD.BUFF = 1.12**:波次加坦克後的校準值。`UNITS.drone.hp/shield` 與 `SQUAD.DMG` 由它 derive;三機隊承傷 ∝ 1/BUFF² — 動它必跑 `npm run bal`(三機體都要回到 ~40% 剩餘)。

**07-17 武裝掛點大遷移**:trex/raptor 輕武器→手持、gorilla/monkey→扛肩、ostrich→左翼。**移武器 MUST 連供彈/供能結構一起搬**(彈鼓/燃料罐/彈鏈箱),MUST NOT 留無用槍管。動掛點 → 重跑 `audit_muzzle.mjs`(槍口 +y 軸 dot ≥0.9)。

**07-17 變形機甲飛行武裝**:手持機種雙臂前伸朝航向射擊、肩掛機種武器轉背部;`_entMuzzle` 對 morph 飛行**不再** fallback 訊息座標。**MUST NOT** 退回「飛行武器收攏折走」。

**07-17 NPC 開火視覺**:`shot` 事件全兵種發送;`ent._aimAt`(2.5s)讓停止 NPC 面向實際攻擊目標;榴彈兵 `_arcTracer()` 把出膛切線角回寫 `rig.gunR.aim`(砲管仰角與弧線一致)。

**07-16 post-pass 通道洩漏(最危險)**:`stepCastPose`/`stepJumpPose` **只准動「步態每幀重新賦值」的通道** — 動未賦值通道(如 `armL.rotation.z`)= 動作結束後永久歪掉。前科:stepQuad 漏賦 `chest.rotation.y`,dance/swing 跨幀累積。防線 = `audit_cast_jump.mjs` 的差分對照組(第二具從未施法,收斂後逐節點比對)。

**07-16 槍口焰待機 scale = 0.01**:焰球是 additive 球體掛在 muzzle 節點;待機不縮到 0.01 會撐大 `_spawnUnit` 的血條/鎖定光暈基準包圍盒。同理:血條/敵標/光暈一律用 spawn 時量好的 `ent.dimTop/dimH/dimR`,**MUST NOT** 事後 `Box3.setFromObject`。

**07-16 morph 手持武器 `heavy.pivot` MUST 留空**:`gunR/gunL` 的 rotation.x 由 gunPitch 每幀賦值,再掛 pivot 會互相覆寫(兩個寫入者打架)。

**07-16 mark 招式限制**:mark 消耗路徑只在 `heroHit` 直擊 → **fan/launcher/missile AoE 武器的角色 MUST NOT 掛 mark**(s09 前科,已換 haste)。另:**死人不出血** — `_blast` 先殺掉的目標不掛 CC。

**07-16 雙展示台並存**:`app.stages.char`/`app.stages.unit` 各自 WebGLRenderer 持久重用(MUST NOT 每次新建);shell 按鈕 class + 閉包綁定(兩 shell 並存不可共用 id);`_celLightDirView` 是共用可變 uniform,各實例「set→render」同步不交錯故安全 — 引入非同步渲染前先重審這條。

**07-15 castfx 掛載**:特效群組直掛 scene、跟隨施放者靠每幀讀 `casterPos()`;**MUST NOT** re-parent 到 ent mesh 底下(effects 迴圈以 `scene.remove` 卸除)。

**07-15 LOS 停用條款**:未上傳 `world` 訊息(e2e/headless)= `_losGrid` 不存在 = LOS 整體停用 — **e2e 斷言依賴這條**,動 LOS 前先確認。擊毀障礙 MUST 標 `_losDirty` 懶重建,MUST NOT 逐殺全量重建(8Hz 熱路徑)。

**07-15 結構物碰撞的 y 還原**:撞牆幀 **MUST 把 y 一併收回位移前上限** — 只還原 x/z 會讓下一幀誤判已在山上、側壁規則解除(實測就是這樣穿牆)。`surfaceAt` MUST NOT 改成無條件 max(地形, 橋面)(橋下就穿不過去)。

**07-14 RECOIL 純客戶端**:後座力手感全住 `game.js`,伺服器完全不涉入 ⇒ bal/e2e 不受影響。**MUST NOT** 搬進 sim。

**07-14 fan 武器破例**:散彈輕武器是「輕武器只准 gun/rail/beam」的**唯一破例**(走 `{t:'plasma', slot:'light'}` 錐判定);launcher/missile 仍只准重武器。`fanFalloff` 無平台、越近越高。

**07-14 重武器射程不等式**:所有重武器解析後射程 > 塔 310(最短 = 電漿 316.8)。改任一重武器 base range MUST 重驗。

**通用(全期有效)**:`_snapPos` 重生瞬移 MUST 歸零 `ent.cfx/fireFx/heavyFx`;`paint` 花紋烤死靜止姿勢局部矩陣(MUST NOT 世界座標投影/每幀更新);`fitToHeight` 以整體 bbox 定尺 — 高聳配件會縮小機體本身,武器一律斜置收在頂高內。

---

## 4. 當前開發焦點

最近五個 commit(07-17 ~ 07-18:調整遊戲設置 / 無人機機體數值調整 / 新增空投物資細節 / 升級曲線調整 / 地圖地形調整)全是 **07-17 大改制後的校準迴圈**。進行中的工作面:

1. **八軌經濟參數微調**(`ECON.UPGRADES` 四面向 lw/hw/sk/ult + 四防禦、`UPG_BASE`/`UPG_INC` 階梯單價、賞金)— 每動必跑 `npm run bal`,盯 ③(收入 ≈ 八軌全滿 ±10%,現值 $2,954 vs $3,000 = 101.6%)與 ④(滿級拆塔 0~20% 剩餘、無人機站外 ≤200s)。
2. **空投物資調參與演出**(`AIRDROP` 表 + `hazards.buildAirdrop` + `game._syncAirdrop`)— 注意空投屬「戰場物資」不是被動收入,bal ③ 解析模型刻意不含它。
3. **波次戰力對齊**(加坦克後)— bal ①:三機體清波剩餘都要 ~40%,無人機靠 `SQUAD.BUFF` 旋鈕。
4. **地圖尺寸與野營佈點**(`MAP_EXPAND`/`CLEAR_F`)— 每動跑 headless camps 冒煙。
5. **NPC 開火視覺收尾** — 動任何武裝掛點後重跑 `audit_muzzle.mjs`。

**改動範式**:調參一律只動 `data.js` 常數 → 跑對應驗證(見根目錄 `/.claude.md` §5 矩陣)→ 不碰架構縫。新增負面狀態/獎勵路徑時,同步接上助攻戳記與重生清理,並補稽核腳本。
