# public/js — 模組層維護指南(蒸餾版,基準日 2026-07-29)

> **本檔定位**:雙層情境系統的**模組層**,搭配根目錄 [`/CLAUDE.md`](../../CLAUDE.md)(核心原則 / 單一真相縫索引 / A 反模式表 / 驗證矩陣)使用。本檔只留兩樣東西:**檔案職責地圖** + **全域層沒有的模組級地雷**(依領域分組)。逐日開發紀錄已蒸餾至此;完整過程見 git 歷史與根目錄 `CLAUDE-orig0718.md`。
> 本目錄 = 客戶端全部(渲染/FPV/輸入/HUD/展示台)**加上全遊戲平衡數值**(`data.js`,伺服器直接 import),是全 repo 改動最集中處。

---

## 1. 檔案職責地圖(Source of Truth)

| 檔案 | 職責 | 邊界與關係 |
|---|---|---|
| `data.js` | 平衡數值唯一真相:UNITS/WEAPONS/ECON(八軌)/GAME/CHARACTERS/HEROIC/SQUAD/THIRD/AIRDROP/ALTITUDE/RECOIL/BALLISTIC/SPECIAL/BOT_DIFF・BOT_OPS + 純客戶端視角數學(VIEW_LOCK/SCOPE/**SPEC_CAM** — 住這裡是為了讓離線稽核 import 得到真品) + 衰減公式 + 機體高度/命中量體(`SOLDIER_H`/`heroTargetH()`/`hitH()`/`hitR()`)+ `tierVal` + `solveTowerSites()` + 砲塔規則稽核(#4/#5)+ 兵線規則稽核(分離/U-turn/出入口)+ `CLASS_SYM` | 伺服器 import;敘事文字去 `lore.js`;推導值 MUST NOT 手寫(全域 §2) |
| `codex.js` | **角色 / 機體檔案格式的唯一真相**:`SECTIONS`(識別/簡介/詳閱/生成四段)+ `PROTO_LAYERS`(原型層與其 `visual` 推導來源)+ `GEN_FIELDS`(生成六欄,**角色與機體同鍵同義**)+ `CODEX_FIELDS` + `VIS_WORDS`/`HAIR_WORD`/`KIND_WORD`(列舉值 → 名詞)+ 組裝 `charCodex()`/`mechaCodex()`/`protoOf()` + 對外三份生成文字 `textSeed()`(文本生成)/`imagePrompt()`(2D 生圖)/`modelSheet()`(3D 建模)+ 驗證 `codexIssues()` | 只准 import `data.js`/`lore.js`/`mecha.js` **三支純資料檔**(無 three、無 DOM ⇒ 離線稽核與生成管線直接吃真品)。**格式住這裡、內容住 lore/mecha、數值住 data.js** —— 三者各一份;三份生成文字 MUST 由本檔組裝,MUST NOT 在 lore/mecha 手寫第二份(A40)|
| `lore.js` | 角色檔案內容:簡介(nat/age/sex/role/look/quote)+ 詳閱(bio/hobby/expertise/bond)+ 生成(`art` + `gen` 六欄) | 客戶端專用,伺服器 MUST NOT import。**機體原型已移到 `mecha.js`**(2026-08-04;舊 `proto` 自由字串整組退場,MUST NOT 復辟);欄位表住 `codex.js`,本檔 MUST NOT 自帶第二份 |
| `mecha.js` | 機體檔案內容:`code` 型號 + `proto` **結構化原型層**(每層 `{ src, note }`;層集由 `visual` 推導)+ `gen` 六欄 | **零 import**(同 `rng.js`/`vernacular.js`)。MUST NOT 手寫全高公尺數(走 `heroTargetH`)、MUST NOT 複製 `hue`/`kind`/`side`(那些住 `data.js`);鍵 = 駕駛員 id,機體換手時整格連同 `visual` 機體欄/`mods`/`MOVE_SIG`/`CAST_SIG` 一起搬 |
| `help.js` | 遊戲說明唯一縫:`CONTROLS_BY_KIND`/`TOUCH_CONTROLS`(觀戰那一欄由 `SPEC_CONTROLS` **join 推導**)+ `SPEC_CONTROLS`/`specControls()`(觀戰逐列說明,面板與選單同吃)+ `HELP` + `UI_TIPS`(GUI 懸浮提示文字;說明分頁「介面」類**由它推導**)+ `helpItemP()`/`helpCatLabel()`/`uiTip()`(輸入裝置分支唯一縫) | 客戶端專用;`if (touch)` 字串分支 MUST NOT 在 main.js 另寫(A21) |
| `tip.js` | GUI 懸浮提示唯一縫:`installTips()`(全域事件委派)+ `tipHTML()`/`tipDot()`/`attachTip()` + 單一氣泡節點 | 客戶端專用純表現層;消費端只准掛 `data-tip` 或用這裡的標記產生器,MUST NOT 另寫第二套氣泡、MUST NOT 退回 `title=`(觸控無 hover)|
| `npcicon.js` | NPC / 攻擊建築 / **機種**圖示唯一縫:`NPC_ICONS` + `npcIconHTML()`、`KIND_ICONS` + `kindIconHTML()`(皆平塗 24×24 SVG,吃 `currentColor`)| 純表現層;名冊真相仍是 `data.js UNITS`/`THIRD.COMP` 與 `CHARACTERS[ch].kind`,缺鍵回退問號圖不破圖。機種圖示的頭像角標一律經 `main.js charAvatarHTML()`(四處頭像共用),MUST NOT 自己拼 `<img>` |
| `models.js` | `makeUnit()`:機體建模、分節骨架、武裝掛點、rig 欄位**生產者**(`weap/hvy/gunR/gunL/muzzles/castSig/gait/...`)、`MORPH_HUMANOID`/`CAST_SIG`/`GUN_MOUNT` | rig 欄位只准在這登記;新增地面體態 MUST 同步 `MORPH_HUMANOID` |
| `locomotion.js` | rig **消費者**:`stepLocomotion` + `stepCombatFx`(MUST 在 stepLocomotion **之前**)+ `stepCastPose`/`stepJumpPose`(post-pass) | 戰場與展示台共用;MUST NOT 另寫預覽專用動畫 |
| `game.js` | `BattleClient`:FPV/物理/插值/彈道/準星/槍口/小地圖/RECOIL 手感 + 座艙取景(`COCKPIT` + `ndcH()`) | 彈道學在此(伺服器只驗證);RECOIL 純客戶端 MUST NOT 搬 sim;座艙稽核 `audit_cockpit.mjs` |
| `charPreview.js` | 展示台:與戰場共用 `makeUnit` + `stepLocomotion` | 事件源(`castFx/fireFx/heavyFx`)與戰場同一套欄位 |
| `castfx.js` | 招式 3D 演出,唯一入口 `spawnCastFx()`;逐角色演出表 `CFX` | 戰場與展示台共用;純視覺不涉 sim |
| `cutin.js` | 招式立繪演出,純 DOM overlay | MUST NOT 拉進 3D 場景 |
| `storyui.js` | 劇情畫面的**標記唯一縫**:`charAvatarHTML()`/`heroChip()`/`kindLabelOf()`(頭像 + 機種角標,五處共用)+ `chapterCardHTML()`/`briefHTML()`/`overText()`/`progressText()`(章節卡 / 開戰簡報 / 結算文案)+ `esc()`/`venueName()`/`chapterMeta()` | **兩個消費端:`main.js` 與 `tools/story_book/`**(本地故事書要「完全仿照正式遊戲的呈現」⇒ 同一份標記 + 同一份 CSS)。三條邊界:①**零 DOM、零 three**(`envLabel` 取自 `data.js`)⇒ 離線稽核 import 得到真品;②不碰狀態(解鎖/通關/主駕一律傳參數);③不綁事件(只帶 `data-i`/`data-ch`,點擊由呼叫端委派)。2026-08-11 自 main.js 抽出,規則不變只是換家 |
| `storytalk.js` | 劇情戰役**攻堅階段對話內容**:`STORY_TALK`(6 章 × 2 陣營 × 3 階段)+ `talkOf()`/`stageKey()` | 客戶端專用純資料(同 `lore.js`/`story.js`,伺服器不 import)。階段順序**只准來自 `data.js SIEGE.STAGES`**,本檔 MUST NOT 自帶第二份;發言者 MUST ⊆ 該章 `story.js` 雙方名冊、每人每場 4~5 句、三場蓋滿名冊(稽核 `audit_story_talk` 釘住)。角色的過去只准取自 `lore.js` 的 `bond` 與 `docs/characters.md` §五關係網,**MUST NOT 在對白裡發明與角色檔案矛盾的設定** |
| `dialogue.js` | 劇情對話**演出層**:`Dialogue`(`radio()` 戰鬥中下緣對話條 / `scene()` 結算畫面全篇對照稿 / `track()` 攻堅進度條) | 純 DOM overlay(同 `cutin.js`,MUST NOT 進 3D 場景);**只負責畫** —— 不認得章節、不判階段、不決定誰說話。定時器全部登記進 `this.timers`,離場 `dispose()` 一次收乾淨(漏收 = 下一場戰役冒出上一場的台詞);逐句切換 MUST NOT 掛 CSS transition(同 `.blood-splat`/`.cc-flash`) |
| `audio.js` | 戰場音效唯一縫 `GameAudio`(程序合成 fallback + CC0 樣本 manifest;SFX/BGM 雙聲道獨立,localStorage `svs_audio`) | app 層持有、跨戰局存活;純表現層不涉 sim |
| `paint.js` | `heroPalette()` 色版 + `paintUnit()` 花紋(徽記/國旗/覆蓋);花紋數值唯一真相(國旗色 = `data.js visual.flag`) | 在 fitToHeight/outlinify **之前**呼叫;烤死靜止姿勢局部矩陣 |
| `main.js` | 房間/選角/雙展示台/HUD DOM/NPC 圖鑑/地圖預建(`startPrebuild`) | shell 按鈕 class + 閉包定位,MUST NOT 共用 id |
| `toon.js` | 賽璐璐渲染核心(`mat()`/`outlinify`/`CEL_PAINT`/`CEL_ATLAS`)+ GPU 回收唯一縫(`markShared()`/`disposeTree()`)+ **軟性物質唯一縫**(`INK_SOFT_A`/`SOFT_KINDS`/`WIND`/`stepCelWind()`/`celWindTime()`)+ **世界曲面安裝點**(`installWorldCurve()`/`worldCurveOn()`;改 three 的 `ShaderChunk.common`+`project_vertex`+`ShaderLib.sprite`,常數自 `data.js` 的 `curveKneeM()`/`curveR()` 展開) | 唯一材質包裝縫;`disposeTree` 只准一份(A25);著色器補丁(含招牌逐實例 UV 的 `atlas` 選項)MUST 全住這裡。軟性 = 細勾線 + 隨風飄揚**共用同一個旗標**,風向/時鐘全場一份(A39)。世界曲面是**唯一一處改 three 共用 chunk** 的地方:模組載入時裝一次(重入守衛戳記掛在 `THREE` 上)、錨點對不上就整套退平面;曲面的**公式**住 `data.js`,這裡只負責把它接上著色器,MUST NOT 在本檔手寫公尺數 |
| `biomes.js` / `ground.js` / `terrain.js` | 地景/道路/橋隧/水域/地被 + 地形射線 `rayTerrain()`。單一縫群:邊界障礙環 `buildEdgeWall()`(biomes;blockers 的第一批)、緩衝空間外緣裙(terrain;地形範圍之外那一圈,與地形共用材質)、站立面 `surfaceAt()`、天然地形高 `natureAt()`(開挖/整平**之前**的高度場快照 —— 明隧道落差掃描問的是天然地形,自家路塹不算)、隧道覆蓋 `tunnelCoverIntervals()`、剖面 `tunFloorAt()`、地下道 `underpassPlan()`、明隧道 `tunnelWallProfile()`、橋去重 `dedupeCrossingBridges()`/`dedupeParallelBridges()`、橫切繞行 `skirtWaterClips()`、墩座 `planTowerBridgePads()`、地貌交界 `planSeamOverlays()`+`SEAM_STYLES`、小區域包裹 `planEnclaves()`+`ENCLAVE_STYLES` | 消費端 MUST NOT 各自呼叫 `heightAt` 當地面高度、MUST NOT 把 `terrain.mesh` 丟進 raycaster(A6)、MUST NOT 硬編第二份樣式/組合表 |
| `hazards.js` / `vfx.js` | 戰場物件(含 `buildAirdrop`)/特效 | 高頻特效一律 `unitGeo` 共用幾何 + scale(A25) |
| `xform.js` | 零件 × 實例變換數學唯一縫:`vegPartXform()` | 渲染與 `audit_object_joints.mjs` 共用同一份(A27) |
| `climb.js` | 攀爬路線唯一縫:`CLIMB` 常數、`planClimbRoutes()`、`surfacePoint()`、`makeClimbIndex()`、`buildClimbMeshes()`、`buildClimbArrows()`、`planLink()` | 純客戶端,伺服器 MUST NOT import(A31) |
| `siteplan.js` | **場址配置規則唯一縫**(哪裡放得下/放幾個/隔多遠/朝哪邊):§A 都市計畫(`URBAN`/`CIVIC`/`CIVIC_KINDS`/`CIVIC_PARTS`/`roadFaceRy()`/`planBlocks()`/`partCollider()`/`civicColliders()`/`buildCivic()`)、§B 樹冠羞避(`CROWN`/`crownGap()`/`planShyGrove()`)、§C 地質排列(`ROCKFIELD`/`strikeRad()`/`planRockField()`) | 前半段(常數/零件表/三支規劃器)**MUST 維持零 THREE**、只認呼叫端給的 `probe` 回呼(同 `beacons.js` 紀律③ —— 這是它離線可驗的唯一理由);零件外廓與幾何合併共用 `beacons.js` 的 `partExtent`/`mergeGeos`,MUST NOT 各抄一份。街廓與公設**零共享 `rnd()`**(A38)|
| `edgewall.js` | **邊界牆型錄 / 緩衝布景 / 視線邊界背景唯一縫**(2026-08-11 使用者定案 15 款):`EDGE_WALL` 參數、`WALL_KINDS`(dom/bio/**slope**/depth/h/label)、`SLOPE_TIERS`/`wallSlopeTier()`(陡 = 只有自然景觀 / 中 = 再加倒木與長城 / 緩 = 全部;門檻由呼叫端注入 `SLOPE.EASE_DEG`·`BLOCK_DEG`)、`wallCandidates()`、`edgeSeed()`、`planWallRuns()`(地貌 / 水陸域 / 坡度級改變或太長就換一款)、`wallParts()`(逐款 `(len, D, H, rnd) => 零件表`;城牆逐節再抽構造 —— 素牆 / 箭樓 / 砲台 / **城門 + 城樓**,門恆關、砲只是幾何)、外廓量測 `partBox()`/`wallFit()`/`wallFaceCover()`、緩衝布景 `PROP_KINDS`/`propKindFor()`/`propParts()`/`planBufferProps()`、視線背景 `BACKDROP_KINDS`/`backdropKindFor()`/`backdropParts()`/`planBackdrop()` | **只 import `rng.js`、零 THREE**(同 `beacons.js` 紀律③/`siteplan.js` 前半段 —— 這是離線稽核算得出零件外廓的唯一理由);**零共享 `rnd()`**,「隨機更換」一律由座標雜湊起自己的 `mulberry32`。建幾何住 `biomes.js`(`buildEdgeWall`/`buildBufferProps`/`buildBackdrop` + 共用發射 `emitWallParts`/`flushPartBatch`),合併走 `beacons.mergeGeos` 唯一縫。A44 |
| `roadgrid.js` | **道路格網量化唯一縫**:`ROAD_GRID` 常數 + `dirAngle`/`halfBin`/`densifyM`/`minStraightM`(全推導)+ `gridAngle()`(地圖主方位:mod 90° 長度加權圓平均,烘焙與 `ground.js` 擺件格網同吃)+ **`GRID_HW`**(主方位取樣面 = 大馬路;`.source` 就是 Overpass 查詢字串)+ **`roadGridRotDeg()`**(一組 way → 旋轉度數的唯一縫:取樣面 + 未旋轉量測框 + 取負號綁在一起,離線烘焙與自訂地圖執行期量測同吃)+ `waySegs()` + `quantizeRoads()`(細分 → 位置空間 DDA 指派方向 → 節點鬆弛 → 逐段長度重解)+ `dirErrorDeg()` | **MUST NOT import 任何東西**(同 `rng.js`/`vernacular.js`):投影由呼叫端以 `toXZ`/`toLL` 回呼注入,這是離線稽核吃得到真品的唯一理由。**零亂數**(§2.3)。旋轉本身不住這裡(住 `data.js llToXZ`);建築朝向也不住這裡(道路一被量化,`nearestRoadAngle`/`roadFaceRy`/`ground.orient` 自動落在同一組 16 方向 —— 在本檔另外碰建築 = 兩套朝向打架)。A42 |
| `venueGrid.js` | 離線烘烤的 28 個場地主方位(度) | 只准由 `tools/bake_venue_grid.mjs` 生成;消費端只有 `venues.js venueConfig()` |
| `venueLanes.js` | 離線烘烤的真實道路兵線(63 組) | 只准由 `tools/bake_venue_lanes.mjs` 生成 |
| `vernacular.js` | 在地文字語料唯一縫(招牌上**寫什麼**):`TEXT_KINDS` 八類、`classifyOsm()`、`harvestOsm()`、`pickName()`/`pickRef()`(**全專案唯一取名規則**)、`spineOf()`(地名主幹)、`SIGN_CLASSES`(五種語料庫語域的容量)、`signCopy()`(挑字 + 去重 + 容量篩)、`textW()`、`LOCALE_LEX`/`ELE_WORD` | **MUST NOT import 任何東西**(同 `rng.js`/`ctrlmode.js`:離線烘焙 `bake_venue_text.mjs` 與稽核 `audit_vernacular.mjs` 直接 import 真品)。只出字串,MUST NOT 碰畫布/幾何;畫在哪一格走 `worldtext.js`(A37)|
| `venueText.js` | 離線烘烤的 27 場地在地文字語料(八類 3550 則) | 只准由 `tools/bake_venue_text.mjs` 生成;改 `vernacular.js` 的分類/取名規則 MUST 重烤 |
| `worldtext.js` | **唯一**的文字圖層(招牌**怎麼畫、畫在哪一格**):`resolveName()`/`canRenderText()`/`SIGN_STYLES`/`signAspect()`/`packCells()`/`SignSheet` | 純表現層;MUST NOT 出現第二套文字圖層(舊 `signage.js` 已退場,A37)|
| `mobile.js` | 觸控/陀螺輸入轉接層 + 手機版型唯一縫:`isTouchUI()`(← `ctrlmode.js usePad()`)、操作方式設定 UI `renderCtrlSettings()`/`syncCtrlSettings()`、`TOUCH` 偏好、`gyroBlockedReason()`、`installTouchUI()`、`lowPower()`、`TouchControls`(虛擬手把)、`Gyro`(兩條感測路徑) | **只准把觸控/陀螺轉成既有輸入**:視角走 `_applyLook()`、移動走 `_moveAxis()`、動作走 `_cmd(act,down)`;MUST NOT 直接改 `yaw/pitch/keys/firing`、MUST NOT 自寫結算;招式 CD 由 `main.js padMirror()` 鏡射 |
| `net.js` | 傳輸層唯一入口 `makeNet()`(`Net` WebSocket / `LocalNet` 單機) | 呼叫端 MUST NOT 自己 `new Net()` |
| `netmode.js` | 連線機制唯一真相:`LINK_MODES`、`netMode()`、`soloOnly()`、`cloudUrl()`/`wsUrl()` | 客戶端專用;`main.js` MUST NOT 自己看 `location.host`(A28) |
| `ctrlmode.js` | 操作方式唯一真相:`CTRL_MODES`(不限定/限定滑鼠鍵盤/限定搖桿)、`ctrlMode()`(生效值)/`ctrlPref()`・`setCtrlPref()`(我的預設)/`roomCtrlMode()`・`setRoomCtrlMode()`(房主定案)、`ctrlScheme()`/`setCtrlScheme()`、`deviceScheme()`/`touchCapable()`、`ctrlMismatchText()`/`onCtrlChange()` | **MUST NOT import 任何東西**(離線稽核 `audit_ctrl_mode.mjs` 直接 import 真品、`server/rooms.js` 也 import 它取合法值);裝置判定只有這一份,`mobile.js isTouchUI()` 只是轉呼 `usePad()`;「操作方式由房主選擇(整房一致)、目前操控併入設定且限定時不可變更」的規則只住這裡 |
| `localhost.js` | 單機:動態載入 `../../server/rooms.js` 在分頁跑 `RoomHub` | import 路徑依賴鏡射佈局;離場 MUST `hub.shutdown()` |
| `geocache.js` | 場地圖資快取(首次完整成功即定案) | 入庫條件 MUST NOT 放寬(見 §2-B) |
| `osmrelay.js` | **路網中繼 payload 唯一縫**:`OSM_RELAY` 常數 + `osmRelayKey()` + `sanitizeOsmRelay()`(形狀/上限,冪等)+ `osmRelayFit()`(位元組封頂) | **MUST NOT import 任何東西、MUST NOT 有任何模組級可變狀態**(同 `ctrlmode.js`:`server/rooms.js` 也 import 它;單機模式下 hub 與客戶端拿到的是**同一個**模組實例 ⇒ 放 store 就等於兩端共用)。中繼資料的 store 住 `biomes.js`(`commitOsmIn`/`osmInOf`)。payload MUST NOT 帶座標框(θ 凍結,A42 ③)。A43 |
| `venues.js` / `mapSelect.js` / `environment.js` | 場地表/選址 UI/環境 | `environment.js` import three ⇒ **環境標籤 `envLabel()` 的唯一縫已抽到 `data.js`**(2026-08-11;它只是 `ENV` 的取名查表,而 Node 端的稽核與 `storyui.js` 都要印它),本檔只留舊入口 re-export,MUST NOT 重寫一份。同理 `portraits.js` 的 `mulberry32` 直取 `rng.js` 而不繞 `hazards.js` |

---

## 2. 模組級地雷(依領域;全域 A 表/縫索引已覆蓋者不重複)

### A. 權威結算與戰鬥(sim / data / game)

- 單機序列化:`_serializeEnt` MUST NOT 洩漏權威可變物件的參考(巢狀欄位一律展開複製,如 `o.up = {...e.upg}`)—— 單機傳輸層不經 JSON 深拷貝,客戶端樂觀更新會就地改到伺服器狀態(雙重遞增)。
- 塗層阻擋(A6b)入口三層:`_terrainSegT`(地形,唯一走 `rayTerrain`)→ `_obstHitT`(障礙盒/圓柱 ∪ 橋面/隧道頂板/**路面**薄板)→ `_layerHitT`(兩者取近)。表現層彈體 `_updateVisShells` 與榴彈虛線 `_arcTrace` MUST 吃 `_layerHitT`;`_updateBullets`/`_resolveAim` 自己併 `_terrainHitT` + `_obstHitT`(需要分辨 hit.terrain)。`_slabHitT` 的 `!tn.open` 閘同時管頂板與路面。**隧道頂板是有厚度的板體 [`tn.ceil`, `tn.roof`]**(2026-08-03;與橋面 [`deckY − deckUnder`, `deckY`] 同語意,`roof` ← `biomes.js tunRoofTop` 單一縫)—— 只驗「跨過底面 `cy`」會漏掉貼著頂板削過去的擦邊彈,也就是站在明隧道頂板上朝洞口低伸射擊那一發。爆點回報(`t:'burst'`)洞內的離地基準 = `btn.floor`(heightAt 是未開挖的山頂),但 `lev` 刻意只報 0/2(橋面報 1 = 橋上砲塔 AoE 免傷)。
- 貫穿:首個目標 MUST 全額(bal 不變式);`ARMING` 最短距離內導引 MUST NOT 接手、`b.oy` 擊發當下取樣;bot 的 line 分支吃加粗但射線長維持 bot 自己的射程判定。
- 榴彈:最小射程唯一縫 `lobMinRange(def)`(爆風半徑推導),過近自損由 `blastFalloff` 導出,MUST NOT 另寫公式。火控解與彈道積分收在 `_lobLadder` / `_arcTrace(…, draw)` 兩支:繪製走 draw=true(寫 `_arcGuide` 緩衝)、射程光暈的可命中判定走 draw=false ⇒ **判定端 MUST NOT 自己寫一份簡化積分**(那正是「光暈亮著卻沒命中」的成因)。對空越障仍走 `LOB_CHARGE` 降裝藥(同初速高角解 = 85°、飛 20 秒,MUST NOT 改用)。
- 榴彈投擲改制(2026-08-02 使用者定案四條),四個各自無聲的寫壞法:
  - ①**準星是唯一目標來源**(2026-08-10 收尾:「拋物線準星沒有瞄敵人時就是打地面」):目標解只有 `_lobCrosshair` 一份(`_updateAaMode` 判要不要進彈射模式、`_lobAim` 取瞄準點,同一幀同吃;它同時是那份「鏡頭/槍口沒動就沿用上一幀」的射線快取 —— 每幀 `_resolveAim` 是 3.7ms 熱點)。兩端各打一條射線 = 模式判成對地、瞄準點卻鎖在飛機上。錐形輔助 `_aaTarget`(8° 錐內最正對的飛行單位)與常數 `BALLISTIC.AA_CONE` **整組退場**:它無條件把瞄準點從準星底下拉到旁邊的無人機,而 `_aimTarget` 對 lob 直接吃火控解 ⇒ **鎖定跟著一起被拉走**(使用者兩次回報)。中間那一版把它降級成「準星什麼都沒解到才接手」仍不夠 —— `ent` 為空同時涵蓋「打在地形上」與「指著天空」,而瞄地面正是榴彈的主要用法 ⇒ 偏離 0° 的地面落點照樣被錐緣 8° 外的目標拉走。**準星沒解到單位就是打地面,那是規則不是缺口**,MUST NOT 以任何條件復辟第二個目標來源。
  - ②**對地固定 45°**:角度定死、**初速反解**(`_lob45Vel`:`v0² = g·L²/(L−dy)`),落點恆為準星那個部位。45° 同時是最大射程角與 dy=0 的最小能量解 ⇒ 弧線恆高於同初速低伸解,越障能力本來就優於舊階梯任一級,階梯因此對地面整組讓位(**MUST NOT 兩套並存**)。`L ≤ dy`(目標仰角 ≥ 45°,懸崖正上方)無解 ⇒ 退回階梯,低伸解的仰角本來就比 45° 更陡 = 同一條曲線的延伸,MUST NOT 改成「打不到」。
  - ③**對空維持現狀**:`aa` 旗標 MUST 一路傳進 `_lobLadder`(`_lobAim` 傳 `fc.aa`、`_reachable` 傳 `TARGET_CLASS==='air'`)—— 漏傳就是光暈拿另一條彈道回答火控解的問題。
  - ④**中途碰撞就爆、目標移開就續飛、出射程原地爆**(見下一條的球面規則)。另:45° 解把初速反解到只剩 41~46m/s,`ARC_MAXP` MUST 收得下整段飛行(384 點 ≈ 11.5s;舊 264 會在落地前用光緩衝 ⇒ 打山谷下方的敵人光暈永遠不亮)。`_spawnVisShell` 的對地弧 MUST 同吃 `_lob45Vel`,否則同一個落點兩端畫出兩條不同的弧。
- **射程 = 以射擊點為中心的球面,與軌跡無關**(2026-08-02 使用者定案;唯一一把尺 `|彈頭 − 發射點| ≥ b.max`,四個消費端 `_updateBullets` / `_arcTrace` / `_updateVisShells` / 解保險 `ARMING` 全吃):弧長、導引修正航跡、離架散布繞的路一律不計。**航跡長 `b.dist` 已整組退場**,連同逐彈道分支旗標(`b.seek`/`b.lob`)—— 一顆球面管全部,復辟第二把尺就是靜默丟包:①導引彈的航跡恆長於直線(離架散布 + 一路修正)⇒ 打射程邊緣的目標必定在飛到之前自毀(實測微型攔截彈滿射程恆差 5.5m);②45° 弧長恆為水平距離的 1.148 倍 ⇒ 射程無聲砍 13%、砲彈在抵達瞄準點之前半空自爆。這條同時把其餘三處對齊成同一個球:失鎖判定、伺服器 `heroBurst` 落點閘門 `dist2d(射手, 落點)`、射程光暈 `from.distanceTo(aim)`;唯一例外 = 射後不理**鎖定後**改吃追擊燃料(A7)。**雷射導引吃同一條規則**:導引點出球面只是 `b.guide = false`(不再修正航向、改吃重力直飛),MUST NOT 在那裡把彈體收掉 —— 它照樣飛到球面才原地爆。**這顆球是遊戲空間的正球**(使用者定案):x/z/y 同一個單位、三軸等權,`game.js` 全檔不得出現 `REAL_SCALE`/`COMBAT_SCALE`(前者只住 `terrain.llToWorld`/`sim.llToMeters` 的經緯度邊界、後者只住 `data.js` 統一縮放塊)。遊戲高程走**真實公尺**而水平吃 `1/REAL_SCALE` ⇒ 地形看起來比真實平緩約兩倍,這是刻意的**地形**設計,MUST NOT 拿它當理由在射程的 y 軸乘補正(球面一變橢球,伺服器就與客戶端那顆正球分家)。稽核 `audit_weapon_gate.mjs` Ⅻ⑷ 以「同一段距離擺水平與擺垂直同判」行為直測釘住。
- 範圍光暈 —— 語意三代,每一代都是使用者回報推動的:①2026-07-29「射程範圍內的敵人都亮」②2026-07-30「榴彈類常常光暈亮著卻沒命中對方」⇒ **打得到**才亮 ③2026-08-03「範圍光暈不是單純在射程範圍內就亮,而是:**準星的目標在射程內被擊中時、同時會受到傷害的單位才亮**」⇒ **這一發的傷害足跡**。
  - ② 亮的是「我打得到的每一個敵人」——190m 的重武器在前線會讓半個畫面同時發光,而其中絕大多數不是**這一發**會傷到的人;玩家真正要的資訊是「現在扣扳機,誰會掉血」。
  - **入口閘仍是 ②**:`data.js reachRule()` → `game.js _reachable()` 逐彈道五類分派(拋物線跑火控階梯、直線跑 `_layerHitT` 線段;爆炸戰鬥部看「落點是否落在 `blastCoreR` 核心帶內」—— **刻意不要求 LOS**,A11 繞射近似;直擊/貫穿看「線段整段淨空」)。判據逐位元不變,只是消費端從「逐敵人」收斂成「準星那一個」:**準星那一發打不到 ⇒ 誰都不會受傷 ⇒ 全場熄燈**(MUST NOT 只熄準星目標那一顆)。
  - **名冊唯一縫 = `_shotVictims`**,分類只走 `aoeClass(def)` 一份,幾何逐類鏡射伺服器:`blast` ← `sim._blast`(量到命中量體最近點 = 水平 `_hitR` + 垂直帶 `_bodyDy`,`blastFalloff > 0` 入列)/ `fan` ← `sim.heroPlasma`(水平夾角 + 逐目標射程 + 射線淨空)/ `line` ← 既有鏡射 `_lancePierced` / `null` ← 只有準星那一個。**blast 那一支 MUST NOT 出現任何 LOS 或射程閘**(A11:牆邊炸開照樣傷到牆後的人、落點外緣的敵人照吃濺射 —— 這正是這一代最想讓玩家看見的那一組單位)。
  - 四個無聲寫壞法:①退回「射程內就亮」(回到 ① 代);②在名冊裡多加 `if (def.type…)`(第二份分類表 = 與 `aoeClass` 分家);③在名冊裡重解一次彈道(拋物線 MUST 吃每幀已定案的 `_lobFc`);④讓警示色逐目標各判一次(`_shotWarn` 是唯一實作、恰兩個消費端 `_reachable` + `_shotVictims` —— 各寫一份就會「同一發在準星上是琥珀、在濺射名冊上是陣營色」)。
  - **扇形與貫穿刻意沒有整發性的入口閘**:錐與圓柱都從槍口張開,伺服器 `heroPlasma`/`heroLance` 同樣不看準星解到誰。準星壓在**射程外**的遠方敵人身上時,錐內 5m 的那一個照樣會掉血 —— 讓入口閘把整組熄掉就是騙人(這條與 blast/單體那半刻意不同,MUST NOT 為了「三類寫法一致」把它們合併)。射程與視線由**逐目標**的 `_inShotRange` 夾(誠實界,與 `_surfD3 ≤ range × _altRange` 同一把尺)。
  - 舊的分幀預算 / TTL 快取(`RANGE_GLOW.TTL_S`/`ARC_PER_FRAME`/`RAY_PER_FRAME`)是「每個敵人各跑一次彈道積分」時代的節流,改制後貴的判定只剩準星那一發 ⇒ 整組退場,MUST NOT 復辟。警示色(琥珀)= 打得到但命中率低/會自損:榴彈最小安全射程內、導引/射後不理的 `ARMING` 軌跡修正期內。稽核 `audit_weapon_gate.mjs` Ⅳ・Ⅴ・**Ⅴ-b**。
- 射程閘門容差:客戶端的彈道上限是**這一發的有效射程** `_effRange(def, ent)`(= `range × _altRangeTo`),伺服器每道閘門 MUST 用 `RANGE_TOL`(單一縫)且**收得下這個上限**。落點/爆點類閘門(`heroBurst`)沒有目標實體、算不出高程差 ⇒ MUST 乘 `altRangeMax()` 當誠實界;寫成比客戶端緊(前科:獨自寫死 1.15)= 高地上合法的那一發被驗證後靜默丟棄,畫面上砲彈在敵人身上炸開、傷害是 0。
  **反過來,寬容 MUST NOT 用在伺服器自己選目標的路徑**:`heroPlasma`(扇形)只收到一個射向、`heroLance` 的逐目標判定沿圓柱由伺服器選人 ⇒ 乘 `RANGE_TOL` 等於白送 25% 隱形射程,光暈不亮的敵人照樣掉血(2026-08-01 使用者回報「攻擊範圍異常」「超過射程範圍就沒傷害」)。兩條都吃誠實界(與 `botFire` 同一條規則);`heroLance` 與 `heroPlasma` 的 `d3` 都從**回報的槍口**量起 = 與 `_reachable` 的 from 同一個點,誠實界不會誤丟。**誠實界路徑的球心 MUST 是槍口**(2026-08-02 扇形補上 `o` 回報 + 與 lance 同一道 `dist2d ≤ 12` 防作弊閘;取不到才退回機體中心 —— bot 沒有客戶端也沒有槍口回報):舊制扇形從機體中心量,而客戶端的彈舌與射程光暈都從槍口量,槍口前伸多少就有多寬的一條邊界帶「光暈亮著卻不掉血」—— 誠實界沒有 `RANGE_TOL` 可以吸收這個差(`heroHit` 吃 ×1.25 所以蓋得住,扇形/貫穿蓋不住)。錐內的 LOS / 稜線射線起點 MUST 跟著同一個球心(起點分家 = 打得到卻被自己那面牆擋住)。
- 地形稜線遮蔽(2026-08-01「直線攻擊與扇形攻擊要避免隔山打牛」):伺服器是**無地形高程**的 2D 平面,`_losBlocked` 只認 occ 柱與 slabs 板。客戶端回報型攻擊被本端 193² 解析地形截斷過所以沒事;**伺服器自己選目標**的兩條路(`heroPlasma` 錐內、`_lanceHits` 圓柱內 —— 後者含 botFire 自建的未截斷射線)沒有這道保護 ⇒ 穿山扣血,而射程光暈吃 `hit:'clear'`(逐段淨空,含地形)早就說打不到。修法:房主 `bakeHeightGrid` 把粗高程網格隨 `t:'world'` 上傳(編碼縫 `hgtEnc`,兩端同吃),伺服器 `_ridgeBlocked` 判稜線。四條 MUST:①**MUST NOT 併進 `_losBlocked`**(客戶端回報路徑再驗一次 = 兩份解析度打架 ⇒ 多擋 = A30 靜默丟包);②判定全程在**絕對**高程框(`_absSightY`;拿離地 y 直接比絕對地形 = 平地也整片判成被擋);③偏差方向一律朝「不擋」(`RIDGE_M` 餘裕 + 兩端 `RIDGE_SKIP_M` 豁免帶 —— 雙方都站在地面上,端點附近地表本來就貼著射線);④**隧道內一律放行**(高程場是未開挖的山體,洞內頭頂恆壓著一座山;洞內外隔絕歸 `_slabBlocked`)。未上傳(e2e/headless/舊版房主)⇒ 判定 no-op,舊行為逐位元不變。稽核 `audit_weapon_gate.mjs` Ⅷ。
- 高度制空射程(2026-08-01 使用者回報的另一半):曲線唯一縫 `data.js altRangeF`;客戶端 `_altRangeTo(ent)` **逐目標**、與伺服器 `sim._altDh`/`_altRange` 逐條同構,MUST NOT 退回「拿前方地表近似目標高程」的第二份公式。**高度差 MUST 同參考框相減**:英雄的 `ay` 是絕對高程(含地形海拔)、NPC/塔在伺服器是離地高 + 基準 0 —— 跨框相減 = 拿場地海拔當高度差,海拔一過 78m(3 個砲塔高)就封頂 ⇒ 英雄對**所有** NPC 恆 +25% 射程,而客戶端光暈恆 1。三個消費端(射程 `_altRange` / 爆擊 `_altCrit` / 閃避 `_dodges`)MUST 全走 `_altDh`,只修射程那一份等於沒修。搜尋半徑走 `_maxRange`(機制上限,只准寬),**解到目標後 MUST 以 `_effRange` 誠實夾回** —— `_tryFire`(擊發 + 彈體 `b.max`)、`_lobAim`(火控階梯的射程包絡)、`_tickLock`(可鎖距離)、`_reachable`(光暈)四處同吃這一個數字。
- 攻擊範圍收斂 + 三軸預算(2026-08-02 使用者定案,見全域 §2.1 三列與 A35):①**輕重武器縮減攻擊範圍,除了以範圍見長的武器之外,其他都避免一次打到兩座塔** —— 上界 `soloBlastRmax()` 由「同塔位塔距 ÷2 − 塔的命中量體半徑 ÷ `BLAST.EDGE`」推導,夾制只作用在 `aoeClass` 的 **blast** 一類(fan 扇形的大面積就是它的本體 = 使用者說的範圍見長;line 貫穿要吃到兩座塔必須射手與兩塔共線,夾它等於把貫穿機制拿掉),豁免走 `AREA_WEAPONS` 凍結名冊。②**收掉的範圍要還回火力,但只能是重分配** —— `bal ①④⑤` 全是單體模型,爆風半徑在那三條裡從來沒進過算式,補償當純加法發下去它們只看得到「傷害整批變高」;故整批除以幾何平均 `AOE_BUDGET.NORM`,`W` 是分配陡度不是總量。③使用者要求的四軸(DPS / 攻擊範圍 / 射程 / 移動速度)一律有價:`aoeTrimF` / `rngDmgF` / `mobDmgF` 與 `counterDmgF` 並列在 `heroWeapon` 的 `dmg` **同一欄**,各只准出現一次(少一處 = 沒計價、多一處 = 平方計價);機動與射程以**同儕幾何中點**為軸(射程逐槽位取,輕重混算會變成量槽位)。④會靜默壞掉的兩件事:夾制**四捨五入**或漏了 `tierVal` Lv4 外推的反解,頂階就會回到上限之上、規則整條失效(畫面上只表現成「這把武器拆塔特別快」);核心帶 `blastCoreR` 隨半徑縮小(6.0m → 1.5m)⇒ **`SEEK.R_M` MUST 重掃**(舊值 200m 在 50% 射程有 4 把導引武器打不進核心帶 = 光暈亮著卻沒命中,A30 家族),現值 60m。稽核 `audit_aoe_trim.mjs`(58 項,三條反向驗證過的紅字)+ `npm run bal` ⑦(前線交戰模型 `tools/lanesim.mjs` —— **攻擊範圍唯一被計價的模型**)。
- 移速壓縮(2026-08-04 使用者定案「所有機體的移速拉近差距,但排序不變」;唯一縫 `data.js SPEED_COMP`/`speedMid()`/`spdComp()`/`heroMobility()`/`evasionMinSpeed()`):曲線與 `FIRE_RATE` 同形(冪次、**嚴格遞增 = 排序不變的保證**),但**軸取幾何中點不取帶底** —— 射速壓縮有 `dmg ÷ f`、`mag × f` 兩欄把 DPS 補回來,移速沒有那種補償欄,錨在帶底就是全體變慢(對局節奏整個拖掉),而錨在中點是快的降、慢的升,壓縮後整組的幾何中點逐位元不動(同 `AOE_BUDGET.NORM` / `BUILD_DPS` ②「收斂 MUST 是重分配而非削弱」)。四個無聲寫壞法:①**消費端直接讀 `UNITS[kind].speed × mods.speed`** —— 那是壓縮**前**的原始值,而且舊制客戶端與 bot 連 `mods.speed` 都沒乘(只跑機種基準速)⇒ 伺服器閃避、兩支平衡模型、圖鑑機動欄與腳下實際跑的速度分成兩份,只有拿碼表量才看得出來;②**閃避門檻沒跟著壓縮** —— `EVASION.MOBILITY_MIN` 是同一條速度軸上的一個點,壓縮把慢的往上抬,門檻不動的話 32 台會整組落到門檻之上、「重甲慢速機體站著吃彈」當場失效(唯一縫 `evasionMinSpeed()`,三個消費端 sim/duel/lanesim 同吃);③取樣面漏掉變形者的飛行巡航(飛行型態不受壓縮);④手寫中點(任一 `mods.speed` 一改軸就失準)。**K 有實測下界**:K ≤ 0.4 起 bal ④ 的變形者推不掉第二座塔 ⇒ 0.5 是「①④⑤⑦ 同時全綠」的最強壓縮。稽核 `audit_speed_comp.mjs`(30 項,兩條反向驗證過的紅字)。
- 建築加乘移除 + 護盾/裝甲分軌剋制(2026-08-02 使用者定案,見全域 §2.1 兩列與 A34):①對建築的兩層加成(各武器 `vs.building` + launcher 專屬 `GRENADE.BUILDING_MUL`)一起拿掉 —— 後者整組刪、前者夾到 ≤`BUILDING_VS_CAP`(**只留懲罰不留加乘**)。夾制是 CHARACTERS 之後的一段推導迴圈,MUST NOT 逐武器手改 vs 表;名冊漏一張(招式 skill/ult、`WEAPONS`、`DECOY`/`HYPER`)= 那條路徑還留著攻城加成,畫面上只表現成「這招拆塔特別快」。②剋制新增「目標**血條層**」這一軸:`vsSp`/`vsHp`/`spPierce` 三個旋鈕組出反護盾 / 穿盾 / 反裝甲三型,拆分只准住 `data.js shieldSplit()`。**四個消費端**(伺服器 `_damage`、客戶端 HUD 估算、`duel.apply`、`balance` 的對塔/對 NPC DPS)MUST 全走這一支 —— 任一端自己寫一次 `Math.min(sp, dmg)`,症狀是「HUD 數字與實際掉血對不上」或「bal 說平衡但打起來不是」,兩者都極難從畫面反推。三個寫壞法:**(a)** 把打穿護盾後溢出的**護盾傷害**直接倒進裝甲層(正解是按「被護盾吃掉的原始傷害預算」折回)—— vsSp 高的武器會在殘盾目標上白賺一次加成,而且只在「盾剛好見底」那一發出現 = 對局裡看起來是隨機爆發傷害;**(b)** 讓 `vsSp = 0` 退化成「無視護盾穿過去」(正解是護盾全擋,原則 6;要穿請用 `spPierce`);**(c)** 只讓 `vsHp` 對英雄生效 —— NPC/建築沒有護盾層,`shieldSplit(wd, dmg, 0)` 就是「整發吃 vsHp」,漏掉這條等於偷偷多了一套只對小兵成立的規則。旋鈕**刻意不過 `tierVal`**(是武器性格不是火力)。中性參數(1/1/0)逐位元還原舊制 ⇒ 沒標旗標的角色完全不受影響。③**三條配置紀律做成推導**(使用者定案):**穿盾/反裝甲只掛在原本吃建築加成的武器上**(名冊 `EX_SIEGE_WEAPONS` —— 那批攻城重武器剛被拿掉加乘,新軸把預算還回去;掛別把 = 舊的沒還又多發一份;反護盾刻意豁免,走另兩條路);**反護盾武器的 `vs` 全欄夾到 ≤1**(護盾軸恆常生效,再疊類別剋制 = 領兩份預算;夾制 MUST 排在 `CLASS_SYM` 之後,否則 armor/air 欄會被等比放大重新推過 1,而且只在部分角色身上失效);**加成越多越廣泛 ⇒ 基礎傷害越低**(`counterDmgF` = `1/(1+K×負載)`,`BROAD ≫ NARROW` 是在標「類別剋制只在遇到那一類時兌現、護盾軸恆常生效」的差價;只作用在掛旗標的武器上,其餘 28 名角色逐位元不變)。校準只走 `dmg` 階梯與 `COUNTER_BUDGET` 三係數,MUST NOT 回頭手改 `vs`。稽核 `audit_shield_counter.mjs`(71 項,六條反向驗證過的紅字)。
- 導引 / 射後不理(2026-07-30 使用者回報「雷射導引與射後不理常常光暈亮著卻沒命中」):`REACH_RULE.guide/fnf` 的 `path:'ray'` 承諾的是「彈頭會被導引到目標」,**底下五件事全部 MUST 為真**,任何一件破掉都是靜默丟包(玩家只看到零傷害)。稽核 `audit_weapon_gate.mjs` Ⅵ。
  - **出膛初速只有一個縫** `data.js shotV0()`:拋射武器吃的是吊射初速(100m/s)不是砲口初速(650m/s),取錯讓推導出來的飛行時間差六倍。`game._shotV0` MUST 只是轉呼它。
  - **「飛多快」與「飛多久」是兩件事**(2026-08-03 使用者回報「榴彈投擲只有遊戲一開始有傷害」):對地拋投自 2026-08-02 起是 **45° 反解初速**(`_lob45Vel`),`shotV0` 從此只是那個解的**上限**,不再是實際初速 ⇒ 飛行時間 MUST 走 `data.js shotFlightS(def, d, dy)` 這個新縫(lob = √(2(d−dy)/g)、其餘 = 3D 斜距 ÷ `shotV0`),**MUST NOT 在任一端自己拿 `d / shotV0` 除一次**。舊制在 172.8m 把 5.94s 算成 1.73s(低估 2.2 倍),兩個症狀都是整輪靜默丟包:①`heroBurst` 的收鏡寬容窗只有 2.7s ⇒ 開完砲右鍵收鏡去移動,天上那一輪全數蒸發;②`_gateFire` 的裝填窗比客戶端晚 4 秒 ⇒ 下一輪只要打得比上一輪近就整輪被吃掉(遠近交替實測 50% 全丟)。
  - **著彈才回報 ⇒ 擊發資格的閘門要換算回擊發時刻**(`flightCapS(def)` = 在落點閘門上界打出去要飛多久,高差取 `altDhMax()`,推導不手寫)。巡飛彈滿射程飛 3.3 秒、45° 榴彈近 6 秒:`heroBurst` 的需瞄準閘門若在著彈時硬驗,玩家收鏡(開完砲就要移動,再自然不過)= 天上那發蒸發;`_gateFire` 的裝填計時器若從著彈起算,整個週期比客戶端晚一整段飛行時間 ⇒ 下一輪只要打得比上一發近就被吃掉。`back` 刻意吃**同高**的誠實估計(高估 = 裝填提早結束 = 偷射速)、`cap` 才吃俯射餘裕(偏差朝「不擋」)。**射速閘刻意仍量真實時鐘** —— 著彈順序可能與擊發順序相反(先打遠再打近),拿回推時鐘量間隔會誤殺後到的那發。
  - **鎖定之後不受射程影響**(2026-08-01 使用者定案):射程只管「能不能鎖定」——`_tickLock` 的 `_effRange` 閘門 + 伺服器 `heroLock` 複驗射程/迷霧/LOS。鎖上之後 A7 的失鎖規則對 `fnf` **不生效**(`b.fnf` 跳過)、射程包絡整條讓位給追擊燃料(`b.chase` 一經鎖定即不可逆 ⇒ 目標中途陣亡也照飛完);伺服器 `heroBurst` 的落點閘門**追加**一條綁鎖定目標的放行 `chased`(核心帶內)。四個寫壞法:①拿 `chaseCapS` 當射程的替身(它只是「不讓失控彈體永遠留在場上」的保險,推導自 `flightCapS × SEEK.CHASE_F`);②有鎖定就放行任意遠點(等於送出無限射程的指哪打哪);③忘了把 `flightCapS` 換成 `chaseCapS` —— 凡是「拿著彈時刻回推擊發時刻」的閘門(收鏡寬容窗、裝填回推)都會在合法的長程追擊上靜默丟棄(A30);④**把那條放行寫成 `if (lockT) … else`**(2026-08-03 使用者定案「中途爆炸也要有傷害」):彈頭在半路撞到小兵/建物/地形就地引爆時,爆點本來就不在鎖定目標身上 —— 替代語意會把它連同一般 `impCap` 閘門一起判掉 = 前線一有東西擋路整發飛彈零傷害(開局空曠打得到、兵線一鋪開就再也打不到)。正解是獨立旗標:沒炸在鎖定目標上就退回一般閘門照常結算,射手與僚機吃同一個 `chased`。
  - **爆風擴散不吃射程**(2026-08-01 使用者定案):射程只限制**擴散中心點**(`heroBurst` 的落點閘門),落下去之後的超壓照物理擴散 ⇒ `_blast` 內 MUST NOT 出現任何 `def.range` 閘門。這與「沒有射程光暈就不該掉血」不衝突:光暈的語意是「我能不能**瞄準**這個敵人」,濺射從來不是瞄準出來的。
  - **射後不理真的會追蹤**:`_tryFire` 的追蹤目標 MUST NOT 只認伺服器複驗過的 `_lockId` —— ①鎖定要一趟伺服器往返才回得來 ②`_updateRangeGlows` **刻意排除**鎖定目標 ⇒ 每一個亮著射程光暈的目標,依定義都保證不會被追蹤。拿不到就退回擊發當下的準星解 `_aimTarget()`(與 `_tickLock` 同一份實作,MUST NOT 在擊發端另寫一份目標解析)。導引對象是表現層決策(彈道本就客戶端權威),傷害仍由伺服器驗落點 ⇒ 不涉 A1。
  - **近炸引信量線段最近點**:初速 1000m/s 的攔截彈一幀飛 16.7m,點取樣會在兩幀之間跨過目標(且跳多遠隨幀率浮動)。引信半徑 MUST = 爆風核心帶 + 目標水平量體(`b.core + _hitR(tgt)`,與 `_reachable` 的 tol 同一式);前科 `Math.max(4, r * 0.5)` 同時犯了手抄 `BLAST.CORE`(第二份實作)與量到中心(A18 家族)。
  - **導引彈的射程包絡量直線**(`b.seek ? b.pos.distanceTo(b.origin) : b.dist`):同一個 `b.max` 在失鎖判定、伺服器落點閘門、射程光暈三處全是直線量法,只有自毀判定曾拿航跡長 —— 而導引彈的航跡恆長於直線(離架散布 + 一路修正航向)⇒ 打射程邊緣的目標必定在飛到之前自毀。無導引彈(拋物線)仍量航跡長(與 `_arcTrace` 同尺,弧長本就該算進射程消耗)。
  - **導引頭轉得過來**:轉向唯一縫 `data.js seekTurn(基礎角速度, 初速)` = 角速度與「轉彎半徑 ≤ `SEEK.R_M`」兩個上限取寬者。固定角速度的毛病是半徑 = 初速 ÷ 角速度:初速 1000m/s 的攔截彈半徑 312m 比自己的射程還大,離架散布注入的橫向偏差整個射程內都拉不回來。**只放寬不收緊**是刻意的(改成單純 `初速 ÷ R_M` 會讓慢速巡飛彈反而變鈍)。
- 機體混編(2026-08-02 使用者定案「機體調整陣營,角色陣營不變」):**陣營 ≠ 機種**。每名角色 MUST 顯式標註 `kind`,`charKind()` MUST NOT 再由 `side` 推機種(漏標 = 資料錯誤,e2e「機體混編陣營分佈」段釘住)。編制 蜂群 7/3/2、鋼鐵 3/7/2、傭兵 2/2/4(無人機/機甲/變形機甲),機體設計 12+12+8 款**每款恰好一名角色使用**(1:1,同一段 e2e 釘住)。三條隨之而來的紀律:①**機體換陣營時,綁機體的東西整組跟著搬** —— `models.js MOVE_SIG`(運動性格)/`CAST_SIG`(施法動作)/lore 的 `proto`・`bond`,MUST NOT 留在原角色身上(留著就會出現「翼龍的懸停參數配在半人馬身上」);綁角色的東西(武器/招式/`paint`/`hue`/castfx)則一律不動 ⇒ 兩陣營的武器型別與招式家族覆蓋率不受混編影響。②`mods.armor` MUST 落在新機種的 `HERO_SIZE` 區間(robot 12~26 / morph 5~24 / drone 3~12),否則體型內插被夾住。③**換機種 = 換 EHP/機動/`CLASS_SYM` 分組**,`npm run bal` ①⑤ 必動:調校只准走 `mods` 與角色 `dmg` 階梯(逐武器手改 `vs` 仍是禁令,全域 §2.1)。bal ⑤a「陣營對稱」已改用**真正的陣營成員**交叉對局(舊制拿 drone×robot 代打,混編後那條等同 ⑤b)。
- 三招機種絕招(2026-08-01 使用者定案改制;舊「重砲模式/巨砲」整組退場)。三招共用 `SPECIAL` 預算與 `game._fireHoldAbility()` 單一派發縫,差異只在載具:
  - **兩個槽位一律載具化 + 發射點分家(2026-08-07 使用者定案第二輪)**:使用者兩句話 ——
    ①「小招也改為輔助機型模式,CD 時間 15~30s,**從玩家身邊召喚**」;②「大招改為**從最近的砲塔或主堡召喚**」。
    ⇒ 載具制自此涵蓋 **32 台 × 兩個槽位 = 64 招**,分類仍是同一支 `abilDelivered(ch, slot)`
    (區域/指向型 = 點遞送、自身/personal 型 = 跟隨編隊),`ultDelivered` 退成它的薄殼。
    兩個槽位**只差兩件事**:發射點 `abilOrigin(slot)`('self' 主機身邊 / 'fort' 最近的我方砲塔或主堡)
    與 CD 帶(小招 `[SK_CD_LO, SK_CD_HI]` = [15,30] / 大招 [30,60],同一支 `abilCarrierCd` 仿射壓入,
    **嚴格保序**)。設計主軸:小招 = 隨身戰術(快 CD、就地生成、幾乎沒有前置);
    大招 = 後方戰略資產(慢 CD、要飛過來、**整段航程都可以被打下來**)。四條紀律:
    ①**MUST NOT 把小招一律做成跟隨編隊** —— 有 `range` 的小招(s03/t08/m05 的 250m 級 EMP)標的是
    幾百公尺外的一片區域,改成貼著自己的編隊 = 效果圈當場搬回腳下 = 把那三招的身分刪掉(無錯誤訊息);
    ②**小招 MUST NOT 憑空取得遞送距離** —— `hyperRange()` 那個預設值只給大招(它才是「從後方送到指定點」
    的戰略遞送),補給小招等於把團隊 buff/治療小招變成遠端指定;
    ③**發射點只有 `abilOrigin` 一份**(sim 恰兩個消費端:`_launchOrigin` / `_launchUltSupport`),
    MUST NOT 在載具生成端另寫 `slot === 'ult' ? …`;
    ④**輔助機隊的效果窗 MUST 自「就位那一刻」起算**(`g.until` 施放當下是 null,`_supArm` 才寫入)——
    工事召喚的實際腿長隨站位變(`ultLaunchLegM()` 93m 只是**校準用的代表值**),施放當下就把 until
    定死成「施放 + 代表腿 + dur」的話,飛得比那久就在半路到期 = **這一招在深推時永遠交付不到**,
    而且沒有任何錯誤訊息(2026-08-07 同一個坑的第二次;第一次見 `_tickSupport` 檔頭)。
    飛得遠的代價因此是**曝險**(整段航程都在場上、可被鎖定擊落)而不是「窗被吃掉」。
    連帶兩件事:①`h.hypers` / `sq.decoys` **改成陣列** —— 同一名玩家的小招與大招可能是同一種載具形式
    (robot t01/m06 兩枚飛彈、morph s03/t11/m05 兩架轟炸機),單槽位會被第二發覆寫,第一發從此不再被
    推進也不會被移除 = 一顆卡在空中、打得到又擋 LOS 的殭屍實體;②`hyperFlightS(d)` 帶參數,
    `hyperHp` 改吃 `hyperMaxArcM()`(= 代表發射腿 + 遞送距離)—— 飛彈全程在高空飛越整條前線
    (它吃的是 `overflyDps`),多飛的那一段是真的多挨打;**kami / decoy 的曝險窗刻意不動**
    (它們量的是「進入敵方前線塔位射程 → 抵達」,而自家前線塔距敵方前線塔 > 塔射程 ⇒ 多出來的那一段
    飛在射程之外)。稽核 `audit_ult_carrier.mjs` Ⅳ(`--break-origin` 反向驗證)+ `audit_self_ult.mjs` Ⅵ。
  - **自身強化型大招 = 跟隨玩家的輔助機隊(2026-08-07 使用者定案)**:最後 9 台也收進載具制 ⇒
    **32 台大招全部載具化,只差形式**(點遞送 23 台 / 跟隨編隊 9 台)。伺服器
    `heroCast → _launchUltSupport` 派出 `supportN(ch)` 架**跟隨主機的輔助機**(沿用 kami 那具小型載具
    ⇒ 客戶端零改動),飛完投放腿(= 點遞送載具的最短飛行腿,每一發都有同樣長的攔截窗)才開始供輸,
    被打下來就**少一份**加成。五條紀律:①**疊加是加法**(`_supSync` 撤下再放,倍率 = 1 + (m−1)×k/N)——
    逐架各推一筆 mods 會被 `_buffMul` 相乘,全員在線時的效果會高於舊制而且只在「剛好幾架活著」那一瞬
    對得上帳;核心不變式 = **N 架全在線 ⇒ 效果值逐位元同舊制**;②**可疊加才分機**(`supportStackable`
    推導,MUST NOT 手寫名冊):二元狀態(匿蹤/免疫/免裝填)沒有一半可言 ⇒ 恆單機,機數走 `kindParts`
    (與 `ultParts` 同一張機種表);③**節奏三分**(`selfUltTempo`:瞬發 = 無時窗 / 間斷 = 逐 tick 入帳 /
    持續 = 狀態時窗)決定要撐多久,`supportTempoF` 的兩端(0/1)是定義、中間 `PULSE_F` 才是旋鈕;
    ④**耐久推導不手寫**(`supportHp` = `frontKillHp(投放腿 + 節奏係數 × dur ÷ 機數)`,與三種點遞送載具
    共用「前線一組塔位」那把尺,armor/護盾 0)—— 投放腿是**平行**曝險 MUST NOT 也除以機數
    (除下去瞬發型每架只剩 20 點 = 一顆步槍子彈);⑤**一次性效果只做一次**(`once` 守衛:復活/解除異常/
    彈匣全滿/定位/匿蹤/無霧視野),而不住在 mods 的二元狀態(`stealthUntil`/`noReloadUntil`)MUST 在機隊
    清空時由 `_supRevoke` 顯式撤掉。CD **刻意不壓進 [30,60] 帶**(那會同時改掉 `selfUltEq` 的分子)。
    稽核 `audit_self_ult.mjs` Ⅴ(`--break-stack`/`--break-tempo` 反向驗證)。
    2026-08-07 第二輪起這一整段**兩個槽位共用**:所有推導都吃 `slot`(預設 'ult' ⇒ 既有呼叫端逐位元不變),
    投放腿走 `abilLaunchLegM(slot)`(小招 `MIN_LEG` / 大招 `ultLaunchLegM()`),
    ⇒「大招的輔助機比小招的硬」是**推導出來的結論**(服務窗比較長),不是另外加的規則。
  - **純自身型大招補償(2026-08-06;那 9 台)**:失去機種絕招 ⇒ 把被移除的預算折進大招本身。
    當量 `data.js selfUltEq()` **推導不手寫**(預算 × `SELF_ULT.REALIZED_F` × 大招 cd ÷ `SPECIAL_CD_S`),
    兌現走 `selfUltBoost()` 這**一個縫**(伺服器 `_castEffect`、圖鑑 `charAbilityRow` 同吃;客戶端
    MUST NOT 自己算一份 —— 算出兩個數字的症狀是「HUD 說 ×2.3、實際掉血 ×1.35」)。補償是**增額**不是
    再乘一層;破隱爆發窗只在 `_gateFire` 的**開火現形**那一刻開(在 `_castEffect` 就開 = 躲著不開火
    也在燒那一秒);`brk`/免裝填/破隱窗的三個共用欄位 MUST 進 `SQUAD_SHARED`。稽核 `audit_self_ult.mjs`。
  - **大招載具遞送(2026-08-06 使用者定案「長按招式取代部分機體的大招」)**:區域/指向型大招
    (strike/emp/summon + 團隊 heal/buff,23 台;判定 = `data.js ultDelivered()` **推導,MUST NOT 手寫名冊**)
    改以**三種載具形式**遞送 —— 長按與 Q/E 同走 `_fireHoldAbility → _castAbility(abilHoldSlot(aiming))`
    (2026-08-06 起長按 = 招式手勢:一般 → 小招 / 狙擊 → 大招,對 32 台一視同仁),
    伺服器 `heroCast → _launchUltCarrier` 生成
    kami×N / 集束轟炸機 / 極音速飛彈的**點遞送**載具(`pt`/`uA` 欄;不索敵不追擊,落點發射當下烤死)。
    三條紀律:①**合併為一招** —— 機種絕招(飽和攻擊/集束炸彈/極音速飛彈)**整組退場**:
    `heroKamikaze`/`heroDecoy`/`heroHyper`、`rooms.js` 三條訊息、客戶端三支 `_launch*`、bots 三個區塊
    一律 MUST NOT 復辟,驗的是「**三種載具的唯一生成點 = `_launchUltCarrier`**」;HUD 的
    kami/decoy/hyper 三格收起,觸控招式鈕鏡射**當下模式那一格**招式的 CD(`w.aiming ? w.ult : w.skill`);
    ②**效果取代傷害** —— 引爆只經 `_ultArrive → _castEffect`(效果唯一結算點,heroCast 瞬發與載具端同吃,
    MUST NOT 在載具端另抄效果分支),四條 uA 引爆/擊落路徑一律不走 `_blast`;擊落 = 該份**完全否定**
    (kami 無殉爆、轟炸機不補投、飛彈本就不引爆);③CD 以 `ultCarrierCd()` 仿射壓入 [30,60]
    (**嚴格保序**,MUST NOT 改分段表),MP/升級階梯沿用大招。**可分預算分批、不可分狀態單載**
    (`ultParts`:strike/heal/summon 依機種分批 —— 擊落幾份少幾份;emp/buff 單載 —— mods 逐筆相乘,
    分批會疊乘)。載具 HP 同三招一把尺(`kamiHp`/`decoyHp`/`hyperHp`,armor/護盾 0);
    支援型未標 range 的遞送距離 = `hyperRange()`;瞄在腳邊也推到 `ULT_CARRIER.MIN_LEG` 之外
    (使用者定案「需要飛行時間」= 每一發都有攔截窗)。另 9 台走上一條(輔助機隊)。
    lanesim `castUltCarrier`/`ultDetonate` 同構分流;bal ⑦f 兩組量測面刻意不合併(載具組量份額交付率、
    自身型組量 EHP 當量/次 + 輔助機損失率)。2026-08-07 起 `kind: 'kami'` 有**兩個具名生成點**
    (`_launchUltCarrier` 點遞送 / `_launchUltSupport` 輔助機隊),MUST NOT 有第三處。
    稽核 `audit_ult_carrier.mjs`(83 項;`--break-cd`/`--break-guard`/`--break-boom`/`--break-origin` 反向驗證)。
    2026-08-07 起 `heroCast` **完全沒有瞬發結算**(`_castEffect` 只剩 `_ultArrive` / `_supSync` 兩個消費端)——
    heroCast 裡再冒出一個 `_castEffect` 就是「某一招又變回瞬發」,而畫面上只表現成「這一招好像特別快」。
  - **飽和攻擊**(無人機,`SQUAD.KAMI` N=4):「攻擊力減半、數量加倍」是**同一件事** —— 每架 = 預算 ÷ `KAMI.N`,N 一改每架自動折半、整份預算逐位元不變。無聲寫壞法:另外手寫一個 0.5 係數(總預算跟著砍半)、橫向站位留著 `i === 0 ? -1 : 1`(N=4 時三架擠在右側;MUST 走 `kamiSide(i)`,伺服器散開角與生成側偏移同吃這一支;客戶端那一個消費端已隨常駐外觀退場)。
  - **自殺攻擊機不常駐**(2026-08-06 使用者定案「自殺攻擊機拿掉常駐模組,攻擊時再出現」):舊制在觸發**前**另有四架純客戶端的貼身護衛機外觀(ㄑ 字編隊 `ESCORT`/`escortSlot`/`escortLagBase`/`escortLagK`/`escortDrift` + `game._buildDroneEscorts`/`_updateEscorts` + `tools/audit_escort_form.mjs`),**整組退場,MUST NOT 復辟**。理由是那批模型剛好站在原則 4 的反面:看得見、跟著飛、卻不在 sim ⇒ 鎖不到也打不到;而衝出後的 kami 本來就是伺服器實體、走 `_spawnEnt` 的一般渲染路徑(`SIZE_F` 縮小)⇒ 使用者要的「攻擊時再出現」不需要任何新機制,只是把常駐那一段刪掉。連帶:`kamiSide` 只剩伺服器一個消費端(生成側偏移/散開角)、`kcd` 只剩 HUD 用途(不再驅動任何顯隱)。
  - **極音速飛彈**(`HYPER`;2026-08-06 起只是機甲**大招的載具**,不再是可單獨觸發的機種絕招 —— `game._launchHyper` 與 `{t:'hyper'}` 訊息一併退場,唯一生成點 = `sim._launchUltCarrier`):它是**伺服器實體**(kind `hyper`,可被鎖定/擊落),不是客戶端彈道 —— 兩相位全在 `sim._tickHypers`。**彈道(2026-08-02 使用者定案)**:「前半段拋物線飛向高空,初始角度 45 度,後半段再以極快速度向下螺旋飛向目標」—— 爬升段水平等速 `hyperClimbVx()`、高度只由 `hyperArcY()` 給,頂點落在**目標正上方** ⇒ 出膛角恆 45°、後半段才是真正的「向下」俯衝。無聲寫壞法:在客戶端補一份爆風/傷害(A1)、在伺服器自己寫一份高度多項式(MUST 走 `hyperArcY` 這一支)、讓爬升段跟著追蹤對象轉(那就不再是拋物線,兩端也不再插值出同一條航跡 —— 目標的移動由**俯衝段**吸收)、螺旋基底由彈道軸現算(頂點在正上方 ⇒ 軸的水平分量趨近 0,最需要螺旋的那一發整個沒有螺旋;MUST 取固定水平法向 `±(uz, −ux)`)、忘了讓螺旋半徑隨接近收斂到 0(落點被偏擺推開一個 `SPIRAL_R`)、把 `hyperApex`/`hyperRange` 寫成真實世界公尺(位置是**遊戲空間**、已吃 `COMBAT_SCALE`,手寫會大一倍)。客戶端只補**俯仰姿態**(由伺服器位置逐幀差分推導;`_spawnEnt` MUST 給 `rotation.order = 'YXZ'`,否則大偏航時 45° 抬頭會歪成側傾)。被擊落**刻意不引爆**(攔截成功 = 完全否定),MUST NOT「補完」。
  - **集束炸彈**(變形者,`DECOY` BOMB_MAX=6):載具整套沿用舊餌機,只改投彈 —— 逐顆走 `sim._decoyBombTarget()` **個別瞄準**(名冊 `d.bombed` 輪替,範圍內目標打完才重新一輪;取不到合法目標就**不投**,寧缺勿錯),落點 = 目標位置而非機體腳下。事件帶投擲點 `fx/fz` ⇒ 客戶端 `_spawnDecoyBomb` 解出「T 秒後落在該點」的初速,畫成**榴彈拋物線**;拋擲解與逐幀積分 MUST 吃同一個 `DECOY_BOMB_GRAV_F`(不同 = 畫出來的落點偏掉)。傷害仍在伺服器事件當下結算,拋物線純表現層 —— 改成落地才回報就是把結算下放客戶端(A1)。
  - **三個載具的 HP 全是推導值**(`data.js towerDps` → `frontDps`/`frontKillHp`/`frontSurviveHp`、飛彈另走 `waveDps`/`overflyDps`/`overflySurviveHp` → `kamiHp`/`hyperHp`/`decoyHp`):使用者一律以「持續射擊撐幾秒」定義(衝進去 2 架 / 剛好打不爆 / 剛好投得完 5+1 顆)。**尺是「前線一組塔位」不是一座孤塔**(2026-08-02 由 bal ⑦f 定案):塔位本來就是同塔位雙塔,舊的單塔基準讓飽和攻擊只兌現 3.8% 預算、極音速飛彈 **0.0%**(它還多飛越一整條兵波 ⇒ 基準另計 `waveDps`),而畫面上只表現成「絕招好像沒什麼用」。三者一律 **armor 0 / 護盾 0** —— 校準要精確,EHP 就不能隨主機角色或升級漂移(舊 `KAMI.HP_F`/`DECOY.HP_F` 已退場)。**爆風半徑同樣是推導值**:`specialBlastR(share)` = `HYPER.BLAST_R × √預算比例` ⇒ 三招總覆蓋面積相同,「把預算切成越多顆」不會憑空生出攻擊範圍(舊逐招常數 `DECOY.R`/`BOMB_BLAST_R` 已退場);演出半徑 MUST 取結算用的同一份 `def.r`。**火力與範圍自 2026-08-06 起是兩件事**(使用者定案「攻擊力太高、常常被一轟就爆,同等於 2.5 架自爆無人機的傷害」+ 同日「範圍改回舊制」):極音速飛彈的戰鬥部只領 `hyperShare()`(= `KAMI_EQ / KAMI.N` = 62.5%,推導不手寫),**爆風半徑仍取 `specialBlastR(1)`** ⇒ 上面那條面積不變式逐位元不受影響。MUST NOT 「順手」把半徑也乘上 `√hyperShare()`(使用者沒要求的第二次削弱,而且會讓這一招同時失去點殺與面壓制兩頭);中途試過的「半徑 = 砲塔射程 2/5」版本已回退 —— 半徑一放大就蓋得住同塔位雙塔,bal ⑦f 的實得傷害反而從 155 升到 197 EHP/次(範圍不動版是 102)。**改砲塔任一數值(dmg/rate/range)或波次編制,三個 HP 都會跟著漂**,MUST 重跑 `audit_flight_power.mjs` Ⅰ、e2e 與 `npm run bal` ⑦f。
  - **射速壓縮 + 連發演出**(2026-08-02 使用者定案;唯一縫 `data.js RATE_DEF`/`FIRE_RATE`/`rateComp`/`compressWeapon`/`fireBurstN`/`fireBurstGap`,客戶端 `game.js _queueBurst`/`_tickBurstFx`/`_burstEchoSelf`/`_burstEchoOther`):輕武器射速壓縮到 2.2~4.45(原 2.2~18),**DPS 靠 `rate ×f` / `dmg ÷f` / `mag ×f` 三欄一起動維持不變** —— 少了 `mag` 那一項,彈夾撐得更久 = 裝填變少 = 持續 DPS 虛胖最多 33.5%,而爆發 DPS(`dmg × rate`)完全正常 ⇒ 只驗一項驗不出來。壓縮前的射速留在 `rate0`,是**連發演出與 `recoilTier` 分級的唯一依據**(壓縮後全部輕武器都掉到 7 以下,分級吃壓縮值會讓機槍當場從 high 掉成 med,而手感只是「怪怪的」)。演出:一次擊發 = `fireBurstN` 發視覺子彈但**只結算一次**,補畫路徑 MUST NOT 送封包 / 進 `this.bullets` / 扣彈藥(進 `this.bullets` 就會回報命中 = 傷害翻倍 = A1);間隔 `fireBurstGap` 把 N 發**平均鋪滿整個擊發週期**,跨週期間隔與週期內相同 = 使用者要的「看起來連續」。**後座、鏡頭震動、擊退 MUST 逐發補上**(三者本來就是逐發累加的純客戶端手感;只在扳機那一下加一次 ⇒ 每秒後座量掉到舊制四成,沒有任何數字看得出來),`prof.burst` 回穩窗同理改**計發數**。陣亡/換座機 MUST 清 `_burstQ`(閉包會在新機體的槍口上補畫舊武器)。稽核 `audit_fire_rate.mjs`。
  - **開火中位移懲罰 ← 後座量**(2026-08-03 使用者定案「後座力越大的武器,射擊中的移動速度下降越多,最大後座力的重武器會將移動速度歸零、直到後座力結束」;唯一縫 `data.js RECOIL_CLIMB_MAX`/`recoilMoveF()`,客戶端 `game.js _recoiling()`/`_recoilMoveF()`):舊制是逐階手寫的 `move`('free'/'slow'/'stop'/'back')+ `slowF`,而那張表**不單調** —— 機槍(輕高後座)`slowF: 0` 開火即停,後座量大它一截的榴彈砲(重中後座)卻完全不受影響。現在整條由後座量推導:尺 = 每發準星上踢 `climb`(六階跨槽位嚴格遞增,也正是玩家看得見的那個後座;`kick`/`back` 是它的表現層夥伴,MUST NOT 當第二把尺),曲線 `1 − (climb/MAX)^MOVE_K`,兩個端點都是**定義**(無後座 ⇒ 不減速、`MAX` = 全表最大 ⇒ 歸零),`MAX` **推導不手寫**。**時間窗 = `recoil.p` 這個狀態本身**(`_recoiling()`:回穩到 `END_RAD` 以內即結束)—— 舊制那個「到下一發窗口」(`max(0.22, 1/rate) × 1.1`)的計時器與後座回穩無關,重武器打完一發早就恢復全速,使用者要的那段「歸零直到後座結束」根本量不到。三個靜默寫壞法:①係數排在 `recoil.p +=` **之後**定案 ⇒ `_recoiling()` 恆為真,「後座未結束取較嚴者」變成單向棘輪(打過一發重砲之後係數再也降不回來,整場都在最嚴懲罰下走路);②回穩速率在 `_updatePlayer` 手寫一個 7 ⇒ 改了回穩快慢懲罰時長卻不動;③把 `back` 擊退也乘上係數 ⇒ 最大後座那一階連後座本身的位移都被夾成 0(擊退走 `this.vel`,不是玩家在走路)。陣亡/換座機 MUST 清 `recoil` 與 `_recoilMoveF0`(`_updatePlayer` 對 dead 早退 ⇒ 不清就凍在那裡,重生後第一步先被上一具機體那發重砲黏住)。伺服器不涉入(同 RECOIL 全族)⇒ bal/e2e 不受影響;圖鑑武器列的「射擊移速 N%」同吃 `recoilMoveF`,MUST NOT 手寫百分比表。稽核 `audit_recoil_move.mjs`(66 項)。
  - **重武器射擊路徑上不得再有任何旁路**:舊巨砲留下的「這一發免彈夾/免電力/免射速閘 + 窗內自動擊發」在 `sim._gateFire` 與 `game._tryFire` 都已拆乾淨,MUST NOT 復辟(新招是獨立實體,與彈夾完全脫鉤)。稽核 `audit_flight_power.mjs` Ⅱ 以全檔無 `barrag` 殘骸釘住。

- 世界高度上限(2026-08-08 使用者定案;唯一縫 `data.js WORLD_H` + `objHeightMax`/`objScaleCap`/`objScaleFit`/`worldCeilY`):客戶端的天花板只有 `game.js _ceilY()` **一份**(吃 `terrain.avgH`/`maxH`,一場算一次),三個消費端 = 飛行夾制 / 上帝視角 / 觀戰起始高度 —— 逐處各自去讀地形統計就是第二份實作,而它壞掉的樣子只是「有些地方飛得比較高」,沒有任何錯誤訊息。飛行那一處 MUST 與既有的「離站立面 320m」**取嚴者**(兩者問的是不同的問題:離腳下多高 vs 離海平面多高)。地形統計缺席(程序生成備援)MUST 回 `Infinity` = 不設限,回 0 會把玩家壓在地上(原則 6)。物件那一半住 `biomes.js`:`OVER.bldCap` 直接就是 `objHeightMax()`(舊制手寫 170 已退場),神木/邊界神木牆/巨岩露頭/地標四處一律夾**縮放**不截幾何 —— 截幾何 = 碰撞柱/淨空/冠幅/攀爬設施與看得見的量體分家(原則 4)。神木與巨岩走**分布版** `objScaleFit`(硬夾會把整片森林壓成每一株一樣高:公稱 72~110m、最小縮放 0.73 ⇒ 連最矮的抽樣都超標),地標的標稱高 MUST 以 `Box3` **實測**(`LANDMARK_COL[].h` 是擋彈高度,對尖頂/天線低報)。夾制**不消耗亂數** ⇒ 佈局序列逐位元不變(§2.3)。稽核 `audit_world_height.mjs`。
- 飛行動力學(2026-07-30 使用者需求;唯一縫 `data.js FLIGHT` + `airSinkM`/`liftMax`/`liftRegen`/`liftDrainPS`,消費端 `game.js _airSinkHit`/`_stepLift`):①**受擊掉高**入帳掛在「快照偵測到掉血」那一處(與受傷暈影 `hud.hurt` 同一個縫,傷害量 = `_prevVital − vital`),只記帳、逐幀以「待落總量 / `SINK_S`」消化 ⇒ **總掉幅只由傷害決定**(8Hz 快照直接扣 y 是瞬移;做成固定速度則同一份傷害分幾發打完會掉不一樣多)。陣亡/重生/換座機/觸地地面型四處 MUST 清帳,否則舊帳把新機體往下拉。②**爬升動力**只在 `_stepLift` 消耗與回充,且 MUST 排在速度積分**之前**(晚一步就是「先飛上去再扣動力」);見底 MUST 只把 `target.y` 歸零(水平分量不動)—— 改成整體減速就與坡度阻擋 `slopeBlocked` 的語意分家,玩家分不出「爬不上去」與「變慢」。兩者的驅動量(傷害、`maxMp`/`mpRegen`)都是伺服器權威快照,客戶端 MUST NOT 自算;規則只作用於**玩家操控**的飛行機體(`_flying()`)—— bot 沒有客戶端,它那半的掉高住 `sim._botAirSink()`(同一支 `airSinkM`;真人 MUST NOT 在伺服器再套一次,兩套會打架),NPC 直升機/集束轟炸機/護衛機/極音速飛彈是伺服器腳本航線,套上去會陷進地形。稽核 `audit_flight_power.mjs`。
- 暴擊與升級成長唯一縫 = `heroWeapon()`;高度差效果全落在較高方(視線點高程差),同高(≤1 塔高)無加成;招式 `heroCast→_blast` MUST NOT 接 `_rollCrit`/`_altRange`/`_altCrit`/`_dodges`。
- mark 只在 `heroHit` 直擊路徑消耗 ⇒ fan/launcher/missile AoE 角色 MUST NOT 配 mark;`_blast` 先殺掉的目標不掛 CC(死人不出血)。
- 完美迴避綁空白鍵**上升邊** + 戰鬥狀態 gate(每幀觸發/把 gate 搬伺服器皆錯);`heroIframe` CD 權威在伺服器;陣營對調 MUST 同時反轉 lanes 點序(只換 bases 會生在敵堡)。
- `bomb.dmg` 是三階陣列 MUST 經 `sim._bombDef(owner)` 解析;`HERO_SPAWN_SIDE` 偏移兩端 MUST 共用;`heavyMpCost` = 彈夾週期均攤到每發(退回每發全額 = mag>1 耗電 ×mag,Lv1 秒枯電)。
- 助攻戳記靠 tick 內在場刷新,`_kill` 只驗 TTL;battery overcharge 的唯一支撐 = 回充迴圈 `if (h.mp < h.maxMp)` 守衛(by design);動 `SQUAD.BUFF` = 三機隊承傷 ∝ 1/BUFF²,跑 bal ①。
- 地形危險雙通道 MUST NOT 混用:客戶端回報 `wet` 是**輸入**(驅動權威結算,A1 相容);主機上傳的粗網格只驅動佈點/移動迴避。分類唯一縫 `terrainEnvCode`;水域減速夾 `Math.min`;凍結用 deadline += dt。
- 蓄力跳水平移速(2026-07-30 使用者需求 +100%)唯一縫 = `CJUMP.AIR_SPD_F`,兩個消費端 MUST 同吃:起跳彈射初速(`_chargeJump` 的 `fwd`)+ 騰空 `_lowG` 期間的操縱移速(`airK`)。只改一處的症狀不會報錯只會「手感怪」(彈得遠但空中推杆慢 / 起跳沒變快只是滑得久);垂直項(`V`/`GRAV_F`)MUST NOT 乘進去 —— 高度與滯空不變是「滿蓄頂點 < `AA_MIN_ALT`」那條設計前提的依據。稽核 `audit_cc_flash.mjs` Ⅴ。
- 地形坡度移動(2026-07-30 使用者需求)唯一縫 = `data.js SLOPE`/`slopeMoveF`/`slopeBlocked` + `game.js _slopeDegAlong`(量測)。兩個消費端:移動位移乘 `_slopeMoveF(move)`、`_updatePlayer` 的 `passable()` 第三條閘 `slopeBlocked`。三個各自無聲的寫壞法:①坡度改量站立面 `_surf` ⇒ 上橋引道被 deckAt 捕捉成垂直跳階 = 假峭壁,橋永遠上不去(隧道天花之下同理)⇒ 一律量裸地形 `heightAt`,人造鋪面(高差 > `STRUCT_M`)整段豁免;②速度倍率改吃當幀位移 ⇒ 同一道坡在 30/120fps 量出不同陡度(前瞻固定 `PROBE_M`);③下坡也擋或騰空也擋 ⇒ 陡坡上把自己鎖死、跳過懸崖邊緣被空氣牆彈回。「爬不上去」MUST 由 `slopeBlocked` 表達,MUST NOT 用倍率 0(玩家分不出「很慢」與「上不去」,且逐軸滑行的沿等高線橫走會失效)。坡度是**離開兵線的代價**:平緩帶綁 `MAPGEO.MAX_ROAD_GRADE_DEG` ⇒ 推線走廊恆全速(遊戲空間餘裕近一倍,見稽核 Ⅲ)。稽核 `audit_slope_move.mjs`。
- 騰空兩條高度線不混:`offGround()`(免地面傷害)≠ `airUnit()`(= `jumpApex`,視同空中不踩雷),門檻由 `UNITS[].jump`/`AIR.GRAV` 推導;異常狀態吃眼位 `_eyeH()`;飛行機種仍走 `fire.maxY`/`AA_MIN_ALT`。
- 兵線 NPC 貼地 = `_buildLaneSurf()` 建構期剖面場(逐幀棘輪會被迷霧重建/重生污染):只有主陣營、非英雄、非飛行單位吃;取樣半徑 MUST < `LANE_MIN_SEP_M`。
- 變形者濕地地板唯一縫 `_wetSurfaceY(x,z)`(水域→waterY、沼澤→+SWAMP_BAND、乾地→null,null 就走既有 yRef)。
- e2e 依賴「未上傳 `world` = LOS 整體停用」;擊毀障礙走 `_losDirty` 懶重建(8Hz 熱路徑勿全量)。
- 小地圖 LOS 陰影畫在**每源暫存罩**再合成(直接在共用罩 `destination-out` 會誤刪他源照亮區);圓柱上限與上傳 occ 同。
- `_updatePlayer` 的 `if(this.dead) return` MUST NOT 移除(死亡單位會恢復物理/開火);高空墜落逾時就地空中爆,不瞬移鏡頭。
- 碰撞:撞牆幀 MUST 連 y 一併收回位移前上限(只還原 x/z = 下一幀誤判在山上而穿牆);`surfaceAt` MUST NOT 改成無條件 `max(地形, 橋面)`(橋下穿不過)—— **隧道頂板那一項是具名例外**:它排在 `curY < tn.ceil` 早退**之後**,「在底下」的情形上一行已經 return 掉了(洞內站路面),剩下的一定是「人在頂板之上」⇒ 無條件 `max` 不會有橋腹那種穿不過去的死區;掃掠只補「終點無重疊」的單幀穿隧,與 push-out 共用垂直閘與 onDeck 豁免;broad-phase 用 `hypot(hw2,hd2)` 勿用 `max`;視覺件超出碰撞盒 MUST 各自補碰撞;de-clip 測試 MUST 把鏡頭塞進盒角。
- 對進戰模型 MUST 確定性(擲骰改期望值);後撤最多 `GAME.LANE_SAFE_M`(刻意不模擬無限風箏)。
- bot:ENGAGE/PUSH 切換 MUST NOT 再收 `state` 手速費(低難度會呆站)。
- 商店掃貨 / 預約(2026-08-02 使用者需求「商店加入掃貨與預約選項」;唯一縫 `game.js _sweepPick`/`_sweepBuy`/`_toggleReserve`/`_tickReserve`)。**純客戶端排程**:每一筆都走 `_optimisticBuy` ⇒ 伺服器逐筆複驗,客戶端只是替玩家連按了很多次(不涉 A1)。四個各自無聲的寫壞法:①UI 端自己算一次「買不買得起」—— 鈕面說可以按、按下去卻沒動作(或反過來),而畫面上看不出哪一邊錯;可用狀態一律吃 `_shopState().sweepable`(← `_sweepPick`)。②掃貨退回「宣告順序」—— 階梯單價隨等級遞增,貪心便宜優先才是最佳解;退回去不會報錯,只是同樣的錢少買一兩階,玩家幾乎不可能自己發現。③把陣營小兵強化也掃/預約進去 —— 它是同陣營共用、且**刻意不做樂觀扣款**(等級由快照校正)⇒ 迴圈條件永遠成立 = 一次掃光全部身家。④預約沒有「同一階只送一次」的閘(`_resSent`)—— 樂觀更新會被下一份快照校正回權威值,那份快照可能比伺服器處理購買還早到(RTT > 125ms)⇒ 重送、第二筆被拒 = 玩家看到假的「資金不足」;`RESERVE_RESEND_S` 只是被拒時的救濟閥,正常解鎖下一階靠**權威等級前進**,MUST NOT 拿逾時當主要機制。`_tickReserve` MUST 排在商店重繪簽章之前、且 MUST NOT 關在 `if (shopOpen)` 裡(預約的用途正是「關著商店去打仗,錢到了自己買」)。稽核 `audit_shop_auto.mjs`。
- 八軌:四軌獨立、精通已整條刪除,MUST NOT 重引任何全域折減旋鈕;Lv4 由 `tierVal` 外推(手寫第 4 階、把 range 丟進 tierVal 皆禁);`UPG_L3=$200` 後 bal ③ 餘裕僅 1.7pp —— 動賞金/波次/單價先看這條。
- 兵線分離 `laneSeparationAudit()` 烘焙/mapSelect/伺服器/runtime 四處共用(座標先轉遊戲公尺);扇出豁免帶只作用「最近距離」,交叉檢查全線不套豁免。
- 招式配置改 `fx` MUST 一併改 `castfx.js CFX[角色].ult`(否則演出與機制脫節);lore 核心招不當交換籌碼,具名豁免的理由隨 kit 改寫。

### B. 地景生成(biomes / ground / terrain)

- 隧道:結構資格唯一縫 `strucTunnel`(人行 PED_HW/室內 indoor 的 tunnel way 不進結構管線,攤平成一般小徑;carve 入口、`dedupeParallelTunnels` 候選、`audit_lane_scenarios` 三處同吃 —— 去重不閘 = 不合格長 way 壓掉合格隧道);`dedupeParallelTunnels` MUST 排在 `way._tun` 指派之前(三消費端吃同一份);端點聚類 `snapTol` **只准橋用**(隧道維持 0,放寬會動洞口與規則 #5);全圖資源上限走具名旋鈕(`TUN.PORTAL_MAX`/`LAMP_MAX`,用罄 = 後排隧道露原始土牆);標線/避車道純視覺(`hw`/decks/牆不動),遠端門洞的透視收窄不是 bug。
- 洞口打洞 `punchPortalHoles` MUST 只刪繪製 index、不動 `heights[]`(碰撞/LOS/heightAt 不變);同步 `markTriDead()`,三角形→格 MUST 由頂點索引推導(`compact()` 會壓實序號);地被層一併打洞(InstancedMesh 縮零);平行雙孔成功判 `touched` 而非 `rims`;結構隧道 MUST 判 `urban`;門洞暗面備援只在 `!punched` 掛。**走廊往洞外延 `TUN.MOUTH_OUT`**(門洞 bore 帶 `out`,明隧道段的 galBores 不延 —— 它的「外」是隔壁小段):洞口外 ±hw、路面以上/天花以下就是車開出來的地方,開挖後仍攤在切面上的地被與地形殘片會斜插進洞口(洞內往外看 = 浮在路面上方的土色薄片);垂直三條界不動 ⇒ 路塹底/圍裙/山體不受影響。另外**走廊判定是「三角形 × 走廊矩形重疊」(`triBore` 的 2D SAT),MUST NOT 退回「任一頂點在走廊內」**(2026-07-31 多視角檢視實測):地形格 ~8.3m、對角 ~11.7m,而走廊縱向只有 0.5m 與 depth 兩道界 ⇒ 出入口與明隧道段端點附近常出現「三頂點全在走廊外、整片卻橫切斷面」的三角形,頂點法漏刪 = 洞口內側卡著一楔地形(使用者回報的「洞內卡著石頭」),且該片不在 `rims` 內 ⇒ collar 也補不到。實例點只有點語意,`inBore` 仍留給 InstancedMesh 縮零。**地下道 bore 的路面參考 = `fp` 剖面取樣**(2026-08-05 使用者回報「洞口殘留混凝土」):下沉剖面是曲線、引道在洞外還一路爬升 ⇒ ①`floorAt` 對帶 `fp` 的 bore 走剖面內插(取樣範圍兩端各加 `FP_PAD` —— 重疊判定含「部分在走廊外」的三角形,遠端頂點可落在走廊外一個地形格對角);②高差判定 MUST **逐頂點**對「該頂點自己所在深度」的路面(質心一枚樣本會把爬升段「路面之下」的路塹底誤刪 —— 遠端頂點的路面比質心高);③collar MUST 跳過兩端都貼著路面的 rim 邊(`nearRoad`;那條邊界下方是保留的路塹底 + 路面緞帶,無縫可封,織下去 = 浮在路面上的混凝土殘片橫跨洞口)。山體隧道/明隧道刻意不帶 fp ⇒ 三處逐位元舊制。稽核 `audit_open_tunnel.mjs` Ⅵ(Ⅵ-b 含舊制反例存證)+ `audit_underpass.mjs` ⑩g;多視角出圖 `tools/shot_tunnels.mjs`。
- 地下道:引道長 MUST 由 `sink/GRADE` 推導;**下沉量 = 全斷面藏進地下**(2026-08-05 使用者回報「深度不夠導致兩側牆超出地表/天花板高於地表」):`underpassPlan` 的地表取樣 MUST 是中心線 + 兩側牆線(`gMinOf`,±(hw+0.6),hw 由呼叫端傳 `strucHw` —— carve 迴圈與 `venue_field.tunnelRunOf` 同一份),且 MUST 在**延伸後**的基準線(hA→hB)上收斂(② 的引道延伸讓最終基準線 ≠ ① 量的 gA→gB;延伸端點落在土丘上 = MARGIN 被無聲吃掉,而畫面上只是牆從平地冒出來);覆蓋區間 MUST 逐點全寬重驗、蓋不住的點拆縫退回露天路塹(`TUN_GAP_CLOSE` 縫短縫是山體隧道的蓋廊語意 —— 蓋廊頂上是山;對地下道那個縫是藏不住結構的露頭,縫進來 = 牆/頂板出土 + `surfaceAt max(地形, roof)` 的隱形站立面 + 地表斜插進洞內的「路面不平」),MUST NOT 靠改地形蓋起來(天花板上方的地表地貌不可動)。引道開挖 = 垂直路塹(run `cut`,`terrain.js CUT_W`),緣石帶 `COPE` MUST ≥ CUT_W+1;引道另登記 `open:true` 物理段 —— 只給 surfaceAt 站立捕捉(站精確下沉剖面)與 `_updatePlayer` 側壁閘(溝底不可爬牆側出,出入口只在道路頭尾兩端),slab 上傳/彈道/天花/lev MUST 濾 `!open`(A29)。側壁物理 = 幾何牆線 `makeTunnelIndex.wallCross`(tunnelSegs `by` = 基準線+KERB,`wallTopAt` 唯一縫;單步 surfaceAt 高差被網格攤緩坡打敗,MUST NOT 退回)。明隧道(2026-07-30 柱列改制;2026-08-01 判定改制):**明隧道 = 一側在地形內部(牆)+ 一側在地形外部(柱),貫穿整個地形的兩側都是牆**(使用者定案)—— 開放側三條件 = 藏不住頂板(`WALL_MIN`)+ 牆背無實體支撐(`NEAR_W`)+ **牆外 `OUT_W`(25m)內地表落到路面以下**;第三條沒有的話,金龍隧道那種薄覆蓋真隧道(側向地表 40m 外仍高出路面 3.7~9.8m)會整段長柱列、還被 `carveGalleryBands` 挖出一條溝。側向取樣 MUST 含最外圈、`open` 膨脹一格、`gy` 只吃 `WALL_MIN` 帶(擴到落差掃描帶會讓矮牆沉到谷底);開放側 = 矮牆(路面+`SILL`)+ 連續柱列(`COL_GAP`,純視覺不進 `cols`)+ 簷口封邊帶,柱間**透明可見可穿透** —— 覆蓋段 tunnelSegs 附 `gal` 開放側位元遮罩(bit1 = side +1)上傳 slab 第 7 欄,伺服器 `tunnelSideExit`/`_slabSep` 對開放側穿出的射線/爆風放行(兩端同判;深埋段 gal=0 逐位元舊行為);幾何欄位(hw/fy/cy/ceilSegs)MUST 與 gal 無關;`EAVE` MUST > 天花外擴 0.6;頂板吃 `polygonOffset`。**明隧道三淨空**(2026-07-31 使用者回報「進洞卡卡/山壁側穿透」):①判 open MUST 加近帶岩背例外(`NEAR_W` 內任一取樣點高過頂板 = 牆背是實體山壁/岩脊 ⇒ 維持整面牆 —— 燕子口岩脊被「7m 內有低點」誤判開放,柱列開向岩壁面);②側坡地表斜穿洞內斷面的地形楔 = 沿明隧道段發 `galBores` 交給 `punchPortalHoles` 清掉(**與洞口同一次呼叫** —— index 壓實不可重入,二次呼叫會把壓實殘尾掃回 index),collar 迴圈同吃合併清單;③`markGradeCorridors` 覆蓋段跳過淨空 MUST 帶明隧道例外(同 `tunnelWallProfile` 縫)否則地物長在洞裡;`game.js _slopeDegAlong` 對 tunnelAt 捕捉(y < ceil)一律豁免坡度(側坡與路面同高處高差閘抓不到,量到的是山坡橫斷坡度 = 洞內忽走忽卡)。**明隧道地形整備**(2026-07-31 使用者回報「路沒鋪平/出入口被擋/柱外沒淨空」):①OSM `tunnel=avalanche_protector` 整段強制覆蓋(落石棚的頂是建的不是山,舊判 covPts=0 ⇒ 結構整段消失攤成裸路);隧道鏈 MUST 同 tunnel 值才併(chainWays 只保種子 tags);②`carveTunnels` 敞開 run **內部**無條件開挖(覆蓋只看中心線 6m 取樣,走廊裡高過門檻的 8m 網格殘峰 = 橫在路上的岩牆),覆蓋轉換端 `PROT_M` 內只保**斜壁帶**(路廊 ≤ full 恆挖 —— 崖留路旁不留路上);③開放側柱外 [hw−0.5, hw+`GAL_CLEAR_W`] 土脊走 `terrain.carveGalleryBands` 真開挖(strip 於開挖前經 tunnelWallProfile 收集;只降不升 ⇒ openness 穩定;斷面內楔形仍走 punch —— 高度場挖不掉拉伸陡面)。稽核 `audit_open_tunnel.mjs` Ⅴ。**頂板是實體結構物**(2026-08-03 使用者回報「明隧道天花板無法從上面踩」):頂面高單一縫 `tunRoofTop(cy)`(galRoof 頂面 / galCols 柱頂 / `makeTunnelIndex.roof` 三處同吃),板體 [`ceil`, `roof`] 與橋面 [`deckY − DECK_UNDER`, `deckY`] 逐條對稱 —— 站得上去(`surfaceAt` 取 `max(地形, roof)`)且上下都擋砲火(`_slabHitT` 板體重疊)。深埋隧道/地下道**逐位元不變**:覆蓋門檻本來就要求地表 ≥ 路面 + `CLEAR` + `ROOF_T` ⇒ `max` 恆取地形;真正修到的是明隧道與落石棚那種**頂板露在地形之外**的段落(側坡已被 `carveGalleryBands` 挖到路面高,舊制 `heightAt` = 踩空穿模)。伺服器不需改(頂板上 `curY >= ceil` ⇒ 回報 `lev 0`,與洞內 `lev 2` 由 `_slabBlocked` ② 擋)。稽核 `audit_layer_block.mjs` Ⅰ・Ⅴ。
- **貼地道路的路面緞帶 MUST NOT 畫進別條路的洞內斷面**(2026-08-01 金龍隧道真圖資檢視):`punchPortalHoles` 刪的是**地形三角形 + 地被實例**,路面緞帶是另一個 mesh、不在打洞範圍內;而判成明隧道的淺覆蓋處,地表本來就落在洞底與頂板之間 ⇒ 山坡上那條路的緞帶整片橫在洞內(玩家看到「洞裡卡著一片斜板」,肉眼像倒下的樹幹 —— 這就是「洞內卡著不明物體」回報裡最後那一件,與地形楔、與神木都不同源)。判定 `inTunBore` 吃 `markGradeCorridors` 回傳的**同一份**走廊清單(新增頂板底面 `cy` = `tunFloorAt` + `TUN.CLEAR`,推導不手寫),MUST NOT 在 `buildRoads` 另算一份隧道剖面;三消費端(路面緞帶 idx / `emitLine` 縱向實線 / `dashLine` 白虛線)MUST 全掛上,且判定 MUST 只作用在貼地段(`!strc && !brg` —— 結構自身路面與橋面就該在那裡)。頂點照推、只是不成面 ⇒ 序號不變、佈局不漂。稽核 `audit_open_tunnel.mjs` Ⅲ-e。
- 結構走廊地物淨空半徑 = `hw + STRUCT_CLEAR_PAD`(2026-07-31;2026-08-01 改推導):MUST ≥ **最寬的那道**開挖足跡且 **MUST NOT 手寫** —— 三道開挖各有自己的縫(山體斜壁 `hw+7`、引道緣石帶 `hw+UND.COPE`、明隧道柱外淨空帶 `hw+TUN.GAL_CLEAR_W`),漏掉最寬的那道就留下一圈「地物站在已挖掉的地面上」的殘環。舊 `hw+4` 只蓋到路面外 4m(使用者回報「殘餘地形在道路之上」);舊手寫 `8` 漏掉 `GAL_CLEAR_W=9`,簷口外緣那 1m 環正好站著行道樹(2026-08-01 真圖資檢視)。橋維持 `hw+4`(橋下淨空語意不同)。稽核 `audit_open_tunnel.mjs` Ⅲ-c。
- 道路路基整平 `terrain.gradeRoadBeds`(2026-07-31 使用者回報「兩側太陡一邊懸空一邊陷地形」):一般道路乾地走廊橫向切填成平台;呼叫端只有 buildBiomes roadInput 定案後一處(橋/步道/結構隧道 run/泡水段不整);全深帶 = 塗裝寬 + 半格(雙線性內插吃得到帶外節點,只到 hw = 整了等於沒整);填方 > `fillMax` 漸退歸零(峽谷不築壩)、水域節點不動、`carvedNodes` 開挖足跡優先、每節點認最近的路(髮夾彎成台階不混平均)。稽核 `audit_road_bed.mjs`。
- 攀爬:掛上判定 MUST 用「指向攀爬軸」的向量而非表面法線(用法線會有一端永遠掛不上);`planClimbRoutes` MUST 排在所有 `blockers.push` 之後;箭頭姿態 MUST 由三個世界向量組基底(勿手寫歐拉鏡射);成對相接率 70~91% 屬預期。**正面規則**(2026-07-30):設施只准掛在「面法線 + 面中心」(盒)或實測驗過的方位(巨岩 `attA`),圓柱(神木)才維持 16 方位;巨岩目前實測全數不合格(碰撞圓比壁面外 4~60m)⇒ 暫不掛抓點,要回復得先把巨岩碰撞體改成「主量體有向盒 + 外伸量體各自登記」。**攀爬柱 vs 設施幾何**(2026-07-31 使用者回報「屋頂不平的建築,長梯會過高」):`y0/y1` = 攀爬柱(權威;頂端恆 = 碰撞柱頂 `b.y+b.h` = `blockerTopAt` 站立面,**MUST NOT** 為了視覺往下修 —— `_collide` 的建物垂直閘是 `myBot >= b.y+b.h−0.1` 才放行,一低就被 push-out 推下屋頂),`vy0/vy1` = 設施兩端(貼齊生成期實測的實體面 `b.ty`,經 `facilityEndY()` 單一縫,落差夾在 `GRAB_UP` 內)。碰撞柱是**刻意加高**的 ⇒ 頂面不平的結構照 y1 畫就會高出屋頂:一般建物/裙樓 +0.5(可見盒頂 = `gy+h−0.5`)、地標取涵蓋尖頂/天線的 `LANDMARK_COL.h`(**手寫的擋彈高,不是屋頂高** ⇒ 自盒心 `rockProbe.topAt` 實測)、巨岩 +1.5 且圓頂低於 `col.h`(實測岩頂)。神木垂直無落差(柱頂 = 樹尖)但幹身向上收窄 ⇒ 走 `tr`(= `trunkR(頂高)`)推導的跨接臂 `arm = r − tr`,否則繩錨吊在幹外好幾公尺。**巨岩 attA 座標系**(2026-07-31 修):`rockProbe` 的射線一律世界座標,舊版拿 local `(0,0,yL,aL)` 去問 = 從世界原點射向原點 ⇒ `wallR` 恆 null、**attA 恆空**(症狀與「縫太大」的既有結論撞在一起);已改為整段世界量(方位不再 `− rot`、半徑不再 `× s`)。**抽中機率 ← 地形陡度**(2026-07-31 使用者需求):`climbShare(siteSlopeDeg(b, heightAt))` —— 平緩帶 `SLOPE.EASE_DEG` 內 = 基準 `CLIMB.SHARE`,線性升到阻擋角 `SLOPE.BLOCK_DEG` = 100%,「不可陡上(`slopeBlocked`)= 100%」由曲線**推導**而非另寫 if;陡度 = 繞腳印外緣 `SLOPE_N` 方位、每方位量 `SLOPE.PROBE_M` 一段取最陡,取**絕對值**(崖頂四周全下坡也算陡地形)。量測純函式不消耗亂數 ⇒ 抽樣序列不變。**相鄰相接**(2026-07-31 追加)同吃這支曲線、基準換成 `CLIMB.LINK`(平地七成 → 陡地形 100%),且與地面路線共用同一次 `siteSlopeDeg` 量測 —— 曲線只准有一份,MUST NOT 為相接另寫 ramp。**高側取捨**(2026-08-01 使用者需求「出現在等高線相對最高那一面的機率隨坡度降低,最低 0%」):`highFaceShare(deg)` = 1 − `slopeRamp(deg)`(平緩帶 100% → 阻擋角 0%),第四枚亂數 `faceRoll` 沒過就把**候選面裡地表最高的那一面**整個排除 —— 高側的地表本來就已抬到接近頂端,插梯子既沒攀爬價值又像半截埋在坡裡。三條曲線(抽中/相接/高側)共用 `slopeRamp` 這一份 ramp;高側 MUST 先掃完全部候選面再比(邊掃邊比 = 「目前為止最高」),**四面等高就沒有高側**(`hi > lo` 守衛拿掉的話,平台/崖頂上的結構會四面全排除 = 整棟掛不上)。稽核 `audit_climb.mjs` Ⅹ。
- 巨岩表面特徵:貼壁/頂面落點一律走 `rockProbe(g)` 射線實測(`wallR`/`slope`/`topAt`),**MUST NOT** 復辟手寫剖面(側壁橢圓 × dome √(1−u²) / taper);`sideDef` 只描述「可附著柱心 px/pz + 高度帶 y」。頂面特徵另過 `seat()`:腳印四角實測踩在同一片頂面才放。溝棒/棧道的內傾 MUST 用 `rotation.z`(Euler 'XYZ' 最內層 = 繞自身切向軸),`rotation.x` 是繞世界 X = A26 的差 90°。
- 落底高度:神木群落/邊界神木牆/邊界巨岩一律 `sinkBaseY()`(腳印周圈最低)—— 只取中心高度的話,陡坡與巨岩崖邊的樹根會整片懸空(2026-07-30 使用者回報)。
- 三個地貌的排列規則(2026-08-03 使用者定案;規則本體全在 `siteplan.js`,本檔只餵地形/淨空、收成果 —— 見全域 §2.1 三列與 A38)。三個各自無聲的寫壞法:
  - **市區**:①對齊「中心線」而不是**建築線** ⇒ 進深一變立面就前後參差(使用者說的「不整齊」原樣回來,而畫面上只是「街景有點亂」);②`nearestRoadAngle` 自己再拼一次 `atan2(nx, nz)` ⇒ 與臨街配置差一個負號,同一條街上一半的樓背對著路;③忘了**市區閘**(`nearUrban`)⇒ 穿過山區的一條 primary 兩旁長出整排街屋;④公設的草坪/柏油面登記碰撞柱 ⇒ 看得見的空地走不進去,而 40m 長的看台只登記外接圓 ⇒ 球場中央一片 40m 直徑的隱形牆(A30 的反面);⑤`frontSegs` 另開一趟迴圈收 ⇒ 第二份街道清單,遲早因為某個 tag 過濾條件分家。街廓配置區塊 MUST 維持**零 `rnd(`**:抽一枚就把後面每一株植被、每一棟圖資建物的序列整條推移(§2.3),而那只表現成「整張圖的佈局變了」。
  - **綠地**:冠幅 `giantCrownR` 由零件表推導(35% 樹高以上的最遠水平點)—— 手寫一欄 `cr` 之後,改了任何一個冠簇的 `px/pz` 或半徑,間隙規則就與看得見的樹冠分家,症狀是「有些樹冠還是黏在一起」。羞避 MUST 走**縮冠**不走淘汰(一律淘汰 = 森林被打成稀疏散株),而縮冠會讓同一群塞得下的株數變少 ⇒ **群落半徑 `cr` 同步放大**是配套,不是可有可無的美化;規則跑在地形淘汰之前(被水域刷掉的那株仍算鄰株)是刻意的保守方向。
  - **裸露地**:走向取樣距 MUST 跨得過地形格(~8.3m;`G = 26`),否則量到的是單格鋸齒而不是這面山坡的走向;色相偏移 MUST **整片共用**(逐顆各抽 = 一片七彩露頭 = 看起來是七顆孤岩擠在一起);岩種輪替 MUST 以**露頭群**為單位遞增(逐顆遞增 = 一片露頭混七種名岩);緊密的界線是**碰撞柱不互穿**(`ROCKFIELD.GAP_M`),再密也不能長進彼此體內。
- 場景豐富度(2026-08-05,sakura-crossing 技法移植;「過於單調死板」案):**變化一律走零共享 rnd 的雜湊縫,而且層級要對**(街區內一致、跨街區才換 —— 逐件全隨機是均勻雜訊,反而讀成單調)。四組縫:①建物 —— 立面款由 `facadeStyle(b)` 依樓高分桶映射(v 仍是收錄期那枚亂數,三條收錄路徑同吃)、外牆/瓦色吃 `blockTone(x,z)` 街區色相家族 + `roofTint`(RPAL 12 色、同街區相鄰兩階);低層住宅屋頂三分(人字 `roofPrisms` / 四坡 `roofGables` / 平頂)吃**同一枚** rv 三段切分(亂數消耗序逐位元同舊制);大平頂第二件配件走 `djAt` 位置雜湊。②神木 —— `hang()` 實例帶 `dj = djAt(落點)`(特徵件接合 MUST 在抖動下成立,`audit_object_joints` 特徵段已改吃 dj;**縮小的零件 MUST 重新落座**,蛋徑走樣時心高跟著縮放比例走的那條就是前科);追加枝走零消耗的 `hangH`;葉簇 tint 疊 110m 格「暖黃 ↔ 冷藍綠」區域家族。③巨岩 —— `decorateMegalith` 內特徵尺寸走樣一律走 `lrng(既抽值)` 局部序列(**不增減共享 rnd 枚數**:要新亂數就把某枚已抽值捕成變數當種子);基部崩積裙/壁面地衣色斑在 `placeMegaliths` 以落點雜湊 `mulberry32` 自帶種子,純表現層(不進 blockers/blocked,全收在 blockArea(r) 內);`MEGA_JIT` 0.12(夾制本就量測後退回)。④公設 —— `buildCivic(kind, seed)`:`vc` 色相變異通道(成對零件同通道 ⇒ 車體車頂一起轉色)、`opt` 非碰撞小件存缺(**col 件恆保留** —— 碰撞柱走 `civicColliders` 靜態表,視覺缺席就是隱形牆);分桶鍵吃變異後的 `pc`(`audit_soft_stroke` 的字面釘已同步)。
- 大型地物的淨空 MUST 掃**整個腳印圓盤**(`areaFree`),MUST NOT 只問中心格(2026-08-01 金龍隧道真圖資檢視):淨空網格 `CELL` 只有 10m,而神木幹半徑可 >10m ⇒ 中心落在隧道走廊淨空之外一格、樹身照樣橫插進洞內斷面(症狀 = 洞裡卡著整根樹幹,與「洞內卡著石頭」同一族但根因不同 —— 那個是地形楔,這個是地物)。巨岩 `placeMegaliths` 一向 `areaFree(r+6)`,`placeGiantGroves` 補上同判;腳印半徑 `foot = def.r*s*1.6` 是**單一縫**(落底 `sinkBaseY` 與淨空同吃)。抽樣紀律:淘汰檢查排在體格 `s` 抽樣之後(§2.3)。稽核 `audit_open_tunnel.mjs` Ⅲ-d。
- 橋:`deckAt` 端點緩坡 MUST smoothstep(線性有斜率硬折角),它是橋面/decks 碰撞/欄杆/橋墩/路燈的單一縫;`makeDeckIndex` 的 margin MUST 拆側向垂距與縱向溢出兩維(合併會夾到相鄰高橋段);「上橋 or 鑽過」兩門檻間不留死區(底緣淨空 vs 機高);站橋面時略過橋下街廓側推。
- 兵線跨水一律全跨補橋(橋數恆定,勿回退查覆蓋率);沉錨判「沒入水下 1m」(勿改 `isWaterPt`/影像藍色);太近兩橋合併門檻 `JOIN_M = RAMP_M×2`(推導),中間乾地高過 `BRIDGE_RISE/2` MUST NOT 併;`joinWaterRouteWays()` MUST 在 `roadInput` 定案前、無水地圖零影響。
- 塔跨橋墩座:重疊判定錨在**兵線中心點**(逐塔找最近橋會吸附到不同平行橋);台面 MUST NOT 往橋心多伸(z-fight);墩身柱頂封在台面**底緣**(封頂面 = 隱形柱推人下橋);橋上塔的視線柱 `h < LOS.TOWER_EYE_M`,MUST NOT 在 game/sim 另判「塔在不在橋上」。
- 道路:路面/標線 `polygonOffset` 壓地被;走廊遮罩用 `rmGrid`/`roadClearAt()`(勿復用 12m 稀疏的 `rdGrid`);`ground.js tryPatch` 的 roadClear 拒絕 MUST 排在首個 `rnd()` 之前(確定性序列);車道數由半寬推導 `round(hw*2/3.2)`。
- 結構接合(2026-07-30 使用者需求「橋/隧道/地下道內的馬路寬度與外部馬路標線與寬度要對齊,接合處貼合,風格盡可能一致」):**通行寬與塗裝寬分家是刻意的** —— `hw = strucHw`(夾到 ≥`PASS_W`/2,餵 decks/tunnelSegs/牆/門洞/走廊/伺服器碰撞)只管「兩台機甲能不能並行」,畫給人看的車道一律 `laneHw = carriageHw(tags)`(= 結構外那條路的通行寬)。三個無聲寫壞法:①標線改吃 `hw` ⇒ 16m 洞被畫成 5 車道鋪滿,與洞外 2 車道橫向錯開;②依「差額夠不夠寬」在兩者間三元切換 ⇒ 標線寬度隨圖資 `lanes` 值在洞口跳動(舊版潛伏縫);③把 `carriageHw`/`flareHw` 餵進走廊/開挖/物理段 ⇒ 開挖窄於路面、伺服器量體漂移。階差由**銜接漸縮帶**收:自結構端往外 `ROAD_FLARE_M` 鋪同桶路面楔形(`flareHw` smoothstep、起點高逐位元取結構路面高),兩端 `d` 與 `p` 同時反號 ⇒ 繞行朝向自動保持 +Y;兩條跳過條件寧缺勿錯(貼邊界 / 往外撞進山體或懸空 —— 楔形沿山坡爬上去比那道階更難看)。橋面 2026-07-30 起與隧道同款:恆柏油(跨河橋中點恆取到水色,舊版整座橋鋪成青灰/郊區橋鋪泥土)+ 照畫標線(舊版 `!brg` 排除 ⇒ 上橋瞬間車道線消失,橋卻鋪著避車道邊帶),基準高 `deckAt − ROAD_LIFT` 才與結構分支的 `tFloorAt` 同語意;地面路燈 MUST 仍排除橋(以 `heightAt` 落地 ⇒ 會從橋面下長出來)。稽核 `audit_road_joint.mjs`。
- 沼澤:`swampY = waterY + WATER.SWAMP_BAND` 分類界/沼面/帷幕三處共用;`inclSwamp` 只給兵線路徑;`SWAMP_EYE_F` MUST NOT 改 1(全沼幾乎不觸發)。
- 鐵軌高架/地面接續逐頂點 lift,端點連續靠世界座標 0.5m 量化 key(整條固定 lift = 接點瞬跳 8m);平交道遮斷器恆抬起(不擋兵線、無碰撞)。
- 佈營底線:`CLEAR_F` MUST > 1.0(淨空 388 > 塔射程 310 才不交火);動 `MAP_EXPAND`/`CLEAR_F` 跑 headless camps 冒煙即可(不需重烤)。
- 規則 #5 烘焙 `tryBearing` 詞典序 MUST 為 `tunBad → resid → score`;已知缺口:自訂地圖無 tunnel tag 套不上。
- `geocache.js`:入庫條件 MUST NOT 放寬(高程全成功 / 影像零缺磚 / Overpass 無 `remark`,殘缺一旦定案 = 永久帶洞);入庫 MUST `structuredClone`(下游就地變異);改格式/查詢 MUST bump `geoKey`;MUST NOT 改成「瀏覽場地即發 Overpass」(限流)。

### C. 模型 / 動畫 / 渲染資源

- rig 契約(`weap/muzzles/wpn/gunR/pose/aimM/castSig` + 槍口節點)改外觀 MUST 零接觸、bbox 漂移 ≤±5%;液壓缸單端錨斜置(雙端跨關節會被步態拉伸);改 `models.js` 幾何一律走 `tools/shot_units.mjs` 生成→渲染→審視閉環。
- 槍口恆朝前的唯一保證 = build-time 世界對齊反解(`getWorldQuaternion().invert()`),勿改回手調角度;`gp.aim0` = 首寫快取的據槍角;調 `aimM` MUST 維持鏈角總和不變;移武器 MUST 連供彈/供能結構一起搬,動掛點重跑 `audit_muzzle.mjs`。
- **post-pass 通道洩漏(最危險)**:`stepCastPose`/`stepJumpPose` MUST 只動「步態每幀重新賦值」的通道,動未賦值通道 = 動作結束永久歪掉;防線 = `audit_cast_jump.mjs` 差分對照。
- FPV 武器外觀唯一來源 = `rig.wpn` 登記(勿另闢來源或退回通用機槍);`fwd` 由 muzzle 相對 ref 的最大分量軸判定;彈體單一縫 `vfx.projectileMesh()`(自機/他人/展示台/SAM 共用,共用幾何勿 dispose)。
- 槍口焰待機 scale=0.01;血條/敵標/光暈 MUST 用 spawn 時量好的 `ent.dimTop/dimH/dimR`(勿事後 `Box3.setFromObject`);morph 手持武器 `heavy.pivot` MUST 留空(與 gunPitch 每幀賦值打架)。
- 拋物線虛線 MUST 預配置固定 `Float32Array` + `DynamicDrawUsage` + `setDrawRange`(每幀 `setFromPoints`/`computeLineDistances` = GPU 洩漏,A25 族)。
- 塗裝:派發唯一縫 `data.js visual.paint`,國旗色只住 `visual.flag`;徽記只貼「完整平面、無配件插入」的外緣板(`paintEmblemPlate` 評分);朝向閘 = `toon.js uPaintFace` + `CEL_PAINT_GATE`(勿回純 `vPaintN.z`,橫置機種徽記會消失);花紋烤死靜止姿勢局部矩陣(勿世界投影/每幀更新)。
- 雙展示台的兩個 `WebGLRenderer` MUST 持久重用(勿每次新建);兩 shell 用 class + 閉包綁定(勿共用 id);`_celLightDirView` 是共用可變 uniform,引入非同步渲染前重審。
- **軟性物質**(雲/芒草/草原/花園/樹葉/旗幟;A39):細勾線走「場景 RT 的 alpha ≡ 勾線門檻倍率」這條契約(`toon.js` 寫、`postfx.js` 讀),擺動是**純頂點位移**、權重錨在**整株局部座標**(逐零件從 0 起算 = 樹冠繞自己中心剪切,疊接縫開開合合)。加新的軟性物件只要在材質選項掛 `soft: { k, span, base, sy }`,MUST NOT 另開名單、MUST NOT 自寫風向或第二支時鐘;雲的勾線是恆等式(sprite 落在天空深度 ⇒ 勾線 pass 早退),它只需要漂移那一半。
- castfx 群組直掛 scene、跟隨靠每幀 `casterPos()`(勿 re-parent 到 ent mesh);`_snapPos` 重生瞬移 MUST 歸零 `ent.cfx/fireFx/heavyFx`;`fitToHeight` 以整體 bbox 定尺 ⇒ 高聳配件會縮小機體,武器一律斜置收在頂高內。
- 傷害數字貼圖是快取 MUST NOT 由單次特效 dispose;特效「單位幾何 + scale」的 scale 語意 = **半徑**,fade 回呼 MUST 乘回 `r`(最易漏);已知未處理:InstancedMesh 全圖無視錐剔除。
- 震波半徑 MUST = 該武器攻擊半徑(`SHAKE.BLAST_F`,勿加固定下限);對空判定 `_updateAaMode()` MUST 排在 `_tickWeapons` 前,擊發/虛線共用 `_aaAim`。
- 死亡過場:FPV 火煙集中左右、中央 1/3 留視野。

### D. FPV / HUD / 小地圖

- 受擊濺血提示(2026-08-02 使用者需求「被攻擊時的畫面要有半透明紅色濺血提示,會視敵人射擊的方向決定濺血位置,傷害越高濺血的血滴越大」;唯一縫 `data.js BLOOD`/`bloodAlpha`/`bloodFrac`/`bloodDropR`/`bloodDropN`/`bloodScreenUv` + `sim._hurtLog`/`_flushHurt` + `game._bloodSplat`/`_updateBlood`/`_clearBlood`)。與 `CC_FLASH` 同族的純表現層,但多一件事:**方位只有伺服器知道**。客戶端手上的受傷訊號是「快照 vital 落差」(受傷暈影與飛行掉高吃的那一份),它只說「被打了」——想從它推方向就得在客戶端重建一份傷害歸屬,那正是 A1 禁的事,且必定與伺服器分家。故伺服器在 `_damage` 記帳 → `hurt` 事件帶攻擊者座標與這一擊的實際損耗,客戶端只做投影與衰減。六個各自無聲的寫壞法:①flush 點放進 `tick()` —— 傷害同時發生在 tick 內與訊息處理當下(heroHit/detonate),漏掉的那些只是「偶爾沒噴血」,查不出來(唯一 flush 點 = `_frame()`,且 MUST 排在取走 `events` 之前);②記到僚機身上 —— 僚機沒有 flush 點,`_hurt` 一路累積漏水(只記主視野機,與受傷暈影同語意);③高程跨框相減 —— 英雄的 `ay` 是絕對高程、NPC/塔在伺服器是離地高且地基恆 0(`_altDh` 踩過同一個坑),症狀是「高海拔戰區被小兵打,血一律噴在畫面頂端」⇒ 只有攻擊者也是英雄且有 `ay` 才附高程,否則垂直畫在中線(原則 6);④血滴大小吃絕對傷害 —— 各機種 EHP 差一個量級,輕甲被擦一下就滿版、重甲被打爆卻沒感覺(吃 `dmg / (maxHp+maxSp) / REF_F`);⑤螢幕座標不夾制 —— 背後中彈(bearing 近 ±π)會算出畫面外的座標,血斑整片消失而不是貼在邊緣(`bloodScreenUv` 一律夾在 `[EDGE, 1−EDGE]`;半視角吃**當下鏡頭** ⇒ 狙擊縮 FOV 自動跟著收,MUST NOT 另寫一份縮放);⑥位置每幀重算或 CSS 掛 transition —— 前者讓血漬跟著鏡頭滑(它該黏在座艙玻璃上),後者與逐幀曲線相爭把峰值鈍化(`.cc-flash` 同一條禁令)。相機矩陣 MUST 自己 invert `matrixWorld`(`matrixWorldInverse` 是 renderer.render 才寫的,與 `_coneAcquire` 同一個坑);陣亡/換座機 MUST `_clearBlood()`(`_updatePlayer` 對 dead 早退不再衰減,不清就凍在畫面上)。稽核 `audit_blood_splat.mjs`。
- 異常狀態致盲白幕(2026-07-30;`data.js CC_FLASH`/`ccFlashAlpha()` 曲線 + `game.js _blindFlash`/`_updateCcFlash`/`_clearCcFlash`):白幕是「被閃到」的**瞬時感光反應**,不是狀態計時器的可視化 ⇒ 長度固定(HOLD 全白 → FADE smoothstep 淡出),MUST NOT 跟著狀態剩餘秒數延長。觸發只在 `_ccFeed` 上升沿(8Hz 快照驅動,與播報同一個縫);重疊觸發「當下更亮者勝」—— 比現值暗的新狀態 MUST NOT 把正在全白的畫面打回較暗峰值。陣亡/換座機 MUST `_clearCcFlash()`(陣亡過場自有白閃,兩層白疊著會蓋掉演出)。`.cc-flash` z 9 = 洗白周邊暈影但壓在 HUD 之下;CSS MUST NOT 加 transition(與逐幀曲線相爭 ⇒ 峰值鈍化成「白得很慢」)。稽核 `audit_cc_flash.mjs`。
- 圖鑑六角能力圖(2026-08-04 使用者定案「角色能力使用六角圖顯示:HP 和護盾合併顯示為耐久,另外加上兩個項目:火力、射程 + 傷害面積」;唯一縫 `data.js HEX_AXES`/`heroHexStats()`/`hexBand()`/`strikeAreaM2()`/`zoneAreaM2()`/`weaponDps()`,繪製 `main.js heroHexHTML`):舊制五條長條全是**生存面**的拆解,「打得多痛、控得住多大一塊地」在圖鑑上完全看不到;合併 HP + 護盾騰出位置,補上火力與制域 ⇒ 生存 3(耐久/護甲/電力)+ 輸出 3(火力/制域/機動)。五條紀律:①**值全走既有唯一縫**(`heroArmor`/`heroMobility`/`heroWeapon` → `weaponDps`)—— 在 UI 端自己寫一份公式 = 圖鑑與實戰分家,症狀是「圖上看起來很強、打起來不是」;②**滿格基準推導**(`hexBand` 取全場 min/max)—— 舊 `BAR_MAX` 是手寫的,改一名角色的數值別人的長條就悄悄失準;③**半徑取對數**:以 0 為內圈的話 32 台的耐久只差 1.5 倍、六個頂點全部貼外圈(每台都畫成同一個正六邊形),而線性 min~max 對長尾軸無效(制域最大值是中位數近十倍 ⇒ 27 台擠在內圈 = 那一軸退化成「是不是扇形」的旗標);④**制域 = 射程 × 足跡等效直徑**,足跡分類走 `aoeClass` 單一縫,單體直擊取一名步兵的命中量體(不是 0 ⇒ 制域退化成純射程的線性量);⑤軸的**說明只寫在 `help.js UI_TIPS.charHex`**(⓵ 懸浮提示與說明分頁同源,2026-07-31 定案),`HEX_AXES` 只管名稱/單位/怎麼算,常駐圖例 MUST NOT 復辟。稽核 `audit_hex_stats.mjs`(58 項)。
- 座艙唯一真相 = `game.js COCKPIT` + `ndcH()`(NDC↔世界換算勿各處自算);頂緣 MUST 逐頂點投影量測(AABB 對旋轉件短量);`_frameCockpitStruct` MUST NOT 平移武器 wrap(破壞消失點角)。稽核 `audit_cockpit.mjs`。
- 小地圖:任何 `_world2mm` 呼叫端 MUST NOT 混用 sim 座標(z 需翻軸);周遭模式半徑 MUST 恆計入瞄準加成(只在 aiming 乘會跳動);貼邊自機不置中是刻意;碉堡標記走 `_seenBunkers` 持久層單一繪製來源。

### E. 觸控 / 陀螺 / 版型

- 虛擬手把:兩支搖桿共用 `_bindStick()`、圓心每次落指重量(版型/左手鏡像會移位);視角搖桿 MUST 在 `tick(dt)` 幀迴圈積分(手指靜止收不到 `pointermove`);十字鍵四向都接了功能(2026-08-01 左向補上機種絕招),再開新版位 MUST 仍吃事件(不吃的話點擊穿到 `#tlLook` 變轉視角)。
- 視野鎖定(2026-08-01 使用者需求「按住後視野會鎖定準星最近的目標,還是會有後座力」;唯一縫 `data.js VIEW_LOCK`/`viewLockStep` + `game.js _tickViewLock`):扳機位 ZR 改成**按住型**鎖定,原本住那裡的機種絕招搬到十字鍵左(連 `.gb-cd` 冷卻鏡射一起;長按 R 那條路不變 ⇒ A22 的派發縫 `_fireHoldAbility` 零接觸)。四個各自無聲的寫壞法:①在 `_tickViewLock` 直接設相機朝向或扣掉 `recoil` ⇒ 後座力與鏡頭震動一起被吃掉,畫面鎖得很死但使用者要的正是「鎖了還是會有後座力」(相機角 = 基準角 + recoil + 震動,鎖定只准動基準角、只准經 `_applyLook`);②自己寫一份「哪個敵人最正對準星」的掃描 ⇒ 與鎖定光暈分家,MUST 走 `_coneAcquire`(同一份實作,只放寬錐角/放行非英雄/換遲滯錨點),瞄準點同理只有 `_entAimPoint`;③逐幀對全場實體跑障礙射線 ⇒ 熱路徑翻倍,索敵節流 8Hz、收斂仍逐幀(不然轉頭一格一格);④`_cmd` 的 lock 排在 `dead` 閘之後 ⇒ 陣亡瞬間的放開事件被吃掉,重生後視角自己黏著目標(與 `firing` 同族)。鎖定中的鈕面亮燈走 `body.vlock`(狀態唯一真相在 game.js,比照 `body.mm-near`)。稽核 `audit_view_lock.mjs`。
  - **2026-08-02 改制**(使用者:「按 ZR 時會輪流切換前方視野內看見的所有敵人,狙擊模式時以狙擊鏡視野為主」):取景從準星錐換成**視野框**(`_coneAcquire` 的 `ndc` 模式 —— 一般 = 畫面矩形、狙擊 = `scopeRvmin` 鏡圈正圓,兩者正規化到同一把尺 `VIEW_LOCK.EDGE`/`DROP`),按一次輪替一個(`_vlockNext` 掛旗標 → tick 取名冊)。四個新的無聲寫壞法:①名冊依「離準星遠近」排序 ⇒ 鎖定會把視角收斂到目標身上,每切一次名冊就重排,第三個以後永遠輪不到(MUST 依畫面由左至右 = 投影後的 NDC x);②放開時把輪替錨點 `_vlockPrev` 一起清掉 ⇒ 按住型鈕每次按下都從頭開始,永遠只鎖同一個人(`_vlockId` 才是放開就清的那個,錨點只在陣亡/離場清);③用 `camera.matrixWorldInverse` 投影 ⇒ 那是 `renderer.render` 才寫的,開場第一幀還是單位矩陣(MUST 自己 invert `matrixWorld`,與 `getWorldDirection` 同樣是上一幀姿態 ⇒ 同調),且相機**後方**的點投影會左右鏡射 ⇒ MUST 先用相機空間 z 擋掉;④為狙擊模式另寫一份角度縮放 ⇒ 與畫面分家,`zoomFov` 本來就吃進投影矩陣,鏡圈半徑則與 CSS `--scope-r` 同吃 `data.js scopeRvmin`(火場縮圈濃度走 `game._scopeFog`,MUST NOT 去 parse CSS 變數字串)。
- 觀戰相機(2026-08-02 使用者需求「觀戰時:上帝視角加入下降操作」+「玩家視角可切換第一人稱/第三人稱跟隨視角/第三人稱自由視角,運鏡時避免太晃,會顯示該玩家所有資訊(包括商店升級)」;唯一縫 `data.js SPEC_CAM`/`specViewNext`/`camSmoothF`/`camAngleStep` + `game.js _updateSpectator`/`_specSetView`/`_specHud`)。**純客戶端視角工具**,與 VIEW_LOCK 同性質(不上行、不碰權威狀態 ⇒ A1 不涉);常數與純數學住 data.js 的理由也與 VIEW_LOCK 一致 —— 離線稽核要 import 真品做行為直測,而 game.js 的 three 走 CDN 解析不了。五個各自無聲的寫壞法:①**平滑退回逐幀固定係數**(`x += (t−x)*0.1`)—— 不會報錯,只會讓 30fps 比 120fps 晃得多,而使用者要的正是「不晃」(稽核 Ⅱ 直測 30/60/240fps 追隨 1 秒的落點差 < 0.5m);②偏航直接賦值 `this.yaw = tgt.ry` —— ry 量化到 0.01 rad、快照 8Hz,貼上去就是一格一格跳(MUST 經 `camAngleStep`,且**跨 ±π 走最短路徑**,否則機體轉過背面時鏡頭繞一整圈);③第三人稱也照著第一人稱藏機體 —— 切過去只剩空鏡頭(`hide` MUST 只在 fpv 成立,且換視角/目標退場都要還原);④第三人稱另立一個「相機抬高」常數 —— 與注視點兩份參數打架,一調俯仰人就跑出畫面(抬高一律由俯仰推導 `camY = aimY − sin(pitch)·dist`);⑤觀戰資訊面板另做一塊 DOM —— 與交戰的 `.hud-self` 必漂(`_specHud()` 的回傳形狀刻意 = `_weaponHud()`,`hud.self` 只多一條 `w.spec` 分流;算不出來的欄位回 null 顯示「—」,MUST NOT 拿觀戰者自己的本地值假裝)。視角序只有 `SPEC_CAM.VIEWS` 一份 ⇒ 鍵盤 F 與觸控十字鍵左**不可能**走出不同的循環(舊的二態切換 `_specFollow(this._specPid ? null : 0)` 已整組退場);上帝視角的升降與飛行機體共用 Space / KeyC・ControlLeft 同一組鍵,觸控 B・ZL 經 `_cmd` 對映過去,MUST NOT 在觸控層另寫垂直位移。**⑥錨點對「掛在機體上的鏡頭」也套 POS_K 平滑**(2026-08-02 使用者回報「第一人稱視角和第三人稱跟隨視角應該要跟著機體」)—— 機體位置本來就已由 `_updateEnts` 逐幀插值過,再平滑一次是**雙重平滑**:穩態落後 = 速度 ÷ `POS_K`,42m/s 實測差 6.66m ⇒ 第一人稱看到自己的機體跑在前面、第三人稱跟隨的機體被推到畫面邊緣,而它既不會報錯也不像 bug(看起來只是「鏡頭有點慢」)。修法:`SPEC_CAM.LOCK_VIEWS` + `specViewLocked()` 單一縫,fpv/tps **剛體貼合**(來源已平滑 ⇒ 不會抖)、偏航仍走 `camAngleStep`(ry 量化到 0.01 rad 那一份是真的會跳);第三人稱**自由**刻意留著平滑 —— 那是觀戰者自己繞著看的鏡頭,不是掛在機體上的。**觀戰操作說明**(同日需求)常駐 `#specHelp`,版位就是 `body.spectating` 收起角色數據後空出來的那一塊:文字唯一真相 = `help.js SPEC_CONTROLS`(選單那一長串由它 join 推導 ⇒ 面板與選單 MUST NOT 各寫一份、也不會有一份先過時),取字經 `specControls(TOUCH_UI())`(A21),重繪掛在 `syncPauseHelp` 上(切鍵鼠 ⇄ 搖桿要換說法);CSS MUST 給 `max-height` —— 跟上玩家時它與完整數據面板同框,沒有上限會把血條推出畫面外。另:進場 toast 對觀戰者 MUST 發觀戰版(舊制無條件發交戰版 ⇒ 一進場就被告知它根本沒有的鈕)。稽核 `audit_spectator_cam.mjs`。
- 操作方式由房主選擇(2026-07-31 使用者需求「操作方式應該由房主選擇 / 目前操控的選擇併入設定」):它從**個人偏好**變成**房間設定**,與電腦難度同一種東西 —— 值住伺服器 `room.config.ctrl`、UI 長在戰區畫面(`renderRoomCtrl` → `renderCtrlModeRow`),設定頁只剩唯讀狀態 + 個人的「目前操控」。四個各自無聲的寫壞法:①房主按下就直接改本地生效值(先斬後奏 ⇒ 房主的版型與隊友不同步,且違反伺服器唯一真相 —— 一律走 `setRoomConfig` 上行、等 `sync` 廣播回來才由 `setRoomCtrlMode` 寫入);②回大廳忘了 `setRoomCtrlMode(null)`(大廳還套著上一場的限定值,下一場開房又把它當「我的預設」傳出去);③在別處自己寫 `room || pref` 合流(生效值有兩份 = 搖桿層與說明文字各認一邊);④非房主的停用態改成「UI 端判 app.isHost 就好」而不接伺服器閘門(真正的規則落點是 `setRoomConfig` 只認 hostId,UI 只是忠實反映)。裝置與定案衝突(手機遇上限定鍵鼠)MUST 明說 `ctrlMismatchText()`,寧可醜話說在前面。稽核 `audit_ctrl_mode.mjs` + `npm test`「操作方式由房主選擇」段。
- `syncBlocked()` MUST 由主迴圈每幀同步(切換點散在暫停/商店/結束三處,逐點呼叫必漏);收起前先 `reset()` 放掉按住中的鈕;**陣亡頁刻意不收**(玩家靠搖桿 ⊟ 進商店)。
- 版型:中央整片留給戰場 MUST NOT 放控件;欄寬/`--tl-band` 由握把/十字鍵尺寸推導(勿寫死百分比);`?ctrl=any|kbm|pad`(舊 `?touch=1|0|auto` 相容)網址逃生門 MUST 保留(手機無 devtools;它設的是**我的預設**,進了戰區仍以房主定案為準);操作方式的三選一與生效規則只住 `ctrlmode.js`;試玩層 MUST 用真的 `TouchControls`;觸控設定 UI 唯一來源 = `TOUCH_SETTINGS` + `renderTouchSettings()`;操作方式 UI 兩個唯一來源 = 戰區畫面 `renderCtrlModeRow()`(房主可改,`#roomCtrlMount`)+ 設定頁 `renderCtrlSettings()`(唯讀狀態 + 目前操控;大廳選單/手機操控面板/戰場設定頁三個 mount)—— 三選一按鈕的 DOM 只准長在前者。
- 透明度唯一縫 = `--tl-alpha`(靜置)/`--tl-alpha-on`(按住/警示),勿逐控件散寫 `opacity`;提亮 class 掛元件自身(群組透明度蓋不過);測試地雷:transition 中 `getComputedStyle().opacity` 回插值,等過渡結束再讀。
- 陀螺:兩路徑輸出 MUST 只經 `Gyro._emit()`、俯仰同號;玩家鎖定 `gyroSrc` 時 MUST NOT 偷換路徑,換來源走 `retune()`;`alpha` null 不整筆丟(仍可出俯仰);失敗 MUST 講原因(前置檢查 + 1.8s 看門狗寫進戰報),MUST NOT「開關綠的但沒反應」;iOS 非使用者手勢 MUST NOT 要權限(保留偏好並提示按鈕);`.off` 警示鈕仍可按;相對模式 + 螢幕旋轉角併四元數(勿手動換軸)。**`TOUCH.gyro` 預設 `false`**(2026-08-04 使用者需求「陀螺儀改成預設關閉」)—— 玩家開過一次就寫進 `svs_touch` 偏好、下一場照舊自動套用,故 iOS 那條「靜默啟用不要權限、保留偏好」的路徑 MUST 原封不動(它守的是**記憶開啟**的人,不是預設值)。
- 商店在**橫式**兩兩左右併列(2026-08-04 使用者需求):純 CSS,`body.touch-ui.ori-landscape .shop-items` 改 grid 兩欄,`.shop-head` MUST `grid-column: 1 / -1`(不跨欄的話標題右邊會補一列升級,那一列看起來就不屬於任何分區 = 買錯軌);直式維持單欄(360~430 寬切一半塞不下「名稱 + Lv + ☆預約 + $價格」);欄寬 MUST `minmax(0, 1fr)`(預設 `auto` 的最小值是 min-content,長名稱會把欄撐出捲軸)。`renderShop()` 的 DOM 一列都沒動 —— 版型問題 MUST NOT 靠改 JS 生成結構解。
- 自適應解析度唯一縫 = `game.js RES_GOV`/`_tickResGov`/`_applyRes`(觸控限定,桌機 `_resGov` 不存在 ⇒ 早退):`_dpr()` 仍是畫質**天花板**(低功耗 1 / 觸控 1.5 / 桌機 2,稽核鎖定),動態縮放只准在天花板以下浮動;動 `setPixelRatio` 一律經 `_applyRes()`(全檔僅初始化 + 此處兩處,稽核計數把關);降階快(只吃 `HOLD_S`)升階慢(指數退避)是刻意 —— 掉幀要立刻救、畫質可以慢慢還;餵入的幀時 MUST 是**未夾制**原始值且過濾尖峰(GC/載入不觸發降階)。稽核 `audit_gpu_lifecycle.mjs` ⑥。
- 全螢幕 **MUST NOT 鎖方向**(2026-08-04 使用者需求「全螢幕時也可以旋轉手機切換直式/橫式」):`toggleFullscreen()` 進場即 `unlock()`,MUST NOT 退回 `lock('landscape')` 或舊制「鎖成當下持握」—— 鎖了就得先退出全螢幕才換得了版型。`unlock()` 而非「不呼叫」:PWA manifest 與前一次的鎖都會延續。版型切換不歸它管,`syncOrientation()` 已在聽 `orientationchange`/`screen.orientation` change。`lowPower()`「未設定過」與「設成 0」MUST 分開判(直接比 `==='1'` 會蓋掉玩家選擇);疊層 ✕ 用 `data-ovclose` 指既有關閉鈕 `.click()` 轉呼叫(關閉副作用勿重寫);`.has-close` 上緣由 CSS 統一預留(排在同特異度觸控段之後);陣亡/結束頁刻意無 ✕;移除功能時死鍵整條路徑(鍵盤/`_cmd`/說明文字)一併拔。
- GUI 說明一律懸浮提示(2026-07-31 使用者需求「遊戲內 GUI 說明一律改成懸浮提示,並彙整到說明分頁」;唯一縫 `tip.js` + 文字表 `help.js UI_TIPS`):常駐一長串說明的代價不是醜,是**把按鍵擠出摺線**(A20 一族) —— 字收進 `data-tip`、列上只留一顆 ⓘ。四個各自無聲的寫壞法:①把說明直接寫進 `index.html` 的 `data-tip`(說明分頁的「介面」類是由 `UI_TIPS` 推導的 ⇒ 兩份會漂;靜態標記只准寫 `data-tipkey`,文字由 `main.js syncTips()` 灌);②退回原生 `title=`(觸控沒有 hover,手機上永遠看不到);③在 `pointerdown` 吃掉短按(提示可以掛在選項鈕上,吃掉 = 整顆鈕失效)⇒ 觸控一律**長按**才出提示;④逐節點綁事件(房間列/商店/圖鑑每次重繪都換一批節點 ⇒ 重繪後靜默失效)⇒ 一律 document 委派。`.tip-dot` 的 hover 已先顯示 ⇒ 點擊 toggle MUST 看 `_viaHover`,否則「移上去看到再點一下」會立刻關掉。切鍵鼠 ⇄ 搖桿 MUST 重跑 `syncTips()`(`UI_TIPS` 有 `pTouch` 兩版)。稽核 `audit_ui_layout.mjs` 0.5 段。
- 按鍵風格統一(2026-07-31 使用者需求):凡「一組互斥選項」一律 `.seg` > `.segb`(戰場選單分頁 / 說明類別 / 操作方式 / 目前操控 / 陀螺來源 / 電腦難度 / 圖鑑陣營)—— 從前每處各寫一版,選取態有實心填、有描邊、有發光三種,玩家分不出哪個是「已選」。消費端 MUST 只掛 class(尺寸變體只准改字級/內距/高度);`.btn` 家族(準備/開戰/離開)刻意不併入 —— 那是動作不是選項。電腦難度 MUST NOT 退回 `<select>`(手機會叫出系統選單、風格與旁邊那列不一)。
- 房間設定分頁(電腦難度 ⇄ 操作方式):兩者同為「整房一個、房主可改」,共用 `#roomCfg` 一塊版位輪流顯示 —— 兩列同時攤開會把陣營卡推出首屏。切換是**純表現層**,MUST NOT 在那裡碰任何設定值。
- NPC 圖鑑圖示走 `npcicon.js` 內嵌 SVG 而非 3D 截圖:圖鑑格只有 96px 寬,截圖在這尺寸只剩色塊,且 `currentColor` 才吃得到選取態與陣營色;缺鍵回退問號圖(看得出漏畫,不破圖)。
- 選單:`.cd-art` 的 sticky 在單欄 MUST 解除為 static(媒體查詢段 MUST 排在 sticky 宣告後);唯一允許上下排的例外 = 選角「頭像牆 ▏詳細說明」;大廳與暫停選單 MUST 共用同一份設定/說明(`syncAudioSwitches`/`bindSettingsControls`/`mountHelp`,真相在 `app.audio`/`lowPower`);`svs_audio` 雙聲道持久化,每次開選單 MUST 重跑 `syncSettingsUi()`。
- ABXY 鈕徑 MUST ≤ 外框 41.4%(否則按 X 打到 A),重疊量測比圓心距;招式 CD 由 `main.js padMirror()` 單向鏡射;稽核用 `THREE_STUB` 由 `tools/pw.mjs` 統一提供(勿各留一份)。

### F. 網路 / 單機 / 預建

- `LocalNet.kill()` MUST 連 `hub.shutdown()`;訊息一律排微任務派發(同步直呼會遞迴);鈕面文字兩份(`LINK_MODES` + 靜態 HTML)是刻意取捨,一致性由稽核逐字把關。
- 地圖預建結果住 `app.pre`(勿直接寫 `app.terrain`,回房會被抹掉);進過戰鬥的地形被 `clearAround` 不可逆變異,MUST NOT 跨場重用;`preloadModels` 經 `warmModels()` 單航班;未 render 過的 group MUST NOT dispose(模組級共享貼圖會壞掉下一場)。

---

## 3. 當前開發焦點(2026-07-29)

1. **地景生成品質主線**(07-28~29 落地:橋隧去重/地下道/明隧道/攀爬/地貌交界/小區域組合風格):共同缺口 = **真機驗證** —— 真瀏覽器目視觀感、`bake_venue_lanes.mjs` 重烤(兵線規則①生效,需 Overpass 外網)、攀爬真開房冒煙。沙箱離線稽核已涵蓋幾何契約;交付說明 MUST 標註未驗項。
2. **平衡校準迴圈**(07-17 大改制後持續):八軌經濟(bal ③ 餘裕僅 1.7pp)、波次戰力(bal ①,`SQUAD.BUFF` 旋鈕)、空投調參(刻意不入 bal ③ 解析模型)。改動範式:只動 `data.js` 常數 → 跑根目錄 `/CLAUDE.md` §5 矩陣對應驗證 → 不碰架構縫。
3. 新增負面狀態/獎勵路徑 MUST 同步接上助攻戳記與重生清理,並補對應稽核腳本(而非 runtime log)。
