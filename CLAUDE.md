# Steel vs. Swarm — Repository Entry (Tier-1 pointer)

> This file supersedes the former Chinese `CLAUDE.md`. Tier-1 SSOT is `AGENTS.md`.
> Read `AGENTS.md` first; then the topic rule in `.claude/rules/`; then the module note
> (`server/`, `public/js/`, `tools/`); then the asserting `tools/audit_*.mjs` header.
> Commands live in `package.json`. In-game UI and narrative dialogue stay Traditional
> Chinese in their content modules; all docs and code comments are English.
