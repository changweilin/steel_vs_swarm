# 無人戰略:鋼鐵與蜂群 (Steel vs. Swarm) — 全域儲存庫準則

> **本檔定位**:雙層情境系統的**全域層**(長期不變標準)。活躍模組層見 [`public/js/.claude.md`](public/js/.claude.md);歷史細節見 `CLAUDE-orig0718.md`(2026-07-18 前逐日檔案庫)與 git 歷史。精煉基準日 2026-07-29。
> 關鍵詞 **MUST / MUST NOT / SHOULD** 依 RFC-2119 解讀。違反 MUST NOT 條目 = 架構違規,直接退回。
> **細節住哪裡**:本檔只記「原則、禁令、改什麼 → 驗什麼」。逐項斷言、幾何公式、邊界案例住各 `tools/audit_*.mjs` 的檔頭註解與斷言本身 —— 查細節先開稽核腳本,MUST NOT 憑記憶重建。

---

## 0. 核心原則(全部 MUST 內化;後文一切規則都是這十條的具體化)

1. **伺服器唯一真相**:HP/傷害/彈藥/經濟/勝負全在 `server/sim.js` 結算。客戶端只做三件事 —— 送輸入與命中回報、渲染 8Hz 快照插值、跑表現層彈道/物理。MUST NOT 有任何「客戶端先改狀態再同步」;防作弊驗證(射程 ×1.25、迷霧、LOS、高度)只住伺服器。
2. **單一真相縫(Single Seam)**:跨檔共用的邏輯與數值只准有**一個**結算點,發現第二份實作即是 bug;推導得出的值 MUST NOT 手寫。縫的索引見 §2。
3. **兩端同量體**:碰撞、彈道、命中、LOS 在客戶端與伺服器 MUST 吃同一份幾何(同一個盒/圓柱/垂直帶/半徑)。兩端分家的代價不是「差一點」而是**靜默丟包** —— 客戶端算命中、伺服器算被擋,傷害無聲蒸發(A18/A30 一族)。
4. **表現層歸表現層**:純視覺改動(材質/擺件/擋土牆/緣石/座艙)MUST NOT 動到權威幾何(通行寬/碰撞/LOS/平衡);反過來,演出取用的尺寸 MUST 來自權威值(看到多粗 = 打到多粗),MUST NOT 為了好看自己放大。
5. **確定性**:場景佈局跨客戶端逐位元一致,散布路徑 MUST NOT 用 `Math.random()`(細則 §2.3)。
6. **降級,不例外;寧缺勿錯**:外部服務掛掉走 fallback、取樣不到合法位置回 null 略過、伺服器對回報「驗證後靜默丟棄」(細則 §2.4、§4)。
7. **真實世界尺度**:`SOLDIER_H`(1.8m)是唯一身高單位,MUST NOT 調回超尺度(細則 §2.5)。
8. **三機制一架構**:雲端/區網/單機只換**傳輸層**不換架構;`rooms.js`/`sim.js`/`bots.js` MUST 保持瀏覽器可執行,URL 佈局 MUST 鏡射儲存庫佈局(細節見 A28)。
9. **稽核為正 + 反向驗證**:本專案無 runtime logger;正確性防線 = 離線稽核(`tools/audit_*.mjs`,以 **執行原文** 驗真品)+ e2e + `npm run bal`。新增系統 SHOULD 同步補稽核而非加 log。**改任何有稽核的判定,改完 MUST 做反向驗證**:把判定故意寫回壞版/舊制,稽核 MUST 在對應條目紅字,否則等於沒驗到。
10. **刻意設計 MUST NOT「補完」**:一批看似 bug 的行為是刻意取捨,修它就是引入 bug —— 彈藥漂移(A9)、爆風不吃 LOS(A11)、直升機不接塔 SAM(A15)、貫穿判定是 2D 而非 3D(A18)、對進戰模型只算武器(§2)、AoE 不爆擊(§4)。動手前先查 A 表。

---

## 1. 系統架構與技術棧

**產品**:瀏覽器 DOTA+FPS — 蜂群同盟 (SWARM) vs 鋼鐵協約 (STEEL);兩陣營主力機種不同但**皆為三機種混編**(2026-08-02)。真實世界地圖選址 → OSRM/Overpass 取真實道路兵線 → 即時 3D 地形開戰。

**架構型態:Server-Authoritative Monolith**。三種遊戲機制(雲端伺服器 / 區網 Tailscale / 單機)共用 `server/rooms.js`(`RoomHub`)與 `server/sim.js`;單機 = 把伺服器整支搬進瀏覽器分頁跑,客戶端一樣只送輸入、收 8Hz 快照 —— MUST NOT 為單機另寫「客戶端自己算」的路徑。

**技術棧絕對規則**

| 項目 | 規則 |
|---|---|
| Runtime | Node.js;唯一 npm 依賴 = `ws`。**MUST NOT** 新增任何 npm 依賴 |
| 前端 | vanilla ES-module JS + Three.js 0.160(CDN importmap)。新函式庫一律 CDN importmap,且**先有離線 fallback 才准接** |
| 建置 | **無 build step、無 bundler、無框架、無 TypeScript — MUST NOT 引入以上任何一項** |
| 語言 | 註解與 UI 字串一律**繁體中文** |
| 3D 資產 | CC0 開源模型優先(`MODEL_MANIFEST` + 程序生成 fallback);法線貼圖 **MUST** 刪除並重寫 gltf 移除引用 |

**分層職責**

| 路徑 | 職責 |
|---|---|
| `server/server.js` | 傳輸層:HTTP 靜態檔 + WebSocket + `/healthz`(單機完全用不到);另有 `/dev/tools` 一條**開發期路由**,只是把 loopback 來的請求轉給 `tools/dev_supervisor.mjs`(雲端模式不掛、出貨版沒有 `tools/`)|
| `server/rooms.js` | `RoomHub` 房間/配對/8Hz 戰鬥生命週期 — 三機制共用,MUST 保持瀏覽器可執行 |
| `server/sim.js` | `BattleSim` 權威模擬核心(single source of truth) |
| `server/bots.js` | `BotBrain` 電腦玩家狀態機(推線/交戰/撤退) |
| `public/js/data.js` | 全遊戲平衡數值唯一真相;**伺服器直接 import 這支客戶端檔** |
| `public/js/*.js` | 渲染/FPV/輸入/HUD(檔案地圖見 `public/js/.claude.md`) |
| `tools/` | 離線工具:平衡驗證、兵線烘烤、稽核腳本、單機版打包、LOGO 管線 |
| `.github/workflows/` | 回歸驗證 CI + 單機特化版部署 GitHub Pages |
| `test/e2e.mjs` | 前段 `BattleSim` 確定性單元測試 + 後段 WebSocket 端對端,約 60 項斷言 |
| `reference/` | 上游唯讀副本 — **MUST NOT** 修改,只准參考 |

---

## 2. 通用標準與慣例

### 2.1 單一真相縫索引

每列 = 一個縫。共通鐵律:消費端 MUST 全部走這個縫,MUST NOT 另寫第二份實作或在別處二次運算;**推導值 MUST NOT 手寫**。

| 領域 | 唯一縫 | 要點 / 禁令 |
|---|---|---|
| 平衡數值 | `data.js` | 射程/傷害/經濟/波次/角色/招式全在此;sim/game MUST NOT 硬編碼;敘事文字去 `lore.js` |
| 角色機種 | `data.js CHARACTERS[ch].kind` + `charKind()`/`heroKindOf()` | 2026-08-02 機體混編:**陣營 ≠ 機種**,32 名角色一律顯式標註 `kind`;MUST NOT 由 `side` 推機種。編制固定 蜂群 7/3/2・鋼鐵 3/7/2・傭兵 2/2/4(無人機/機甲/變形機甲),機體設計 12+12+8 款**每款恰好一名角色使用**。機體換陣營時,綁機體的 `models.js MOVE_SIG`/`CAST_SIG`、`mecha.js` 那一整格(型號/原型層/生成段)與 lore `bond` MUST 整組跟著搬;武器/招式/塗裝綁角色不動 |
| 推導值 | 各推導式 | 賞金表、`UNITS.drone.hp`/`SQUAD.DMG`(← `SQUAD.BUFF`)、`UNITS.bunker.hp`(= 塔一半)、塔位 `solveTowerSites()`、`MINES.PER_LANE`/`AA_SITE.range`(等面積)、`TOWER_SEP_F`(= 2 − TOWER_OVERLAP)、`FAN_MUZZLE`(← `FALLOFF.PLATEAU`)—— 一律 MUST NOT 手寫 |
| 砲塔佈局規則 | `data.js towerLayoutAudit()`(#4 射程重疊)/`towerTunnelAudit()`(#5 洞口涵蓋) | 烘焙/mapSelect/伺服器/稽核共用;#5 MUST NOT 下放成執行期挪塔(兩端塔位會分家) |
| 兵線導航規則 | `data.js laneUTurnAudit()`(U-turn)/`laneTurnAccumAudit()`(主軸偏航累積 ±`TURN_ACCUM_MAX_DEG`,順逆抵消)/`laneStructEntryAudit()`(橋隧只走出入口) | 生成期硬門檻淘汰;規則①只在離線 bake(唯一有逐邊結構旗標);②③互相獨立,MUST NOT 以一代二 |
| 離線工具的場地高度場 / 圖資 / 結構剖面 | `tools/venue_field.mjs` | Node 端(無瀏覽器、無 three)取得「與執行期同形」地形與結構的**唯一縫**:高度管線是 `terrain.js buildTerrain` 的逐字鏡射,結構判定與剖面(`tunnelCoverIntervals`/`tunFloorAt`/`underpassPlan`/`tunnelWallProfile`/`deckAt`)一律 `new Function` **執行 biomes.js 原文**、常數也從原文解析 —— 抄一份公式進工具 = 公式改了工具照舊全綠。消費端(`audit_lane_scenarios`/`audit_traverse`)MUST 走這一支,MUST NOT 各自再抄一份高度管線(第三份必定漂);圖磚/圖資快取共用 `tools/.scen_cache/` |
| 隧道/地下道剖面 | `biomes.js tunFloorAt()` + `underpassPlan()` + `strucHw()` | 山體隧道 = 平直內插;地下道 = 同基準減 smoothstep 下沉。消費端 MUST 吃 `way._tun[ri].pts`(含引道延伸段) |
| 隧道/明隧道/地下道**頂板** | `biomes.js tunRoofTop()`(頂面高)+ `makeTunnelIndex` 的 `roof` 欄;消費端 = `main.js surfaceAt`(站立面)+ `game.js _slabHitT`(彈道板體) | 2026-08-03 使用者定案「明隧道天花板無法從上面踩,跟橋面一樣需要遵守物理碰撞法則,不可穿越或穿透攻擊;隧道和地下道的天花板也同樣需要遵守」。頂板 = **有厚度的板體** [`ceil`, `roof`],與橋面板體 [`deckY − DECK_UNDER`, `deckY`] 逐條對稱:上表面站得上去、板體上下都擋砲火(「擋得住 = 站得上去」,只做一半就是打不穿卻踩得穿的破圖)。①頂面高只有 `tunRoofTop(cy)` 一份(外露頂板 `galRoof` 頂面 / 柱列 `galCols` 柱頂 / `makeTunnelIndex.roof` 三個消費端同吃)—— 手寫第二份 `+ TUN.ROOF_T` = 「看到的頂面」與「踩得到的頂面」分家(原則 4);②站立面 MUST 取 `max(地形, roof)` **而非直接指派**:深埋山體隧道與地下道的覆蓋門檻本來就要求地表 ≥ 路面 + `CLEAR` + `ROOF_T`(= 頂板頂面)⇒ 地形恆勝出、行為逐位元不變(直接指派會在山坡上長出隱形平台);而**明隧道**的頂板露在地形之外(側坡已被 `carveGalleryBands` 挖到路面高)⇒ 舊制走 `heightAt` 就是「穿過看得見的頂板掉進洞裡」;③mount 台階測試 MUST NOT 另寫 —— 走到那一行的前提已是 `curY >= ceil` 而 `ROOF_T < DECK_STEP`,洞內(`curY < ceil`)在上一行就 return 了(MUST NOT 讓洞內單位被吸到頂板上);④彈道 MUST 是**板體重疊**而非「跨過底面 `cy`」:只驗跨越會漏掉貼著頂板削過去的擦邊彈 —— 站在頂板上朝洞口低伸射擊那一發正是使用者要擋的「從上面穿透攻擊」;⑤`open` 段(引道露天路塹)三處一律濾掉(A29)。**伺服器不需改**:站頂板上 `curY >= ceil` ⇒ 回報 `lev 0`,與洞內 `lev 2` 由 `_slabBlocked` ②(同 ribbon 層不符)擋下,兩端同判。稽核 `audit_layer_block.mjs` Ⅰ・Ⅴ |
| 道路塗裝寬/結構接合 | `biomes.js carriageHw()`(塗裝車道半寬)+ `flareHw()`/`ROAD_FLARE_M`(結構端漸縮帶)+ `markBaseAt`(標線基準高) | **通行寬 `strucHw`(≥`PASS_W`/2,吃 decks/tunnelSegs/走廊/伺服器碰撞)與塗裝寬 `carriageHw`(= `roadWidth`/2 = 結構外那條路的通行寬)是兩件事**:標線/避車道邊帶/漸縮帶三消費端 MUST 全吃 `carriageHw` ⇒ 洞口/橋頭車道數與路緣線逐條對齊;MUST NOT 在結構側改吃 `hw`、MUST NOT 依差額三元切換。差額 = 避車道邊帶,階差交給往結構外 `ROAD_FLARE_M` 的漸縮帶(**純視覺**,MUST NOT 進 decks/tunnelSegs/cols/走廊)。**邊帶 MUST 跟著漸縮帶同步收到零**(2026-07-31 使用者回報「洞體內外的道路沒接好、寬度不同」):結構內是「車道 + 兩側邊帶」,漸縮帶只鋪柏油 = 出洞那 12m 變全寬柏油、邊帶憑空消失 = 斷面在洞口跳一階;邊帶吃**同一個 `fhw`**(MUST NOT 自己再算一次半寬),車道緣全程不動。橋與隧道同為工程結構物 ⇒ 恆柏油(`strc \|\| brg`)且橋面照畫標線,基準高 MUST 走 `markBaseAt`(結構 `tFloorAt` / 橋 `deckAt − ROAD_LIFT`),回退貼地取樣會把標線畫到山頂/河床。稽核 `audit_road_joint.mjs`(86 項)|
| 明隧道判定 | `biomes.js tunnelWallProfile()` | **明隧道 = 一側在地形內部(整面牆)+ 一側在地形外部(柱列);貫穿整個地形(兩側都在地形內)一律以隧道處理,兩側都是牆**(2026-08-01 使用者定案)。開放側 MUST 三條件同時成立:①`WALL_MIN` 內藏不住頂板 ②牆背 `NEAR_W` 內無高過頂板的實體支撐 ③**牆外 `OUT_W` 內地表落到路面以下**(= 這一側真的在地形之外)。少了③ 就是舊制:金龍隧道那種只差 0.2~1.5m 蓋滿頂板的真山體隧道會被整段判成明隧道,柱列開向山肚子、`carveGalleryBands` 還把山壁挖成一條溝(2026-08-01 使用者回報)。判定跑三次(carveGalleryBands 呼叫端在開挖**前**、buildRoads / markGradeCorridors 在開挖**後**)MUST 同解:①② 吃 heightAt 但開挖只降不升,③ 吃 `terrain.natureAt`(**天然**地形快照)⇒ 自家路塹不算證據(金龍隔壁孔的洞口路塹曾讓同一段路開挖前判牆、開挖後判柱 = 柱外淨空帶沒挖卻長出柱列)。地下道恆非明隧道(三消費端各自以 `sink` 擋掉;改制後路面沉在地表下 ⇒ 條件③ 天生不成立,是第二道防線)。構件共用同一份 `open/gy/nx,nz`;落地基準只有 `galBase` 一份;開放側 = 矮牆 + 連續柱列,柱間**透明可見可穿透**(2026-07-30 使用者定案):覆蓋段 tunnelSegs 附 `gal` 遮罩 → slab 第 7 欄 → 伺服器 `tunnelSideExit`/`_slabSep` 開放側放行,兩端 MUST 同判;幾何(`hw`/fy/cy/ceilSegs)MUST 與 gal 無關;柱列純視覺 MUST NOT 進 `cols`(伺服器無逐柱語意)。**近帶岩背**(2026-07-31):`NEAR_W` 內任一取樣點高過頂板 = 牆背是實體山壁/岩脊 ⇒ 該側維持整面牆(只看「7m 內有低點」會把柱列開向岩壁);**洞內三淨空** = 地形楔 `galBores`(併入 punchPortalHoles **同一次**呼叫,壓實不可重入)+ `markGradeCorridors` 明隧道段照樣 blockArea(同縫判定)+ `game.js _slopeDegAlong` 洞內(tunnelAt 捕捉)豁免坡度閘。**落石棚強制覆蓋**(2026-07-31):OSM `tunnel=avalanche_protector` 的頂是**建的**不是山,整段強制視為覆蓋(MUST NOT 依「地表藏得住天花板」判 —— 峽谷落石棚常整段裸露 = 結構整段消失);隧道鏈 MUST 同 tunnel 值才併(chainWays 只保種子 tags,混併會吃掉旗標);**柱外淨空帶** `terrain.carveGalleryBands`(strip 於開挖前經 tunnelWallProfile 單一縫收集):開放側 [hw−0.5, hw+`GAL_CLEAR_W`] 的土脊真開挖到路面(只降不升、只動頂板以下 ⇒ openness 對這刀穩定;斷面**內**的楔形仍走 punch —— 高度場挖不掉拉伸陡面) |
| 地形稜線遮蔽 | `data.js LOS.HGT_*`/`RIDGE_*` + `hgtEnc()` + `main.js bakeHeightGrid()` + 伺服器 `sim._ingestHgt()`/`_hgtAt()`/`_absSightY()`/`_ridgeBlocked()` | 2026-08-01 使用者需求「直線攻擊與扇形攻擊要避免隔山打牛」。伺服器本是**無地形高程**的 2D 平面(`_losBlocked` 只認 occ 柱與 slabs 板)⇒ 房主載圖後把粗高程網格隨 `t:'world'` 上傳(與 occ/slabs/wet 同一條路;編碼縫 = `hgtEnc`,烘烤端與解析端 MUST NOT 各寫一份字母表)。**只給「伺服器自己選目標」的路徑用**(`heroPlasma` 錐內選人、`_lanceHits` 圓柱內選人 —— 後者含 botFire 自建的未截斷射線);MUST NOT 併進 `_losBlocked` —— 客戶端回報型攻擊(heroHit/heroBurst/heroLance 的射線端點)已被本端 193² 解析地形截斷過,再驗一次只會因兩份解析度不同而**多擋**掉合法傷害(A30)。判定 MUST 全程在**絕對**高程框(`_absSightY`:英雄取 `ay`、其餘取粗網格地表 + 離地高);偏差方向一律朝「不擋」(`RIDGE_M` 餘裕 + 兩端 `RIDGE_SKIP_M` 豁免帶,原則 6)。三條放行:未上傳網格 / 任一端在隧道內(高程場是**未開挖**的山體,洞內頭頂恆壓著一座山;洞內外隔絕歸 `_slabBlocked`)/ 距離短於兩倍豁免帶 |
| 英雄武器/招式解析 | `heroWeapon()`/`heroAbility()` | HEROIC ×1.2/×1.5、SQUAD 折算、rangeCap 全在這;MUST NOT 二次乘算 |
| 射速壓縮 / 連發演出 | `data.js RATE_DEF`+`FIRE_RATE`+`rateComp()`+`compressWeapon()`(推導迴圈)+ `fireBurstN()`/`fireBurstGap()`;客戶端排程 `game.js _queueBurst()`/`_tickBurstFx()`,補畫 `_burstEchoSelf()`/`_burstEchoOther()` | 2026-08-02 使用者定案「DPS 不變、降低輕重武器射速,射速越高降越多,拉近射速差異、但射速的排名不變,高射速武器動畫做對應調整,例如機槍做成 3 連發實質一次傷害,整體看起來攻擊動畫是連續的」。①**是推導迴圈不是改數值**(同 `CLASS_SYM` / `VS_DEFS` 夾制):32 角 × 兩槽位的 `rate`/`dmg`/`mag` 由 `compressWeapon()` 一次改寫,MUST NOT 回頭逐武器手改階梯;原始射速只留在 `rate0`(連發演出與 `recoilTier` 分級的唯一依據 —— 壓縮後全部輕武器都掉到 7 以下,分級吃壓縮值會讓機槍當場從 high 掉成 med)。②曲線 `PIVOT × (r/PIVOT)^K`,**嚴格遞增 = 排名不變的保證**;MUST NOT 改成分段表或整數倍率(rate 8 → 4.0 而 rate 9 → 3.0,排名當場翻掉)。K ≥ 1 逐位元回到舊制。③錨點 `PIVOT = RATE_DEF`:32 把重武器一把都沒標 `rate`,全部落在預設值 3 上 = 現役射速帶的底 ⇒ 重武器與慢速輕武器折減 0%(這就是「降最少」那一端)。**MUST NOT 把錨點下修到全場最低 2.2** —— 重武器彈夾只有 2~5 發,④ 的整數化在那個尺度上誤差 14%(實測 mag 3 → 2 = DPS −14%)。④**「DPS 不變」靠三件事一起動**:rate ×f、dmg ÷f、**mag ×f**。少了 mag 那一項,彈夾撐得更久 ⇒ 裝填次數變少 ⇒ 持續 DPS 最多虛胖 33.5%(實測),而爆發 DPS 完全正常 = 看不出來。唯一誤差源是彈夾整數化(≤ 半發)。⑤**連發是純表現層**:一次擊發 = `fireBurstN` 發視覺子彈但只結算一次,補畫路徑 MUST NOT 送任何封包 / 進 `this.bullets` / 扣彈藥(A36);間隔 `fireBurstGap` 把 N 發**平均鋪滿整個擊發週期** ⇒ 跨週期間隔與週期內相同 = 使用者要的「看起來連續」(打成「一串急促連發 + 一段空白」就不是)。後座與鏡頭震動 MUST 逐發補上(兩者本來就是逐發累加的純客戶端手感,只在扳機那一下加一次會讓每秒後座量掉到舊制四成),`prof.burst` 回穩窗同理改**計發數**。稽核 `audit_fire_rate.mjs`(56 項)|
| 機體移速壓縮 | `data.js SPEED_COMP`+`speedMid()`(推導中點)+`spdComp()`+`heroMobility()`+`evasionMinSpeed()`;消費端 `game.js _mobility()`(三個移動端)/`bots.js _speed()`/`sim._dodges`/`duel`/`lanesim`/圖鑑機動軸 | 2026-08-04 使用者定案「所有機體的移速拉近差距,但排序不變」。①形狀同 `FIRE_RATE`(冪次曲線,**嚴格遞增 = 排序不變的保證**;MUST NOT 改成分段表或整數倍率),但**軸取幾何中點不取帶底**:射速壓縮有 `dmg ÷ f`/`mag × f` 兩欄把 DPS 補回來,移速沒有那種補償欄 ⇒ 錨在帶底 = 全體變慢(對局節奏整個拖掉),不是「拉近差距」。錨在中點 ⇒ 快的降、慢的升,壓縮後整組的**幾何中點逐位元不動**(同 `AOE_BUDGET.NORM` 與 `BUILD_DPS` ②「收斂 MUST 是重分配而非削弱」)。②中點 `speedMid()` **推導不手寫**,取樣面 = 每台機體的**每一種型態**(32 台地面 + 8 台變形者飛行巡航)—— 漏掉飛行那一組 = 飛行型態不受壓縮。③**唯一取速處 = `heroMobility`**:MUST NOT 在任何消費端讀 `UNITS[kind].speed × mods.speed`(那是壓縮**前**的值)。舊制客戶端 `game.js` 與 `bots.js` 更是連 `mods.speed` 都沒乘(只跑機種基準速)⇒ 伺服器閃避門檻、兩支平衡模型、圖鑑機動欄與腳下實際跑的速度分成兩份,而這種分歧只有拿碼表量才看得出來(沒有任何錯誤訊息)。④**閃避門檻 MUST 走同一張映射**(`evasionMinSpeed()` 單一縫,sim/duel/lanesim 三個消費端同吃):`EVASION.MOBILITY_MIN` 是同一條速度軸上的一個點,壓縮把慢的往上抬 —— 門檻不跟著抬,32 台會整組落到門檻之上 =「重甲慢速機體站著吃彈」當場失效,而畫面上只表現成「重機甲好像變得比較難打中」。⑤**K 有實測下界**(逐值掃描 1/0.6/0.5/0.4/0.3):K ≤ 0.4 起 bal ④ 的變形者剩餘 EHP 掉到 −0.0%(第二座塔推不掉)⇒ 0.5 是「①④⑤⑦ 同時全綠」的最強壓縮;K = 1 逐位元回到舊制。稽核 `audit_speed_comp.mjs`(30 項)|
| 圖鑑六角能力圖 | `data.js HEX_AXES`(六軸唯一真相 + 繪製順序)+`heroHexStats()`/`hexBand()`/`HEX.FLOOR`+`strikeAreaM2()`/`zoneAreaM2()`;持續 DPS 縫 `weaponCycleS()`/`weaponDps()`;繪製 `main.js heroHexHTML()`(兩個消費端)+ `style.css .hx-*`;文字 `help.js UI_TIPS.charHex` | 2026-08-04 使用者定案「角色能力使用六角圖顯示:HP 和護盾合併顯示為耐久,另外加上兩個項目:火力、射程 + 傷害面積」。舊制五條長條全是**生存面**的拆解 ⇒ 合併 HP + 護盾騰出位置,補上火力與制域(射程 + 傷害面積的合成名),成為生存 3(耐久/護甲/電力)+ 輸出 3(火力/制域/機動)。①**六軸值全走既有唯一縫**(`heroArmor`/`heroMobility`/`heroWeapon` → `weaponDps`),UI 只負責畫 —— 在 UI 端另寫一份公式 = 圖鑑與實戰分家(症狀是「圖上看起來很強、打起來不是」,沒有任何錯誤訊息)。②**滿格基準 `hexBand` 推導不手寫**(取全體現役角色的 min/max);舊 `BAR_MAX` 是手寫的,改一名角色的數值別人的長條就悄悄失準。③**半徑取對數內插、內圈留 `HEX.FLOOR`**:以 0 為內圈 ⇒ 32 台的耐久只差 1.5 倍、六個頂點全部貼外圈(每台畫成同一個正六邊形);線性 min~max ⇒ 對長尾軸無效(制域最大值是中位數近十倍,27 台擠在內圈 = 那一軸退化成「是不是扇形」的二元旗標)。④**制域 = 射程 × 打擊足跡等效直徑**(m²);足跡分類 MUST 走 `aoeClass` 單一縫(MUST NOT 比對 `def.type`),單體直擊取一名步兵的命中量體(`hitR`,**不是 0** ⇒ 制域退化成純射程的線性量)。⑤**持續 DPS 只有 `weaponDps` 一份**:同一條彈匣週期公式原本被 `buildDps` 與 `balance.mjs slotDps` 各抄一份,火力軸是第三個消費端 ⇒ 收成單一縫,MUST NOT 再手抄 `mag/rate + reload`。⑥**軸的說明只寫在 `help.js UI_TIPS.charHex`**(⓵ 懸浮提示與說明分頁同源,2026-07-31 定案),`HEX_AXES` 只管名稱/單位/怎麼算;常駐圖例 MUST NOT 復辟。⑦圖上取 **Lv1 未升級**,SVG MUST 只用 class 上色(陣營色走 CSS `--railW`)。稽核 `audit_hex_stats.mjs`(58 項)|
| 開火中位移懲罰(後座 → 移速)| `data.js RECOIL`(`MOVE_K`/`DECAY`/`END_RAD`)+ `RECOIL_CLIMB_MAX`(推導)+ `recoilMoveF()`;客戶端 `game.js _recoiling()`(時間窗)/`_recoilMoveF()`(係數)/`_tryFire` 定案點 | 2026-08-03 使用者定案「後座力越大的武器,射擊中的移動速度下降越多,最大後座力的重武器會將移動速度歸零、直到後座力結束」。①**逐階手寫的 `move`/`slowF` 整組退場**:舊表**不單調** —— 機槍(輕高後座)`slowF: 0` 開火即停,而後座量大它一截的榴彈砲(重中後座)`move:'back'` 位移完全不受影響、重低後座又是全停;「越大降越多」在那張表上根本不成立,畫面上只表現成「這把槍動不了、那把重砲反而能跑」。②**尺只有一把** = 每發準星上踢 `climb`(六階跨槽位嚴格遞增,也正是玩家看得見的那個後座);`kick`(震動)/`back`(擊退)是它的表現層夥伴,MUST NOT 當第二把尺 —— 兩把尺遲早排出不同的序。③曲線 `1 − (climb/RECOIL_CLIMB_MAX)^MOVE_K`,兩個端點都是**定義**而非校準(無後座 ⇒ 不減速;`MAX` ⇒ 歸零),`RECOIL_CLIMB_MAX` **推導不手寫**(任一階 `climb` 一改自己跟著走),`MOVE_K = 1` 即「降幅正比於後座量」;MUST NOT 改回分段表或逐階手寫。④**時間窗 = `recoil.p` 這個狀態本身**(`_recoiling()`:回穩到 `END_RAD` 以內即結束,回穩速率 `DECAY` 與 `_updatePlayer` 的衰減同一份)—— MUST NOT 退回舊制「到下一發窗口」的計時器(`max(0.22, 1/rate) × 1.1` 與後座回穩無關,重武器打完一發早就恢復全速,使用者要的那段量不到)。⑤係數 MUST 在 `recoil.p +=` **之前**定案:加完之後 `_recoiling()` 恆為真,「後座未結束取較嚴者」(重砲的歸零 MUST NOT 被隨手切一把輕武器補一槍就解除)會退化成**單向棘輪** —— 打過一發重砲後係數再也降不回來。⑥係數只夾**移動輸入**;`back` 擊退走 `this.vel` MUST NOT 吃它(那是後座本身的位移,不是玩家在走路)。⑦飛行機體另套 `AIR_F` 折扣(空中減半,沿用舊制)。伺服器不涉入(同 RECOIL 全族)⇒ bal/e2e 不受影響;圖鑑武器列「射擊移速 N%」同吃 `recoilMoveF`,MUST NOT 手寫百分比表。稽核 `audit_recoil_move.mjs`(66 項)|
| 傷害衰減 | `data.js dmgFalloff/blastFalloff/fanFalloff` | sim 結算與客戶端 HUD 共用 |
| 彈匣惰性補彈 | `sim.js _refillIfDone()`;消費端只有 `_gateFire()` 與 `heroReload()` | 伺服器**沒有逐 tick 掃彈匣**:`reloadUntil` 過期之後 `ammo` 仍寫著 0,要等下一次開火判定才補回去 ⇒ 凡是要讀 `h.ammo` 下決定的路徑 MUST 先過這一支,MUST NOT 直接讀。2026-08-03 使用者回報「榴彈投擲/射後不理/雷射導引只有第一擊有傷害」就是 `heroReload` 漏了這一步:AoE 的命中是**著彈**才回報,而客戶端彈匣一見底就送 `{t:'reload'}`(送出時刻 = 擊發當下,那一匣的彈頭還在天上)⇒ `heroReload` 讀到過期的 `ammo = 0`,「彈匣已滿就不重裝」那道守衛失效 ⇒ 每個彈匣都把 `reloadUntil` 重新推到一整個 `reload` 之後,下一批彈頭整批落在填彈窗裡被**靜默丟棄**,而彈匣再也補不回來 = 從第二個彈匣起整場零傷害(第一個彈匣剛好 `ammo == null` → 補成 `mag` → 守衛擋下,所以「只有第一擊有傷害」)。射速閘/`back` 回推擊發時刻的紀律見 `_gateFire` 檔頭 |
| 扇形錐緣 | `data.js fanArcHalf()`(標稱半角)+ `fanConeHalf(def, d, hitR)`(有效半角) | 2026-08-03 使用者回報「部分武器打得到一般單位、但不到建築」。錐內判定 MUST 量到目標**命中量體的近側表面** —— 與 `_surfD3`(射程)、`_lanceHits` 的 `R + hitR(t)`(貫穿)、`_blast` 的 `dh − hitR`(爆風)同一條規則,扇形是最後一個還在量**中心**的縫:貼著砲塔(hitR 7)/ 主堡(hitR 20)的牆面噴,整個錐子都打在牆上而中心還在 30~70° 之外 ⇒ 舊制一發都不掉血,同一處的小兵卻照樣被噴死。三個消費端 MUST 全吃這一支(伺服器 `heroPlasma` 結算 / 客戶端 `_shotVictims` 範圍光暈名冊 / `tools/lanesim.mjs` bal ⑦ 的攻擊範圍計價),MUST NOT 任一端另寫 `Math.cos(arc)`。**量體只放寬「打不打得到」,MUST NOT 放大傷害**:`offAxisFalloff` 的分母仍是 `fanArcHalf`(標稱半角)⇒ 靠量體才進錐的目標一律吃錐緣保底 `AOE_EDGE`;小兵級量體(hitR 0.6)的錐內傷害逐位元不變 |
| 波次編制/節奏 | `data.js waveComp()`(固定編制)/`waveMarchSpeed()`(= 編制最慢者)/`waveSpacingM()`(= `GAME.WAVE_S` × 行軍速度)+ `sim.js waveInterval()` | 出兵間隔**固定**(2026-07-30;`GAME.WAVE_S`,不再逐波加速);開場預置兵線 `sim._prefillLanes()` 的間距 MUST 吃 `waveSpacingM()`,擺位與常規出兵共用 `_spawnLaneWave()`,MUST NOT 各寫一套。預置上限 = 該兵線**第一座砲塔**(`solveTowerSites` 回傳序末項的 frac);塔位 MUST 吃 `sim.towerSites` 那一份解,MUST NOT 再解一次 |
| 陣營小兵強化 | `data.js CREEP_UPG`/`creepUpgMul()` + `sim.js _creepMul()`/`_bounty()` | 等級住 `sim.creepUpg[side][lane]`(**同陣營共用、兵線分開**);倍率於 `_spawnLaneWave` 生成當下寫進 `e.cu`(不追溯既有小兵),hp/dmg/armor/賞金四個消費端 MUST 吃同一份 `e.cu`。賞金(擊殺全額 + 助攻 ×`ASSIST.F`)只准經 `_bounty()`,MUST NOT 直接讀 `ECON.BOUNTY[kind]`。解鎖門檻 = 八軌全滿(`_upgAllMax()`,商店 UI 同判);解鎖後該區塊 MUST 排在商店**最前面** —— `.shop-box` 是 `max-height:86vh` 的捲動容器,八軌本身就已溢出一般筆電視窗且無捲動提示,掛在最後 = 使用者根本看不到(2026-07-30 回報);解鎖當下八軌保證全滿,上面 8 列全是死列 |
| 商店掃貨 / 預約 | `game.js _sweepPick()`(挑哪一項)/`_sweepBuy()`/`_toggleReserve()`/`_tickReserve()` + `_shopState()` 的 `sweep`·`toggleReserve`·`sweepable`;UI `main.js renderShop` + `#shopSweepBtn` | 2026-08-02 使用者定案「商店加入掃貨與預約選項」。**純客戶端排程 = 替玩家按下那顆按鈕**,每一筆仍走 `_optimisticBuy` ⇒ 伺服器逐筆複驗(不涉 A1)。①「買不買得起」只有 `_sweepPick` 一份,鈕面的可用狀態吃 `sweepable` —— UI 端自己再算一次就是「鈕面說可以按、按下去卻沒動作」;②掃貨**貪心便宜優先**(階梯單價隨等級遞增 ⇒ 同一筆錢換到最多階),MUST NOT 退回宣告順序;③**掃貨只作用於八軌**:陣營小兵強化是同陣營共用、且刻意不做樂觀扣款 ⇒ 掃進去等於把錢全倒進兵線,而且迴圈條件永遠成立。**預約收它**(2026-08-02 使用者定案「商店的預約包含兵線升級」)—— 預約是一次一階,不吃那個坑;鍵的格式只有 `RES_CREEP`/`_creepResKey`/`_resCreepLane` 一份(UI 拿 `_shopState().creepKey(lane)` 當不透明字串,MUST NOT 在 main.js 自己拼:拼錯不報錯,只表現成「★ 亮著卻永遠不成交」),解鎖門檻只有 `_upgAllMax()` 一份(商店要不要畫那個區塊 + 排程送不送得出去同吃),而**同一輪的餘額 MUST 自己記帳**(`pend`)—— 共用值沒有樂觀扣款 ⇒ 三條兵線在同一份快照裡都看到同一筆錢而全數下單,後兩筆被伺服器拒 = 假的「資金不足」;④預約**同一階只送一次**(`_resSent`):樂觀更新會被下一份快照校正回權威值,那份快照可能比伺服器處理購買還早到(RTT > 125ms)⇒ 沒有這道閘就會重送、第二筆被拒 = 玩家看到假的「資金不足」;`RESERVE_RESEND_S` 是被拒時的救濟閥,MUST NOT 短到蓋不住一次往返、也 MUST NOT 拿它當正常流程(解鎖下一階靠「權威等級前進」)。⑤`_tickReserve` MUST 排在商店重繪簽章之前且 MUST NOT 關在 `if (shopOpen)` 裡(預約的用途正是「關著商店去打仗,錢到了自己買」)。稽核 `audit_shop_auto.mjs`(30 項)|
| 陣營對抗對稱化 | `data.js CLASS_SYM` 推導區塊 | 校正係數整組等比套回;MUST NOT 逐武器手改 `vs` 湊平衡;個別角色改 `dmg` 階梯 |
| 對進戰模型 | `tools/duel.mjs`(bal ⑤ 匯入) | **只算武器**是刻意的(只模擬一半招式家族會系統性偏袒);招式導向角色走具名豁免 |
| 機種絕招預算(退場後 = 補償的尺)| `data.js SPECIAL` + `specialBudget()` + `SPECIAL_CD_S` | 總預算隨輕/重綜合等級(可分數階,MUST NOT 進 `tierVal`);MUST NOT 手寫傷害常數或退回單一武器軌。**2026-08-06 機種絕招整組退場後,這份預算不再有任何玩家可觸發的出口** —— 只剩兩個消費端:①純自身型大招的補償當量 `selfUltEq`(見下方 SELF_ULT 那一列);②載具 HP 校準與爆風面積計價仍以它為基準。`SPECIAL_CD_S`(= `SQUAD.KAMI.CD_S`,三招同值)是當量換算的分母,MUST NOT 手寫 30 |
| 三種載具形式(2026-08-06 起只服務大招遞送) | 無人機**自殺攻擊機群** `SQUAD.KAMI`(N=4)/ 變形者**集束轟炸機** `DECOY`(BOMB_MAX=6)/ 機甲**極音速飛彈** `HYPER` + `hyperLaunchRad()`/`hyperApex()`/`hyperArcY()`/`hyperClimbVx()`/`hyperClimbS()`/`hyperRange()`/`hyperDiveSpd()`/`hyperFlightS()`/`hyperTrackR()`/`hyperClimbLen()`/`hyperTerminalF()`;**唯一生成點 = `sim._launchUltCarrier()`**(2026-08-06 起三者只是大招的載具,不再是玩家可單獨觸發的招式)| **機種絕招(飽和攻擊/集束炸彈/極音速飛彈)2026-08-06 整組退場**:`heroKamikaze`/`heroDecoy`/`heroHyper` 三個入口、`rooms.js` 的三條訊息、客戶端三支 `_launch*`、bots 的三個區塊**一律 MUST NOT 復辟**(長按改成招式手勢,見下方 `abilHoldSlot` 那一列)。以下彈道/HP/面積的規則**原封不動地繼續適用於載具**。舊「重砲模式/巨砲」(`BARRAGE`/`barrageShots`/`barrageDur`/`LANCE.BARRAGE_F`/`_barragingDmg`)**整組移除**:重武器射擊路徑上 MUST NOT 再有任何「這一發免彈夾/免電力/免射速閘」的旁路。飽和攻擊的「攻擊力減半、數量加倍」= 同一件事(每架 = 預算 ÷ `KAMI.N`),MUST NOT 另寫折半係數(那會把總預算也砍半);橫向站位走 `kamiSide(i)` 單一縫(伺服器散開角 + 生成側偏移同吃),MUST NOT 復辟 `i === 0 ? -1 : 1`。**護衛機不常駐**(2026-08-06 使用者定案「自殺攻擊機拿掉常駐模組,攻擊時再出現」):觸發前的四架純客戶端貼身外觀(`ESCORT` 系 + `game._buildDroneEscorts`/`_updateEscorts` + `audit_escort_form.mjs`)**整組退場、MUST NOT 復辟** —— 那批模型看得見、跟著飛、卻不在 sim(鎖不到也打不到)= 原則 4 的反面;衝出後的 kami 本來就是伺服器實體、走一般渲染路徑 ⇒「攻擊時再出現」不需要任何新機制,只是把常駐那一段刪掉。極音速飛彈是**伺服器實體**(kind `hyper`,可被鎖定/擊落):兩相位全在 `sim._tickHypers`,客戶端只渲染與播報,MUST NOT 在客戶端算爆風(A1);被擊落**刻意不引爆**(攔截成功 = 完全否定)。**火力(2026-08-06 使用者定案)**:「攻擊力太高了,常常被一轟就爆,同等於 2.5 架自爆無人機的傷害」⇒ 戰鬥部改領 `hyperShare()`(= `HYPER.KAMI_EQ / SQUAD.KAMI.N` = 62.5%,**推導不手寫** —— 改 `KAMI.N` 時「2.5 架份」這句話恆成立,手寫 0.625 則當場分家);Lv1 300 → 188、Lv4 705 → 441,對最脆的無人機(EHP 728)從幾乎一發帶走變成六成。**只動火力、不動範圍**(同日使用者定案「範圍改回舊制」):爆風半徑仍取 `specialBlastR(1)` ⇒ 三招總覆蓋面積逐位元不變,`hyperBlastR`/`HYPER.BLAST_R_F` 那一版已退場、MUST NOT 復辟。**MUST NOT 悄悄調回整份預算**(那正是「一轟就爆」的成因),也 MUST NOT 「順手」把半徑也乘上 `√hyperShare()`(使用者沒有要求的第二次削弱,而且會讓這一招同時失去點殺與面壓制兩頭 —— 實測範圍 62m 版的實得傷害是 197 EHP/次、範圍不動版 102、兩者都改則更低)。**彈道(2026-08-02 使用者定案)**:「前半段拋物線飛向高空,初始角度 45 度,後半段再以極快速度向下螺旋飛向目標」——①爬升段水平**等速** `hyperClimbVx()`(= 出膛速度的水平分量)、高度只由 `hyperArcY()` 這一支給(`y = apex·f(2−f)`,f = 已走水平距離 ÷ 到目標的水平距離)⇒ f = 0 的切線斜率恆 = `tan(LAUNCH_DEG)`,「初始角度」是**推導**出來的不是畫出來的;②頂點取在**目標正上方** ⇒ 頂點高 = `d·tanθ/2` 隨交戰距離變,後半段才是真正的「向下」俯衝(頂點放在半途 = 26° 滑翔,不是使用者要的)。水平航線**發射當下定案**(`x0/z0/ux/uz/arcD`),追蹤對象的移動由**俯衝段**吸收(2026-08-05 起**有條件**,見下)—— 爬升段跟著轉就不再是一條拋物線,兩端也不再插值出同一條航跡。俯衝的螺旋基底 MUST 取**固定水平法向**(`±(uz, −ux)`),MUST NOT 由彈道軸現算:頂點在目標正上方 ⇒ 軸的水平分量趨近 0,現算會在最需要螺旋的那一發整個退化成沒有螺旋。舊制 `APEX_F`(頂點 = `GUN_CEIL_M` × 常數、爬升段**垂直**上升)已退場,MUST NOT 復辟(垂直爬升放不下「初始角度」)。**終端追擊有射程(2026-08-05 使用者定案)**:「前 2/3 的軌跡是飛往發射時目標的初始地點,後 1/3 軌跡時如果目標還在特定範圍(砲塔射程的 1/2)才會以螺旋軌跡繼續追擊目標,如果目標已遠離特定範圍則保持原軌跡」——①**「前 2/3 / 後 1/3」不是新的相位邊界,而是既有兩相位的推導比例**(`hyperTerminalF()` = 俯衝段佔全航跡長度;爬升弧長 `hyperClimbLen()` 為閉式解、與頂點高同樣正比於交戰距離 ⇒ **比例與距離無關、只由發射角決定**,45° 恰為 30.3%)⇒ 判定 MUST 掛在**既有的頂點切換點**上,MUST NOT 另立一個「走完 2/3 就轉彎」的第三相位(那會把頂點從目標正上方挪走 = 拋物線與 ② 的「向下俯衝」當場不成立);②**判定只做一次**(`m.chase`,頂點那一刻):俯衝不到一秒,而追與不追的兩個落點相距可達 `hyperTrackR()` ⇒ 逐 tick 重判會讓彈道在最後零點幾秒**整條折斷**,而客戶端只做插值 ⇒ 兩端插不出同一條航跡;③爬升段因此**完全不讀目標的即時位置**(`m.tx/m.tz` = 發射瞬間烤死的落點,舊制的無條件跟蹤已退場),放棄追擊 = 清掉 `tid` 沿原軌跡打下去,而**螺旋仍在**(那是俯衝段本身的演出,不是追擊才有的東西);④`hyperTrackR()` 推導不手寫,量的是「目標離**原定落點**多遠」而不是離飛彈多遠(頂點就在原定落點正上方,兩者在判定當下等價,但只有前者在整個俯衝段都不漂),伺服器恰一個消費端、`tools/lanesim.mjs` 吃同一條規則(否則 bal ⑦f 把這一招算成必中);⑤`hyperFlightS()`(HP 反解的曝險窗)**刻意仍取原軌跡**(爬升 + 垂直落下),MUST NOT 改吃追擊斜距 `hypot(apex, trackR)` —— 那讓曝險窗長 2.8%、HP 881 → 905,而 bal ⑦f 實測那 2.7% 剛好落在「這一發被打下來沒有」的門檻上,三招實得比 1.65× → **1.93×**(155 → 183 EHP/次,出界);追擊是「打得到人」的加分,MUST NOT 連生存性也一起加成。**⚠ 2026-08-06 起這一整段是死碼**:hyper 只由 `_launchUltCarrier` 生成,而它恆給 `tid: 0`(點遞送不索敵不追擊)⇒ `m.chase` 永遠是 false。規則與稽核都留著(要讓載具追蹤只需給 `tid`),但**在它重新被走到之前,`audit_flight_power` 的終端追擊那幾條驗的是執行不到的路徑** —— 這件事要不要收掉是使用者的決定,MUST NOT 自行刪除定案。客戶端俯仰姿態純表現層(由伺服器位置逐幀差分推導,`_spawnEnt` MUST 給 `rotation.order = 'YXZ'`,否則大偏航時抬頭會歪成側傾)。集束炸彈逐顆走 `sim._decoyBombTarget()` 個別瞄準(名冊 `d.bombed` 輪替),事件帶投擲點 `fx/fz` ⇒ 客戶端畫**榴彈拋物線**;拋擲解與逐幀積分 MUST 吃同一個 `DECOY_BOMB_GRAV_F`(不同 = 畫出來的落點偏掉),傷害仍在伺服器事件當下結算 |
| 招式載具的**發射點**與**槽位 CD 帶**(小招也是輔助機型模式) | `data.js abilOrigin()`(發射點唯一縫)+ `abilDelivered(ch, slot)`(形式分類)+ `abilCdMapped()`/`abilCdBand()`/`abilCdRange()`/`abilCarrierCd()`(CD 帶)+ `ULT_CARRIER.SK_CD_LO·SK_CD_HI` + `ultLaunchLegM()`/`abilLaunchLegM()`(代表發射腿)+ `hyperMaxArcM()`;伺服器 `sim._launchOrigin()`(唯一呼叫點 = `heroCast`)+ `_launchUltCarrier(h, A, x, z, org)` / `_launchUltSupport(h, A, org, slot)` + `_tickSupport` 的 deploy 分支;名冊 `h.hypers`/`sq.decoys`(陣列)/`sq.sups`;模型 `tools/lanesim.mjs` 的 `ownFort` | 2026-08-07 使用者定案兩句:①「**小招也改為輔助機型模式,CD 時間 15~30s,從玩家身邊召喚**」;②「**大招改為從最近的砲塔或主堡召喚**」。載具制自此涵蓋 **32 台 × 兩個槽位 = 64 招**,分類仍是同一支 `abilDelivered`(`ultDelivered` 退成薄殼)⇒ **`heroCast` 完全沒有瞬發結算**(`_castEffect` 只剩 `_ultArrive` / `_supSync` 兩個消費端;裡面再冒出一個 = 「某一招又變回瞬發」,畫面上只表現成「這一招好像特別快」)。兩個槽位**只差兩件事**:發射點(`abilOrigin`:'self' 主機身邊 / 'fort' 最近的我方砲塔或主堡)與 CD 帶(小招 [15,30] / 大招 [30,60],同一支 `abilCarrierCd` 仿射壓入,**嚴格保序 = 排名不變的保證**)。設計主軸:小招 = 隨身戰術(快 CD、就地生成、幾乎沒有前置);大招 = 後方戰略資產(慢 CD、要飛過來、整段航程都可被打下來)。六條紀律:①**MUST NOT 把小招一律做成跟隨編隊** —— 有 `range` 的小招(s03/t08/m05 的 250m 級 EMP)標的是幾百公尺外的一片區域,改成貼著自己的編隊 = 效果圈當場搬回腳下 = 把那三招的身分刪掉,而且沒有任何錯誤訊息;②**小招 MUST NOT 憑空取得遞送距離** —— `hyperRange()` 那個預設值只給大招(它才是「從後方送到指定點」的戰略遞送);③**發射點只有 `abilOrigin` 一份**(sim 恰兩個消費端),MUST NOT 在載具生成端另寫 `slot === 'ult' ? …`;④**最短飛行腿改自發射點量**(站在自家塔下對腳邊施放也要有攔截窗);⑤**輔助機隊的效果窗 MUST 自「就位那一刻」起算**(`g.until` 施放當下是 null,`_supArm` 才寫入)—— 工事召喚的實際腿長隨站位變(`ultLaunchLegM()` = 半個塔距,**只是校準用的代表值**),施放當下就把 until 定死成「施放 + 代表腿 + dur」的話,飛得比那久就在半路到期 = **這一招在深推時永遠交付不到**(同一個坑的第二次,第一次是投放腿被到期判定搶先收掉);飛得遠的代價因此是**曝險**而不是「窗被吃掉」;⑥**`h.hypers` / `sq.decoys` MUST 是陣列** —— 同一名玩家的小招與大招可能是同一種載具形式(robot t01/m06 兩枚飛彈、morph s03/t11/m05 兩架轟炸機),單槽位會被第二發覆寫,第一發從此不再被推進也不會被移除 = 一顆卡在空中、打得到又擋 LOS 的殭屍實體。**HP 校準的連帶**:`hyperFlightS(d)` 帶參數、`hyperHp` 改吃 `hyperMaxArcM()`(= 代表發射腿 + 遞送距離)—— 飛彈全程在高空飛越整條前線(吃 `overflyDps`),多飛的那一段是真的多挨打;**kami / decoy 的曝險窗刻意逐位元不動**(它們量的是「進入敵方前線塔位射程 → 抵達」,而自家前線塔距敵方前線塔 = `tower.range × TOWER_SEP_F` > 塔射程 ⇒ 多出來的那一段飛在射程之外)。稽核 `audit_ult_carrier.mjs` Ⅳ(83 項;`--break-origin` 反向驗證)+ `audit_self_ult.mjs` Ⅵ |
| 大招載具遞送(長按招式取代部分機體的大招) | `data.js ULT_CARRIER`+`ultDelivered()`(轉換判定)/`ultCdBand()`/`ultCarrierCd()`/`ultParts()`/`ultPartN()`+`heroAbility` 的 `carrier` 欄;伺服器 `sim._castEffect()`(招式效果唯一結算點)/`_launchUltCarrier()`(唯一發射縫)/`_ultArrive()`(載具端出口);客戶端 `game._fireHoldAbility`(2026-08-06 起無 converted 分支 —— 長按對 32 台一視同仁)| 2026-08-06 使用者定案三條:①**區域/指向型大招全轉**載具形式(strike/emp/summon + 團隊 heal/buff = 23 台;判定 MUST 是 `ultDelivered` 推導,MUST NOT 手寫名冊 —— 純自身型 9 台維持瞬發且 heroAbility 輸出逐位元不動);②**合併為一招**:轉換角色的長按與 E 同縫(`_fireHoldAbility → _castAbility('ult')`,converted 分支排在機種分派之前)⇒ 伺服器 `heroCast → _launchUltCarrier` 以**該機種絕招的載具形式**點遞送(kami×N / 集束轟炸機逐批 / 極音速飛彈拋物線;`pt`/`uA` 欄,不索敵不追擊);純傷害機種絕招對這些角色退場。**2026-08-06 第二階段把守衛換成了更硬的尺**:三個機種絕招入口**整組退場** ⇒ 驗的不再是「舊路徑對 converted 關著」,而是「**三種載具的唯一生成點 = `_launchUltCarrier`**」(`kind: 'kami'|'decoy'|'hyper'` 全檔各恰一處)—— 後者連新增第二條生成路都擋得住。CD 以 `ultCarrierCd` 仿射壓入 [30,60](**嚴格保序** —— 排名不變的保證,MUST NOT 改成分段表);MP 與升級階梯沿用大招;③**效果取代傷害**:引爆只經 `_ultArrive → _castEffect`(瞬發與載具端共用的效果單一縫,MUST NOT 在載具端另抄一份分支),四條 uA 引爆/擊落路徑一律 MUST NOT `_blast`(補血機炸人 = 預算雙重領,畫面上只是「傷害偏高」);擊落 = 該份**完全否定**(kami 無殉爆、轟炸機不補投、飛彈本就不引爆 —— 同極音速飛彈語意)。**可分預算分批、不可分狀態單載**(`ultParts`:strike 彈著數 / heal 量 / summon 隻數依機種分批 ⇒ 擊落幾份少幾份;emp/buff 是狀態時窗且 mods 逐筆相乘 ⇒ 恆單載,分批會疊乘);整數份額 MUST 走 `ultPartN`(各自 round 湊不回 total)。載具 HP/armor 同三招一把尺(`kamiHp`/`decoyHp`/`hyperHp`,armor/護盾 0);支援型未標 range 的遞送距離 = `hyperRange()`(單一把尺);對自身施放也 MUST 推到 `ULT_CARRIER.MIN_LEG` 之外(使用者定案「需要飛行時間」= 每一發都有攔截窗)。節奏天然分成 爆發型(單彈頭)/ 間斷型(轟炸機每 `BOMB_GAP` 一份)/ 連擊型(kami 魚貫),持續型 = 效果本身的 dur。lanesim `castUltCarrier`/`ultDetonate` 同構分流(strike 傷害 / heal 自補 / dmgTaken 減傷 / emp 武器離線 / summon 加兵,`damage()` 收 `now` 消費減傷 buff);**載具轟炸機的收尾只認 `dieAt`,MUST NOT 加「飛到落點就自爆」**(那是機種絕招時代的撞擊自爆;加回去 = 第一份投出的當格把飛機收掉、剩下的份全數作廢,實測交付率從 50% 掉到 35%,而那是模型自己弄丟的)。bal ⑦f 量**份額交付率**(見下方 SELF_ULT 那一列)。稽核 `audit_ult_carrier.mjs`(52 項) |
| 招式啟動手勢 + 純自身型大招補償 | `data.js abilHoldSlot()`(模式分流唯一縫)/ `SPECIAL_CD_S` / `SELF_ULT`(`ALPHA_S`/`MUL_MAX`/`ALPHA_MAX`/`REALIZED_F`/`REVIVE_INV_S`)/ `selfUltEq()`/`selfUltBoost()`/`selfUltDps()` + `heroAbility` 的 `regen`·`cleanse`·`revive`·`brk` 欄;伺服器 `sim._castEffect()` 的 rally/recon/overdrive/alpha 分支 + `_reviveBody()`/`_breakOnHit()` + `_gateFire()` 的免裝填與破隱窗 + `SQUAD_SHARED` 的 `noReloadUntil`·`alphaArm`·`alphaX`;客戶端 `game._fireHoldAbility()`;圖鑑 `main.charAbilityRow` 的 `MUL_LABEL`/`.cd-boost` | 2026-08-06 使用者定案三條:①**長按右鍵 = 招式手勢**,依當下模式分流(一般 → 小招 / 狙擊 → 大招);②機種絕招整組移除;③失去機種絕招的 9 台純自身型大招改為**提高大招效果**補償。①分流只有 `abilHoldSlot(aiming)` **一份**(長按右鍵 / 長按 R / 觸控招式鈕同吃),MUST NOT 在任一輸入端另寫 `aiming ? 'ult' : 'skill'` —— 第二份就是「某顆鈕在狙擊模式下放的是小招」這種只在特定狀態現形的分歧,而且沒有任何錯誤訊息;短按仍是切換模式,兩者以按住時長區分。**A22 的機種分派縫隨機種絕招一併退場**(`isDrone`/`isMorph` 分派表不得復辟)。②**當量 MUST 推導不手寫**(`selfUltEq` = `specialBudget × REALIZED_F × 大招 cd ÷ SPECIAL_CD_S`):機種絕招每 `SPECIAL_CD_S` 秒發一份,同一段時間裡大招只放得出 `cd / SPECIAL_CD_S` 次 ⇒ 一次要帶回這麼多份,否則「移除」就是淨削弱;手寫一個 2 倍係數的下場是之後改任一 `ult.cd` 或 `SPECIAL.BASE` 補償當場失準,而畫面上只表現成「這幾台好像變弱了」。③**兌現是增額不是再乘一層**(`m + B.dmgMul`;相乘 = 1.35 × 2.35 而不是 2.35 = 多發一份預算),逐 fx 分派:有傷害窗的 buff → `mul.dmg` 增額(除以 `selfUltDps` = **重武器**持續 DPS,走 `weaponDps` 單一縫)/ 自補 → 治療量增額(1:1)/ 匿蹤 → 破隱後 `ALPHA_S` 秒的倍率 / 重新設計的三台(s12 rally・t02 overdrive・m04 recon)→ **效果本身即補償,不再另加乘數**。④**破隱爆發窗只在「開火現形」那一刻開**(`_castEffect` 只上膛 `alphaArm`,窗開在 `_gateFire` 的 `stealthUntil = 0` 那一行**之前**)—— 在 `_castEffect` 就開 = 躲著不開火也在燒那一秒,畫面上只表現成「爆發好像沒生效」。⑤`brk`(t02 超載「挨一發就結束」)只准住 `_damage` 的英雄分支(`_breakOnHit`),判定散出去就是「有些傷害來源不會打斷」;免裝填的補滿 MUST 排在「打空 → 推進填彈計時器」**之前**(排後面 = 免裝填等於沒有);`noReloadUntil`/`alphaArm`/`alphaX` MUST 進 `SQUAD_SHARED`(彈匣只有一份,逐機體各記一份 = 主視野機免裝填、僚機照裝填,只有拿碼表才量得出來)。⑥復活(s12)只救**仍在重生倒數中**的隊友、回場半血 + `REVIVE_INV_S` 無敵幀,位置刻意不動 ⇒ MUST NOT 走 `_respawn`(那支會瞬移回主堡),且 MUST 清 `_trail`(落點閘門會回推到死前的位置)。⑦**`REALIZED_F = 0.35` 自 2026-08-06 起是凍結的歷史量測**:它量的機種絕招已不存在,最接近的類比(帶 strike payload 的載具)差在支配這個數字的性質上 —— 機種絕招自動追蹤、載具是點遞送不追擊,實測對機體+砲塔只有 4.3 EHP/次(名目 391)⇒ 拿 1.1% 重算會把這 9 台折到近乎歸零。要調係數 MUST 改看 bal ⑦f 的「自身型補償」那一行(逐台 EHP/次),MUST NOT 再宣稱它是當輪量出來的。⑧**bal ⑦f 的量測面因此分成兩組且刻意不合併**:載具組量**份額交付率**(對每一種 payload 都成立 —— 效果型沒有 EHP 可量但「有沒有飛到」照樣量得到)、自身型組量 **EHP 當量/次**;硬塞進同一個平均就是舊制失效的原因(效果型 payload 的 EHP 恆為 0,會被讀成「這一招不會交付」)。自身型組**不設離群比** —— 匿蹤不可鎖定 / 無霧視野 / 定位 / 大跳躍 / 原地復活 / 解除異常本模型不計價,那幾台的數字是**下界**,對量不到的東西設離群比就是假精確。⑨夾制(`MUL_MAX`/`ALPHA_MAX`)只是防呆上限,但**現役就已經頂到**(Lv1:s04/t04/m01 的 dmgMul、m08 的 alphaX)⇒ 那幾台的補償**不是**完整推導值,改 `REALIZED_F` 對它們無感;稽核 MUST 把頂到上限的逐台印出來(靜默截斷 = 「補償是推導的」這句話對它們其實不成立)。稽核 `audit_self_ult.mjs`(202 項;2026-08-07 起同檔 Ⅴ 另驗輔助機隊)|
| 自身強化型招式 = 跟隨玩家的輔助機隊(2026-08-07 起兩個槽位共用) | `data.js ULT_SUPPORT`(`SLOT_R`/`SLOT_ALT`/`TURN_K`/`PULSE_F`)+`kindParts()`(機種分批數唯一縫,`ultParts` 同吃)+`supportStackable()`/`supportN()`/`supportF()`/`selfUltTempo()`/`supportTempoF()`/`supportSpeed()`/`supportLegS()`/`supportServiceS()`/`supportHp()`/`supportFleetHp()` + `heroAbility` 的 `support` 欄;伺服器 `sim._launchUltSupport()`(唯一發射縫)/`_tickSupport()`/`_supArm()`/`_supSync()`(疊加唯一縫)/`_supRevoke()`/`_supLost()` + `_castEffect()` 的 `frac` 份額(`mf`/`vf`)與 `once` 守衛;模型 `tools/lanesim.mjs` 的 `ufFrom`/`supSync`/`supDeliver` + `stepAbils` 的 sup 分支 + bal ⑦f「輔助機損失」那一行 | 2026-08-07 使用者定案三條:①「自身強化類的改成**跟隨玩家的輔助機型**進行提供加成」;②「**某些招式換成多個輔助機型,多機型的狀態可以疊加**」;③「持續型招式的輔助機型耐久會比瞬發型與間斷型高,**持續時間越久耐久也越高**」。最後 9 台自身強化型自此也收進載具制 ⇒ **32 台大招全部載具化,只差形式**:點遞送(`ultDelivered`,23 台:飛出去、抵達即引爆) vs 跟隨編隊(這一列,9 台:留在身邊供輸)。效果因此不再是「按下去就一定拿到」——輔助機是 sim 實體(可鎖定、可擊落)。①**疊加是加法不是相乘**:k 架在線 ⇒ 倍率 = 1 + (m − 1) × k/N,實作走 `_supSync` 的「撤下再放」;逐架各推一筆 mods 會被 `_buffMul` **相乘**((1+(m−1)/N)^N ≠ m)⇒ 全員在線時的效果高於舊制,而且只在「剛好幾架活著」那一瞬對得上帳,沒有任何錯誤訊息。**核心不變式 = N 架全在線 ⇒ 效果值逐位元同舊制**;`frac = 1` 也讓瞬發與點遞送三條路徑逐位元不動(`mf`/`vf` 退化成恆等)。②**可疊加才分機**(`supportStackable` 推導,MUST NOT 手寫名冊):二元狀態(匿蹤 / 免疫 / 免裝填)沒有「一半」可言 ⇒ 恆單機 ——這就是使用者說的「**某些**招式換成多個」;機數本身走 `kindParts`(drone `KAMI.N` / morph `DECOY.BOMB_MAX` / robot 1),與 `ultParts` **同一張機種表**(形式即機種絕招)。③**節奏三分同樣推導不手寫**(`selfUltTempo`):瞬發 = 沒有時窗(s11 一次交付完)/ 間斷 = 有時窗但逐 tick 入帳(s12 的 regen 補進去的血不會因為輔助機被打掉而吐回來)/ 持續 = 有時窗的**狀態**(撐不到底就是整段沒有)。係數 `supportTempoF` 的兩個端點(0 / 1)是**定義**,只有中間的 `PULSE_F` 是旋鈕(校準錨 = bal ⑦f 的自身型組)。④**耐久推導不手寫**(`supportHp` = `frontKillHp(投放腿 + 節奏係數 × dur ÷ 機數)`):與三種點遞送載具共用「前線一組塔位」那一把尺(砲塔數值一改,四種載具的 HP 一起漂),armor / 護盾恆 0。**兩段窗的曝險方式不同,MUST NOT 整段除以機數**:投放腿是 N 架**同時**在飛(平行,每架各扛一段)、效果窗才是敵人一次點名一架(串行,÷ N)。整段除的下場是瞬發型每架只剩 20 點 —— 一顆步槍子彈就打掉四分之一份治療(2026-08-07 實測:稽核場景的機隊在第一個 tick 就被路過的小兵清掉 3 架);「瞬發型耐久最低」不等於「見光即死」。使用者③的兩條因此是**推導出來的結論**而不是另外加的規則。⑤**投放腿 = 點遞送載具的最短飛行腿**(`ULT_CARRIER.MIN_LEG ÷ supportSpeed()`,推導不手寫)⇒ 每一次施放都有同樣長的攔截窗(使用者 2026-08-06 定案「需要飛行時間」);`_tickSupport` 的**投放腿推進 MUST 排在到期判定之前** ——排反了瞬發型(dur = 0)會在 tick 量化那一格被收掉 = 這一招**永遠交付不到**(實測 s11 補血恆為 0)。⑥**一次性效果只做一次**(`once` 守衛):復活 / 解除異常 / 彈匣全滿 / 定位 / 匿蹤 / 無霧視野 MUST 只在第一次施放時做,重放會把時窗一路展期(「這一招怎麼一直沒結束」);而**不住在 mods 的二元狀態**(匿蹤 `stealthUntil`、免裝填 `noReloadUntil`)MUST 在機隊清空時由 `_supRevoke` **顯式撤掉**(留著就是「輔助機都被打下來了還是隱形」)。視野 `visionUntil` 是具名例外(已經給出去的情報收不回來,而且它是陣營層級的取大值)。⑦**CD 刻意不壓進 [30,60] 帶**:那是 23 台「合併為一招」那一輪的仿射映射,把 9 台也壓進去會同時改掉 `selfUltEq` 的分子(補償 ∝ cd)⇒ 一個改動同時動兩個平衡面。⑧渲染沿用 kami 那具小型載具(縮小成該角色的無人機)⇒ 客戶端零改動;`kind: 'kami'` 因此有**兩個具名生成點**(`_launchUltCarrier` / `_launchUltSupport`),MUST NOT 有第三處。⑨模型端(bal ⑦f)輔助機 MUST 當**真的實體**跑(進 `foesOf`)—— 模型看不到它就等於這一輪改制沒有發生;損失率由「輔助機損失」那一行印出來(恆 0 = 這一條沒有兌現),而模型的選敵是**最近優先**、輔助機貼著主機 ⇒ 那個比例是實戰的**下界**。⑩**2026-08-07 第二輪起這一段兩個槽位共用**:所有推導吃 `slot`(預設 'ult' ⇒ 既有呼叫端逐位元不變),投放腿走 `abilLaunchLegM(slot)`(小招 `MIN_LEG` / 大招 `ultLaunchLegM()`)⇒「大招的輔助機比小招的硬」是**推導出來的結論**(服務窗比較長),不是另外加的規則;效果窗自**就位**起算(見上一列 ⑤)。稽核 `audit_self_ult.mjs` Ⅴ・Ⅵ(`--break-stack` / `--break-tempo` 反向驗證)|
| 自殺攻擊機不常駐 | `SQUAD.KAMI` 檔頭的具名退場記錄;衝出後的載具走 `sim._launchUltCarrier`/`_tickKamis` 與客戶端 `_spawnEnt` 的一般渲染路徑 | 2026-08-06 使用者定案「自殺攻擊機拿掉常駐模組,攻擊時再出現」。觸發**前**的四架純客戶端貼身護衛機外觀(`ESCORT`/`escortSlot()`/`escortLagBase()`/`escortLagK()`/`escortDrift()`、`game.js _buildDroneEscorts`/`_updateEscorts`、`tools/audit_escort_form.mjs`)**整組退場,MUST NOT 復辟**:那批模型看得見、跟著飛、卻不在 sim(鎖不到也打不到)= 原則 4 的反面;而衝出後的 kami 本來就是伺服器實體、以 `SIZE_F` 縮小走一般渲染路徑 ⇒「攻擊時再出現」**不需要任何新機制**,只是把常駐那一段刪掉(想「補完」成「觸發瞬間先擺個編隊再衝」= 又把第二份實作放回來)。連帶兩件事:`kamiSide()` 只剩伺服器一個消費端(生成側偏移),`kcd` 隨 2026-08-06 機種絕招退場一併退出 HUD。稽核 `audit_flight_power.mjs` Ⅰ(客戶端零殘留 + `kamiSide` 單一消費端)|
| 絕招載具 HP 校準 / 爆風面積計價 | `data.js towerDps()`/`towerSurviveHp()`/`towerKillHp()` → `TOWER_SITE_N`/`frontDps()`/`frontSurviveHp()`/`frontKillHp()`/`waveDps()`/`overflyDps()`/`overflySurviveHp()` → `kamiHp()`/`hyperHp()`/`decoyHp()`(各自 `kamiExposureS()`/`hyperFlightS()`/`decoyExposureS()`);半徑 `specialBlastR()`(招內切分)/ `hyperBlastR()`·`hyperShare()`(具名例外)| 使用者把三招的生存性一律以「持續射擊剛好…」表述(飽和攻擊剛好衝進去 2 架 / 極音速飛彈剛好打不爆 / 集束轟炸機剛好投得完 5+1 顆)⇒ 三個 HP 全是**推導值**,MUST NOT 手寫。三種載具一律 **armor 0 / 護盾 0**:校準要精確,EHP 就 MUST NOT 隨主機角色或升級漂移(舊 `KAMI.HP_F`/`DECOY.HP_F` 已退場,MUST NOT 復辟)。**尺 = 前線一組塔位,不是一座孤塔**(2026-08-02 由 bal ⑦f 定案):塔位的幾何本來就是**同塔位雙塔**(② 的塔距不變式 / ④ / lanesim 場景),載具要穿越的是**兩座**。舊的單塔基準讓兩招在有塔的前線**結構性歸零**(實測有效傷害佔預算比:飽和攻擊 3.8%、極音速飛彈 **0.0%**、集束炸彈 35.5%),而且完全看不出來(玩家只覺得「絕招好像沒什麼用」)。三招 MUST 共用這一把尺,只調其中一招 = 把相對強弱寫死成手感。極音速飛彈**另計一波兵**(`overflyDps` = `frontDps` + `waveDps`):只有它的設計用途是「站在塔的射程外拆塔」(`hyperRange > tower.range`)⇒ 航路必然從敵方兵波頭上飛過去,而且它沒有另兩招那種「被打下來還剩一半」的緩衝(攔截 = 完全否定),差一點就是差全部。**爆風半徑也是預算**:半徑 ∝ √(該彈頭分到的預算比例),基準 = 單一戰鬥部的 `HYPER.BLAST_R` ⇒ 三招**總覆蓋面積相同**、「把預算切成越多顆」MUST NOT 憑空生出攻擊範圍(舊制 4×r22 / 6×r14+1×r20 / 1×r26 = 19.7k / 16.0k / 6.9k m²,差到 3 倍;這是 A35「攻擊範圍要計價」在絕招上的同一條)。**半徑與火力自 2026-08-06 起是兩件事**:極音速飛彈的戰鬥部只領 `hyperShare()`,但爆風**仍取 share = 1 的基準**(使用者定案「範圍改回舊制」)⇒ 這條面積不變式逐位元不受火力改制影響;MUST NOT 「順手」把半徑也乘上 `√hyperShare()`。舊的逐招常數 `DECOY.R`/`DECOY.BOMB_BLAST_R` 已退場,MUST NOT 復辟;演出半徑 MUST 取結算用的同一份 `def.r`(看到多大 = 打到多大)|
| 飛行動力學(受擊掉高 / 爬升動力) | `data.js FLIGHT` + `airSinkM()`/`liftMax()`/`liftRegen()`/`liftDrainPS()`;消費端 `game._airSinkHit()`/`_stepLift()` | 2026-07-30 使用者需求。①掉高 = **位移** ∝ 傷害,校準錨「打完平均護盾+裝甲 = `SINK_TOWERS` 個砲塔高」⇒ 係數由 `SQUAD.DRONE_AVG_HP` × `TARGET_H.tower` 推導(MUST NOT 手寫、MUST NOT 改成速度制或逐機體血量制);②動力只有**往上飛**消耗,耗速 = 上限 ÷ `DRAIN_S`(推導不手寫),上限/回速正比於電力(`maxMp` / `mpRegen`×`chargeF`),見底 = **爬不上去**(MUST NOT 改成減速,與 `slopeBlocked` 同語意)。位置本就客戶端權威 ⇒ 真人那半住客戶端物理;**bot 沒有客戶端**,掉高由 `sim._botAirSink()` 補上同一支 `airSinkM`(兩條扣血路徑都要掛,含護盾全擋的早退),真人 MUST NOT 在伺服器再套一次;NPC 直升機/集束轟炸機/護衛機/極音速飛彈(伺服器腳本航線)MUST NOT 套 |
| bot 操作節奏 | `data.js BOT_DIFF/BOT_OPS/botOpGap()` + `bots.js _op()` | MUST NOT 另寫 tick 計數節流;持續開火刻意只吃反應時間、不吃手速閘 |
| 電腦玩家學習策略 | 策略檔 `botPolicy.js`(工具產出、零 import、人手 MUST NOT 編輯)+ `data.js BOT_TACTIC_BASE`(凍結基準快照)/`BOT_LEARN`(白名單+邊界)/`botPolicySanitize()`(夾制唯一縫)/覆寫迴圈(恰一處 `BOT_TACTIC[k] =`)/`balanceFingerprint()`;讀取縫 `bots.js this.tac`;學習迴圈 `tools/bot_learn.mjs` | 2026-08-06 使用者需求「最佳操作策略的電腦玩家,平衡性調整時可不斷學習」。手寫 BOT_TACTIC 是基準,離線自對戰(CRN 配對鏡射 + 鏡射高斯擾動 ES)優化**取捨型旋鈕**寫回策略檔,平衡指紋一變即暖啟動重學 —— 策略追指紋不追 commit。四條紀律:①**只學取捨不學能力**(視野/手速/準度 MUST NOT 進白名單,A32);②**使用者定案值(PULL_SP/BASE_HP/PULL_HP/RESUME_HP/EXEC_MAX)與帳的時鐘(THREAT_S)不可學** —— 後者因 `sim._hurtLog` 累加淡出吃同一支 `botThreatDecay`,逐 brain 各走各的秒數 = 記帳與讀帳兩個時鐘;③白名單鍵 MUST 只被 tactic/elite 分支消費 ⇒ 新手/低難度**結構性地**逐位元維持舊制;④夾制只有 `botPolicySanitize` 一份(執行期與學習工具同吃),邊界鏡射 audit_bot_tactics 守門線 ⇒ 學壞了也寫不出非法策略;空 policy = 中性 = 逐位元同基準(原則 6)。bots.js 旋鈕讀取 MUST 經 `this.tac`(`BOT_TACTIC.` 零殘留 —— 第二條讀取路 = 學習候選蓋不到的死角);helper 尾參數 `T = BOT_TACTIC` 不傳 = 舊行為。學習輪的**收尾閘門**:乾淨種子上沒贏過被取代的策略就不寫檔。設計全文 `docs/bot_learning.md`;稽核 `audit_bot_policy.mjs`(40 項)|
| 電腦玩家視野 | `data.js BOT_VIEW`/`botFovHalf()` + `bots.js _fovHalf()`/`_bearing()`/`_face()`/`_turn()`/`_alertLook()` + 方位來源 `sim.js _hurtLog()` | 2026-08-02 使用者定案「電腦玩家跟玩家一樣只有前方特定角度範圍的視野,其他方向敵人來襲視角要跟著轉向,**不可以有全角度視野**」。①**半視角推導不手寫** = `atan(tan(UNITS[kind].fov/2) × BOT_VIEW.ASPECT)`(相機吃的是**垂直** fov;bot 沒有畫面 ⇒ 寬高比取寬螢幕基準,偏差朝「不苛刻 bot」);**只限水平** —— bot 沒有俯仰狀態,硬套垂直錐會讓巡航無人機對正下方整批失明(原則 6)。②選敵閘門只住 `_acquire`(塔/主堡同樣吃),MUST NOT 退回全角度掃描。③`h.ry` 的**唯一寫入點 = `_turn`**,步進只准經 `viewLockStep`(真人視野鎖定輔助的同一支角速度上限)—— `_face` 只寫意圖 `_wantRy`;直接指派目標角 = 瞬間回頭 = 視野錐當場失效,而畫面上只看得出「bot 轉得比較快」。④**受擊警戒**:方位只准來自 `_hurtLog`(全伺服器唯一知道「被誰從哪裡打」的地方,與濺血提示同一份帳;另開第二份必定分家),記在**主視野機**上但僚機挨打也算,轉到攻擊者落進視野錐或逾時 `ALERT_S` 即交還;警戒 MUST 排在狀態機**之後**(來襲方向優先於原本想看的方向)。⑤推線的朝向 MUST 取**前進方向**(沿兵線前瞻 `PUSH_LOOK_M` ≫ 側向散開 `LANE_JITTER_M`)—— 看向腳下那個線上目標點 = 機體停在它旁邊 ±jitter 處、那條連線幾乎垂直於兵線 = 一路側著頭推線、正前方的敵人全數失明。稽核 `audit_bot_vision.mjs`(94 項)|
| 電腦玩家戰術(選敵 / 撤退 / 打帶跑) | `data.js BOT_DIFF.tactic·elite` + `BOT_TACTIC` + `botTargetPrio()`/`botThreatDecay()`/`botSalvo()`/`botExecW()`/`botKiteF()` + 記帳 `sim.js _hurtLog()`(威脅)/`_dmgOut()`(輸出)+ 消費端 `bots.js _threatOf()`/`_recentDmg()`/`_prioritize()`/`_pullWant()`/`_inFight()`/`_pickRally()`/`_rally()`/`_progAt()` | 2026-08-02 使用者定案。**分層只認旗標**:`tactic`(中/高)= 威脅選敵 + 兩段撤退線、`elite`(高)= 撿尾刀/打帶跑/扛半條護盾就後撤;bots.js MUST NOT 比對難度字串,新手/低難度**逐位元維持舊制**。①選敵三項(對我傷害最高 / 對我方總輸出最高 / 快陣亡)MUST 正規化成**候選集內的佔比**再進 `botTargetPrio`(絕對傷害量跨場次無可比性);撿尾刀權重 `EXEC_MAX` 刻意 ≫ 1(「剩一口氣」是排序偏好,「這秒打得死」才是插隊理由)。②**帳只有一份**:威脅住 `_hurtLog`(與濺血方位、受擊警戒同一次記帳,英雄以 pid 為鍵)、輸出住 `_dmgOut`(**兩條結算路徑都要記** —— 只掛一般結算那條會漏掉護盾全擋的發數 = 系統性低估高護盾對手);威脅累加 MUST **先淡出舊帳再加**(`v += dealt` = 永不遺忘,每秒刮 2% 的騷擾一分鐘後會被讀成「剛扛了半條護盾」)。③撤退三道閘缺一不可:遲滯帶(進場 `PULL_SP` < 出場 `RALLY_SP`)、RETREAT 不被 RALLY 搶走(裝甲只有主堡補得回來)、「扛半條護盾」量**近期吃下的傷害**且**排除塔/主堡刮傷**(拆塔本來就是站在射程裡挨打;漏掉這條 = 每次攻堅到一半自己退掉,實測攻堅產出腰斬)。④RALLY 行為分兩半:**還在挨打邊退邊打、脫離接觸就停火停步等護盾**(繼續開火 = 引來還擊、脫戰計時一直被重置 ⇒ 卡在 RALLY 直到裝甲見底)。⑤集結復出的 `prog` MUST 走 `_progAt`(當下位置投影回兵線),吃集結點進度會把機體往回拉。稽核 `audit_bot_tactics.mjs`(109 項)|
| 電腦玩家定位分類與策略 | `data.js BOT_ROLE_FEATS`(特徵唯一真相)/`BOT_ROLES`(四個定位:剖面 + 乘數 + 採購序)/`BOT_ROLE`(幅度與合法域)/`BOT_BUY_ORDER` + `botRoleFeats()`/`botRoleScores()`/`botRoleOf()`(唯一分類處)/`botRoleRoster()`/`botRoleNorm()`/`botRoleMul()`/`botRoleTactic()`(唯一覆寫處)/`botBuyOrder()` + `botTacticCross()`(交叉約束唯一實作,與 `botPolicySanitize` 同吃)+ `BOT_TACTIC` 的 `KEEP_F`·`KEEP_STRUCT`·`PRIO_HERO`·`PRIO_STRUCT`·`CAST_HURT` 五個新旋鈕;消費端 `bots.js _resolveRole()`(唯一解析縫)→ `this.tac`(唯一讀取縫)+ `_acquire`/`_engage`/`_castSupport`/採購迴圈 | 2026-08-08 使用者需求「依機體技能等數值為電腦玩家分類,並設計不同策略」。改制前 32 台機體共用**同一張** `BOT_TACTIC`:同樣的距離環/選敵權重/撤退線/採購序 —— 一台 6.4m/s 的偵察無人機與一台扛 26 護甲的攻城機甲用一模一樣的打法,而那張表只可能對其中一種是對的(沒有錯誤訊息,只表現成「這台 bot 不太會用自己的機體」)。①**分類推導不手寫**:七項特徵一律轉呼既有唯一縫(五條吃 `HEX_AXES[].val` —— 圖鑑六角圖那六軸的**同一份**取值函式、攻堅吃 `buildDps`、支援吃 `heroAbility` 的團隊供輸比例)⇒ 改任一平衡數值,圖鑑與 bot 分類**同步**改;MUST NOT 出現任何逐角色名冊(`s01: 'raider'` 一寫下去,改平衡時沒有任何東西會提醒你它過期了)。②**特徵是相對全場的位置**(對數分位,同 `hexBand`:差幾倍而不是差多少)—— 絕對值跨槽位沒有可比性;`aid` 是具名例外(本來就是 0~1 的佔比,取分位會退化成二元旗標),`power` 刻意不收(它決定「多久放一次招」,不是「該站多近、該先打誰」)。③**定位 = 剖面內積的 argmax**:分數 = `Σ w×(特徵−0.5)`,**特徵置中**是公平性的來源(各項都中庸的機體對四個定位一律 0 分,不會因為某個剖面正權重多就先天佔便宜)、`Σ|w| = 1` 讓四個分數落在同一把尺上。四個定位 = 突襲(高機動貼身點殺)/ 壓制(大制域中距離)/ 攻堅(硬底盤拆工事)/ 支援(招式供輸隊友)。④**策略是既有旋鈕的覆寫,不是第二套決策系統**:只改 11 個旋鈕 + 八軌採購序,bots.js 的狀態機/選敵/撤退流程一行不動,且 MUST NOT 出現任何 `if (role === …)` 行為分支(第二套決策系統會與難度分層打架)。五個新旋鈕的基準值**逐一等於**改制前 bots.js 的硬編碼常數(0.6/0.85/0.55/1.3/0.55)⇒ 中性逐位元同舊制。⑤**覆寫是重分配不是通膨**:乘數以**角色數加權幾何平均 = 1** 正規化(同 `AOE_BUDGET.NORM`/`SPEED_COMP`/`BUILD_DPS`)⇒ 全場幾何中點逐位元錨在使用者定案值;少了這一步,「給每個定位一點加強」= 整批 bot 悄悄變強,而 `npm run sim` 的勝負旗標對這種整體漂移是飽和的(量不出來)。夾到邊界的項目 MUST 逐項印出來(靜默截斷 = 「乘數是推導的」對它其實不成立);**`PRIO_STRUCT` 的下界刻意是 1**:定位最多把工事加價收到「不加價」,MUST NOT 變成優先拆塔(把塔排在英雄/小兵之前 = bot 站在塔下被集火)。⑥**難度分層不得被繞過**(A33):只在 `BOT_DIFF.tactic` 之下解析 ⇒ 新手/低難度**結構性地**逐位元維持舊制。⑦**與學習迴圈疊加而非取代**:`bot_learn` 學的是全體共用的**基準**,定位是疊在其上的相對偏移(五個新旋鈕**不進**學習白名單 ⇒ `botPolicy.js` 與平衡指紋逐位元不受影響);**刻意不為四個定位各學一份**(搜尋空間 ×4、樣本數 ÷4 = 訊號淹在雜訊裡)。解析點 MUST 在 `update()` 而非建構函式 —— 學習迴圈是**構造之後**才注入基準(`b.tac = candTac`),在建構函式裡定案會被那一行整份蓋掉;基準 MUST 只記一次(`_tacBase` 守衛),不記就會在自己身上累乘,換座機幾次之後距離環飄到夾制邊界而且完全無聲。設計全文 `docs/bot_roles.md`;稽核 `audit_bot_role.mjs`(80 項)|
| 機體碰撞量體 / 實體碰撞 | `data.js SELF_F`/`selfCollider()`/`COLLIDE_KINDS` + 客戶端 `game.js _collide()`/`_sweepBlockers()`/`COLLIDER` + 伺服器 `sim.js solidResolve()`/`_solidsNear()`/`solidPush()`/`solidEnter()` + 呼叫端 `bots.js _move()` | 2026-08-02 使用者定案「電腦玩家的碰撞法則一律跟正常玩家一樣,移動與攻擊都不可穿牆穿越各種物理碰撞的物件」。舊制 bot 直接寫 `h.x/h.z`,只在 `_push` 繞開 `hazBlockers`(交戰/撤退兩段連那個都沒有)⇒ 從建物/砲塔中間穿過去。**量體係數只有 `selfCollider` 一份**(舊制住 game.js、climb.js 另抄一份 0.317):兩端各寫一份 = 真人撞得到、電腦穿得過(碰撞版的 A30)。障礙集兩個來源鏡射客戶端的兩個迴圈 —— 上傳碰撞柱走 `_losGrid`(LOS 與碰撞本來就是同一組實體,MUST NOT 另建第二份索引)、有量體的實體走 `COLLIDE_KINDS` ∪ 英雄 ∪ `HAZARDS[].block`(未上傳世界的 headless 對局只剩這條,舊行為不變)。垂直閘一律把上傳碰撞柱視為 `[0, top]`(伺服器無地形高程,同 `_losBlocked` 的近似)⇒ 機體底高過柱頂就飛得過去。**掃掠與 push-out 缺一不可**:只做 push-out 的話低速貼牆完全正常、擊退/掉幀那一幀直接穿到另一側;`fwd === 0`(終點剛好落在通過障礙中心的平分面)MUST 歸「遠半」交給掃掠夾住 —— 最小穿透軸的推出符號在正中央等機率翻向另一側 = 一步穿牆(2026-08-02 逐位元復現),兩端 MUST NOT 只改一邊。bots.js 的 `h.x`/`h.z` **唯一寫入點 = `_move`**;碰撞上線後 MUST 配撞牆繞行(`_skirt`/`_stuck`)—— bot 沒有尋路,正面頂著建物會**原地卡死** = 整條兵線不推,而「bot 站著不動」很容易被誤判成 AI 難度問題。**2026-08-04 使用者定案「NPC 與機體之間的物理碰撞不要破圖」**:客戶端的**單位**(NPC/敵方英雄/阻擋型障礙)舊制只做 push-out、而且自成一段迴圈跑在掃掠之前 ⇒ ①「掃掠與 push-out 缺一不可」對建物成立、對單位一樣成立(擊退/蓄力跳/掉幀那一幀的位移動輒數公尺 ≫ 一名步兵的直徑,終點早就落在另一側 = 整台機體從小兵/戰車/敵機身上穿過去,而伺服器 `solidResolve` 一向是兩段都做 ⇒ 兩端分家);②名冊只有 `game.js _unitSolids()` 一份,掃掠與 push-out MUST 同吃(各掃一次 `ents` = 兩份實作,症狀是「衝過去那一下穿過小兵、停下來才被推開」);③圓柱幾何收成 `_circleEnter()`/`_pushOutCircle()` **各一份實作**(障礙圓柱與單位同吃),與伺服器 `solidEnter`/`solidPush` 的圓柱分支逐案例同值;④單位與世界障礙 MUST 在**同一趟** pass 迴圈交錯收斂,順序 MUST 是**先單位、後世界障礙** —— 擠得下時兩者都滿足、順序無關,擠不下時(小兵把機體頂在牆面上而縫隙不夠一台機體寬)最後一趟說了算,**MUST 讓世界幾何贏**:陷進 NPC 只是兩個模型交疊,陷進建物是鏡頭看穿牆面的破圖而且爬不出來;⑤垂直帶 ε MUST 與伺服器 `_solidsNear` 的 `span()` 同式。稽核 `audit_bot_vision.mjs` + `audit_npc_collide.mjs` |
| 飛行體的貼地渲染基準 | `game.js _flySurf()`(唯一縫);消費端只有 `_updateEnts` 的 `ent.flies` 分支 | 2026-08-04 使用者回報「有時候 NPC 飛行單位在橋上會越飛越低」。**飛行體的高度基準 MUST 是 (x, z) 的純函式**,MUST NOT 沿用地面單位那條逐幀棘輪:`surfaceAt(x, z, curY)` 是為地面單位設計的「我在橋上還是橋下」消歧規則,curY 由上一幀高度回填 ⇒ 它是**單向棘輪**。飛行體的種子 = `cur.y − heroY`,兵線擁擠時 `sim._advance` 的側推把直升機推出橋面足跡一次(`deckY` 回 null)⇒ 種子掉回橋下河床,之後上橋的兩個條件再也不成立(距橋面 > `DECK_STEP`、橋腹淨空又 ≥ 最大機體)⇒ 基準面**永久**停在河床 = 貼著谷底飛、穿過橋面板(「有時候」= 剛好被推出去的那幾架;同一個棘輪在隧道段是 `curY < ceil` ⇒ 直升機一頭鑽進山肚子)。基準面 = **地表 ∪ 橋面 ∪ 隧道頂板**取最高者 ⇒ 沒有狀態就沒有回不去的狀態(核心不變式 = **路徑無關**:同一座標不管從哪飛來都逐位元相同)。**點狀地物刻意不收**(`blockerTopAt` 的建物/神木/巨岩/地標):那不是連續結構面,收了直升機經過路邊電塔會整台彈到 35m 再落回來,而兵線走廊本就淨空。地面單位那半(兵線剖面場 `_laneSurfAt` 種子 + 棘輪)MUST 逐位元維持舊制。稽核 `audit_npc_collide.mjs` |
| 攻擊範圍收斂(一發不得吃兩塔)| `data.js AREA_WEAPONS`(名冊)+ `towerPairSepM()`/`soloBlastRmax()`/`blastFootprintR()`(幾何上界)+ 夾制迴圈 + `areaValue()`/`aoeTrimRaw()`/`aoeTrimF()`(火力回補) | 2026-08-02 使用者定案「輕重武器縮減攻擊範圍,除了以範圍見長的武器之外,其他都避免一次打到兩座塔」。①上界 **推導不手寫** = (`2×TOWER_SIDE_OFF`/2 − `TARGET_R.tower`) ÷ `BLAST.EDGE`(距離量到**命中量體表面**,與 `sim._blast` 同判;量到中心會少算一個塔半徑 = 夾不夠緊)。②三分類只有 **blast** 進夾制(同 `aoeClass` 一份):`fan` 扇形「大面積就是它的本體」= 使用者說的範圍見長、`line` 貫穿要吃到兩座塔必須射手與兩塔共線(夾它等於把貫穿機制拿掉)。③豁免名冊 MUST 附理由且是**凍結清單**(增刪 MUST 同步稽核)—— 只靠「有沒有附理由」擋不住膨脹。④**收掉的範圍 MUST 還回火力,但只能是重分配不是通膨**:bal ①④⑤ 全是單體模型(爆風半徑從來沒進過算式)⇒ 補償當純加法發下去,那些模型只看得到「傷害整批變高」(實測 W=0.35 純加法:drone ① 衝到 46%、t11→s07 的射程壓制 35%→57%)。故整批補償除以 `AOE_BUDGET.NORM`(未正規化係數的**幾何平均**)⇒ 讓出越多範圍拿越多火力、讓得少的讓出去,火力水位不動;`W` 的語意是**分配陡度**不是總量,W→0 逐位元回到舊制。⑤夾制 MUST **無條件捨去**且末階由 `tierVal` 外推式**反解**(逐項四捨五入會把頂階推回上限之上;逐項捨去後 Lv4 外推 2·v₃−v₂ 會反彈 —— t09 [15,17,19] 實測回到 4.445 = 規則整條失效)。⑥**改 r / 名冊 / 上界 MUST 重掃 `SEEK.R_M`**:核心帶 `blastCoreR` 跟著縮(6.0m → 1.5m),舊的導引頭半徑在 50% 射程有 4 把武器打不進核心帶 = 光暈亮著卻沒命中(A30 家族)。⑦**2026-08-04 使用者定案:名冊清空 + 爆炸型改吃「家族帶」**(`BLAST_BAND`/`blastFamily()`/`blastCapR()`)—— 使用者兩條:「榴彈類射程調整為較短射程的那一類,縮小爆炸範圍、**不可一次命中兩座砲塔**」+「增加雷射導引、射後不理的攻擊範圍,同爆炸傷害、但**範圍較榴彈類小**」。前者直接廢掉豁免名冊(裡面本來就只有那三把榴彈類)⇒ **沒有任何爆炸型武器再享有豁免**,`AREA_WEAPONS` 保留為空的具名縫。後者於是只能靠家族帶表達:三族全部擠進同一條 `soloBlastRmax()` 之下 ⇒ 榴彈吃滿上限、導引(guide + fnf **同一族**)取其 `GUIDED_F` 倍。夾制因此從「等比收斂到頂階貼齊上限」改成**把授權階梯仿射映射進家族帶 [cap × LO, cap]** —— 舊制讓授權階梯越陡的武器 Lv1 被壓得越小(導引類授權 11~15m 實測只剩 2.88~3.17m = 幾乎是點命中),`LO` 才是使用者說的「增加攻擊範圍」那一項。仿射的**定義域取整個家族的授權跨距**(不是逐把武器各自伸展到帶的兩端):逐把伸展會把「誰的範圍比較大」整組抹平 —— 集束子母彈與無後座砲會拿到逐位元相同的半徑,那是刪掉一個設計維度。範圍照樣要付錢:授權值與定案值的價差由既有 `aoeTrimF` 自動計價(半徑漲的武器 `aoeTrimRaw < 1` = 基礎傷害折減)。榴彈類射程 MUST 是全體重武器解析後射程的**最小值**(= 扇形那一帶)。稽核 `audit_aoe_trim.mjs`(73 項)|
| 對建築 DPS 收斂 | `data.js BUILD_DPS` + `buildDps()` + 收斂迴圈;唯一寫入點 = `vs.building` | 2026-08-04 使用者定案「所有重武器之間與輕武器之間對建築的 DPS 不要落差太大」。改制前實測(對砲塔持續 DPS,Lv1):重武器 2.8 ~ 30.8(**10.8 倍**)、輕武器 19.9 ~ 62.8(3.2 倍)—— 10.8 倍的意思是同樣站在塔下打滿一場,有的機種拆得掉兩座塔、有的連一座都刮不完,而玩家看到的只是「我這把武器打建築好像沒有傷害」。①**旋鈕是 `vs.building` 不是 dmg 階梯**:dmg 同時決定對所有目標的火力(bal ①④⑤⑦ 全吃它),拿它調建築就是把四條不變式一起推走;`vs.building` 是類別剋制那一軸,**只**作用在建築上。②形狀同其餘三個預算(逐槽位幾何中點為軸,推導不手寫):`vs.building' = vs.building × (該槽位 DPS 幾何中點 ÷ 這把武器的 DPS) ^ K`,再夾到 (0, `BUILDING_VS_CAP`] ⇒ 新離散度 = 舊 ^ (1 − K),K = 0 逐位元回到舊制。**逐槽位**取中點(同 `RANGE_BUDGET`):輕重武器的建築 DPS 本來就不同級距,混一鍋量到的是槽位不是武器。③**收斂 MUST 是重分配而非削弱,而水位 MUST 量在 bal ④ 量的那個量上** = 滿級(Lv4)逐角色拆塔 DPS 的**算術**平均 —— 只鎖幾何中點是不夠的:壓縮保住乘性中點卻必然壓低算術平均(把長尾收進來),而拆一座塔的秒數吃的是算術量;實測只鎖幾何中點時 ④ 的機甲/變形者從剩餘 2.7%/4.9% 掉到 −0.1%/−0.1%(兩座塔都推不掉)。另一半原因是 A34 ① 的上夾:高的可以無限降、低的最多補到 1.0,補不滿的缺口一樣要回填 ⇒ 壓縮後等比抬回水位再夾,重複到收斂。④MUST 排在 `COMBAT_SCALE` 統一縮放**之後**(`buildDps` 走 `heroWeapon`,射程縮放會定案 `rangeMid`/`rngDmgF` 的快取)。⑤這一段**不改 `counterDmgF`**:夾制後 `vs.building ≤ 1` ⇒ 它對 `narrow` 的貢獻恆為 0,升降都不回頭改基礎傷害(無回饋迴圈)。現值 K = 0.55:輕武器 3.16× → **1.71×**、重武器 10.8× → **2.92×**。稽核 `audit_shield_counter.mjs` Ⅵ |
| 三軸預算(範圍 / 機動 / 射程)| `data.js AOE_BUDGET`+`aoeTrimF()` / `MOB_BUDGET`+`mobMid()`+`mobDmgF()` / `RANGE_BUDGET`+`rangeMid()`+`rngDmgF()`;套用點只有 `heroWeapon` 的 `dmg` 一欄(與 `counterDmgF` 並列) | 2026-08-02 使用者定案「涵蓋 DPS / 攻擊範圍(考量實質戰鬥角度)/ 射程 / 移動速度,重新調正武器平衡性」。**每一軸都要有價格**:DPS 本來就是 `dmg` 階梯,其餘三軸過去全是免費的。機動/射程一律以**同儕的幾何中點為軸**(高於中點者火力下修、低於者上修 ⇒ 重分配不通膨,與 `CLASS_SYM` 取幾何中點同理),K = 0 即逐位元回到舊制。射程的中點 **MUST 逐槽位取**(輕 102~122m / 重 158~194m;混一鍋 = 量到的是槽位不是射程優勢)。解析後射程只有 `heroRange()` 一份(計價與實戰同吃)。**K 有實測上界**:`MOB_BUDGET.K` ≥ 0.15 起 t02 的 bal ⑤ 勝率頂到 80%、0.28 另把 ④ 的 drone 站外攻堅推到 205s > 200s 預算;`RANGE_BUDGET.K` ≥ 0.25 同樣讓 t02 出界 ⇒ 現值 0.10 / 0.15 是「bal ①④⑤⑦ 同時全綠」的最大值,MUST NOT 只因為 ⑦ 好看就調過頭。**招式刻意不吃**(使用者:先不考慮長按技和大小招)。⑦ 量到的軸靈敏度:射程 +15% = +21.3pp 勝率 ≫ 火力 +15% = +4.1pp ≫ 範圍 +50% = +2.1pp ≫ 移速 +15% = +0.6pp —— 射程貴在 `dmgFalloff` 的平台/衰減段都是**射程的比例**,長射程不只是打得遠,是同一個距離上衰減更少。要靠火力折減吃回 12% 的射程優勢得砍近八成傷害(不可能也不該);真正的旋鈕是 `rangeCap ← UNITS[kind].sight`,牽動 #INC-104,**另案**。稽核 `audit_aoe_trim.mjs` Ⅳ |
| 前線交戰模型 | `tools/lanesim.mjs`(bal ⑦ 匯入;擊發排程唯一縫 = `reFire()`) | **`reFire()`(2026-08-02)**:舊制 `next = t + 1/rate` **每發丟棄不滿一格的殘量** ⇒ 模型的有效射速被步進格點吃掉最多 43%(步進 0.125s 之下 rate 7 只打得出 4.0 發/秒),量到的是「射速落在格點哪一側」而不是武器強弱;正式對局的射速閘量的是**連續時鐘**,根本沒有這一層。改成從**排定時刻**累加 ⇒ 長期平均射速 = 標稱、與步進無關(檔頭「步進收斂」那條這才真的兌現)。空窗超過一格一律只補一格,MUST NOT 讓沒有目標的那段時間把發數存起來事後連射。三個排程端(機體槽位 / NPC / 砲塔)MUST 全吃這一支。射速壓縮把全部輕武器重新落到格點上,舊 bug 因此會在 ⑦c 上表現成「某個機種憑空變強 3.3pp」。 2026-08-02 使用者定案的模擬方式:「須同時考量射程/速度/攻擊/範圍/兵波/砲塔等等,簡易模擬環境只有前線雙砲塔 + 兵波NPC(生成頻率同正式遊戲),模擬從雙方射程外開始接敵,有錢就升級,先擊毀敵方機體或一座砲塔者獲勝」。與 `duel.mjs` **分工不合併**:那支是 1v1 純武器 + 高度差掃描的快速前置篩,對「攻擊範圍」這一軸完全無感;本支是**攻擊範圍唯一被計價的模型**(三類範圍各按自己的 2D 幾何選目標,分類縫仍是 `aoeClass`)。場景距離/秒數全部由 `data.js` 推導(塔距 = `tower.range × TOWER_SEP_F`、塔位橫向 = `TOWER_SIDE_OFF`、編制 = `waveComp()`、間隔 = `waveInterval()`、步進 = `GAME.TICK_MS`),MUST NOT 手寫。三個曾經量錯的地方都寫在檔頭:①**升級 MUST NOT 重建槽位**(彈藥與裝填計時歸零 = 有錢的一方無限彈藥,實測 mag 3 / reload 12s 的 152mm 在 8.5s 打出 3071 傷害);②**站位要同時有下限與上限** —— 沒有下限 = 完美風箏(drone 65.5% / robot 41.4%,量到的是模型允許無限後撤)、沒有上限 = 全員貼臉(drone 36.8% / robot 61.8%),兩者都不是機體真的強弱,故以**兵線接觸線 ± `LANE_SAFE_M`** 框住並以 `towerHold` 當objective 下限;③**閃避刻意不吃「這一步有沒有位移」**(模型只有兵線軸一個自由度,拿「到位就停 = 不閃避」建模會讓加速反而變弱,實測 +15% 移速勝率 42.2%)。步進收斂已實測(dt 0.05~0.20 四段結論一致 ±1.2pp)。**長按 = 大招,兩組都在模型內**(2026-08-06 使用者定案;`TRACKS` 仍不含 sk/ul —— 升級軌不買,但招式本身要跑):①**載具組**的載具 MUST 當**真的實體**跑(進 `foesOf` ⇒ 敵方砲塔/小兵/機體都打得到它,擊落照付賞金),差別才落在「飛幾秒 / 那幾秒裡被不被打下來 / 幾份飛得到」上;②**自身型組**走 `castSelfUlt` 開一段時窗(`M.uf`:dmgF / takenF / evade / rangeF / speedF / regenF / noReload / vamp / brk),效果值一律經 `selfUltBoost` 這一個縫取,模型自己算一份就是「bal 說平衡、打起來不是」。**施放時機有兩道閘,少一道這一招在模型裡就是淨損**:交戰中才放(舊制靠「reach 內有 aim」天然擋住開場空放,自身型沒有落點也就沒有那道閘 ⇒ t=0 站在射程外把 75~85 電力倒掉,實測放大招那側**必敗**)、補血型等真的掉血(`LANE.HEAL_FRAC`,鏡射 `bots._castSupport` 的 `hurt < 0.55`;滿血放掉的治療被 `Math.min` 整份吃掉而帳上仍記一次施放 ⇒ 讀成「兌現 0」)。記帳 MUST **分桶**(hero / tower / creep;自身型的 `dealtEff` 同一條原則):總實得會被清兵灌爆(半徑大的一招掃兵波動輒吃到兩三隻,而兵波每 `waveInterval` 就補一批、對兩個勝利條件幾乎沒貢獻;實測總傷害與有效傷害差到 4 倍)⇒ ⑦f 只認 hero + tower 那兩桶。**本模型看不到的價值 MUST 逐項列在檔頭並在 ⑦f 印出來**(匿蹤不可鎖定 / 無霧視野 / 定位 / 大跳躍 / 原地復活 / 解除異常)—— 那幾台的數字是**下界**,靜默吞掉就是拿「量不到」冒充「沒有價值」。爆風命中名冊只有 `blastHits` 一份(絕招引爆直接吃它、武器路徑另乘 `dmgFalloff` —— 爆風本身沒有距離衰減,對齊 `sim._blast`)。單軸擾動 `tw` 的 `dmg`/`aoe` MUST **一起作用在招式上**(那兩軸現在有一大半是招式在扛,只擾動武器 = 訊號被稀釋到量不出方向性;實測「攻擊範圍 ×2」的自驗勝率會從 54.2% 掉到 49.3% 而失效),`range`/`speed` 刻意不套(招式的施放距離與飛行速度是各招自己的機制);`tw.noUlt` 是 ⑦f 的**反事實對照組**旗標(同一台機體只差不放大招),MUST NOT 拿它當一般擾動軸 |
| 目標類別剋制 / 建築加乘 | `data.js TARGET_CLASS` + `vsMult()` + `BUILDING_VS_CAP` 夾制段 | 2026-08-02 使用者定案「移除對建築物加乘的武器」:①`GRENADE.BUILDING_MUL`(launcher ×1.4)**整組刪除**,MUST NOT 復辟;②各武器 `vs.building` **只留懲罰不留加乘** —— 夾到 ≤`BUILDING_VS_CAP`(=1)。夾制是 CHARACTERS 之後的**一段推導迴圈**,MUST NOT 回頭逐武器手改那 60 幾行 vs 表(32 角一動就漂移的老病)。名冊 MUST 涵蓋所有帶 vs 的 def(`WEAPONS`/`DECOY`/`HYPER`/CHARACTERS 的 light·heavy·skill·ult)—— 漏一張表 = 那條路徑還留著攻城加成,而畫面上只表現成「這招拆塔特別快」,沒有任何錯誤訊息。`heroWeapon`/`heroAbility` 的 `vs` 是**同一個物件參照** ⇒ 夾在源頭即全鏈生效。`_heroDmg()` 從此**恆無任何情境倍率**(只剩 `vsMult × _buffMul`)。稽核 `audit_shield_counter.mjs` Ⅰ |
| 護盾 / 裝甲分軌剋制 | `data.js shieldSplit()`(結算)+ `shieldRoleName()`(標籤)+ 武器欄 `vsSp`/`vsHp`/`spPierce` | 2026-08-02 使用者需求「加入擅長打護盾但主 HP 傷害較弱、會穿透護盾造成低比例 HP 傷害但總傷害較低等各類型武器」。舊制剋制軸只有「目標**類別**」,沒有「目標**血條層**」⇒ 新增三個旋鈕:`vsSp`(對護盾層)、`vsHp`(對裝甲 HP 層)、`spPierce`(0~1,該比例無視護盾直接進 HP 層)。組出三型:反護盾(vsSp>1、vsHp<1)/ 穿盾(spPierce>0、總傷害低)/ 反裝甲(vsSp<1、vsHp>1)。**四個消費端 MUST 全吃 `shieldSplit`**:伺服器 `sim._damage`(英雄雙層 + 非英雄以 sp=0 呼叫)、客戶端 HUD 估算 `game._hitFeedback`/`_lanceFeedback`、對進戰 `duel.apply()`、平衡 `balance.slotDps`/對塔 DPS —— 任一端自己手寫一次 `Math.min(sp, dmg)` 就是第二份拆分邏輯,症狀是「HUD 數字與實際掉血對不上」或「bal 說平衡但打起來不是」,兩者都極難從畫面反推。**核心不變量 = 溢出按「預算」折回**(`spent` = 被護盾吃掉的原始傷害預算):MUST NOT 把打穿護盾後溢出的**護盾傷害**直接倒進裝甲層 —— 那會讓 vsSp 高的武器在殘盾目標上白賺一次加成,且只在「盾剛好見底」那一發出現 = 對局裡看起來是隨機爆發傷害。`vsSp = 0` 因此自然退化成「護盾全擋」而非「無視護盾穿過去」(原則 6;要穿請用 `spPierce`)。**`vsHp` 對無護盾的 NPC/建築一樣生效**(只對英雄成立 = 隱形的第二套規則);`vsSp`/`spPierce` 對它們天然無作用。中性參數(1/1/0)**逐位元還原舊制** ⇒ 未標註的武器完全不受影響。旋鈕**刻意不過 `tierVal`**(是武器性格不是火力,隨階級漂移 = Lv1 與 Lv4 是兩把武器)。`_damage(t, dmg, by, pen, floorHp, wd)` 的 `wd` 只由**握有武器 def 的傷害路徑**傳入,環境/地雷/塔 SAM 一律不帶 = 中性。稽核 `audit_shield_counter.mjs`(71 項)|
| 護盾軸配置紀律 | `data.js EX_SIEGE_WEAPONS`(名冊)+ `VS_DEFS` 夾制段(反護盾獨占)+ `COUNTER_BUDGET`/`counterLoad()`/`counterDmgF()`(基礎傷害折減) | 2026-08-02 使用者定案三條,**全部做成推導**(「當初調的時候記得」不算數據):①**穿盾 / 反裝甲只准掛在原本吃建築加成的武器上** —— 那批攻城重武器剛被拿掉 `vs.building` 加乘,新軸正好把騰出來的預算還回去;掛到別把武器 = 舊的沒還、新的又多發一份。名冊 `EX_SIEGE_WEAPONS` 是留檔(夾制後查不回原值),改動 MUST 同步稽核 Ⅴ。**反護盾刻意不受此限**(走②③那條路),稽核釘住這個豁免。②**反護盾武器(`vsSp > 1`)的 `vs` 全欄夾到 ≤1** —— 護盾軸對「每一個有護盾的對手」恆常生效,再疊類別剋制 = 一把武器領兩份預算;懲罰(< 1)保留,拿掉的是加成不是剋制關係。夾制 MUST 排在 `CLASS_SYM` **之後**(那段把 drone 的 armor 欄 / 非 drone 的 air 欄整組等比放大,排前面等於沒夾,而且只在部分角色身上失效更難察覺),且與建築夾制**共用同一份 `VS_DEFS`**(各掃一次遲早漏一張表)。③**加成越多、越廣泛 ⇒ 基礎傷害越低**:負載 = `BROAD ×(護盾軸超出中性)+ NARROW ×(類別剋制超出 1)`,折減 = `1/(1 + K × 負載)`。`BROAD ≫ NARROW` 是在標差價 —— 類別剋制只在遇到那一類時兌現,護盾軸恆常生效,同樣寫 1.5 不是同樣的價錢。**只作用在掛了護盾軸的武器**(`broad <= 0` 直接回 0)⇒ 其餘 28 名角色傷害逐位元不變;校準只走 `dmg` 階梯與這三個係數,MUST NOT 回頭手改 `vs`(那是 `CLASS_SYM` 的欄位,手改會被下次對稱化等比放大蓋掉)。`K` 的校準錨 = bal ⑤(K=0.5 曾把兩把變形機甲重武器同時砍近 26% ⇒ morph 44.4% 出界)|
| AoE / 彈道分類 | `data.js aoeClass()`(blast/fan/line)/`trajClass()`(lob/flat/line/guide/fnf) | 由 `def.type`/`fan`/`guide` 推導;sim/演出/HUD 共用;MUST NOT 手寫逐武器分類表 |
| 貫穿演出 | `game.js _lanceVisual()` + `lanceR(def)` | 自機/他人/bot 共用;粗細 = 伺服器判定半徑。2026-08-01 起**無任何情境倍率**(舊 `LANCE.BARRAGE_F` 隨巨砲移除);`lanceR` 的第二參數 MUST NOT 復辟 |
| 榴彈火控 | `game.js _lobAim()`(每幀定案 `_lobFc`)+ 準星解 `_lobCrosshair()` + 火控解 `_lobLadder()`(對地 `_lob45Vel` / 對空裝藥階梯)+ 積分 `_arcTrace(…, draw)` | 出膛向量/瞄準虛線/鎖定光暈/砲管仰角共用;光暈 = `_arcTrace minD ≤ LOB_TOL`,MUST NOT 退回直射線判定。**火控解與積分只准一份**:`_lobAim`(draw=true 寫繪製緩衝)與射程光暈判定 `_reachable`(draw=false)MUST 同吃 `_lobLadder`(含 `aa` 旗標)—— 判定端另寫簡化積分 = 光暈與實際彈道分家。**2026-08-02 使用者定案四條**:①**目標以準星優先** —— 目標解只有 `_lobCrosshair` 一份(`_updateAaMode` 判彈射模式、`_lobAim` 取瞄準點同吃),錐形輔助 `_aaTarget` 只在準星解不到實體時接手(舊制無條件吃 8° 錐內最正對的飛行單位 ⇒ 瞄準點與**鎖定**都被從準星底下拉走);②準星瞄地面 ⇒ 固定 **45°** 投擲,初速由「落點 = 瞄準點」反解(`v0² = g·L²/(L−dy)`,MUST NOT 手寫初速表);仰角 ≥ 45° 無解才退回裝藥階梯(低伸解本來就更陡,兩者在 45° 處連續);③準星瞄空中 ⇒ 維持彈射模式(裝藥階梯 + `AA_MV`),MUST NOT 被 45° 接管;④中途碰撞即引爆、沒碰撞就續飛到射程界原地爆。**射程 = 以射擊點為中心的球面,與軌跡無關**(2026-08-02 使用者定案;見下一列)|
| 射程界(彈體/積分/解保險) | `game.js _updateBullets` 的 `spent = b.pos.distanceTo(b.origin)` + `_arcTrace` 的 `spent = p.distanceTo(from)` + `_updateVisShells` 同式 | 2026-08-02 使用者定案「**射程範圍與軌跡無關,是射擊點為中心的球面**」:凡「這一發還能不能再飛」的判定 MUST 只量 `|彈頭 − 發射點|`,弧長/導引修正/離架散布繞的路一律不計。**球面是「遊戲空間」球面,不是真實世界球面**(使用者定案):座標系只有一個,x/z/y 全是遊戲公尺、三軸等權 ⇒ 直接對原始座標 hypot 就對了。兩個比例尺都在戰鬥層之外一次套完,MUST NOT 滲進任何射程/彈道判定 —— `REAL_SCALE`(遊戲世界 = 真實 ×2)只住經緯度→遊戲公尺的邊界(`llToMeters`/rooms 的 `SC_GAME`)、`COMBAT_SCALE`(reach 減半)只住 `data.js` 統一縮放塊(`def.range` 進到判定時已是遊戲公尺;`game.js`/`sim.js` 全檔不得出現它)。**會壞掉的改法**:看到「遊戲地形比真實平緩約兩倍」(高程走真實公尺、水平吃 `1/REAL_SCALE`)就想在 y 軸乘個補正把球面拉回真實比例 —— 那會讓伺服器的射程變成橢球,而客戶端光暈/彈體是純遊戲空間的正球 ⇒ 兩端立刻分家(A30)。**航跡長 `b.dist` 整組退場**,MUST NOT 復辟(逐彈道分支 `b.seek`/`b.lob` 一併移除 —— 一顆球面管全部);解保險距離 `ARMING` 吃同一顆球(光暈的琥珀帶 `surf < arm` 本來就是直線量的)。這條同時把其餘三處對齊成同一個球:失鎖判定、伺服器 `heroBurst` 落點閘門 `dist2d(射手, 落點)`、射程光暈 `from.distanceTo(aim)`。唯一例外 = 射後不理**鎖定後**改吃追擊燃料(A7)。**雷射導引吃同一條規則**(使用者定案):中途碰撞就爆、目標移開就續飛、飛到球面原地爆 —— 導引點出球面只是 `b.guide = false`(不再修正航向),MUST NOT 在那裡把彈體收掉。**球心 = 射擊點 = 槍口**:客戶端一律量槍口(彈體 `b.origin`、光暈 `_reachable` 的 from),伺服器凡吃**誠實界**(無 `RANGE_TOL`)的路徑 MUST 量同一個點 —— `heroLance` 與 `heroPlasma`(2026-08-02 補上)都收客戶端回報的槍口 `o:[x,z,y]` + 同一道 `dist2d ≤ 12` 防作弊閘,取不到才退回機體中心(bot 無槍口回報)。球心差一個槍口前伸量,誠實界沒有寬容可吸收 = 那一整條邊界帶「光暈亮著卻不掉血」。**具名例外**(刻意的水平圓柱,只放寬不收緊):`heroBurst` 落點閘、`_acquireTarget` NPC/塔索敵、`heroCast` 招式落點、`_decoyBombTarget` 投彈足跡 —— 稽核 `audit_weapon_gate.mjs` Ⅻ 逐條釘住。**球心 = 彈藥擊發當下的位置,後續機體的移動不影響**(2026-08-05 使用者定案;縫 = `sim.js _trailPush()`(位置軌跡唯一取樣點,排在 `tick` 尾端掃 `_allBodies`)+ `_shotOrigin()`(球心與擊發時刻回推量 `back` 的**同一個解**)+ `data.js shotTrailS()`(保留窗)):客戶端一向如此(`b.origin = muzzle.clone()`,擊發那一刻就烤死),少的是伺服器那一半 —— AoE 彈頭是**著彈**才回報,而 45° 拋投滿射程要飛近 6 秒,舊制的 `dist2d(h.x, h.z, 落點)` 讓球心一路跟著機體跑,兩個方向都沒有錯誤訊息:①開完砲往後退(最自然的動作)⇒ `dImp` 憑空變大 ⇒ 合法彈著被驗證後靜默丟棄 = 零傷害;②開完砲往前衝 ⇒ `dImp` 憑空變小 ⇒ 射程外的彈著反而收下 = `RANGE_TOL` 之外的隱形射程。①**球心 MUST 由伺服器自己的位置軌跡回推**,MUST NOT 改成收客戶端回報的擊發座標 —— 那等於把落點閘門的球心交給客戶端指定,`impCap` 當場失效(A1);②回推 MUST 是「由新到舊取第一筆滿足 `已過時間 ≥ 從那裡飛到落點的時間` 的樣本」,MUST NOT 改成迭代逼近(拋物線的 `dT/dd ≈ 0.017 s/m` 配上高速機體只收縮到 0.68,三五次迭代還差幾十公尺);③`back` MUST 取同一個解回傳的飛行時間(在 `heroBurst` 裡拿 `dImp` 再算一次 `shotFlightS` 就是第二份實作,機體一移動兩份立刻分家),取飛行時間而非 elapsed 也讓**靜止射手逐位元同舊制**(elapsed 有 tick 量化);④**僚機吃同一條規則**(整個小隊在那 6 秒裡是一起移動的,只修主視野機 = 僚機那 1/3 傷害照樣被丟);⑤保留窗 `shotTrailS()` **推導不手寫** = 全體武器裡最長的那個回推窗(與 `cap` 同一條式:fnf 取 `chaseCapS`、其餘 `flightCapS`),取樣率 = tick 率(落點閘有 `RANGE_TOL` 吸收那 125ms 的 3m,而球心從「跟著機體跑」變成「釘在擊發點」是差幾十公尺的事);⑥重生 MUST 清軌跡(瞬移);軌跡不足以回推 ⇒ 退回機體當下位置 = 逐位元同舊制(原則 6)。**`heroLance`/`heroPlasma` 不在此列**:兩者的球心本來就是回報的槍口 = 擊發點,而扇形是瞬發、貫穿彈的飛行時間 ≈ 0.2s(槍口防作弊閘 12m 蓋得住) |
| 出膛初速 / 彈頭飛行時間 | 初速 `data.js shotV0()`;**飛行時間 `shotFlightS()`** + `flightCapS()`(= 在落點閘門上界打出去要飛多久,高差取 `altDhMax()`) | **兩者是兩件事,MUST NOT 互相代用**:拋射武器吃吊射初速而非砲口初速(差六倍;`game._shotV0` MUST 只是轉呼),但**對地拋投的實際初速自 2026-08-02 起是 45° 反解**(`_lob45Vel`),`shotV0` 只剩「解的上限」語意 ⇒ 飛行時間 MUST 走 `shotFlightS`(lob = √(2(d−dy)/g)、其餘 = 3D 斜距 ÷ `shotV0`),**MUST NOT 在任一端自己拿 `d / shotV0` 除一次**(低估 2.2 倍:172.8m 真實 5.94s vs 舊估 1.73s)。AoE 彈頭**著彈**才回報 ⇒ 凡是拿著彈時刻驗「擊發資格」的閘門(`heroBurst` 需瞄準、`_gateFire` 裝填計時器 `back`)MUST 經這一支換算回擊發時刻,MUST NOT 手寫秒數。低估的兩個症狀都是靜默丟包(2026-08-03 使用者回報「榴彈投擲只有遊戲一開始有傷害」):①開完砲收鏡去移動 ⇒ 天上那一輪整組蒸發;②伺服器的裝填窗比客戶端晚 4 秒 ⇒ 下一輪只要打得比上一輪近就整輪被吃掉。`back` 刻意吃**同高**的誠實估計(高估 = 裝填提早結束 = 偷射速)、`cap` 才吃 `altDhMax()` 的俯射餘裕(偏差朝「不擋」,原則 6);**射速閘刻意仍量真實時鐘**(著彈順序可能與擊發順序相反) |
| 導引頭機動 | `data.js SEEK` + `seekTurn()` | 角速度與「轉彎半徑 ≤ `SEEK.R_M`」兩個上限取寬者;`game._updateBullets` 兩處轉向 MUST 經此,MUST NOT 手寫 rad/s。**只放寬不收緊**(單純 `初速 ÷ R_M` 會讓慢速巡飛彈變鈍) |
| 射程閘門容差 | `data.js RANGE_TOL` + `altRangeMax()` | 伺服器複驗客戶端回報(命中/落點/射線)的網路寬容**只有一個值**;sim.js 每道射程閘門 MUST 吃它,MUST NOT 逐處手寫倍率。落點/爆點類閘門(`heroBurst`)拿不到目標實體 ⇒ 高度制空取 `altRangeMax()` 上限當誠實界(比客戶端緊 = 高地合法彈著被靜默丟棄)。**寬容只放給「客戶端已自行夾過射程的回報」**:`heroPlasma`(扇形)只收到一個射向、`heroLance` 的逐目標判定沿圓柱由伺服器選人(端帽本就可外溢 `R + hitR`)⇒ 兩者 MUST 吃誠實界(不乘 `RANGE_TOL`,與 `botFire` 同一條規則),否則寬容直接變成 25% 的隱形射程 = 光暈不亮的敵人照樣掉血(2026-08-01 使用者回報)。`heroLance` 與 `heroPlasma` 的 `d3` 都是從**回報的槍口**量起(2026-08-02 起扇形也收 `o`),與客戶端 `_reachable` 的 from 同一個點 ⇒ 誠實界不會誤丟;誠實界路徑的球心 MUST NOT 退回機體中心(前科:扇形舊制從機體量,槍口前伸的那一段變成「光暈亮著卻不掉血」)|
| 高度制空射程 | `data.js altRangeF()` + 伺服器 `sim._altDh()`/`_altRange()` + 客戶端 `game._altRangeTo()`/`_effRange()`/`_maxRange()` | 曲線只有 `altRangeF` 一份、兩端同吃;客戶端 MUST 逐目標算(舊制拿「前方地表」近似 = 第二份公式,與伺服器必然分家)。**高度差 MUST 同參考框相減**(`_altDh` 單一縫,射程/爆擊 `_altCrit`/閃避 `_dodges` 三個消費端同吃):英雄回報的 `ay` 是**絕對**高程(含地形海拔),NPC/塔在伺服器是**離地**高 + 基準 0 ⇒ 跨框相減等於拿場地海拔當高度差,真實場地(海拔 > 3 個砲塔高 = 78m)一律封頂 = 英雄對所有 NPC 恆 +25% 射程。兩邊都拿得到絕對高程才用絕對框,否則退回離地框(原則 6 寧缺勿錯)。找目標的搜尋半徑走 `_maxRange`(機制上限,只准寬),**找到目標後 MUST 以 `_effRange(def, ent)` 誠實夾回** |
| 「打得到嗎」判定 | `data.js REACH_RULE`/`reachRule()` → `game.js _reachable()` | **準星那一發**的唯一判據。逐彈道五類全覆蓋:`path` arc(火控階梯)/ray(直線段);`hit` blast(落點落在 `blastCoreR` 核心帶,**刻意不吃 LOS** ← A11)/clear(線段整段淨空,對齊伺服器 `_losBlocked`);`arm` = 導引/射後不理的軌跡修正期轉警示色。消費端 MUST NOT 自己比對 `trajClass`/`def.type` |
| 範圍光暈名冊(這一發會傷到誰) | `game.js _shotVictims()`(+ `_inShotRange()`/`_bodyDy()`/`_shotWarn()`);唯一消費端 `_updateRangeGlows()` | 2026-08-03 使用者定案「範圍光暈不是單純在射程範圍內就亮,而是:**準星的目標在射程內被擊中時、同時會受到傷害的單位才亮**」——光暈語意第三代:①距離夠近 → ②這一發打得到 → ③**這一發的傷害足跡**。舊制在前線會讓半個畫面同時發光,而其中絕大多數不是這一發會傷到的人。②那份判據沒被丟掉,而是收斂成足跡的**入口閘**(準星目標打不到 ⇒ 誰都不受傷 ⇒ 全場熄燈),入口閘 MUST 仍走 `_reachable`。**足跡分類只走 `aoeClass(def)` 一份**(MUST NOT 在此比對 `def.type`),幾何逐類鏡射伺服器:blast ← `sim._blast`(量到命中量體最近點 = 水平 `_hitR` + 垂直帶 `_bodyDy`,`blastFalloff > 0` 入列;**刻意不吃 LOS 也不吃射程** ← A11,牆邊炸開照樣傷到牆後的人 —— 這正是這一代最要讓玩家看見的那一組單位)/ fan ← `sim.heroPlasma`(水平夾角 + 逐目標射程 + 射線淨空,`d2 ≤ 8` 視為錐內)/ line ← `_lancePierced`(`sim._lanceHits` 的既有客戶端鏡射)/ null ← 只有準星那一個。拋物線 MUST 吃每幀已定案的火控解 `_lobFc`,MUST NOT 在名冊裡重解一次彈道;扇形與貫穿**刻意沒有整發性的入口閘**(錐/圓柱都從槍口張開,伺服器 `heroPlasma`/`heroLance` 同樣不看準星解到誰)—— 準星壓在射程外的遠方敵人身上時,錐內 5m 的那一個照樣會掉血,把整組熄掉才是騙人;它們的射程與視線一律由**逐目標**的 `_inShotRange` 夾回(誠實界,與 `_surfD3 ≤ range × _altRange` 同一把尺)。警示色是**整發**的性質(名冊一起轉琥珀),`_shotWarn` 是唯一實作、恰兩個消費端。舊的分幀預算/TTL 快取(`RANGE_GLOW.TTL_S`/`ARC_PER_FRAME`/`RAY_PER_FRAME`)隨「逐敵人各跑一次彈道積分」一併退場,MUST NOT 復辟。稽核 `audit_weapon_gate.mjs` Ⅴ-b |
| 爆風超壓帶 | `data.js BLAST`(`CORE`/`EDGE`/`EXP`)+ `blastCoreR()` | `blastFalloff` 與「打得到」判定共用同一組轉折點;曲線形狀不變,MUST NOT 在任一端手寫 0.5/1.8/1.3 |
| 受擊濺血提示 | `data.js BLOOD` + `bloodDur()`/`bloodAlpha()`/`bloodFrac()`/`bloodDropR()`/`bloodDropN()`/`bloodScreenUv()`;方位來源 `sim.js _hurtLog()`/`_flushHurt()`;消費端 `game.js _bloodSplat()`/`_updateBlood()`/`_clearBlood()` + `main.js hud.blood()` | 2026-08-02 使用者需求「被攻擊時的畫面要有半透明紅色濺血提示,會視敵人射擊的方向決定濺血位置,傷害越高血滴越大」。純表現層(傷害仍伺服器結算),但**方位 MUST 來自伺服器**:客戶端只有血量落差(受傷暈影那一份)⇒ 自己猜的方向必定與伺服器分家(A1)。伺服器於 `_damage` 兩條路徑(護盾全擋早退 + 一般結算)記帳,同一 tick **同一攻擊者併成一筆**(散彈一次多發 = 一坨大血漬),flush 點**只有 `_frame()`**、且 MUST 排在取走 `events` 之前 —— 傷害同時發生在 tick 內與訊息處理當下(heroHit/detonate),放進 `tick()` 會漏掉 tick 之間回報進來的命中;只記**主視野機**(僚機沒有 flush 點,累積會漏水)。**高程只在攻擊者也是英雄且有 `ay` 時附上**(絕對框,與客戶端 `pos.y + _eyeH()` 同式):NPC/塔在伺服器是離地高、地基恆 0,跨框相減 = 拿場地海拔當高度差(`_altDh` 同一個坑),取不到就退回「只吃水平方位、垂直畫在中線」(原則 6)。血滴大小吃**傷害佔自機總量(maxHp + maxSp)的比例**而非絕對值(各機種 EHP 差一個量級);螢幕座標一律夾在 `[EDGE, 1−EDGE]` ⇒ 背後中彈貼齊左右邊緣、MUST NOT 折回中央;半視角吃**當下鏡頭**(狙擊縮 FOV 自動跟著收,MUST NOT 另寫一份縮放)。位置與圖樣**建立時烤死**(血漬黏在座艙玻璃上,MUST NOT 每幀重算);`.blood-splat` MUST NOT 掛 CSS transition/animation(與逐幀曲線相爭);陣亡/換座機 MUST `_clearBlood()`。稽核 `audit_blood_splat.mjs`(78 項)|
| 角色 / 機體檔案格式(含不顯示的生成資料)| **格式** `codex.js`(`SECTIONS`/`PROTO_LAYERS`/`GEN_FIELDS`/`CODEX_FIELDS`/`VIS_WORDS`/`HAIR_WORD`/`protoLayers()`/`charCodex()`/`mechaCodex()`/`textSeed()`/`imagePrompt()`/`modelSheet()`/`codexIssues()`)・**內容** 角色 `lore.js` / 機體 `mecha.js`;唯一顯示端 `main.js protoHTML()` | 2026-08-04 使用者定案「角色與機體的完整數據與文字說明的格式**完全對齊**,特別是機體原型的介紹,也包括**不顯示與遊戲、用於文本生成、2D 生圖或 3D 建模**使用的部分,都統一標準格式」。①**格式與內容分家**:段/欄/必填/怎麼組裝只住 `codex.js`,`lore.js`/`mecha.js` 只填內容 —— 兩邊各寫一份欄位表就是「請保持一致」那種撐不住的約定。②**對齊是結構性的**:兩種檔案共用同一張 `SECTIONS`(識別/簡介/詳閱/生成)與同一張 `GEN_FIELDS`(`sil`/`mass`/`mat`/`parts`/`tag`/`note` 六欄**同鍵同義**),`codexIssues()` 逐欄比對兩邊鍵集**逐位元相同** ⇒ 只在角色那邊加一個欄位當場紅字。③**機體原型改成結構化的層**,而且**該有哪幾層由 `visual` 推導**(`real` 恆有 / `frame` ← `visual.proto` / `bionic` ← `visual.creature` / `air`・`ground` ← `visual.flight`・`ground`;合計 64 層) —— 舊制是一條自由字串由 `main.js` 用正規式切「現實/機體/體態/仿生原型:」四個標籤 = **第二份格式定義**,而且沒有任何東西保證誰該有幾層:實測 32 台長出 4 種標籤、**8 台只有一層**、**變形者的兩個型態被擠在同一句「A ↔ B」裡**(那是一層不是兩層),而畫面照樣顯示、沒有任何錯誤訊息。變形者因此恆有**兩層**形態原型;每層恰兩欄 `{ src 名稱, note 設計理由 }`,MUST NOT 揉成一句(揉了就退回自由字串)。④**值一律到原處取**:全高走 `heroTargetH()`(隨 `mods.armor` 內插)、機體名/主色/外觀參數走 `data.js`、機種走 `charKind()` —— `mecha.js` MUST NOT 手寫公尺數或複製 `hue`/`kind`/`side`(抄一份的症狀是「改了 data.js,圖鑑與生圖單沒跟著動」)。⑤**對外的三份生成文字**(`textSeed` 文本生成 / `imagePrompt` 2D 生圖 / `modelSheet` 3D 建模)一律由 `codex.js` **組裝**,MUST NOT 在 `lore.js`/`mecha.js` 手寫第二份(手寫的不會跟著欄位變,只表現成「生出來的圖與設定慢慢對不上」)。⑥`mecha.js` **零 import**(同 `rng.js`/`vernacular.js`):離線稽核與文本/生圖/建模管線要直接吃真品;`codex.js` 只准 import `data.js`/`lore.js`/`mecha.js` 三支純資料檔。⑦`tag` 是**名詞短語**不是句子(≤ `TAG_MAX` 且無標點)—— 那一欄是餵給 2D 模型的關鍵詞,寫成敘述就失去作用。⑧**型態姿態 `FORM_POSE`、動作姿態 `SHOT_POSES`、適用性 `visualUses` 是三個新的單一縫**(2026-08-05)。**`SHOT_POSES` 是圖的第二個維度**(靜止/移動/重武器)且與型態**正交**:`FORM_POSE` 講「機體在哪」、`SHOT_POSES` 講「它在做什麼」,由 `shotFraming(ch, form, pose)` 合成。收成表的理由:三種姿態的名字本來只住覆核台的 `POSES`(只有 key/label,**語意一個字都沒有**)⇒ 出圖端只畫得出 `_static`,120 格裡的 38 格 moving/heavy **沒有任何工具生得出來** —— 覆核台數得出缺口,卻沒有東西關得掉它。`POSES` MUST 由 `SHOT_POSES` 推導。**`static` MUST NOT 宣告 framing 覆寫**(沿用 `FORM_POSE` 那一份 ⇒ 既有靜止稿的提示詞逐位元不變);`moving`/`heavy` 的 framing MUST **逐 medium 分開**(「奔跑」與「高速掠過」在地面與空中是兩件事,共用一句會把飛行體畫成在地上跑)。**設計錨 `refShotOf` 分兩級**:①已通過的入庫圖(覆核意見「機體仿照移動那張的外觀重繪此動作」的字面要求)②本輪剛畫、尚未覆核的那張;**被判退的那張永遠不當錨**(指向它 = 把錯誤原樣複製一遍);兩級都沒有 ⇒ 同一台機體的待畫張數串在**同一段對話**裡(§5.0.1 MUST 3 的變形者成對規則是這一條的特例)。其餘兩縫:前者是使用者定案「變形者的飛行型態要另外下 prompt 說明是飛行姿態」的唯一落點,`imagePrompt(kind, ch, form)` 取 `zh`、`tools/ai3d/prompt.mjs masterPrompt` 取 `en`/`framing`,中英同放一格是刻意的(同一件事的兩種寫法,拆成兩檔的症狀是「工具畫的是飛行姿態、人照著覆核台複製的那份不是」,兩邊都不報錯);**飛行取景 MUST NOT 留著 `standing`** —— 舊制只說「零件重排」(在地上也成立)而取景還寫著站姿,六台變形者的飛行稿因此全被判「需要是飛行姿態」。**關鍵詞串**畫哪一型就只帶哪一層原型(那邊沒有標籤,兩層一起送 = 同一張圖被要求長成兩種東西);長敘述(`masterPrompt` 的 Design brief)相反,**兩層都送** —— 它有「飛行型原型 / 地面型原型」標籤分得開,而兩型本來就共用同一組零件,而**「有沒有這個型態」MUST 推導**(`formPose` 查該型的原型層在不在),MUST NOT 比對機種字串。後者答「這一欄外觀描述子這台機體用不用得到」:`frame`/`body` 只有 `buildDrone` 讀、`wing` 只有 `buildFixedWing` 讀 ⇒ 擬態翼無人機身上那幾欄是**換機種留下的殘值**,舊制照樣翻成名詞送進生圖(t07 翼龍被寫成「共軸雙旋翼、球形艙」、t08 東亞龍「六旋翼、厚板機身」、m04 獵鷹「共軸雙旋翼、方艙機身」—— 使用者逐一手刪才重下 prompt),而同一個殘欄還讓那三台各多排一張**沒有 rig 節點掛得上去**的旋翼臂切圖(`auditCoverage` 只驗反方向,抓不到)。三個消費端(`visualWords`/`chassisSpec`/`DRAW_SLOTS.rotor_arm`)MUST 全吃這一支。⑨**離線 2D 生圖管線是 `protoOf` 的第二個消費端**(`tools/ai3d/prompt.mjs` 的 `protoBrief()`,`masterPrompt`/`slotPromptNoRef` 同吃):設計敘述 MUST 走這一個縫,MUST NOT 回頭讀舊制 `lore.js` 的自由字串 `proto` —— 那個欄位在 ③ 已經結構化,讀到的是 `undefined` → 空字串 → 被 `filter(Boolean)` 濾掉 ⇒ 提示詞裡**最權威的那一段整段消失**,而且沒有任何錯誤訊息,只表現成「生出來的圖跟設定對不上」(2026-08-05 實測:2026-08-03 那批 masters 的帳本 prompt 都有 `Design brief` 那一行,同一支函式當天產出的已經沒有)。規格書 `docs/codex_format.md`;稽核 `audit_codex.mjs`(90 項)|
| 在地文字語料(招牌上**寫什麼**)| `vernacular.js`(`TEXT_KINDS`/`classifyOsm()`/`harvestOsm()`/`pickName()`/`pickRef()`/`spineOf()`/`SIGN_CLASSES`/`signCopy()`/`textW()`/`LOCALE_LEX`)+ 離線底本 `venueText.js`(`tools/bake_venue_text.mjs` 產出)| 2026-08-03 使用者需求「收集地名/路名/商家/宗教/交通/政府機關/水文地形的圖資文字並建庫,生成招牌/路標/佈告欄/看板/解說牌時寫上去」。方法來源 sakura-crossing,已抽成 skill `.claude/skills/local-vernacular-signage/`。**與世界文字的分工**:本檔管「寫什麼」(純字串),`worldtext.js` 管「怎麼畫」,`biomes.js` 管「掛在哪裡」——三者各一份。①**本檔零 import**(同 `rng.js`/`ctrlmode.js`):語料要同時被瀏覽器、離線烘焙、稽核吃到 —— import 了 three/DOM,那兩支就只能各抄一份分類規則。②**取名規則只有 `pickName` 一份**,`worldtext.resolveName` 只是替它補上設定頁語言預設值的薄殼;主名 MUST 保留**在地文字**,`name:en` 只當拉丁副名。③日常副行 MUST 只收**自由文字**欄位 —— 列舉值(`religion=taoist`/`waterway=river`/`place=neighbourhood`)是 OSM 的英文識別字不是人看的字,直接上牌 = 廟埕告示寫著 taoist;取不到退回 `LOCALE_LEX`,連詞表都沒有(`und`)就留白。④**地名主幹 `spineOf`**:滑窗 MUST 只在連續 CJK 段內滑(先刪非 CJK 再滑會生出原名裡不存在的字串,首烤實測 spine=`路巷`、spineHits=0)、2 與 3 字窗都要(假名地名 `ひばり` 需要 3 字)、含地物通名(路/街/町/山/駅…)的窗一律丟。⑤**一鎮一家**:`signCopy` 的去重帳全世界**一本**,合成名與詞表保底 MUST 一併入帳;連鎖是具名例外且 MUST 只認 `brand` —— `operator` 太鬆(公車站牌都有),用它當旗標等於整批繞過去重帳。⑥每次挑字**固定消耗 3 枚**亂數且走**專屬 seed**。稽核 `audit_vernacular.mjs`(287 項)|
| 異常狀態致盲白幕 | `data.js CC_FLASH` + `ccFlashAlpha()`/`ccFlashDur()`;觸發 `game.js _blindFlash()` | 純表現層(狀態效果仍伺服器結算):光學/電子系狀態(emp/conf/stun)**上升沿**觸發,白幕長度固定 = `ccFlashDur()`,MUST NOT 隨狀態剩餘秒數延長;強度表 MUST NOT 收物理系(slow/bleed/mark);逐幀曲線唯一驅動 ⇒ `.cc-flash` MUST NOT 掛 CSS transition/animation |
| 道路路基整平 | `terrain.js gradeRoadBeds()`(呼叫端 = `buildBiomes` roadInput 定案後唯一一處) | 一般道路(非橋/非結構隧道/非步道)乾地走廊橫向整成**切填平台**(陡橫坡一邊懸空一邊陷地形的解;2026-07-31)。四紀律:填方超過 `fillMax` 漸退、水域/沼澤節點不動、隧道開挖足跡(`carvedNodes`)優先、每節點認**最近的那條路**(髮夾彎成台階,MUST NOT 混平均);全深帶 = 塗裝寬 + 半格(雙線性內插吃得到帶外節點);零 rnd、floors 修改前整批取樣。稽核 `audit_road_bed.mjs` |
| 地形坡度移動 | `data.js SLOPE` + `slopeDeg()`/`slopeMoveF()`/`slopeBlocked()`;量測 `game.js _slopeDegAlong()` | 平緩帶 `EASE_DEG` = 兵線坡度限制 `MAPGEO.MAX_ROAD_GRADE_DEG`、阻擋角 = ×`BLOCK_F` 推導,兩者 MUST NOT 手寫(設計語意:**兵線走廊恆全速**,離開大路爬野山才減速)。坡度一律量**裸地形 `heightAt`**,MUST NOT 改量站立面 `_surf`(上橋引道會被 deckAt 捕捉成假峭壁);速度倍率吃固定前瞻 `PROBE_M`,MUST NOT 拿當幀位移(手感隨幀率浮動);三條豁免 = 飛行/騰空、人造鋪面(`STRUCT_M`)、零位移;**下坡一律不擋**、「爬不上去」MUST 由 `slopeBlocked` 表達而非倍率 0 |
| 視野鎖定 | `data.js VIEW_LOCK`/`viewLockStep()` + `game.js _tickViewLock()`(目標解析 `_coneAcquire()`、瞄準點 `_entAimPoint()`) | 觸控 ZR **按一次 = 輪流切換前方視野內看見的下一個敵人**、按住 = 視野收斂到該目標(2026-08-02 使用者定案)。取景 = **玩家真的看得見的那一塊**:一般模式 = 畫面矩形、狙擊模式 = `SCOPE`/`scopeRvmin()` 那個鏡圈正圓(遮罩黑掉的地方 MUST NOT 鎖得到;鏡圈半徑是 CSS `--scope-r` 與判定的**同一份**,MUST NOT 讀 CSS 字串或另寫縮圈式)。狙擊 FOV 本來就吃進相機投影矩陣 ⇒ MUST NOT 為狙擊模式另寫一份角度縮放;兩種取景一律正規化成偏離度(1 = 取景邊界),`EDGE`/`DROP` 因此共用同一把尺。名冊排序 MUST 是**畫面由左至右**(依「離準星遠近」排 ⇒ 鎖定收斂後每切一次就重排,第三個以後永遠輪不到);輪替錨點 `_vlockPrev` MUST **跨放開保留**(這是按住型鈕,放開就清 = 每次都從頭鎖同一個人),陣亡/離場才清。**純客戶端視角輔助**(伺服器不參與,不涉 A1);每幀轉角只准經 `viewLockStep`(逼近係數與角速度上限取小者,MUST NOT 手寫 W/EASE)、套用只准經 `_applyLook`。**後座力刻意不抵銷**(使用者指定):相機角 = 基準角 + `recoil` + 震動,鎖定只拉基準角 —— 在 `_tickViewLock` 動 `camera.rotation`/`lookAt`/扣 `recoil` 都會把上踢無聲吃掉。目標解析與瞄準點各只有一份(`_coneAcquire` 只放寬參數、`_entAimPoint` 兩端同吃),MUST NOT 另寫掃描 |
| 觀戰相機 | `data.js SPEC_CAM`(視角序 `VIEWS`/名稱 `NAMES`/係數)+ `specViewNext()`/`camSmoothF()`/`camAngleStep()`/`wrapPi()`;消費端 `game.js _updateSpectator()`(相機)/`_specSetView()`(視角唯一寫入點)/`_specCycleView()`(F・十字鍵左唯一出口)/`_specFollow()`(換人)/`_specHud()`(玩家資訊) | 2026-08-02 使用者定案:①**上帝視角加入下降操作**(Space 升 / C・**Ctrl** 降 —— 與飛行機體同一組鍵,MUST NOT 在觸控層另寫垂直位移;降到站立面上方 `FLOOR_M` 停住,鑽進地形只會看到黑畫面);②**玩家視角三選一**(第一人稱 / 第三人稱跟隨 / 第三人稱自由),與上帝視角合成**四態循環**,序只有 `SPEC_CAM.VIEWS` 一份(鍵盤 F 與觸控十字鍵左同吃 ⇒ MUST NOT 退回舊的「上帝 ⇄ 玩家」二態切換)。**「運鏡不晃」的全部本錢在 `camSmoothF` 的幀率無關性**(`1 − e^(−k·dt)`;退回逐幀固定係數不會報錯,只會讓 30fps 比 120fps 晃得多)+ 偏航走 `camAngleStep` 最短路徑 + 超過 `SNAP_M` 的瞬移直接貼上(換人/重生 MUST NOT 拉出橫越全圖的長鏡頭)。**只有第一人稱藏機體**(第三人稱藏了就只剩空鏡頭);第三人稱的距離/注視點一律以 `heroTargetH` 為尺,相機抬高由俯仰推導(MUST NOT 另立抬高常數,兩份參數會打架)。玩家資訊面板與交戰**共用同一塊 `.hud-self`**(`_specHud()` 回傳形狀 = `_weaponHud()`),欄位一律照抄快照(伺服器 `_serializeEnt` 的 `o.act` 區塊本來就發 `$`/`up`/`mp`/`ab`/`cds`)—— 觀戰端 MUST NOT 自算任何一項(A1);算不出來的(別人的彈夾/爬升動力/機動 CD)一律回 null 顯示「—」,MUST NOT 拿本地值假裝。八軌軌名吃 `ECON.UPGRADES` 同一份表。③**第一人稱與第三人稱跟隨 MUST 剛體貼合機體**(2026-08-02 使用者定案「觀戰時第一人稱視角和第三人稱跟隨視角應該要跟著機體」;名冊 `SPEC_CAM.LOCK_VIEWS` + 判定 `specViewLocked()` 單一縫)—— 機體位置本來就已由 `_updateEnts` 逐幀插值,再套一次 `POS_K` 就是**雙重平滑**:穩態落後 = 速度 ÷ `POS_K`(42m/s 實測差 6.66m)⇒ 第一人稱看到自己的機體跑在前面、第三人稱跟隨的機體被推到畫面邊緣。第三人稱**自由**刻意維持平滑(那是觀戰者自己繞著看的鏡頭)。④**觀戰操作說明常駐畫面**(`#specHelp`;文字唯一真相 = `help.js SPEC_CONTROLS`,選單那一長串由它 join 推導 ⇒ 面板與選單 MUST NOT 各寫一份)—— 版位就是 `body.spectating` 收起角色數據後空出來的那一塊,CSS MUST 給 `max-height`(跟上玩家時它與完整面板同框,沒有上限會把血條推出畫面)。稽核 `audit_spectator_cam.mjs`(123 項) |
| 蓄力跳水平移速 | `data.js CJUMP.AIR_SPD_F` | 兩個消費端 MUST 同吃 —— 起跳彈射初速(`_chargeJump`)+ 騰空(`_lowG`)操縱移速;垂直項(`CJUMP.V`/`GRAV_F`)MUST NOT 吃(否則跳高/滯空一起變,滿蓄頂點會撞 `AA_MIN_ALT` 前提) |
| 世界高度上限(遊戲最高高度 / 物件最高高度)| `data.js WORLD_H` + `objHeightMax()`/`objScaleCap()`/`objScaleFit()`/`worldCeilY()`;高程統計 `terrain.js` 的 `avgH`(與 `minH`/`maxH` 同一組取樣面);客戶端唯一取值處 `game.js _ceilY()`(三個消費端:飛行夾制 / 上帝視角 / 觀戰起始高度);物件端 `biomes.js OVER.bldCap` + 四個縮放夾制點(神木群落 / 邊界神木牆 / 巨岩露頭 / 地標)| 2026-08-08 使用者定案兩句:①「遊戲最高高度 = max(平均海拔 + 4 倍砲塔高度, 最高海拔 + 2.5 倍砲塔高度)」②「所有物件的最高高度限定 2 倍砲塔高度」。**2026-08-09 使用者定案「提高物件高度上限」⇒ 係數整組抬高成 `OBJ_F 4 / CEIL_PEAK_F 4.5 / CEIL_AVG_F 6`(物件 104m、峰頂餘裕 117m、平原餘裕 156m)**,②那一句的「2 倍」自此**作廢**(規則形狀不變,只換倍數)。改的理由是一個**兩個 PR 各自綠、合起來壞**的語意衝突:2 倍上限把建物壓到 52m,而 `biomes.js` 手寫的「高層商辦」門檻是 55m(`MASS.MIN_H`)/ 退縮頂塔 `b.h > 55` / 第二層退縮 `b.h > 100` / 屋頂天線 `b.h > 60` ⇒ **那四條結構性地永遠不成立**:整棟量體庫節點(`building/mass_a`·`mass_b`,兩輪 img→3D 的產出)一顆都擺不出去、天際線的「婚禮蛋糕」剪影整個消失,而 `intake_parts` 245 / `audit_siteplan` 197(連 `--break-mass` 反向驗證都照樣紅)/ 對照台孤兒數 0 **全部是綠的**,`npm test`·`npm run bal` 也全綠(那兩支不模型化地物)。⇒ 守門線補在稽核 Ⅲ:**吃建物高度的門檻 MUST 全部 < `objHeightMax()`**(門檻**從 biomes 原文抽**,之後有人再加一條 `b.h > N` 自動跟著驗),反向驗證 `--break-cap`(把上限調回 2 倍 ⇒ 紅字並列出構不到的 55 / 60 / 100m)。**第三個係數關係 MUST 一起看**:`CEIL_AVG_F > CEIL_PEAK_F` 是「取 max 的兩端各自勝出」(下面 ③)的**前提** —— 地表恆 ≤ 最高海拔 ⇒ 平均項的係數若不大於峰頂項,平坦市區那一項**永遠贏不了**,規則 ③ 當場退化成單一項,而畫面上只表現成「天花板好像變低了」。①**尺只有一把 = `TARGET_H.tower`**:三個係數全是它的倍數 ⇒ 砲塔量體一改,天花板與物件上限一起跟著漂;手寫 52 / 104 / 65 的下場是改了塔高之後那三個數字悄悄與塔脫鉤,而畫面上只表現成「這張圖的樓好像比塔高一點」。②**兩條規則是同一個設計的兩半**:天花板到**任一點地表**的餘裕最少是 `CEIL_PEAK_F × 塔高`(地表恆 ≤ 最高海拔),而物件上限是 `OBJ_F × 塔高` ⇒ 只要 `CEIL_PEAK_F > OBJ_F`,**物件恆構不到天花板**(結構保證,不是校準);把 `OBJ_F` 調到 ≥ `CEIL_PEAK_F` 就會生出「山頂上的建物頂端頂到飛行天花板」那種穿模。③**取 max 不是 min**:平坦市區由平均海拔那一項勝出、落差數百公尺的峽谷由最高海拔那一項接手 —— 天花板 MUST 是**全圖一個值**,谷底那半若吃平均海拔就會被關在山谷裡。④**上限一律夾縮放,MUST NOT 事後截幾何**(`objScaleCap`):截幾何 = 碰撞柱/淨空/冠幅/攀爬設施與看得見的量體分家(原則 4);夾縮放則整株等比縮小,落底、footprint、緊密判定全部自動跟著對。夾制**不消耗亂數** ⇒ 佈局序列逐位元不變(§2.3),且 MUST 夾在該物件的第一個消費端之前(神木晚一步夾 = 「冠幅按大株算、樹身按小株長」)。⑤**分布版 `objScaleFit` 不是第二條規則**(壓縮率 = `objScaleCap` 套在該處最大縮放上的比值,上限一改兩支同步走):硬夾只認得手上這一株,**抽得到的每一個值都超標時就會全部夾成同一個數字** —— 神木公稱高 72~110m、最小縮放 0.73 ⇒ 硬夾之後整片森林**每一株一樣高**,而「同種群聚、株高各異」是這套群落既有的設計(樹冠羞避的縮冠量正是從株高變異來的);巨岩露頭的「核心最大、往外遞減」同理。⑥**地標的標稱高 MUST 實測**(`Box3` 量未縮放的群組)而不是讀 `LANDMARK_COL[].h` —— 那一欄是手寫的**擋彈**高度,對細長尖頂/天線常低報數公尺 ⇒ 拿它當分母會讓真正的尖頂越過上限、碰撞柱卻收在上限之下 = 看得到的尖頂打不到(同一族病灶已在 `ty` 屋頂實測那段記過一次)。⑦**位置本就客戶端權威**(同 FLIGHT 全族)⇒ 天花板只住客戶端物理,伺服器 MUST NOT 再驗一次(A1 的另一半);bot / NPC 直升機 / 極音速飛彈沒有那道夾制,靠的是**各自的高度本來就低於最小餘裕**(bot 巡航 52m / 直升機 13m / 飛彈頂點 93m < 117 或 156)—— 這個前提由稽核 Ⅵ 守門,MUST NOT 改成「在伺服器把腳本航線半路截斷」(那會讓兩端插不出同一條拋物線,原則 10)。⑧飛行夾制**兩道取嚴者**:既有的「離站立面 320m」問的是「離腳下多高」、天花板問的是「離海平面多高」,刻意都留著。⑨取不到地形統計(程序生成備援)⇒ `_ceilY` 回 `Infinity` = 不設限,MUST NOT 變成 0 把玩家壓在地上(原則 6)。稽核 `audit_world_height.mjs`(49 項)|
| 機體高度/半徑 | `data.js SOLDIER_H/HERO_SIZE/heroTargetH()/TARGET_H/hitH()` + `hitR()`(`HERO_HIT_R`/`TARGET_R`) | 同一把尺餵渲染縮放與伺服器命中量體;**爆風/貫穿/射程閘門一律量到命中量體最近點**(水平 `hitR(t)` + 垂直帶 `_bodyDy`,兩軸同一把尺 —— `_blast` 只做垂直、水平仍量中心 = 榴彈直擊 20m 主堡牆面只吃約五成超壓);貫穿半徑 = `lanceR(def) + hitR(t)`;`game.js COLLIDER` MUST 由 hitR/hitH 推導,但鍵集 MUST NOT 隨 `TARGET_R` 擴張 |
| 攀爬路線 | `climb.js`(規劃/抓握索引/設施幾何/`attachFaces()` 正面候選) | 詳見 A31 |
| 巨岩表面落點 | `biomes.js rockProbe(g)`(`wallR`/`slope`/`topAt` 射線實測) | 貼壁(峭壁樹/岩菇/侵蝕溝/棧道)與頂面(石屋/疊石/鳥巢台/電塔)落點一律**實測真幾何**;多面體小面內縮 4~5%、疊層收分、崩落塊撐大外廓 ⇒ 手寫剖面公式(側壁橢圓 × dome/taper)一律算不準,MUST NOT 復辟 |
| 圓形腳印落底 | `biomes.js sinkBaseY()` | 神木/邊界巨岩/巨岩/地標一律「中心 + 腳印周圈取最低」,寧可陷入山坡不懸空;MUST NOT 只取中心高度(下坡側整片浮空) |
| 連線機制 | `netmode.js`(模式/網址/`wsUrl()`)+ `net.js makeNet()` | `main.js` MUST NOT 自己 `new Net()`/看 `location.host`/寫單機文案分支;鈕面真相 = `LINK_MODES` |
| 操作方式(輸入裝置) | `ctrlmode.js`(`CTRL_MODES`/`ctrlMode()`/`ctrlPref()`/`setRoomCtrlMode()`/`ctrlScheme()`/`deviceScheme()`)+ 房主 UI `mobile.js renderCtrlModeRow()` + 設定 UI `renderCtrlSettings()` | 三選一(不限定/限定滑鼠鍵盤/限定搖桿)是**房間設定,由房主選擇**(2026-07-31 使用者定案):值住 `room.config.ctrl`(伺服器 `setRoomConfig` 只認 hostId、合法值取自 `CTRL_MODES` 同一份),客戶端 `_room` 的**唯一寫入路徑 = `sync` 廣播** ⇒ 房主按下只准上行、MUST NOT 先斬後奏。生效值合流只有 `ctrlMode()` = `房主定案 ?? 我的預設` 一處,MUST NOT 在別處自己寫 `room \|\| pref`;`ctrlPref()`(我的預設)只在開房時上行。**「目前操控」的選擇併入設定頁**:`setCtrlScheme` 只受理「不限定」—— 這就是「遊戲中不可變更,除非戰區選不限定」的唯一落點,消費端 MUST NOT 另判一次。裝置判定(`maxTouchPoints`/`pointer:coarse`/短邊)只有 `deviceScheme()` 一份,`mobile.js isTouchUI()` MUST 只是轉呼;搖桿層建/毀只住 `game.js _applyCtrlScheme`;`main.js` 的 `TOUCH_UI` MUST 是函式(常數 = 切換後文字停在舊版)|
| 戰場選單開關(ESC / ☰) | `game.js _escMenu()` | 2026-08-01 使用者定案「**遊戲中隨時都可以 esc**」。三個來源(鍵盤 keydown、指標解鎖 `_onPlc`、觸控 ☰ `_cmd('menu')`)MUST 全走這一支,MUST NOT 在任一端另判「這個狀態該不該開」—— 條件散出去就是「某個時刻按了沒反應」(前科:未鎖定指標的交戰玩家、重生後還沒點畫面)。只有兩條規則:商店開著先收商店(疊層逐層退出)、其餘一律**切換**選單;唯一不受理的是 `_gameOver`(結束頁獨佔,與 `_setPaused` 同判)。**`_onPlc` 同樣 MUST NOT 用 `dead` 當門檻**(2026-08-02 使用者需求「重生倒數計時也要可以按 ESC」):陣亡頁是 `pointer-events: none` ⇒ 玩家隨手點畫面就重新鎖上指標,那顆 ESC 的 keydown 被瀏覽器吃掉、只剩解鎖這條路,`!this.dead` 會把它一起擋掉 = 「倒數中按 ESC 沒反應」。陣亡過場那次 `exitPointerLock` 改以**我方主動戳記** `_plcSelf` 略過,且 MUST 只在**真的鎖著**時打(沒鎖就不會有 pointerlockchange,戳記會留下來吃掉玩家下一顆真 ESC)。ESC 的 keydown MUST 排在 `_onKey` 的 `paused` 早退**之前**(選單開著時其餘輸入全凍結,這顆是收回選單的鑰匙);鎖定中那顆 ESC 瀏覽器不派發 ⇒ `_onPlc` 那條路 MUST 留著,兩條路的重入由 `ESC_GAP_S` 去彈跳窗擋(部分瀏覽器解鎖後**還會**補送 keydown = 開了又立刻關)|
| GUI 說明 / 懸浮提示 | `tip.js`(`installTips()`/`tipHTML()`/`attachTip()` + 單一氣泡)+ 文字表 `help.js UI_TIPS` + 取字 `uiTip()` | 2026-07-31 使用者定案:**遊戲內常駐說明一律改成 ⓘ 懸浮提示,並彙整到說明分頁**。說明分頁的「介面」類由 `UI_TIPS` **推導**(`Object.values`)⇒ 文字 MUST NOT 抄第二份;`index.html` 靜態標記只准寫 `data-tipkey`,文字由 `main.js syncTips()` 灌。MUST NOT 退回 `title=`(觸控無 hover)、MUST NOT 另寫第二套氣泡、MUST NOT 逐節點綁事件(重繪即失效 ⇒ 一律 document 委派);觸控一律**長按**才出提示(短按 MUST 原樣放行,否則掛了提示的選項鈕整顆失效)|
| 分段按鈕樣式 | `style.css` `.seg` > `.segb`(+ `.seg-lg`/`.seg-sm` 尺寸變體) | 2026-07-31 使用者定案「按鍵風格統一」:凡「一組互斥選項」的控件一律掛這一套(戰場選單分頁 / 說明類別 / 操作方式 / 目前操控 / 陀螺來源 / **電腦難度** / 圖鑑陣營切換)。消費端 MUST 只掛 class,MUST NOT 自寫 padding 與選取態配色;電腦難度 MUST NOT 退回 `<select>`。`.btn` 家族(準備/開戰/離開)刻意不併入 —— 那是動作不是選項 |
| NPC / 建築 / 機種圖示 | `npcicon.js` `NPC_ICONS` + `npcIconHTML()`、`KIND_ICONS` + `kindIconHTML()`;頭像角標標記 `main.js charAvatarHTML()` | 平塗 24×24 SVG 吃 `currentColor`(選取態/陣營色自動跟著翻);名冊真相仍是 `UNITS`/`THIRD.COMP`,缺鍵回退問號圖。MUST NOT 改成 3D 截圖資產(96px 格子只剩色塊,且吃不到 currentColor)。**機種圖示**(2026-08-02 使用者定案「設計無人機/機甲/變形者的圖示,標示在頭像旁邊」)與 NPC 圖示同住一支 —— 同一套平塗風格、同一條「缺鍵回退問號」保底規則,分兩支就會長出兩份;名冊真相是 `CHARACTERS[ch].kind`,取值 MUST 走 `charKind()`(混編後陣營 ≠ 機種)。四處頭像(選角牆 / 放大視窗選角牆 / 劇情主駕小卡 / 角色卡立繪標籤)MUST 全走 `charAvatarHTML()`,MUST NOT 自己拼 `<img>`(漏一處 = 那面牆沒有角標,畫面上只像是漏渲染)|
| 賽璐璐明暗階梯(ramp)| `toon.js RAMPS` + `toonGradient(bands)` | 2/3/4/soft 四組,依「這塊面積在畫面上要表現多少層次」選(淺色大面積走 `soft`、大型結構走 4、小件深色走 2)。**3 階 MUST 逐位元 [102,182,255]**(改了整個場景重新上色);每組暗階 MUST ≥ 102(A14/#INC-106:低於此深色件疊 `cool` 會塌成全黑);ramp 的 `DataTexture` MUST **只在 toon.js** 建構,呼叫端一律經 `toonMat`/`envMat` 的 `opts.bands`(散出去 = 同一個場景兩套明暗規則,而畫面上只表現成「某些物件的陰影邊界跟別人對不上」)|
| 地表屬性場 | `field.js makeField()`/`makeToneLadder()`/`bakeFieldTexture()`/`coarseHash()`(亂數走 `rng.js mulberry32`)| 回答「這一點比周圍多還是少」的低頻無方向性場:地表色階梯(`terrain.js` 無影像路徑)與將來的風化/苔蘚密度同吃。三條紀律:①MUST 是**加權平均** `s / max(W_MIN, w)`,MUST NOT 加總(橢圓一多會飽和成常數,比沒有場更糟);②分母下限不可省(空白處 w→0 會爆成 ±∞);③種子由呼叫端給(戰場中心),每個橢圓固定消耗 6 枚亂數(§2.3)。**色階門檻 MUST 取該場地自己的分位數**,MUST NOT 手寫固定門檻 —— 場的分布隨種子漂,固定門檻實測 27 場地 × 三種隊制最壞讓單一色階佔到 51.3%(iguazu L3),那就退回「88% 的坡面同一個顏色」那個病灶本身 |
| 畫面表現旋鈕(art-direction 拉桿)| `visualPrefs.js VISUAL_KNOBS`/`visualPref()`/`setVisualPref()`/`onVisualChange()`;消費端 `toon.js`(陰影偏色 + 風化幅度)/`postfx.js`(勾線強度)/`main.js renderVisualSettings`/`matsample.js` | 2026-08-03。`docs/visual_upgrade_plan.md` 有幾項卡在「**需要美術方向確認**」(P1-B 最典型:它改掉每一台機甲的暗面色相)。這種東西 MUST NOT 由 commit 定案 —— 一個人在自己螢幕上調到滿意,換一台螢幕就不對了 ⇒ 做成**設定頁拉桿 + 即時樣品**交給使用者。四條:①**旋鈕表只有一份**(本檔),拉桿清單 MUST 由它推導,MUST NOT 在 `main.js` 再寫一次(兩份清單分家的症狀只是「某一根拉桿不見了」,不報錯);②**預設值 = 交付定案值**,而卡在「需美術確認」的兩項定案值就是 **0 = 逐位元同舊制**(否則「加了個設定」= 偷偷改了所有人的畫面);③改值 MUST 只動**共享 uniform**,MUST NOT 重建材質(材質早發到 GPU 了,拖拉桿會變幻燈片);④**樣品 MUST 走真品材質與真品後製管線**(`toonMat`/`envMat`/`Pipeline`),MUST NOT 另畫一份 2D 近似 —— 那就是同一個場景兩套明暗規則,症狀是「樣品調好了、進戰場不是那樣」。本檔**零 import**(同 `rng.js` 的理由:離線稽核要直接執行它驗預設與夾制)。純表現層偏好,與 `lowPower` 同層級:只住 localStorage、不上行、不進判定。稽核 `audit_visual_prefs.mjs`(98 項)|
| 景深模糊(狙擊模式的遠景散焦)| `data.js DOF`+`combatReachM()`(交戰距離上界,推導)+`dofNearM()`/`dofFarM()`+`dofAimBlend()`(進鏡程度);pass `postfx.js` 的 `_dofMaterial`/`dofTaps`/`setDof`(距離唯一寫入點)/`setDofBlend`/`_pushDofA`(`uDofA` 唯一寫入點);消費端 = `game.js` 建管線後那一行 + render 前逐幀那一行 + `matsample.js`(樣品尺度)+ `tools/shot_scene.mjs`;拉桿 `visualPrefs.js dof` | 2026-08-09 使用者定案「加入遠的物件隨距離景深模糊的效果」。**這是這批唯一「加成本」的效果**(後製多一個全螢幕 pass;同日已就「不省效能」達成共識後才做)⇒ 三個關法任一成立即**整個 pass 退出鏈**(`?dof=0` / 呼叫端沒餵距離 / 拉桿 0%),MUST NOT 改成「跑一個乘 0 的 pass」。①**兩個轉折點推導不手寫**,係數是使用者提的 1.5×/2×,但**錨不是他原話的「狙擊模式可視範圍」** —— `COMBAT_SCALE` 把 sight 與射程一起砍半而**地圖邊長沒砍**(REAL_SCALE 是另一條路)⇒ 現役狙擊可視只有 192~216m,而**最遠交戰距離 304m 比它還遠**(重武器射程上限 = `sight × AIM_SIGHT_MULT × RANGE_SIGHT_F`,再乘高度制空與 `RANGE_TOL`)⇒ 照原話取 1.5 × 216 = 324m 會讓起糊點落進交戰距離**裡面**。故錨在 `combatReachM()`:**「打得到的東西恆為全清晰」因此是結構保證而不是校準**(456m 起糊 / 608m 全糊,餘裕 1.50×),糊掉打得到的目標就不再是表現層改動而是玩法改動(原則 4),而畫面上只表現成「這場好像比較難瞄」。`combatReachM` 的取樣面 MUST 涵蓋塔射程 / 32 角 × 兩槽位 × 四階武器(× `altRangeMax × RANGE_TOL` = 伺服器真的會收下的誠實界)/ 招式施放距離 / 大招載具最長航程 —— 漏一條就是那一種目標被糊掉。②**錨也 MUST NOT 取相機 far 平面**(= 地圖邊長 × 2):那隨隊制變(L1/L2/L3 邊長 800/1000/1200),同一把武器在大地圖上要更遠才開始糊,而「多遠算遠」是戰鬥尺度不是地圖尺寸的性質。③**全糊那一圈刻意 = 使用者原本要「不顯示」的那一圈**:日後真做距離剔除時,物件消失的邊界已經在全糊帶裡 ⇒ 憑空消失的那一下被模糊蓋住(同一組係數不是巧合)。④**順序 MUST 排在勾線之後**:勾線讀的是**深度**,先糊後勾 = 深度仍銳利 ⇒ 在糊掉的色塊上畫出清楚的黑線。⑤**鄰居取樣 MUST 過焦外閘**(`step(coc*0.5, ct)`):不擋就是把近處清晰物件抹進遠景 = 前景剪影外一圈光暈,而 FPV 的座艙/武器占畫面很大一塊。⑥取樣點是**黃金角螺旋**(半徑開根號 = 面積均勻),單一圓環在低取樣數下糊成甜甜圈邊;取樣數低功耗折半,**展開在 shader 原始碼裡**(兩者是不同的程式不是分支)。⑦**最大半徑是螢幕高度的比例不是像素**:寫成像素的話高解析度上相對變窄、RES_GOV 一降階又相對變寬(同一根拉桿在不同裝置上是不同的效果)。⑧**天空刻意不早退**(與勾線相反):天空深度就是 far ⇒ coc 恆 1,而它是平滑漸層 ⇒ 糊它看不出差別也幾乎沒成本,特判反而在天際線切出「糊的地面 / 清晰的天空」硬邊。⑨**距離只有 `data.js` 一份**,postfx 不手寫公尺數、game.js 直接轉呼;**每一個 `Pipeline` 消費端都 MUST 呼叫 `setDof`** —— 沒餵 = `_dofRange` 恆為 null = 這一 pass 永遠不掛,而每一張圖與每一行讀數都照樣正常(`shot_scene` 就是這樣差點永遠拍不到交付版本的樣子)。⑨-b **只在狙擊模式**(2026-08-09 使用者補充「遠景景深模糊只有在狙擊模式」):一般視角 `_dofBlend = 0` ⇒ **整個 pass 退出鏈**,逐位元不付這一 pass 的錢(這也讓它真的便宜 —— 只在進鏡那幾秒跑)。強度 MUST 由**當下的 `camera.fov` 反解**(`dofAimBlend`),MUST NOT 判 `aiming` 布林(進鏡瞬間硬切),更 MUST NOT 自己跑一條淡入 —— 右鍵拉近本來就有一條緩動,第二條時間曲線遲早不同步而症狀只是「模糊比鏡頭慢半拍」;由 fov 反解 ⇒ 兩者**結構上**同一條曲線,連緩動係數都不必共用。呼叫點**恰一處**(相機定案之後、render 之前,與 `updateCelLight` 同一層)—— 散進狀態機的話陣亡/觀戰那幾條路會留著上一幀的值 =「死了畫面還糊著」;觀戰的滾輪縮放也吃 `camera.fov` ⇒ MUST 明確排除。`_dofBlend` **預設 1**(= 不做狙擊閘):那是戰場才有的概念,預設 0 會讓樣品與定場鏡頭組靜靜地什麼都不糊。`uDofA` = 拉桿強度 × 進鏡程度,**唯一寫入點 `_pushDofA`**(兩個來源各寫一次 = 後寫的把前一個蓋掉)。**連帶**:③ 那條「全糊帶蓋住未來剔除邊界」只在狙擊模式成立 —— 之後真做距離剔除時,一般視角的 pop 沒有東西蓋。⑩**樣品 MUST 餵自己那一組尺度**(24m 場景全部落在 456m 的焦內帶 = 拉桿拉了看不出差異 —— 陰影偏色與風化密度各踩過一次的同一個坑),並補一排有輪廓線的背景方塊:焦外帶只剩平坦地面的話,平面在勾線的二階差分恆為 0 ⇒ 那裡一條線都沒有,糊與不糊只差在苔蘚的低頻起伏。稽核 `audit_visual_prefs.mjs` Ⅵ(45 項)|
| 陰影偏色(ramp 層)| `toon.js SHADOW_HUE`/`shadowTintRGB()` + `RAMP_HOOK` 對 `getGradientIrradiance` 的替換 + `_rampTint.mech`/`.env` 兩軌共享 uniform | 2026-08-03(計畫書 P1-B)。舊制 `CEL_COOL` 是**事後**拌 `outgoingLight` 且**只有 `envMat` 開** ⇒ 機甲/英雄/武器的陰影只是「比較暗」不是「有顏色」。改接在 **ramp 查表**上 ⇒ 吃得到每一盞燈、也自動跟著 ramp 的階走(暗階偏得多、亮階不偏)。三條:①**偏色 MUST 亮度中性** —— 色相向量先除以自身 Rec.709 亮度,`luma(tint) ≡ 1` ⇒ 暗階**亮度**逐位元不動;不做正規化就是繞過 A14/#INC-106 的「暗階 ≥ 102」後門,而症狀只是深色件在暗面塌黑;②**機體與環境分兩軌**(`toonMat` → mech、`envMat` → env),MUST NOT 併成一個值(機甲要保住陣營塗裝的色相);③`RAMP_HOOK` 是 three r160 的**原文錨點**,升級 three MUST 重新核對 —— 替換靜默失效的落地保險是 `uCelRampFb` 的等效路徑(原則 6),MUST NOT 拿掉。**2026-08-03 使用者回報「機體陰影、環境陰影調整時看不出差異」的三個成因,全部沒有錯誤訊息**:④**錨點 MUST 指向 `getGradientIrradiance()` 真正的家 = `gradientmap_pars_fragment`**,`lights_toon_pars_fragment` 只是**呼叫**它的地方(計畫書 P1-B 的原句就寫錯了,而它被照著實作)—— 錨錯 chunk ⇒ `RAMP_PATCHED` 恆為 null、每一份材質都走落地保險。chunk 名因此只准有 `RAMP_CHUNK` **一份**,`#include` 指令 MUST 由它推導(兩個名字各寫一次就會再分家一次)。⑤**落地保險的權重 MUST 取同一張 ramp 的階值**(`texture2D(gradientMap, …)`),MUST NOT 退回手寫的 `dot·0.5+0.5` 線性斜坡 —— 斜坡把偏色攤平在整顆球上,實測峰值只剩 11/765,「壞掉時剛好也看不出來」= 等於沒有保險。⑥**拉桿上限 MUST > 1**(`TINT_MAX_A`,與 `VISUAL_KNOBS.shadow*.max` 同值):`SHADOW_HUE` 是**方向**,1.0 只是它自己那個長度,而偏色只乘得到暗階的**直接光**那一項(暗階直接光 = 滿亮的 0.4 倍,旁邊還有一份完全不偏色的環境光在稀釋)⇒ 100% 的實測峰值只有 **+5/255 藍、−2/255 紅**,把它當天花板等於這根拉桿沒有作用。**亮度中性與上限無關**:`luma(1 + (c−1)a) = 1 + (luma(c) − 1)·a` 而 `luma(SHADOW_HUE_N) ≡ 1` ⇒ 任何強度亮度都恰好是 1,上限存在只為了不讓通道被拉到負數,MUST NOT 拿它當「安全亮度」的保險。**2026-08-04 使用者再次回報「機體陰影、環境陰影調整時,展示樣品看不出差異」的兩個成因**(同樣沒有錯誤訊息;逐像素量測方式 = 離線複刻樣品場景的整條像素鏈 ramp → rim/metal/cool → postfx grade → sRGB):⑦**偏色權重是「這一階在 ramp 上有多深」,不是「這一階有多亮」**(`rampFloor()` + GLSL `celRampDepth()`,兩條路徑同吃一份公式):舊制直接拿 `celG` 當權重,而 `celG` 是那一階的**亮度**,`RAMPS` 的暗階 ≥ 102 是 A14/#INC-106 為了「深色件不塌黑」訂的下限 ⇒ **那條保命規則順便把偏色也一起夾掉** —— 三階 ramp 的最暗階就算拉桿拉到底也只吃得到 60% 的偏色,而拉桿說明寫的是「100% = 天光藍本身的濃度」。兩件不同的事被同一個數字綁著。正規化(以**那一組 ramp 自己的**暗階為 0、頂階為 1)之後最暗階吃滿整份偏色(濃度 ×1.67),而「最亮階恆不偏」逐位元不變。基準 `rampFloor` MUST **推導不手寫**且 `bands` MUST 一路傳到 `applyCelPatch`(漏傳 = soft/2/4 階材質拿 3 階的 0.4 去量,症狀是「某些材質的陰影偏色比別人淡」);⑧**樣品的鍵光 MUST 讓暗面真的出現在畫面上**(`matsample.js SUN_DIR`,燈與 `uCelLightDir` 同吃這一個常數):偏色只作用在暗階,而舊制的 (0.4, 0.8, 0.4) 幾乎與視線同向 ⇒ 量到的暗階佔比是**地面 0%(整片壓在最亮階 = 偏色的定義值就是 0)/ 岩塊 0% / 機甲臂 0% / 機甲球 1%** —— 這根拉桿控制的那一階在畫面上幾乎不存在,拉到哪裡都一樣。改側後方鍵光後暗階佔比 = 岩塊 80% / 機甲臂 55% / 機甲球 19%,地面整片落進中間階,峰值差 100% 拉桿 4/255 → 8、300% 11 → 21,「看得出來在動」的畫面比 6% → 23%。`SUN_DIR` 的 **y 分量 MUST < 0.5**(再高地面法線就跳進四階 ramp 的頂階 = 畫面最大的那 47% 退出作用範圍)、**z 分量 MUST < 0**(從相機肩膀上打過去就沒有暗面);覆寫走 `updateCelLight(camera, dirWorld)` 的選用參數,MUST NOT 讓樣品去寫 `_sunDirWorld`(那是本場太陽的唯一寫入點,戰鬥中一開設定就把整場光向換掉且換不回來)。稽核 `audit_visual_prefs.mjs` Ⅱ・Ⅴ |
| 風化屬性場(這一區有多老)| `field.js bakeFieldTexture()`(烤成純資料)+ `toon.js setWeatherField()`/`celWeatherF()`(唯一寫入點與唯一取樣點)+ 安裝端 `terrain.js installWeatherField()` | 2026-08-03(計畫書 P2-A)。舊制 moss/wash 對全場一視同仁 = 沒有「這一區比那一區老」,那正是大面積看起來像貼圖重複的原因。四條:①**MUST NOT 由地形推**(高度 / 離水距離在均勻區域內是常數 —— 那是病灶本身不是解法),一律吃散布橢圓場;②**種子 MUST 與地表色階梯錯開**(`^ WEATHER_SEED_MIX`):兩者鎖在一起 = 顏色深的地方剛好最斑駁 = 兩個訊號看起來只有一個;③**烤成小貼圖再取樣**,MUST NOT 在片段著色器裡跑 26 個橢圓的迴圈;貼圖只准在 `toon.js` 建(與 ramp 的 `DataTexture` 同一條規矩,`field.js` MUST 維持零 three);④乘數 = `1 + (場 − 0.5) × 2 × 強度` ⇒ **拉桿歸零逐位元回舊制**,取不到世界座標的材質(機體)恆回中性;⑤**苔蘚權重 MUST 夾在 [0,1]** —— `celWeatherF()` 最高到 `1 + WEATHER_SPREAD × 拉桿上限`,而 `mix` 在 t > 1 是**外插**(往苔色的反方向推出去):場最高的那幾塊會過飽和,拉桿上半段也只是外插得更遠 = 看不出差異。⑥**設定頁樣品走自己那張場**(`ensurePreviewField`;2026-08-03 使用者回報「風化密度調整時看不出差異」):大廳還沒有戰場 ⇒ 場是 1×1 中性貼圖(恆 0.5 ⇒ 乘數逐位元恆 1,**證明性地**什麼都不會發生),而戰鬥中世界那張場的取樣框是整張圖 ⇒ 樣品那 24m 只取到單一個值。換軌 MUST 只換「哪一張貼圖 + 取樣框」兩個 uniform(取樣規則與強度仍是同一份,同 `_rampTint` 的 mech/env 兩軌);樣品 MUST NOT 去呼叫 `setWeatherField`(那是世界場的唯一寫入點,戰鬥中一開設定就會把整場的風化場換掉、關掉也換不回來)。樣品地面 MUST 帶 `moss`:苔蘚是世界 Y 軸投影 ⇒ 沒有一片正朝上的面,這根拉桿就只剩 `wash` 那 ±7% 在動。稽核 `audit_visual_prefs.mjs` Ⅲ |
| 軟性物質(細勾線 + 隨風飄揚)| `toon.js` 的 `INK_SOFT_A`(倍率)+ `SOFT_KINDS`(分類與擺動參數)+ `WIND`(全場風)+ `stepCelWind()`/`celWindTime()`(時鐘);材質入口 `toonMat`/`envMat` 的 `soft` 選項;讀取端 `postfx.js` 的勾線 pass;消費端 `biomes.js SOFT_BY_VEG_KEY`/`vegSoftKind()`/`vegSpan()`/`flag()`、`siteplan.js CIVIC_PARTS[].sf`、`environment.js makeClouds().step()` | 2026-08-04 使用者定案「不同類型物件有不同線條輪廓的粗細,例如雲朵、芒草、草原、花園、樹葉、旗幟這些軟性的物質的線條會細得多,其他堅硬的物體則依據設定的數值」+「這些軟性物質加入隨風飄揚之類的重複性變化」。①**一個旗標管兩件事**:「這個零件是不是軟的」同時決定勾線多細與會不會被風吹動 —— 拆成兩份名單遲早分家,症狀是「這叢草會飄但線是硬的」(沒有錯誤訊息)。②**細勾線的通道 = 場景 RT 的 alpha**(契約:`alpha ≡ 這一格的勾線門檻倍率`,1 = 硬性):世界的線由 `postfx.js` **一個螢幕空間 pass 蓋全場**(外殼包住整個世界是幾百個 draw call),而螢幕空間天生不認識「這個像素是什麼東西」⇒ 必須有逐像素通道;alpha 是空的(three 對不透明材質一律 `NoBlending` = 直寫、最後一 pass 輸出 1.0、畫布 context 又是 `alpha: false`),而半透明件混合後 alpha 只會被推向 1 = **硬性 = 舊行為**(原則 6)。③**「細」= 抬高門檻,不是縮小取樣半徑**:半徑已經是 1 像素(`INK.THICK`,再小就斷線),而 `|e|` 從邊緣往外遞減 ⇒ 倍率乘進 `smoothstep` 的**輸入**,線帶真的變窄;乘在 `ink` **之後**只會變淡不變細。倍率 MUST 取「這一格 + 四鄰」的**最小值** —— 一條輪廓線同時落在物件側與背景側,只看中心的話背景側那半條仍是硬性粗細(葉叢邊緣一半粗一半細);四次取樣排在 `ae <= EDGE0` 的早退**之後**(soft ≤ 1 ⇒ 硬性跨不過的一定也跨不過)⇒ 平均成本接近零。④**擺動權重 MUST 錨在「整株局部座標」**(`uSoftBase + 頂點高 × uSoftSy`):逐零件各自從 0 起算 = 每顆樹冠繞自己的中心剪切,疊接縫開開合合(A26/A27 的接合完成度 MUST 與擺動無關);連續的位移場才讓樹幹頂與樹冠底拿到**同一個**權重。⑤**空間相位取實例原點**不是逐頂點(逐頂點 = 同一片葉子的兩端各走各的 = 幾何被拉扯變形);**世界風向 MUST 轉進零件局部座標**(`v * mat3(model×instance)`)—— 實例的 `ry` 是亂數,直接拿世界向量當局部向量就是每株各吹各的,那不是風。⑥`span` 一律**推導不手寫**(`vegSpan()` 掃零件包圍盒 / 公設掃合併後幾何):手寫的話加高樹冠之後梢端擺不到滿幅。⑦分類**由既有的 `part.key` 推導**(`seasonColor` 同吃那一欄),`sf` 只是逐零件覆寫(芒花穗/蘆葦/箭竹葉有固定色所以沒有 key,但它們正是使用者點名的「芒草」);MUST NOT 另開一張「哪些會飄」的名單。⑧`turf`(草坪/內場草皮)刻意 `amp: 0` —— 0.5m 厚的鋪面擺起來只會跟旁邊的步道錯開一條縫,它只要細線那一半。⑨**雲的勾線是恆等式**:雲是 `depthWrite: false` 的 sprite,那些像素的深度是天空的 far ⇒ 勾線 pass 第一行就早退(檔頭 ③),雲從來沒有被畫過線 ⇒ 那邊只做漂移;漂移 MUST 吃同一支 `celWindTime()`(自己數 dt 的話暫停一次就與地面錯開)、環繞取模 MUST 先加半個 `WRAP`(JS 的 `%` 對負數回負值 ⇒ 半邊的雲每一圈跳到另一側)。⑩軟性 MUST 進 `customProgramCacheKey`(不進的話 three 共用舊程式 = 那批材質整批沒有擺動也沒有細線)。純表現層:MUST NOT 動碰撞/LOS/通行寬(原則 4),伺服器完全不知道它的存在。稽核 `audit_soft_stroke.mjs`(73 項)|
| 零件級細節抖動 | `xform.js partId()`/`partJitter()`;消費端 `vegPartXform`(植被)/`hazards.js jitterParts`(障礙)/`biomes.js jitterMegalith`(地標) | 2026-08-03(計畫書 P2-B)。規則與振幅語意只有 `partJitter` 一份:`jr` 水平半徑**只增不減**(縮小會拉開「名義上剛好貼合」的接合,`audit_object_joints` 紅過)、`spin` **只給軸心件**(`px = pz = 0`;偏移件的貼合靠特定朝向的頂點)、MUST NOT 抖 `y`/`px`/`pz`/縱向尺寸。`dj = 0` 恆中性 ⇒ 舊呼叫端逐位元不變。**演出半徑 MUST 收在權威碰撞柱內**(原則 4 / A30 家族):兩個新消費端一律**抖完實測水平外廓**(`Box3.setFromObject`),頂出 `HAZARDS[].r` / `MEGALITHS[].col.r` 就把**那一件**退回原樣,MUST NOT 憑感覺給振幅。**建物刻意不吃**:建物的有向盒碰撞就是它自己的足跡(`hw2`/`hd2` 與拉伸幾何同源),沒有餘裕可花 —— 抖了就是「看得見卻打不到」。地標的 `dj` MUST 由**落點**推,MUST NOT 再抽一枚 `rnd()`(會把後面所有佈局的亂數序列整條推移,§2.3)。稽核 `audit_visual_prefs.mjs` Ⅳ + `audit_object_joints.mjs` |
| 世界文字(**唯一**的文字圖層)| `worldtext.js`(`resolveName()`/`resolveRef()`/`canRenderText()`/`SignSheet`/`SIGN_STYLES`/`signAspect()`/`packCells()`)+ 接線 `biomes.js buildWorldSigns()`;語言選項 `visualPrefs.js worldTextLang` | 2026-08-03(計畫書 V-E 的正確版本:**不是「畫招牌」,是把世界本來就有的名字放回去**)。圖資的 tags 一路都留著(`out geom` / `out center tags` 預設帶 tags)⇒ 洞口匾額與橋名牌**一行 Overpass 都不用加**。八條:①**沒有名字就不掛牌**(`resolveName` 回 null),MUST NOT 拿場地名/假名填充(原則 6)——假名比沒有名字更糟;②**缺字偵測**(`canRenderText`:字元寬度 == 私用區字元寬度 = 字型畫不出來)缺超過兩成整串放棄 —— A2 不准加字型檔,一排豆腐框比沒有招牌難看得多;③**全場文字合併成一個 mesh + 一張 atlas**:每塊牌一個材質 = draw call 爆掉(與 `postfx.js` 檔頭「外殼包住整個世界不可行」同一個瓶頸);格數 `SIGN_MAX` **就是額度**,額度是特性不是限制;④**牌面與 atlas 格子同為 4:1**,寬由高推導 —— 為了容納長名字而拉長牌面就會與格子分家 = 字被壓扁(程序貼圖的經典坑),長名字一律縮字級(字級由 `measureText` 決定);⑤**雙面牌 = 兩片背對背的單面四邊形**,MUST NOT 用 `DoubleSide`(背面會變鏡像字);⑥牌面**不透明**(透明件在勾線 pass 的深度緩衝上會變成斑點)、`rim: 0`(掠射角洗白會讓字整片消失);⑦位置與朝向 MUST 取自各構件**已經定案的幾何**(`portals` 那一筆記錄 / `signSpots` 的 `deckAt` / 建物 `generic` / poi 投影),MUST NOT 在掛牌時重算一次橋在哪、洞口朝哪(第二份幾何 = 牌子飄在結構外);⑧**零 `rnd()` 消耗**(§2.3):掛哪些、掛什麼字、朝哪邊全由圖資與座標推導 —— 多抽一枚就把整張圖的植被/建物佈局推移。純表現層(原則 4):MUST NOT 新增碰撞柱或動通行寬,標牌柱刻意不擋路。**改 Overpass 查詢 MUST 同步跳 `geoKey('osmF', N)` 的版本** —— 不跳的話舊快取照樣命中,新標牌在所有「以前開過這張圖」的機器上永遠不出現,而且沒有任何錯誤訊息。**2026-08-03 併入「在地日常文字」**:舊 `signage.js`(第二份圖集 + 第二套逐實例 UV 規則)整支退場 —— 一個世界兩套文字圖層就是「第二份實作即是 bug」(原則 2)。併入後多四條:⑨**語域 = `SIGN_STYLES` 的一列**:前四種(stone/enamel/guide/lightbox)是**構件自己的名字**、後五種(wallsign/billboard/roadsign/notice/scenic)是**語料庫挑字**帶三層文字(主名/日常副行/拉丁副名);一塊牌讀起來是店家還是機關由**欄位結構**決定,不是由字體決定。⑩**牌面長寬比逐語域各自定**(直式招牌 1:5、路標 16:5)⇒ ④ 的「4:1」改成 `signAspect(style)` 推導,固定格網塞不下 ⇒ 走**貨架式裝箱** `packCells`(純算術,離線可直測);裝不下 MUST 記在 `signDropped` **明講**,MUST NOT 靜默截斷。⑪額度改成「格數硬上限 **與** 圖集面積」兩道閘 —— 混合長寬比之後「格數」不再等於「裝得下」。⑫缺字 MUST 驗**整塊牌**(副行也會變豆腐框)。⑧ 的零亂數只約束構件名牌那四種;語料庫那五種一定要挑字,MUST 走**呼叫端注入的專屬 seed**(`biomes` 的 `signRnd`),MUST NOT 自己 new 一個、更不可以動共享序列。稽核 `audit_world_text.mjs`(57 項)|
| 都市計畫(市區:建築沿街整齊排列 + 公設)| `siteplan.js` §A(`URBAN`/`CIVIC`/`CIVIC_KINDS`/`CIVIC_PARTS`/`CIVIC_TREES`/`plotSeed`/`frac`/`roadFaceRy()`/`planBlocks()`/`civicExtent()`/`partCollider()`/`civicColliders()`/`buildCivic()`)+ 接線 `biomes.js` 的 `frontSegs` 收集與街廓配置區塊 | 2026-08-03 使用者定案「建築沿道路整齊排列,遵循都市規劃的原則來設計,包含公園、運動場、停車場等公設」。舊制 `densifyUrban` 是**從既有建物長出去**的網格(補間),街廓只在單一種子內對齊 —— 相鄰種子各轉各的,沿街立面因此參差,而且沒有任何公共空間。①**對齊的是「建築線」不是「中心線」**:第 r 排的臨街立面一律落在 `hw + SETBACK + r × ROW_PITCH`,中心 = 建築線 + **自身**進深一半 ⇒ 進深逐棟不同也不影響那條線;對齊中心的話進深一變立面就前後參差(正是使用者說的「不整齊」),而畫面上只表現成「街景有點亂」。②**排距是推導的**(`urbanRowPitch()` = 進深上限 + 巷弄):手寫兩份的症狀是後棟壓進巷弄,巷弄不成直線。③**先劃公設用地、再劃建築基地**(`taken` 名冊在同一支規劃器裡):反過來做的話公設只能撿建築剩下的畸零地,而使用者點名的三種公設都要一整塊。④**公設是開放空間**:草坪/跑道/柏油面 MUST NOT 登記碰撞柱(登記了就是「看得見的空地走不進去」),只有涼亭/看台/燈柱/收費亭/停放車輛才掛,且**長條件 MUST 登記有向盒**(A30:40m 的看台只登記外接圓 = 球場中央一片 40m 直徑的隱形牆)。⑤**朝向公式只有一份** `roadFaceRy()`,`nearestRoadAngle` 同吃 —— 兩份差一個負號就是「同一條街有一半的樓背對著路」。⑥**零共享 `rnd()` 消耗**(§2.3;同 `beacons.js`):量體/樣式/公設款式全由 `plotSeed`/`frac` 雜湊決定 ⇒ 這一整段的存在與否**不會推移**任何一株植被、任何一棟圖資建物的佈局。⑦**市區閘不可省**:只在既有聚落(圖資建物/地標)周邊配置,否則穿過山區的一條 primary 兩旁會長出整排街屋。⑧`URBAN.MAX` 的天花板是 `LOS.MAX_OCC`(4000)不是美觀 —— 超過就被 `slice` 靜默截斷 = 後排建物在伺服器眼裡不存在(打得穿)。稽核 `audit_siteplan.mjs`(171 項)|
| 聚落場 + 建物來源信任階梯(所有會生出建物的路都要圖資背書)| `biomes.js` 的 `settlement()` + `UC`/`URBAN_DENS_P·Q`/`URBAN_MIN_PEAK` + `infillSeeds` + `classifyImg()`(純影像判,零亂數);消費端恰三個 —— `planBlocks` 的 `probeLot`/`probeCivic`(經 `nearUrban`)、`densifyUrban(\{ seeds \})`、`placeBoundary` 的邊界樓型 | 2026-08-04 使用者回報「太魯閣、合歡山不在市區還這麼多建築」。**建物有兩個放大器**(沿街配置 planBlocks、街廓補間 densifyUrban),舊制兩個各有各的門檻而且都太鬆:①`nearUrban` 只問「±1 格內**有沒有**圖資建物」—— 峽谷裡一間廁所就讓整條台8線兩旁取得配置資格(閘的註解寫的是「只在既有**聚落**周邊」,實際判的是「附近有一棟房子」);②`densifyUrban` **一道地貌閘都沒有**,它檔頭那句「只從既有建物長出去 ⇒ 郊野維持開闊」的前提是「種子 = 圖資建物」。③而 densifyUrban 的呼叫點排在 planBlocks **之後**、又就地 `generic.slice(0, INFILL.maxSeeds)` ⇒ 種子裡混進剛配出來的臨街樓 = **滾雪球**,且**圖資越稀疏滾得越兇**(市區的 slice 名額早被圖資建物佔滿,荒野才輪得到新配的)—— 這正是「不在市區反而樓更多」的成因。故收成一支 `settlement(x, z)`,兩個放大器 MUST 同吃;補間種子 MUST 在 planBlocks **之前**定案。④**門檻是局部標準化的比例,MUST NOT 手寫棵數**(2026-08-04 使用者定案「市區建築不是圖資都有嗎?建立局部標準化判斷」)—— 理由同 `field.js` 的地表色階(「色階門檻 MUST 取該場地自己的分位數」):`local(x,z)` = ±1 格內的建物棵數,基準 `peak` = **這張圖自己**的局部密度 P90(取分位數不取最大值 —— 單一密集格會綁架基準;取樣點是每一棟既有建物自己的位置 —— 全圖每一格會被空地的 0 拉到 0),閘門 = `DENS_Q × peak`。手寫棵數做不到跨圖可比:同一個數字在東京是空地、在峽谷是市鎮。⑤**退化保險 `URBAN_MIN_PEAK` 推導不手寫** = `INFILL.cols[0] × INFILL.rows[0]`(densifyUrban 自己畫得出來的最小一塊街廓)—— 少了它,只有三棟房子的峽谷 peak = 3、閘門 = 1.05,那三棟自己就過關,比例式反而比舊制還鬆。`DENS_Q = 0` 逐位元回到舊制(下限 1 = 「附近有一棟就算」)。**MUST NOT 改吃 `venue.mix`** —— 那是手寫的場地宣告不是圖資(見下一列),拿它當閘門就是「地貌由人宣告」。⑥**建物來源信任階梯**(2026-08-05 使用者回報「綠地/裸露地建築太多、不符真實圖資,設計更符合現實世界的判斷機制」):尺 = 圖資建物密度(聚落場)> 純影像分類(`classifyImg`)> 手寫 `mix`(只當全離線最後備援)。三個出口逐一收攏 —— ㋐**邊界樓**(`placeBoundary` 的 urban 型):衛星低飽和像素(裸岩/陰影/道路)與 mix 的 55% 改寫都會把荒野判成 urban,舊制沿整圈邊界每 ~30m 抽一次 = 綠地/裸露地被圈上數十棟 22~70m 高樓而圖資裡一棟都沒有 ⇒ 市區判定 MUST 過 `settlement`(邊界樓進 `generic` = 與圖資建物同路的權威幾何),不背書就**降格不留缺口**(按 mix 主要野地地貌擺巨岩/神木 —— 視覺牆選型不是權威地貌,mix 在這裡合法);㋑**備援程序街區** MUST 只在圖資**查詢失敗**(`!osm` = 不知道)**且場地宣告有市區成分**(`mix.urban > 0.1`;備援的語意是「重建一座宣告中的市區」,荒野場地宣告全是 0 = 沒有東西可重建)時觸發 —— 查詢成功但零建物 = 真實答案就是沒有建物,荒野 MUST 維持荒野(「不符圖資」寧缺勿錯、「圖資取不到」才降級,原則 6 的兩件事);㋒**市區種子 `urbanPts`**:影像在手只收**純影像**判為 urban 的點(mix 改寫 MUST NOT 憑空生出市區種子),影像也沒有(全離線)才退回 classify 結果 = mix 當最後一層。`classifyImg` 是 classify 第一層抽出來的**同一份**色彩門檻(零亂數 ⇒ 收集閘多呼叫一次不推移共享序列,§2.3;classify 的呼叫與亂數消耗逐位元不變 ⇒ 植被佈局不漂移)。稽核 `audit_siteplan.mjs` Ⅴ・Ⅵ・Ⅶ(`--break-gate` 反向驗證)|
| 立體結構的建置範圍 | `biomes.js buildRoads()`(簽章**無兵線參數**)+ 剔除記帳 `strucDrop`;三把具名的刀 `dropLaneBridges`/`dedupeCrossingBridges`/`dedupeParallelBridges` | 2026-08-04 使用者定案「高架橋/地下道/隧道/明隧道**就算在兵線之外也建立**,除非會干擾兵線,或違反其他規則」。建置端本來就沒有兵線距離判定(逐 way 跑完整份 `roadInput`,離兵線一公里的橋照建、照上碰撞、照登記走廊)—— 這一列釘的是「**沒有**那道閘」,而「沒有」正是最容易被不知不覺加上去的東西:場地選單只看兵線(`scen` 標記),很容易讓人以為結構只服務兵線,順手加一道「離兵線 N 公尺外就不建」來省 draw call,而畫面上只表現成「這張圖的橋比較少」。防線是**簽章**(`buildRoads` 拿不到 `cfg` ⇒ 結構性地看不見兵線/主堡/塔位),比「原文裡沒有距離判定」更硬。會讓結構消失的刀**恰三把**,每一把都對得上使用者那句話裡的一個例外、而且 MUST 記帳(少一座橋在畫面上完全看不出原因):`laneWet`(兵線跨水補橋是唯一結算 ⇒ 走廊內重疊的真橋剔除 = 干擾兵線)/ `crossing`(十字路口只留一座橋,2026-07-28 定案 = 其他規則)/ `parallel`(平行雙幅橋、雙孔隧道疊在一起 = 單層原則)。**新增第四把刀 MUST 同步記帳**。另兩條既有結論不得倒退:結構鏈排在一般道路之前(`maxRuns` 截斷依陣列序,2026-07-22 倫敦橋數浮動案)、`strucTunnel` 資格閘(人行/室內 tunnel 不成洞)。稽核 `audit_road_joint.mjs` Ⅵ |
| 場地地貌宣告 vs 圖資實測 | 宣告 `venues.js VENUES[].mix`/`.type`(**手寫**)/ 實測 `tools/audit_venue_biome.mjs` + `venue_field.mjs landcoverFor()` | 2026-08-04 使用者問「有正確從圖資判斷地貌嗎」。答案是**沒有**:`mix` 與 `scen`/`relief`「MUST 由實測產生」的紀律相反,它是手寫宣告,消費端只有 `classify()`(地被色階/植被加權)與 `placeBoundary()`(邊界帶要不要長樓)——**建物一格都不吃它**。於是錨點一旦被搬到有路網的聚落(`ll` 註解寫得很清楚:合歡山 → 埔里鎮、陽明山 → 天母、青木原 → 河口湖町、烏魯魯 → Yulara、奧卡萬戈 → Maun、威尼斯 → Mestre),那張圖拿到的就是那個城鎮的建物,而宣告仍寫著「裸露地 80%」。稽核把兩件**互相獨立**的事擺在一起,刻意不合併成一個數字:①**地被組成**(landuse/natural/leisure 多邊形面積佔比)對上 `mix`;②**建蔽率**(建物輪廓面積 ÷ bbox)對上 `mix.urban` —— `landuse=residential` 涵蓋整個街廓(含道路與院子)而建蔽率量的是真被樓蓋住的地,兩者一起看才分得出「圖資說這裡是市區」與「這裡真的都是樓」。門檻 `TOL` 是**判斷值**(放寬到差一個量級才算不符):用途是把該看的圖挑出來,不是把 28 張圖判死。取不到圖資的場地 MUST 標成**未驗**、MUST NOT 洗成通過(原則 6 / §5 通則 ④)|
| 樹冠羞避(綠地:神木森林)| `siteplan.js` §B(`CROWN`/`crownGap()`/`planShyGrove()`)+ 冠幅推導 `biomes.js giantCrownR()` | 2026-08-03 使用者定案「多個神木組成森林,遵循 Crown shyness 的規則設置」。①**冠幅 MUST 推導不手寫**(`giantCrownR` 掃零件表 35% 樹高以上的最遠水平點):手寫一欄 `cr`,改了任何一個冠簇的 `px/pz` 或半徑,間隙規則就與看得見的樹冠分家 —— 而畫面上只表現成「有些樹冠還是黏在一起」。②**塞不下走縮冠不走淘汰**:使用者要的是「森林」,一律淘汰會把群落打成稀疏散株;縮到 `FIT_MIN` 以下才放棄(原則 6)。縮冠量由不變式 `dist ≥ cr_i·s_i + cr_j·s_j + gap` **解**出來,gap 以**候選原體格**估(保守:縮小後只會更寬)。③**間隙隨樹高遞增**(`GAP_F`)—— 高樹搖擺幅度大,裂隙也大,這正是羞避的成因;手寫定值就變成「所有樹一律隔 3m」。④**抽樣與規則分開**:落點/體格照舊逐株抽(§2.3 固定枚數),整群抽完才過規則,規則本身**零亂數**。⑤傾斜 `lean` 遠離鄰冠,MUST 併進既有 `tx/tz` 剛體通道(A27),MUST NOT 進逐零件歐拉角。⑥**群落半徑同步放大**:冠緣要留間隙,林子攤不開就只能少種樹 —— 改 `CROWN` 任一項 MUST 回頭看 `placeGiantGroves` 的 `cr`。稽核 `audit_siteplan.mjs` Ⅲ |
| 地質排列(裸露地:巨石露頭)| `siteplan.js` §C(`ROCKFIELD`/`strikeRad()`/`planRockField()`)+ 接線 `biomes.js placeMegaliths()` | 2026-08-03 使用者定案「巨石依地質特性緊密排列,形成壯麗景觀」。舊制是「全圖散 12 顆、彼此至少隔 70m、每顆 `rnd() × 2π` 各轉各的」—— 那是**孤立奇岩地標**的擺法,不是露頭。「依地質特性」在這裡是三件具體的事:①**走向**(層面/節理面與水平面的交線)= **等高線方向**,故由地形梯度推導(`strikeRad`;取樣距 MUST 跨得過地形格 ~8.3m,否則量到的是單格鋸齒),平地回 null 退回落點雜湊(原則 6);②**節理間距成排 + 相鄰排錯縫**,錯縫相位 MUST 以**中央排**為基準 —— 拿 `r % 2` 當相位會讓排數為奇數時的中央排被錯開半格 ⇒ 露頭正中央沒有岩塊、體格係數永遠取不到 1(「核心最大」的設計當場失效,而畫面上只是「中間空空的」);③**同源同相**:整片同一種岩、**同一份色相偏移**(逐顆各抽各的 = 一片七彩露頭 = 看起來是七顆孤岩剛好擠在一起)、長軸一律對齊走向、體格自核心往外遞減。**緊密的界線是「碰撞柱不互穿」**(`ROCKFIELD.GAP_M`),再密也不能長進彼此體內 —— 那是破圖不是景觀;逐顆仍走既有的水域/淨空/平坦度/邊界四道閘。稽核 `audit_siteplan.mjs` Ⅳ |
| 語意化地標(兵線 / 重生點 / 建築單位旁的認路物)| `beacons.js`(`BEACON`/`BEACON_KINDS`/`KIND_PARTS`/`partExtent`/`kindExtent`/`beaconAnchors`/`beaconKindFor`/`planBeaconSites`/`beaconSeed`/`buildBeacon`/`beaconCollider`)+ 接線 `biomes.js placeBeacons()` | 2026-08-03(計畫書 P2-C)。使用者定案「**放在兵線 / 重生點 / 建築單位旁邊,在周遭可以看見即可**」—— 這句話正是這一項能落地的原因:P2-C 卡了兩個月不是做不出來,是**驗不動**(往兵線裡放東西正是 V-B 泛洪稽核的守備範圍,而那支要 Overpass + 高程磚)。改成「擺在旁邊」之後,「會不會擋住兵線」**由構造保證**:落點一律要通過 `areaFree(blocked, …, foot + PAD)`,而 `blocked` 裡本來就有兵線走廊(半寬 17m)/塔位(30m)/主堡(70m)/橋樑走廊/隧道敞開段 —— 與建物、巨岩、神木**同一道閘**,只是多留一圈 `PAD`。六條:①**錨點推導只有 `beaconAnchors` 一份**,三類全由既有幾何推導(兵線折線 / `cfg.bases` / `solveTowerSites()`),一次 fetch 都不加;塔位 MUST 吃 `solveTowerSites` 那一份解(MUST NOT 用 `TOWER_FRACS` 自己重算,同 `buildClearance`),且**序末項 = 前線**故整條反序 ⇒ 前線優先。②**零共享 `rnd()` 消耗**(§2.3):落點由確定性搜尋決定、外觀差異由**落點雜湊**自帶種子(同 `buildHazard` 以 seed 起 `mulberry32`)—— 抽一枚共享亂數就把後面每一株植被、每一棟建物的序列整條推移,而畫面上只表現成「整張圖的佈局變了」,沒有任何錯誤訊息。③**碰撞柱是量出來的**(`beaconCollider`,原則 4 / A30):標稱 `foot` 只是規劃期預留空間的依據,登記的半徑一律取實測 —— 看到多粗 = 撞到多粗 = 打到多粗。`foot` MUST 貼著零件表實算外廓 `kindExtent`(**雙向**釘住:低報 = 演出頂出碰撞柱、虛胖 = 地標被無謂推遠而使用者要的是「旁邊」),改零件表 MUST 重跑稽核校對這一欄。④**零件表是純資料**(`['box'|'cyl'|'cone'|'ico', …]` + 位移/旋轉),而且 `beacons.js` 的前半段(常數/型錄/零件表/錨點/落點規劃)MUST 維持**零 THREE** —— 這不是潔癖:three 走 CDN、A2 不准進 `package.json` ⇒ 零件一旦直接寫成 `new THREE.Mesh(...)`,外廓就只能靠真瀏覽器量,而 P2-C 卡住的原因正是「驗不動」。⑤**高度隨錨點淨空圈分級**:淨空圈越大 ⇒ 地標被推得越遠 ⇒ 必須越高才看得見(重生點 70m 圈 → 通訊塔 29m;塔位 30m 圈 → 水塔 23m;兵線 17m 圈 → 電塔 35m / 貨櫃堆 10m / 敖包 13m);`BEACON.FAR` MUST 大於主堡那一圈(70 + foot + PAD),否則重生點永遠掛不上地標而且不會有錯誤訊息。⑥**不掛反轉外殼描邊、零件依材質色合併**:一座電塔十幾個零件 = 十幾個額外 draw call,而世界的線本來就由 `postfx.js` 的螢幕空間勾線 pass 蓋全場(同計畫書 P0-C 的後續項「勾線上線後修剪重複的外殼」)。**呼叫 MUST 排在一般植被散布之前**(`blockArea` 之後小植被才會避開整件地標)、**且排在巨岩/神木之後**(大地標先佔位)。稽核 `audit_beacons.mjs`(68 項)|
| 離線工具的開挖後地形 | `tools/venue_field.mjs makeCarvedField()`(執行 `terrain.js carveTunnels` **原文**)| 2026-08-03(計畫書 V-C)。引道路塹與地下道斜坡是**挖出來的** ⇒ 拿天然地形走那一段會把通的路報成不通(`audit_traverse` 的泛洪站立面吃這一支)。`carveTunnels` 的輸入是覆蓋段的**敞開補集**(洞體是刪三角形,不是把山壓平),分段規則 MUST 逐字鏡射 `biomes.js` 那一段。**淨空(V-D)刻意仍吃天然地形**(`hf.heightAt`)—— 那一項問的是「這座山藏不藏得住頂板」,與 `tunnelWallProfile` 條件③吃 `natureAt` 同一條理由。**`shot_tunnels.mjs` 的掃描 ②~⑤ MUST 留在 Playwright**:它們量的是 `Raycaster` 對真實**網格**的命中,而 three 走 CDN、A2 不准進 `package.json` ⇒ Node 端沒有 three;硬「近似」成幾何式就是抄第二份公式,而且會永遠全綠 |
| 決定性亂數 | `rng.js mulberry32()` | 全專案唯一縫(`hazards.js` re-export 舊入口)。該檔**零 import** —— 亂數與 three 綁在同一個檔會逼每一支離線工具再抄一份,那就不是同一條序列了 |
| 後製管線 | `postfx.js Pipeline`(勾線 → 調色 → FXAA);唯一消費端 `game.js` 的 `this.pipeline` | 世界的描邊只能靠**一個 pass 蓋全場**(`biomes.js` 從來沒呼叫過 `outlinify`,而用外殼包住整個世界是幾百個額外 draw call)。四條:①RT 尺寸 MUST 由 `renderer.getDrawingBufferSize()` 取得 —— 管線自己算像素比 = `RES_GOV` 調節器整個變成 no-op,而畫面上只表現成「手機還是一樣卡」;②A25:3 個 RT + depthTexture + 3 個 `FullScreenQuad` 材質 MUST 全部 dispose;③**FXAA pass 恆執行**(它兼任線性 → sRGB),`?fxaa=0` 只把邊緣混合關掉 —— 整個跳過會讓畫面變暗變濁,看起來像「調色把畫面弄壞了」;④勾線門檻 = **距離項 + 掠射項**,只有其中一項時「地形網格折邊」與「建物輪廓」的值域會重疊(兩種寫法都實測過:不是整片山坡畫滿等高線,就是連建物的線一起消失)|
| 共用視覺入口 | `spawnCastFx()`/`stepCombatFx()`/`terrain.surfaceAt()` | 戰場與展示台共用,MUST NOT 各寫一套 |

### 2.2 狀態鍵與迴圈粒度
- 英雄以 **pid(連線 id)為鍵**存於 `heroes` Map(bot 用字串 pid 如 `'b1'`);MUST NOT 改用陣列索引或 socket 物件當鍵。
- 小隊共享狀態(金錢/電力/彈藥/招式)住 `sq.ps`,經 `_bindShared()` getter 掛回每架 ent。迴圈粒度 MUST 分清:`heroes.values()` = 一隊一次;`_allBodies()` = 每架一次 —— 搞錯 = 收入三倍或增益疊三層。

### 2.3 確定性(Determinism)
- 跨客戶端場景一致靠 `mulberry32`(戰場中心為種子);隨機散布 MUST NOT 用 `Math.random()`。
- 抽樣紀律:每候選消耗**固定枚數**亂數、淘汰檢查一律排在抽樣**之後**;MUST NOT 改成「淘汰就跳過抽樣」(佈局序列跨客戶端分歧)。

### 2.4 外部服務防禦
- OSRM / Overpass / AWS 地形磚 / Esri 影像皆會限流或掛掉:每條 fetch 路徑 MUST 保留程序生成 fallback(合成貝茲兵線、程序建物),改 fetch 邏輯時 MUST NOT 移除。
- **鏡像輪替 MUST 逐站計時**(唯一縫 = `biomes.js overpassQuery()`;Node 端 `tools/venue_field.mjs overpass()` 一向如此):一組鏡像共用**一個** `AbortController` 的話,任何一站「掛住不回應也不斷線」就把整份預算吃光,後面的鏡像永遠輪不到 —— 2026-08-03 實測 #2 kumi.systems 穩定 25s 無回應、#3 mail.ru 2.2s 就回得漂亮,結果路網查詢固定死在第二站。路網是隧道/明隧道/地下道/橋的**唯一** OSM 輸入 ⇒ 拿不到就退回合成兵線 = **一張圖上的立體結構整批消失**,而且沒有任何錯誤訊息(使用者看到的是「太魯閣的明隧道不見了」)。總預算只准用來決定「什麼時候收手」,MUST NOT 用來中止單一次嘗試。

### 2.5 世界尺度
- `SOLDIER_H`(真人 1.8m)是全遊戲唯一身高單位;人員/載具/建物一律用真實公稱尺寸,英雄體型只住 `heroTargetH()` 這一個縫。
- 改 `REAL_SCALE` MUST 同步 +1 `GEO_SCALE_VER` 並重跑 `node tools/bake_venue_lanes.mjs`。

---

## 3. 絕對反模式(A 編號恆定,供跨檔引用)

| # | 禁令 |
|---|---|
| A1 | 客戶端 MUST NOT 先改權威狀態;防作弊邏輯 MUST NOT 下放(= 原則 1) |
| A2 | MUST NOT 新增 npm 依賴 / build 工具 / TypeScript / 框架 |
| A3 | MUST NOT 修改 `reference/` |
| A4 | 確定性散布路徑 MUST NOT 用 `Math.random()` |
| A5 | 重武器 CD 唯一實作 = `mag:1 + reload=cd`,MUST NOT 另發明第二套 |
| A6 | 射擊 raycast 只打單位;地形走解析射線 `terrain.rayTerrain()`、建物/巨物走解析圓柱/盒(`_blockerHitT`)。MUST NOT 把 `terrain.mesh`/植被/建物 InstancedMesh 加進 raycast 目標(three 逐面線性掃 = 開火掉幀主因);MUST NOT 讓砲火穿越碰撞障礙 |
| A6b | **塗層雙面阻擋**:把上下空間隔開的實體面(地形高度場/橋面/隧道**頂板**與**路面**/障礙頂面底面)MUST 不分方向截斷砲火 —— 往上往下同判,**透明可穿透處例外**;能擋砲火的那一面同時 MUST 是站立面(橋面 `deckY`、隧道頂板 `tunRoofTop`,見 §2.1),只做一半 = 打不穿卻踩得穿(`punchPortalHoles` 打掉的三角形、地下道引道 `open` 段)。禁令兩條:①MUST NOT 用 `y <= heightAt(x,z)` 當彈道閘(那問「在地表以下」不是「穿過地表」:由下往上漏放、且不吃打洞 ⇒ 洞口變隱形山體),一律走 `_terrainSegT`→`_terrainHitT`;②障礙的垂直判定 MUST 是「穿越區間 ∩ 垂直帶」,MUST NOT 只驗入點高度或加掛 `dy < 0`(伺服器 `_losBlocked` 一向是區間語意 ⇒ 單向版 = 兩端分家靜默丟包)。爆點回報的離地基準 MUST 在洞內改取隧道路面(`heightAt` = 未開挖的山頂),但 `lev` **刻意只報 0/2** —— 橋面報 1 會被 `_slabSep` 判成「與橋上砲塔分屬板體兩側」= 塔對 AoE 免傷(前提:`sim._unitLev` 讓塔/主堡恆 0)。稽核 `audit_layer_block.mjs` |
| A7 | 飛彈失鎖(離開發射源射程 → 直線飛行)只適用**雷射導引**(`guide`,要玩家持續指示)與塔 SAM(`sim._tickMissiles`),兩端共用。**射後不理(`fnf`)刻意不吃**(2026-08-01 使用者定案「鎖定後持續追擊,不受射程影響 —— 但只能射程內鎖定」):射程只管**能不能鎖定**(客戶端 `_tickLock` 的 `_effRange` + 伺服器 `heroLock` 複驗射程/迷霧/LOS),鎖上之後彈道的射程包絡整條讓位給追擊燃料 `chaseCapS`(推導不手寫,只為了不讓失控彈體永遠留在場上,MUST NOT 拿它當射程的替身)。伺服器落點閘門因此**追加**一條綁鎖定目標的放行(`chased`):炸在該目標的爆風核心帶內 ⇒ 不吃 `impCap`(MUST NOT 放寬成「有鎖定就隨便一個遠點都收」)。**這是加分題不是替代題**(2026-08-03 使用者定案「中途爆炸也要有傷害」):沒炸在鎖定目標身上 MUST 退回一般 `impCap` 閘門照常結算,MUST NOT 退回舊制的 `if (lockT) … else` —— 彈頭在半路撞到小兵/建物/地形就地引爆時,爆點本來就不在鎖定目標上,舊制把它連同一般閘門一起判掉 = 前線一有東西擋路整發飛彈靜默丟包(開局空曠打得到、兵線一鋪開就再也打不到)。射手與僚機 MUST 吃同一個 `chased`。稽核 `audit_weapon_gate.mjs` Ⅸ |
| A8 | FOV 全機種一律 68(zoom 35);MUST NOT 用 FOV 做差異化 |
| A9 | 客戶端 `wstate` 彈藥與伺服器小幅漂移 by design(miss 不回報);MUST NOT「修正」 |
| A10 | 迷霧 = 伺服器快照過濾;客戶端 MUST NOT 二次遮蔽 |
| A11 | 爆風 `_blast` 刻意不吃 LOS(繞射近似)**也不吃射程**(2026-08-01 使用者定案「射程只限制擴散中心點,擴散範圍造成的傷害不受射程限制」)—— `_blast` 內 MUST NOT 出現任何 `def.range`/`RANGE_TOL`/`_altRange` 閘門,逐目標傷害只由「爆心 → 命中量體最近點」與 `blastFalloff` 決定。與「沒有射程光暈就不該掉血」不衝突:光暈的語意是「我能不能**瞄準**這個敵人」,而濺射從來不是瞄準出來的。兩者皆 MUST NOT「補完」 |
| A12 | `[#INC-103]` 無人機重生 `deadTick` 跨 tick 守衛 MUST NOT 移除 |
| A13 | `[#INC-105]` 中立 ents(`side:null`):`_acquireTarget`/`_acquire`/tick 主迴圈三處 MUST skip neutral,否則 `UNITS[kind]` undefined 直接炸 |
| A14 | `[#INC-106]` toon 三階 ramp 暗部 MUST NOT 低於 102;材質一律走 `toon.js mat()` |
| A15 | `[#INC-109]` 直升機 creep 刻意未接塔 SAM(以 pid 查找,heli 無 pid);MUST NOT「補完」 |
| A16 | SkinnedMesh 量尺寸 MUST `computeBoundingBox()` + 關 `frustumCulled`;`outlinify()` 跳過透明材質與 `userData.noOutline` |
| A17 | FPV 座艙掛在 camera 底下 — camera 本身 MUST `scene.add`,忘了整個座艙不見 |
| A18 | 貫穿 `heroLance` 判定 = 水平垂距 + 垂直帶,MUST NOT「修正」成 3D(伺服器無地形高程,高低差會整條落空)。**垂直帶的射線高 MUST 先經 `_lanceBandY` 換算到目標自己的垂直框**(2026-08-04 使用者回報「反器材武器打建築會跳傷害數據,但血條沒變」):`oy` 是射手**離自己站立面**的高度、`_bodySpan(t)` 是目標**離自己腳下地表**的量體帶 —— 兩個框各自以「自己腳下」為 0,高低差超過約 ±25m(120m 水平距實測)就整發被靜默丟棄,而客戶端 `_lanceFeedback` 的圓柱鏡射沒有這一道、照樣跳傷害數字。換框吃 `_absSightY`/`_hgtAt`,遵守 `_altDh`「兩端都拿得到絕對高程才用絕對框」的同一條紀律(取不到 ⇒ 逐位元退回舊近似,e2e 不受影響);**只有 line 類有這道垂直帶**(輕武器 `heroHit` 回報目標 id、扇形 `heroPlasma` 沒有)⇒ 症狀集中在反器材/貫穿砲,而建築是唯一「固定站在兵線上任你從高處狙」的目標。半徑 MUST = `lanceR(def) + hitR(t)`、軸距量**線段上最近點**(`s` 夾制)、排序用**原始 `s`**;準星 `_resolveAim(pierce=true)` MUST NOT 停在第一個單位;line 類一發只過一次 `_gateFire`,MUST NOT 另送 `hitMissile`。稽核 `audit_lance_hit.mjs` |
| A19 | 觸控疊層開著 MUST 整層收起 `#touchLayer`(`syncBlocked()`)。`#game` 是 `position:fixed` 堆疊脈絡,body 層搖桿壓在其上 ⇒ MUST NOT 用調 z-index「修」 |
| A20 | 手機直式 MUST NOT 一律 `flex-direction:column`(桌機並排 → 直排 = 按鍵被推出摺線、操作直覺破壞);只准收窄欄寬 + 降字級,塞不下才 `flex-wrap`。直排兩陷阱:`flex:1 1 0` 的 basis 落在高度、`.center-screen` 子列寬度不拉滿(MUST 給 `width:100%`)。稽核 `audit_ui_layout.mjs` |
| A21 | 操作說明的裝置分支只住 `help.js`(`pTouch`/`labelTouch`,取字經 `helpItemP()`/`helpCatLabel()`);MUST NOT 在 `main.js` 另寫 `if (TOUCH_UI)` 字串分支;判定旗標 = `mobile.js isTouchUI()` |
| A22 | 招式手勢派發只住 `game.js _fireHoldAbility()`(長按右鍵 / 長按 R / 觸控招式鈕共用),模式分流只准經 `data.js abilHoldSlot()`;MUST NOT 在輸入端各自寫 `aiming ? …`。**機種分派表(比對 `isDrone`/`isMorph`)隨機種絕招於 2026-08-06 一併退場,MUST NOT 復辟**。同功能只准一顆鈕(前科:陀螺儀開關) |
| A23 | `#tlLook` 空處開火出口只有 `_setLookFire()` → `_cmd('fire')`,MUST NOT 直接改 `client.firing`。MUST 先要求一次完整輕點、長按在幀迴圈判定、與 A 鈕互查 `firing` 才停火 |
| A24 | 小地圖 `_mmSeen`/`_mmBase` 座標框 MUST 恆為全圖(`_world2mmFull`),MUST NOT 跟顯示窗跑(持久資料會整片錯位);`_mmShadows()` MUST 回世界座標 |
| A25 | 一次性 3D 物件移除 MUST 釋放 GPU 資源(只 `scene.remove()` = 洩漏):彈體走物件池 `_takeProjectile`/`_dropBullet`、特效走 `_freeEffect` → `toon.js disposeTree`(唯一實作);共用幾何 MUST `markShared()` 註冊;高頻特效 MUST「單位幾何 + scale」不重配。稽核 `audit_gpu_lifecycle.mjs` |
| A26 | 程序生成零件擺位方向與旋轉方向 MUST 同調、錨點半徑 MUST 取該高度的錨體半徑(`trunkR(y)` 類單一縫)。三大病灶:差 90°(徑向 vs 切向)、差正負號(MUST 由軸的實際世界向量推,不手寫鏡射式)、拿基部半徑當通用半徑。堆疊件 MUST NOT 用 y 交錯偽裝、躺地件軸心高 = 自身半徑。改擺位 MUST 跑 `audit_object_joints.mjs` |
| A27 | 實例朝向 `ry` 與微傾斜 `tx/tz` MUST 當剛體整株套用(`xform.js vegPartXform` 單一縫),MUST NOT 併進逐零件歐拉角(Euler 'XYZ' 把 ry 夾在中間會攪亂方向);接合完成度 MUST 與 ry/tx/tz 無關 |
| A28 | 三機制兩條線 MUST NOT 斷:①`rooms.js`/`sim.js`/`bots.js` MUST NOT import Node 內建、用 `process.*`/`Buffer`/`require()`(加一行只有單機炸);②URL 佈局 MUST 鏡射儲存庫佈局(`/public/**` + `/server/*.js`)—— 否則 `data.js` 變兩份模組實例且不報錯。單機離場 MUST `hub.shutdown()`。稽核 `audit_net_modes.mjs` + `audit_solo_boot.mjs` |
| A29 | 地下道 MUST NOT 另開第二套結構 —— 沿用山體隧道整套,差異只有四個具名旗標:①剖面 = `tunFloorAt` 的 `sink`(旗標 `under`);②引道開挖 = 垂直路塹(run `cut` → `carveTunnels` 過渡帶 `hw+CUT_W`,山體隧道 MUST 維持 `hw+7`)—— 出入口只在道路頭尾兩端,側面 MUST NOT 留可通行開挖斜坡;③引道露天物理段(tunnelSegs `open:true`)只服務 surfaceAt 站立捕捉與移動側壁閘,slab 上傳/`_slabHitT` 彈道/`ceilingAt` 天花/lev 回報 MUST 濾 `!open`(漏濾 = 伺服器把露天溝當洞內 = 兩端分家靜默丟包)。側壁閘 = 單步高差 + **幾何牆線**(tunnelSegs `by` 牆頂 ← `wallTopAt` 單一縫、`makeTunnelIndex.wallCross`「由內跨出 ±hw 且牆頂高出腳下 >2.6m」即擋)—— 高度場網格會把垂直路塹攤成緩坡,單步高差在洞口內側永不觸發,MUST NOT 退回純高差判定;`by` 只住客戶端移動物理,slab 上傳 MUST NOT 帶出、山體隧道 MUST 無 `by`(恆放行)。地下道恆非明隧道(gallery `open` 歸零);引道擋土牆/緣石帶純表現層;門洞 `slope` MUST 取走廊平均而非洞口瞬時斜率。**覆蓋 = 全斷面藏進地下**(2026-08-05 使用者回報「深度不夠導致兩側牆超出地表/天花板高於地表」):下沉量 MUST 以中心線 + 兩側牆線(±(hw+0.6),hw 走 `strucHw` 單一縫由呼叫端傳入)取樣、MUST 在**延伸後**的基準線(hA→hB)上收斂(① 量的 gA→gB 與最終基準不是同一條,延伸端點落在土丘上就把 MARGIN 無聲吃掉),覆蓋區間 MUST 逐點全寬重驗拆縫 —— `TUN_GAP_CLOSE` 縫短縫是山體隧道的蓋廊語意,對地下道就是牆/頂板出土 + `surfaceAt` 隱形站立面;蓋不住一律退回露天路塹,MUST NOT 靠改地形蓋起來(天花板上方地表地貌不可動)。④**洞口打洞的路面參考 = fp 下沉剖面取樣**(2026-08-05 使用者回報「洞口殘留混凝土」):地下道門洞 bore MUST 帶 `fp`(每 2m 取樣 `tFloorAt` 同一份剖面,範圍兩端各加 `FP_PAD` —— 重疊判定含「部分在走廊外」的三角形,遠端頂點可落在走廊外一個地形格對角),`punchPortalHoles` 打洞/地被縮零/collar 投影三個消費端同吃 —— 下沉剖面是曲線、引道在洞外還一路爬升,舊制線性 `slope` 外推把 fy 夾成洞口地板 ⇒「引道路面之下」本該被路面緞帶蓋住的地形被誤刪 ⇒ 洞緣(rim)跑進斷面中央、collar 沿它織出橫跨洞口的混凝土殘片,而且沒有任何錯誤訊息(症狀只是洞口幾片灰白殘片 + 視線缺面)。三條缺一不可:fp 剖面本身、punch 高差判定**逐頂點**對各自深度的路面(8.3m 三角形拿質心一枚樣本,遠端頂點的路面更高 = 照樣誤刪)、collar 跳過兩端貼路面的 rim 邊(下方是保留的路塹底,無縫可封)。山體隧道/明隧道刻意不帶 fp(平直剖面下線性外推本來就精確 ⇒ 逐位元不變)。稽核 `audit_underpass.mjs` ⑩g + `audit_open_tunnel.mjs` Ⅵ-b(含舊制反例存證與平直剖面逐位元等價)|
| A30 | 障礙的碰撞/彈道/伺服器 LOS MUST 同一橫斷面:建物**與地標**= 有向盒(`hw2/hd2/ry`),圓只准當 broad-phase 且 MUST 是外接半對角;occ 上傳時 `ry` MUST 反號(sim 座標 z 鏡射),`setWorld` 預算 cos/**−**sin(8Hz 熱路徑)。**盒面朝向 MUST 與實例矩陣同調**:`ry` 與 three `Euler(0,ry,0)` 吃同一個值 ⇒ local 軸反解一律 `sn = −sin(ry)`(寫 `+sin` = 盒子鏡射差 2·ry:看得見的牆在這裡、擋彈與掛梯的牆在另一邊)。`_mmShadows` 是具名例外(純顯示)。稽核 `audit_climb.mjs` Ⅲ/Ⅶ |
| A31 | 攀爬路線只住 `climb.js`。**設施正面 MUST 面對結構**(`attachFaces()` 單一縫):有向盒只准四個面法線 + 面中心、巨岩只准生成期實測驗過的方位(`attA`;一個都沒有就不掛)、圓柱(神木)維持 16 方位;頂端錨件 MUST 以跨接臂接回結構(`arm`;圓柱走 `r − tr`,`tr` ← `trunkR` 推導)。**攀爬柱 vs 設施幾何是兩件事**:`y0/y1` = 攀爬柱,頂端 `y1` MUST = `b.y + b.h`(= `blockerTopAt` 站立面;MUST NOT 為視覺往下修 —— `_collide` 垂直閘 `myBot >= b.y+b.h−0.1` 一低就把人從屋頂推下去);`vy0/vy1` = 設施兩端,MUST 經 `facilityEndY()` 貼齊生成期實測的實體面 `b.ty`(碰撞柱刻意高過實體:建物 +0.5、巨岩 +1.5、地標取涵蓋尖頂的 `LANDMARK_COL.h`),落差夾在 `GRAB_UP` 內;設施幾何 MUST NOT 再直接引用 `r.y0/r.y1`,提示箭頭則刻意仍吃(標的是人站在哪)。抽中機率 MUST 經 `climbShare(siteSlopeDeg())` 隨地形陡度放寬(平緩帶 = 基準、阻擋角 = 100%;「不可陡上 = 100%」由曲線推導,MUST NOT 另寫 `slopeBlocked` 分支);**相鄰相接同吃這支曲線**(基準換成 `CLIMB.LINK`)且與地面路線共用**同一份** `siteSlopeDeg` 量測,MUST NOT 為它另寫第二條 ramp。**掛在「等高線相對最高那一面」的機率**反向隨陡度收到 0%(2026-08-01;`highFaceShare()` = 1 − 同一支 `slopeRamp`,平緩帶 100% → 阻擋角 0%)—— 三條曲線 MUST 共用 `slopeRamp` 這一份 ramp;高側 = 全部候選面裡地表最高的那一面(逐面邊掃邊比會誤判成「目前為止最高」),**四面等高時沒有高側**(`hi > lo` 守衛 MUST 留著,否則平台上的結構整棟掛不上)。攀爬軸 MUST 在碰撞體外 `CLIMB.OFF`(推導值,> 最大機體碰撞半徑)⇒ `_collide` MUST NOT 開任何豁免;上下移動吃 `_moveAxis` 前後推杆,MUST NOT 新增按鍵(A21/A22);每候選固定 4 枚亂數(抽中/起相位/相接/高側)、四面皆堵不掛(原則 5/6);相鄰相接沿用同一種路線型別(設施架較高者、`y0` = 低頂高),下端落地 MUST 用 `r.bx/r.bz`;箭頭動畫 MUST 併進 `biomes.js dynamics` 桶。稽核 `audit_climb.mjs` |
| A32 | **電腦玩家 MUST NOT 比真人多看/多走**(2026-08-02 使用者定案)。①選敵 MUST 過前方視野錐(`bots.js _acquire`,半角走 `botFovHalf`)—— MUST NOT 全角度掃描、MUST NOT 在 `_acquire` 以外另開第二條選敵路;②`h.ry` MUST NOT 直接指派目標角(瞬間回頭 = 視野錐當場失效),唯一寫入點 `_turn` + `viewLockStep`;③受擊警戒的方位 MUST 只來自 `sim._hurtLog`,MUST NOT 在 bots.js 自造(A1 家族);④位置 MUST 只經 `bots._move` → `sim.solidResolve`,MUST NOT 直接寫 `h.x`/`h.z`;⑤`solidResolve` 的量體 MUST 走 `selfCollider`、掃掠與 push-out 缺一不可。稽核 `audit_bot_vision.mjs` |
| A34 | **建築無加乘、護盾分軌只有一份拆分**(2026-08-02 使用者定案)。①任何武器/招式的 `vs.building` MUST ≤ `BUILDING_VS_CAP`(=1),`GRENADE.BUILDING_MUL`/`grenadeBuildingMul` MUST NOT 復辟;夾制 MUST 是推導迴圈,MUST NOT 逐武器手改 vs 表;②護盾/裝甲拆分只准住 `data.js shieldSplit()`,伺服器/客戶端估算/duel/balance 四個消費端 MUST 全走這一支,MUST NOT 自己讀 `vsSp`/`vsHp`/`spPierce` 去乘(兩端分家 = HUD 數字與掉血對不上);③溢出 MUST 按**預算**折回,MUST NOT 把溢出的護盾傷害直接倒進裝甲層;④`vsSp = 0` MUST 退化成「護盾全擋」而非「無視護盾穿過去」;⑤`vsHp` MUST 對無護盾的 NPC/建築一樣生效;⑥穿盾/反裝甲 MUST 只掛在 `EX_SIEGE_WEAPONS` 名冊內的武器上、反護盾武器 MUST 無任何 `vs > 1`、掛了護盾軸的武器 MUST 吃 `counterDmgF` 折減(三條配置紀律,見 §2.1)。稽核 `audit_shield_counter.mjs` |
| A35 | **攻擊範圍收斂與三軸預算**(2026-08-02 使用者定案)。①非「範圍見長」的爆炸型武器 MUST NOT 一發同時傷到同塔位的兩座塔;上界只准由 `soloBlastRmax()` 推導,MUST NOT 在任何消費端手抄 30 / 7 / 1.8;②豁免只走 `AREA_WEAPONS` 凍結名冊(附理由,增刪同步稽核),fan/line 依機制豁免;③夾制與火力回補 MUST 由**同一次**推導迴圈定案,MUST NOT 回頭逐武器手改 `dmg` 階梯;④回補 MUST 是重分配(整批係數幾何平均 = 1),MUST NOT 當純加法發下去;⑤`aoeTrimF`/`mobDmgF`/`rngDmgF` 的套用點各只准有 heroWeapon 的 `dmg` 一欄(少一處 = 沒計價、多一處 = 平方計價);⑥招式 MUST NOT 吃這三軸(使用者:先不考慮長按技和大小招);⑦改任一爆炸型 `r` / 名冊 / 上界 MUST 重掃 `SEEK.R_M`(核心帶縮小 ⇒ 導引頭打不進核心帶 = 光暈亮著卻沒命中);⑧(2026-08-04)家族上限只准由 `blastCapR()` 推導、家族分組只准由 `blastFamily()` 判(guide 與 fnf **同一族**),導引類在**每一階**的爆風半徑 MUST 恆小於榴彈類(上限/下限/幾何中點三個端點都驗),且榴彈類射程 MUST = 全體重武器最短的那一帶;⑨家族內排序 MUST 保留(授權半徑越大 ⇒ 定案半徑越大)。稽核 `audit_aoe_trim.mjs` |
| A36 | **射速壓縮的三欄一起動、連發演出不碰權威狀態**(2026-08-02 使用者定案)。①`rate`/`dmg`/`mag` MUST 由 `compressWeapon()` **同一次**推導定案,MUST NOT 逐武器手改、MUST NOT 只改其中兩欄(少了 `mag ×f` = 持續 DPS 虛胖,而爆發 DPS 正常 ⇒ 只驗一項驗不出來);②壓縮曲線 MUST 嚴格遞增(排名不變的**保證**),MUST NOT 改成整數倍率或分段表;③`recoilTier` 與 `fireBurstN` MUST 吃壓縮前的 `rate0`;④連發補畫是純表現層:MUST NOT 送任何網路訊息、MUST NOT 進 `this.bullets`、MUST NOT 扣彈藥/電力/動 `lastFireAt`(任一條破了就是 A1,而畫面上只看得到「傷害偏高」);⑤陣亡/換座機 MUST 清 `_burstQ`。稽核 `audit_fire_rate.mjs` |
| A38 | **導引彈的追蹤目標只認擊發當下的準星解**(2026-08-04 使用者回報「雷射導引…準星沒有照到目標、不在狙擊模式時,會往先前鎖定的敵人飛去」)。`game._tryFire` 決定 `homing` 時 MUST NOT 讀 `this._lockId` —— 那是**上一個**伺服器複驗過的鎖定,準星移開後要等 `_tickLock` 下一次 4Hz 心跳才清得掉,那段空窗裡開火,彈體會繞過準星飛向一個玩家已經不再瞄的人。準星解不到 ⇒ **不追蹤**(直飛),MUST NOT 退回任何舊目標;鎖定該有的黏著住在 `_coneAcquire` 的遲滯錨 `keepId`(準星仍壓在同一個錐內才續留),不在擊發端。仍 MUST 用**擊發當下**的準星解而非等鎖定成立(鎖定要一趟伺服器往返,甩準星立刻開火那一發等不到)。稽核 `audit_weapon_gate.mjs` Ⅵ③ |
| A37 | **文字圖層只有一個、語料一份、比例推導、純表現層**(2026-08-03 使用者需求)。①全世界的字 MUST 只經 `worldtext.js SignSheet` 一張圖集一個 mesh,MUST NOT 另開第二套(舊 `signage.js` 已退場,MUST NOT 復辟);②取名規則只有 `vernacular.js pickName` 一份,主名 MUST 是在地文字(`name:en` 只當拉丁副名);③日常副行 MUST NOT 是 OSM 列舉值,取不到就留白;④去重帳全世界一本且**合成名/詞表保底一併入帳**,連鎖旗標 MUST 只認 `brand`;⑤牌面尺寸 MUST 由 `signAspect(style)` 推導,MUST NOT 兩個維度各自亂數;⑥雙面牌 MUST 是兩片背對背的單面四邊形,MUST NOT `DoubleSide`;⑦招牌 MUST NOT 進 `blockers`/碰撞/LOS —— 純表現層,權威幾何一格都不能動(原則 4);⑧構件名牌零亂數,語料庫招牌只准用注入的專屬 seed(A4 家族);⑨裝不下 MUST 記 `signDropped`,MUST NOT 靜默截斷。稽核 `audit_world_text.mjs` + `audit_vernacular.mjs` |
| A38 | **三個地貌的排列規則各只有一份、全住 `siteplan.js` 的純區塊**(2026-08-03 使用者定案)。①規則(哪裡放得下/放幾個/隔多遠/朝哪邊)MUST 是純幾何,落點只准問呼叫端給的 `probe` 回呼 —— 規劃器 MUST NOT 認得地形/`blocked`/`occ`,也 MUST NOT 出現 `THREE`(離線稽核靠的就是這一條);②街廓配置與公設 MUST **零共享 `rnd()` 消耗**(A4 家族:抽一枚就推移全圖植被與圖資建物的佈局);③建築線、排距、冠幅、走向一律**推導不手寫**(`urbanRowPitch`/`giantCrownR`/`strikeRad`);④公設的開放鋪面 MUST NOT 登記碰撞柱,有量體零件的碰撞體 MUST 由零件表實算且**長條件登記有向盒**(A30);⑤朝街朝向只有 `roadFaceRy` 一份,`nearestRoadAngle` MUST 轉呼它;⑥樹冠羞避 MUST 走**縮冠**而非淘汰、傾斜 MUST 併進既有 `tx/tz` 剛體通道(A27);⑦巨岩長軸 MUST 吃 `cell.ry`,`g.rotation.y = rnd() * Math.PI * 2` MUST NOT 復辟。稽核 `audit_siteplan.mjs` |
| A39 | **軟性物質:一個旗標管兩件事、細勾線只走 alpha 契約、擺動只動頂點**(2026-08-04 使用者定案)。①「這個零件是不是軟的」MUST 是**同一個旗標**同時決定勾線粗細與擺動,MUST NOT 拆成兩份名單;分類 MUST 由既有的 `part.key`/`sf` 推導,MUST NOT 另開「哪些會飄」的清單。②細勾線 MUST 只經「場景 RT 的 alpha ≡ 勾線門檻倍率」這條契約傳到 `postfx.js`,倍率 MUST 乘進 `smoothstep` 的**輸入**(乘在 ink 之後只變淡不變細)且 MUST 取「這一格 + 四鄰」的最小值;`postfx.js` MUST NOT 手抄那個倍率(它只讀 alpha)。③擺動 MUST 是**純頂點位移**:MUST NOT 動碰撞柱/LOS/通行寬/`blockers`,伺服器一格都不能改(原則 4)。④擺動權重 MUST 錨在整株局部座標(`uSoftBase + 頂點高 × uSoftSy`),MUST NOT 逐零件從 0 起算(A26/A27:接合完成度與擺動無關);相位 MUST 取實例原點、風向 MUST 轉進局部座標。⑤`span` MUST 推導不手寫。⑥風向與時鐘全場**各一份**(`WIND`/`celWindTime()`),雲/植被/旗幟 MUST 同吃,MUST NOT 各自數 dt 或各寫一個風向。⑦軟性 MUST 進 `customProgramCacheKey`。稽核 `audit_soft_stroke.mjs`(`--break-ink` / `--break-anchor` 反向驗證)|
| A40 | **角色與機體的檔案格式只有一份、原型層由 `visual` 推導、生成文字只由 `codex.js` 組裝**(2026-08-04 使用者定案)。①段/欄/必填只准住 `codex.js`(`SECTIONS`/`PROTO_LAYERS`/`GEN_FIELDS`/`CODEX_FIELDS`),`lore.js`/`mecha.js` MUST NOT 自帶第二份欄位表;②角色與機體的**生成段鍵集 MUST 逐位元相同且順序相同**,MUST NOT 只在其中一邊加欄位;③機體原型 MUST 是結構化的層且**層集由 `visual` 推導**(漏一層 = 紅字,不是「那台剛好比較短」),變形者 MUST 有飛行型 + 地面型**兩層**,每層 MUST 分成 `{ src, note }` 兩欄;④自由字串的 `lore.proto` 與 `main.js` 的標籤正規式切割 MUST NOT 復辟;⑤全高/機體名/主色/機種/陣營 MUST 到原處取(`heroTargetH`/`data.js`),`mecha.js` MUST NOT 手寫公尺數或複製那些值;⑥`textSeed`/`imagePrompt`/`modelSheet` 三份對外生成文字 MUST 只由 `codex.js` 組裝,MUST NOT 手寫第二份;⑦`mecha.js` MUST 維持零 import、`codex.js` MUST 只 import 三支純資料檔;⑧(2026-08-05)**型態姿態語只准住 `FORM_POSE`**、取得只准經 `formPose(ch, form)`(型態存不存在 MUST 由原型層推導,MUST NOT 比對機種字串),飛行那一組 MUST 明講機體離地在飛且取景 MUST NOT 是 `standing`,地面那一組 MUST 逐字維持舊制(非變形者的提示詞逐位元不變);⑨**用不到的 `visual` 欄位 MUST NOT 進生圖與切圖清單**,適用性只准經 `visualUses()`(規則對得上 `models.js` 的實際消費點),`visualWords`/`chassisSpec`/`DRAW_SLOTS` 三個消費端 MUST 全吃;⑩出圖管線的設計敘述 MUST 取 `codex.js`(`mechaCodex`/`protoOf`),**已退場的 `lore.proto` MUST NOT 復辟** —— 它恆為 undefined,而 `proto && …` 會把整條設計敘述靜默丟掉、提示詞看起來卻仍然很長。稽核 `audit_codex.mjs`(`--break-layer` / `--break-align` / `--break-pose` 反向驗證)|
| A33 | **電腦玩家戰術的帳只有一份、分層只認旗標**(2026-08-02 使用者定案)。①威脅(誰對我造成多少傷害)只准住 `sim._hurtLog`、輸出(誰對我方造成多少總傷害)只准住 `sim._dmgOut`,bots.js **只讀不寫**(自算 = A1 家族);②難度分層 MUST 走 `BOT_DIFF.tactic`/`elite`,MUST NOT 比對難度字串 —— 新手/低難度的選敵與撤退 MUST 逐位元維持舊制(難度階梯塌掉會被誤判成「AI 壞了」);③三項選敵指標 MUST 正規化成候選集內佔比後才進 `botTargetPrio`;④撤退的遲滯帶(`PULL_SP` < `RALLY_SP`)MUST NOT 收成同一個門檻;⑤(2026-08-08)**機體定位分類 MUST 推導不手寫**(逐角色名冊一律禁止)、定位策略只准是既有旋鈕的覆寫(bots.js MUST NOT 出現 `if (role === …)` 的行為分支)、乘數 MUST 以角色數加權幾何平均 = 1 正規化(重分配而非通膨)、且只在 `tactic` 旗標之下解析(新手/低難度逐位元同舊制)。稽核 `audit_bot_tactics.mjs` + `audit_bot_role.mjs` |

---

## 4. 錯誤處理與狀態管理

**失敗策略**:見原則 6(降級不例外、寧缺勿錯、驗證後靜默丟棄)。

**權威狀態流**
- 快照 8Hz;`snapshotFor(side)` 只過濾「單位」,塔/主堡/中立物恆可見;同 tick 三份快照共用一份 frame 快取(`_tickN`),events 只能清一次 — 動快照邏輯 MUST 維持此共用。
- 雙層 HP:護盾(先扣、不吃護甲、脫戰回復)→ 裝甲 hp(吃 `armorMul`)。爆擊只在直擊武器,AoE 不爆。
- 擊殺 bot 一律 `BOT_KILL_SCORE`(3)— 刷 bot 不能速成招式,MUST NOT 移除。
- `createRoom` MUST 附合法預建 `battleConfig`;環境由 `resolveEnv` 開房定案進 `cfg.env` 全房一致,MUST NOT 客戶端各自重算。

---

## 5. 核心指令與回歸驗證矩陣

```bash
npm start            # server on http://localhost:8620(--port <n> 覆寫;PowerShell 的 PORT=x 前綴無效)
npm run lan          # 區網 / Tailscale 對戰(--https;印出區網 + Tailscale + MagicDNS 網址)
npm run cloud        # 雲端節點($PORT 監聽、/healthz、--max-rooms 戰區上限)
npm run build:solo   # 打包單機特化版到 dist/(純檔案複製,無 bundler;GitHub Actions 同一支)
npm run audit:net    # 三種連線機制稽核(瀏覽器安全 / 單一真相縫 / URL 佈局鏡射)
npm test             # node test/e2e.mjs,約 60 項斷言(不會自動啟動伺服器!)
npm run bal          # 平衡七不變式:①一波 NPC = 玩家 60% EHP ②前線敵我塔重疊 80% 且不對射
                     #              ③單線 30% 擊殺/40% 助攻 10 分鐘 ≈ 八軌升滿 ④滿級單推同塔位雙塔剩 0~20%
                     #              ⑤對進戰勝率(陣營/機種/較高方皆 ≈50%、角色不離群、接近期損失 ≤40% EHP)
                     #              ⑥招式配置 ← 武器射程剖面(扇形武器優先貼身套件)
                     #              ⑦前線交戰(tools/lanesim.mjs;射程/速度/火力/**攻擊範圍**/兵波/砲塔/經濟):
                     #                a 一發打不到兩座塔 b 單軸擾動方向性自驗 c 機種交叉 d 武器類型交叉
                     #                e 模擬長度 f **長按 = 大招**:載具組量份額交付率(三種形式同量級)
                     #                  + 自身型組量 EHP 當量/次(9 台全數 > 0)+ 輔助機損失率(印不擋);兩組刻意不合併
node tools/audit_aoe_trim.mjs   # 攻擊範圍收斂 + 三軸預算稽核(58 項;不需伺服器/瀏覽器)
node tools/audit_fire_rate.mjs  # 射速壓縮 + 連發演出稽核(56 項;不需伺服器/瀏覽器)
node tools/audit_recoil_move.mjs # 後座力 → 開火中移速稽核(66 項;不需伺服器/瀏覽器)
node tools/audit_speed_comp.mjs # 移速壓縮(拉近差距 / 排序不變)稽核(30 項;不需伺服器/瀏覽器)
node tools/audit_self_ult.mjs   # 自身強化型招式:補償 + 跟隨玩家的輔助機隊稽核(210 項;不需伺服器/瀏覽器;
                                #   --break-eq / --break-alpha / --break-brk / --break-stack / --break-tempo 反向驗證)
node tools/audit_ult_carrier.mjs # 招式載具遞送稽核(83 項;不需伺服器/瀏覽器;
                                #   --break-cd / --break-guard / --break-boom / --break-origin 反向驗證)
node tools/audit_npc_collide.mjs # NPC 飛行高度基準 / NPC ⇄ 機體實體碰撞稽核(44 項;不需伺服器/瀏覽器;
                                #   --break-ratchet / --break-deck / --break-sweep 反向驗證)
node tools/audit_world_height.mjs # 世界高度上限(遊戲最高高度 / 物件最高高度)稽核(49 項;不需伺服器/瀏覽器;
                                #   --break-ceil / --break-obj / --break-cap 反向驗證)
node tools/audit_hex_stats.mjs  # 圖鑑六角能力圖稽核(58 項;不需伺服器/瀏覽器)
node tools/audit_codex.mjs     # 角色 / 機體檔案格式稽核(90 項;不需伺服器/瀏覽器;
                                #   --break-layer / --break-align / --break-pose 反向驗證)
npm run codex                   # 機體美術覆核台 = 「2D 生圖對照台」(臨時工具,dev-only:已生成機體圖 ×
                                #   角色頭像 × 3D 展示台 + 武器招式 + 遊戲未公開生成段;確認勾選 /
                                #   局部重繪框 / 重下 prompt)。圖有**兩個來源**(`SOURCES` 單一縫):
                                #   已入庫 `public/assets/…/mechs/*.png` + 尚未驗收的 AI 設定稿
                                #   `tools/ai3d/masters/*.jpg`,兩者計數 MUST 分開(合併 = 把「還沒有
                                #   正式圖」藏起來)。遊戲設定頁「開發工具」列可直接**啟動 / 停止**這一支
                                #   (後端 `tools/dev_supervisor.mjs`,只回應 loopback),不必回終端機。
                                #   `--report` = 不開瀏覽器直接印配對表(缺圖 / 孤兒 / 覆核進度)
npm run parts                   # 「3D 零件對照台」(臨時工具,dev-only;同一套操作與邊界,埠 8622):
                                #   `docs/ai3d_runbook.md` 生成的 3D 物件 **vs 原版**並排 —— 左「原版」
                                #   (零件庫 GLB 走保險絲 primitive / 純資料件走 baseline rev 的**那一版
                                #   自己的 buildBeacon**)、右「AI 生成」(遊戲實際路徑),同一顆座號同一顆
                                #   相機 + 實測碰撞柱疊圖 + 三角形/draw call 讀數;逐件說明**用哪個生成方法**
                                #   與**吃哪一張 img**(授權/出處/查詢字),並列出缺件 / 孤兒節點 / 未記載來源。
                                #   `--report` = 直接印對照表;`--photos <某個 tools/ai3d 目錄>` 指定照片家
node tools/ai3d/intake_parts.mjs # AI 零件庫入庫閘(外廓契約 + 三角形預算;讀取縫 = tools/ai3d/parts_src.mjs)
node tools/ai3d/measure_megalith_tris.mjs # families.megalith 預算量測 + 「有無零件庫 rnd() 枚數相同」對帳(需 playwright)
node tools/audit_vernacular.mjs # 在地文字語料稽核(287 項;不需伺服器/瀏覽器/網路)
node tools/bake_venue_text.mjs  # 27 場地在地文字語料重烤 → public/js/venueText.js(外網 Overpass,㋓)
node tools/shot_signs.mjs --venue shibuya   # 世界文字視覺閉環(㋓ 需 playwright;九種語域排面 + 圖集 PNG)
node tools/shot_facades.mjs [--only pit --cols 3]  # 立面貼圖排面(㋓ 需 playwright;22 款 × 貼圖 + 自發光
                                #   + 斜頂那六款的屋頂帶邊界標尺)。它蓋的是**只有畫出來才看得見**的兩件事:
                                #   立面上下顛倒(v 的方向)、窗格印在斜屋頂上(屋頂帶)
node tools/audit_traverse.mjs   # 兵線與結構可通行泛洪稽核(27 場地;高程/圖資走網路 + tools/.scen_cache)
node tools/audit_cel_pipeline.mjs # 賽璐璐管線稽核(52 項:ramp 家族 / 天空穹頂 / 地形色階梯 / 描邊寬度)
node tools/audit_visual_prefs.mjs # 畫面表現旋鈕 / 陰影偏色 / 風化場 / 零件抖動 / 景深模糊稽核(172 項;不需伺服器/瀏覽器)
node tools/audit_soft_stroke.mjs # 軟性物質(細勾線 + 隨風飄揚)稽核(73 項;不需伺服器/瀏覽器;--break-ink / --break-anchor 反向驗證)
node tools/audit_world_text.mjs  # 世界文字(圖集/版面/裝箱/接線)稽核(57 項;不需伺服器/瀏覽器)
node tools/audit_beacons.mjs     # 語意化地標(P2-C)稽核(68 項;不需伺服器/瀏覽器;--break-extent / --break-pad 反向驗證)
node tools/ai3d/mesh_sym.mjs --gate  # 零件庫節點「另一面空不空」量測 + 該補的名冊(不需伺服器/瀏覽器)
node tools/ai3d/node_sheet.mjs --glb <parts glb> [--ref <舊 glb>]  # 節點四面黏土對照(需 playwright)
node tools/ai3d/measure_veg_tris.mjs --venue aokigahara --team 3  # families.veg 三角形預算量測(綠地場地;需 playwright)
node tools/ai3d/photo_sheet.mjs <photos 目錄>   # 照片 contact sheet(人眼複核第一站:MUST 先看照片再看網格)
node tools/audit_siteplan.mjs    # 場址配置規則稽核(197 項;都市計畫/樹冠羞避/地質排列/**聚落場**/**整棟量體庫節點**;
                                #   --break-line / --break-shy / --break-strike / --break-gate / --break-mass 反向驗證)
node tools/audit_venue_biome.mjs --offline  # 場地地貌宣告自洽 + 建物管線不讀 mix(不需網路;CI 收這一半)
node tools/audit_venue_biome.mjs # 完整版:宣告 mix/type vs 圖資實測的地被組成與建蔽率(㋓ 需 Overpass)
node tools/shot_scene.mjs --venue taroko  # 定場鏡頭組(㋓ 需 playwright;--ink=0/--grade=0/--post=0 逐層隔離)
npm run sim          # headless 加速模擬完整 bot 對局(平衡/難度壓測)
node tools/bot_learn.mjs        # 電腦玩家策略學習迴圈(平衡調整後重跑;--eval 驗收、--reset 回中性)
node tools/audit_bot_policy.mjs # 學習策略稽核(40 項;--break-clamp / --break-neutral 反向驗證)
node tools/audit_bot_role.mjs   # 電腦玩家定位分類與策略稽核(80 項;末段印現役定位名冊;
                                #   --break-role / --break-norm / --break-tier 反向驗證)
```

**測試標準流程(MUST 逐步,#INC-101/102)**:
1. `netstat -ano | grep :8620` — 檢視**全部** LISTENING(Windows SO_REUSEADDR 允許兩個 server 同時 LISTEN)。
2. `taskkill` 所有監聽者(**含 npm 父進程**),確認 0 個 LISTENING。
3. `node server/server.js` 起新伺服器 → `npm test`。**沒重啟伺服器 = 測到舊程式碼還全綠**。

**矩陣通則**(適用下表全部,不逐列重述):
- ㋐ 改任何有離線稽核的判定 → 該稽核 MUST 全綠 **且 MUST 做反向驗證**(原則 9)。
- ㋑ 稽核腳本以「執行原文」驗真品;詳細斷言看各腳本檔頭,本表只記入口與關鍵不變量。讀原文與抽方法區塊 MUST 走 `tools/audit_src.mjs`(`readSrc()`/`grabMethod()`),MUST NOT 自己 `readFileSync` 原文 —— 那支把換行正規化成 `\n`:逐行剝註解(`/\/\/.*$/`)與 `split('\n')` 在 CRLF 檢出的工作區會**靜默失效**(`.` 不吃 `\r`、`$` 不匹配 `\r` 之前),註解裡提到的名字就被算進「全檔只有 N 處」的單一縫計數 ⇒ 同一份程式碼 LF 全綠、Windows 紅字。
- ㋒ 純表現層改動 ⇒ `npm run bal`/e2e 天然不受影響,但相鄰稽核仍 MUST 全綠。
- ㋓ 需外網(Overpass/OSRM)的項目沙箱跑不動 → 走 GitHub Actions / 真機;需真瀏覽器(CDN three)的冒煙待真機,MUST 在交付說明中標註未驗項。

**改了什麼 → MUST 跑什麼**

| 改動 | 驗證 |
|---|---|
| 任何平衡數值(小兵/角色武器/SQUAD.BUFF/HEROIC/塔/賞金/八軌價格) | `npm run bal` 全綠;動角色武器一併看 ⑤ 角色離群列 |
| AoE 彈頭在天上時的填彈(`sim._refillIfDone`・`_gateFire` 的 `back`/填彈中閘門・`heroReload`)或 扇形錐緣(`data.js fanArcHalf`/`fanConeHalf`・`sim.heroPlasma` 的錐內判定與 `offF`・`game._shotVictims` 的 fan 分支・`tools/lanesim.mjs hits()` 的 fan 分支) | `audit_weapon_gate.mjs`(299 項;Ⅴ-b 新增:fan 足跡的錐緣走 `fanConeHalf` 單一縫、兩端各恰一個消費端、lanesim 同吃、舊 `cosA` 手寫錐已退場;行為直測「砲塔量體中心偏出標稱錐 12° 仍亮 / 同偏角拉遠則不亮」)+ `npm test`「AoE 彈頭在天上時的填彈訊息」段(6 項:三類 AoE 各跨**三個彈匣**全數結算 —— 病灶從第二個彈匣起才現形,只驗一個彈匣一定全綠)與「扇形錐緣量到命中量體」段(8 項:塔/主堡的牆面正中與邊緣都有傷害、靠量體進錐不高於正中、小兵傷害逐位元不變、錐外仍打不到)+ **反向驗證**(拿掉 `heroReload` 的 `_refillIfDone` ⇒ 三類 AoE 各丟 6~8 發;把錐緣改回標稱半角 ⇒ 塔/主堡的牆面邊緣紅字)+ `npm run bal` 全綠(⑦ 的 fan 幾何本來就已經算到量體 ⇒ 模型**不動**,動的是伺服器往模型對齊)+ `audit_aoe_trim.mjs`/`audit_shield_counter.mjs`/`audit_lance_hit.mjs`/`audit_fire_rate.mjs`/`audit_bot_tactics.mjs`/`audit_bot_vision.mjs`/`audit_blood_splat.mjs`(共用 `_gateFire`/`_damage`/傷害鏈)+ 真機冒煙(榴彈/巡飛彈/雷射導引連打**四五個彈匣**,每一發落地都掉血;貼著砲塔或主堡牆面噴電漿/散彈,準星壓在牆上任何一點都掉血,而錐外的敵人照樣打不到)|
| 射速壓縮 / 連發演出(`RATE_DEF`・`FIRE_RATE`(K/PIVOT/BURST_MAX)・`rateComp`/`compressWeapon`/`fireBurstN`/`fireBurstGap`・`heroWeapon` 的 `rate0` 欄・`recoilTier` 的射速分級・任何角色武器的 `rate`/`dmg`/`mag`・`game._queueBurst`/`_tickBurstFx`/`_burstEchoSelf`/`_burstEchoOther`/`_spawnVisShell` 的 `heavy` 參數・`_tryFire` 的回穩窗計數・`lanesim.reFire`) | `audit_fire_rate.mjs`(56 項;Ⅰ 曲線 —— 錨點以下逐位元原值、嚴格遞增(排名不變的保證)、折減率單調不減(越快降越多)、K ≥ 1 逐位元回到舊制、常數不被消費端手抄;Ⅱ **DPS 不變雙軌** —— 以純函式 `compressWeapon` 合成掃描全值域,爆發 DPS 與持續 DPS 各自不動,且**逐案例**釘住「唯一誤差源是彈夾整數化(≤ 半發)」,另把「mag 不跟著縮 ⇒ 持續 DPS 虛胖 25.5%」當反例存證;Ⅲ 現役 32 角 —— 排名逐對複驗、全距比值收窄、32 把重武器逐位元不動、`recoilTier` 吃 `rate0`、彈夾恆為 ≥1 整數;Ⅳ 連發 —— N ∈ [1, BURST_MAX] 且取最近整數、**使用者指定的機槍 = 3 連發**、N 隨原射速單調不減、`gap × N × rate === 1`(平均鋪滿擊發週期);Ⅴ 純表現層 —— 排程單一縫且排在 `_tickWeapons` 之後、補畫路徑無 `net.send`/不進 `this.bullets`/不扣彈藥、陣亡與換座機清佇列、回穩窗計發數;Ⅵ `reFire` 長期平均射速 = 標稱且與步進無關、空窗不儲存發數)+ **`npm run bal` 全綠**(⑦c 機種交叉最敏感:壓縮把全部輕武器重新落到模型的步進格點上)+ `npm test`(**射速一改,任何寫死秒數的測項都會靜默失效** —— 彈夾上限那一段的步進與追加發數 MUST 由解析後的 `wl.rate`/`wl.reload` 推導)+ `audit_aoe_trim.mjs`/`audit_weapon_gate.mjs`/`audit_shield_counter.mjs`/`audit_flight_power.mjs`/`audit_gpu_lifecycle.mjs`(dmg 階梯與彈體池都動到)+ 真機冒煙(**機槍扣一次扳機吐出三發連續曳光、傷害只結算一次;整條彈鏈聽起來/看起來與壓縮前一樣密**;準星上踢與鏡頭震動的節奏與舊制相同;敵方與 bot 的機槍在我畫面上同樣是連發;重武器手感逐位元不變;彈夾數字每扣一次扳機只減 1)|
| 機體移速壓縮(`SPEED_COMP.K`・`speedMid`/`spdComp`/`heroMobility`/`evasionMinSpeed`・任一機種 `UNITS[kind].speed`·`fly`・任一角色 `mods.speed`・`game._mobility` 與其三個呼叫點・`bots._speed`・`charPreview.topSpeed`・`sim._dodges`/`duel.dodgeP`/`lanesim.dodgeP`) | `audit_speed_comp.mjs`(30 項;Ⅰ 曲線 —— 全值域嚴格遞增、任一對的比值都不放大、中點是不動點、壓縮後**幾何中點逐位元不動**、對整體縮放齊次;Ⅱ 中點推導不手寫且取樣面含變形者飛行;Ⅲ 現役 32 台 —— 逐對複驗排序、全距比值 = 舊比值 ^ K、機種階梯不翻、**閃避門檻同步壓縮且「誰在門檻之上」逐位元不變**;Ⅳ 取速唯一縫 —— `_mobility` 是 heroMobility 的轉呼且三個移動端全走它、game/bots 全檔不得再讀機種基準速、門檻只有一份;Ⅴ K = 1 逐位元回到舊制)+ **反向驗證**(K 改回 1 ⇒ Ⅰ/Ⅲ 紅;把地面移動改回 `u.speed` ⇒ Ⅳ 紅)+ **`npm run bal` ①④⑤⑦ 四條一起看**(移速同時牽動 EVASION 與 MOB_BUDGET;④ 的變形者剩餘 EHP 是 K 的下界守門值)+ `npm test`(bot 沿兵線推進 MUST 仍綠)+ `audit_aoe_trim.mjs`(`mobDmgF` 的中點跟著漂)+ `audit_bot_vision.mjs`/`audit_bot_tactics.mjs`(bot 的位置寫入唯一縫)+ 真機冒煙(**機甲明顯比改制前跟得上無人機、無人機仍是最快的那一台**;變形者飛行型態仍快於自己的地面型態;蓄力跳的水平距離跟著機體速度走;bot 的推線速度與真人同機體一致)|
| 圖鑑六角能力圖(`HEX_AXES`/`heroHexStats`/`hexBand`/`HEX.FLOOR`/`strikeAreaM2`/`zoneAreaM2`/`weaponCycleS`/`weaponDps`・`main.heroHexHTML`/`hexPt`/`hexPoly` 與其兩個消費端・`style.css .hx-*`/`.cd-hex*`・`help.js UI_TIPS.charHex`・任一角色的 `mods`/武器數值) | `audit_hex_stats.mjs`(58 項;Ⅰ 六軸定義 —— 恰 6 軸、**耐久逐角色 = 裝甲 HP + 護盾**、舊 `BAR_MAX`/`heroStatCells` 不得復辟、機動/護甲/火力逐角色與唯一縫比對、說明只在 help.js 且六個軸名全講得到;Ⅱ 持續 DPS 唯一縫 —— `weaponDps`/`weaponCycleS` 各一份,`buildDps` 與 `balance.mjs` 都吃它、兩檔都不得再有手寫 `const cycle`;Ⅲ 制域 —— 四類足跡幾何逐條直測、分類走 `aoeClass`、單體直擊足跡 > 0、射程加倍制域加倍、現役 32 台全 > 0;Ⅳ 正規化 —— `hexBand` 逐軸比對 min/max、f ∈ [FLOOR,1]、每軸都有人貼外框也有人在內圈、保序、對數尺度等比等距、未知角色不炸;Ⅴ **執行 main.js 原文**產生 SVG —— 六頂點、逐頂點半徑 = f × R、第一軸在正上方且順時針、格線與軸線齊全、**標籤含數值那一行都收在 viewBox 內**(溢出會壓到下面的說明)、不同機種形狀明顯不同、只用 class 上色、說明走 ⓘ 懸浮提示、每個 class 都有 CSS)+ `audit_shield_counter.mjs` Ⅵ(`buildDps` 改吃 `weaponDps` 之後那條「同式」斷言改釘單一縫)+ `audit_ui_layout.mjs`/`audit_ctrl_mode.mjs`(說明分頁「介面」類多一則)+ `npm run bal`(㋒ 純表現層 + DPS 只是抽成同一支函式 ⇒ 七不變式 MUST 逐位元不動)+ `npm test`(㋒)+ 真機冒煙(角色卡與放大視窗都畫出六角圖、六軸名稱與數值都看得到;切不同角色形狀跟著變(扇形武器的制域頂到外框);滑鼠移上 ⓘ / 觸控長按看得到六軸說明,說明分頁「介面」類有同一則;蜂群/鋼鐵兩側的圖形配色跟著陣營色;手機直式不出現橫向捲軸)|
| 開火中位移懲罰(`RECOIL` 的 `MOVE_K`/`DECAY`/`END_RAD`/任一階 `climb`・`RECOIL_CLIMB_MAX`/`recoilMoveF`・`game._recoiling`/`_recoilMoveF` 與其兩個消費端(地面移動鏈 / 飛行推杆鏈)・`_tryFire` 的係數定案段・`_updatePlayer` 後座回穩段・陣亡與換座機清帳・`main.charWeaponRow` 的「射擊移速」)| `audit_recoil_move.mjs`(66 項;Ⅰ 曲線 —— `MAX` 推導不手寫(定義式內無數字字面值)、最大後座**嚴格 0** 且只有那一階歸零、無後座恆 1、六階 climb 遞增 ⇒ 係數遞減、連續掃描單調、`MOVE_K = 1` 逐點 = 正比、任一 K > 0 端點不動、舊制 `move`/`slowF` 兩欄與原文殘留雙向清查;Ⅱ 單一縫 —— 係數只在 `_tryFire` 定案、`_recoilMoveF`/`_recoiling` 各一份實作各兩個消費端、`END_RAD` 只在 `_recoiling`、回穩吃 `RECOIL.DECAY` 且無手寫 7、**定案排在 `recoil.p +=` 之前**、擊退不吃係數、舊計時器欄位全清;Ⅲ 行為直測(執行 `_recoiling`/`_recoilMoveF` 原文 + 真的回穩曲線)—— 擊發當下歸零、飛行吃 `AIR_F`、**全程 0 直到後座結束才恢復全速**、時長 = 閉式解 `ln(climb/END_RAD)/DECAY`、60fps 與 240fps 一致、後座越大撐越久、連射把窗推回滿載;Ⅳ 取較嚴者(直接執行 `_tryFire` 那段賦值原文)—— 結束後取新值 / 未結束取較嚴 / 較輕的不解除重砲 / 冪等 / 首發不炸;Ⅴ 現役 32 角係數 ∈ [0,1]、歸零只出現在重武器、同槽位「高 ≤ 中 ≤ 低」、圖鑑由 `recoilMoveF(recoilTier(…))` 推導)+ `audit_fire_rate.mjs`(`recoilTier` 分級仍吃 `rate0`)+ `audit_view_lock.mjs`(**後座力仍不得被鎖定抵銷**)+ `npm test` / `npm run bal`(㋒ 純客戶端手感,伺服器不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**手持最大後座重武器邊走邊開一砲:人當場定住,約半秒後準星回穩才又能走**;機槍連射時走得動但明顯變慢、停火後恢復;低後座輕武器只有輕微拖慢;無人機在空中的同一發只慢一半;打完一發重砲立刻切輕武器補槍,那半秒的定身**不會**提早解除;重砲的向後擊退照樣把人推開;陣亡重生後第一步就是全速)|
| 前線交戰模型改動(`tools/lanesim.mjs` 的 `blastHits`/`castAbil`/`stepAbils`/`dropBomb`/`detonate`/`ABIL_KIND`/`abilReach`・`laneMatrix` 的 `abil` 分桶・`balance.mjs` ⑦f) | `npm run bal` ⑦ 全段(**b 的三條單軸自驗是模型有沒有壞掉的哨兵**;改了絕招那半 MUST 確認 `dmg`/`aoe` 兩軸仍 > 50%)+ `audit_aoe_trim.mjs` Ⅴ(場景全推導 + 三類範圍幾何直測)+ `npm test`(㋒ 離線模型,e2e MUST 仍全綠)|
| 爆風家族帶 / 榴彈類短射程(`BLAST_BAND`・`blastFamily`/`blastCapR`・夾制迴圈的仿射映射・`AREA_WEAPONS` 名冊・任一 lob/guide/fnf 武器的 `range`) | `audit_aoe_trim.mjs`(73 項;Ⅰ-b 新增:家族分組/上限推導不手寫、`blastCapR` 全檔恰一個呼叫點、導引的上限/下限/幾何中點三個端點都嚴格小於榴彈、每一族頂階恰好貼齊自己的家族上限、底階 = 上限 × `LO`、**家族內排序保留**(授權半徑越大 ⇒ 定案半徑越大)、榴彈類射程 = 全體重武器最短那一帶、導引類射程恆長於榴彈類)+ **反向驗證**(`GUIDED_F` 改回 1 ⇒ 家族分離三條紅字;名冊復辟一筆豁免 ⇒ 凍結清單 + 帶的上緣紅字)+ `npm run bal` ⑦a(逐把實算「一發打不到兩座塔」)與 **①④⑤ 一起看**(範圍換火力的補償會位移單體模型)+ `npm test`「攻擊範圍收斂」段 + `audit_weapon_gate.mjs`(**核心帶變動 ⇒ MUST 重掃 `SEEK.R_M`**)+ 真機冒煙(對著同塔位兩座塔中間丟榴彈/溫壓火箭/152mm/集束子母彈,**現在一律只有一座掉血**;榴彈類明顯要走得更近才打得到,而雷射導引/射後不理的爆風圈比以前大一圈、但仍看得出比榴彈小)|
| 對建築 DPS 收斂(`BUILD_DPS`・`buildDps`・收斂迴圈)或 任一武器的 `vs.building` | `audit_shield_counter.mjs` Ⅵ(K 值域 / 回填有界 / `buildDps` 單一縫且與 balance.mjs 的 `slotDps` 同式 / `vs.building` 只有一個寫入點 / **逐槽位離散度守門線**(light ≤ 2.0×、heavy ≤ 3.2×)/ **與「中性 vs.building」對照組比較**(收斂後 MUST 更緊 —— 只釘絕對倍率的話量不到這一段有沒有真的在做事)/ 滿級拆塔 DPS 的離散度與水位)+ `npm test`「對建築 DPS 收斂」段 + **`npm run bal` ④ 是主要校準錨**(只壓縮不回填水位 ⇒ 機甲/變形者一律推不掉兩座塔)、⑤ 逐角色離群一併看 + 真機冒煙(**任兩把重武器輪流拆同一座塔,秒數不再差到一個量級**;反器材/微波炮這類過去「打建築像沒傷害」的武器,血條看得出在掉)|
| 攻擊範圍收斂 / 三軸預算(`AREA_WEAPONS`・`towerPairSepM`/`soloBlastRmax`/`blastFootprintR`・夾制迴圈・`AOE_BUDGET`/`areaValue`/`aoeTrimRaw`/`aoeTrimF`・`MOB_BUDGET`/`mobMid`/`mobDmgF`・`RANGE_BUDGET`/`rangeMid`/`rngDmgF`・`heroRange`・`heroWeapon` 的 dmg 欄・任何爆炸型武器的 `r`・`GAME.TOWER_SIDE_OFF`・`TARGET_R.tower`・`BLAST`・`tools/lanesim.mjs`)| `audit_aoe_trim.mjs`(58 項;Ⅰ 幾何上界推導不手寫 + **上界是臨界值**(等於上界打不到、+1mm 就打得到)+ 夾制迴圈單一縫、Ⅱ 逐把武器**實算**「打不打得到兩座塔」(Lv1~Lv4 全掃)+ 名冊凍結清單雙向驗(名冊內的 MUST 仍打得到)+ fan/line 逐位元不動、Ⅲ 補償幾何平均 = 1・NORM 是 derive・未夾過恆 ×1・讓出越多拿越多・階梯形狀保留・頂階含 tierVal 外推 ≤ 上界、Ⅳ 機動/射程中點推導 + 單調 + 逐槽位取中點 + 三個係數各恰 2 處(定義 + heroWeapon)+ 招式不吃、Ⅴ lanesim 場景全推導 + 三類範圍幾何行為直測(blast 足跡內外、**fan 錐寬隨距離張開**、line 逐一衰減)+ 終局理由/採購清單)+ `npm run bal` ⑦ 全綠(**①④⑤ 也會位移**:範圍在那三條裡從來不進算式 ⇒ 補償若變成純加法會立刻在 ① drone 剩餘率與 ⑤ 射程壓制上炸開)+ `npm test`「攻擊範圍收斂」段(7 項)+ `audit_weapon_gate.mjs`(**核心帶縮小 ⇒ MUST 重掃 `SEEK.R_M`**,Ⅵ⑥ 逐把導引武器要落進核心帶)+ `audit_shield_counter.mjs`(傷害鏈比對 MUST 走全鏈,只比 `counterDmgF` 會假紅字)+ 真機冒煙(對著同塔位兩座塔中間丟榴彈,只有一座掉血;溫壓火箭/152mm/集束子母彈仍兩座一起掉;導引飛彈/巡飛彈仍打得中 —— 爆風核心帶變小了,沒命中就是 `SEEK.R_M` 沒重掃)|
| 建築加乘 / 護盾分軌剋制(`BUILDING_VS_CAP`/`VS_DEFS` 夾制段・`shieldSplit`/`shieldRoleName`/武器欄 `vsSp`·`vsHp`·`spPierce`・`EX_SIEGE_WEAPONS`/`COUNTER_BUDGET`/`counterLoad`/`counterDmgF`・`sim._damage` 的 `wd` 參數與兩個分支・`sim._heroDmg`・`game._hitFeedback`/`_lanceFeedback` 估算・`duel.apply`/`shieldEqF`・`balance.slotDps` 與對塔 DPS・`main.charWeaponRow` 標籤) | `audit_shield_counter.mjs`(71 項;Ⅴ = 三條配置紀律逐一反查(名冊/獨占夾制/折減),含「反護盾不受名冊限制」的豁免與「未掛旗標者折減恆 ×1」;Ⅰ 逐張 vs 表掃「加乘全刪、懲罰保留」+ 解析後仍夾住 + 夾制是唯一一段推導迴圈 + 五個檔案原文無 `grenadeBuildingMul` 殘留 + `_heroDmg` 恆無情境倍率、Ⅱ 單一縫:四個消費端都吃 `shieldSplit`・sim 全檔無第二處手寫護盾拆分・消費端不直接讀旋鈕、Ⅲ 行為:中性參數**逐位元同舊制**・溢出按預算折回(反例值一併釘死)・穿盾保底・`vsSp=0` 退化成盾全擋・雙旋鈕單調性・標籤死區、Ⅳ 真 `BattleSim._damage` 直測含「NPC 也吃 vsHp、但不受 vsSp/spPierce 影響」)+ `npm run bal`(**四不變式會位移**:拆塔 DPS 少掉建築加乘 ⇒ ④ 的剩餘 EHP 邊際變薄、drone 站外攻堅秒數上升;⑤ 逐角色勝率隨新旋鈕漂移,新掛旗標的角色 MUST 仍在 20~80%)+ `npm test`(「建築加乘移除 + 護盾分軌剋制」段 13 項 + **拆堡逾時上限已放寬**:同一發打主堡剩約 1/2.8)+ `audit_bot_tactics.mjs`(`_damage` 早退區塊改名 ⇒ 記帳斷言同縫)+ `audit_blood_splat.mjs`/`audit_flight_power.mjs`(`_damage` 是 `_hurtLog`/`_botAirSink` 的共用縫)+ 真機冒煙(反護盾武器打滿盾敵人護盾條掉得飛快但血條幾乎不動、盾破後換輕武器才磨得動;穿盾武器對滿盾敵人一定見血;圖鑑武器列顯示「反護盾 / 穿盾 / 反裝甲」標籤與倍率;**任何武器拆塔都不再有攻城加速**)|
| 角色機種編制(`CHARACTERS[].kind`/`visual` 機體欄/`mods.armor` 換機種) | e2e「機體混編陣營分佈」(三陣營 7/3/2・3/7/2・2/2/4 + 32 名皆有 kind + 12/12/8 款設計 1:1)+ `npm run bal` ①(換機種 = 換 EHP 池與 `UNITS.drone.hp`/`SQUAD.ARMOR_F` 推導)+ ⑤(換機種 = 換 `CLASS_SYM` 分組;⑤a 量真正的陣營成員)+ `audit_muzzle.mjs`/`audit_cockpit.mjs`/`audit_cast_jump.mjs`(㋓ 需真瀏覽器:掛點、座艙、施法/跳躍動作全依 `visual` 重生成)|
| 出兵節奏 `GAME.WAVE_S` / 波次編制 `waveComp` / 開場預置 `_prefillLanes`・`_spawnLaneWave` | `npm run bal` ③(波數 × 波賞金 = 收入預算,±10%)+ e2e「出兵間隔固定 + 開場預置兵線」(6 項;間距 = 間隔 × 行軍速度、最前一波 ≤ 第一座砲塔、各波等距、波序為負)+ e2e「開局兵線」隻數(期望值 MUST 由同一份規則推出,不可手寫) |
| 陣營小兵強化(`CREEP_UPG`/`creepUpgMul`/`_creepMul`/`_bounty`/`buy(…,'creep',lane)`) | e2e「陣營小兵強化」(11 項;曲線 1+log10(1+LV)、八軌未滿不解鎖、非法兵線被拒、共用/分兵線、hp・armor・賞金同吃 `e.cu`、快照帶等級)+ `npm run bal` 四不變式 MUST 不動(bal 模型是**未強化**的基準波次)+ 真機冒煙(八軌買滿商店才出現該區塊、隊友出資同陣營都看得到播報) |
| 角色大小招 `fx`/`add`(招式家族配置) | bal ⑥:雙扇形 MUST 兩招貼身、單扇形 ≥1、密度 ≥ 非扇形 ×2;s07/m07 具名豁免 MUST NOT 為湊標換掉 |
| 對進戰模型(`duel.mjs`)/`ALTITUDE.*`/`FAN_*`/`CLASS_SYM.K` | bal ⑤:陣營與機種 50±5pp、**較高方 50±3pp**、非豁免角色 ∈ 20~80%、接近期損失 ≤40%。改 `K` 一併看 ①(校準值 0.5) |
| `SPECIAL`/`SPECIAL.DECOY_IMPACT`/`KAMI.N`/`DECOY.BOMB_MAX`/`hyperBlast`(絕招預算) | e2e「機種絕招三招同預算」(飽和攻擊/集束炸彈/主機自毀/極音速飛彈四者 ≤2%)+ **`npm run bal` ⑦f**(名目等值 ≠ 實得等值:切幾顆會同時動到半徑(`specialBlastR`)與「被打下來還剩多少」)+ bal ①④⑤ MUST 不動(那三條刻意不含三招 burst) |
| 三種載具形式(`SQUAD.KAMI.N`・`kamiSide`・`HYPER`/`hyper*()`(含 `KAMI_EQ`/`hyperShare`/`LAUNCH_DEG`/`TRACK_R_F`/`hyperArcY`/`hyperClimbVx`/`hyperClimbS`/`hyperTrackR`/`hyperClimbLen`/`hyperTerminalF`/`hyperFlightS`)・`DECOY.BOMB_*`/`DROP_N`・`sim._tickHypers`(含 `m.chase` 判定 —— **2026-08-06 起是死碼**,見 §2.1)/`_hyperBoom`/`_hyperShotDown`/`_decoyBomb`・`tools/lanesim.mjs` 的 hyper 分支・`game._spawnDecoyBomb`/`_tryFire`/`_updateEnts` 的飛彈姿態段) | `audit_flight_power.mjs` Ⅰ・Ⅱ(HP 三式推導不手寫、**出膛角以數值微分驗真品 = `LAUNCH_DEG`**、拋物線只有 `hyperArcY` 一份實作、爬升水平等速、頂點在目標正上方、螺旋基底不由彈道軸現算、切分計價的兩招總覆蓋面積相同、極音速飛彈是具名例外(傷害 = 2.5 架自爆無人機且逐等級成立、半徑 = 砲塔射程 2/5、兩者原文皆推導不手寫)、`kamiSide` 只剩伺服器一個消費端且客戶端常駐護衛機零殘留、巨砲旁路整組退場、`_tryFire` 三道閘門無條件生效;**終端追擊**:判定半徑推導不手寫、「後 1/3」是與交戰距離無關的推導比例、判定只做一次且掛在既有頂點切換點、前 2/3 不讀即時位置、伺服器/lanesim 各恰一個消費端、客戶端不參與、曝險窗仍取原軌跡)+ **反向驗證**(爬升段改回無條件跟蹤 ⇒ Ⅱ 紅;判定半徑手寫成 77.5 ⇒ Ⅱ 紅)+ e2e「極音速飛彈」段(機種閘門、射後不理不需狙擊/不吃彈夾、**45° 出膛 + 拋物線爬到頂點 + 頂點在目標正上方**、落點爆風、被擊落不引爆;**終端追擊** 8 項:前 2/3 頂點仍在初始地點正上方、超出判定半徑保持原軌跡且目標毫髮無傷、界內轉螺旋追擊、追上之後不中途折回、界內外各驗一次、半徑與比例推導)+ **`npm run bal` ⑦f**(三招實得比 MUST ≤ 1.8× 且全數 > 0;改 `KAMI_EQ` MUST 回頭看這一項(實測 2026-08-06:改制前 155 EHP/次(1.63×)→ **102 EHP/次(1.09×)**,火力減多少就等比少交付多少;中途試過的「半徑 = 砲塔射程 2/5」版本是 197 EHP/次(2.38×)—— 半徑一放大就蓋得住同塔位雙塔,總量反而升,已回退);這一招的實得傷害對「追不追得到」與「打不打得下來」都很敏感:2026-08-05 實測 基準 1.65× → 只加追擊判定 1.63× → 曝險窗也改吃斜距 **1.93× 出界**)+ e2e「集束炸彈」段(HP/彈數推導、逐顆個別瞄準、事件帶投擲點、墜毀補投)+ e2e「飽和攻擊」段(4 架、`kamiHp` 推導、左右對半散開、每架殺不死步槍兵兩架才行)+ e2e「載具 HP 一律由一座砲塔反解」段 + 真機冒煙(**飛彈以 45° 斜射出去、畫一條拋物線飛到目標上空,再近乎垂直地螺旋撲下**;全程看得見/打得下來、機首跟著彈道抬頭與低頭;集束炸彈拋物線分別落在不同敵人頭上、4 架護衛機左右各兩架散開;**平時無人機身邊沒有任何小機,長按右鍵那一刻才冒出四架衝出去**;一發飛彈不再把滿血機體直接轟掉,而爆風圈與改制前一樣大) |
| 招式載具的發射點與槽位 CD 帶(`abilOrigin`/`abilDelivered`/`abilCdMapped`/`abilCdBand`/`abilCdRange`/`abilCarrierCd`/`ULT_CARRIER.SK_CD_*`/`ultLaunchLegM`/`abilLaunchLegM`/`hyperMaxArcM`/`hyperFlightS(d)`・`heroAbility` 的 carrier·support·cd·range 欄・`sim._launchOrigin`/`_launchUltCarrier` 的 org 參數/`_launchUltSupport` 的 org·slot 參數/`_tickSupport` 的 deploy 分支/`_supArm` 的 `g.until ??=`/`h.hypers`·`sq.decoys`·`sq.sups` 名冊・`game.js` 的 decoy/hyper 播報・`lanesim` 的 `ownFort`) | `audit_ult_carrier.mjs`(83 項;Ⅳ 新增 = 64 招逐一重算分類、每招恰一種形式、有 range 的小招走點遞送且**保留 range**、小招不補 `hyperRange` 預設、小招 CD 帶 [15,30] 端點貼齊 + 嚴格保序、兩帶不重疊、自身型大招 cd 仍不壓帶、`abilOrigin` 單一縫 + 兩支發射縫都不自己判 slot、`ultLaunchLegM` 推導不手寫、投放腿逐槽位推導、`hyperHp` 吃最長航程;行為直測 = 三種載具形式的大招都自最近工事出發 / 小招自主機身邊出發 / 小招輔助機就位才上線且打光即下線 / 兩個槽位的轟炸機同時在空且都被回收)+ **反向驗證 `--break-origin`**(發射點退回「一律主機自己」⇒ Ⅳ 紅)+ `audit_self_ult.mjs` Ⅵ(小招那一組的分類/機數/節奏/耐久四支全吃 slot、大招那一組逐位元不動、小招投放腿短於大招、補償只給大招)+ `npm test`「小招也是輔助機型模式 + 大招自最近的砲塔/主堡召喚」段 + **`npm run bal` 全綠**(⑦f 的載具交付率會位移:2026-08-07 實測 kami 90.2→89.2% / 飛彈 81.7→**92.0%**(HP 隨曝險窗漲)/ 轟炸機 50.1→51.8%,比值 1.80→1.78×;自身型組 9 台仍全數 > 0)+ 相鄰稽核 `audit_flight_power`(Ⅱ 的最長航程/代表發射腿)/`audit_weapon_gate`/`audit_fire_rate`/`audit_shield_counter`/`audit_bot_tactics`/`audit_bot_vision`/`audit_blood_splat`/`audit_npc_collide`/`audit_gpu_lifecycle`(共用 `heroCast`/`_castEffect`/`_tickKamis`/`_tickHypers`/`_tickDecoys` 原文)+ 真機冒煙(**一般模式長按右鍵放小招:載具/輔助機從自己身邊冒出來;切狙擊模式長按放大招:載具從後方最近的砲塔或主堡升空,一路飛過來 —— 沿途看得見、打得下來**;小招 CD 明顯拉長到 15~30s;打掉飛過來的大招載具,那一份效果就沒有;機甲連放小招與大招時兩枚飛彈同時在天上、都會落地)|
| 大招載具遞送(`ULT_CARRIER`/`ultDelivered`/`ultCdBand`/`ultCarrierCd`/`ultParts`/`ultPartN`・`heroAbility` 的 `carrier`/`cd`/`range` 欄・`sim._castEffect`/`_launchUltCarrier`/`_ultArrive`・`_tickKamis`/`_tickDecoys` 的 `pt`/`uA` 分支・`_kamiBoom`/`_kamiDeathBoom`/`_hyperBoom`/`_decoyBoom` 的 uA 分支・`game._fireHoldAbility`/`_weaponHud`/`ultfx` 事件・`main.js` 招式鈕面(`abCd`)・`lanesim.castUltCarrier`/`ultDetonate`/`damage` 的 `now` 參數・`balance.mjs` ⑦f 過濾・任一角色的 `ult` 欄位(fx/target/cd/range)) | `audit_ult_carrier.mjs`(52 項;Ⅰ 轉換判定推導 + CD 帶仿射保序 + 未轉換逐位元不動 + 分批規則、Ⅱ 單一縫原文(效果分支只住 `_castEffect`、**三入口整組退場 + 三種載具唯一生成點**、四條 uA 路徑無 `_blast`、長按走 `abilHoldSlot`)、Ⅲ 真 `BattleSim` 行為直測(飛行時間 / 擊落 = 該份否定 / 效果取代傷害 / MIN_LEG / 守衛雙向))+ **反向驗證**(`--break-cd` ⇒ Ⅰ 紅、`--break-guard` ⇒ Ⅱ 紅、`--break-boom` ⇒ Ⅲ 紅)+ `npm test`「大招載具遞送」段(sim 直測)與「無人機長按 = 大招載具」段(WS:舊 kami 路徑被擋 + cast ult 發射載具 + ult 旗標)+ **`npm run bal` 全綠**(⑦f 的載具組量份額交付率;⑦c 機種交叉會隨載具大招位移,守門線內)+ 相鄰稽核 `audit_flight_power`/`audit_weapon_gate`/`audit_shield_counter`/`audit_bot_tactics`/`audit_bot_vision`/`audit_blood_splat`/`audit_fire_rate`(共用 `heroCast`/`_damage`/`_tickKamis` 原文)+ `npm run sim`(bot 對局照常分得出戰果)+ 真機冒煙(**選 t01 長按右鍵:飛彈 45° 升空、著彈點打出雪崩齊射的多發爆風;選 s02 長按:四架小機飛向瞄準點、抵達後半徑內友軍裝甲/護盾大修 —— 中途被打下兩架就只補一半;選 s03 長按:轟炸機飛抵落點、區內敵人武器離線**;HUD 不再有機種絕招格、觸控招式鈕在狙擊模式顯示大招 CD([30,60]s)、一般模式顯示小招 CD)|
| 自殺攻擊機常駐外觀退場(`SQUAD.KAMI` 檔頭・`game._spawnEnt` 的 drone 分支/`_removeEnt`/`_updateEnts` 的 isSelf 分支・`data.js` 的 `ESCORT` 系) | `audit_flight_power.mjs` Ⅰ(客戶端原文零 `escort`/`ESCORT` 殘留、`data.js` 非註解區零 `ESCORT`/`escortSlot`/`escortDrift`/`escortLag`、`kamiSide` 只剩伺服器生成側偏移一個消費端且舊制 `i === 0 ? -1 : 1` 仍不得復辟)+ **反向驗證**(在 `_spawnEnt` 塞回一行 `ent.escorts = []` ⇒ Ⅰ 紅)+ `audit_gpu_lifecycle.mjs`(`_removeEnt` 少掉一組 mesh 回收 ⇒ A25 生命週期 MUST 仍全綠)+ `audit_npc_collide.mjs`/`audit_spectator_cam.mjs`/`audit_view_lock.mjs`(`_updateEnts` 原文抽取的相鄰稽核)+ `npm test`(㋒ 純表現層,MUST 仍全綠)+ `npm run bal` 不動(不模型化外觀)+ 真機冒煙(**平時繞到敵方無人機旁邊,身邊一架小機都沒有;長按右鍵那一刻才冒出四架衝出去**;自爆後 CD 期間與 CD 結束都不會有任何機體「重現」在它兩側;切自機視角 / 切回他人視角都不會冒出殘影)|
| 招式手勢 + 純自身型大招補償(`abilHoldSlot`・`SPECIAL_CD_S`/`SELF_ULT`/`selfUltEq`/`selfUltBoost`/`selfUltDps`・`heroAbility` 的 `regen`·`cleanse`·`revive`·`brk`・`sim._castEffect` 的 rally/recon/overdrive/alpha 分支/`_reviveBody`/`_breakOnHit`/`_gateFire` 的免裝填與破隱窗/`_altRange` 的 `range` buff/`SQUAD_SHARED` 三欄・`game._fireHoldAbility`/`_weaponHud`・`main.charAbilityRow` 的 `MUL_LABEL` 與 `.cd-boost`・`lanesim.castSelfUlt`/`ufAt`/`LANE.HEAL_FRAC`・`balance.mjs` ⑦f・任一自身型角色的 `ult` 欄位) | `audit_self_ult.mjs`(102 項;Ⅰ 當量推導 —— 載具化角色恆 0、逐台獨立重算比對、正比於大招 CD、定義式無手寫倍率、`SPECIAL_CD_S` 三招同值;Ⅱ 兌現形式與夾制 —— 逐 fx 走對通道、重新設計的三台不再疊乘數、治療 1:1、**頂到上限的逐台印出來**、夾制可達;Ⅲ 單一縫原文 —— boost 只在 `_castEffect` 取一次、客戶端零計算、lanesim 只取一次、增額是相加、破隱窗開在 `_gateFire`、免裝填排在填彈計時器之前、三欄進 `SQUAD_SHARED`、`_breakOnHit` 只有一個呼叫點、`ccImm` 三條施加路徑都判;Ⅳ 真 `BattleSim` 行為直測 —— rally 復活/免疫/不救已回場者、overdrive 免裝填 + **在時窗中段**驗挨一發即撤銷(在時窗尾端問「還在不在」恆為否 = 假綠)、recon 射程加成走 `_altRange` 單一縫、alpha 開火才開窗、s11 治療 = 基礎 + 補償)+ **反向驗證**(`--break-eq` ⇒ Ⅰ 紅 / `--break-alpha` ⇒ Ⅲ・Ⅳ 紅 / `--break-brk` ⇒ Ⅲ・Ⅳ 紅)+ `audit_ult_carrier.mjs`/`audit_flight_power.mjs`(機種絕招退場的兩端)+ `audit_weapon_gate.mjs`/`audit_fire_rate.mjs`(`_gateFire` 共用縫)+ `audit_hex_stats.mjs`/`audit_ui_layout.mjs`/`audit_ctrl_mode.mjs`/`audit_touch_layout.mjs`(圖鑑列與鈕面字)+ **`npm run bal` ⑦f**(自身型組 9 台 MUST 全數 > 0;載具組交付率 ≤ 1.8×)+ `npm test`+ 真機冒煙(**一般模式長按右鍵放的是小招、切狙擊模式長按放的是大招,兩者與 Q/E 同一份冷卻**;十字鍵左的鈕面字是「招式」且冷卻跟著模式換;s11 大修的修復量比改制前明顯多、圖鑑那一行括號標著「含補償 +280」;t02 超載期間彈匣打不完、挨一發立刻恢復正常裝填;m08 匿蹤後開的第一槍傷害暴增約 1 秒;s12 大招把倒數中的隊友原地拉起來;m04 大招期間射程與跑速明顯拉高)|
| 自身強化型大招的輔助機隊(`ULT_SUPPORT`・`kindParts`/`supportStackable`/`supportN`/`supportF`/`selfUltTempo`/`supportTempoF`/`supportSpeed`/`supportLegS`/`supportServiceS`/`supportHp`/`supportFleetHp`・`heroAbility` 的 `support` 欄・`sim._launchUltSupport`/`_tickSupport`/`_supArm`/`_supSync`/`_supRevoke`/`_supLost`・`_castEffect` 的 `frac`(`mf`/`vf`)與 `once` 守衛・`heroCast` 的 support 分流・`_kill` 的 supG 分支・`lanesim.ufFrom`/`supSync`/`supDeliver`/`stepAbils` 的 sup 分支・`balance.mjs` ⑦f 的輔助機損失列・任一自身型角色的 `ult` 欄位) | `audit_self_ult.mjs` Ⅴ(Ⅴ-a 分類與機數推導不手寫且「某些招式換成多個」雙向成立;Ⅴ-b **疊加是加法**:全員在線份額恆 1 = 舊制效果值、份額單調、夾在 [0,1];Ⅴ-c 耐久 —— 節奏係數兩端是定義、`PULSE_F ∈ (0,1)`、同 dur 持續 > 間斷 > 瞬發、dur 越久越硬、逐台以獨立重算比對、**投放腿平行不除以機數**(除下去瞬發型只剩一顆子彈的量);Ⅴ-d 單一縫原文 —— `_launchUltSupport`/`_supSync` 各 1 定義 + 呼叫點、`supportHp` 只在生成處取一次、客戶端不自算耐久/機數、撤下再放 + until 改寫 + 全滅只撤不放、**投放腿推進排在到期判定之前**、`once` 守衛齊全;Ⅴ-e 真 `BattleSim` 行為直測 —— 生成/HP/armor 0、投放中加成未上線、全員就位逐位元同舊制、擊落一半剩一半、全滅整份下線、瞬發型抵達即交付且擊落一架少一份、匿蹤被擊落即現形、時窗不展期、一次性效果不重放、點遞送 23 台不受影響)+ **反向驗證**(`--break-stack` 疊加改成相乘 ⇒ Ⅴ 紅;`--break-tempo` 節奏係數全 1 ⇒ Ⅴ 紅)+ `audit_ult_carrier.mjs`(kami 改成**恰兩個具名生成點**;`--break-cd`/`--break-guard`/`--break-boom` 仍 MUST 咬得住)+ `npm test`「自身強化型大招 = 跟隨玩家的輔助機隊」段 + **`npm run bal` ⑦f**(自身型組 9 台 MUST 全數 > 0;「輔助機損失」那一行 MUST > 0 —— 恆 0 = 模型裡根本沒被打過,⑦f 的自身型欄就仍是舊制的瞬發值)+ 相鄰稽核 `audit_flight_power`/`audit_weapon_gate`/`audit_fire_rate`/`audit_shield_counter`/`audit_bot_tactics`/`audit_bot_vision`/`audit_blood_splat`/`audit_npc_collide`/`audit_gpu_lifecycle`(共用 `_castEffect`/`_gateFire`/`_tickKamis`/`_kill` 原文)+ 真機冒煙(**狙擊模式長按右鍵:身邊冒出數架小型輔助機,繞著自己飛;加成要等它們就位那一刻才上線**;打掉對方的輔助機,對方的加成當場變弱(打光就整份消失);m08 的輔助機被打下來的瞬間對方現形;s11 的四架各補四分之一,半路被打掉幾架就少補幾份;t02/t04 那種單機型只有一架,打掉它超載立刻結束)|
| 絕招載具 HP 校準 / 爆風面積計價(`towerDps`/`towerSurviveHp`/`towerKillHp`・`TOWER_SITE_N`/`frontDps`/`frontSurviveHp`/`frontKillHp`・`waveDps`/`overflyDps`/`overflySurviveHp`・`kamiHp`/`hyperHp`/`decoyHp`・`specialBlastR`/`hyperShare` 與四個 `*Blast()`・`UNITS.tower.dmg・rate・range`・`waveComp`) | `audit_flight_power.mjs` Ⅰ(改砲塔數值三個 HP MUST 自動跟著漂;任一個手寫即紅字 + 切分計價的兩招總覆蓋面積逐位元相同 + 極音速飛彈的具名例外(2.5 架份 / 砲塔射程 2/5)+ 舊逐招半徑常數與 `HYPER.BLAST_R` 已退場)+ e2e「載具 HP」段(前線基準 = 2 座塔、飛越基準 = 塔位 + 一波兵、三招面積相同)+ **`npm run bal` ⑦f**(三招實得傷害比 MUST ≤ 1.8× 且全數 > 0 —— 這是唯一量得到「某一招在有塔的前線根本沒兌現過」的地方)+ ⑦c 機種交叉 MUST 仍在守門線內(HP/半徑一動,三機種的勝率會整組位移)—— **改砲塔任一數值或波次編制 MUST 重跑全部**(三招的「剛好」全掛在前線火力上) |
| 飛行動力學(`FLIGHT`/`airSinkM`/`liftMax`/`liftRegen`/`liftDrainPS`・`game._stepLift`/`_airSinkHit`/飛行段消化/`#liftBox`・`sim._botAirSink`) | `audit_flight_power.mjs` Ⅲ~Ⅵ(65 項;校準錨 = 打完平均護盾+裝甲掉 2 個砲塔高、掉幅是位移不隨幀率/分幾發變、5 秒耗盡且耗速 ∝ 爬升率、見底只鎖垂直、回充 ∝ 電力回速 × 充能軌、清帳點齊全)+ e2e「bot 飛行機體受擊掉高」(4 項)+ `npm run bal` 不動(bal 不模型化飛行高度)+ 真機冒煙(被集火時高度一路被壓下來、動力條見底爬不上去但仍可平飛/下降、地面機甲看不到動力條) |
| `BOT_DIFF`/`BOT_OPS`/`bots.js _op()` | e2e「電腦難度操作節奏」+ `npm run sim` + 沙包輸出 MUST 隨難度單調遞增 |
| 電腦玩家學習策略(`botPolicy.js`・`data.js BOT_TACTIC_BASE`/`BOT_LEARN`/`botPolicySanitize`/覆寫迴圈/`balanceFingerprint`・`bots.js this.tac` 讀取縫・四支 helper 的 `T` 參數・`tools/bot_learn.mjs`・`test/simrun.mjs buildConfig` 匯出) | `audit_bot_policy.mjs`(40 項;Ⅰ 中性不變式 —— 空 policy ⇒ BOT_TACTIC 逐位元同凍結基準、非白名單鍵恆不動、垃圾輸入一律取基準;Ⅱ 夾制與白名單 —— 邊界收在 audit_bot_tactics 守門線內、交叉約束(W_THREAT 恆最重/KITE 近<遠)對任意輸入成立、使用者定案值與 THREAT_S 與能力欄不可學;Ⅲ 單一縫原文 —— bots.js `BOT_TACTIC.` 零殘留、夾制/覆寫各恰一份、botPolicy.js 零 import、bot_learn 不手寫邊界不抄戰場;Ⅳ 真 BotBrain 注入直測;Ⅴ CRN 決定性 + 中性適應度恆 0 + 指紋穩定)+ **反向驗證**(`--break-clamp` ⇒ Ⅱ 紅、`--break-neutral` ⇒ Ⅰ 紅)+ `audit_bot_tactics.mjs`/`audit_bot_vision.mjs`(讀取縫升級後 MUST 仍全綠)+ `npm test`(bot 沿兵線推進)+ `npm run sim` + `npm run audit:net`/`audit_solo_boot.mjs`(data.js 多一支 import 的客戶端模組)+ `npm run bal`(中性策略逐位元同舊制 ⇒ MUST 全數不動;**學習輪落地後**四不變式仍 MUST 全綠 —— 學的是 AI 取捨,不是平衡)+ 真機冒煙(高難度 bot 行為與策略檔一致:落地新策略後打帶跑距離環/撤退時機看得出變化;`--reset` 後回到改制前手感;新手/低難度前後完全一樣)|
| 電腦玩家定位分類與策略(`data.js BOT_ROLE_FEATS`/`BOT_ROLES`/`BOT_ROLE`/`BOT_BUY_ORDER`/`botRoleFeats`/`botRoleScores`/`botRoleOf`/`botRoleRoster`/`botRoleNorm`/`botRoleMul`/`botRoleTactic`/`botBuyOrder`/`botTacticCross`・`BOT_TACTIC` 的 `KEEP_F`·`KEEP_STRUCT`·`PRIO_HERO`·`PRIO_STRUCT`·`CAST_HURT`・`bots.js _resolveRole`/`_acquire` 的類別折算/`_engage` 的距離環/`_castSupport` 的血線/採購迴圈・任一角色的武器/機體/招式數值(分類會跟著位移)) | `audit_bot_role.mjs`(80 項;Ⅰ 分類 —— 七項特徵**獨立重算**逐角色比對(不呼叫被測的那一支去驗自己)、五條機體特徵 MUST 是 `HEX_AXES` 那一份的**同一個參照**、分位型的全場最低恰 0 最高恰 1、剖面 `Σ|w| = 1` 且置中公平、四個剖面互不相同、每名角色恰一個定位且**四個定位都至少一名**(全落在同一個 = 分類沒作用)、決定性、查無角色回 null、**定位區塊零逐角色名冊**;Ⅱ 策略 —— **每個旋鈕的角色數加權幾何平均 = 1**(核心不變式)、正規化不改變定位之間的兩兩比值、原始乘數收在 `MUL_MAX` 幅度內、與學習白名單重疊的邊界 MUST 收在學習邊界內、覆寫後仍滿足交叉約束、非覆寫鍵原封不動、**夾到邊界的逐項印出來**、採購序是八軌的排列且四個定位互不相同;Ⅲ 單一縫原文 —— 分類/覆寫/正規化/交叉約束各一份、bots.js 解析縫恰一處且由 `diff.tactic` 把關、`_tacBase` 守衛、**零定位鍵行為分支**、舊硬編碼(0.55/1.3/0.6/0.85/血線)零殘留、本地 `BUY_ORDER` 已退場;Ⅳ 真 BattleSim + 真 BotBrain —— 新手/低難度 `this.tac` **參照**恆 = 全域、四個定位的距離環/工事加價/撿尾刀/撤退線逐對比較、學習注入的非覆寫鍵原封保留而覆寫鍵以乘數疊加、重複解析不累乘、換角色從基準重算、選敵直測(攻堅停下來拆塔 vs 突襲繞過去咬人,擺位由武器自己的 `vsMult` 反解)、距離環直測(壓制後撤 / 突襲貼上)、招式血線直測、採購序第一筆落點;Ⅴ 不回歸 —— 五個新旋鈕基準 = 改制前硬編碼、不進學習白名單、`botPolicySanitize({})` 仍逐位元同基準、`botRoleTactic(base, null)` 回傳 base 本身)+ **反向驗證**(`--break-role` 四剖面同形 ⇒ Ⅰ 紅 / `--break-norm` 乘數不正規化 ⇒ Ⅱ 紅 / `--break-tier` 低難度也套覆寫 ⇒ Ⅳ 紅)+ `audit_bot_tactics.mjs`/`audit_bot_policy.mjs`/`audit_bot_vision.mjs`(讀取縫與難度分層 MUST 仍全綠)+ `npm test`(bot 沿兵線推進)+ `npm run bal` **不動**(bal 不模型化 AI;平衡指紋未變 ⇒ botPolicy.js 不過期)+ `npm run sim` + **AI 退化量測**(判準走「電腦玩家戰術」那一列的結構性指標 —— 繞行% ≫ engage% ≫ 開火數;**MUST NOT 拿 `npm run sim` 的勝負旗標**,長局二元訊號天生飽和。專屬作法 = 定位版 vs 關掉 `_resolveRole` 的同一批 bot 做 CRN 配對鏡射自對戰,量工事損血差)+ 真機冒煙(高難度對局裡看得出四種打法:重甲機甲貼著砲塔硬拆、高速無人機專挑殘血與後排、扇形/大範圍機體停在中距離不貼臉、帶團隊治療的機體站最後面且很早就放招;新手/低難度手感與改制前完全一樣)|
| 電腦玩家戰術(`BOT_DIFF.tactic`/`elite`・`BOT_TACTIC`/`botTargetPrio`/`botThreatDecay`/`botSalvo`/`botExecW`/`botKiteF`・`sim.js _hurtLog` 威脅帳/`_dmgOut`/`SQUAD_SHARED` 的 `dmgOut`・`bots.js _threatOf`/`_recentDmg`/`_prioritize`/`_acquire`/`_pullWant`/`_inFight`/`_enterPull`/`_resume`/`_progAt`/`_pickRally`/`_setRally`/`_rally`/`_faceLaneFwd`/`_pulling`/`_engage` 距離環) | `audit_bot_tactics.mjs`(109 項;Ⅰ 旗標分層單調 + 三項權重與遲滯帶推導不手寫 + 撿尾刀權重 ≫ 1、Ⅱ 原文單一縫:威脅只在 `_hurtLog`・輸出**兩條結算路徑都記**・bots 只讀不寫・不比對難度字串・交戰判定只有 `_inFight`、Ⅲ 真 `BattleSim`+真 `BotBrain` 選敵直測:三項各自生效/撿尾刀跑遠 1.5× 也去收/中難度不撿/塔的累計輸出不列入/威脅逾時歸零/**低與新手三項全不吃**、Ⅳ 撤退線直測:<25% 回主堡・25~32%+護盾半條退塔後・高難度滿血扛半條就撤・**工事刮傷不算但裝甲照算**・慢刮不算・脫戰不撤・RETREAT 不被 RALLY 搶走・集結點在最近**存活**塔後方且塔全滅退回主堡・等滿護盾復出且 prog 接回當下位置・RALLY 邊退邊打而**脫離接觸停火停步**、Ⅴ 打帶跑:高難度裝填中拉開/可擊發貼上去/中低難度與建築不套、Ⅵ 不回歸)+ `audit_bot_vision.mjs`(A32 不得倒退)+ `audit_blood_splat.mjs`(`_hurtLog` 是共用縫)+ `npm test`(bot 沿兵線推進 MUST 仍綠)+ `npm run bal` 不動(不模型化 AI)+ `npm run sim`(**MUST 仍分得出勝負**)+ 自寫單場診斷對照(`npm run sim` 的勝率太鈍;工事損血/擊殺/狀態佔比才看得出退化 —— 2026-08-02 基準:高難度 2v2×240s 工事損血 ≈3600、擊殺 ≈76、RALLY 佔比 ≈0.10,RALLY 佔比一旦衝到 0.3 以上就是「bot 一直在撤退」)+ 真機冒煙(被集火時 bot 會退到自家塔後面停火等護盾回滿再上,而不是一路跑回主堡;剩一口氣的敵人會被高難度 bot 丟下手上目標去收掉;高難度 bot 打空彈夾會往後拉開、裝填完再貼上來)|
| 飛行體貼地渲染基準(`game._flySurf`・`_updateEnts` 的 `ent.flies` 分支)或 客戶端單位碰撞(`game._unitSolids`/`_circleEnter`/`_pushOutCircle`/`_collide`/`_sweepBlockers`・`sim.solidEnter`/`solidPush`/`_solidsNear` 的 `span()`) | `audit_npc_collide.mjs`(44 項;Ⅰ `_flySurf` 是純函式 —— 橋上取橋面/足跡外回地表/隧道上方不落洞內/明隧道取頂板而 open 段不取/不吃 `blockerTopAt`、Ⅱ **路徑無關性**:合成河谷 + 橋樑跑一趟「被側推出足跡再回橋心」的航線,舊制棘輪復現「回到橋心仍停在河床」(反向對照組),新制逐點只由座標決定且反向飛逐位元相同、Ⅲ 消費端單一縫 + 飛行分支內無 `cur.y` + 地面單位那半逐位元維持舊制、Ⅳ 圓柱幾何**與伺服器逐案例同值**(執行 `sim.js` 的 `solidEnter`/`solidPush` 原文,>5000 案例掃描 t 與推出位移的最大差 < 1e-12)、Ⅴ 單一縫:三支各一份實作、`_collide` 不再自己掃 `ents`、掃掠收單位、pass 內順序「先單位後障礙」、垂直帶 ε 兩端同式、Ⅵ 以真品 `_collide` 原文行為直測:單幀 30m 擊退橫越步兵被夾在近側 / 慢速貼上去仍沿邊滑(手感不變)/ 沒碰到的一步逐位元不動 / 飛越單位垂直帶不碰撞 / **擠不下時停在牆外而不是牆裡** / 自己的僚機不碰撞)+ **反向驗證**(`--break-ratchet` ⇒ Ⅲ 紅 / `--break-deck` ⇒ Ⅰ②・Ⅱ 紅 / `--break-sweep` ⇒ Ⅴ・Ⅵ① 紅)+ `audit_bot_vision.mjs`(伺服器那半 MUST 逐位元不動)+ `audit_climb.mjs` Ⅲ/`audit_layer_block.mjs`/`audit_slope_move.mjs`(game.js 原文抽取的相鄰稽核)+ `npm test` / `npm run bal`(㋒ 純客戶端渲染與物理,伺服器完全不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**跟著敵方攻擊直升機飛過長橋:全程貼著橋面上方等高飛,不會飛著飛著沉到橋面下、也不會鑽進隧道山體**;衝刺撞上小兵/戰車會被擋在外面而不是穿過去;被小兵擠在牆邊時人停在牆外,鏡頭不會穿進牆裡;沒碰到單位時走位手感與改動前一模一樣)|
| 電腦玩家視野 / 碰撞(`BOT_VIEW`/`botFovHalf`・`SELF_F`/`selfCollider`/`COLLIDE_KINDS`・`bots.js _fovHalf`/`_bearing`/`_face`/`_turn`/`_alertLook`/`_move`/`_skirt`/`_stuck`/`_acquire`/`_push` 朝向・`sim.js solidResolve`/`_solidsNear`/`solidPush`/`solidEnter`/`_hurtLog` 警戒・`game.js _collide`/`_sweepBlockers`/`COLLIDER`) | `audit_bot_vision.mjs`(94 項;Ⅰ 半視角與碰撞量體推導不手寫 + 不是全角度也不是針孔、Ⅱ 原文單一縫:`h.ry`/`h.x` 各恰一處寫入・轉頭只經 `viewLockStep`・量體兩端同吃 `selfCollider`・舊 hazBlockers 繞行已退場、Ⅲ 真 BotBrain 直測:正前鎖得到/正後與 90° 鎖不到/邊界內外/轉身後鎖得到/塔同吃/**推線朝向沿兵線而非腳下目標點**、Ⅳ 受擊警戒:記帳・角速度上限・轉到看得見即收手・逾時放掉・真人不記・僚機挨打也算・環境傷害不記、Ⅴ 真 `BattleSim` 碰撞:有向盒/圓柱/塔/巨石各擋・單幀高速橫越不穿・**終點落在中心平分面仍夾在進入面**・沿牆橫移不誤擋・飛越屋頂・無障礙逐位元不變、Ⅵ 撞牆繞行左右輪替、Ⅶ 每條開火路徑都驗 LOS + 真 `BattleSim` 直測「牆後打不掉血 / 沒牆打得到」)+ `npm test`(bot 沿兵線推進 MUST 仍綠)+ `npm run bal` 不動(不模型化 AI)+ **AI 退化量測**(**碰撞上線最易壞的是「bot 原地卡死」**)。判準走上一列「電腦玩家戰術」那一套(工事損血/擊殺/狀態佔比,MUST NOT 拿 `npm run sim` 的勝負旗標 —— 5v5 塔+主堡總 HP 數十萬,改前改後在 1800s 上限內一律「未分勝負」,二元訊號天生飽和),另加**碰撞/視野專屬的兩項**(2026-08-02 基準,高難度 5v5×600s):`繞行%` = `_skirtUntil` 生效的 tick 比,**≈4.0%(逐場 3.7~4.2,SD≈0.2 —— 全套指標裡唯一精確的一個)**,卡死就會飆高;`engage%` ≈ **9%**(改前 9.4 / 改後 9.0),視野錐有沒有誤殺交戰機會看它。**樣本數 MUST 跟著場景規模走**:上一列的 2v2×240s 基準是 **24 場**的平均;若改跑 5v5×600s,單場工事損血在 **433~10298** 之間跳(`Math.random()` 進了 jitter/巡航高度/aimErr)⇒ n≤3 能同時「證明」變好與變壞(2026-08-02 各踩一次:先用 3v3 的 n=3 得出「變好」、再用 5v5 的 n=2 得出「變差」,補到 n=6 才看出**值域幾乎完全重疊**)。結論 MUST 以變異小的結構性指標(繞行% ≫ engage% ≫ 開火數)為準,工事損血只用來看有沒有塌到量級外。**取 base 對照時 `git stash` 不可靠**(變更已 commit 就是 no-op,會安靜地拿新程式碼當基準;切分支也會污染仍在跑的背景樣本)—— 一律 `git show <rev>:path` 把 `sim.js`/`bots.js`/`data.js` 三支(彼此自足、無外部 import)倒進暫存目錄各跑各的+ 真機冒煙(繞到 bot 背後它不會立刻轉身鎖你,開一槍才轉過來;bot 不再從建物/砲塔中間穿過去,而是沿牆繞過;高空無人機照樣飛越屋頂)|
| 商店掃貨 / 預約(`game._sweepPick`/`_sweepBuy`/`_toggleReserve`/`_tickReserve`/`_upgAllMax`/`_creepResKey`/`_resCreepLane`/`_shopState` 的 `sweep`·`toggleReserve`·`sweepable`·`allMax`·`creepKey`・`RESERVE_RESEND_S`/`RES_CREEP`・`main.renderShop` 的掃貨鈕與逐列 ☆ 鈕・`#shopSweepBtn`・`help.js UI_TIPS.shopAuto`) | `audit_shop_auto.mjs`(46 項;Ⅰ 判定單一縫 —— `_sweepPick` 恰兩個消費端、UI 不自算單價、`_tickReserve` 一份實作一個呼叫點且排在商店簽章前又不關在 `if (shopOpen)` 裡、Ⅱ 以真品原文直測貪心便宜優先:錢剛好買兩階就停、資金無限恰好買滿八軌總階數(推導不手寫)、每軌停在自己的 max、Ⅲ **掃貨**只作用於八軌(原文無 creep + 八軌全滿仍一筆不下單)、預約鍵格式與解鎖門檻各只有一份(main.js 不自己拼鍵、不自己算 allMax)、非法鍵(`creep` 無索引 / `toString` / 兵線越界)一律拒收、Ⅲ-b 兵線預約行為直測:未解鎖只等不踢出名單、錢一夠帶對兵線下單、共用值沒回來前不重送、等級一前進就買下一階、**三線同輪只送買得起的筆數**、滿級退出名單、Ⅳ 預約行為直測:錢不夠不下單 / 錢一夠自動下單 / **權威值晚到不重送** / 逾時放行救濟 / 權威等級前進即解鎖下一階 / 滿級自動退出名單 / 再按一次取消 / 沒有 side 一律不動作)+ `audit_ui_layout.mjs` 0.5 段(掃貨鈕與 ⓘ 存在、`sweepable` 單一縫)+ `npm test`(㋒ 純客戶端排程,經濟段 MUST 仍全綠)+ `npm run bal` 不動(bal 不模型化玩家的購買順序)+ 真機冒煙(錢不夠時掃貨鈕變灰;$200 按一下掃貨買到兩階不同軌;把「裝甲強化」設預約後關掉商店去打仗,擊殺賞金一到就自動成交並跳播報;滿級的軌自動取消預約;八軌買滿後,把「第 2 兵線」設預約再關掉商店,錢一到自動成交;三條兵線全預約而只有一階的錢時,只成交一筆、不跳假的「資金不足」)|
| `ECON.UPG_*`(八軌階梯單價) | bal ③(±10%)+ e2e「八軌升級第三階單價」 |
| `aoeClass`/`trajClass`/`LANCE`/`ARMING` | 32 角分類覆蓋(重武器全歸類、輕武器不歸類)+ `heroLance` 衰減直測(首發全額、之後 `DECAY^i`)+ bal 不動 |
| 直線貫穿命中判定(`_lanceHits`/`lanceR`/`hitR` 系・**垂直帶換框 `_lanceBandY`**) | `audit_lance_hit.mjs`(41 項;塔身側面命中、近側表面命中、傾洩加粗生效於伺服器;**⑪ 垂直帶換框**:單一縫 + 走 `_absSightY`/`_hgtAt`,並以合成粗高程網格(等坡,避免崖緣自己變成稜線)行為直測「射手高出/低於砲塔 30~200m 瞄塔身中段仍命中」、兩組反面對照「朝塔頂上方掠過 MUST 落空」、「未上傳網格 ⇒ 逐位元退回舊近似」)+ **反向驗證**(垂直帶改回 `oy + slope × sc` ⇒ 五個高低差案例全紅)+ `npm test` / `npm run bal`(㋒ e2e 不上傳 `hgt` ⇒ MUST 全數不動)+ 真機冒煙(**站在稜線上用反器材砲打山谷裡的砲塔,血條真的在掉**;洞內對射與平地互轟手感不變)|
| `BALLISTIC.LOB_*`/`AA_MV`/`ARC_MAXP`/`_lobAim`/`_lobCrosshair`/`_lob45Vel`/`_lobLadder`/`_arcTrace`/`_updateAaMode`/`_aaTarget`/`_updateBullets` 的 `b.lob` | `audit_weapon_gate.mjs` Ⅳ・Ⅴ・**Ⅺ**(火控解與積分只有一份、`_lobAim` 與 `_reachable` 同吃且都傳 `aa`;Ⅺ = 準星優先原文 + 45° 行為直測(逐距離/高差出膛角恆 45°、落點落在瞄準點 `LOB_TOL` 內、滿射程仍抵達、超射程 `cut='range'`、仰角 ≥45° 退回階梯且更陡、對空維持全裝藥且比 45° 平)+ 射程界兩處同一把尺)+ Ⅵ④(`spent` 量球面)+ **Ⅻ 全攻擊路徑對帳**(7 條傷害路徑量 3D 球面 + 扇形球心 = 回報槍口的真 `BattleSim` 差分直測 + 4 條具名 2D 例外)+ `npm run bal` 不動(不模型化彈道)+ 真機冒煙:**準星壓在地面敵人身上時,旁邊有無人機也不會把瞄準點/鎖定拉走**;對地砲管抬到 45° 且弧高明顯、落點環貼在準星那個部位;準星移到空中單位上立刻轉高速平射;滿射程的敵人打得到(砲彈不在半空自爆);目標走開後砲彈照樣飛完打在原落點;雷射導引同理(碰到就爆、目標閃開就續飛到射程邊界才爆) |
| 射程閘門容差 / 爆風量體 / 射程光暈可命中判定 / 高度制空射程(`RANGE_TOL`・`altRangeMax`・`altRangeF`・`BLAST`/`blastCoreR`・`REACH_RULE`/`reachRule`・`sim._blast` 水平量體・`heroBurst` 落點閘門・`sim._altDh`/`_altRange`/`_altCrit`/`_dodges`・`sim.heroPlasma` 射程閘門・`game._altRangeTo`/`_effRange`/`_maxRange`/`_reachable`/`_shotVictims`/`_inShotRange`/`_bodyDy`/`_shotWarn`/`_updateRangeGlows`) | `audit_weapon_gate.mjs`(八段;**Ⅴ-b 範圍光暈 = 這一發的傷害足跡**:名冊單一縫 + 分類走 `aoeClass` + blast 分支原文無 LOS/射程閘 + 舊分幀預算不得復辟,並以真品 `_shotVictims` 跑合成場景直測「準星目標與爆風內的第二人一起亮 / 掩體擋在爆心與他之間仍亮 / 準星那一發被擋住則全場熄燈 / 單體直擊只亮一個 / 圓柱內全亮而柱外與射程外不亮 / 錐內全亮且不需準星目標」;Ⅰ 容差/超壓帶單一縫且推導不手寫、Ⅱ 兩端閘門同界 + 真 `BattleSim` 直測「range×1.20 落點不再被丟棄 / 超界仍丟棄」、Ⅲ `_blast` 量到近側表面(主堡牆面全額、EDGE 外歸零、人員級位移 <15%)、Ⅳ 五類彈道全覆蓋 + 消費端單一縫 + 積分唯一、Ⅴ `_reachable` 逐彈道行為直測、Ⅵ 見下列、**Ⅶ 光暈 ⇔ 傷害**:`_altDh` 同框比較 + 三個消費端同吃 + 真 `BattleSim` 直測「海拔 500m 的英雄對地面小兵無加成 / 英雄對英雄仍精確吃高度制空」、扇形武器 1.05~1.24 × 射程 MUST NOT 掉血、客戶端 `_effRange`/`_altRangeTo` 逐條照抄 `sim._sightY`)+ `npm run bal` 不動(四不變式不模型化爆風幾何;⑤ 的高度差掃描是抽象 dh,不吃 `_sightY`)+ 真機冒煙(**雙方站著不動:沒有射程光暈的敵人一發都打不掉血、有光暈的必掉血**;榴彈對著稜線後方的敵人不亮光暈、繞到側面亮、貼臉導引彈轉琥珀、直擊武器隔牆不亮;beam/missile 武器開火不再整幀卡住;**準星壓在一群敵人中間的那一個身上,只有會被這一發炸到的幾個亮 —— 準星移開落點,整組跟著換人;換上步槍只剩準星底下那一個亮;霰彈的錐內全亮;貫穿光束沿線一整排亮**) |
| 射程球心 = 擊發位置(`sim._trailPush`/`_shotOrigin`・`data.js shotTrailS`・`heroBurst` 的 `org`/`dImp`/`back`/僚機閘門・`tick` 尾端取樣點・`_respawn` 清帳) | `audit_weapon_gate.mjs` Ⅻ⑸(球心由 `_shotOrigin` 回推、舊制 `dist2d(h.x, h.z, x, z)` 已退場、`back` 與球心是同一個解、僚機同吃、`_shotOrigin`/`_trailPush` 各一份實作且消費端/取樣點數得出來、保留窗推導不手寫且蓋得住最長的拋物線飛行時間、重生清帳,並以真 `BattleSim` 直測「擊發時在射程內、飛行中後退到當下位置量已超程 ⇒ 傷害照結算」+ 三組對照「無軌跡退回舊制被丟棄 / 擊發時就超程再衝進來仍丟棄 / 原地不動逐位元不變」+ 「`sim.tick` 一跑就記下位置」)+ **反向驗證**(`org` 改回 `{ x: h.x, z: h.z }` ⇒ Ⅵ・Ⅻ⑸ 共 5 條紅字)+ `npm test`「射程球心 = 彈藥擊發的位置」段(6 項)+ `audit_lance_hit.mjs`/`audit_aoe_trim.mjs`/`audit_shield_counter.mjs`/`audit_fire_rate.mjs`/`audit_bot_tactics.mjs`/`audit_bot_vision.mjs`/`audit_blood_splat.mjs`/`audit_npc_collide.mjs`(共用 `_gateFire`/`heroBurst`/`tick` 的相鄰稽核)+ `npm run bal` 不動(bal 不模型化伺服器的落點閘門,⑦ 的 lanesim 也沒有飛行時間)+ 真機冒煙(**用榴彈/巡飛彈打接近滿射程的目標,開完砲立刻往後退再繼續走位 —— 砲彈落下時血條照樣掉**;反過來站在射程外開砲再往前衝,落點照舊零傷害;小隊三機同時後撤時三份傷害都在;重生後第一發不會因為上一條命的位置被誤判)|
| 射後不理追擊 / 爆風擴散(`SEEK.CHASE_F`/`chaseCapS`・`game._updateBullets` 的 `b.fnf`/`b.chase`/`b.fuel` 與失鎖分支・`sim.heroBurst` 的 `lockT`/`cap`/落點閘門・`sim._blast`) | `audit_weapon_gate.mjs` Ⅸ・Ⅹ(19 項;`chaseCapS` 推導不手寫且追擊里程 ≫ 射程、失鎖規則對 fnf 不生效、追擊旗標不可逆、包絡讓位、並以真 `BattleSim` 直測:無鎖定的遠距落點仍丟棄 / 有鎖定 2.3× 落點閘門照樣結算 / 有鎖定但炸在遠離目標處仍丟棄 / 鎖定過期退回一般閘門 / 射程外目標吃得到射程內落點的爆風 / `_blast` 原文無任何射程閘)+ `npm run bal` 不動(bal 不模型化彈道與爆風幾何)+ `npm test`(㋒)+ 真機冒煙(鎖定遠方目標後開飛彈,彈頭一路追出射程外並命中;沒鎖定的飛彈仍在射程邊界自毀;榴彈打在射程邊緣時,再外面一點的敵人照樣被濺射傷到) |
| 地形稜線遮蔽(`LOS.HGT_M`/`HGT_MAX`/`RIDGE_M`/`RIDGE_SKIP_M`・`HGT_CHARS`/`HGT_STEP`/`hgtEnc`・`main.bakeHeightGrid` 與 `t:'world'` 的 `hgt` 欄位・`rooms.js` 轉存・`sim._ingestHgt`/`_hgtAt`/`_absSightY`/`_ridgeBlocked` 與其兩個消費端・`server.js maxPayload`) | `audit_weapon_gate.mjs` Ⅷ(21 項;編碼單一縫 + 消費端恰兩處且 MUST NOT 併進 `_losBlocked` + 隧道豁免 + 常數不手寫,並以真 `BattleSim` 直測:合成 60m 山脊 ⇒ 脊前掉血/脊後不掉血、**未上傳網格時逐位元同舊制**、均勻緩坡互射不誤擋、殘缺網格整份不收)+ `npm test`(㋒ e2e 不上傳 `hgt` ⇒ 確定性斷言 MUST 全數不動)+ `npm run bal` 不動(bal 不模型化地形)+ 真機冒煙(隔著山稜開散彈/電漿與貫穿光束,山背後的敵人不掉血;洞內互射照常有效;上橋/高地俯射不被誤擋)。**改 `LOS.HGT_MAX` 或任何 world 上傳上限 MUST 重算 `server.js maxPayload` 餘裕** —— 超過就是 ws 直接關連線 = 房主整份 world 靜默上傳失敗 |
| 導引 / 射後不理(`shotV0`・`flightCapS`・`SEEK`/`seekTurn`・`sim.heroAim` 的 `aimOffAt`/`heroBurst` 需瞄準閘門/`_gateFire(…, back)`・`game._aimTarget`/`_tickLock`/`_tryFire` 的 homing・`_updateBullets` 近炸引信與 `b.seek` 射程包絡) | `audit_weapon_gate.mjs` Ⅵ(①初速單一縫 ②`flightCapS` 推導 + 真 `BattleSim` 直測「飛行途中收鏡仍結算 / 超過最長飛行時間仍丟棄 / 裝填後打更近的目標不再被丟棄」 ③追蹤目標 MUST NOT 只認 `_lockId` ④引信量線段最近點且半徑 = 核心帶 + 命中量體 ⑤`seekTurn` 只放寬不收緊 ⑥鏡射彈道積分:8 把武器 × 5 段距離導引狀態全數落在核心帶內、無導引一律落空)+ `npm run bal` 不動(伺服器不模擬彈道)+ 真機冒煙(準星壓著敵人立刻開火,飛彈會追過去;開完砲右鍵收鏡照樣算傷害;連打整個彈夾後裝填完打近距目標仍生效) |
| `hitH`/`TARGET_H`/`HERO_SIZE`(命中量體) | headless `_blast` 直測(垂直帶內同額、EDGE·r 外歸零、塔頂 = 塔底);動 `hitR`/`TARGET_R` 一併跑 `audit_lance_hit.mjs` + `audit_weapon_gate.mjs` Ⅲ(同組值 = COLLIDER 碰撞半徑 + 爆風水平量體) |
| `AIR`/`envTrigger`/`TERRAIN_FX`(騰空/地形異常) | headless 直測(小跳仍踩雷、蓄力跳不踩;無人機 y=10 仍灼傷;騰空 wet 立停)+ 真機水域冒煙 |
| 世界高度上限(`WORLD_H`/`objHeightMax`/`objScaleCap`/`objScaleFit`/`worldCeilY`・`TARGET_H.tower`・`terrain.js` 的 `avgH`・`game._ceilY` 與其三個消費端・`biomes.js` 的 `OVER.bldCap` 與四個縮放夾制點・任一張物件表(`GIANT_DEFS`/`MEGALITHS`/`LANDMARK_COL`/`BEACON_KINDS`/`VEG_DEFS`)) | `audit_world_height.mjs`(49 項;Ⅰ 三個係數全是砲塔高的倍數(獨立重算 + 定義式內無字面值 + **塔高 ×2 兩個上限同步 ×2**)、取 max 的兩端各自勝出(**前提 `CEIL_AVG_F > CEIL_PEAK_F` 另立一條**:地表恆 ≤ 最高海拔 ⇒ 平均項係數不大於峰頂項的話那一項永遠贏不了)、單調、缺輸入不 NaN;Ⅱ **結構性不等式** `CEIL_PEAK_F > OBJ_F` + 掃描任一場地任一點的「天花板 − 地表」下界 > 物件上限;Ⅲ 五族物件以**真品原文的表**逐款重算(神木 11 款 × 群落/株高上下端、巨岩 10 款 + 合成岩、地標 13 款、語意化地標 5 款、建物 `OVER.bldCap`),全數 ≤ 上限且**最高的那一個恰好貼齊上限**(夾制真的在做事)、植被標稱高遠在上限之下、**吃建物高度的門檻(`MASS.MIN_H` 與三條 `b.h > N`,自 biomes 原文抽)全數 < 物件上限** —— 這一條就是 2026-08-09 那次「兩個 PR 各自綠、合起來把整棟量體庫節點變成死碼」的守門線;Ⅳ `objScaleFit` 的壓縮率由 `objScaleCap` 推導、頂端貼齊、**相對變異逐位元保留**,並以合成輸入存證「硬夾會把全數超標的一組壓成同一個高度」;Ⅴ 單一縫 —— `_ceilY` 一份實作、公式只取一次、消費端恰三處、`avgH` 只算一次、**伺服器沒有第二份天花板**、物件夾制恰五個生成點、舊制 `bldCap: 170` 不得復辟,並執行真品 `_ceilY` 原文直測(吃統計 / 缺 avgH / 無地形回 Infinity / 一場只算一次);Ⅵ bot 巡航・直升機・防空門檻・**極音速飛彈頂點**皆低於最小餘裕)+ **反向驗證**(`--break-ceil` 天花板改成手寫常數 ⇒ Ⅰ 紅;`--break-obj` 物件上限拉到 ≥ 餘裕 ⇒ Ⅱ 紅;**`--break-cap` 把上限調回 2026-08-08 的 2 倍 ⇒ Ⅲ 紅並列出構不到的 55 / 60 / 100m**)+ `audit_siteplan.mjs`(`placeBoundary` 原文多吃一支 `objScaleFit`,注入的 `GIANT_DEFS` 樁 MUST 帶 `h`)+ `audit_beacons.mjs`/`audit_object_joints.mjs`/`audit_climb.mjs`/`audit_layer_block.mjs`/`audit_npc_collide.mjs`/`audit_slope_move.mjs`(biomes/terrain/game 原文抽取的相鄰稽核)+ `npm test` / `npm run bal`(㋒ 純客戶端物理 + 地物散布,伺服器完全不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**無人機一路往上飛會在某個高度停住,而那個高度隨場地起伏不同**;觀戰上帝視角同樣升不上去;神木林仍看得出高矮參差、只是整片比改制前矮;巨岩露頭仍是核心大、外圍小;市區最高的樓與砲塔高度比約 4:1;沒有任何建物/地標/巨岩/神木的頂端接近飛得到的天花板;**夜裡最高的十幾棟商辦要看得到亮著的窗格**(那幾棟才是整棟量體庫節點,離線可走 `shot_scene --live --time night` 的 `mass_near`))|
| 地形坡度移動(`SLOPE`/`slopeDeg`/`slopeMoveF`/`slopeBlocked`・`_slopeDegAlong`/`_slopeMoveF`/`_updatePlayer` 移動段與 passable 閘)/ `MAPGEO.MAX_ROAD_GRADE_DEG` | `audit_slope_move.mjs`(66 項;Ⅰ 常數推導不手寫、Ⅱ 曲線平緩帶恆 1 → 上坡遞減/下坡遞增且夾制、**下坡一律不擋**、Ⅲ 合格兵線在遊戲空間 ≈8.2° < 平緩帶 ⇒ 推線走廊恆全速、Ⅳ 消費端單一縫、Ⅴ 合成高度場行為直測含橋面/隧道/騰空豁免 + 洞內 tunnelAt 捕捉豁免)+ 真機冒煙(爬陡坡明顯變慢、峭壁上不去但可沿等高線橫走、下坡加速、上橋引道與洞口不被誤擋) |
| 受擊濺血提示(`BLOOD`/`bloodDur`/`bloodAlpha`/`bloodFrac`/`bloodDropR`/`bloodDropN`/`bloodScreenUv`・`sim._hurtLog`/`_flushHurt`/`_damage` 記帳點/`_frame` flush 點・`game._bloodSplat`/`_updateBlood`/`_clearBlood`・`main.hud.blood`/`#bloodVig`/`.blood-vig`/`.blood-splat`) | `audit_blood_splat.mjs`(78 項;Ⅰ 曲線推導不手寫 + 峰值半透明 + 常數不被消費端手抄、Ⅱ 血滴大小吃「佔自機總量比例」且隨傷害單調遞增、Ⅲ 方位映射含 ±π 全掃恆落在畫面內(背後貼邊不折回)+ 狙擊縮 FOV 自動跟著收、Ⅳ 伺服器原文:`_damage` 兩條路徑都記帳・flush 只在 `_frame()` 且排在取走 events 之前・只記主視野機・高程跨框守衛、Ⅴ 真 `BattleSim` 直測:同攻擊者併筆/不同攻擊者分筆/跨 tick 不重複/環境傷害不發/護盾全擋仍發/僚機不發且不留帳、Ⅵ 客戶端原文:唯一觸發 = `hurt` 事件且驗 `tpid`(MUST NOT 從血量落差自造方位)・z 反號・自己 invert `matrixWorld`・清帳齊全、Ⅶ HUD 依 id 對帳不重建 DOM + 位置烤死 + CSS 無 transition)+ `npm test`(㋒ 純表現層,MUST 仍全綠)+ `npm run bal` 不動(不模型化表現層)+ 真機冒煙(從左/右/背後/上方被打,血漬各噴在對應邊;一發重砲的血滴明顯大於一發步槍;狙擊模式血斑跟著收攏;血漬留在原地不跟著轉頭滑動,約 2.7 秒淡掉;陣亡重生後畫面乾淨) |
| 角色 / 機體檔案格式(`codex.js` 全支(含 `FORM_POSE`/`formPose`/`SHOT_POSES`/`shotFraming`/`visualUses`)・`mecha.js` 全支・`lore.js` 的 `gen` 段與任一敘事欄・`main.js protoHTML` 與其呼叫點・`data.js CHARACTERS[].visual` 新增列舉值或新增/移除角色・`tools/ai3d/prompt.mjs` 的 `masterPrompt`/`designBrief`/`limbLine`/`bodyPlanLine`/`chassisSpec`/`LEX`・`tools/codex_review/review.js` 的 `openReview`) | `audit_codex.mjs`(90 項;**Ⅵ 型態姿態 + 動作姿態 + 適用性**:恰三種動作且各有 zh/en、覆核台的 `POSES` 由 `SHOT_POSES` 推導(原文反查它沒有第二份清單)、出圖端不自帶第二份動作語、**`static` 不覆寫取景 ⇒ 舊有靜止稿逐位元不變**、moving/heavy 的取景逐 medium 分開、飛行的三種動作取景一律不含 `standing` 且都講明離地、地面機體三種動作取景各不相同、同一台機體三種動作的提示詞互不相同、不給動作逐位元同舊行為;恰兩個型態且各有 zh/en/framing、飛行語明講離地在飛(中英各驗)、飛行取景不得含 `standing`、地面取景逐字同舊制、兩個消費端都 import 且都不自帶第二份姿態語、覆核台的重下 prompt 帶型態、行為直測「同一台變形者兩型提示詞不同 / 各自只帶自己那一層原型 / 不給型態逐位元同舊行為 / 非變形者不受影響」、設計敘述取 `mechaCodex` 且 `loreOf(` 不得復辟、**以 `models.js` 原文反查** frame/body/wing 的實際消費點再驗擬態翼機體的生圖詞不含旋翼與機身殼、多旋翼機體照舊帶旋翼骨架詞;Ⅰ 格式對齊 —— 四段只有一份定義、兩種檔案的生成段鍵集**逐位元相同且順序相同**、`GEN_KEYS` 由 `GEN_FIELDS` 推導、`lore.js`/`mecha.js` 不得自帶第二份欄位表;Ⅱ 原型層 —— 逐台**獨立重算**該有哪幾層再比對(不呼叫被測的 `protoLayers`)、8 台變形者恆兩層形態、4 台人形機甲恆有機體原型、12 台獸型恆有仿生原型、總層數 64、`src` 不是整句、`note` 不是 `src` 的複製、舊標籤整組退場;Ⅲ 內容齊備 —— `codexIssues()` 零筆、1:1、型號唯一且前綴對得上編制 12/12/8、64 份檔案的 `tag` 全是名詞短語、`parts` ≥ 4 件、外觀/髮型詞彙表蓋得住現役每一個列舉值;Ⅳ 單一縫 —— `mecha.js` 零 import、`codex.js` 只 import 三支純資料檔、`mecha.js` 無手寫公尺數/無 `0x` 主色/無 `kind`・`side`、`heroTargetH` 恰一處呼叫、三份生成文字各一份實作、`main.js` 無 `PROTO_LABEL` 與標籤切割且改由 `codex.protoOf` 取;Ⅴ 生成輸出行為直測 —— 64 份 × 三份文字格式一致、機體提示帶原型出處與 `#rrggbb` 主色且**關鍵詞去重**、**改 `tag` ⇒ 文本種子與 2D 提示跟著變而建模單不變 / 改 `parts` ⇒ 建模單跟著變 / 改 `data.js` 的機體名 ⇒ 兩種檔案同時跟著變**(推導不手寫的證據)、逐台全高 = `heroTargetH`)+ **反向驗證**(`--break-layer` 抽掉一台變形者的飛行型原型 ⇒ Ⅱ・Ⅲ 紅;`--break-align` 角色生成段多一欄 ⇒ Ⅰ・Ⅲ 紅;`--break-pose` 飛行姿態語退回舊制「只說零件重排」⇒ Ⅵ 紅)+ `node tools/ai3d/gen2d.mjs --audit`(rig 節點涵蓋 / 描述子詞表 / **補圖優先序**三支)+ `audit_hex_stats.mjs`/`audit_ui_layout.mjs`/`audit_ctrl_mode.mjs`(main.js 原文抽取與角色卡版型的相鄰稽核)+ `audit_net_modes.mjs`/`audit_solo_boot.mjs`(新增兩支客戶端模組 ⇒ 單機打包與 URL 佈局鏡射)+ `npm test` / `npm run bal`(㋒ 純表現層 + 客戶端專用資料,伺服器完全不涉入 ⇒ MUST 全數不動)+ 真機冒煙(角色卡的「設計原型」下方,獸型機體多一行「仿生原型」、**變形者是飛行型與地面型各一行**、四台人形機甲是「機體原型」;每一行都是「名稱」—— 說明的排版;沒有任何一台只剩一行光禿禿的設計原型)|
| 在地文字語料(`vernacular.js` 全支・`venueText.js`/`tools/bake_venue_text.mjs`) | `audit_vernacular.mjs`(287 項;Ⅰ 分類/取名/副行/主幹 —— 宗教先於泛用 building、`name:en` 不當主名、列舉值不上牌、標高冠詞在取字時補、主幹 MUST 真的出現在語料裡且不是地物通名、假名 3 字窗;Ⅱ 挑字 —— 語域只挑自己那幾類、容量在取字階段就篩、去重帳跨類別一本、決定性逐字元相同、**固定 3 枚亂數**、零字場地有詞表退通用牌面/無詞表回 null;Ⅲ 接線 —— `signage.js` 已退場、五種語域在 worldtext 的樣式表裡、挑字單一入口、`resolveName` 只是 `pickName` 的薄殼;Ⅳ 單一縫 —— 零 import、無 `Math.random`、專屬 seed、去重帳一本、不碰 blockers;Ⅴ 烘焙語料 —— 27 場地八類鍵齊全/主名合法/無列舉值殘留/主幹 hits ≥2/每場至少寫得出一塊牌)+ **反向驗證**(三處故意寫壞:主幹切詞退回「先刪非 CJK」、詞表保底不進帳、圖集 v 不翻 ⇒ MUST 分別紅字)+ `audit_world_text.mjs`(語料一改,牌面就跟著改)+ `npm test` / `npm run bal`(㋒ 純表現層,MUST 全數不動)。**改 `vernacular.js` 的分類/取名規則 MUST 重跑 `node tools/bake_venue_text.mjs`** —— 語料是它的輸出;不重烤 = 底本走舊規則、執行期補收走新規則,兩份語料在同一張圖上打架 |
| 異常狀態致盲白幕(`CC_FLASH`/`ccFlashAlpha`/`_ccFeed`・`_blindFlash`・`_updateCcFlash`/`.cc-flash`)/ 蓄力跳水平移速(`CJUMP.AIR_SPD_F`/`_chargeJump`/騰空移動段) | `audit_cc_flash.mjs`(44 項;Ⅰ 曲線全白→漸淡→歸零、Ⅱ 強度表只收光學系、Ⅲ 觸發/衰減/清除各一處且都接上、Ⅳ 行為直測「當下更亮者勝」、Ⅴ 水平倍率兩端同吃且垂直不吃)+ 真機冒煙(被雷爆彈/EMP 招式命中畫面全白後漸淡、HUD 仍讀得到;蓄力跳同蓄力比例跳得更遠但高度不變) |
| 射程/傷害/`sight`/`RANGE_SIGHT_F` | e2e 重驗:輕武器 NPC range ≥170(#INC-104)、t01/s02 crit:0、s02 heavy = launcher、「塔 310 > 所有輕武器」與「所有重武器 > 塔 310」雙不等式 |
| 骨架/關節/步態 | 全角色 rig 稽核 + `audit_cast_jump.mjs` |
| FPV 座艙取景(`COCKPIT`/`ndcH()`/`_buildCockpit` 系) | `audit_cockpit.mjs`(視野帶淨空、裝置 < 武器、消失點對準星;頂緣逐頂點投影量測) |
| 武裝掛點/槍口 | `audit_muzzle.mjs`(32 英雄 + NPC 四陣營) |
| `MAP_EXPAND`/`CLEAR_F`/`LANE_MIN`/塔位 | headless 建 `BattleSim` 數 `sim.camps.length`(L1 2/2、L2 4/4、L3 6/6) |
| `VENUES[].ll` / `MAPGEO` 尺寸常數 | `node tools/bake_venue_lanes.mjs` 重烤 `venueLanes.js`(外網,㋓) |
| 場地場景標記(`VENUES[].scen`/`relief` 系・`SCEN_LABEL`・`spansWater`) | `audit_lane_scenarios.mjs`:標記 MUST 由實測產生(多標/漏標/`relief` 對不上皆紅);㋓ 走 Actions「兵線場景掃描」,`ci.yml` 刻意不含。**③ 陸上高架橋 / ⑨ 水上高架橋分流只有 `spansWater()` 一支**(scanVenue 與 `--probe` 同吃)—— 兩份實作會出現「探測說陸橋、掃描說水橋」這種只在特定場地現形的分歧 |
| 場地選單的路線/地形說明(`venues.js venueRoute`/`reliefTier`/`RELIEF_TIERS`/`venueTip`/`venueBrief`・`main.js renderVenues`/`renderVenuesOpen`/`syncVenueTips`/兩處狀態列・`.venue-desc`) | `audit_ui_layout.mjs`(㋒ 純表現層)+ 路線摘要 MUST 全由 `venueConfig()` + `data.js laneTacticsXZ()` 推導(手寫長度/彎曲度 = 重烤兵線後靜默分家)、起伏門檻 MUST 是 `altTier()` 的倍數(與 ⑧ 判定同一把尺)+ 說明 MUST 走 `tip.js attachTip`,**MUST NOT 退回 `title=`**(觸控無 hover ⇒ 手機上整份說明消失)+ 換人數 MUST 重掛(摘要吃人數)+ 真機冒煙(滑鼠移上/觸控長按看得到、選定後狀態列同步、切人數數字跟著變) |
| `venueLanes.js` 重烤 / `TOWER_*` / `tower.range` | `audit_map_rules.mjs`(#4)+ `audit_lane_sep.mjs` + `audit_lane_grade_sep.mjs`(#5 洞內塔 ≥20% 射程涵蓋洞口外) |
| 兵線導航規則(`UTURN_MAX_DEG`・`TURN_ACCUM_MAX_DEG`/三 audit/bake 閘門) | `audit_lane_navigation.mjs`(35 項);規則①③生效於既有場地需重烤(㋓)+ **`audit_traverse.mjs`**(導航規則驗的是**幾何契約**,這支驗「一個機體真的走得過去嗎」—— 兩者不可互相取代) |
| 可通行性(`tools/venue_field.mjs` 的高度管線/結構剖面抽取/**`makeCarvedField`**・`tools/audit_traverse.mjs` 的 `flood`/`makeSurfaces`/`buildStructs`/`carveRuns`・`SLOPE.STRUCT_M`・`sim.solidResolve`) | `audit_traverse.mjs`(逐場地泛洪 + 航點斷言:兩座主堡・每一座塔位・每一座洞的兩端洞口與洞中・每一段橋面・地下道兩端引道)+ **反向驗證 `--break-slope`**(把坡度閘寫死成全擋 ⇒ MUST 紅字;原則 9)。另有「開挖鏡射(合成場)」四項**不需網路**的行為直測(路廊壓到路面高 / 過渡帶外逐位元不動 / 過渡帶是斜壁 / 天然高度場未被就地改寫)—— 開挖走廊是**敞開補集**,分段規則 MUST 逐字鏡射 `biomes.js`;淨空(V-D)MUST 仍吃天然地形。三條會靜默失效的線:①visited 的鍵 MUST 是 **(格, 高度桶)** —— 一格一位元會把每一段階梯/引道/洞口報成不可達;②高度桶 MUST 是**固定量化**,MUST NOT 用 ±tol 模糊比對(斜坡上無限乒乓,別處實測 770k 格跑出 53.6M 次拜訪不會結束);③換層 MUST 兩端都「通天」(`buried ≤ OPEN_M`),否則泛洪會直接穿過山體走進洞裡 = 假綠;④**例外 MUST NOT 洗成「跳過」** —— 跳過只留給外部服務取不到(高程磚/圖資)的降級,程式自己的 ReferenceError/TypeError 洗成跳過就是假綠:收尾照印「✅ 通過 N 項」,而那 N 項裡根本沒有那個場地(2026-08-03 前科:`WATER` 漏 import ⇒ 27 個場地裡**每一個有橋的**共 10 個整批靜默不驗,橋面航點與橋下淨空從來沒跑過)。取不到路網時**自動降級成地形層**並在結尾標示未驗(原則 6),MUST NOT 因此報綠 |
| 地下道(`underpassPlan`/`tunFloorAt`/`UND.*`/引道 `cut`・`open` 系)/ 結構資格閘(`strucTunnel`) | `audit_underpass.mjs`(155 項;**山體隧道 MUST 逐位元不變**、引道回地表、縱坡 ≤ GRADE_MAX、四放棄條件、引道垂直路塹 + open 段消費端閘門、⑩ **全寬覆蓋**:下沉量全斷面取樣 + 延伸後基準線收斂 + 覆蓋區間全寬拆縫(側向橫坡/網格細碎起伏/延伸端土丘/側向溝壑四場景行為直測「牆頂與頂板 MUST 全程藏在地表下」,平地逐位元不變)、Ⅵ 資格閘:人行/室內 tunnel 不成洞且去重候選同閘、Ⅶ 幾何側壁:覆蓋段/圍裙/深路塹跨出必擋・道路兩端與淺端放行・山體無 by 不變、Ⅱ-d 明隧道誤判雙防線:碗緣高於路面/碗緣是自家開挖 ⇒ 連 under=false 都不判,天然峽谷才是對照組)+ **反向驗證**(gMinOf 改回只量中心線 / 拿掉基準線收斂 / 拿掉全寬拆縫 ⇒ 分別 ⑩a・⑩b / ⑩c / ⑩d 紅字) |
| 明隧道(`tunnelWallProfile`/`TUN.*`(含 `OUT_W` 落差掃描)/`terrain.natureAt` 天然地形快照/`gal` 遮罩上傳/`galBores` 洞內打洞/明隧道淨空/sim `tunnelSideExit`・`_slabSep` 隧道分支/落石棚強制覆蓋・同 tag 併鏈/`carveGalleryBands` 柱外淨空帶/`carveTunnels` 敞開段殘峰) | `audit_open_tunnel.mjs`(159 項;**深埋隧道 MUST 逐點同舊制且 gal=0 伺服器逐位元不變**、幾何 `hw`/segs 不動、柱列不進 `cols`、⑪ 近帶岩背不判開放、④-b/⑫ 覆蓋薄但兩側都在地形內 ⇒ 貫穿隧道不判明隧道(金龍實案)、⑬ 對開挖單調穩定、⑭ 落差掃描只認天然地形、Ⅴ 三個呼叫端全餵 `natureAt`、Ⅱ 明隧道段發 bore 蓋滿/深埋不發、Ⅲ-b 淨空三消費端 + punch 單次呼叫、Ⅲ-c 走廊地物淨空 ≥ 最寬開挖足跡且推導不手寫、Ⅲ-d 神木腳印整盤驗淨空(只問中心格 = 巨幹橫插洞內)、Ⅲ-e 貼地道路緞帶/標線不得畫進別條路的洞內斷面(走廊清單帶頂板高 cy,三消費端全掛 + inTunBore 行為直測)、Ⅳ 以真 BattleSim 直測兩端可穿透與 z 鏡射側別、Ⅴ 執行 terrain 原文直測:敞開段殘峰無條件開挖/覆蓋端斜壁帶保護/柱外淨空帶只降不升・端帽不外溢 + avalanche_protector 強制覆蓋/同 tag 併鏈靜態規則、Ⅵ 執行 `punchPortalHoles` 原文:斷面裡的三角形全刪**含跨越走廊但無頂點在內**那一片、走廊外/路面下/天花上三條界不得放寬、洞緣進 rims)+ `audit_underpass.mjs`(山體/地下道不變式)+ `audit_slope_move.mjs`(洞內豁免坡度閘)+ `tools/shot_tunnels.mjs`(㋓ 需 playwright:三結構 × 兩端出入口/洞體中間多視角出圖 + 斷面殘留・洞口見天・由上而下看穿三項掃描) |
| 道路路基整平(`gradeRoadBeds`/`carvedNodes` 足跡/biomes 呼叫端收集) | `audit_road_bed.mjs`(16 項;Ⅰ 橫坡切填帶內壓平帶外不動、Ⅱ 填方上限漸退不築壩、Ⅲ 水域紀律、Ⅳ 開挖足跡優先、Ⅴ 確定性逐位元、Ⅵ 呼叫端橋/步道/結構/泡水段不整 + 排在 markGradeCorridors 前)+ `audit_slope_move.mjs` + `audit_underpass.mjs`/`audit_open_tunnel.mjs` 不變式 + 真機冒煙(陡橫坡路段路面貼地可踩、峽谷邊不長土壩) |
| `SOLDIER_H`/`HERO_SIZE.mul`/`BRIDGE_RISE`/`TUN.CLEAR` | **`audit_traverse.mjs` 的「淨空」段**(2026-08-03 起這條有腳本了;在此之前它是一次**手動**檢查,而剖面寫錯基準是**完全無聲**的 —— 隧道就是一個黑洞、裡面有個黑影在動,肉眼永遠看不出來拱頂差了多少)。資料層兩條(隧道淨空 / 橋面抬升 ≥ 最大機體 + 0.2)不需網路;有圖資時另逐座量「橋下跨中淨空」與「洞體覆蓋厚度」。機體高度 MUST 由 `heroTargetH` 推導,MUST NOT 手寫 4.5 |
| 塔或機甲任一數值 | 重算 `towerHp = 1.8 × heroEHP × heroDPS / towerDPS` |
| 攀爬路線(`climb.js` 系;含 `attachFaces`/`attA`/錨件跨接臂/`facilityEndY`/`slopeRamp`/`climbShare`/`highFaceShare`/`siteSlopeDeg`、生成端 `b.ty`/`b.tr`) | `audit_climb.mjs` Ⅰ・Ⅱ・Ⅳ・Ⅴ・Ⅵ・**Ⅶ**・**Ⅷ**・**Ⅸ**・**Ⅹ**(211 項;Ⅶ = 正面規則 + 視覺/碰撞盒朝向同調 + 跨接臂;Ⅷ = 設施端點貼齊實體面 —— 攀爬柱 `y1` 逐位元不動、`vy1` 收到實測屋頂、落差夾在 `GRAB_UP`、四種結構生成端都餵實測值;Ⅸ = 抽中機率 ← 陡度,曲線轉折點與 `slopeMoveF` 同源、`slopeBlocked` 逐度反查恆 100%、地面路線與相鄰相接同曲線同量測、量測不消耗亂數;Ⅹ = 高側取捨 ← 陡度,平緩帶 100% → 阻擋角 0%(一次都不出現)、與 `climbShare` 共用同一份 `slopeRamp`、四面等高不排除任何一面、陡地形只換一面不整棟放棄)+ 真機冒煙(掛梯/推杆/登頂開火/跳離/箭頭辨識/相鄰相接;**長梯/抓點頂端貼著屋頂不再突出、陡坡山城的建築幾乎棟棟有梯且梯子一律在下坡那幾面、平地街廓仍約三成**) |
| 障礙橫斷面(`_blockerHitT`/occ 上傳/`_losBlocked`) | `audit_climb.mjs` Ⅲ:兩端對同一盒同線段 MUST 同判(含 ry 反號、細長樓側面、圓柱不變、上下方向對稱) |
| 塗層阻擋 / 隧道頂板(`_slabHitT`/`_terrainSegT`/`_layerHitT`/`_blockerHitT` 垂直帶/爆點基準・`biomes.js tunRoofTop`/`makeTunnelIndex` 的 `roof` 欄/`galRoof` 的 `topAt`/`galCols` 的 `y1`・`main.js surfaceAt` 的頂板站立面) | `audit_layer_block.mjs`(61 項;Ⅰ 頂板**板體** [ceil, roof] 與**路面**雙面 + 板體內/擦邊都算穿越 + 頂板上方水平對射不誤擋 + open 放行 + 橋面同側不誤擋 + 兩組反向對照(拿掉路面項 / 頂板退回零厚度)、Ⅱ 盒頂/盒底不分方向、Ⅲ MUST NOT 退回單面 `heightAt`、Ⅳ 洞內爆點基準取路面且 lev 只報 0/2、**Ⅴ 頂板頂面 = 可站立結構面**:`tunRoofTop` 只有一份定義且三個消費端全吃、除門檻式外無手寫 `+ TUN.ROOF_T`,並執行 **main.js 真品 `surfaceAt` 原文**直測「明隧道站得上頂板 / 自上方落下停在頂板 / 陷進板體被抬回頂面 / 洞內仍站路面不被吸上去 / ribbon 外照踩地表 / 深埋隧道逐位元仍是山體地表 / open 段無隱形頂板」+ 反向對照「拿掉頂板站立面 ⇒ 明隧道整組踩空,而洞內/深埋不動」)+ `audit_underpass.mjs`/`audit_open_tunnel.mjs`(山體隧道與明隧道幾何 MUST 逐位元不變)+ `npm test` / `npm run bal`(㋒ 客戶端物理 + 表現層,伺服器 lev 語意不動 ⇒ MUST 全數不動)+ 真機冒煙(**從山坡走上明隧道/落石棚頂板不再穿模掉進洞裡,走到簷口才落下**;站頂板上朝洞內開火一發都打不到、洞內往上打也打不到站頂板的人;貼著頂板削過去的擦邊彈打在頂板上;洞內通行與洞口進出手感不變) |
| 橋交會去重(`dedupeCrossingBridges` 系) | `audit_bridge_crossing.mjs`(16 項;優先度 兵線 > 鐵路 > 大馬路 > 小馬路;鐵路容差 `gap=0` 只認真交叉) |
| 馬路橫切繞行(`skirtWaterClips` 系) | `audit_water_skirt.mjs`(8 項;斜交對稱穿越 MUST 建橋不繞;步道一律不進橋樑管線 `PED_HW`) |
| 道路塗裝寬/標線/結構接合(`carriageHw`・`flareHw`/`ROAD_FLARE_M`・`markBaseAt`・`mHw`/避車道 `avoidHw`・橋隧柏油定調) | `audit_road_joint.mjs`(86 項;Ⅰ 塗裝寬 === 一般路通行寬且推導不手寫、Ⅱ 漸縮曲線兩端貼合 + 切線歸零 + 單調、Ⅲ 標線消費端單一縫 + 橋隧同款柏油與標線 + 路燈/行道樹仍排除、Ⅳ 漸縮帶行為直測:半寬 hw→車道寬、起點高逐位元貼結構路面、三角形全朝 +Y、四條跳過條件、Ⅴ 通行寬仍只有 `strucHw`,塗裝縫不得滲進走廊/開挖/物理段;Ⅳ 另含**邊帶同步漸縮**:邊帶外緣與楔形同一條 `fhw`、外端收到零、車道緣不漂、差額 < `AVOID_MIN` 時與楔形同進退)+ `audit_underpass.mjs`/`audit_open_tunnel.mjs` 不變式 MUST 不動 + 真機冒煙(進出洞口/上下橋標線與路緣線連續、橋面不再是泥土/水色、接合處無橫向階) |
| 橋上砲塔墩座(`planTowerBridgePads` 系) | `audit_bridge_tower_pad.mjs`(23 項;沿橋軸與水側走位帶 ≥ 基座 + 8m;`TOWER_BASE_R` 不變) |
| 地形射線(`rayTerrain`/`punchPortalHoles` 系) | `audit_terrain_ray.mjs`(11 項;與暴力掃逐條一致;加速比 MUST 兩位數) |
| 都市規劃朝向(`ground.js` orient/`gridA`) | `audit_ground_qc.mjs` ⑦(垂直街道網 mod 90° 摺疊不抵銷;orient 固定抽 2 枚 rnd) |
| 地貌交界(`planSeamOverlays`/`SEAM_STYLES`/`seamAlpha`) | `audit_ground_seam.mjs`(49 項)+ `audit_ground_qc.mjs` 全綠 |
| 小區域組合風格(`planEnclaves`/`ENCLAVE_STYLES`) | `audit_ground_enclave.mjs`(33 項;消費端 MUST NOT 硬編第二份組合表) |
| 賽璐璐管線(`toon.js RAMPS`/`toonGradient(bands)`/`outlineMaterial` 的螢幕下限・`field.js` 全支・`rng.js`・`environment.js skyStops`/`makeSkyDome`/`makeClouds`・`terrain.js` 的 `envMat` 兩路徑/`GROUND_TONES`/`paintTerrainTones`・`postfx.js` 全支・`game.js` 的 `this.pipeline` 與 `antialias`)| `audit_cel_pipeline.mjs`(52 項;Ⅰ ramp 家族 —— 3 階逐位元、每組暗階 ≥102、DataTexture 只在 toon.js;Ⅱ 天空穹頂 —— `skyStops` 內**無任何十六進位色值**(第四張色表的唯一檢出點)+ 兩道封頂以真品原文直測 + 地平線階 = 霧色本身;Ⅲ 地形 —— 三處 `envMat` 都傳 `rim:0`、色階亮度嚴格遞增且**階差嚴格遞減**、**27 場地 × 三種隊制實跑 `field.js` 真品驗單一色階佔比 ≤ 35%**;Ⅳ 描邊 —— 螢幕下限由 `projectionMatrix[1][1]` 反推、`max(世界寬, 螢幕下限)` 保證近距離逐位元同舊制)+ `audit_gpu_lifecycle.mjs` ⑦(後製管線的 A25 生命週期與 `RES_GOV` 交互)+ **`shot_scene.mjs` 定場鏡頭組前後對照**(㋓ 需 playwright;`--ink=0`/`--grade=0`/`--post=0` 逐層隔離 —— 「這張圖變醜是哪一層造成的」要能回答)+ `npm test` / `npm run bal`(㋒ 純表現層 ⇒ MUST 全數不動)+ 真機冒煙(**幀時 30 秒穩態桌機 + 觸控模擬前後對照 —— 這是唯一決定勾線可不可行的數據**;`vfx.js`/`castfx.js`/`hazards.js` 的**寫深度透明件**要逐項看過,寫深度的粒子/光束/貼花在勾線 pass 下會變成斑點)|
| 世界文字(`worldtext.js` 全支(含 `SIGN_STYLES` 九種語域/`signAspect`/`packCells`/`PAINT` 版面表)・`vernacular.js` 的 `pickName`/`signCopy`・`biomes.js buildWorldSigns` ⑤~⑨ 與 `planCorpusSites`・`biomes.js` 的 `buildWorldSigns`/`signSpots`/`portals` 的 `tags` 欄/`generic` 的 `tags` 欄/`fetchOsmFeatures` 的具名點位查詢與 `geoKey('osmF', N)`・`visualPrefs.js worldTextLang`・`main.js renderVisualSettings` 的選項分支・`help.js UI_TIPS.visual`) | `audit_world_text.mjs`(57 項;Ⅰ 名字解析 —— 執行 `resolveName`/`resolveRef` 真品原文:無名/全空白/超長一律 null、三種語言各取各的、沒有譯名退回原文、控制字元與連續空白收斂、ref 多值取第一個、旋鈕表的每一個語言解析器都認得;Ⅱ 牌面規則 —— 缺字偵測基準與兩成門檻、三道閘、額度 = atlas 格數(推導)、寬由格子長寬比推導、長名字縮字級、無 `DoubleSide` 而是兩片背對背、不透明、`rim:0`、只有一個 `THREE.Mesh`、空清單回 null、UV 內縮半像素;Ⅲ 接線 —— 一份實作一個呼叫點、錨點取自真幾何、**零亂數消耗**、不新增碰撞、住宅不掛招牌、**快取版本已跳**、四類具名點位都進查詢且分支排在「其餘當建物」之前;Ⅳ 設定頁選項渲染成分段按鈕、鍵不寫死、說明有講;**Ⅴ 圖集裝箱** —— 執行 `packCells` 真品:九種語域混排全裝得下/不重疊/不越界/回傳順序還原(不還原 = UV 全部配錯牌)/裝不下記 dropped 不靜默/決定性/v 翻轉/ClampToEdge)+ `audit_vernacular.mjs`(語料那一半)+ **`tools/shot_signs.mjs`**(㋓ 需 playwright:九種語域各一張排面 + 圖集 PNG —— **版面長什麼樣、缺字偵測在有字型的環境成不成立,只有這裡看得到**) + `audit_visual_prefs.mjs`(旋鈕表多一個 `choices` 型別)+ `audit_object_joints.mjs` / `audit_open_tunnel.mjs` / `audit_underpass.mjs` / `audit_road_joint.mjs`(biomes.js 原文抽取的相鄰稽核) + `npm test` / `npm run bal`(㋒ 純表現層,伺服器完全不涉入 ⇒ MUST 全數不動) + 真機冒煙(**走進一座有名字的隧道,洞口門樑上方掛著匾額且收在洞口寬度內**;橋頭兩端各一塊橋名牌朝著上橋方向;山峰/地名/交流道的標牌兩面都讀得到字(**背面不是鏡像**);有名字的商辦立面上緣有招牌;**沒有名字的商業建物改掛直式招牌,上面是這座城市真的有的店名/廟名 + 一行日常副行**;路肩綠底路牌寫著真實路名 + 箭頭;住宅區佈告欄寫著里名;地標旁解說牌寫著地名 + 標高;同一張圖不出現兩塊同名招牌;設定頁把「世界文字語言」切成中文,牌面當場換成譯名、沒有譯名的維持原文;圖資取不到時一塊牌都不掛、也沒有空白牌)|
| 語意化地標(`beacons.js` 全支・`biomes.js placeBeacons` 與其呼叫點/`stats.beacons`) | `audit_beacons.mjs`(68 項;Ⅰ 型錄與外廓 —— 純區塊零 THREE(本項能離線驗的原因)、標稱 `foot` **雙向**貼著零件實算(低報 = A30、虛胖 = 被推遠)、高度隨錨點淨空圈分級、`FAR` 大於主堡那一圈、傾斜件外廓取 3D 半對角;Ⅱ 錨點推導 —— 三類齊全且座標真的落在兵線/主堡/塔位上、前線塔位優先、切向是單位向量、**零亂數**、沒有兵線就沒有錨點;Ⅲ 以真品 `planBeaconSites` 跑合成 probe 行為直測 —— 由近而遠取最近一圈、`probe` 收到的腳印恆為 `foot + PAD`、側向優先(落在路邊不是正前方)、`ry` 面向錨點、**全拒就一件都不擺**(原則 6)、近處不行往外找、超過 `FAR` 不擺、`SEP`/`MAX`、逐位元確定性、三款輪替都輪得到;Ⅳ 消費端單一縫 —— 一份實作一個呼叫點、`probe` 四道閘(`areaFree`/水域沼澤腳印周圈/`flatRadiusAt`/圖外)、碰撞柱取實測且 biomes 不讀標稱 `foot`、`blockArea` + `blockers` 都登記、落底走 `sinkBaseY`、**不碰共享 `rnd`**、排在植被之前巨岩之後、不掛 `outlinify`)+ **反向驗證** `--break-extent`(撐爆一件零件 ⇒ Ⅰ 紅)/ `--break-pad`(預留餘裕歸零 ⇒ Ⅲ 紅)+ `audit_object_joints.mjs` / `audit_gpu_lifecycle.mjs`(新的世界物件與合併幾何)+ `npm test` / `npm run bal`(㋒ 地物散布,伺服器不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**出主堡就看得到一座通訊塔、推到塔位旁有水塔、兵線中段路邊有電塔/貨櫃堆/敖包 —— 而且沒有任何一件站在路上**;地標不在水裡也不掛在懸崖邊;同一張圖重開兩次地標位置逐位元相同;走過去撞得到、打得到,外廓與碰撞一致;`stats.beacons` 有數字)|
| 建物零件庫消費端(**兩個整棟量體桶**)(`biomes.js` 的 `BLD_LIB`/`bldGeo`/`bldLibN`/`MASS`/`buildBldBucket`・一般建物繪製段的 `massOk`·`massPick`·`emitMass`·`t.ord`・`tools/ai3d/tri_budget.json families.building`(含 `mass` 那一塊)・`tools/ai3d/parts_src.mjs bldLibDescs`/`nodeCap`・`tools/ai3d/measure_building_tris.mjs`・`tools/parts_review/review.js` 的 `bld` 分支) | **2026-08-09 使用者定案「開」第二個「整棟量體」桶(低矮建物)並選定 8/8 切分**(runbook §5al-c 的 (a);縫 + 兩顆節點 `masslow_a`/`masslow_b` 同輪出貨 = §5am)。**兩個桶吃同一個 `facade_wall` 桶 ⇒ 額度是同一份**:`MASS.PICK_N`(高層 8)+ `MASS.PICK_N_LOW`(低矮 8)**加起來**才是 tri_budget 的 `pick_n`(= 額外 draw call 上界 16),`node_cap` 的除數正是那個總數 ⇒ 切分比例改不動逐節點上限(2,981),已出貨的 mass_a/mass_b 不受影響;加總對不上 = 額度憑空多出一份,而畫面上只表現成「這張圖的高樓好像特別多」(稽核釘住)。**資格取自兩個既有判準的對角線兩格**:`commercial && h > MIN_H` → `mass`、`!commercial && h <= MIN_H` → `masslow`,另兩格(低矮商辦 / 高層住宅)**刻意維持方盒** —— 為它們再訂一個門檻就是第三個手寫數字。排序兩桶各有各的(高層排最高、低矮排**足跡面積**),同值時一律再以座標定序。**探詢收成一支 `libOk(key)`、桶建構器仍是一份 `buildBldBucket.mass(n, mat, i, key)`** —— 逐桶各抄一份就是「兩桶的保險絲不一樣」;保險絲**逐桶各自成立**(該名冊沒載到 ⇒ 那一桶一棟都不挑)。**`masslow` 名冊同樣 MUST ≥2 顆**才算開好(2026-08-09 出貨:穀倉 + 鄉村教堂)。**兩桶的 `node_cap` 是同一份推導**,`tri_budget` 的 `families.building.masslow` 只是為了 `nodeCap(fam, kind)` 以名冊鍵查表多存一格(稽核釘住兩者相等 + `same_as: 'mass'`);**實體化參數不是全族一份** —— 教堂的鐘樓尖頂要 `--offset 0.014` 才收斂成水密單元件(0.006 / 0.010 分別是 3 / 2 元件,而「watertight 但 2 元件」最容易被當成過了)。**離線工具的節點前綴 MUST 是 `building/mass`(無底線)** —— `building/mass_` 那個底線會把 `masslow_*` 整組排除掉(`shot_scene` 的 `mass_near` 認人已犯過一次)。以下為兩桶共通的現制:2026-08-08 佇列 F(runbook §5aa;使用者定案推翻 asset_plan §8.1 的 `BUILDERS` 那一列)。四桶:三桶屋頂配件 **整桶換**,第四桶 `mass`(整棟量體)**只換一個子集** —— 而那是量出來的:主量體是單位方盒 12 tris × 全圖 1,325 個 instance ⇒ 整桶換的逐節點上限只有 **36 tris**,而 §5o 實測 500 面就留不住 Art Deco 的退縮量體。子集大小 `pick_n` 由「細節下限 cap ≥ 2 × 500」與「額外 draw call ≤ 立面段現行的 16」取較嚴者反推 = **16**,`node_cap` = 3 × 15,900 ÷ 16 = 2,981;高度門檻沿用既有的退縮頂塔 55m。**五條紀律**:①`pick_n`/`min_h` 是 biomes 與 tri_budget **同一份**值(稽核釘住相等,改一邊不會報錯、只讓預算與實際挑中的棟數分家);②挑選是純函式(只讀 `b.commercial/h/x/z`、**零 rnd**、等高以座標定序 —— 靠 sort 穩定性決定誰入選 = 跨客戶端分家);③名冊或庫取不到 ⇒ 一棟都不挑 ⇒ **逐位元同舊制**;④碰撞/LOS 有向盒一格不動(A30);⑤逐實例色抖吃拆桶前的原始序 `t.ord`(吃新索引會把其餘每一棟的配色平移,沒有錯誤訊息);⑦**挑中的那幾棟純視覺附件改推丟棄桶**(`vis()`:節點自帶頂部造型,程序頂塔/看板/天線會浮在半空)—— 只換「推去哪裡」、`rnd()` 照抽(A4),而**帶碰撞柱的兩件 MUST NOT 進丟棄桶**(主量體 + 臨街裙樓;少一根碰撞柱 = 載到庫與沒載到的客戶端權威幾何分家)。⑧**層高**(2026-08-09 使用者定案「不同建築可以有不同窗戶大小,但**上下樓層間距要在合理差異範圍以內**」):立面貼圖沒有 per-instance repeat ⇒ 一張圖被拉滿**那一件**的高度 ⇒ **層高 = 件高 ÷ 列數**。列數 MUST 由 `facadeRows(h, commercial)` **推導**(唯一縫;方盒 / 整棟量體 / 斜頂三桶同吃 `rowsOf(t)`),MUST NOT 回到「立面款自帶的常數 + 樓高分桶」—— 舊制實測全面出界(住宅 h=8m → 1.6m、商辦 h=13m → 1.1m、h=104m → 8.0m)。①**規則是「先落在帶內、再貼近目標」**:候選 = 級距上讓層高落在 `[STOREY.MIN, STOREY.MAX]` 的列數(這一帶就是使用者說的「合理差異範圍」),取其中**對數距離**最接近該類別目標者(層高是比例量:20 列差一列與 2 列差一列不是同一件事);`MIN·MAX/(MAX−MIN) = 5.01m` 是「必有合法列數」的推導門檻,低於它的件只放得下一層(層高 = 件高,而 2.6~5.0m 的單層倉庫/教堂中殿本來就合理)。②**逐件不是逐棟**:退縮頂塔(0.22h)與臨街裙樓(max(6, 0.12h))有自己的高度,吃主體的列數會壓出 **0.6~1.8m** 的層高 —— 那是舊制最誇張的一格,而它在「逐棟」的框架裡永遠修不到。③**級距是 draw call / 貼圖記憶體的旋鈕**:列數進了分桶鍵(款 × 列數)⇒ 級數一多桶數就長(taipei101 `--live` 實測:×1.2 的 17 級 + 款自由跨帶 = **145 個** facade 桶;×1.35 的 12 級 + **款綁級距帶** = **50 個**)。級距比 MUST < 帶寬比(`MAX/MIN`),否則某些高度找不到帶內候選而**靜默出界**;級距上界推導自 `objHeightMax() ÷ 最小的目標層高`(只取 `÷ MAX` 是「放得下」不是「貼得近」—— 實測那樣 104m 的住宅一路貼著上界 4.73m)。④**款綁在級距帶上**(每帶 `LOOKS_PER_BAND` 款,由收錄期的 `v` 在帶內二選一):讓款自由跨帶,桶就是「款數 × 級數」全部 populated;桶數的**結構上界**是 2 × 款數 × 級數(附件件吃自己的列數 ⇒ 不只落在對角線上)。⑤`facadeTexH(rows)` 讓每層固定拿到 24 texel、上下限 [256, 1024](**10 列以下維持 256 ⇒ 絕大多數建物的貼圖逐位元不變**)—— 層高對了窗仍可能細:35 列的塔樓在 256px 的圖上一層只有 6.6px、窗高 3px。⑥`FACADE_PX` 的欄位刻意叫 `H_MIN`/`H_MAX`:叫 `MIN_H` 會與 `MASS.MIN_H` 撞名,而 `audit_world_height` 與 `audit_siteplan` **都**以原文裡的 `MIN_H` 抓那個門檻(實測兩支同時紅字,訊息還會指向錯的地方)。⑦`pick_n` **刻意不隨立面桶數放寬** —— 立面段由 16 漲到 50 不是「整棟量體也可以變多」的許可證。⑥**材質由呼叫端傳**(該立面款現做的 `wall`:窗格 + 夜間自發光 + tint)⇒ 節點契約多一條「匯出端 MUST 給盒投影 UV」,否則整棟只採到 (0,0) 一個 texel。**UV 契約有三條,後兩條是 2026-08-09(§5ao)補的,而它們壞掉時三個離線閘門全綠**:㋐**有 UV**;㋑**方向** —— 牆面的 v MUST 隨高度遞增。glTF 的 UV 原點在左上、Blender 在左下 ⇒ 匯出端會把 v 翻過來,而消費端那張 `CanvasTexture` 的 `flipY` 是預設的 true(v=0 採到畫布底部)⇒ 不在 `normalize_parts` 補償的話,**庫節點的立面是上下顛倒的**(基座暗帶印在屋簷、店面遮陽棚印在頂樓),而方盒那條路走 BoxGeometry 自己的 UV 是正的 = 同一張圖上兩種方向(四顆已出貨節點實測 `corr(高度, v) = −1.0000`,兩輪截圖都沒看出來 —— 窗格陣列近乎上下對稱);㋒**屋頂帶**(斜頂那一桶):庫節點只有**一個材質群組** ⇒ three 取 `material[0]`,方盒那條路「第 3/4 格 = 屋頂材質」對它不生效 = 窗格印在斜屋頂上(2026-08-09 使用者回報)。拆群組會讓**每一棟多一個 draw call**,而 `PICK_N` 的整條推導就是 draw call 上界 ⇒ 區分 MUST 移進 UV:`normalize_parts.py --roofband <node>=<frac>|<minz>` 把朝上面(法線 `n.y > minz`,**平面投影**)壓進 `v ∈ [0, frac]`、其餘壓進 `[frac, 1]`,`facadeTex` 把畫布底部那一條畫成屋頂。**兩個數字是量出來的、且只有一份**(`MASS.ROOF_BAND`/`ROOF_MINZ` === tri_budget `families.building.masslow.roof_band`/`roof_minz`,audit_siteplan 釘住;`intake_parts` 直接量成品 GLB 的 UV 帶):`ROOF_MINZ` = 牆的尖峰與屋頂的尖峰之間**空檔的中點**(實測牆止於 0.15、屋頂起於 0.45/0.65)—— **MUST NOT 沿用盒投影的「主導軸」**(等價於門檻 0.577,會把穀倉整個屋頂判成牆);`ROOF_BAND` = 朝上面積 ÷(朝上 + 側面)的名冊平均(= 兩帶 texel 密度相同),masslow 0.272/0.275、平頂那一桶只有 0.138/0.124 ⇒ 後者刻意不開帶(俯視機會低)。**圖樣 MUST NOT 假設坡向**(兩顆節點的屋脊軸實測就差 90°)⇒ 四款一律「一向排列 + 另一向接縫」。斜頂六款的「牆 × 屋頂形式」MUST 兩兩不同,且方盒那條呼叫端 MUST NOT 傳屋頂色(⇒ `band = 0` ⇒ `WH === H` ⇒ 既有 16 款與六支地標逐位元不變)。名冊值第一格可為**陣列**(輪替名冊,除數由長度推導);**`mass` 名冊 MUST ≥2 顆** —— 只有一顆時同一張圖上挑中的十幾棟塔樓是同一個剪影(尺寸各異、形狀相同),而所有離線閘門全綠(2026-08-09 §5ae 補上 `mass_b`)。分到哪一顆走**位置雜湊**,零 `rnd()`;draw call 不隨名冊長度增加(上限仍是 `pick_n`)。**離線工具端的節點清單 MUST 由 `partlib.js libNames()` 推導**(手寫的那一份在名冊擴充時會靜默過期 —— 同 `% 3` 輪替除數那個坑:檔案在、intake 綠,而 `shot_scene` 的載入讀數與 `mass_near` 機位永遠看不到新節點);`libNames()` **只給離線量測/出圖工具**,遊戲路徑一律走 `libGeo(具名節點)`。驗證:`audit_siteplan.mjs`(218 項;Ⅴ 的整棟量體十一條 + 屋頂帶五條 + 層高十條)+ **反向驗證 `--break-mass`**(pick_n 與預算分家 / 等高不再以座標定序 / 色抖吃新索引)與 **`--break-mass2`**(兩桶挑選數加總超額 / 兩桶資格重疊 / 低矮桶拿掉保險絲)與 **`--break-roof`**(帶寬與 tri_budget 分家 / 斜頂那條呼叫端不傳屋頂色)與 **`--break-storey`**(拿掉「先取落在帶內的候選」那一步 ⇒ 層高全域不變式紅字)+ `intake_parts.mjs`(269 項;`nodeCap` 會去讀 `families[fam][kind].node_cap`,另有 **UV 契約 16 項** —— 反向驗證兩支:`--roofband <node>=0.10` 帶寬分家 ⇒ 2 紅、拿掉匯出端 v 翻轉的補償 ⇒ **8 紅**)+ **逐位元不變 MUST 用量的**:`measure_building_tris.mjs --live --osm-cache` 錄播 Overpass 後做 A/B(同一張圖兩次 `--live` 差到 ±70%,各抓各的圖資量到的全是圖資差異)+ `audit_object_joints`/`beacons`/`gpu`/`soft_stroke`/`cel`/`visual_prefs` + `npm test` + `npm run bal`(㋒ 地物散布 ⇒ MUST 逐項不動)+ 3D 零件對照台 0 缺件/0 孤兒/0 未記載 + **`audit_traverse`(㋓,整棟建物會動到街廓夾出來的通道寬;縫是 no-op 時可省,節點入庫那一輪不可省)** + **鏡像貼補見下一列**(2026-08-09 推廣到巨岩/假山之後,那一把刀與它的閘門自成一列)+ **`shot_scene.mjs` 的 `mass_near` / `masslow_near` 兩個機位**(整棟量體是唯一吃立面貼圖的庫節點 —— 盒投影 UV 一錯就是一塊沒有窗的純色板,而所有離線閘門全綠;兩張是**不同的取景**,`mass_near` 對著最高那一叢、低矮桶換了什麼只有 `masslow_near` 拍得到)+ **`shot_facades.mjs` 排面**(貼圖那一側:方向 / 屋頂帶 / **層高**只有畫出來才看得見,而它不需要圖資 ⇒ 每一輪都跑得起;排面的軸是樓高,每格標「N 列 = 層高 X m」並在出帶時標紅)+ **`measure_building_tris.mjs --live` 的 `draw` 欄**(立面桶數自 2026-08-09 起是變數,而它正是 `pick_n` 那條推導引用的量)+ 真機冒煙(換到的是**最高的十幾棟**:夜間立面要亮、頂塔/裙樓不能與整棟節點自己的頂部造型疊成兩頂帽子;斜頂那幾棟的屋頂面 MUST 是瓦/浪板而不是窗格,基座暗帶與遮陽棚 MUST 在地面那一端)|
| **鏡像貼補**(img→3D 的「另一面是空的」)⚠**改制待執行**(`tools/ai3d/normalize_parts.py` 的 `--rework`/`--mirror`/`_weld`/`_shade`/`_topo`/`_warp`/`_ext`/`_restore_ext`/`MIRROR_MIN_F`・`tools/ai3d/mesh_sym.mjs`(含 `EMPTY_ASYM`)・`tools/ai3d/node_sheet.mjs`・`tools/ai3d/parts_src.mjs parseGlb` 的 `idx` 欄) | ⚠ **2026-08-09 使用者再定案:「只補有洞的,洞很小的話直接貼平,不需要用對稱法補」——觸發條件從『半空間不對稱』改成『真的有洞』、小洞改平面貼補,規格與回退清單見 `docs/ai3d_runbook.md` §5aj-C,尚未實作。在那之前本列描述的是現行出貨行為;動這一族之前 MUST 先看 §5aj-C。** 以下為現制:2026-08-09 使用者定案「img to 3D 會出現另一面是空的問題,由正面對稱的區塊去補對應的區塊,包含建築/巨岩/假山都這樣處理」(runbook §5ad;§5ac 是同一條的建築首例)。**這是條件句** ⇒ 補哪幾顆 MUST 是量出來的:①**尺 = 半空間表面積不對稱**(`mesh_sym.mjs`)—— 空的那半在網格上**不是洞、是一片光滑的板**,開放邊與元件數對它完全無感;②**門檻 `EMPTY_ASYM = 0.12` 錨在使用者自己判定過的那一顆**(§5ac-a 的 `mass_a` 0.123),MUST NOT 退回逐顆手挑的名冊 —— 對四面都長好的岩體照樣鏡射,換來的是一顆左右對稱、接縫帶凹槽的假石頭;③刀落在**已出貨的節點**(`--rework`,要有 `--base`)而不是重跑原檔(`parts_manifest` 的 `source_gap`:重跑復現不出出貨那一顆),**外廓與面數逐位元不動**(`nodeExtent` 那兩個數動完等比還原、鏡射多出的切面減面回原值)⇒ intake 的上下界結構上不受這一刀影響;④**先焊頂點再動刀**(glTF 匯入器不焊回匯出時的法線接縫拆分 ⇒ 平面著色節點在 Blender 眼裡是三角形湯:hoodoo_a 382→96 面、tower_a 開放邊 0→170),焊完 MUST 依**原拆分比**還原著色(不還原 = 低面數岩體變平滑馬鈴薯,而讀數全正常);⑤**兩把刀依主體是不是人造的選**:`half`(bisect+clip+焊,建築)對圓渾岩體會做出**葉緣**、`union`(精確布林,岩體)取外包絡 ⇒ 接縫是內凹岩溝;⑥**三道閘 MUST 排在減面之前**(面數 ≥0.8×原值、鬆散元件不增、邊界邊 ≤ +5% 面數)—— 少了它們壞掉的節點會**安靜地**出貨(tower_a 走 union 元件 1→14 而面數只掉 6%);⑦去對稱化 `warp` 的位移 **MUST 只是位置的函數**(徑向),**MUST NOT 沿頂點法線**(座標重合的獨立頂點法線不同 ⇒ 沿每條硬邊撕開,mega_a 開放邊 0→164),**有破口的節點 MUST warp 0**(位移會把裂縫拉開),振幅錨在未鏡射節點的天然殘差帶;⑧`solidify_parts.py` 端 MUST NOT 復辟鏡射(§5ac-b 兩種寫法都撕爛網格)。驗證:`node tools/ai3d/mesh_sym.mjs --gate`(名冊)+ **`node tools/ai3d/node_sheet.mjs --ref <舊 glb>`(四面黏土對照 —— 這一族的錯只有截圖看得到,§5ac-e 的教訓是「我從來沒有看過這顆節點的背面」)** + `intake_parts`(外廓與預算 MUST 逐位元不動)+ `audit_object_joints --seeds 8` + `audit_beacons` ± `--break-extent` + `audit_siteplan` ± `--break-shy`+ cel/visual_prefs/gpu/soft_stroke + `npm test` + `npm run bal`(㋒ 地物幾何 ⇒ MUST 逐項不動)+ 3D 對照台 0-0-0+ **反向驗證**(拿一顆已知撐不住的節點跑那一刀,三道閘 MUST 紅字)+ 真機冒煙(繞著岩體走一圈看四面)|
| 場址配置規則(`siteplan.js` 全支・`biomes.js` 的 `frontSegs` 收集/街廓配置區塊/`nearestRoadAngle`/`giantCrownR`/`vegSpan`/`partGeo`(AI 零件庫消費端縫)/**零件表的 `lib:` 欄**/`buildVegMeshes`/`placeGiantGroves` 的 `cr` 與羞避段/`placeMegaliths` 露頭段・`beacons.js mergeGeos`・`partlib.js PART_LIBS`・`tools/ai3d/tri_budget.json`) | `audit_siteplan.mjs`(174 項;Ⅰ 都市計畫 —— 純區塊零 THREE、排距不變式推導、**建築線對齊**(進深不同立面仍共線)、沿街節奏為 PITCH 整數倍、路口留白、前後棟留得下巷弄、門朝街法線指向路心、**公設優先**(兩者不重疊)、短段不配、全拒不擺、確定性、上限/間距/三款輪替、`roadFaceRy` 兩側恰差 180°;Ⅱ 公設 —— `foot` **雙向**貼著 `partExtent` 實算、鋪面不掛碰撞、掛碰撞的一律有量體、**方盒登記有向盒且圓恆為外接半對角**(A30)、碰撞體收在 foot 內、停車場的車各自實心、`partCollider` 實算、園樹落點收在 foot 內;Ⅲ 樹冠羞避 —— **冠緣不相碰**(核心不變式,過密群落實跑)、縮冠量由不變式解出、保留率不崩、稀疏群落不無故縮樹、傾斜方向遠離鄰株且有界、確定性、退化輸入不炸;Ⅳ 地質排列 —— 走向 ⟂ 梯度(五組)、平地回 null、格點數/**由核心往外**/體格遞減不超過 FALL、**長軸同向**、節理成排且排距 = pitch、**相鄰排錯縫**、緊密度區間、確定性與平移不變;Ⅴ 消費端單一縫 —— 五支規劃器各一個呼叫點、`nearestRoadAngle` 吃 `roadFaceRy` 且舊手寫朝向式已退場、街道線段只收一次、**街廓配置區塊零 `rnd(`**、probe 五道閘齊全、巨岩吃 `cell.ry` 且舊制隨機轉向已退場、冠幅由 `giantCrownR` 推導且 `GIANT_DEFS` 無手寫冠幅欄、**AI 零件庫解析只有 build 時的 `partGeo` 一份且佈局數學(冠幅/擺動分母)只讀保險絲 `p.g`**(庫幾何隨載入成敗而異,佈局讀它 = 跨客戶端分家 §2.3)、**每一列 `lib:` 都留著保險絲 `g`**、**`lib:` 的家族都在 `PART_LIBS` 裡**(不在 = 整批永久走保險絲,畫面與今天一樣而沒有錯誤訊息)、**原文的 `lib:` 筆數 = 可執行零件表解析到的筆數**(對不上 = 有 lib 列住在離線驗不到的表,那一列等於沒被驗過)、羞避傾斜併進 `tx/tz`、公設碰撞柱只有一條登記路徑)+ **反向驗證**(`--break-line` 進深撐破排距 ⇒ Ⅰ 紅 / `--break-shy` 冠緣間隙歸零 ⇒ Ⅲ 紅 / `--break-strike` 長軸抖動放大 ⇒ Ⅳ 紅;手動:`PART_LIBS` 拿掉一族 / 一列只留 `lib` 拿掉 `g` ⇒ Ⅴ 紅)+ **`node tools/ai3d/intake_parts.mjs`**(外廓契約與三角形預算是**兩個消費端共用**的那一份:GLB 節點外廓 MUST 收在**該列**保險絲的包絡內且不虛胖;三角形**兩道閘** —— 單件 ≤ 該族量測上限、**逐款 Σ 庫零件 ≤ `kind_factor` × 該款現值**(單件合格 ≠ 整株合格:一株神木十幾件全換掉,每件都「合格」卻是 20 倍)。**改 `GIANT_DEFS` 任一零件表 MUST 重量 `tri_budget.json` 的 `kind_tris`**,否則閘門拿舊基準放行)+ `audit_beacons.mjs`(`mergeGeos` 抽成共用縫後 MUST 逐位元不變)+ `audit_object_joints.mjs` / `audit_gpu_lifecycle.mjs`(新的世界物件與合併幾何)+ `audit_open_tunnel.mjs` / `audit_underpass.mjs` / `audit_road_joint.mjs` / `audit_climb.mjs` / `audit_ground_qc.mjs`(biomes.js 原文抽取與地物散布的相鄰稽核)+ **`audit_traverse.mjs`**(㋓ 需網路:沿街多出數百棟建物 ⇒ 街廓夾出來的通道要仍走得通;沙箱取不到路網會自動降級成地形層並標示未驗,MUST NOT 拿那個當綠燈)+ `npm test` / `npm run bal`(㋒ 地物散布,伺服器完全不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**市區沿街的樓立面連成一條線、退縮一致、門一律朝街;街廓裡找得到公園(草坪/水池/涼亭/園樹)、運動場(跑道/球門/看台/燈柱)、停車場(白線車格/停放車輛/收費亭)**;走得進公園草坪與停車場,但撞得到涼亭/看台/車;路口沒有樓長上去;山區的省道兩旁不長街屋;**神木林的樹冠彼此不相接,林冠上看得到一道道裂隙,樹身微微朝空隙側傾**;**裸露地的巨石成片排列、長軸同向、順著等高線成排錯縫,整片同一種岩色**,而不是各轉各的散石)|
| 聚落場 / 建物放大器 / 建物來源信任階梯(`biomes.js` 的 `settlement`/`UC`/`URBAN_DENS_P·Q`/`URBAN_MIN_PEAK`/`infillSeeds`・`densifyUrban` 的 `seeds` 參數與其呼叫點・`nearUrban`・`classifyImg`/`classify`・`urbanPts` 收集閘・備援程序街區的 `!osm` 閘・`placeBoundary` 的 settlement 閘與降格選型)或 場地地貌宣告(`venues.js VENUES[].mix`/`.type`/`ll`) | `audit_siteplan.mjs` Ⅴ・Ⅵ・**Ⅶ**(聚落場恰一份實作、放大器同吃、補間種子排在 planBlocks **之前**、`densifyUrban` 不自己撈 `generic`、閘門不讀 `venue.mix`;Ⅶ 信任階梯:`classifyImg` 恰一份且零亂數、市區種子純影像閘、備援街區 `!osm` 閘、邊界樓過聚落場且**降格不留缺口**(荒野邊界零棟/市區照長/裸露地降格成巨岩的行為直測),**反向驗證 `--break-gate`**;**行為直測**:1~3 棟孤立設施 ⇒ 一棟都不配且補間種子 0、4 棟 ⇒ 照配、地標同權計數,並以「四棟同格」的種子讓市區個案**逐位元回到舊制**(+90 棟 / 8 根碰撞柱)⇒ 證明這一刀只切荒野不切市區)+ `audit_venue_biome.mjs --offline`(宣告自洽 + 建物管線不讀 mix)+ **完整版 `audit_venue_biome.mjs`**(㋓ 需 Overpass:地被組成與建蔽率實測,列出不符宣告的場地)+ `audit_beacons.mjs` / `audit_object_joints.mjs` / `audit_open_tunnel.mjs` / `audit_underpass.mjs` / `audit_road_joint.mjs`(biomes.js 原文抽取與地物散布的相鄰稽核)+ `npm test` / `npm run bal`(㋒ 地物散布,伺服器完全不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**開太魯閣・燕子口與合歡山:峽谷/草原兩旁只剩圖資本來就有的那幾棟,沒有整排街屋、沒有公園/球場/停車場,地圖邊界一圈是巨岩/神木牆而不是高樓**;開台北 101 或首爾江南:街景與改動前一模一樣,樓一樣密、邊界樓照長)|
| 景深模糊(`data.js DOF`/`combatReachM`/`dofNearM`/`dofFarM`/`dofAimBlend`・`postfx.js` 的 `dofTaps`/`_dofMaterial`/`setDof`/`setDofBlend`/`_pushDofA`/`_dofA`/`_dofBlend`/chain 的 dof 那一格・`game.js` 建管線後的 `setDof` 那一行、render 前的 `setDofBlend` 那一行與 `?dof=0`・`matsample.js` 的 `DOF_NEAR`/`DOF_FAR`/`BG_Z`/背景排・`tools/shot_scene.mjs` 的 `LAYERS.dof` 與 `pipe?.setDof`・`visualPrefs.js` 的 `dof` 旋鈕・任一武器/招式的 `range`、`UNITS.tower.range`、`altRangeMax`/`RANGE_TOL`/`hyperMaxArcM`) | `audit_visual_prefs.mjs` Ⅵ(45 項;Ⅵ-a 兩個轉折點定義式內無公尺數字面值 + 比值 = 各自係數 + 係數 = 使用者提的 1.5×/2×;**Ⅵ-b 核心不變式** —— 交戰上界在稽核端**獨立重算**(塔 / 武器 × altRangeMax × RANGE_TOL / 招式 / 載具航程)後對帳、起糊距離 > 上界且餘裕 ≥ 1.4×、**反例存證**「最遠狙擊可視 216m < 交戰上界 304m ⇒ 錨在可視範圍會侵入交戰距離」、舊 `aimSightM` 錨零殘留、全糊距離仍 < 最小地圖對角(L1 也看得到這個效果);Ⅵ-c 距離只有 data.js 一份(postfx 不手寫公尺、game.js 直接轉呼且無第二份算式、**三個消費端都餵 setDof**);**Ⅵ-c2 只在狙擊模式** —— `dofAimBlend` 行為直測(兩端是定義 / 中途線性 / 單調 / 帶外夾制 / `zoomFov` 未設恆 0 / 現役三機種都進得了鏡)+ `setDofBlend` 恰一個呼叫點 + 陣亡與觀戰恆 0 + **MUST NOT 判 aiming 布林** + 0 時整個 pass 退出鏈 + `uDofA` 恰一個寫入點 + 預設 1(樣品/定場照不必知道有瞄準這回事);Ⅵ-d 順序在勾線之後 / 調色之前;Ⅵ-e 三個關法任一成立即退出鏈 + 非法距離退回不掛;Ⅵ-f 焦外閘 / 焦內早退 / 黃金角螺旋 / 半徑是螢幕比例 / 四個 quad 都在 dispose 名冊;Ⅵ-g 樣品景深帶夾住樣品場景(前景全落焦內、背景排與地面遠端落全糊帶)+ 背景排共用幾何進 `_geos`)+ **反向驗證八支**(錨退回狙擊可視範圍 ⇒ Ⅵ-a・Ⅵ-b 紅 / 景深排到勾線之前 ⇒ Ⅵ-d 紅 / 拿掉焦外閘 ⇒ Ⅵ-f 紅 / 半徑寫死像素 ⇒ Ⅵ-f 紅 / 拉桿 0% 不退出鏈 ⇒ Ⅵ-e 紅 / 改判 aiming 布林 ⇒ Ⅵ-c2 紅 / 不在狙擊模式也掛 pass ⇒ Ⅵ-c2・Ⅵ-e 紅 / `uDofA` 兩個寫入點 ⇒ Ⅵ-c2 紅。**寫反向驗證腳本 MUST 用 CRLF 容忍的樣式**(`?
`)並在替換無效時當場失敗 —— 含 `
` 的字面替換在這個工作區是無聲 no-op,㋑ 的同一個坑,實測第一次就踩到:稽核照樣全綠而那個反向驗證是假的)+ `audit_gpu_lifecycle.mjs` ⑦(A25:多一個 quad)+ `audit_cel_pipeline.mjs`/`audit_soft_stroke.mjs`(同一支 postfx.js;**勾線的 alpha 契約 MUST 逐位元不動** —— 景深排在它之後,讀的是它的輸出)+ `npm test`(**這次不是 ㋒**:data.js 動了,而伺服器直接 import 它)+ `npm run bal`(只新增函式、一個平衡數值都沒動 ⇒ 七不變式 MUST **逐項不動**)+ `npm run audit:net`/`audit_solo_boot.mjs`(postfx.js 多一條 `data.js` import 邊)+ `audit_ui_layout.mjs`/`audit_ctrl_mode.mjs`(設定頁多一根拉桿、說明分頁多一段)+ **`shot_scene.mjs` 前後對照**(㋓ 需 playwright;`--dof=0` 逐層隔離 —— **散焦長什麼樣只有畫出來才看得見**,而它壞掉時每一行讀數都正常)+ 真機冒煙(**一般視角全程銳利(這是預設狀態,先確認它沒變);按右鍵進鏡,遠景隨拉近動畫漸漸散焦 —— 456m 外開始、608m 外糊成一片,而任何打得到的敵人(含站在塔射程邊緣的)一律全清晰**;放開右鍵模糊跟著鏡頭一起退回去(不得慢半拍);陣亡與觀戰畫面不得留著模糊;近處機體/座艙的輪廓外**沒有**一圈光暈;天際線不出現「糊的地面 / 清晰的天空」硬邊;設定頁把「景深模糊」拉到 0% 遠景當場恢復銳利、拉到 150% 明顯更糊,而樣品裡**前排兩件恆清晰、後排三個方塊跟著糊**;手機上拉到 0% 幀時要回到這批改動之前)|
| 畫面表現旋鈕 / 陰影偏色 / 風化場 / 零件抖動(`visualPrefs.js` 全支・`toon.js` 的 `SHADOW_HUE`/`shadowTintRGB`/`RAMP_HOOK`/`_rampTint`/`setWeatherField`/`celWeatherF`・`field.js bakeFieldTexture`・`terrain.js installWeatherField`・`postfx.js` 的 `uInk` 與訂閱・`xform.js partId`/`partJitter`・`hazards.js jitterParts`・`biomes.js jitterMegalith`/`djAt`・`main.js renderVisualSettings`/`disposeVisualSettings`・`toon.js` 的 `rampFloor`/`RAMP_DEPTH_FN`/`updateCelLight` 的光向覆寫・`matsample.js SUN_DIR`・`#pauseVisualMount`/`#lobbyVisualMount`/`.vset-*`) | `audit_visual_prefs.mjs`(124 項;Ⅰ 旋鈕表 —— **需美術確認的兩項預設 MUST 為 0**、每一項都拉得到 0、夾制與非法鍵、同值不重播、單一訂閱者拋出不阻斷其餘、零 import;Ⅱ 陰影偏色 —— 執行 `shadowTintRGB` 真品原文:強度 0 = **純白**、全值域 `luma ≡ 1`(A14 的暗階亮度逐位元不動)、方向與單調、`RAMP_HOOK` 是具名錨點、落地保險仍在、機體/環境兩軌不得併成一個值,**偏色權重是 ramp 深度不是階的亮度**(執行 `rampFloor` 真品原文:逐組由 `RAMPS` 推導、最暗階深度 = 0 ⇒ 吃整份偏色、最亮階 = 1 ⇒ 逐位元不偏、逐階嚴格遞增、濃度較舊制 ×1.67)、`celRampDepth` 一份 GLSL 實作兩條路徑同吃、`uCelRampLo` 由 `rampFloor(bands)` 推導且 `bands` 一路傳到 `applyCelPatch`;Ⅲ 風化場 —— 烤場確定性/值域/**格心取樣**、`field.js` 仍零 three 不建 DataTexture、兩條路徑都安裝、種子與色階梯錯開、乘數在強度 0 恆為 1、`celWeatherF` 一份定義兩個消費端;Ⅳ 零件抖動 —— `partJitter`/`partId` 各一份、消費端沒抄雜湊、只增不減 / 偏移件不自轉 / `dj=0` 恆中性 / 確定性、**`vegPartXform` 抽縫後逐位元同舊制**(200 組零件 × 種子)、兩個新消費端都**實測**外廓再套用、地標的 dj 不得消耗 `rnd()`;Ⅴ 設定頁 —— 一份實作兩個掛載點、拉桿由 `VISUAL_KNOBS` 推導、標籤不抄第二份、樣品走真品材質與真品管線且非 2D、**鍵光讓暗面真的在畫面上**(`SUN_DIR` 一處定義兩處消費、z < 0 側後方、正規化 y < 0.5 否則地面整片壓進不偏色的頂階、燈與 `uCelLightDir` 同吃、戰場/角色預覽不傳覆寫、樣品不碰 `setCelSun`)、回收點齊全、`uInk` 夾制、ⓘ 說明、`max-width` 版型) + **反向驗證**(權重寫回 `celG` ⇒ Ⅱ 紅 / 鍵光改回過肩光 ⇒ Ⅴ 紅 / `bands` 不傳 ⇒ Ⅱ 紅) + `audit_cel_pipeline.mjs` / `audit_gpu_lifecycle.mjs`(ramp 家族與 A25 生命週期都動到)+ `audit_object_joints.mjs`(抖動的接合不變式)+ `audit_ui_layout.mjs` / `audit_ctrl_mode.mjs`(設定頁多一區) + `npm test` / `npm run bal`(㋒ 純表現層 + 純客戶端偏好,伺服器完全不涉入 ⇒ MUST 全數不動) + 真機冒煙(**設定頁一開就看得到樣品,而且樣品裡的兩個物件都看得到一塊明顯的暗面**;拉「機體陰影偏色」左邊那顆球與那截手臂的暗面當場轉藍綠、亮面不動;拉「環境陰影偏色」右邊那塊岩石與地面跟著轉,而球不動(兩軌不得互相影響);拉「勾線強度」到 0 樣品的黑線消失、拉滿變濃;**拉「風化密度」樣品地面的苔蘚當場由均勻一片變成一塊濃一塊淡(大廳與戰鬥中都要有反應)**;戰鬥中開設定拉拉桿,關掉選單後戰場畫面就是樣品那個樣子;按「還原預設」四根拉桿一起回位且鈕變灰;關掉設定頁後再開仍記得上次的值;手機直式設定頁不出現橫向捲軸)|
| 軟性物質(`toon.js` 的 `INK_SOFT_A`/`SOFT_KINDS`/`WIND`/`stepCelWind`/`celWindTime`/`applyCelPatch` 的 `soft` 分支與 `CEL_SOFT`·`CEL_SWAY`·`CEL_SWAY_H` 三個 define/`toonMat`·`envMat` 的 `soft`·`rim` 欄・`postfx.js` 勾線 pass 的 `ae`/`soft`/早退・`biomes.js` 的 `SOFT_BY_VEG_KEY`/`vegSoftKind`/`vegSpan`/`flag()`/`extractNatureParts` 的葉片分支/任一 `VEG_DEFS` 零件的 `key`·`sf`・`siteplan.js` 的 `CIVIC_PARTS[].sf`/`buildCivic` 分桶鍵與 `bk.top`・`environment.js makeClouds().step()`・`game.js` 的 `stepCelWind(dt)`) | `audit_soft_stroke.mjs`(73 項;Ⅰ 參數表 —— 倍率「細得多」且恰一處定義一處消費、postfx 沒手抄、`turf` 擺幅 0、草比葉擺得開、風向恰一處、諧波比不可通約;Ⅱ **alpha 契約** —— 寫入恰一處且排在 `opaque_fragment` 之後(排前面被 `OPAQUE` 的 `a = 1.0` 蓋掉)、未標軟性恆寫 1、倍率乘在 smoothstep 的**輸入**、取四鄰最小值、早退門檻 = **EDGE0 本身**且排在取樣之前、調色/FXAA 輸出 alpha = 1;Ⅲ 擺動原文 —— 排在 `project_vertex` 之前、權重錨在整株座標且取二次、相位取實例原點、風向轉局部、兩個正弦、進快取鍵;Ⅳ **消費端覆蓋**(執行真品 `VEG_DEFS`/`vegSoftKind`/`vegSpan`,幾何以與 three 同值的樁餵入)—— 每一款喬木都有軟性樹冠、芒草族**每一個**零件都是軟性、枯木/巨岩/香菇/樹幹一律不是、span 推導(整株加高 3m ⇒ span +3)、GLB 葉片 regex 只有一條、兩處旗幟都改走 `flag()`、公設 `sf` 全部認得且一律無碰撞柱、分桶鍵含 `sf`;Ⅴ 時鐘與雲(執行原文)—— dt 夾制/負值不倒退/不取模、`game.js` 排在 `envFx.update` 之前、雲不自己數 dt、環繞取模對負數補位、一個週期後**逐位元回到起點**)+ **反向驗證** `--break-ink`(倍率當成 1 ⇒ Ⅰ 紅)/ `--break-anchor`(權重錨點拿掉 ⇒ Ⅲ 紅)+ `audit_cel_pipeline.mjs`/`audit_gpu_lifecycle.mjs`/`audit_visual_prefs.mjs`(ramp 家族與後製管線同一支檔)+ `audit_object_joints.mjs`(擺動是頂點位移 ⇒ 零件表的接合 MUST 逐位元不動)+ `audit_siteplan.mjs`/`audit_beacons.mjs`(公設零件表多一欄)+ `npm test` / `npm run bal`(㋒ 純表現層,伺服器完全不涉入 ⇒ MUST 全數不動)+ 真機冒煙(**樹冠/芒草/旗幟隨風擺動,同一片林子是一波一波掃過去而不是全體同步點頭;樹幹不動、樹冠動,接合處不開縫**;**葉叢與草叢的黑線明顯比旁邊的建物/岩石細,而建物的線與改動前一模一樣**;把「勾線強度」拉到 0 兩者一起消失、拉滿時軟性仍明顯較細;公園草坪與運動場草皮不再被畫上硬黑邊、花圃會微幅擺動;天上的雲沿同一個風向緩緩漂移並輕微起伏;暫停選單期間草與雲一起停住,回到遊戲一起繼續)|
| 表現層資源生命週期(池/`markShared`/`disposeTree` 系)/ 自適應解析度(`RES_GOV` 系) | `audit_gpu_lifecycle.mjs`(42 項)+ 真機 60s 開火 heap 不單調上升 + 目視光束粗細(scale 就是半徑) |
| 三種遊戲機制(`rooms.js`/`netmode.js`/`build_solo` 系) | `audit_net_modes.mjs` + `audit_solo_boot.mjs`(`data.js` 單一模組實例)+ `npm test` 單機段與 WS 全段 + `audit_ui_layout.mjs` |
| 區網同時多路徑(`server.js` 的 `demux`/單埠雙協定・`ensureCert`/`sanNames`/`certSan`/`SAN_FILE`・`ADDR_WATCH_MS` 位址監看・`noServer` + `handleUpgrade`) | `audit_net_modes.mjs` ⑥(靜態 + **行為直測**:起真的 server 以 `--lan --https` 在同一個埠打 http/https/ws/wss 四路皆通、憑證 SAN 涵蓋當下每一個對外 IPv4)。三條會靜默壞掉的線:①`demux` MUST 全程停在 paused(`read(1)` + `unshift`),**MUST NOT** 用 `once('data')` + `resume()` —— TLSSocket 在 nextTick 才補餵已緩衝位元組,先 resume 會把握手第一段流掉,表徵是「https 連得上但永不回應」而明文那半正常(極易誤判成憑證問題);②`WebSocketServer` MUST 只有**一個**實例(心跳掃 `wss.clients`,分兩份 = 一半死連線回收不掉 ⇒ RoomHub 無真人收房永不啟動);③憑證 SAN MUST **取聯集**(蓋不到才重簽、簽過的名字留著)—— 「有檔案就用」= 之後才上線的路徑永遠蓋不到,「每次重簽」= 隊友天天重按憑證例外。沒開 TLS MUST 維持 http 伺服器自己 listen(分流層只在 `--https` 之下存在)。另 + `npm test` WS 全段 + 真機冒煙(有線/Wi-Fi/Tailscale 三台同時進同一場;開伺服器後才接 Wi-Fi,20 秒內該位址自己進憑證、不重啟就連得上) |
| 主堡陣營歸屬對調(`rooms.js rollSideSwap`;開房 + 再戰回房各擲一次) | `audit_net_modes.mjs`(實作只有一份、恰兩處呼叫、MUST NOT 移進 `startBattle` —— 那會讓客戶端房間階段的地形預建整份作廢)+ `npm test`「再戰重擲主堡陣營歸屬」段(擲中 ⇒ 歸屬對調 + 兵線點序反轉 + 仍通過開房驗證;沒擲中 ⇒ 逐位元不變。**MUST 先重啟伺服器**,見 §5)+ 真機冒煙(同一間房連按「再戰」數場,約半數換到對面主堡開場) |
| 程序生成物件擺位(`BUILDERS`/`VEG_DEFS`/`vegPartXform`/`MEGALITHS`/`synthMegalith`/`decorateMegalith`/`rockProbe`) | `audit_object_joints.mjs`(`--seeds 8` 約 23000 接合;FLOAT/PARTIAL/DETACHED/ISOLATED 四硬失敗;豁免附理由;巨岩段含「兩端支承」具名救援) |
| 小地圖顯示範圍(`mmMode`/`_world2mm*` 系) | `audit_minimap_view.mjs`(16 項)+ 真機切換冒煙(已探索迷霧不得錯位) |
| 視野鎖定(`VIEW_LOCK`/`viewLockStep`・`SCOPE`/`scopeRvmin`・`game._tickViewLock`/`_coneAcquire`/`_entAimPoint`/`_scopeFog`・觸控 ZR `data-act="lock"`・絕招改掛十字鍵左) | `audit_view_lock.mjs`(67 項;Ⅰ 常數推導不手寫 + DROP > EDGE 遲滯 + 鏡圈半徑單一縫(`scopeRvmin` 兩端點/單調/夾制,main 與 game 同吃)、Ⅱ **後座力不得被抵銷** + `_tickViewLock` 排在相機合成前、Ⅲ `viewLockStep` 直測不瞬移/不過衝/幀率無關、Ⅳ 鍵位與單一縫 + `_cmd` 的 lock 排在 dead 閘之前、**Ⅴ 輪替**:按下只掛旗標、`_vlockPrev` 放開不清而陣亡/離場清、名冊 = 同一次掃描的 list 模式且依畫面由左至右、狙擊改吃鏡圈、相機後方先擋、反矩陣自己算)+ `audit_touch_layout.mjs`(ZR 列位改認 lock、絕招與鎖定各恰好一顆、絕招鈕面仍帶 .gb-cd)+ `audit_ui_layout.mjs` + 真機冒煙(按一次 ZR 鎖上、再按一次換下一個且順序照畫面由左至右、狙擊模式只鎖得到鏡圈內的、開火照樣上踢且回穩後自己貼回、放開立刻恢復自由視角、十字鍵左絕招顯 CD) |
| 觀戰相機(`SPEC_CAM`/`specViewNext`/`camSmoothF`/`camAngleStep`/`wrapPi`・`game._updateSpectator`/`_specSetView`/`_specCycleView`/`_specFollow`/`_specHud`/`_specRoster`・觀戰鍵位 `_onKey` 的 F・Q/E 與 `_cmd` 的 special・swap・dive・玩家資訊面板 `main.renderSpecUpg`/`hud.self` 的 spec 分支/`#specUpg`/`body.spectating`) | `audit_spectator_cam.mjs`(123 項;Ⅰ 視角序單一縫 + 貼合名冊 `LOCK_VIEWS`/`specViewLocked` 只有一個消費端 + `_specView` 恰兩個寫入點 + game.js 無本地常數表、Ⅱ **平滑幀率無關直測**(30/60/240fps 追隨 1 秒差 < 0.5m 且 = 解析解)+ 角度最短路徑/不過衝/無穩態誤差、Ⅲ 以真品 `_updateSpectator` 原文行為直測:三顆升降鍵(含 Ctrl)與 `MOVE_MPS`・`BOOST_F` 推導、下降地板夾制、四種視角的相機解、只有第一人稱藏機體且換視角/退場都還原、**第一人稱/第三人稱跟隨的錨點逐位元貼合機體**(等速移動 2 秒落後恆 0,並以第三人稱自由當反面對照組)、第三人稱自由的小位移平滑而 > `SNAP_M` 直接貼上、第三人稱不鑽地、Ⅳ 鍵位與派發單一縫 + help.js 四種視角名稱齊全、**Ⅳ-b 觀戰操作說明**:`SPEC_CONTROLS` 逐列形狀 + 兩版都講得到四種視角 + 選單那一長串是 join 推導 + `renderSpecHelp` 一份實作且掛在 `syncPauseHelp` 上(切鍵鼠 ⇄ 搖桿要換說法)+ `#specHelp` 在 `.hud-self` 內、靜態標記為空、CSS 有 max-height、Ⅴ 面板欄位全來自快照 + 八軌吃 `ECON.UPGRADES` + 面板 DOM 只有一塊)+ `audit_ctrl_mode.mjs` Ⅶ(觸控鈕位/鈕面字/版型)+ `audit_touch_layout.mjs` + `audit_ui_layout.mjs` + `npm test`(㋒ 純表現層,MUST 仍全綠)+ 真機冒煙(上帝視角按住 C 或 Ctrl 一路降到地面就停住;F 連按四下走完四種視角並回到上帝視角;**跟著一台全速衝刺的無人機,第一人稱不會看到自己的機體跑在前面、第三人稱跟隨的機體恆在畫面中央**;左下角常駐觀戰操作說明且切鍵鼠 ⇄ 搖桿文字跟著換;第三人稱看得到自己跟的那台機體、鏡頭平順不抖;第一人稱視角跟著該玩家轉頭;跟隨中左下角顯示該玩家裝甲/護盾/電力/武器/招式冷卻/金錢/擊殺與八軌升級、離開跟隨即收起) |
| 機種絕招觸發(`ABILITY_HOLD_S`/`_tickHoldAbility` 系) | 真機冒煙:一般/狙擊長按皆出招且不誤切模式、短按仍切換、十字鍵左同招顯 CD、三機種各一次 |
| 操作方式(`ctrlmode.js` 系 / `renderCtrlModeRow`・`renderCtrlSettings`・`syncCtrlSettings` / `main.renderRoomCtrl`・`onSync` 的 `setRoomCtrlMode` / `rooms.js` 的 `config.ctrl` / `game._applyCtrlScheme` / `main.TOUCH_UI`)或 戰場選單(`game._escMenu` 三來源、`_setPaused` 的 side 閘、觀戰指標鎖定、`mobile.setKind` 的 HOME) | `audit_ctrl_mode.mjs`(127 項;Ⅰ 裝置判定單一縫 + ctrlmode 無 import、Ⅱ 真品直測「預設不限定 / 不限定吃裝置判定 / **戰區定案蓋過我的預設** / **離開戰區退回預設** / 限定時目前操控不可變更 / 非法定案視同沒有 / 裝置衝突警告 / 舊鍵與 `?ctrl=` 遷移」、Ⅲ 選項 DOM 只有一份 + 大廳區塊已移除 + 上行/套用各只有一處 + `TOUCH_UI` 是函式、Ⅳ 搖桿層建毀單一縫 + dispose 解訂閱、Ⅴ **ESC 隨時可用**:`_escMenu` 是唯一出口且不判 side/dead/鎖定狀態、排在 `paused` 早退之前、`_onPlc`/觸控 ☰ 同縫且 **`_onPlc` 亦不得用 `dead` 當門檻**(陣亡解鎖改走 `_plcSelf` 戳記,且只在真的鎖著時打)、去彈跳只生效一次 + 六格狀態矩陣行為直測、Ⅵ 伺服器端原文:合法值取自 ctrlmode、房主閘門、列表帶出)+ `npm test`「操作方式由房主選擇」段(5 項行為直測:開房預設 / 房主改廣播全房 / **非房主改不動** / 非法值靜默忽略 / 戰區列表帶出;**MUST 先重啟伺服器**,見 §5)+ `audit_touch_layout.mjs`(觀戰版型:HOME 留著、其餘戰鬥鈕收起)+ `audit_ui_layout.mjs` + 真機冒煙(房主在戰區畫面選限定搖桿 → 全房桌機也長出搖桿、隊友那格是唯讀灰;不限定 → 戰鬥中設定頁即時切換鍵鼠 ⇄ 搖桿、說明文字跟著換;觀戰按 ESC 叫得出選單並離開;**陣亡倒數中點一下畫面重新鎖住指標,再按 ESC 仍叫得出戰場選單**,而剛陣亡的那一瞬間不會自己彈出選單)|
| GUI 懸浮提示(`tip.js`・`help.js UI_TIPS`/`uiTip`・`main.syncTips`・`data-tipkey` 標記)/ 按鍵風格(`.seg`/`.segb`・`renderBotDiff`・`renderCtrlModeRow` 的 `bare`)/ 房間設定分頁(`#roomCfg`/`roomCfgTabs`)/ NPC 圖示(`npcicon.js`) | `audit_ui_layout.mjs` 0.5 段(44 項;常駐說明已下架 + 靜態標記只寫 key + 「介面」類由 UI_TIPS 推導 + 分段按鈕只有一套 + 圖示覆蓋現役名冊 + **機種圖示與 `HERO_SIZE` 現役機種一一對應、未知機種回退問號、所有頭像走 `charAvatarHTML` 單一縫、機種取自 `charKind`** + 商店掃貨鈕/ⓘ/`sweepable` 單一縫;**不需 playwright,沙箱也跑得到**)+ `audit_ctrl_mode.mjs`(Ⅲ 新增 6 項:`.tset-seg*` 已改名、`.pause-tab`/`.help-cat`/`.unit-side-btn`/`.diff-select` MUST NOT 各寫一份鈕面樣式、三處都掛上 `.segb`)+ `audit_touch_layout.mjs` + `npm test`(㋒ 純表現層,MUST 仍全綠)+ 真機冒煙(ⓘ 滑鼠移上即顯示、觸控長按才顯示且短按仍選得動、切鍵鼠 ⇄ 搖桿提示文字跟著換、說明分頁「介面」類與 ⓘ 內容一字不差、戰區兩顆分頁鈕互切且非房主整排變灰、**兩顆分頁鈕正下方就是該組選項(不再歪在左緣)**、NPC 圖鑑每格都有圖示且選取態圖示翻深色、**選角頭像右下角有機種角標(無人機/機甲/變形者三款各認得出來),選取態跟著翻色**)|
| `mobile.js`/`_applyLook`/`_moveAxis`/`_cmd`/觸控 CSS | ①桌機 MUST 不回歸 ②`audit_touch_layout.mjs`(60 組;四分區零重疊、觸控目標 ≥44×40、`.tl-sys` 固定 grid)③疊層可點性 ④`audit_touch_gesture.mjs`(17 項)⑤真機冒煙 |
| 橫式商店兩欄(`style.css` `body.touch-ui.ori-landscape .shop-items`/`.shop-head`/`.shop-item`) | 2026-08-04 使用者需求「商店在手機橫向時改成兩兩左右併列」。**純 CSS**,`main.renderShop` 的 DOM MUST NOT 為版型改結構。`audit_touch_layout.mjs` 升級工坊段(六種持握各一組:橫式恰兩欄且前兩列同上緣不重疊、直式恰單欄、`.shop-head` 橫跨整列、無橫向溢出、鈕高 ≥40)+ 真機冒煙(橫握開 ⊟ 一屏掃得完八軌、☆ 預約與 $價格同一行不掉行、直握維持單欄) |
| 全螢幕方向(`mobile.toggleFullscreen`) | 2026-08-04 使用者需求「全螢幕時也可以旋轉手機切換直式/橫式」。MUST NOT 鎖方向,進出各 `unlock()` 一次。`audit_ctrl_mode.mjs` Ⅶ(原文無 `orientation.lock`、`unlock()` 恰兩處、`syncOrientation` 兩個轉向監聽仍在)+ 真機冒煙(進全螢幕後轉手機,版型即時換且**沒有退出全螢幕**) |
| `#touchLayer` 節點位置 / `--tl-*` CSS 變數 | 搖桿 MUST 留 body 層 + 保留 `body.touch-ui` 保險預設值;真機大廳端對端量測(不設覆寫) |
| 選單版型 / 任何鈕面文字 | `audit_ui_layout.mjs`(309 項;鈕面無括號補述、桌機並排直式維持並排、`.cd-art` 解除 sticky、疊層 ✕ 規則) |
| 機體美術覆核台 / 2D 生圖來源(`tools/codex_review.mjs` 的 `SOURCES`/`manifest`/`wantShots`/`DEFAULT_PORT`・`tools/codex_review/review.js` 的 `rowStat`/`shotCard`/`renderOrphans`・`tools/ai3d/gen2d.mjs` 的 `relOut`/`gen_manifest.json`/`buildJobs` 的 `--masters` 分支・**`tools/ai3d/slots.mjs` 的 `reviewUnits`/`masterQueue`/`auditQueue`**(2026-08-05 使用者定案「優先把 2D 生成圖無通過的機體補齊,再來通過數最少的」:覆核判決 → 生成端的**唯一**回饋路徑。舊制 `--masters` 只看檔案在不在 ⇒ 覆核台按了「⟳ 重下 prompt」沒有任何工具會去重畫它,`--redo` 也救不了(要先進得了 `missingMasters()` 才輪得到)。**單位 = 角色 × 型態** —— 變形者的飛行型是獨立交付物,併成一台來數會被地面型的通過數蓋掉;優先序 MUST 推導,MUST NOT 手抄角色清單,而**未覆核 ≠ 被退回**(重畫沒人抱怨的圖 = 燒額度))・**★ 外觀權威**(`tools/codex_review.mjs` 的 `state.star`/`charOfSlot`・`tools/ai3d/slots.mjs` 的 `starSlots`/`starOf`/`auditStars`・`gen2d.mjs buildJobs` 切圖分支的參考圖;2026-08-05 使用者定案「生圖對照台的圖片加入星號功能,一個機體只能標註一張;img to 3D 時如果外觀有衝突,以星號圖片為主」)。①**「一台只能一張」MUST 是資料結構不是驗證迴圈**:`state.star` 是 `機體 id → slot` 的映射 ⇒ 同一個鍵覆寫,兩張星號在結構上不可能存在;改成 `slot → true` 的集合就需要「存之前先掃掉同機體的其他星號」那種規則,而規則會漏 —— 漏掉的症狀正是這個功能要解決的問題本身(兩張同時亮著星,img→3D 只好隨便挑一張)。②**衝突只在切圖那一步被解決**:切圖 = image→3D 的輸入,而它只吃**一張**參考圖(`slotPrompt` 的 `{REF}`);舊規則「取地面型設定稿」是一條幾何規則,表達不了「使用者覺得哪一張才是這台機器」⇒ 星號 MUST 排在它**之前**,而星號解析不到檔案時逐位元退回舊規則(原則 6)。③**slot → 機體 MUST 由 `wantShots` 反查**,MUST NOT 拆字串(第二份命名規則);伺服器 MUST NOT 拿前端送來的 `starChar` 當設定星號的依據(那等於讓頁面指定要蓋掉誰的星)。④**死星號 MUST 紅字**(`auditStars`,`gen2d` 每次執行都跑):指到不存在的機體/不屬於它的格子/沒有圖的格子,三者的共同症狀都是**沉默** —— img→3D 照舊規則挑圖,而使用者以為星號在生效。⑤星號與覆核判決是**兩件事**:一張被判「局部重繪」的圖仍可能是最接近設計的那一張 ⇒ MUST NOT 綁進「儲存」那顆鈕的條件裡)或 **3D 零件對照台 / 生成物來源帳**(`tools/parts_review.mjs` 全支・`tools/parts_review/review.js` 的**開機順序**與 `mountStage`/`tick` 取景・`tools/ai3d/parts_src.mjs`(消費端 lib 描述子 / `PART_LIBS` / fallback 包絡 / GLB 解析的**共用讀取縫**,`intake_parts.mjs` 同吃 —— 這四件事以前只住入庫閘,對照台問的是一模一樣的四個問題,各抄一份 = 入庫閘說外廓合格、對照台畫出另一個外廓)・`tools/ai3d/provenance.mjs` 的 `METHODS`/`loadProvenance`/`photoRoots`・`tools/ai3d/parts_manifest.json`(2026-08-05 使用者定案「須說明使用哪個生成方法與 img」:那兩件事在此之前只存在於 runbook 的散文裡,沒有任何工具讀得到 ⇒ **方法字彙住 `METHODS`、事實住 JSON、推導得到的數字(外廓/三角形/件數)一律不准抄進去**;沒有帳的生成物 MUST 列進「未記載來源」,MUST NOT 看起來正常)。**兩側一律由遊戲自己的建構器建**,MUST NOT 在對照台寫第二套組裝器或第二份 primitive 產生器 —— **一個消費端一支 builder**(`beacon` → `beacons.buildBeacon` / `veg` → `biomes.buildVegMeshes` / `mega` → `biomes` 的 `synthMegalith`→`decorateMegalith`→`jitterMegalith`→`bakeContactAO`,順序照 `placeMegaliths`;巨岩那一支 MUST 建**合成**岩,名岩 `MEGALITHS[].build` 一顆庫零件都不吃 ⇒ 兩側逐位元一樣)。**認不得的 builder/kind MUST 空著並 log**(2026-08-06):`buildBeacon` 對未知 kind 是 `KIND_PARTS[kind] || KIND_PARTS.cairn` 的**靜默回退** —— 巨岩那三顆節點就是這樣掉進裂縫的,台上畫的是地標疊石,而疊石自己也吃 `rock/*` 節點 ⇒ 左右真的長得不一樣、讀數也在動 = 看起來完全正常,只是那顆巨岩從來沒上過台。同理**碰撞柱要講清楚是哪一種**(beacons 實測 / 神木包圍盒 / 巨岩登記值 `meta.col`),三者寫同一個字就是騙人;**取景框 MUST 量台上真的建出來的那一團幾何**(2026-08-06 使用者回報「PR147 畫的 3D 物件在展示台看不到,只看到跟舊有物件重繪」):框心與半徑一律取實測包圍球、近遠平面跟著距離走,MUST NOT 拿離線描述子(fallback 包絡 + 零件表座標)或登記碰撞柱當基準 —— 舊制三個症狀同一個成因:①恆 `lookAt(0, y, 0)` ⇒ 離軸 5.8m 的神木冠簇永遠不在畫面中央;②距離只看高度不看水平尺寸 ⇒ 58m 高但 207m 寬的巨岩把相機關在石頭裡面(整格灰牆);③單位包絡節點(`ico(1)`、座標 `[0,0,0]`)推不出東西 ⇒ 那顆鈕只好禁用 = 那一輪的主角在台上**沒有任何一種取景看得到它**。三者都沒有錯誤訊息,畫面上只表現成「AI 生成跟原版差不多」。「零件」取景 = 只顯示**換掉的那幾顆 mesh**、鏡頭框住其中**最大的一顆**(同一顆節點會被擺很多次 —— `rock/mega_a` 在一顆合成岩上有 7 處、散在 290m 的露頭上,框住聯集等於每一處都縮成畫面上一個點)。認人兩條路,先後不可對調:①**先在「AI 生成」那一側依實測頂點數認**(`megaGeo`/`buildBeacon` 都是 clone,頂點數不變),原版那一側**以位置配對**(中心落在庫節點包圍球 × `PAIR_F` 內)—— **MUST NOT 假設兩側索引對得起來**:命令式巨岩會把好幾件 primitive 換成一顆庫節點(2026-08-06 實測 92 → 49 顆 mesh),逐索引配對在它身上整組落空,而症狀只是「這一列退回整件取景」;②認不出來(beacons 依材質**合併成桶**,cairn 11 件 → 8 顆 mesh ⇒ 桶裡混著別的零件、頂點數對不上)才退回**逐索引差集** = 「這一款換掉的全部」,那條路仍要求兩側粒度一致。**退回整件時 MUST 分清是哪一種**:「這顆座號沒用到這個節點」(命令式巨岩逐座號挑型,換個座號就看得到 —— 而且 MUST 直接指名是哪一顆)跟「台子配不起來」是兩件事,寫同一句就是把可以按的那顆鈕講成壞掉。**座號組 MUST 蓋得到每一顆庫節點**(舊組 `[1,3,7]` 實測蓋不到 `rock/mega_e`/`mega_f` ⇒ 改 `[1,7,10]`;新增節點 MUST 重掃)。隱藏不是第二套組裝器(群組仍是遊戲自己建的、一顆頂點都沒動)且**讀數仍量整件** ⇒ 不會因為隱藏而說謊;`far` 平面 MUST 跟著距離走(固定 500 的下場是 290m 露頭整格全黑,看起來像「這一列建不起來」);**「原版」MUST 全部在 `loadPartLibs()` 之前建完並快取**(`libGeo` 是模組層的表,順序寫錯不報錯,只讓左右兩側長得一模一樣 = 一個「AI 跟原版差不多」的假結論);純資料件的原版走 `git show <rev>:public/js/beacons.js` 由**那一版自己的 buildBeacon** 建,rev MUST 只認來源帳裡記著的那幾個(參數零信任);dev 台的 `<base href="/public/">` MUST 留著 —— `partlib.js`/`models.js` 取的是**相對**資產網址,少了它 GLB 全數 404 而整族靜靜退回程序生成)或 開發工具啟停(`tools/dev_supervisor.mjs` 全支・`server/server.js` 的 `/dev/tools` 路由・`main.js` 的 `syncDevTools`/`renderDevTools`/`devToolRow`/`devToolAction`・`#pauseDevMount`/`#lobbyDevMount`) | `node tools/codex_review.mjs --report`(配對表:入庫 / AI 稿 **分開數**、缺圖與孤兒逐項列出 —— 兩個數字合併就是把「這一格還沒有正式圖」藏起來;另印 ★ 外觀權威幾台 + 指到空格的死星號)+ `node tools/ai3d/gen2d.mjs --audit`(rig 節點涵蓋 / 描述子詞表 / 補圖優先序 / **★ 外觀權威** 四支)+ ★ 行為直測(真的起一次覆核台 dev server 打 API:標第二張時第一張自動消失、`state.star` 的 s01 鍵恆只有一個、兩台各一張互不影響、`starOf` 解析到入庫檔、未標回 null、**死星號 `starOf` 回 null 而 `auditStars` 紅字**、slot 不屬於該機體紅字、不存在的 slot 不受理;另以 `gen2d --plan --only <角色>` 對照「有星號 ⇒ 每張切圖都標 ★<slot>」與「無星號 ⇒ 逐位元同舊制」)+ `node tools/parts_review.mjs --report`(逐件:方法 / 來源圖 / 消費端 / 實測 / 缺件 / 孤兒節點 / 未記載來源;**孤兒節點數 = 0** 是「新生成物真的接上消費端」的第一道閘 —— 有列不代表畫得出來)+ **對照台行為直測**(起 dev server 打 `/api/parts` + 頁面內選列讀兩側讀數:巨岩兩側 mesh 數相同而三角形不同(= 換的是幾何不是件數)、**碰撞柱兩側逐位元相同**(佈局不讀庫幾何,§2.3)、以 `libGeo` 的頂點數反查那一顆合成岩真的長著 17 件庫節點)+ **反向驗證**(拿掉 `mega` 那一支 builder ⇒ 兩側 MUST 變成「(建不起來)」,MUST NOT 退回舊制的靜默疊石)+ `node tools/ai3d/intake_parts.mjs`(14 項;讀取縫抽成 `parts_src.mjs` 之後 MUST 逐項不變)+ **`npm run audit:net` ⑦ 段**(這是全專案唯一一條「HTTP 進來 → spawn 行程」的路徑 ⇒ 閘門本身就是要驗的東西:雲端不掛路由、動態 import 載不到就當沒有、埠號從工具自己 import、spawn argv 全來自常數、POST 要非簡單標頭、只停自己開的那支、build_solo 不複製;行為直測 loopback 200 / 區網位址 404 / 無標頭 403 / 未知動作 404 / `--cloud` 404 / 真的 start→listening→stop)+ `audit_ui_layout.mjs` (8) 段(一份實作兩個掛載點、小標由 JS 產生、`main.js` **一個埠號都不准寫死**、鈕面吃 `listening`、別人起的那支停不掉會變灰)+ `audit_ctrl_mode.mjs`/`audit_touch_layout.mjs`(設定頁多一區)+ **`npm test`(這次伺服器有動,不是 ㋒)**+ `npm run bal`(平衡數值未動 ⇒ MUST 不變)+ 真機冒煙(`npm run codex` 起台子:t10/t12/m02/m06 那幾格出現藍色虛線框的「AI 稿」、s12 那兩張列在孤兒區可指派到 s03;遊戲設定頁最下方「▎開發工具(本機)」按「▶ 啟動」後那一列的網址亮起、「↗ 開啟」由灰轉亮、按「⏹ 停止」變回「未啟動」;**遊戲伺服器一關,它開的那支覆核台也要跟著收掉**;單機版 / GitHub Pages / 隊友從區網位址連進來時,那一整區看不到;`npm run parts` 起 3D 對照台:**左右兩側明顯不是同一個東西**(疊石那三顆:左邊是 ico 多面體、右邊是實拍岩體;水塔:左邊光禿禿的腿、右邊有 X 形斜撐;**巨岩 `rock/mega_*`:兩側都是同一顆合成岩,左邊的崩落塊/伴生丘是 ico 球、右邊是實拍岩塊** —— 看到疊石就是 builder 沒接上;**每一列按「零件」都框得到那顆節點本身**(左 primitive / 右 GLB 各一顆,畫面中央、看得清輪廓),按「整件」看得到完整的一件(巨岩不會變成一整格灰牆、水塔不會只剩一根腿)),兩側同角度同時轉、青色碰撞柱一樣粗,來源圖看得到縮圖與 CC0 出處連結,方法那一格寫得出 SF3D 的參數與峰值 VRAM)|
| 陀螺儀(`Gyro`/`gyroSrc`/`LOOK.GYRO_*`/`TOUCH.gyro` 預設值) | `audit_gyro.mjs`(25 項;兩感測路徑、俯仰同號、自動切換、**宣告的預設值為關**)+ `audit_ctrl_mode.mjs` Ⅶ(預設值的 CI 版斷言 —— audit_gyro 要 playwright,沒裝就整支跳過)+ **MUST 用 https/localhost 真機測**(非 secure context 靜默無感測事件) |

**e2e 結構備忘**:前段 import `BattleSim` 直測(測試假人無 `lane`,tick 前 MUST 刪掉);迷霧下偵察 MUST 另開 `mode:'spectator'` client。瀏覽器冒煙借 mapping_elf 的 Playwright,`window.__SVS` 存取 app 狀態。
