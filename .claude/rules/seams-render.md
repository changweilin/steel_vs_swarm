# §2.1 F — Presentation & Picture (single seams)

> Layer 2: full definitions. Principles live in root `AGENTS.md`; assertion details live in audit script headers (layer 4). English only; no operations.

| Topic | Single seam | Constraints | Guards |
|---|---|---|---|
| Day-night cycle and shadows | `DAYCLOCK`, `phaseBlend()`, sun/moon direction, `SHADOW`, hour setter | Clock derives from authoritative elapsed seconds only; single main light with handoff at zero intensity; MUST NOT rebuild materials or lights per frame; shadow radius derived with texel-quantized center. | `audit_daynight` |
| Cel shading schools | `toon.js` school flag | School fixed at module load; bands strictly increasing in width and midpoint; floor derived from one source with renormalized luminance. | `audit_cel_pipeline` |
| School-exempt cel materials | `toon.js toonPlain` | Listed materials stay on the ramp path; MUST NOT convert them to patched materials; frozen roster derived, not handwritten. | `audit_cel_pipeline` |
| Post pipeline | `postfx.js Pipeline` | Targets sized from the drawing buffer; every derived quad disposed via registry; FXAA last; new passes route through the registry. | `audit_gpu_lifecycle`, `audit_visual_prefs` |
| Outline width | Outline material, scale helpers, outline applier | Extrapolation quantities stay in local units; displacement vertex-only. | `audit_cel_pipeline` |
| World curvature | `CURVE` plus knee/horizon/eye/radius/drop helpers | Curve applies in the vertex shader only; knee at combat reach with zero near settlement; MUST NOT subdivide beams for smoothing; degrade to flat on anchor mismatch. | `audit_world_curve` |
| Depth of field | `DOF` plus near/far/aim-blend helpers, focus setter | Turn point anchored at combat reach, MUST NOT anchor at camera far; ordered after outlines; strength derived from field of view at one call site. | `audit_visual_prefs` |
| Aerial perspective | `postfx.js` air helpers, fog setter | Implemented in post, not materials; near and far match scene fog exactly; near color derives from existing tables; ordered before grading. | `audit_visual_prefs`, `audit_cel_pipeline` |
| Ink info buffer | Ink declare/pack/unpack helpers, MRT flag | Declared unconditionally; coverage derives from the shader chunk; sentinel cleared explicitly; view-space normals with nearest filtering. | `audit_cel_pipeline` |
| Outline contribution | `inkQuant()`, `inkRepeat()`, `inkCtrM()`, contribution uniform | Callers pass measured pitch or size; MUST NOT hand-write contributions except the named veto; authorization values quantized; derivation strictly monotonic. | `audit_cel_pipeline` |
| Surface groups | `SURF_ID`, `surfGroup()`, `joinSurfGroup()`, surface attribute | Land and concrete slots reserved; joined before compile; zero shared randomness; suppression limited to the normal term inside groups. | `audit_cel_pipeline` |
| 3D LUT grading | Post LUT builder/applier | Replaces split-tone, never stacks; math identical between generator and shader; bit-identical when disabled. | `audit_visual_prefs` |
| Seamless terrain land | Shared land id, terrain material land branch, normal sampler | One land id across terrain and cover layers; true terrain normals with grid-pitch sampling; luminance path through the table. | `audit_cel_pipeline` |
| Ink-break noise | `INK_BREAK`, `celInkBreak()` | Reuses the existing tint axis; rotation-only anchor matrix; one shared noise routine; literal-one early-out at zero. | `audit_soft_stroke` |
| Grazing term | Post ink specular constant | Exactly one grazing term in the denominator; no second thresholds. | `audit_soft_stroke` |
| Fog-matched ink fade | Fade factor, fade-matrix helper, fade uniforms | Fade derives from scene fog at one site; start floored by combat reach; legacy fallback when fog absent; MUST NOT hand-write the factor. | `audit_cel_pipeline` |
| Screen wipe transition | `WIPE`, `wipeAt()`, wipe setter/player | Ordered after grade, before antialias; chain exits at zero with no extra target; two independent uniforms with feathered endpoints; driven by the pipeline clock only. | `audit_visual_prefs` |
| Transition call sites | `game._wipeCut` | Cover paired with reveal at the same site; no delayed state gates; reentry guarded; synchronous degrade when disabled. | `audit_visual_prefs` |
| Spawn dissolve | `DISSOLVE`, `dissolveAt()`, dissolve setter | Discards before the opaque fragment; uniform object stored in material userdata; grid anchored at unit origin; outline shell hidden during dissolve; limited to listed vehicle kinds. | `audit_cel_pipeline` |
| Despawn dissolve | Out-dissolve helpers, dissolve enabler | Define enabled only for flagged units; authority records detached before ghosting; unseen units removed immediately; far path structurally disabled. | `audit_cel_pipeline` |
| Visual knobs | `visualPrefs.js` knob table and setters | Single knob table with shipped defaults; shared uniforms changed only, no rebuilds; samples routed through production materials and pipeline. | `audit_visual_prefs` |
| Shadow hue | Shadow-hue helpers, tint function, ramp hook | Hue stays luminance-neutral; mech and environment tracks split; bands threaded through the patch; hue state shared across schools. | `audit_visual_prefs` |
| Ground attribute fields | `field.js` field/tone-ladder/texture builders | Weighted average with denominator floor; caller-provided seeds with fixed draws; thresholds derived from own quantiles; module renderer-free. | — |
| Weather fields | Weather field baker/setter/sampler | MUST NOT derive from terrain; seeds offset from the ground ladder; small textures baked centrally; bit-identical output at zero strength. | `audit_visual_prefs` |
| Soft matter | Softness/kind/wind constants, wind stepper, wind clock | One flag drives outline and flutter; single alpha write site; pure vertex displacement anchored in local space with derived span; thin ink restricted to opaque items. | `audit_soft_stroke` |
| Gust envelope | Gust magnitude/scale/rate helpers, gust sampler | Mean exactly one; gust scale far above wave scale; vegetation sampled at instance origin, waves per vertex. | `audit_soft_stroke` |
| Sea waves | Sea softness entry, wave height, tessellation helpers | Phase sampled per vertex, displaced along world up in local space; tessellation derived from the stricter of curve and wavelength limits; water level and wading logic untouched. | `audit_soft_stroke` |
| Foam and reflection blocks | `FOAM`, `REFL`, foam sampler, depth-field setter | Foam driven from baked depth, not scene depth; same wave phase subtracted; applied after the opaque fragment scaled by sea fade; field size derived; no foam on neutral textures. | `audit_water_edge`, `audit_soft_stroke` |
| Character trampling | Trample constants, slot setter, sway entry | Radius speed-dependent; unfilled slots zeroed centrally; effect presentation-only. | `audit_soft_stroke` |
| Trample feed | Slot collector and setter | MUST NOT re-derive speed locally; position-difference damped speeds; ordered after entity interpolation; main view slot prioritized without duplicates; dead and hidden units excluded. | `audit_anim_weights` |
| National flags | `flags.js` designs, nation picker, flag texture | Renderer-free rule module; faction roster derived; zero shared randomness; per-nation meshes merged without collision; unknown designs omitted. | `audit_soft_stroke` |
| Petals and leaves | `petals.js` rules, field planner/stepper, dedicated RNG | Rules separated from geometry; sites derived from the final vegetation roster and fuses; zero shared randomness with fixed draws; tones derived from seasons; stepping through the existing dynamics bucket only. | `audit_leaf_card` |
| Leaf cards and tree silhouette | `leafcard.js` planner/counter/RNG/surface-id | Count derived from envelope area; horizontal extent contained within layout radius; parametric coordinates jittered only; fuses read-only; opaque alpha-tested patched materials. | `audit_leaf_card` |
| Silhouette priority (rocks, stacks, backdrop) | Surface groups, contribution derivation, backdrop injection | Megalith groups split by measured extent ratio; measurement ordered before jitter and compile; zero shared randomness; MUST NOT alter collision or probe logic. | `audit_rock_ink` |
| Structural ink authorization | Pitch/meter helpers, contribution derivation, concrete surface id | Cel entries only with derived contributions; pitch measured as maximum; concrete shares one class id; brightening through emissive only with frozen base colors; MUST NOT alter structural geometry. | `audit_cel_pipeline` |
| Part jitter | Part-id and jitter helpers | Horizontal radius increased only; spin restricted to pivot parts; vertical and pivot positions untouched; visual radius contained inside the authority column. | `audit_visual_prefs`, `audit_object_joints` |
| Presentation lifecycle | Disposal tree, shared-geometry registry, merge helper, contact-AO baker | Contact shading multiplied into existing vertex colors; bucket keys unchanged while merging; emissive variants never merged. | `audit_gpu_lifecycle` |
| Hero modeling | `geo3d.js` blocks (sole seam), `forge/geo.js` vocabulary, `forge/forge.js` scaffold, per-unit files, `forge/roster.js`; entered via `models.makeUnit()` | One block implementation (the vocabulary module only re-exports); morph units carry two trees; variants share one stage; legacy builders frozen. | `audit_muzzle`, `audit_cockpit` |
| FPV cockpit | Cockpit constants, framing helpers, HUD band fitter | Motion envelope clamped, not rest pose; weapons pivot at own mounts without moving ballistics; central grid cell cleared by translation; parts hidden in scope mode. | `audit_cockpit` |
| Morph transition | `morphrig.js`, tagged forge helpers, plan capture, swap and pose drivers | Per-part transforms precomputed at build; parts matched by tags; fades completed around tree swaps; root-fold fallback when the plan is absent. | `audit_morph_rig` |
| Gait joint curves | `gaitcurve.js`, chain/limb/profile helpers | Fore and hind timing topologies separated; duty cycle a free variable; zero output at zero amplitude; biped posture derived without per-unit rosters. | `audit_gait_anat` |
| Combat posture | Locomotion brace/attack helpers | Fire-hold window only; lower-body gait unchanged; insect wing behavior distinct from avian glide. | `audit_gait_anat` |
| Jump grading | Jump-pose stepper with apex/weight/float helpers | Graded from latched apex height; tuck at apex, deeper landing with height. | `audit_gait_anat` |
| Frame-rate independent damping | `frictionFPS()`, `lerpFPS()` | Residual as exponential original, weight as its complement; MUST NOT retain linear frame-dependent forms or duplicate exponentials; fixed angular caps MUST NOT convert to damping. | `audit_damp_fps` |
| Animation weight vector | `animweights.js`, `animWeights()`, weight keys | Ground tracks sum to one with finite values; key set derived centrally; caller injects unit-free metrics; written without reading at the gait tail. | `audit_anim_weights` |
| Land moss and wet masks | Mask count/id/noise/mask helpers | Current land-field terrain only; world normals with multi-scale noise across three planes; hard steps with road exclusion; integer slots with no new knobs. | `audit_cel_pipeline` |
| Cloth flag waves | Cloth-sway constants, flag helper | Pole side pinned at zero rising to the tip; rate and phase varied by settled instance hash only; global wind time with lateral delay reused. | `audit_soft_stroke` |

Assertion details live in layer-4 audit script headers.
