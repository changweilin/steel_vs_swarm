# §2.1 E — Terrain / Structures / Traversal (single seams)

> Layer 2: full definitions. Principles live in root `AGENTS.md`; assertion details live in audit script headers (layer 4). English only; no operations.

| Topic | Single seam | Constraints | Guards |
|---|---|---|---|
| Compact map scale | `MINI`, `FULL_STAGES`, chain/scale helpers, tower solver, `terrain.bufferM`, venue lane keys, `battleConfig.mini` | Shrink ratios derive from lane-chain needs; all lane counts use compact single-stage towers; edge buffers uniform; phones lock to single-lane battles. | `audit_mini_map`, `audit_world_edge` |
| Story map | `STORY_MAP`, `mapPlan()`, `laneChainOf()`, `mapScaleF()`, `siteCPs()`, boss helpers, `battleConfig.defSide` | One side-string flag derives lane count, tower split, scale, and siege lock with the lock derived server-side and never resent by clients; story geometry stays bit-identical to the mini battle; all tower-adjacent consumers use one accessor; boss identity follows the defending side. | `audit_story_map` |
| Tower layout rules | `towerLayoutAudit()`, `towerTunnelAudit()` | Shared across generation, selection, server, and audit; tunnel coverage MUST NOT devolve into runtime tower moving. | — |
| Lane navigation rules | Lane U-turn, turn-accumulation, and structure-entry audits | Generation-time hard elimination; the latter two are independent and MUST NOT derive from each other. | — |
| Offline terrain section | `tools/venue_field.mjs` with carved-field builder | Sole Node-side mirror of runtime terrain; the height pipeline mirrors the runtime builder verbatim while structure judgments execute biome sources; carved corridors are open complements; clearance eats natural terrain; the structure list lives here alone; administrative-boundary and coastline queries keep their own cache key and MUST NOT merge into scan caches. | — |
| Tunnel profile | `tunFloorAt()`, `underpassPlan()`, `strucHw()` | Mountain tunnels interpolate straight while underpasses sink from the same datum; consumers read the planned points. | — |
| Tunnel roof slab | `tunRoofTop()`, tunnel index roof | The roof is a thick slab with a standable top that blocks shells on both faces; one height source only; ballistics test slab overlap rather than plane crossing; the server needs no change since both ends judge identically. | `audit_layer_block` |
| Carriageway and joints | `carriageHw()`, `flareHw()`, `markBaseAt` | Traffic width and paint width are distinct with all marking consumers on the paint measure; tapers are purely visual and MUST NOT enter structural indexes; bridges and tunnels stay asphalted on the shared datum. | `audit_road_joint` |
| Pedestrian network | `pedestrian.js` plan, archetypes, predicates, planner | Underground paths clear before pruning keeping endpoints; outdoor paths become footbridges only on explicit flags; footbridge decks enter deck indexes while entrances stay presentation-only outside blockers; zero shared randomness. | `audit_pedestrian_plan` |
| Open tunnel judgment | `tunnelWallProfile()` | Open means one buried wall side plus one column side; the open side needs all conditions together read from the natural snapshot; repeated runs MUST agree; underpasses are never open; columns stay transparent and penetrable with both ends judging alike. | `audit_open_tunnel` |
| Roadbed grading | `terrain.gradeRoadBeds()` at its single call site | Dry corridors grade to cut-fill platforms; wet nodes stay untouched, tunnel footprints win, each node takes the nearest road only with no averaging; zero randomness. | `audit_road_bed` |
| Slope movement | `SLOPE`, `slopeDeg()`, `slopeMoveF()`, `slopeBlocked()` | The ease angle derives from the maximum road grade so lane corridors stay full speed; slopes always measure bare terrain; downhill never blocks; unclimbable slopes express through the blocker, not a zero multiplier. | — |
| Projection with bearing | `data.js mapRot()`/`rotXZ()`/`llToXZ()`/`xzToLL()` with thin forwarding consumers | Rotation is part of projection with all layers rotating together as an isometry; one angle derivation serves both pipelines; capture boxes compute at zero rotation; the mirrored consumer stays a thin shell and MUST NOT rotate again. | `audit_road_grid` |
| Battle box and capture | `battleRect()`, `battleBBox()` | Boxes stay axis-aligned with client terrain and server bounds sharing one box; rotation MUST only enlarge the box per axis. | `audit_road_grid` |
| Prune plus quantization | `roadgrid.js` prune and grid helpers, one hookup after road fetch before all consumers | Import-free and randomness-free with pruning before quantization; loop gates cap per-component drops without adding dead ends; quantize with per-road binning, hard drift caps, and shared solved nodes; MUST NOT touch lanes. | `audit_road_grid` |
| Structure build scope | `buildRoads()` with lane-blind signature plus the removal ledger | Elevated and buried structures build even outside lanes with the signature structurally blind to tactics; exactly three accounted removal knives exist and any new knife MUST ledger too. | `audit_road_joint` |
| Flyer ground reference | `game._flySurf()` with the flying branch as sole consumer | MUST be a pure position function; the datum takes the highest of surface, decks, and tunnel roofs as a path-independence invariant; the ground-unit half stays bit-identical. | `audit_npc_collide` |
| World height cap | `WORLD_H`, `objHeightMax()`, `objScaleFit()`, `worldCeilY()` | One tower-anchored yardstick for all coefficients with objects structurally below the ceiling; scales clamp before first consumption without randomness; the server MUST NOT re-verify client-authoritative positions. | `audit_world_height` |
| World boundary | `WORLD_EDGE` with inset/height/depth helpers, ring builder, terrain skirt, player clamp | Ring and buffer share one line; uncrossable by construction with corner overlap; the ring is authoritative geometry in oriented boxes; blockers lead; ring height floors on the tallest hull; skirts are presentation-only. | `audit_world_edge` |
| Edge wall catalog | `edgewall.js` with biome builders and shared emitters | Authoritative ring geometry stays untouched while only looks change; the catalog stays render-free importing randomness only; randomness is coordinate-hashed with zero shared consumption; staging fits inside collision boxes with inner faces covered to eye height; slope gates hard on caller-injected thresholds; one split rule with short runs merged steepest-first and never across water; buffer and backdrop stay presentation-only. | `audit_world_edge` |
| Giant rock landing | `rockProbe()` | Wall and top landings MUST measure true geometry; handwritten profile formulas MUST NOT return. | — |
| Round footprint landing | `sinkBaseY()` | Takes the lowest of center plus footprint ring preferring embedment over hover; MUST NOT sample the center only. | — |
| Traversability | `tools/audit_traverse.mjs` with flood, surfaces, structures, carving | Visited keys pair cells with fixed-quantized height buckets with no tolerance matching; layer changes need open sky at both ends; tool errors MUST NOT launder into skips; missing networks degrade to the terrain layer marked unverified. | `audit_traverse` |

Assertion details live in layer-4 audit script headers.
