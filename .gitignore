# 預設場地兵線預算工具的 Overpass 回應快取(可重新抓取,勿入版控)
tools/.osm_cache/

# 兵線場景稽核的 Overpass / terrarium 高程磚快取(同上,可重新抓取)
tools/.scen_cache/

# `node tools/shot_tunnels.mjs` 的多視角出圖(每座結構 24 張 ~7MB,隨時可重跑,勿入版控)
tools/.shots_tunnels/

# `npm start -- --https` 生成的自簽憑證(含私鑰,MUST NOT 入版控;刪掉會重新生成)
.certs/

# `npm run build:solo` 產出的單機特化版(純複製,隨時可重生;GitHub Actions 也會自己打包)
dist/


# 開發期才裝的 three(離線工具 shot_tunnels / 未來的 Node 端 3D 檢視用;A2:MUST NOT 進 package.json)
node_modules/three/

# 定場鏡頭組出圖(V-A;隨時可重拍,勿入版控)
tools/.shots_scene/

# 在地文字語料烘焙的 Overpass 回應快取(可重新抓取;`venueText.js` 才是產物)
tools/.text_cache/

# `node tools/shot_signs.mjs` 的招牌圖集/排面出圖(隨時可重拍,勿入版控)
tools/.shots/

# 照片庫(Track B 的離線輸入;照片永不入庫,入庫的只有零件 GLB —— skill photo-to-prop-forge §1)
tools/ai3d/photos/
tools/ai3d/.venv/
tools/ai3d/weights/
tools/ai3d/vendor/
tools/ai3d/out/
tools/ai3d/photo_manifest.json
# 自己放圖的地方 + 週期採集迴圈的帳(都是資料家的東西,與照片同一條規矩)
tools/ai3d/inbox/
tools/ai3d/harvest_log.jsonl
tools/ai3d/harvest_state.json
# 本機註冊在案的儲存庫外語料家(絕對路徑 ⇒ 逐台不同,入版控就是別人的機器上一定錯);
# 讀取縫 provenance.extraHomes(),另有環境變數 SVS_PHOTO_HOMES 同效
tools/ai3d/corpus_homes.json

# 離線視覺工具(shot_*.mjs)用的全域 playwright 連結:npm link 產生,MUST NOT 進版控 ——
# 這個儲存庫的 node_modules 是**受版控**的(唯一依賴 ws 隨庫附帶,A2),
# 連結漏進去會看起來像「本專案依賴 playwright」。
node_modules/playwright/
node_modules/playwright-core/
node_modules/.bin/
# 三角形預算量測的中間產物(量測結論住 tools/ai3d/tri_budget.json,原始快照不入版控)
tools/ai3d/.tri_measure/
tools/ai3d/__pycache__/

# YOLO 暫存權重與中間切片快取 (隨時可重新運算, 勿入版控)
*.pt
out/image_cache/
out/targets/
out/yolo_features/

