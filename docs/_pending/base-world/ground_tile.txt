== Ⅰ 選款區塊(顏色的最小尺度)==
  ✓ 抖動 0.42 < 0.5 格距(超過的話最近的 lot 中心會落在 3×3 候選之外 = 分割破洞)
  ✓ 區塊間距 9 格(以底毯格數計 ⇒ 改 cell 自己跟著走,不手寫公尺數)
  ✓ 最近點分割正確:3×3 候選與 7×7 暴力搜尋逐格同解(6400 格)
  ✓ 決定性(同輸入重呼逐位元相同)
  ✓ 純函式:carpetLotAt / planCarpetVariants 原文零 rnd / 零 Math.random / 零 THREE(§2.3、A4)
  ✓ 同款連續長度中位數 117m ≥ 一個區塊 94m(同公式取格心 39m / 2026-08-12 出貨基準 13m)
  ✓ 「走不到兩格就換色」4.3% ≤ 2026-08-12 出貨基準 52% 的四分之一(同公式取格心 3.7%)

== Ⅱ 逐格花紋(同顏色的相鄰拼圖畫不同的圖案)==
  ✓ 共邊的同款鄰格**恆**不同變體(3032 對;硬條件)
  ✓ 對角的同款鄰格「盡可能」不同:同變體率 0.51(舊制對照組 0.98)
  ✓ 三個變體都用得到(只用兩個 = 白白少一種花紋)
  ✓ '!' 崖與 null 未鋪格不指派變體(維持 0,不影響 keys 組裝)
  ✓ 決定性(同一份輸入跑兩次逐位元相同)
  ✓ 異款之間不設限(交界兩側各挑各的:異款本來就是兩張不同的貼圖)

== Ⅲ 靜態規則(執行原文)==
  ✓ baseFill 不把底色抖開(同種地表的全部變體共用同一個底色 ⇒ 逐格換變體 ≠ 逐格換顏色)
  ✓ 仍照抽三枚亂數(每一支畫筆後續的筆觸序列逐位元同舊制 —— 改的只有底色那一格)
  ✓ 舊的 vary(逐變體抖底色)已無殘留
  ✓ cellSubAt 的取值點 = 選款區塊中心(carpetLotAt 單一縫;場的公式一格未動)
  ✓ cellSubAt 只回款名(變體不在這裡挑 —— 逐格獨立挑一定挑得出相鄰同款同變體)
  ✓ keys 兩段組裝:整張挑顏色(subGrid)→ 整張挑花紋(planCarpetVariants)→ 合成 key
  ✓ enclave 換裝仍接在選款上(唯一真相 ENCLAVE_STYLES;水深仍逐格看)
  ✓ planSeamOverlays:同款異變體不發外溢(共用底色 ⇒ 沒有要 cross-fade 的東西)
  ✓ 緩衝空間底毯存在且只在拿得到 bufferHeightAt 時才鋪(降級不例外,原則 6)
  ✓ 高度走 terrain.bufferHeightAt(裙的外推高度唯一縫;拿 heightAt 會被夾回圖界)
  ✓ 分區與選款鏡射回圖內取(三角波在圖界上恆等 ⇒ 接縫兩側同款)
  ✓ 格距放粗(BUF_CELL_F;原尺寸鋪滿要多兩倍半的格子)
  ✓ 發射進圖內同一批 buckets(底毯 carpetBuckets / 外溢 spillBuckets ⇒ 一個 draw call 都沒有多)
  ✓ 圖內那一格沒鋪(崖/灘線/灰帶)⇒ 界外也不鋪(寧缺勿錯)
  ✓ 零共享 rnd 消耗(§2.3:插在建構流程任何位置都不推移植被佈局)
  ✓ 界線拼圖 / 特徵拼圖 / 3D 細節都不進緩衝空間(那些要吃共享 rnd 序列與空間索引)
  ✓ 角點抖動,但圖界那兩條線上的角點不動(那是與真地形的接縫,動了就開縫)
  ✓ 交界外溢走圖內同一支 planSeamOverlays(單一縫;少了它粗格之間就是硬邊直角)
  ✓ 貼地 3×3 面只有 emitFace 一份實作 + face9 一份排列,且只剩緩衝空間在呼叫
  ✓ 高度來源由呼叫端注入(圖內 terrain.heightAt / 緩衝空間 terrain.bufferHeightAt)

== Ⅳ 認養地形三角形 ==
  ✓ inQuad / invBil 原文零 rnd / 零 Math.random / 零 THREE(§2.3、A4)
  ✓ 抖動 0.45 格會生出凹四邊形(5 / 256 格)—— 對角線拆法本來就不成立
  ✓ 認養分割無破洞:每個點都有主人(10609 個探針)
  ✓ 認養分割無重疊:沒有點被兩格同時認養(半開邊)
  ✓ 主人恆落在名義格的 3×3 候選內(ground.js 只掃 3×3 的行為證明)
  ✓ 反雙線性在四角恆等(α 端點 0/1 不漂 ⇒ 與不透明底毯仍水密)
  ✓ 共用邊上兩格算出同一個 α(相鄰外溢共用的那顆地形頂點不開縫)
  ✓ 索引序與 terrain.js 的三角化逐字同向(反對角線 —— 共面的本錢就在這一行)
  ✓ 頂點取地形格點座標(tvx / tvz),不再自己切 face9 子格
  ✓ 圖內底毯 / 外溢 / 脊帶不再套貼合抬升(共面 ⇒ 虧損恆 0;再抬就是浮在地形上)
  ✓ 灘線閘仍以 face9 九點整格判(landCells 與共享 rnd 序列逐位元同舊制)
  ✓ 地形頂點數推導不手寫(terrain 只給 x 軸格距;z 軸要用 worldH 自己回推)
  ✓ 認養表建一次、零共享 rnd 消耗(§2.3:不推移植被佈局)
  ✓ 無主四邊形記帳外露(結構上恆 0;> 0 = 抖動幅度或候選範圍有人動過,畫面上只是偶爾一格禿掉)

== Ⅴ 農牧地表四季設計 ==
  ✓ 吃季節的畫筆 10 支 = 標 seasonal 的地表 10 種
  ✓ 使用者點名的農牧地表(水田/旱田/菜園/果園/牧場/茶園/葡萄園/魚塭/溫室/廢棄農田)全數有四季設計
  ✓ 10 支畫筆 × 四季 兩兩畫出不同的東西,且不只是換底色
  ✓ 季節只進 groundTex 的快取鍵,bucket 鍵仍是 sub#variant(一場一個季節 ⇒ draw call 不變)
  ✓ 畫筆種子仍只由 sub#variant 導(同一塊田的壟溝與缺株位置四季不動,只有作物換了)
  ✓ 兩個 mesh 消費端都把 season 傳進去,且對 seasonal 地表跳過 SEASON_TINT(免調兩次色)
  ✓ 牧場 pasture 六處註冊齊全(PAINTERS / DEFS / SIZE / ZONES.green / FAMS.rectFarm / scatterDetails)

== Ⅵ 選款清單的顏色路徑(換款的「幅度」那一半)==
  ✓ SUB_COL 恰涵蓋底毯款(CARPET ∪ ENCLAVE_STYLES[].carpet;27 款)
  ✓ 每一款底毯畫筆的底色都取自 SUB_COL(brick 是具名例外:代表色由 BRICK_C 推導)
  ✓ brick 的代表色由磚色陣列推導,不手寫第二個數字
  ✓ carpetOrder 原文零 rnd / 零 Math.random / 零 THREE(§2.3、A4)
  ✓ 排序保留重數(重複項 = 權重,排完各款佔比逐位元不變)
  ✓ 同款排在一起(權重成為漸層上的一段平台,不是散在清單各處)
  ✓ 沒有任何一份清單的最大相鄰色距被排壞
  ✓ 實際改善的清單數 11/19(0 = 排序沒有在做事)
  ✓ carpetLists 在冬季覆寫之後才排序(冬季那兩份是新組的清單)
  ✓ enclave 樣式的底毯清單也排序,且不就地改寫模組級常數
  ✓ 紅磚地 + 水泥地實得佔市區底毯 11% ≤ 出貨基準 43% 的一半
  ✓ brick / concrete 在市區底毯各只剩一格,且 brick 已退出特徵層 ZONES.urban

== Ⅶ 同顏色拼圖上的多種點綴(2026-08-13「草地的小花/沙地的小石頭/水域的游魚」)==
  ✓ 新增三款點綴 fish / shell / mushroom 都有零件表
  ✓ 三款都登記了 TILT 與 REG(漏了的那一款會恆直立且恆隨機朝向,不會報錯)
  ✓ fish 進 AQ_DET(水生細節,免吃岸線高度淘汰)
  ✓ fish 走 DIVE(沉在水面下)且水深不足就不擺(§4 寧缺勿錯)
  ✓ 每一款底毯地表都撒得出兩種以上的點綴(26 款)
  ✓ 水域三款(淺水/深水/荷塘)都有游魚

🎉 ALL PASS
