{
 "lanes": [
  {
   "lane": "lane-ink(渲染核心;toon/postfx/visualPrefs/data 的唯一擁有者)",
   "ownsFiles": [
    "public/js/toon.js",
    "public/js/postfx.js",
    "public/js/visualPrefs.js",
    "public/js/data.js",
    "tools/audit_cel_pipeline.mjs",
    "tools/audit_visual_prefs.mjs",
    "tools/audit_soft_stroke.mjs",
    "tools/audit_gpu_lifecycle.mjs"
   ],
   "items": [
    "P0 全域基準(先於一切,不寫任何檔):mobile-rest 已落盤的 BASELINE.txt / bal.txt / e2e.txt 直接沿用;另補拍序 12 要求的改制前定場照 —— taroko+shibuya × {預設, --pref inkMrt=on --pref lutSrc=baked --pref shadowMech=1 --pref shadowEnv=1, --time dusk --elapsed 300, --time night},以及 seq8 ④-3 要的五種天氣各一輪。md5 全表落盤。㋓ 沙箱跑不動 ⇒ 交付說明 MUST 標未驗並在真機/Actions 補",
    "I1 mobile-rest C-0:tools/audit_cel_pipeline.mjs:313 的 readdirSync 改遞迴(照抄 audit_client_syntax.mjs:61-63 的 listJs),反向驗證 = 往 public/js/forge/forge.js 注入一支不帶 INK_INFO_DECL 的 ShaderMaterial ⇒ Ⅵ MUST 紅(今天注入後仍綠)。**MUST 排在本輪任何新模組之前** —— 這道閘漏子目錄,之後每一次「全綠」都不算數",
    "I2 seq3-ink-contrib 全項:INK_CLASS 換索引 0/1/2 → INK_PACK_GLSL/INK_UNPACK_GLSL/INK_LEVELS/inkQuant → applyCelPatch 的 ink 參數與 uInkCtr → postfx 三個讀取點解包(哨兵 min(cls)>0.5、LUT 帶 cls>0.5&&cls<1.5)→ 最近面覆寫(ceil/floor 硬決定,NONE 不投票)→ _mkRT 的 Array.isArray 守衛 + texture[1] NearestFilter → inkRepeat(pitchM)。①~④ MUST 同一個 commit(讀取端與寫入端分段交付 = inkMrt=on 當場壞)。⚠ --break-lutland 的字面替換樣式綁著舊那一行,MUST 同步更新並確認它仍紅(§5.4 ㋑)",
    "I3 **seam batch —— 契約凍結並公告(= lane-world / lane-motion 的解鎖閘)**:一次把下游四道要用的縫全部落在 toon/postfx/visualPrefs/data,呼叫端一個都不接、預設值一律中性。內容見 sharedSeams 的 S1~S11。此步結束 MUST 跑:audit_cel_pipeline(全 + 五支既有 break)/ audit_visual_prefs / audit_soft_stroke / audit_gpu_lifecycle / audit_client_syntax(㋖)/ npm run bal / npm test(先照 §5.2 重啟伺服器)—— 全部 MUST 逐項不動,並把 seam 契約寫進 docs/_pending/lane-ink.md 公告",
    "I4 seq4-ink-noise:①-2 雜訊斷線(inkBreak 旋鈕 + INK_BREAK 常數 + celInkBreak() + CEL_INKB 的 mat3(modelMatrix) 錨點 + 快取鍵補 B)、①-4 只落地「掠射抑制項恰一項」斷言 + --break-graze(**建議不換 K_S**,理由見 userDecisions)、①-5 LUT 九條斷言 + --break-lutstack。⚠ 動 audit_soft_stroke Ⅱ 的三條既有斷言時,語意(恰一處寫入 / 非軟性件恆寫 1 / 只給不透明件)MUST 逐條保住,--break-ink 與 --break-anchor MUST 仍各自紅字。⚠ 落地前 MUST grep public/js 的 opacity 呼叫端,確認沒有 transparent:false 但 opacity<1 的 cel 材質(否則旋鈕 0 也不中性)",
    "I5(收尾協同,要等 lane-world 的消費端上線)rock-silhouette 的 M3 定案:postfx 的 INK_MRT.SELF_F ∈ {1.0,1.6,2.2,3.0} × GRAZE_K ∈ {0,1,2,3} 掃參數,判準三條(巨岩主量體 ico/圓柱小面線消失 / rib·鑿面·棧道·鏡牆帶的線仍在 / 山脊線仍在而坡面高度場格線消失)。MUST 取平均、**MUST NOT 逐場地挑參數**(同 A46 ⑩ dn_iter 的紀律)。同輪補 audit_cel_pipeline Ⅷ 的 --break-selff",
    "I6(可選,需使用者放行)seq8 的 ④-3 單獨抽出:勾線遠處淡出改由 scene.fog 推導(INK.FADE_F + _inkFadeM(),fadeEnd = max(fog.far, combatReachM()/F))+ audit_cel_pipeline 新 Ⅸ + --break-fade。只動 postfx.js 與 audit_cel_pipeline ⇒ 完全落在本道之內;seq8 其餘三條延後(見 notes)"
   ],
   "rationale": "toon.js 被 14 份規格中的 10 份、postfx.js 被 4 份指名,而且它們是賽璐璐 + 勾線 + 風 + 世界曲面 + MRT 五個系統的交會點 ⇒ 硬約束 2 要求單一擁有者。我選的切法是**「seam foundry 先做完,其他道之後只讀」**:本道不只做序 3/4,而是把 rock / seq5 / seq7 / seq9 / seq10 / seq12 想要的 toon/postfx 側縫**一次全部落完**(I3),再把消費端(biomes / ground / game / locomotion)整批交給別的道。代價是 I3 是一道 gate(lane-world 與 lane-motion 的一部分要等它);收益是 gate 之後四道真的可以同時動手,而且「同一個縫只有一份實作」是構造保證而不是紀律。序 3 排在最前面是硬約束 3(它是序 4 / 序 7 / 山頭巨石 / 序 12b 的共同前置);I1 排在序 3 之前,是因為 gInfo 閘門對子目錄失效,本輪要新增 5 支客戶端模組,不先修這一格,之後每一次「全綠」都是假的。"
  },
  {
   "lane": "lane-world(世界幾何與地被;biomes/ground/terrain/main/siteplan/hazards/edgewall/beacons 的唯一擁有者)",
   "ownsFiles": [
    "public/js/biomes.js",
    "public/js/ground.js",
    "public/js/terrain.js",
    "public/js/main.js",
    "public/js/siteplan.js",
    "public/js/hazards.js",
    "public/js/edgewall.js",
    "public/js/beacons.js",
    "public/js/petals.js",
    "public/js/leafcard.js",
    "public/js/vehicles.js",
    "tools/audit_ambient_motion.mjs",
    "tools/audit_water_edge.mjs",
    "tools/audit_vehicle_spec.mjs",
    "tools/audit_leaf_card.mjs",
    "tools/audit_object_joints.mjs",
    "tools/audit_siteplan.mjs",
    "tools/audit_beacons.mjs",
    "tools/audit_ground_tile.mjs",
    "tools/audit_ground_qc.mjs",
    "tools/audit_world_edge.mjs",
    "tools/shot_scene.mjs",
    "tools/ai3d/tri_budget.json"
   ],
   "items": [
    "**gate:等 lane-ink 的 I3 公告 seam 契約**(S2/S3/S4/S6/S7/S8/S9 全部要用到)。gate 之前可以先做只讀的事:留基準(audit_object_joints --seeds 8 / audit_siteplan / audit_beacons / audit_ground_* 的完整輸出落盤,那是後面每一項「逐位元不動」的唯一依據)",
    "W1 rock-silhouette 的消費端:placeMegaliths 的調色 traverse 內分兩群組(MEGA_BODY_F 起手 0.5,量測 MUST 排在 jitterMegalith **之前**)、ground.js 細節發射迴圈逐 type 取 surfGroup() + ctr: inkCtrM(detailR(type)*2)(**取號 MUST 在零件迴圈之外**)、buildBackdrop / buildEdgeWall 的 flushPartBatch 各加 ctr。零 rnd 消耗 ⇒ audit_siteplan/beacons/object_joints/ground_* MUST **逐項不變**(不是「仍全綠」)",
    "W2 seq5 的 ⑤-4 落花:新建 public/js/petals.js(零 THREE、只 import rng.js)+ biomes.js 在 dynamics 之後接線。落點由**最終植被實例名冊 items** 推導(part.key === 'foliage')、色調取 ENV.seasons[].accent、專屬 mulberry32(gseed ^ 0x5E7A1)⇒ **零共享 rnd 消耗**。⚠ ⑤-5 雲層飄移**零工作**(environment.js:222/:505 早已符合,audit_soft_stroke Ⅴ 已有四條在守)—— 只寫進交付說明,MUST NOT 動 environment.js",
    "W3 seq9 水:terrain.js 的 seaFadeAt 抽函式 + bakeSeaDepth()(零 rnd,無水域不烤)→ main.js 一行 stampSeaBlockers(**MUST 吃 A30 的有向盒 hw2/hd2/ry,不是外接圓**)→ biomes.js 退場舊的 buildWaterEdges 泡沫分支(**這是替換不是新增** —— 計畫書那句「本專案水面只有波形沒有 shore band」是過期的)→ buildWaterReflections(反射體名冊 MUST 排除邊界牆環:它是 blockers 的第一批,naive slice 剛好只選到它)。新斷言全部進 tools/audit_water_edge.mjs",
    "W4 seq10 的 10a(收斂,可證逐位元):新建 public/js/vehicles.js(**零 import**,才進得了 edgewall.js 的零 import 契約)→ 依序接 hazards.wreck / biomes.car / siteplan.CIVIC_PARTS.lot / edgewall 三款 + beacons.depot / ground.DETAIL_DEFS(**只換形狀不換外廓**)→ 公設與障礙的 mergeGeos 合併(吃 lane-ink 的 S9:bakeContactAO 改乘)→ ③-2 真凹處(只上載具/公設/邊界牆三處)→ ③-3 可視角(新 standEyeM(),**MUST NOT 碰有缺陷的 curveEyeM**)。哨兵 = detailR('carwreck')/detailR('container') 逐位元等於凍結常數。⚠ 每接一個消費端 MUST 同步改稽核裡的 new Function 樁件注入(audit_object_joints:577 / audit_siteplan:96 / audit_soft_stroke:260·436 —— 漏了整支當場 ReferenceError,而錯誤訊息與接合完全無關)",
    "W5 seq7 葉片卡(**gated on 使用者裁決**,見 userDecisions 10):新建 public/js/leafcard.js(零 THREE、只 import rng.js)+ biomes.buildVegMeshes 的逐株面號(leafSurfId 由落點雜湊,零共享 rnd)+ leafCardTex/leafRowGeo + 重量 measured_kind_tris 並**重跑 intake_parts**(它是整層總量閘的分母,不重量就是拿舊基準放行)。castShadow MUST 維持 false;transparent MUST 是 false + alphaTest"
   ],
   "rationale": "biomes.js 被 8 份規格指名、ground.js 被 2 份,而 seq5/7/9/10/rock 的消費端全部落在這一族檔案上 ⇒ 第二個必然的單一擁有者。把它與 lane-ink 拆開的關鍵是:這五項的 toon/postfx 側縫已經由 I3 一次落完,本道只寫幾何、材質選項與自己的新稽核。道內順序由「共享 rnd 序列的風險」決定:W1 零消耗、W2 零消耗、W3 零消耗、W4 只要包絡不動就零消耗(10b 才會推移)、W5 零消耗 —— 每一步的驗收面都是同一組落盤基準逐項比對,先做零風險的、把 detailR 那個哨兵留到最後才有機會被踩。audit_soft_stroke 留在 lane-ink(它是墨線契約的稽核),本道的新斷言一律進自己的新稽核檔,這是本道能與 lane-ink 並行的第二個前提。"
  },
  {
   "lane": "lane-motion(動作權重與音效;game/locomotion/audio 的唯一擁有者)",
   "ownsFiles": [
    "public/js/game.js",
    "public/js/locomotion.js",
    "public/js/animweights.js",
    "public/js/audio.js",
    "public/audio/README.md",
    "public/audio/amb/",
    "public/audio/bgm/",
    "tools/audit_anim_weights.mjs",
    "tools/audit_audio_layers.mjs",
    "tools/audit_damp_fps.mjs",
    "tools/audit_gait_anat.mjs"
   ],
   "items": [
    "M0 基準:audit_gait_anat 完整輸出落盤(M1 要求它逐字不變)。**無 gate,可與 lane-ink 的 I1/I2 同時開工**",
    "M1 seq11-birds 的 ⑥-3 動畫權重向量(硬約束 3:它是 ⑦-2 的前置):新建 public/js/animweights.js(零 import,WEIGHT_KEYS 有序、地面三軌和恆 1、缺欄回 0 不回 NaN)→ locomotion.stepLocomotion 收尾**只寫不讀** L.w ⇒ audit_gait_anat 八段 MUST 逐字不變 → game.js 三處收斂(刪 ent._moveSpd 這第二份速度推導、_moveCat 的 `>3` 這第三個離地門檻改吃 w.air、_updateMoveAudio 的 moveGate 改吃 w.walk+w.run)。⚠ ent.loco 在重生瞬移那一幀是 null(game.js:8160),沒有守衛就是 NaN 進 setTargetAtTime、把整條幀迴圈打斷",
    "M2 seq6-audio(⑦-1/2/3/4):照規格的步 1→步 10 順序做 —— ⑦-3 多 take(名冊仍全單字串 ⇒ 先驗管線)→ ⑦-4 低記憶體階梯(**setLowPower(false) MUST 補 _loadSamples(),漏了就是「設定好像沒作用」而每條斷言全綠**)→ ⑦-1 名冊與純函式 ambienceMix → 常駐床(**MUST NOT 走 _play**,那有去重窗與 _MAX_VOICES ⇒ 齊射時環境音會消失)→ ⑦-2 gain-ride(chop LFO 恰一顆、兩條鏈掛同一顆)。新稽核 tools/audit_audio_layers.mjs 八段 + 七支 --break。⚠ 稽核內 MUST NOT 出現字面 '/audio/'(audit_net_modes.mjs:118 的 strayPaths 會紅,而訊息完全不相干)",
    "M3 seq5 的 ⑤-1 餵入(**gate:等 lane-ink I3 的 setCelChar 契約**):game.js 新增 _charSlots(dt)(槽 0 = 主視野機體,其餘依離相機距離;速率用**位置差分**經 lerpFPS 平滑 —— 拿 this.vel 會漏掉攀爬 / push-out / 蓄力跳三條路徑)+ 在 stepCelWind(dt) 之後呼叫 setCelChar(...),MUST 排在 _updateEnts 之後(那時 ent.mesh.position 才是本幀插值完的值)+ killswitch ?tread=0",
    "M4 收尾:audit_damp_fps ±--break-damp、audit_gait_anat/morph_rig/paper_doll 逐項不變、npm run bal / npm test 逐項不動(data.js/sim.js/server 一行未改)。⚠ ⑥-3 的音效端**刻意不是逐位元中性**(離地門檻 3m→2m、gate 曲線換來源),MUST 列進交付說明的未驗項並真機聽一次(㋕)"
   ],
   "rationale": "game.js 被 4 份規格指名(seq5/6/8/11)⇒ 第三個必然的單一擁有者。把 ⑥-3 與 ⑦ 綁在同一道是計畫自己的依賴(⑦-2 的 gain-ride 吃 ⑥-3 的權重向量),而計畫的順序表把 ⑦ 排在序 6、⑥-2 排在序 11 卻沒給 ⑥-3 獨立序號 —— 這是計畫順序表的一個缺口,本計畫把 ⑥-3 提前到 ⑦ 之前(見 notes)。⑤-1 的 game.js 餵入放進本道而不是 lane-world,是因為它寫的是 game.js 不是 biomes.js;它與 toon.js 側的 GLSL 位移項由 setCelChar 這個縫接起來(S5)。⑥-2 鳥群整項延後(理由見 notes),因此本道完全不碰 biomes.js。"
  },
  {
   "lane": "lane-zonecut(§0-a 可行性樁;純 tools,零出貨風險)",
   "ownsFiles": [
    "tools/zonecut.mjs",
    "tools/audit_zone_cut.mjs",
    "tools/venue_field.mjs",
    "tools/audit_traverse.mjs",
    "tools/logo_lib.mjs",
    "tools/CLAUDE.md"
   ],
   "items": [
    "Z0(先做,不寫規則)量 29 個場地的 cell / gnx×gnz / worldW×worldH / edgeBufferM / 含裙跨距,更正計畫 §0-a「最大可行範圍」那張表(實測 paris 3v3 含裙 3362m ⇒ 1024² = 3.28 m/texel,不是計畫寫的 2.06)。這一步先做,因為序 14 的貼圖規格整個掛在它上面",
    "Z1 venue_field 新增 cutLinesFor(id, bbox)(administrative + coastline,額度隨 bboxKm2 縮放,失敗回 null 並標未驗)",
    "Z2 把 buildStructs/projectArc/ptAt/sampleAlong 從 audit_traverse 搬進 venue_field 並 export。**搬家前後各跑一次 audit_traverse --only=<有快取的場地> --json=,兩份 MUST 逐位元相同** —— 這是純搬家的唯一證明,「還是全綠」不算",
    "Z3 tools/zonecut.mjs(零 import / 零亂數 / 純函式):rasterLines / floodFaces(**4 鄰**,8 鄰會讓對角相碰的兩面漏成一面)/ mergeSmall(比**那個鄰居**的面積、共邊才算相鄰、小的貼上大的)/ faceSamples(決定性分層取樣)",
    "Z4 樁的線工組裝與線分級掃參數;Z5 第二段逐面標籤(**readSrc 抽 cellZoneAt 原文丟 new Function 執行真品**,判定順序一行不改由原文閘守);Z6~Z9 三個驗收面(界線離真值距離 / 決定性含順序無關 / surfaceId 量化算術)+ 成本量測 + 對照 PNG;Z10 八支 --break",
    "Z11 報告 MUST 附兩份清單交給下一輪:①序 14 的第一道門(執行期要多抓的四類圖資 + geoKey('osmF',3) + osmrelay 中繼 + measure_osm_relay)②序 15 若成立,哪幾支稽核的哪幾條會失去對象(= 取消成本)"
   ],
   "rationale": "本項 public/** 與 server/** 一行不動(git diff --stat -- public/ server/ MUST 為空),與其他四道**零檔案交集**,是本輪唯一可以從第一分鐘跑到最後一分鐘、完全不等任何人的道。它的產出是序 14/15 的 go/no-go 判決,而硬約束 4 明講序 14/15 在它通過前不做 ⇒ 越早開工越好。唯一要注意的是它獨佔 tools/audit_traverse.mjs —— 這也是我把 seq8 的 ④-4(純註解併進六支稽核檔頭,其中含 audit_traverse)整項延後的原因之一。"
  },
  {
   "lane": "lane-mobile(⑧-5 觸控硬化;CSS 與觸控層)",
   "ownsFiles": [
    "public/css/style.css",
    "public/js/mobile.js",
    "tools/audit_ctrl_mode.mjs"
   ],
   "items": [
    "X1 style.css:52-53 的 html, body 補 -webkit-text-size-adjust: 100% / text-size-adjust: 100%(全 repo 目前零命中;iOS 橫式會自己放大 HUD 文字,而 HUD 的 1/6 是 fitHudBand() 量出來的)",
    "X2 新增裝置 class(建議 touch-dev),判定唯一縫 = ctrlmode.touchCapable(),掛載點與 installTouchUI() 同一支。**MUST NOT 在 mobile.js 或 CSS 再寫一份 maxTouchPoints / pointer: coarse**(A21 家族)",
    "X3 把 style.css:1638-1644 的五條頁面級硬化(overscroll-behavior / user-select / touch-callout / tap-highlight / #game{touch-action:none})由 body.touch-ui 改綁 body.touch-dev;--tl-* 控件變數、.tl-* 逐控件 touch-action、安全區變數 --tl-sl/sr/sb/st **全部留在 body.touch-ui**(那是版型不是裝置)。⚠ 這修的是真缺陷:硬化現在綁在**房主可關的房間設定**上,房主選「限定滑鼠鍵盤」時真手機上這四條全部消失",
    "X4 audit_ctrl_mode 新增 Ⅹ 段五條(viewport-fit=cover / text-size-adjust / 硬化掛在裝置 class 且該 class 只轉呼 touchCapable() / 捲動容器 MUST NOT 被 touch-action:none 蓋到 / 安全區仍在 body.touch-ui)+ 四支 --break(viewport / touchact / touchdev / textadj)。全 repo 目前**沒有任何一支**驗過 viewport meta / touch-action / safe-area",
    "X5 回歸:audit_ctrl_mode / audit_touch_layout / audit_touch_gesture / audit_ui_layout / audit_client_syntax 對照基準 MUST 逐項不動(audit_touch_layout 的 8/60 既有紅 MUST 維持在 8,變多就是 X3 動到版型)"
   ],
   "rationale": "三個檔案沒有任何其他規格指名,零交集、零 gate,是最便宜的一道。刻意**不含 ⑧-4(桌機 DPR)** —— 那一項是純裁決題(見 userDecisions 18),而且它要動 game.js(lane-motion 的地盤)與 audit_gpu_lifecycle(lane-ink 的地盤),裁決下來之前一格都不該碰。mobile-rest 的 C-0 也不在本道:那是 audit_cel_pipeline,歸 lane-ink 並排在它的第一項。"
  }
 ],
 "sharedSeams": [
  {
   "seam": "S1 gInfo.a 半位元組打包(類別 + outlineContribution)",
   "file": "public/js/toon.js(編碼/解碼字串)+ public/js/postfx.js(三個讀取點)",
   "owner": "lane-ink / seq3-ink-contrib",
   "consumers": [
    "lane-world W1 rock ctr",
    "lane-world W5 seq7 的 INK_CLASS.GROUP",
    "延後的 seq12b 逐結構 contrib",
    "延後的 seq4 ①-3"
   ],
   "contract": "export const INK_CLASS = { NONE: 0, LAND: 1, HARD: 2, GROUP: 3 }(**索引,不再是 0/0.5/1 浮點帶**);export const INK_LEVELS = 16;export function inkQuant(c): number(回 round(clamp01(c)*15)/15);export const INK_PACK_GLSL: string(提供 `float inkPack(float cls, float ctr)`,= (cls*16.0 + floor(clamp(ctr,0.,1.)*15.0+0.5))/255.0);export const INK_UNPACK_GLSL: string(提供 `float inkCls(float a)` 與 `float inkCtr(float a)`)。魔數 16/15/255 **只准出現在這兩個字串裡**,postfx 三個讀取點一律 import 前置。哨兵改 min(cls) > 0.5、LUT 地貌帶改 cls > 0.5 && cls < 1.5。level 15 的解碼 MUST 是**嚴格 1.0**(31/16 與 47/16 在 float32 上精確)⇒ ink *= ctr 與 ceil/floor 覆寫都是恆等 = 逐位元中性的證明面"
  },
  {
   "seam": "S2 applyCelPatch 的選項擴充(所有下游材質的唯一入口)",
   "file": "public/js/toon.js",
   "owner": "lane-ink / I3 seam batch",
   "consumers": [
    "lane-world 全部四項",
    "延後的 seq8 dissolve",
    "延後的 seq12 toonPlain"
   ],
   "contract": "applyCelPatch(mat, { …既有 metal/rim/wash/moss/cool/paint/tint/preview/soft/bands/land/landNrm, **ink: 'hard'|'land'|'group'|'none' = 'hard'**, **contrib = 1**, **surf = null**, **surfAttr = false**, **card = false**, **refl = false** });envMat(color, opts) 與 toonMat(color, opts) 逐項透傳(envMat 收全部,toonMat 至少收 contrib/ink/surf/surfAttr/card);celOpts(toon.js:600)MUST 一併記錄新欄位(applyPaint 以它重跑,漏了 = 上塗裝的機體貢獻靜默重置);customProgramCacheKey(toon.js:966)MUST 併入**每一個**新的 define 旗標(uniform 不進鑰匙 —— contrib 是 uniform,進鑰匙就是每個值編一支程式)。⚠ 現行簽章在 toon.js:567,審核用的正規式綁著 `{ metal: !!celMetal, rim, soft, bands` 與 `tint: 'env', preview, soft, bands` 兩處呼叫形狀(audit_visual_prefs),新參數 MUST 加在尾端不得拆散那兩段"
  },
  {
   "seam": "S3 表面群組(同一個東西共用一個 surfaceId)",
   "file": "public/js/toon.js",
   "owner": "lane-ink / I3",
   "consumers": [
    "lane-world W1(巨岩兩群、ground 逐 type、背景/邊界牆)",
    "lane-world W5(逐株樹的幹/枝/冠共用一號)",
    "延後的 seq12b(坑門混凝土家族)"
   ],
   "contract": "export const SURF_ID = { LAND: 0, CONCRETE: 1 };const LAND_SURF_ID = SURF_ID.LAND;(**MUST 保留 `const LAND_SURF_ID = 0;` 這個字面** —— audit_cel_pipeline Ⅶ 有 /const LAND_SURF_ID = 0;/ 這條樣式,改寫法會紅字而理由是假的);export function surfGroup(): number —— 回一個新群組號,**整數格 k/64**,與 nextSurfId 的半整數格 (k+0.5)/64 恆差 ≥ 0.5/64 = 0.0078 > INK_MRT.ID 的 0.004 門檻;export function joinSurfGroup(mat, id): void —— **MUST 在 scene.add / new InstancedMesh 之前呼叫**(uSurfId 在首次編譯當下凍結,晚一步就是一行都不生效、線照畫、console 無訊息、每條原文斷言全綠);逐實例版走 S2 的 surfAttr: true(attribute float aSurfId → varying vSurfId → gInfo.b),`#ifdef CEL_SURF_A vSurfId #else uSurfId #endif`"
  },
  {
   "seam": "S4 outlineContribution 的推導(尺寸/間距 → 這一款東西值不值得一條線)",
   "file": "public/js/toon.js(inkRepeat)+ public/js/data.js(INK_CTR / inkCtrM)",
   "owner": "lane-ink / I2+I3",
   "consumers": [
    "lane-world W1(ground 細節逐 type 的 inkCtrM(detailR*2))",
    "延後的 seq12b(逐結構授權表)"
   ],
   "contract": "toon.js: export const INK_REPEAT_M(授權值,見 userDecisions 1)+ export function inkRepeat(pitchM) = clamp01(pitchM / INK_REPEAT_M);data.js: export const INK_CTR + export function inkCtrM(sizeM) —— **嚴格單調遞增、sizeM ≥ FULL_M 恆等於 1**。⚠ **兩份 contribution 常數表並存 = 「拉桿改了一半的東西」** ⇒ 落地時 lane-ink MUST 在 I3 的公告裡明說哪一個住 toon.js、哪一個住 data.js,lane-world 一律轉呼、MUST NOT 自己寫第三份。授權值一律先經 inkQuant 收成 k/15(§0-c 只有 16 階,不量化就與編碼端的 round 對不上)"
  },
  {
   "seam": "S5 玩家位移擾動(⑤-1 的兩半接縫)",
   "file": "public/js/toon.js",
   "owner": "lane-ink / I3",
   "consumers": [
    "lane-motion M3 的 game._charSlots(dt)"
   ],
   "contract": "export const CHAR = { N, R0, R_PER_MPS, SPD_REF, PUSH_F, SPD_K };export function setCelChar(list): void,list = [{ x, z, spd }, …],長度 ≤ CHAR.N,**沒填到的槽位由本函式顯式寫 spd = 0**(呼叫端不必補);GLSL 側的位移加項住既有 CEL_SWAY 區塊、排在 #include <project_vertex> 之前,距離取實例原點的 XZ、半徑吃 uCharSpd。三條硬規則:①**MUST NOT 出現第三個 sin(**(audit_soft_stroke Ⅲ 的正規式會跨到 CEL_WAVE 區塊,count(/sin\\(/g) === 2 是全域計數,多一個就紅而理由與擺動無關)②**MUST NOT 對零向量 normalize() 之後乘 0**(NaN × 0 仍是 NaN ⇒ 那批 InstancedMesh 整批消失、console 無訊息)③位移 MUST 是**早退不加**而不是「加一個 0」(x + 0.0 對 -0.0 不是恆等)。單一角色 vs 4 槽見 userDecisions 4"
  },
  {
   "seam": "S6 海面深度場與泡沫(⑤-2 的兩半接縫)",
   "file": "public/js/toon.js",
   "owner": "lane-ink / I3",
   "consumers": [
    "lane-world W3 的 terrain.bakeSeaDepth / main.stampSeaBlockers"
   ],
   "contract": "export const FOAM / export const REFL;export function seaFieldN(worldW, worldH, low): number(**推導,MUST NOT 手寫 1024** —— 手寫的話低功耗折半靜默失效);export function foamBandM(): number;export function setSeaDepthField(data: Uint8Array, size: number, bounds: { minX, minZ, w, h }): void —— 照 setWeatherField(toon.js:358-371)逐條寫,共享 uniform _seaField/_seaRect,預設 1×1 中性「很深」貼圖,**old?.dispose() 不可少**(A25);GLSL 側 celFoam() 住 #ifdef CEL_WAVE、相位 MUST 減去 celSeaH(浪一來泡沫沖上岸)、套用 MUST 排在 #include <opaque_fragment> **之後**(寫在 diffuseColor 上會再過一次 toon ramp = 硬邊被階梯切開)、MUST 乘 vSeaFade(否則 53m 外環水面會被 ClampToEdge 拉出一圈全白)。⚠ celSeaH( 的呼叫點計數斷言(audit_soft_stroke.mjs:361 === 6)會因此紅,那是預期的,期望值 MUST 由 lane-ink 同步改成 7"
  },
  {
   "seam": "S7 葉片卡材質與群組早退(②-1 的兩半接縫)",
   "file": "public/js/toon.js + public/js/postfx.js + public/js/visualPrefs.js",
   "owner": "lane-ink / I3",
   "consumers": [
    "lane-world W5 的 leafcard.js / biomes.leafRowGeo",
    "延後的 rock 若要擴大群組早退範圍"
   ],
   "contract": "toon: toonMat(col, { card: true, map, alphaTest: 0.5, surfAttr: true, ink: 'group' }) 開 CEL_LEAFCARD;attribute 契約 **aCard = vec3(角落x, 角落y, 旋轉)**,四個角在視域空間展開;快取鍵 MUST 併入 card 與 surfAttr(漏了 = 四個角都落在中心、整叢卡片塌成一個點,畫面上只是「那幾棵樹沒有葉子」)。postfx: INK_GRP 早退 —— 「五格同號**且至少一格是 GROUP**」⇒ 整格不畫;_wantInfo() 收第三個消費端。visualPrefs: inkGroup(def 'off')、leafCard(def 'off',步 6 才翻)。**_mrtCap 為假或 inkGroup !== 'on' 時 leafCard MUST 自動視為 off**(卡片沒有群組早退時比舊制更糟:12~24 張卡 = 12~24 個黑色多邊形)"
  },
  {
   "seam": "S8 內部折邊門檻(山頭/巨石/石堆的 M3)",
   "file": "public/js/postfx.js",
   "owner": "lane-ink / I5",
   "consumers": [
    "lane-world W1 的驗收(定場照掃參數)"
   ],
   "contract": "INK_MRT 追加 SELF_F(同群組時法線折邊門檻的倍率,MUST > 1,否則整段是恆等式)與 GRAZE_K(MUST > 0);勾線 pass 推導 same 與 nz,**只換法線折邊那一項的門檻,深度那一項刻意不抬**(深度跳變的語意就是剪影;順手抬了 = 兩顆重疊的石頭之間的輪廓一起被吃掉 = 一坨)。門檻 MUST 經 mix(…, same) 切換而不是常數。⚠ audit_cel_pipeline Ⅷ 的斷言 MUST 直接讀 INK_MRT 的**解析值**比大小,MUST NOT 只比對字面(否則 --break-selff 的期望值會跟著 break 跑,break 永遠是綠的,§5.4 ㋑)"
  },
  {
   "seam": "S9 接地 AO 改「乘」不是「覆寫」(③-4 合併的前提)",
   "file": "public/js/toon.js",
   "owner": "lane-ink / I3",
   "consumers": [
    "lane-world W4 的 siteplan.buildCivic / hazards.buildHazard 合併"
   ],
   "contract": "bakeContactAO(root, fade) MUST 改成**乘進** geometry.color 而不是覆寫。理由:mergeGeos(geos, colors)(beacons.js,頂點色合併縫)與 bakeContactAO 都寫同一個通道,現況兩個消費端剛好互斥所以從沒撞過,seq10 讓它們第一次同時出現;撞到的症狀是「公設/障礙整組沒有接地陰影」或「整組變灰白」,兩種都不報錯而 audit_gpu_lifecycle 照樣全綠(它量的是 dispose 不是顏色)"
  },
  {
   "seam": "S10 visualPrefs 旋鈕表(單一擁有,別道一律開票)",
   "file": "public/js/visualPrefs.js",
   "owner": "lane-ink",
   "consumers": [
    "lane-world(leafCard / foam / reflect)",
    "lane-motion(若 ?tread=0 要升級成旋鈕)",
    "延後的 seq8 wipe / seq12 celSchool / seq11 birds"
   ],
   "contract": "VISUAL_KNOBS 由 lane-ink 單一擁有;**別的道要旋鈕一律在 I3 之前開票給 lane-ink 一次加完**,MUST NOT 自己往那張表塞一列。紀律①不變:def 那一欄 = 交付定案值,而需美術方向確認的一律 def = 「這一項不生效」。本輪一次加完的清單:inkBreak(def 0)/ inkGroup(def 'off')/ leafCard(def 'off')/ foam(def 1)/ reflect(def 0)。killswitch 走 URL 參數的那一批(?tread=0 / ?petal=0)不進這張表"
  },
  {
   "seam": "S11 稽核歸屬(平行化能不能成立的第二個前提)",
   "file": "tools/audit_cel_pipeline.mjs / audit_visual_prefs.mjs / audit_soft_stroke.mjs / audit_gpu_lifecycle.mjs",
   "owner": "lane-ink",
   "consumers": [
    "lane-world 全部四項",
    "lane-motion M1/M2"
   ],
   "contract": "**縫的稽核跟著縫的擁有者走**:S1~S10 的原文不變式與 --break-* 一律由 lane-ink 寫進上列四支;**lane-world 與 lane-motion 的新斷言一律進自己的新稽核檔**(audit_ambient_motion / audit_water_edge / audit_vehicle_spec / audit_leaf_card / audit_anim_weights / audit_audio_layers)。原規格裡寫著「audit_soft_stroke 新增 Ⅹ/Ⅺ 段」「audit_cel_pipeline 新增 Ⅷ 段」的那幾條,消費端那一半 MUST 改寫到自己的新檔。這樣四支墨線/賽璐璐稽核只有一個寫入者,而每一道仍然驗得到自己那一半"
  },
  {
   "seam": "S12 動畫權重向量(⑥-3 → ⑦-2)",
   "file": "public/js/animweights.js",
   "owner": "lane-motion / M1",
   "consumers": [
    "lane-motion M2 的 audio._updateMoveAudio",
    "日後的 ⑥-2 鳥群與自機 stem"
   ],
   "contract": "零 import;export const WEIGHT_KEYS = ['idle','walk','run','air','land','aim','charge','morph','surge','brake'](**有序**);export function animWeights(L, rig, opts) → 逐鍵物件。三條硬規則:①地面三軌 idle/walk/run **和恆為 1**(誤差 < 1e-9;和不為 1 時 gain-ride 的交叉淡入會在中間速度掉一塊音量)②每一格恆為**有限數**(L 缺欄回 0,不得回 NaN)③鍵集由 WEIGHT_KEYS 推導,**消費端 MUST NOT 手寫鍵表、MUST NOT 出現逐機種/逐角色名冊**。離地門檻由呼叫端注入(opts.groundY),模組內 MUST NOT 寫死度量 —— 現況有三個不同的離地門檻(MORPH.GROUND_Y=2 / SPEC_CAM.FLY_M=2.5 / game.js:8281 的 3)"
  },
  {
   "seam": "S13 文件與規則檔的合流(本輪唯一不由任何道擁有的檔案)",
   "file": "CLAUDE.md / .claude/rules/**.md / docs/anime_style_plan.md / public/js/.claude.md / tools/CLAUDE.md",
   "owner": "整合者(平行窗結束後的序列步驟)",
   "consumers": [
    "全部五道"
   ],
   "contract": "**平行窗期間沒有任何一道可以寫這六類文件**(12 份規格全都指名 verification.md 與 anime_style_plan.md,寫下去必撞)。每一道把自己的文件差異寫進 docs/_pending/<lane>.md,格式固定四段:①seams-*.md 要新增/修訂的**那一列**原文 ②verification.md §5.1 要加的指令與 §5.5 要加的對照列 ③根 CLAUDE.md §2.1 目錄要加的**主題名**(§2.1 的鐵律:目錄裡查不到就會被當成沒有規則)④anime_style_plan.md 執行紀錄那一列。整合者最後序列合併,並在同一次把 notes 列的四處計畫更正一起寫進 anime_style_plan.md"
  }
 ],
 "userDecisions": [
  "【範圍,最先要答】本輪是否照本計畫**延後五項**:seq8(④ wipe/dissolve,只留 ④-3)、seq11 的 ⑥-2 鳥群、seq12(School B)、seq12b(⑨ 立體結構)、seq4 的 ①-3(遮罩折進 surfaceId)。五項各自的理由見 notes;若使用者要求本輪就做,平行度會掉到 2~3 道而且 seq12/12b 的驗收面(定場照)本來就要等真瀏覽器。",
  "【seq3】INK_REPEAT_M 的授權值。提案 SOLDIER_H × 2 = 3.6 m(在 ⑨-3 的三組上給出 拱圈 1 / 欄杆立柱 ≈0.56 / 法枠工格網 ≈0.42,順序與量級都對)。但它**不是從既有量推導出來的**(試過 SOLDIER_H 與 heroTallestH() 兩個現成錨,前者把三組全推到 1、後者全推到近 0)⇒ 它與 MINI.BUFFER_F = 1/3、SELF_ULT.REALIZED_F = 0.35 同級的授權值,MUST 在原地註明「不是量測值,校準面是定裝照」。",
  "【seq3 / seq12b】計畫 §⑨ 的「MUST 原封不動」表與 ⑨-3 授權清單裡有**六個名字在本儲存庫查無**(buildCribs / cribColumn / quadTo / boreProfile / boreClearance / sweptSolid,全庫 grep 零命中),連帶「法枠工格網 / 待避所 / 坑門冠石 / 橋面伸縮縫 / 欄杆立柱」也沒有對應幾何(本專案的欄杆是一條連續緞帶不是立柱)。三條路:①對號入座到既有幾何(額牆頂梁當冠石、欄杆緞帶當立柱,法枠工/待避所/伸縮縫不做)②為它們新增幾何(= 新增世界內容,要回答 §2.3 的 rnd() 帳,而 ⑨ 的前提是只改材質層)③留給後續項目。**建議 ① 並同輪更正計畫檔那一格。**",
  "【seq4】①-3(把地貌遮罩折進 surfaceId,讓草↔岩出線)**與兩條有日期的定案正面相反**:2026-08-13「LUT 與勾線不針對地貌作用,不要看出地貌拼圖接縫」與 2026-08-11「兩側若是相同地貌則不需要分界線」。要問三句:①「草↔岩要出線」是不是**只**指跨地貌(green↔bare)那一種?②同 zone 內的大色跳已經有 borderKindOf 的窄門在畫實體界線拼圖,要不要**再**多一條墨線?③既然 §0-a(序 14/15)會把整個地貌系統換掉、而 ①-3 的載體(逐頂點 aLandId)屆時會整組退場,值不值得先付一輪?**本計畫的預設是不做。**",
  "【seq4】①-4 深度門檻:**建議維持 INK.K_S、不換成 1−n.z**。量測:K_S 版在掠射 2° 給 11.7×、45° 給 1.37×;1−n.z 版的上界恆為 1+K_N ⇒ 配 2° 就把 45° 斜面推到 3.09 倍過度抑制、配 10° 就讓近地平線只剩 0.30 倍 —— **兩條曲線在任何單一係數下都配不起來**。另外三條:1−n.z 只有 inkMrt 開著才拿得到法線(而它預設關、WebGL1 沒有)、哨兵像素沒有法線要第二份門檻(= 第二份實作)、K_S 版的 e 在低解析度會飽和而 1−n.z 版沒有上界(手機降階時地形折邊會變強)。這也是一個定案,請確認。",
  "【seq5】⑤-1 的「玩家」是誰:計畫原文只寫 charPos/charSpeed 兩個 uniform(= 單一角色),本計畫建議 **4 槽**(主視野機體 + 離相機最近的 3 台)—— 第三人稱與觀戰下兩台常同框,只撥開自機腳邊的草很明顯不對。代價是逐頂點 4 次 length()+smoothstep,落在草/稻那幾列(全場頂點數最高的 InstancedMesh)上,而且會有槽位換手的瞬跳。兩種都自洽。",
  "【seq5】落花的季節與內容語意:本專案 VEG_DEFS **沒有櫻花樹種**(闊葉/白樺/五種針葉/灌木/紅樹林…),「落花」沒有對應的來源幾何。建議由既有的 ENV.seasons[].accent 推導(春 = 粉色落花 / 秋 = 橘紅落葉 / 夏冬不下)。若要「四季都有」「只在特定場地」或「真的加一款開花樹種」,那是內容決定。",
  "【seq5 / seq11】?tread=0 / ?petal=0 / ?birds=0 要不要升級成設定頁旋鈕。現行慣例是 URL killswitch(?sag=0/?morph=0/?gait=0/?cockanim=0);落花是唯一會增加逐幀 CPU 成本的一項,與 dof/shadow 同類,做成旋鈕有理由,但會連帶動到 audit_visual_prefs Ⅰ 與設定頁版型。",
  "【rock】兩項不影響本輪落地的美術決定:①hazards.js 的障礙岩 chiselRock(IcosahedronGeometry(r,1) 非索引 ⇒ 逐面硬邊 80 面 + 反轉外殼描邊)要不要一起收進表面群組 —— 它不在使用者點名的三個對象裡,而動它會連帶動到 audit_object_joints 的 42 枚亂數樁;②遠景背景那一圈要「線調淡」還是「整圈不出線」(INK_CTR.BACKDROP = 0)—— 前者是遠山,後者是一張背景板,兩種都自洽。",
  "【rock / seq13】**雪線 / 草↔岩界線本輪 MUST NOT 做**(已按此排),理由要讓使用者知道:現制 icefield/scree/plateau 的換手發生在 ground.js 的逐格投票邊界與 CARPET_LOT 量化格上、不在真實雪線上,而全部地貌共用 LAND_SURF_ID 正是 2026-08-13 為了藏那條接縫定的案 ⇒ 現在給它 id 邊 = 把剛藏起來的拼圖接縫用黑線重新描一次。它是 §0-a 遮罩面的推論,要等序 14/15。",
  "【seq6】三件:①七床的名冊內容與優先序(現提案 tunnel > water > swamp > camp > urban > forest,base 恆亮)—— 宣告順序就是規則本身,重排一次就是換一套聽感;②**自機是否納入移動床**(現制 game.js:8303 明確排除自機;⑦-2 講的「走進水裡會踏空一拍」在語意上就是自己的腳步,納入是玩家第一次聽到自己的機體 = 可聽的行為改變);③七床 + 兩份行動版 BGM 的 **CC0 音檔尚未取得**(本輪未下載任何東西),缺檔時系統照常運作(該床靜默、base 頂著)但 ⑦-1 在真機上聽不出來。建議來源與規格見 seq6 規格的 blockedOn。另回報一條刻意不做的:audio.js 全檔沒有 visibilitychange 處理(切背景分頁 BGM 與常駐床照播),計畫 ⑦ 的四條沒列它,依「刻意設計 MUST NOT 補完」本輪不動。",
  "【seq7】三件:①**葉片卡是否取代已入庫的 AI 冠簇節點**(tree/canopy_a5/b5/c6、bl_crown_a、cf1~4_crown_a、bush_a09、vleaf_a12/a20 是 2026-08-05/08-08 定案「走零件庫」的產物,吃掉整層成長額度的 92.4%)—— 三態旋鈕 off / **auto(建議:只換解析不到庫節點的葉列,intake_parts 的分母與 node_cap 完全不動)** / all;②計畫寫「一支新的葉片 ShaderMaterial」,本規格改走 applyCelPatch 的 define(CEL_LEAFCARD)以**結構性繼承** gInfo 宣告 / 軟性 alpha / 世界曲面三條契約 —— 這是對計畫字面的偏離,請放行或否決;③樹幹的多邊形折邊線要不要一起收掉(現提案:木質列 ink:'hard'、葉列 ink:'group',早退判據是「五格同號**且至少一格是 GROUP**」⇒ 幹的內部折邊留著、幹與冠的交界不出線;要連幹的折邊也收是一行的事,但 110m 神木近距離會讀成一根沒有轉折的實心柱)。",
  "【seq9】三件:①**岸邊泡沫走哪一條**(a 烤好的深度場 = 零額外 pass、能繞地形與所有登記過的 blockers、繞不過純表現層擺件與移動中的機體,成本 1MB VRAM + 建構期 20~40ms【建議】;b 逐幀深度 prepass = 連機體都繞,但多一趟全場 render;c 水面 depthWrite:false + 後製 = 零 pass,但**水岸那條勾線會整條消失**且狙擊景深改對焦到水底);②倒影塊是**亮的還是暗的**(亮 = 天光高光帶 / 暗 = 用 shadowTintRGB 的同一個色相,畫面上是兩種完全不同的東西,正是「需美術方向確認 MUST NOT 由 commit 定案」);③倒影塊要不要帶物件自己的顏色(逐反射體顏色沒有現成來源,blockers 只有幾何 ⇒ 要就得新開一份逐棟代表色的帳;現行預設是全場共用一個色,刻意的降級)。",
  "【seq10】三件:①**§2.5 真實尺度 vs §2.3 確定性**——ground.js 的 carwreck 實得世界長 1.71m、container 2.70m,與其他三份副本差 2.6~2.3×,而 §2.5 明文要求真實公稱尺寸;但改尺寸 ⇒ detailR 變 ⇒ detFree 淘汰結果變 ⇒ **全圖散佈序列整條推移**。本計畫按 10a(只收斂形狀、包絡凍結)落地,10b 留給使用者;②`curveEyeM()` 的**引數順序缺陷**(data.js:1002 寫 heroTargetH(ch, lv) 而簽章是 (kind, ch) ⇒ 每一輪都走 SOLDIER_H*4,實得 4.0824 而正解 0.76545,差 5.33×;audit_world_curve.mjs:89 抄了同一份錯誤呼叫 ⇒ 那道閘從來沒量到東西)—— 修它 = 13 張定場照全變,屬序 12 那一級,**本輪 MUST NOT 碰**,③-3 改用新的 standEyeM();③③-2 的真凹處要不要上一般方盒建物(那是單位 BoxGeometry 的 InstancedMesh,凹處深度會隨每棟樓的進深伸縮,唯一可行是另開一顆貼在臨街面的 InstancedMesh + 底層那一帶改吃素牆帶)。本輪只做載具/公設/邊界牆三處。",
  "【seq11】三件:①**GPGPU vs CPU 零狀態積分器**(計畫明寫 GPGPU,但本專案沒有任何 ping-pong RT、postfx 是唯一 RT 持有者、世界內容的運動傳統一律零狀態純函式;鳥群量級是數十到兩百隻,GPGPU 在 1e4 以上才回本,卻要付兩張浮點 RT + 第二條 render 迴圈 + **反向驗證在 GLSL 積分器上離線做不出來**)—— 建議六項規則(曲線/逐軸噪聲/弱彈簧 0.0003/摩擦/分群/uSnap)一項不刪,只把積分器落在零 THREE 的 JS 模組;②鳥群的「真實理由」順位(提案 水域岸線 > 神木林 > 地標,**排除**兵線/塔位/主堡,三類都錨不到就不放);③⑥-3 的音效行為改變是否接受(離地門檻 3m→2m、gate 曲線換來源,兩項都聽得出來、都沒有離線模型守得住)。",
  "【seq12】四件(整項本計畫建議延後,但這四件遲早要答):①**旋鈕 vs 硬切**(建議 celSchool 旋鈕、預設舊制 ⇒ RAMPS 與 audit_cel_pipeline Ⅰ / audit_visual_prefs Ⅱ 共 27 條原封不動,風險從『高』降到 opt-in;代價是計畫 §0-b 那句「把 shadowCut 換回 ramp 查表,ramp 斷言 MUST 紅字」在旋鈕制下講不通,反向驗證要改成新 Ⅷ 段的 --break-school);②School B 的預設色相量 HUE_MIN_A(兩根偏色拉桿 def = 0 照搬過去就是**灰色陰影**,而色相位移是這一換的全部收益);③bands = 4 的中間那一階要不要補回來(硬切只有一個終端 ⇒ 整片山坡回到兩塊色);④A14 的改寫措辭(A 表被 siteplan.js/prompt.mjs/sim.js/各稽核交叉引用,編號 MUST NOT 動)。",
  "【seq13】兩件:①**surfaceId 從常數換成 f(zone)**(驗收面 3 要的「草↔岩、乾↔濕出線」必須這樣做,而那是推翻 2026-08-13「不要看出地貌拼圖接縫」定案的**形狀**;樁只出算術報告、不動 shader);②'!'(懸崖不鋪)與 null(留白露衛星底圖)在遮罩制下怎麼表達(計畫的次款預算表只有六個分區,沒有這兩格;樁會印面積佔比)。",
  "【mobile-rest】⑧-4 桌機 DPR 三選一,**MUST NOT 自行決定**:(甲)維持天花板 2(HiDPI 滿檔畫質,dpr=2 每幀 8.29Mpx + 221MB RT);(乙)照參考專案 dpr≤2 ? min(dpr,1.15) : min(dpr,1.5)(dpr=2 省 66.9% 像素與 148MB,但**失去桌機唯一的空間抗鋸齒來源**(MSAA 在 post 開啟時是關的)⇒ 1px 墨線變粗變抖、FPV 遠距辨識變差,而 **dpr=1 的一般桌機完全不受影響**、省不到那一群人);(丙)天花板不動、把 RES_GOV.MIN 由 0.7 降到 0.575(撐得住的機器**逐位元同現況**,只有已降階的才走更低;但 dpr=1 的地板會掉到 0.575)。⚠ 關鍵事實:參考專案的 1.15 是 setPixelRatio(1) 之上的 RT 縮放,本專案的 2 是**裝置像素比的天花板** —— 兩個不同的量,把裁決簡化成「2 太高改 1.15」會讓方向講反。另 RES_GOV.MIN × 2 ⇒ 桌機有效像素比的**地板是 1.40**,1.15 在今天的自適應範圍**之外**。",
  "【mobile-rest】⑧-5 的一個可選項:viewport 是否加 interactive-widget=resizes-content(大廳 5 個文字輸入 + .screen 是 position:fixed,Android 軟鍵盤預設 resizes-visual 可能蓋住輸入框)。這是新增行為不是缺陷修補。另一半(text-size-adjust + 觸控硬化改綁裝置 class)我判定是**缺陷**(硬化綁在房主可關的房間設定上),已排進 lane-mobile,但它會改變觸控裝置上的行為,仍請有機會否決。",
  "【流程】文件合流方式:平行窗期間**沒有任何一道寫 CLAUDE.md / .claude/rules/** / docs/anime_style_plan.md / public/js/.claude.md**,各道寫 docs/_pending/<lane>.md,由整合者最後序列合併(見 S13)。若使用者希望改成「指定某一道獨佔文件、其他道口頭交付」,也可以,但那一道會變成所有人的收尾瓶頸。"
 ],
 "notes": "【切法的核心決定】硬約束 2 問的是 toon.js/postfx.js 要不要集中給一個道。答案是**要,而且要比字面更進一步**:lane-ink 不只做序 3/4,它在 I3 一次把 rock / seq5 / seq7 / seq9 / seq10 想要的 toon/postfx 側縫**全部落完並凍結契約**,呼叫端一個都不接、預設值一律中性。gate 之後 lane-world 與 lane-motion 只寫消費端(biomes/ground/terrain/main/game/locomotion),與 lane-ink 的後半段(seq4、SELF_F 掃參數)真正並行。代價是把 5 個功能各自切成「縫」與「消費端」兩半交給兩個 agent —— 這在別的專案是壞主意,在這個專案剛好相反:單一真相縫本來就要求縫與消費端分家,而且 S1~S12 的契約(函式簽章 / uniform 名 / attribute 名 / define 名)都寫死了,合不起來會在稽核上當場紅而不是靜默。\n\n【為什麼是 5 道而不是更多】檔案爭用實測:toon.js 10 份、biomes.js 8 份、game.js 4 份、visualPrefs.js 5 份、audit_soft_stroke 5 份、audit_cel_pipeline 5 份。把 toon/postfx/visualPrefs/data + 四支墨線稽核收成 lane-ink,biomes/ground/terrain/main/siteplan/hazards/edgewall/beacons 收成 lane-world,game/locomotion/audio 收成 lane-motion 之後,剩下的 seq13(純 tools)與 mobile 觸控(css + mobile.js + audit_ctrl_mode)天生零交集 ⇒ 5 道就是這份檔案圖的自然分割,再切下去一定會踩到同一支檔案。\n\n【依賴與 gate 的滿足】硬約束 3:序 3 是 lane-ink 的第二項(I1 之後立刻),序 4 / 序 7 / rock / 序 12b 全部排在它之後或延後 ✅;⑥-3 是 lane-motion 的第一項、⑦ 是第二項 ✅。硬約束 4:序 14/15 在 lane-zonecut 的樁通過前完全不做(本輪連提都沒提)✅;序 12 的定場照基準在 lane-ink 的 P0(全域第一步,先於任何一行程式)✅。\n\n【本輪不該做的項目與理由】(1)**seq8 ④-1/④-2/④-4**:④-1 的旋鈕 def 與 ④-3 的地板兩件都卡使用者裁決、④-2 的「消失」那一半範圍未定(要動 this.ents 的 20+ 個消費端,延後移除會讓客戶端解到幽靈目標)、④-4 是純註解卻要動六支稽核檔頭(含 audit_traverse ⇒ 與 lane-zonecut 撞、含 audit_ground_drape/road_joint/layer_block/underpass ⇒ 與 lane-world 撞),而它整項跨 lane-ink + lane-motion 兩道。只把 ④-3 抽出來放進 lane-ink 的 I6(它只動 postfx + audit_cel_pipeline,完全落在一道之內)。(2)**seq11 的 ⑥-2 鳥群**:GPGPU vs CPU 是未決的裁決題,而且它要寫 biomes.js(lane-world 的地盤)⇒ 延後之後 lane-motion 完全不碰 biomes.js,這正是三道能並行的關鍵之一。(3)**seq12 School B**:高風險、四件裁決未決、驗收面是圖不是斷言、要寫 biomes.js 與 environment.js;而且它與序 12b 若同輪落地,洞內出問題時分不出是換學派還是 ⑨-5 沒補夠。(4)**seq12b**:依賴序 3 且要寫 biomes.js,又要先答「六個不存在的函式名」那一題。(5)**seq4 的 ①-3**:與兩條有日期的定案正面相反,且載體在 §0-a 之後會整組退場。(6)**seq10 的 10b(真實尺寸)**:§2.5 與 §2.3 正面衝突,MUST 先裁決 + 先重拍全部定場照 + 單獨 commit。\n\n【與計畫牴觸、MUST 一併更正計畫檔的四處】使用者本輪明講「衝突時一律以計畫為主」,以下四處不是我要改規則,是**計畫對現況的描述已經過期或查無**,MUST 由整合者寫回 docs/anime_style_plan.md:①§⑨ 的「MUST 原封不動」表列的 buildCribs / cribColumn / quadTo / boreProfile / boreClearance / sweptSolid **全庫 grep 零命中**(那是 sakura 的詞彙),連帶法枠工/待避所/伸縮縫/立柱都沒有幾何 ⇒ ⑨-3 目前沒有東西可掛;②⑤ 的「本專案的水面現在只有波形,沒有 shore band」**過期**:biomes.js:5093 buildWaterEdges 早有一條,只是它正是計畫要否定的那一種(8m 格點 + 徑向漸層軟 alpha + 固定在 waterY+0.1 的平板,而浪高 ±0.9m 會穿過去)⇒ ⑤-2 是**替換不是純新增**,執行順序表那一格的「是(純新增)」對 ⑤-2 是錯的;③⑤-5 雲層飄移**已經完成**(environment.js:222 先加半個 WRAP、:505 吃 celWindTime(),audit_soft_stroke Ⅴ 已有四條在守)⇒ 零工作;④§0-a 的「最大可行範圍」表(計畫寫 93×93 / 2111m,實測 zoneGrid 是 175×146、paris 3v3 含裙跨距 3362m ⇒ 1024² = 3.28 m/texel 不是 2.06),由 lane-zonecut 的 Z0 更正。另 ⑧-3 已隨序 2 落地、⑧-1/⑧-2 已於 2026-08-16 落地,⑧ 只剩 ⑧-4(裁決題)與 ⑧-5(lane-mobile)。\n\n【最高的三個風險,依序】(1)**audit_cel_pipeline 的 gInfo 閘對子目錄失效**(readdirSync 不遞迴)。本輪要新增 5 支客戶端模組(petals / leafcard / vehicles / animweights / wildlife),漏宣告 gInfo 的代價是 WebGL2 INVALID_OPERATION ⇒ 整批物件不畫、console 一個字都沒有,而稽核全綠。所以 I1 是**全域第一項程式碼改動**,而且它的反向驗證是「注入一支不帶宣告的 ShaderMaterial 到 forge/ 底下 ⇒ Ⅵ MUST 紅」——今天注入後仍是綠的,那就是證明。(2)**共享 rnd() 序列推移**。lane-world 的每一項都碰得到,而症狀是「整張圖變了」、沒有錯誤訊息、audit_siteplan/beacons/object_joints 照樣全綠(它們驗規則不驗位置)⇒ 判準 MUST 是「**逐項不變**」而不是「仍全綠」,所以 lane-world 的第一件事是把基準輸出落盤。ground.js 的 detailR('carwreck')/detailR('container') 是唯一的顯式哨兵。(3)**純表現層漏進權威側**。本輪 sim.js 與 server/** 一行都不該動;data.js 只准加不進 balanceFingerprint 的表現層常數(INK_CTR / DISSOLVE 家族,前例是 CURVE/DOF)⇒ npm run bal 與 npm test MUST 逐項不動,而 lane-world 另有八支幾何稽核(layer_block / open_tunnel / underpass / road_joint / road_bed / bridge_* / water_skirt / traverse)當第二道判決面。\n\n【每一道的收尾都要做的三件】①自己那一批 --break-* 逐支真的跑一次,字面替換一律 CRLF 容忍(\\\\r?\\\\n)、替換無效 MUST 當場 process.exit(1)、斷言的期望值 MUST NOT 隨 --break-* 改變(§5.4 ㋑ —— 2026-08-14 --break-roof 綁死現值變成靜默 no-op、紅字由 2 條掉成 1 條而壞版根本沒被造出來);②audit_client_syntax(㋖)+ npm run audit:net + audit_solo_boot(新模組一定要過這三道:名冊由目錄推導、URL 佈局鏡射、data.js 單一模組實例);③把 ㋓/㋕ 未驗項逐條列進交付說明,**MUST NOT 把沙箱跳過當綠燈**。特別注意 audit_lane_grade_sep / audit_lane_scenarios 在無快取時印「跳過」、audit_traverse 取不到路網會降級標未驗 —— 那些都不是綠燈。\n\n【跑全套稽核的副作用】npm run audit:net 的 ⑦ 段會**真的 spawn 每一支 dev 工具**,含 tools/ai3d/harvest_loop.mjs(永不結束、會連外網下載)與一個 --lan --https 隨機埠伺服器。跑完 MUST 回頭 Get-Process node 清孤兒,否則下一次 §5.2 的埠檢查會讀到誤導的結果。另本機此刻有第三方 node 行程(不是我們起的 server --port 8647),而 8620 上 LISTENING 的是 tailscaled(只綁 tailnet IP)⇒ npm test MUST 自己指定埠並用 WS_URL 指過去,㋓ 的 audit_muzzle/cockpit/cast_jump MUST 用 SVS_URL 指向本工作區的埠(本輪已用 --port 8677 驗過三支全綠 32/32)。\n\n【基準已落盤,五道直接引用】C:\\\\Users\\\\user\\\\AppData\\\\Local\\\\Temp\\\\claude\\\\C--Users-user-Documents-app-steel-vs-swarm\\\\c1a94433-3e6a-489c-b725-7c742b5042a5\\\\scratchpad\\\\BASELINE.txt(79 支離線稽核 72 綠)、bal.txt(🎉 全綠)、e2e.txt(🎉 624 ✅)。7 支非綠全部是既有紅或環境不足,已逐支歸因(audit_touch_layout 8/60 的 667×375 橫式重疊、audit_traverse 91/18、audit_lane_grade_sep/lane_scenarios 吃外網、三支瀏覽器稽核只是連錯埠)⇒ **本輪落地後這 7 支的紅字內容 MUST 逐字相同,多一條就是新壞的。**"
}