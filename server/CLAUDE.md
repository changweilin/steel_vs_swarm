# server/ — Authority Layer (module pointer)

> Tier-1 SSOT is root `AGENTS.md`. Topic definitions live in `.claude/rules/`
> (`seams-balance`, `seams-weapons`, `seams-bots`, `seams-ui-net`); per-assertion
> formulas live in `tools/audit_*.mjs` headers. Module roles live in `server/AGENTS.md`.
> Commands live in `package.json`; change-to-guard mapping lives in `verification.md`.
